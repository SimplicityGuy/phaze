"""Jinja-render tests for _partials/cross_fs_fingerprint_notice.html (Phase 28 D-14).

Wave 0 stub — the cross-FS-fingerprint dismissible banner partial lands in
Plan 28-06. This stub anchors the file path so Nyquist sampling can resolve
the corresponding 28-V-NN test entry.
"""

from __future__ import annotations

import pytest


pytest.skip("Wave 0 stub — implementation lands in Plan 28-06", allow_module_level=True)
