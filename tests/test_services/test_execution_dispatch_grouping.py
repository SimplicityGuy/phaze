"""Unit tests for src/phaze/services/execution_dispatch.py grouping/chunking (Phase 28 D-09).

Wave 0 stub — the SELECT-and-group helper + revoked-agent filter + chunk-into-N
helpers land in Plan 28-03. This stub anchors the file path so Nyquist sampling
can resolve test IDs 28-V-06..28-V-08.
"""

from __future__ import annotations

import pytest


pytest.skip("Wave 0 stub — implementation lands in Plan 28-03", allow_module_level=True)
