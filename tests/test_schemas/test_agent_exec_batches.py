"""Unit tests for src/phaze/schemas/agent_exec_batches.py (Phase 28 D-06).

Wave 0 stub — the ExecBatchProgressPayload schema (`extra="forbid"` + cross-field
`model_validator(mode="after")` coupling `failed_at_step` to `terminal_step ==
"failed"`) lands in Plan 28-02. This stub anchors the file path so Nyquist
sampling can resolve test IDs 28-V-10..28-V-13.
"""

from __future__ import annotations

import pytest


pytest.skip("Wave 0 stub — implementation lands in Plan 28-02", allow_module_level=True)
