"""Integration tests for POST /execution/start dispatch rewrite (Phase 28 D-09, D-11).

Wave 0 stub — the controller dispatch rewrite (per-agent grouping + sub-batch
chunking + Redis hash initialization + SSE event extension) lands in Plan 28-04.
This stub anchors the file path so Nyquist sampling can resolve test IDs
28-V-18..28-V-21.
"""

from __future__ import annotations

import pytest


pytest.skip("Wave 0 stub — implementation lands in Plan 28-04", allow_module_level=True)
