"""Contract tests for POST /api/internal/agent/exec-batches/{batch_id}/progress (Phase 28 D-05, D-17).

Wave 0 stub — the router and schema land in Plan 28-02 along with the full set of
auth / cross-tenant / idempotency / counter-math test cases enumerated in
28-VALIDATION.md (28-V-10..28-V-17). Plan 28-01 only creates the file so Nyquist
sampling and `pytest -k` lookups resolve without ModuleNotFoundError.
"""

from __future__ import annotations

import pytest


pytest.skip("Wave 0 stub — implementation lands in Plan 28-02", allow_module_level=True)
