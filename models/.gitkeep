# phaze-he8m: keep ./models/ in the repo so a git clone materializes it owned by
# the cloning operator (uid 1000), NOT auto-created root:root by rootful dockerd.
# The uid-1000 agent worker auto-downloads model weights into ./models:/models:rw
# on first boot; a root-owned dir fails with EACCES.
