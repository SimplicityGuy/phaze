---
phase: 92-milestone-close-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/services/pipeline.py
  - scripts/perf_explain.py
  - .planning/phases/92-milestone-close-tech-debt-cleanup/92-VERIFICATION.md
autonomous: true
requirements: [CLEAN-01]
must_haves:
  truths:
    - "get_stage_progress issues its independent reads concurrently via asyncio.gather, each read in its own AsyncSession"
    - "The fan-out is bounded by asyncio.Semaphore(4); a pool-timeout on session acquisition degrades to the safe default, never aborting the poll"
    - "The returned dict shape, key order, and derived done buckets are byte-identical on a quiescent DB"
    - "Each read keeps its _safe_count / _safe_bucket_counts degrade wrapper (D-04)"
    - "The fan-out sessionmaker (phaze.database.async_session) and _STATS_FANOUT semaphore stay patchable seams so 92-03 Task 2 can route the fan-out through the per-test connection"
    - "Before/after /pipeline/stats poll latency at 200K scale is recorded in 92-VERIFICATION.md (D-05)"
  artifacts:
    - path: "src/phaze/services/pipeline.py"
      provides: "Parallelized get_stage_progress with bounded per-task-session fan-out"
      contains: "asyncio.gather"
    - path: ".planning/phases/92-milestone-close-tech-debt-cleanup/92-VERIFICATION.md"
      provides: "Recorded before/after p50/p95 for get_stage_progress() DIRECT and GET /pipeline/stats at 200K, with the <1s verdict and DENORM-01 disposition"
      contains: "before"
  key_links:
    - from: "src/phaze/services/pipeline.py get_stage_progress"
      to: "phaze.database.async_session"
      via: "per-task session acquisition"
      pattern: "async_session\\(\\)"
---

<objective>
CLEAN-01 / PERF-02 follow-up: parallelize the independent reads in `get_stage_progress` via `asyncio.gather`, each read in its own `AsyncSession` from the app sessionmaker, bounded by `asyncio.Semaphore(4)`, and re-measure `/pipeline/stats` poll latency at 200K scale. The measured result decides DENORM-01's fate (D-05).

Purpose: Collapse the ~1290 ms serial poll path under the ~1 s budget without inflating the deliberately-lean connection pool.
Output: Parallelized `get_stage_progress`; before/after latency numbers in `92-VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-milestone-close-tech-debt-cleanup/92-CONTEXT.md
@.planning/phases/92-milestone-close-tech-debt-cleanup/92-RESEARCH.md
@.planning/phases/92-milestone-close-tech-debt-cleanup/92-PATTERNS.md

<interfaces>
<!-- Extracted anchors — executor uses these directly, no exploration needed. -->
From src/phaze/services/pipeline.py:
- `async def get_stage_progress(session: AsyncSession) -> dict[str, dict[str, int | None]]` (def at :488, return dict at :558-592; 9-key dict: discovery, metadata, fingerprint, analyze, scan_search, scrape, match, proposals, execute)
- `async def _safe_count(session, stmt, *, node: str) -> int` (:319-336) — try/except → log → guarded rollback → return 0; never raises
- `async def _safe_bucket_counts(session, stage: Stage) -> dict[str, int]` (:339-376) — same degrade discipline; never raises
- existing DEFERRED import convention at `:827`: `from phaze.database import async_session  # noqa: PLC0415 -- deferred: keeps the agent-worker import boundary intact` — reuse this seam for the fan-out

From src/phaze/database.py:
- `async_session = async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)` (:45) — the module-level app sessionmaker, bound to settings.database_url at import
- engine pool: `pool_size=settings.db_pool_size` (5), `max_overflow=settings.db_max_overflow` (5), `pool_timeout=settings.db_pool_timeout` (10s) → hard ceiling 10 conns/worker (:35-43; load-bearing incident comment :24-34)

From src/phaze/tasks/discogs.py:59-66 (the ONLY existing Semaphore+gather analog):
- `semaphore = asyncio.Semaphore(...)`; `async def _match_one(...): async with semaphore: return await ...`; `await asyncio.gather(*[_match_one(t) for t in eligible])`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture BEFORE baseline at 200K + confirm perf-DB routing (Wave-0 gate for D-05)</name>
  <files>scripts/perf_explain.py, .planning/phases/92-milestone-close-tech-debt-cleanup/92-VERIFICATION.md</files>
  <read_first>
    - scripts/perf_explain.py (whole file — the `time_stage_progress()` and `time_endpoint()` instruments)
    - scripts/seed_perf_corpus.py (the `--reseed` perf-DB-name guard + N param)
    - justfile (the `perf-db-up`, `perf-seed`, `perf-explain` targets)
    - .planning/phases/92-milestone-close-tech-debt-cleanup/92-RESEARCH.md (§ "D-05 Measurement Harness", Open Question 1, Pitfall 5 port footgun)
  </read_first>
  <action>
    RESEARCH Open Question 1 (BLOCKING before AFTER numbers are meaningful): after Task 2 lands, `get_stage_progress` opens its own sessions from the module-level `phaze.database.async_session`, which binds to `settings.database_url` at import — bypassing the bench's `dependency_overrides[get_session]`. FIRST confirm the bench routes the internal `async_session()` fan-out to the perf DB. Run `just perf-db-up` then a small-N smoke (`PHAZE_DATABASE_URL=<perf dsn> just perf-seed N=1000` + `PHAZE_DATABASE_URL=<perf dsn> just perf-explain ITER=3`) and verify the timings are real (non-degrade) reads against the perf DB, not the app default. If exporting `PHAZE_DATABASE_URL` is insufficient because `settings` is imported before the env is read, make the MINIMAL harness tweak in `scripts/perf_explain.py` to construct/point the module-level engine (or the sessionmaker `get_stage_progress` imports) at the perf DSN for the bench run. Document the exact env/command used.

    Then capture the BEFORE (serial) baseline: `PHAZE_DATABASE_URL=<perf dsn> just perf-seed N=200000` (idempotent) and `PHAZE_DATABASE_URL=<perf dsn> just perf-explain ITER=20`. Record `get_stage_progress() DIRECT` and `GET /pipeline/stats` p50/p95 in a new `92-VERIFICATION.md` "PERF-02 Re-measurement (D-05)" section under a "Before (serial)" subheading. Phase-82 baseline to sanity-check reproduction: ~1290.9 ms / ~1405.3 ms p50.

    Mind the 5432/5433 test-DB port footgun (RESEARCH Pitfall 5) — the perf DB is a dedicated `postgres:18-alpine` on its own port via `just perf-db-up`; use its DSN, not the app/test default.
  </action>
  <verify>
    <automated>test -f .planning/phases/92-milestone-close-tech-debt-cleanup/92-VERIFICATION.md && grep -qi "before" .planning/phases/92-milestone-close-tech-debt-cleanup/92-VERIFICATION.md && grep -qi "get_stage_progress" .planning/phases/92-milestone-close-tech-debt-cleanup/92-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The small-N smoke confirms the internal `async_session()` fan-out hits the perf DB (documented command shows `PHAZE_DATABASE_URL=<perf dsn>`); if a harness tweak was needed it is committed in `scripts/perf_explain.py` and scoped to bench-only.
    - `92-VERIFICATION.md` contains a "Before (serial)" subsection with numeric p50/p95 for both `get_stage_progress() DIRECT` and `GET /pipeline/stats` at N=200000, ITER=20.
    - The exact perf-DB DSN/port and `just` commands used are recorded so a reviewer can reproduce (no 5432/5433 ambiguity).
  </acceptance_criteria>
  <done>Perf-DB routing confirmed for the internal fan-out; before/serial 200K p50/p95 recorded in 92-VERIFICATION.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parallelize get_stage_progress with bounded per-task-session fan-out</name>
  <files>src/phaze/services/pipeline.py</files>
  <behavior>
    - Existing test `tests/integration/test_stage_progress_buckets.py` still passes: returned dict keys, order, and derived `done` buckets unchanged on a quiescent DB.
    - Existing degrade test (`tests/analyze/core/test_stage_progress.py`) still passes: a single failing read degrades to its safe default and does not poison siblings or abort the poll.
    - The full `/pipeline/stats` route test (`tests/shared/routers/test_pipeline.py`) does NOT raise "another operation is in progress" / IllegalStateChangeError under the fan-out.
  </behavior>
  <read_first>
    - src/phaze/services/pipeline.py:319-376 (_safe_count, _safe_bucket_counts — REUSE VERBATIM per D-04)
    - src/phaze/services/pipeline.py:488-592 (get_stage_progress body + the 9-key return dict to preserve)
    - src/phaze/services/pipeline.py:820-829 (the existing deferred `from phaze.database import async_session` import-boundary convention — reuse it)
    - src/phaze/database.py:24-51 (engine pool caps, async_session, get_session)
    - src/phaze/tasks/discogs.py:55-68 (the Semaphore + gather + per-item-coroutine analog)
    - .planning/phases/92-milestone-close-tech-debt-cleanup/92-RESEARCH.md (§ "CLEAN-01" recommended shape, Pitfall 1 snapshot-skew, Pitfall 2 acquisition guard, Pool Headroom Analysis)
    - .planning/phases/92-milestone-close-tech-debt-cleanup/92-PATTERNS.md (pipeline.py CLEAN-01 section)
  </read_first>
  <action>
    Rewrite the body of `get_stage_progress` (def :488) to issue every independent read concurrently. Per D-02, parallelize ALL independent reads (the 3 enrich `_safe_bucket_counts` reads for METADATA/FINGERPRINT/ANALYZE AND the other independent `_safe_count` reads: music_video_total, tracklist_total, discovery, convergence_total, scan_search, scrape, match, proposals, execute, execute_total) — not just the 3 named.

    Per D-03: each concurrent read gets its OWN `AsyncSession` from the `async_session` sessionmaker. Resolve it via a DEFERRED `from phaze.database import async_session` (matching the existing agent-worker import-boundary convention at `pipeline.py:827`) so the single patchable seam is the SOURCE `phaze.database.async_session` — a single AsyncSession/asyncpg connection cannot run concurrent queries. Bound the fan-out with a module-level `_STATS_FANOUT = asyncio.Semaphore(4)` (per RESEARCH Pool Headroom — full ~13-way fan-out is UNSAFE against the 10-conn/worker pool). Introduce a helper coroutine (e.g. `_read_in_own_session(fn)`) that does `async with _STATS_FANOUT:` then `async with async_session() as s:` then `return await fn(s)`.

    SEAM NOTE (required for 92-03 Task 2): keep `async_session` resolvable at call time from `phaze.database` (deferred import, re-read each call — do NOT bind it into a closure default or an early module-local alias), and keep `_STATS_FANOUT` a module-level attribute of `phaze.services.pipeline`. Plan 92-03 Task 2 monkeypatches `phaze.database.async_session` (to route the fan-out through the per-test connection) and `phaze.services.pipeline._STATS_FANOUT` (to `Semaphore(1)` for serialization under a shared test connection). If these are not patchable, seed-then-read tests (`test_stage_progress.py`, `test_pipeline.py`) break at the wave-1→wave-2 boundary.

    Per D-04: keep `_safe_count` / `_safe_bucket_counts` VERBATIM as the per-read wrappers (they still try/except → log → rollback-their-own-session → safe default; never raise). Call each inside its own session via the helper.

    Per RESEARCH Pitfall 2 (do NOT skip): wrap the session ACQUISITION itself in the degrade discipline — `async with async_session()` can raise `TimeoutError` after `pool_timeout=10s` OUTSIDE the wrapper, which would abort the whole gather. Either try/except the acquisition and return the node's safe default, OR use `asyncio.gather(..., return_exceptions=True)` and post-map exceptions to defaults. Prefer the former to keep the "never raises into the 5s poll" contract end-to-end.

    Assemble the SAME 9-key return dict in the SAME key order (:558-592) from the gathered values — keys, order, and derived `done` buckets byte-identical on a quiescent DB. Do NOT introduce the `merge_frozen_result` ORM-merge pattern (RESEARCH anti-pattern — reads return scalars/dicts, no merge needed).

    Keep the incoming `session` parameter for signature stability (Open Question 2 recommendation — least blast radius); document it as unused-by-design (the reads now run in their own sessions). This is behavior-preserving except latency.

    DO NOT change `_safe_count`/`_safe_bucket_counts`, the SQL statements, or any other function in the module.
  </action>
  <verify>
    <automated>uv run pytest tests/analyze/core/test_stage_progress.py tests/shared/routers/test_pipeline.py -q && uv run mypy src/phaze/services/pipeline.py && grep -q "asyncio.gather" src/phaze/services/pipeline.py && grep -q "Semaphore(4)" src/phaze/services/pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/phaze/services/pipeline.py` contains `asyncio.gather` and a module-level `_STATS_FANOUT = asyncio.Semaphore(4)` used inside a per-read helper that opens `async_session()` per task via a deferred `from phaze.database import async_session`.
    - `_safe_count` and `_safe_bucket_counts` bodies are unchanged (git diff shows no edits to :319-376).
    - The return dict at the end of `get_stage_progress` has the same 9 keys in the same order as before.
    - Session acquisition is wrapped so a `pool_timeout` TimeoutError degrades to the node's safe default (grep shows acquisition inside a try/except OR `return_exceptions=True` with a mapping step).
    - `uv run pytest tests/analyze/core/test_stage_progress.py tests/shared/routers/test_pipeline.py -q` exits 0 (no "another operation is in progress"). NOTE: these pass here under the OLD conftest (real commits, wave 1); the create_savepoint rewrite (92-03) keeps them green only via the Task-2 fan-out routing — the seam note above is what makes that routing possible.
    - `uv run pytest tests/integration/test_stage_progress_buckets.py -q` exits 0 (dict shape/values byte-identical on quiescent DB).
    - `uv run mypy src/phaze/services/pipeline.py` exits 0.
  </acceptance_criteria>
  <done>get_stage_progress runs its reads concurrently in bounded per-task sessions with acquisition-degrade; dict shape preserved; the fan-out seam (async_session + _STATS_FANOUT) is patchable; targeted + integration tests green; mypy clean.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Capture AFTER numbers at 200K and record DENORM-01 verdict (D-05)</name>
  <files>.planning/phases/92-milestone-close-tech-debt-cleanup/92-VERIFICATION.md</files>
  <read_first>
    - scripts/perf_explain.py (the same instruments used in Task 1)
    - .planning/phases/92-milestone-close-tech-debt-cleanup/92-VERIFICATION.md (the "Before (serial)" section from Task 1)
    - .planning/phases/92-milestone-close-tech-debt-cleanup/92-RESEARCH.md (§ "D-05 Measurement Harness" step 4-5, Pitfall 1 snapshot-skew wording)
  </read_first>
  <action>
    On the parallelized code (Task 2), re-run `PHAZE_DATABASE_URL=<perf dsn> just perf-explain ITER=20` against the same N=200000 perf DB. Record `get_stage_progress() DIRECT` and `GET /pipeline/stats` p50/p95 under an "After (parallelized)" subheading in `92-VERIFICATION.md`, alongside the Task-1 "Before" numbers.

    Add the verdict: state whether the AFTER `/pipeline/stats` p50 is under the ~1 s budget (SC1 acceptance bar). Then record the DENORM-01 disposition explicitly: if still over budget → DENORM-01 becomes a live v2 candidate; if under → DENORM-01 stays killed/deferred. Do NOT inflate `db_pool_size` as an escalation (RESEARCH — PgBouncer session-mode pinning risk); the correct escalation if still slow is DENORM-01.

    Add the byte-identity caveat verbatim in intent (RESEARCH Pitfall 1): "the returned dict is byte-identical on a quiescent DB; under concurrent writes independent sessions may reflect MVCC snapshots microseconds apart — acceptable for a 5s poll." Do NOT claim strict byte-identity under live writes.

    This is the D-05 acceptance gate — a lightweight overlap-only proof does NOT satisfy SC1; the 200K endpoint + direct numbers are required.
  </action>
  <verify>
    <automated>grep -qi "after" .planning/phases/92-milestone-close-tech-debt-cleanup/92-VERIFICATION.md && grep -qi "DENORM-01" .planning/phases/92-milestone-close-tech-debt-cleanup/92-VERIFICATION.md && grep -qiE "quiescent|snapshot" .planning/phases/92-milestone-close-tech-debt-cleanup/92-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `92-VERIFICATION.md` contains both "Before (serial)" and "After (parallelized)" numeric p50/p95 for `get_stage_progress() DIRECT` and `GET /pipeline/stats` at N=200000, ITER=20.
    - An explicit `< ~1 s` verdict is recorded for the AFTER endpoint p50.
    - The DENORM-01 disposition (live v2 candidate vs stays killed) is stated and tied to the measured number.
    - The quiescent-DB byte-identity caveat is present; no claim of strict identity under live writes.
  </acceptance_criteria>
  <done>Before/after 200K numbers + <1s verdict + DENORM-01 disposition + snapshot-skew caveat recorded in 92-VERIFICATION.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| get_stage_progress fan-out → asyncpg pool | N extra concurrent session checkouts every 5s poll contend for the deliberately-lean per-worker pool (post-PgBouncer-incident cap: pool_size=5/max_overflow=5=10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-02-DoS | Denial of Service | get_stage_progress concurrent session fan-out vs lean pool | mitigate | Bound fan-out with `asyncio.Semaphore(4)` (≤4 extra checkouts/poll, ≥6 slots free for request traffic); wrap session acquisition so a `pool_timeout` degrades to safe default rather than exhausting/500ing the poll; D-05 measurement validates the cap at 200K |
| T-92-02-SKEW | Information Disclosure (stale read) | independent-session MVCC snapshots | accept | Sub-second cross-node snapshot skew under live writes is invisible on a 5s dashboard poll; documented in 92-VERIFICATION.md, not claimed as strict identity |
| T-92-02-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in Phase 92 (RESEARCH §Standard Stack) — no supply-chain surface |
</threat_model>

<verification>
- Targeted + integration + route tests green; mypy clean.
- Both before/after 200K numbers recorded in 92-VERIFICATION.md with the DENORM-01 verdict.
- Semaphore(4) + acquisition-degrade guard present; fan-out seam (async_session + _STATS_FANOUT) patchable for 92-03.
</verification>

<success_criteria>
- The independent reads in `get_stage_progress` run concurrently via `asyncio.gather` over per-task sessions, bounded by Semaphore(4).
- `/pipeline/stats` poll latency measurably reduced at 200K scale, both numbers recorded (D-05 / SC1).
- Dict shape + derived done buckets byte-identical on a quiescent DB; degrade discipline preserved (D-04).
</success_criteria>

<output>
Create `.planning/phases/92-milestone-close-tech-debt-cleanup/92-02-SUMMARY.md` when done.
</output>
