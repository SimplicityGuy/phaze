---
phase: 92-milestone-close-tech-debt-cleanup
plan: 04
type: execute
wave: 3
depends_on: [92-03]
files_modified:
  - tests/analyze/tasks/test_reconcile_cloud_jobs.py
  - tests/analyze/tasks/test_submit_cloud_job.py
  - tests/analyze/tasks/test_recovery.py
  - tests/analyze/tasks/test_release_awaiting_cloud.py
  - tests/analyze/test_force_skip_writer.py
  - tests/analyze/test_retry_affordances.py
  - tests/analyze/core/test_dispatch_snapshot.py
  - tests/analyze/core/test_staging_cron.py
  - tests/analyze/core/test_reenqueue.py
  - tests/review/routers/test_duplicates.py
  - tests/agents/routers/test_agent_push.py
  - tests/agents/routers/test_agent_s3.py
  - tests/discovery/tasks/test_scan_reaper.py
autonomous: true
requirements: [CLEAN-02]
must_haves:
  truths:
    - "Every independent-verify-session call site under analyze/review/agents/discovery reads through the shared verify fixture bound to the per-test connection"
    - "No test constructs its own async_sessionmaker(async_engine) independent read session outside the shared connection"
    - "The migrated buckets pass under per-bucket isolation"
  artifacts:
    - path: "tests/review/routers/test_duplicates.py"
      provides: "verify reads via the shared connection-bound fixture (canonical 5-site migration)"
  key_links:
    - from: "migrated verify reads"
      to: "tests/conftest.py verify fixture (_db_connection-bound)"
      via: "shared per-test connection"
      pattern: "verify"
---

<objective>
CLEAN-02 migration: rebind the 23 independent-verify-session call sites (across 14 non-integration test files) from `async_sessionmaker(async_engine)` — which under create_savepoint would read on a DIFFERENT pool connection and NOT see in-test rows — to the shared `verify` fixture bound to the per-test connection (from plan 92-03). This is the load-bearing coupling flagged in 92-PATTERNS.md.

Purpose: Make the create_savepoint isolation compatible with the commit-then-independent-read pattern, so the migrated buckets go green again.
Output: 14 migrated test files.

DEPENDS ON 92-03 (the `verify` fixture must exist). Intermediate red in these buckets before this plan is EXPECTED; the full-suite gate is plan 92-05 (D-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-milestone-close-tech-debt-cleanup/92-CONTEXT.md
@.planning/phases/92-milestone-close-tech-debt-cleanup/92-PATTERNS.md
@.planning/phases/92-milestone-close-tech-debt-cleanup/92-03-SUMMARY.md

<interfaces>
<!-- The pattern being migrated (92-PATTERNS.md 🔴 LANDMINE, canonical at test_duplicates.py:314-317): -->
BEFORE: `verify_factory = async_sessionmaker(async_engine, class_=AsyncSession, expire_on_commit=False)` then `async with verify_factory() as verify: ...`
AFTER: obtain the read session/factory from the shared `verify` fixture (plan 92-03, bound to `_db_connection`) so the read shares the one outer-transaction connection and sees in-test commits.

Per-file site counts (from 92-PATTERNS.md landmine enumeration):
- analyze: test_reconcile_cloud_jobs.py (3), test_force_skip_writer.py (2), test_submit_cloud_job.py (1), test_recovery.py (1), test_release_awaiting_cloud.py (1), test_retry_affordances.py (1), test_dispatch_snapshot.py (1), test_staging_cron.py (1), test_reenqueue.py (1)
- review: test_duplicates.py (5)
- agents: test_agent_push.py (2), test_agent_s3.py (1)
- discovery: test_scan_reaper.py (1)

EXCLUDE: tests/integration/** (e.g. test_drain_double_dispatch.py) builds its OWN engine and does NOT consume async_engine/session — leave untouched. GREP-VERIFY before editing (RESEARCH A4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate analyze-bucket verify sites (9 files) to the shared verify fixture</name>
  <files>tests/analyze/tasks/test_reconcile_cloud_jobs.py, tests/analyze/tasks/test_submit_cloud_job.py, tests/analyze/tasks/test_recovery.py, tests/analyze/tasks/test_release_awaiting_cloud.py, tests/analyze/test_force_skip_writer.py, tests/analyze/test_retry_affordances.py, tests/analyze/core/test_dispatch_snapshot.py, tests/analyze/core/test_staging_cron.py, tests/analyze/core/test_reenqueue.py</files>
  <read_first>
    - tests/conftest.py (the new `verify` fixture from 92-03 — its name, shape, and docstring)
    - .planning/phases/92-milestone-close-tech-debt-cleanup/92-PATTERNS.md (🔴 LANDMINE box — the exact BEFORE pattern + full file list)
    - tests/review/routers/test_duplicates.py:298-347 (canonical BEFORE example, for the mechanical transform)
    - grep the actual sites first: `grep -rn "async_sessionmaker(async_engine\|verify_factory" tests/analyze`
  </read_first>
  <action>
    For each of the 9 analyze-bucket files, find every independent-verify read that constructs `async_sessionmaker(async_engine)` (or a local `verify_factory`) to prove a router/task COMMITTED, and rebind it to the shared `verify` fixture from plan 92-03 (bound to `_db_connection`). Minimize per-file diff: add the `verify` (or `verify_factory`) fixture parameter to the affected test function signature and replace the local factory construction with the injected one; keep the assertion bodies unchanged. The read must now share the per-test outer-transaction connection so it sees in-test commits under create_savepoint.

    Do NOT change what is asserted (still "the writer COMMITTED row X") — only WHERE the read session comes from. Do NOT touch any `tests/integration/**` file. If a grep surfaces a site not in the enumerated list, migrate it too and note it in the SUMMARY (the count was 23 across 14 files; reconcile any drift).
  </action>
  <verify>
    <automated>! grep -rn "async_sessionmaker(async_engine" tests/analyze && just test-bucket analyze</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "async_sessionmaker(async_engine" tests/analyze` returns no matches (all rebinding done).
    - Every migrated read obtains its session from the shared `verify` fixture (grep shows the fixture param in the affected test signatures).
    - Assertion bodies are unchanged (git diff shows only session-acquisition lines + signature params changed).
    - `just test-bucket analyze` is green in isolation (mind the 5432/5433 port footgun — export the correct DB URL).
  </acceptance_criteria>
  <done>All 9 analyze-bucket verify sites read through the shared connection-bound fixture; analyze bucket green in isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate review/agents/discovery verify sites (5 files) + grep-verify integration exclusion</name>
  <files>tests/review/routers/test_duplicates.py, tests/agents/routers/test_agent_push.py, tests/agents/routers/test_agent_s3.py, tests/discovery/tasks/test_scan_reaper.py</files>
  <read_first>
    - tests/conftest.py (the `verify` fixture)
    - tests/review/routers/test_duplicates.py:298-347 (5 sites — the canonical example)
    - .planning/phases/92-milestone-close-tech-debt-cleanup/92-PATTERNS.md (landmine file list + the integration-exclusion note)
    - grep: `grep -rn "async_sessionmaker(async_engine\|verify_factory" tests/review tests/agents tests/discovery tests/integration`
  </read_first>
  <action>
    Migrate the remaining non-integration verify sites the same mechanical way as Task 1: test_duplicates.py (5), test_agent_push.py (2), test_agent_s3.py (1), test_scan_reaper.py (1) → rebind to the shared `verify` fixture; assertion bodies unchanged.

    Then GREP-VERIFY the integration exclusion (RESEARCH A4 / Pitfall 3): confirm every remaining `async_sessionmaker(async_engine)` usage in `tests/integration/**` (e.g. test_drain_double_dispatch.py) is against integration's OWN engine (tests/integration/conftest.py), NOT the top-level `async_engine` fixture. If any integration file DOES consume the top-level `async_engine`, flag it in the SUMMARY as a scope escalation (do not silently migrate integration — it manages its own DB). Record the verification result.
  </action>
  <verify>
    <automated>! grep -rn "async_sessionmaker(async_engine" tests/review tests/agents tests/discovery && just test-bucket review && just test-bucket agents && just test-bucket discovery</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "async_sessionmaker(async_engine" tests/review tests/agents tests/discovery` returns no matches.
    - `just test-bucket review`, `just test-bucket agents`, `just test-bucket discovery` all green in isolation.
    - The integration-exclusion grep result is recorded in the SUMMARY: either "no tests/integration file consumes the top-level async_engine" (expected) or an explicit flag if one does.
    - Total migrated sites reconcile to the enumerated 23-across-14 (drift noted if any).
  </acceptance_criteria>
  <done>Review/agents/discovery verify sites migrated + green in isolation; integration exclusion grep-verified and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test verify-read ↔ shared connection | Rebinding verify reads to the per-test connection; test-infra only, no runtime surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-04-VIS | Tampering (false-green test) | migrated verify reads | mitigate | Reads rebind to the shared outer-transaction connection so they still assert real commits; assertion bodies unchanged; per-bucket green in isolation confirms |
| T-92-04-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in Phase 92 (RESEARCH §Standard Stack) — no supply-chain surface |
</threat_model>

<verification>
- `just test-bucket analyze|review|agents|discovery` green in isolation.
- No `async_sessionmaker(async_engine)` remains outside `tests/integration/**`.
- Integration exclusion grep-verified.
</verification>

<success_criteria>
- All 23 verify-session call sites read through the shared connection-bound fixture.
- The four migrated buckets pass under per-bucket isolation.
</success_criteria>

<output>
Create `.planning/phases/92-milestone-close-tech-debt-cleanup/92-04-SUMMARY.md` when done.
</output>
