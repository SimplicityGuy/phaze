---
phase: 92-milestone-close-tech-debt-cleanup
plan: 03
type: execute
wave: 2
depends_on: [92-01, 92-02]  # ordering: verify CLEAN-01/03 green under the OLD harness before rewiring conftest
files_modified:
  - tests/conftest.py
  - tests/shared/test_conftest_hermeticity.py
autonomous: true
requirements: [CLEAN-02]
must_haves:
  truths:
    - "The test schema is created ONCE (session-scoped engine), not per test"
    - "Each test runs inside a per-test outer transaction on ONE connection with AsyncSession(join_transaction_mode='create_savepoint'), rolled back at teardown"
    - "In-test commits are visible to sibling sessions bound to the same connection, yet nothing survives into the next test"
    - "A shared verify fixture bound to the per-test connection is available for independent-verify reads"
    - "get_stage_progress's PRODUCTION fan-out sessions (opened via phaze.database.async_session) are routed, under the DB fixture, through the same per-test _db_connection — so a seed-then-get_stage_progress / GET /pipeline/stats read sees the in-test savepoint state instead of reading zero/stale from a different pool connection"
    - "A mutation-safe fixture-contract test proves rollback isolation AND the production-fan-out visibility (a green suite alone does not prove hermeticity)"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Session-scoped engine + per-test create_savepoint session + shared verify fixture + seed-once fileserver + production-fan-out routing (async_session → _db_connection)"
      contains: "join_transaction_mode"
    - path: "tests/shared/test_conftest_hermeticity.py"
      provides: "Mutation-safe hermeticity contract test (rollback isolation + seed-then-get_stage_progress visibility)"
  key_links:
    - from: "tests/conftest.py client override"
      to: "tests/conftest.py session"
      via: "get_session → lambda: session (single-connection funnel)"
      pattern: "get_session"
    - from: "tests/conftest.py stats-fanout routing"
      to: "phaze.database.async_session"
      via: "monkeypatch to a _db_connection-bound create_savepoint sessionmaker + Semaphore(1)"
      pattern: "async_session"
---

<objective>
CLEAN-02 core: fix the 83-01/83-03 flake class at its shared root in `tests/conftest.py` (D-06 global). Replace the function-scoped `create_all`/`drop_all`+committed-seed fixture with a session-scoped engine + per-test outer transaction + `AsyncSession(join_transaction_mode="create_savepoint")` (D-07), add a shared `verify` fixture bound to the per-test connection, ROUTE `get_stage_progress`'s production fan-out sessions through that same per-test connection so seed-then-read tests still see their rows, and prove it all with a mutation-safe contract test.

Purpose: Make the suite hermetic by construction — no committed row ever survives teardown to collide (`pk_agents`) — WITHOUT blinding the production `get_stage_progress` fan-out (92-02) to the per-test savepoint state.
Output: Rewired `conftest.py` (fixtures + fan-out routing) + `tests/shared/test_conftest_hermeticity.py`.

NOTE ON INTERMEDIATE RED: this plan rewires the shared conftest that all buckets import. The **13 test files (21 sites)** that build INDEPENDENT verify sessions via `async_sessionmaker(async_engine)` will go RED after this lands and are migrated in plan 92-04. That is a DIFFERENT failure class from the one Task 2 fixes here: those are TEST-SIDE independent read sessions (migrated in 92-04); Task 2 fixes the PRODUCTION-SIDE reads inside `get_stage_progress` (which open their own `phaze.database.async_session` and are consumed by `tests/analyze/core/test_stage_progress.py` and `tests/shared/routers/test_pipeline.py`). The production-fan-out routing MUST land in THIS plan (Task 2), before the `shared` bucket is claimed green, because `test_pipeline.py` lives in the `shared` bucket and seeds-then-hits `GET /pipeline/stats`. The phase does not merge until the plan 92-05 full-suite gate is green (D-08). Do NOT treat the intermediate red in non-`shared` verify-session buckets as a regression — verify only the `shared` bucket + the contract test in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-milestone-close-tech-debt-cleanup/92-CONTEXT.md
@.planning/phases/92-milestone-close-tech-debt-cleanup/92-RESEARCH.md
@.planning/phases/92-milestone-close-tech-debt-cleanup/92-PATTERNS.md

<interfaces>
<!-- Current conftest anchors — executor uses these directly. -->
From tests/conftest.py (being rewired):
- `async_engine` (:198-219): function-scoped, create_all/drop_all per test, commits a `test-fileserver` seed row (:213-215) — the flake root (D-06)
- `session` (:222-227): fresh `async_sessionmaker(async_engine)` per test
- `client` / `authenticated_client` (:230-236 / :260-280): override `get_session` with `lambda: session` — the single-session funnel to PRESERVE
- `make_file` + sibling factories (:415-445, :448+): call `await session.commit()` — the in-test commits create_savepoint must intercept-yet-rollback; `make_file` hardcodes `agent_id="test-fileserver"` (:431)
- `DB_FIXTURES` (:45) + `pytest_collection_modifyitems` auto-marking (:145-164)
- imports (:10-13): `from sqlalchemy.ext.asyncio import AsyncSession, async_sessionmaker, create_async_engine`

From src/phaze/services/pipeline.py (after 92-02):
- `get_stage_progress` fans out its reads over per-task sessions opened from the module-level `phaze.database.async_session`, bounded by a module-level `phaze.services.pipeline._STATS_FANOUT = asyncio.Semaphore(4)`. Per the existing agent-worker import-boundary convention (`pipeline.py:827` uses a DEFERRED `from phaze.database import async_session`), the single patchable seam is `phaze.database.async_session` (re-resolved at call time) — this is Task 2's monkeypatch target.

Recommended async fixture shape: 92-RESEARCH.md:107-142 (SQLAlchemy 2.0 docs, cited).
Placement analog: tests/shared/test_conftest_dsn_coercion.py (infra test of conftest).
Behavioral analog (commit-then-independent-read): tests/review/routers/test_duplicates.py:298-347.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire conftest to session-scoped engine + create_savepoint session + shared verify fixture</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py:1-20 (imports) and :145-164 (auto-marking) and :198-280 (async_engine/session/client fixtures) and :415-460 (make_file + factories)
    - .planning/phases/92-milestone-close-tech-debt-cleanup/92-RESEARCH.md:95-142 (CLEAN-02 mechanism + recommended fixture shape) and Pitfall 3/4/5/6
    - .planning/phases/92-milestone-close-tech-debt-cleanup/92-PATTERNS.md (tests/conftest.py CLEAN-02 section + the 🔴 LOAD-BEARING LANDMINE box)
    - MEMORY project_get_session_never_commits (the invariant to preserve)
  </read_first>
  <action>
    Rewire the fixtures per D-06/D-07 using the SQLAlchemy 2.0 built-in — do NOT hand-roll the `after_transaction_end` / `begin_nested()` event listener (RESEARCH: `create_savepoint` IS that recipe):

    1. `async_engine` → `scope="session"`: create schema ONCE via `run_sync(Base.metadata.create_all)`; seed the stable FK-parent `test-fileserver` Agent ONCE, committed for real via a throwaway `async_sessionmaker(engine)` (durably, OUTSIDE per-test txns) so `make_file`'s `agent_id="test-fileserver"` FK target survives all tests; `drop_all` + `dispose` on session teardown.
    2. New function-scoped `_db_connection` fixture: `async with async_engine.connect() as conn: yield conn`.
    3. `session` → function-scoped, bound to `_db_connection`: begin a per-test outer transaction (`outer = await _db_connection.begin()`), construct `AsyncSession(bind=_db_connection, join_transaction_mode="create_savepoint")`, yield it, and on teardown `await s.close()` then `await outer.rollback()` (discards ALL in-test commits, including savepoint-release commits from mutating routers).
    4. New shared `verify` fixture (function-scoped) that yields an independent read session BOUND TO THE SAME `_db_connection` (e.g. a factory `async_sessionmaker(bind=_db_connection, ...)` or a second `AsyncSession(bind=_db_connection, join_transaction_mode="create_savepoint")`). This is the migration target for plan 92-04's 21 verify-session call sites — reads through it see in-test commits because they share the one outer-transaction connection (RESEARCH Pitfall 3 / CRITICAL wiring corollary). Give it a clear name and docstring.
    5. Keep the `client` / `authenticated_client` `get_session` override yielding the SAME `session` object (`lambda: session`) — the single-connection funnel (:234) MUST stay intact.
    6. Decide whether the new `_db_connection`/`verify` fixtures join `DB_FIXTURES` (:45) for auto-marking; keep the integration/migration auto-mark behavior (:145-164) unchanged.

    RESEARCH Pitfall 4: several pipeline functions use `session.begin_nested()`; under create_savepoint they create a nested SAVEPOINT — PostgreSQL/SQLAlchemy support this, but the full-suite gate (plan 92-05) is what confirms they stay green. Do not special-case them here.

    Do NOT modify `tests/integration/conftest.py` or anything under `tests/integration/**` / `tests/test_migrations/**` — they build their own engines against `MIGRATIONS_TEST_DATABASE_URL` / a real broker and are auto-marked `integration` (RESEARCH A4 / Pitfall 3). Grep-verify none of them consume the top-level `async_engine`/`session` fixtures.

    Do NOT add the production-fan-out routing yet — that is Task 2. This task's verify deliberately runs only the shared-bucket INFRA tests (`-k "conftest or dsn"`), NOT the full `shared` bucket, because `test_pipeline.py` cannot go green until Task 2 routes the `get_stage_progress` fan-out.
  </action>
  <verify>
    <automated>grep -q 'join_transaction_mode="create_savepoint"' tests/conftest.py && grep -q "scope=\"session\"" tests/conftest.py && uv run pytest tests/shared -q -k "conftest or dsn" </automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` `async_engine` fixture is `scope="session"` and calls `create_all` exactly once; the `test-fileserver` seed is committed once in session setup, not per test.
    - A `session` fixture constructs `AsyncSession(bind=_db_connection, join_transaction_mode="create_savepoint")` and teardown calls `await outer.rollback()`.
    - A shared `verify` (independent-read) fixture bound to the same `_db_connection` exists with a docstring.
    - The `client`/`authenticated_client` `get_session` override still yields the same `session` object (funnel intact).
    - `grep -rn "async_engine\|from tests.conftest import session\|\bsession\b" tests/integration tests/test_migrations` confirms those suites do NOT consume the top-level fixtures (documented in SUMMARY).
    - `uv run pytest tests/shared -q -k "conftest or dsn"` passes (the shared-bucket infra tests); the existing `test_conftest_dsn_coercion.py` still passes. (Full `shared` bucket is proven in Task 2, after fan-out routing.)
  </acceptance_criteria>
  <done>conftest rewired to session-scoped engine + per-test create_savepoint transaction + shared verify fixture + seed-once fileserver; single-session funnel preserved; integration/migration suites untouched and grep-verified. Fan-out routing deferred to Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Route get_stage_progress's PRODUCTION fan-out sessions through the per-test connection</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (the new `_db_connection` / `session` / `verify` fixtures from Task 1)
    - src/phaze/services/pipeline.py:488+ (get_stage_progress fan-out from 92-02) and :820-829 (the existing DEFERRED `from phaze.database import async_session` import-boundary convention — the same seam)
    - src/phaze/database.py:45 (`async_session = async_sessionmaker(engine, ...)` — the monkeypatch target)
    - .planning/phases/92-milestone-close-tech-debt-cleanup/92-RESEARCH.md (Open Question 1 — the perf-harness routes the SAME internal fan-out to the perf DB via PHAZE_DATABASE_URL; this is the test-fixture analog) and Pitfall 3 (every session must share the outer-transaction connection)
    - tests/shared/routers/test_pipeline.py (seed-then-`GET /pipeline/stats` tests that assert exact rendered counts, e.g. the awaiting-cloud card `>3<` — these break without this routing)
  </read_first>
  <action>
    THE FAILURE CLASS THIS FIXES (verified): after 92-02, `get_stage_progress` no longer reads via the passed-in `session`; it opens its OWN per-task sessions from the module-level `phaze.database.async_session` (the production engine, its own pool connection), independent of the test's `dependency_overrides[get_session]`. Under Task 1's `create_savepoint` fixture, seeded rows live only in the UNCOMMITTED per-test outer transaction on `_db_connection`. A fan-out session on a DIFFERENT pool connection is a different transaction → read-committed → it reads ZERO/STALE. This breaks EVERY test that seeds via `session.add(...); await session.commit()` and then exercises `get_stage_progress` directly or via `GET /pipeline/stats` — notably `tests/analyze/core/test_stage_progress.py` (analyze bucket) and ~a dozen `tests/shared/routers/test_pipeline.py` cases (SHARED bucket — the exact bucket this plan proves green). This is BROADER than, and distinct from, the 21 test-side verify-session sites migrated in 92-04.

    FIX (option a — recommended, smallest blast radius, preserves the seed-then-read tests unchanged): under the DB fixture, monkeypatch the production sessionmaker so its sessions bind to the same per-test connection:
    1. Build a connection-bound factory: `async_sessionmaker(bind=_db_connection, class_=AsyncSession, join_transaction_mode="create_savepoint", expire_on_commit=False)`.
    2. `monkeypatch.setattr("phaze.database.async_session", <that factory>)`. Because `pipeline.py` resolves `async_session` via a DEFERRED `from phaze.database import async_session` at call time (the agent-worker import-boundary convention at `pipeline.py:827`), patching the SOURCE `phaze.database.async_session` is picked up by every fan-out read — no `pipeline.py` edit needed. (If 92-02 instead bound the name at module import into `phaze.services.pipeline`, also patch `phaze.services.pipeline.async_session`.)
    3. `monkeypatch.setattr("phaze.services.pipeline._STATS_FANOUT", asyncio.Semaphore(1))`. RATIONALE: all fan-out sessions now share ONE asyncpg connection, and a single connection cannot run concurrent statements ("another operation is in progress"). `Semaphore(1)` serializes the fan-out on the shared connection — preserving VISIBILITY of the per-test savepoint state while avoiding the concurrency collision. Production keeps `Semaphore(4)` + one pool connection per task (true concurrency); only the test run serializes.
    4. Scope the routing to DB-fixture tests via the `_db_connection`/`session` fixture chain (a function-scoped helper fixture that depends on `_db_connection` and the pytest `monkeypatch` fixture). Do NOT make it `autouse=True` globally — that would force `_db_connection` creation (and a DB) for DB-free tests.

    This mirrors the perf-harness routing for RESEARCH Open Question 1 (route the internal `async_session()` fan-out to the intended DB) — same intent, different mechanism (per-test monkeypatch vs process-wide `PHAZE_DATABASE_URL`).

    FALLBACK (option b — only if a clean monkeypatch is genuinely unreachable): give `get_stage_progress` an explicit session-factory seam it reads at call time, OR migrate the affected seed-then-read tests (`test_stage_progress.py` + the affected `test_pipeline.py` cases) to a durably-committed seed path analogous to the seed-once `test-fileserver`. Prefer option a. If you must use option b, record why in the SUMMARY.
  </action>
  <verify>
    <automated>grep -q '"phaze.database.async_session"' tests/conftest.py && grep -q "_STATS_FANOUT" tests/conftest.py && just test-bucket shared</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` monkeypatches `phaze.database.async_session` to a `_db_connection`-bound `create_savepoint` sessionmaker for DB-fixture tests, and monkeypatches `phaze.services.pipeline._STATS_FANOUT` to `asyncio.Semaphore(1)`.
    - The production module default is UNCHANGED (`phaze.services.pipeline._STATS_FANOUT` is still `Semaphore(4)` outside the test run; the patch is per-test, reverted by `monkeypatch` teardown).
    - The routing is scoped to DB tests via the fixture chain (NOT `autouse=True` global).
    - `just test-bucket shared` is GREEN, including `tests/shared/routers/test_pipeline.py` seed-then-`GET /pipeline/stats` cases that assert exact rendered counts (e.g. the awaiting-cloud card `>3<`, `test_pipeline_stats_partial`, `test_stats_partial_emits_awaiting_cloud_card_oob`) — proving the fan-out reads the per-test state, not zero.
    - No `another operation is in progress` / `IllegalStateChangeError` under the routed fan-out (mind the 5432/5433 port footgun — export the correct DB URL).
  </acceptance_criteria>
  <done>get_stage_progress's production fan-out is routed through the per-test connection under the DB fixture; the shared bucket (incl. the seed-then-/pipeline/stats tests) is green with correct non-zero counts and no concurrency error.</done>
</task>

<task type="auto">
  <name>Task 3: Add mutation-safe fixture-contract hermeticity test (rollback + production-fan-out visibility)</name>
  <files>tests/shared/test_conftest_hermeticity.py</files>
  <read_first>
    - tests/shared/test_conftest_dsn_coercion.py:1-13 (header/import template for an infra test of conftest)
    - tests/review/routers/test_duplicates.py:298-347 (commit-then-independent-session-read assertion shape)
    - tests/conftest.py (the new `session`, `verify`, and fan-out-routing fixtures from Tasks 1-2)
    - .planning/phases/92-milestone-close-tech-debt-cleanup/92-VALIDATION.md (Wave 0 Requirements — the fixture-contract test) and 92-RESEARCH.md Wave-0 Gaps
    - MEMORY feedback_mutation_test_guard_tests (a green fixture proves nothing — break it, watch it fail, restore)
  </read_first>
  <action>
    Create `tests/shared/test_conftest_hermeticity.py` proving the create_savepoint mechanism actually isolates AND that the production fan-out sees in-test state. It must assert FOUR things:
    1. An in-test `commit()` (e.g. add + commit an Agent like `test-agent-01` via the `session` fixture) IS visible to a sibling read on the SAME connection — read it back through the `verify` fixture and assert present.
    2. The NEXT test sees a clean DB — a second test asserts that `test-agent-01` (committed in test 1) is ABSENT, proving teardown rollback discarded it (no surviving row → no `pk_agents` collision). Order-independence: make both tests assert-then-create so either ordering proves rollback.
    3. **Production-fan-out visibility (guards the BLOCKER-1 regression):** seed a file via the `session` fixture (add + commit, e.g. `make_file`), then call `get_stage_progress(session)` (or hit `GET /pipeline/stats` via the `client` fixture) and assert the derived count reflects the seeded row (NOT zero) — proving Task 2's routing makes the production fan-out read the per-test savepoint state. This turns the exact collision into a guarded regression rather than something only discovered at the D-08 gate.
    4. Mutation-safety: document in the module docstring the exact manual mutations that must make the test FAIL — (a) revert `async_engine` to function-scoped `create_all`/`drop_all` with a committed seed and re-run: the seed-collision / surviving-row returns; (b) remove the Task-2 `phaze.database.async_session` monkeypatch and re-run: assertion 3 flips to reading zero. (Encode as a docstring recipe the reviewer can run; do not leave a permanently-broken variant in the file.)

    Place in the `shared` bucket. Use `pytest_asyncio` async tests. This is the mutation-safe proof that CLEAN-02 isolates for an intermittent flake AND that the production reads stay wired to the per-test state.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/test_conftest_hermeticity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/shared/test_conftest_hermeticity.py` exists with ≥3 async tests: commit-visible-to-sibling-`verify`-read; committed row absent in a subsequent test; and a seed-then-`get_stage_progress`(or `/pipeline/stats`) read that sees the seeded row (non-zero).
    - The module docstring contains BOTH explicit mutation recipes (function-scoped revert → collision returns; drop the fan-out monkeypatch → assertion 3 reads zero) per feedback_mutation_test_guard_tests.
    - `uv run pytest tests/shared/test_conftest_hermeticity.py -q` exits 0.
    - `just test-bucket shared` is green.
  </acceptance_criteria>
  <done>Mutation-safe hermeticity contract test green in the shared bucket; proves commit-visibility + rollback-isolation + production-fan-out visibility, each with a documented break-recipe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture ↔ shared DB connection | Test isolation boundary; a leak here is a hermeticity failure, not a runtime security surface (test-infra only) |
| production fan-out ↔ per-test connection (under fixture) | The monkeypatch reroutes `get_stage_progress`'s production sessions onto the per-test connection; test-infra only, reverted per test by `monkeypatch` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-03-ISO | Tampering (test-state leak) | conftest transactional fixture | mitigate | create_savepoint + per-test outer-transaction rollback discards all in-test commits; mutation-safe contract test proves it; production code untouched |
| T-92-03-VIS | Information Disclosure (false-zero read) | production fan-out reading a different connection than the seed | mitigate | Task 2 routes `phaze.database.async_session` to the per-test connection + `Semaphore(1)` serialization; contract test assertion 3 + shared-bucket `test_pipeline` prove non-zero reads; monkeypatch is per-test and reverted |
| T-92-03-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in Phase 92 (RESEARCH §Standard Stack) — no supply-chain surface |
</threat_model>

<verification>
- `uv run pytest tests/shared -q -k "conftest or dsn"` green after Task 1 (infra tests).
- `just test-bucket shared` green after Task 2 (incl. the seed-then-`/pipeline/stats` tests reading correct non-zero counts).
- Contract test green; both mutation recipes documented.
- Integration/migration suites grep-verified as not consuming the rewired fixtures.
- Intermediate red in analyze/review/agents/discovery verify-session buckets is EXPECTED here and closed by plan 92-04; `test_stage_progress.py` (analyze) is fixed by Task 2's routing, NOT a 92-04 verify-session migration.
</verification>

<success_criteria>
- conftest uses session-scoped engine + per-test create_savepoint rollback + shared verify fixture (D-06/D-07).
- get_session-never-commits invariant preserved (single-connection funnel).
- Production `get_stage_progress` fan-out routed through the per-test connection so seed-then-read tests see their rows.
- Hermeticity + fan-out visibility proven by a mutation-safe contract test.
</success_criteria>

<output>
Create `.planning/phases/92-milestone-close-tech-debt-cleanup/92-03-SUMMARY.md` when done.
</output>
