---
phase: 92-milestone-close-tech-debt-cleanup
plan: 03
type: execute
wave: 2
depends_on: [92-01, 92-02]  # ordering: verify CLEAN-01/03 green under the OLD harness before rewiring conftest
files_modified:
  - tests/conftest.py
  - tests/shared/test_conftest_hermeticity.py
autonomous: true
requirements: [CLEAN-02]
must_haves:
  truths:
    - "The test schema is created ONCE (session-scoped engine), not per test"
    - "Each test runs inside a per-test outer transaction on ONE connection with AsyncSession(join_transaction_mode='create_savepoint'), rolled back at teardown"
    - "In-test commits are visible to sibling sessions bound to the same connection, yet nothing survives into the next test"
    - "A shared verify fixture bound to the per-test connection is available for independent-verify reads"
    - "A mutation-safe fixture-contract test proves rollback isolation (green suite alone does not prove hermeticity)"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Session-scoped engine + per-test create_savepoint session + shared verify fixture + seed-once fileserver"
      contains: "join_transaction_mode"
    - path: "tests/shared/test_conftest_hermeticity.py"
      provides: "Mutation-safe hermeticity contract test"
  key_links:
    - from: "tests/conftest.py client override"
      to: "tests/conftest.py session"
      via: "get_session → lambda: session (single-connection funnel)"
      pattern: "get_session"
---

<objective>
CLEAN-02 core: fix the 83-01/83-03 flake class at its shared root in `tests/conftest.py` (D-06 global). Replace the function-scoped `create_all`/`drop_all`+committed-seed fixture with a session-scoped engine + per-test outer transaction + `AsyncSession(join_transaction_mode="create_savepoint")` (D-07), add a shared `verify` fixture bound to the per-test connection, and prove it with a mutation-safe contract test.

Purpose: Make the suite hermetic by construction — no committed row ever survives teardown to collide (`pk_agents`).
Output: Rewired `conftest.py` + `tests/shared/test_conftest_hermeticity.py`.

NOTE ON INTERMEDIATE RED: this plan rewires the shared conftest that all buckets import. The 14 test files that build INDEPENDENT verify sessions via `async_sessionmaker(async_engine)` will go RED after this lands and are migrated in plan 92-04. The phase does not merge until the plan 92-05 full-suite gate is green (D-08). Do NOT treat the intermediate red in non-`shared` buckets as a regression — verify only the `shared` bucket + the contract test in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-milestone-close-tech-debt-cleanup/92-CONTEXT.md
@.planning/phases/92-milestone-close-tech-debt-cleanup/92-RESEARCH.md
@.planning/phases/92-milestone-close-tech-debt-cleanup/92-PATTERNS.md

<interfaces>
<!-- Current conftest anchors — executor uses these directly. -->
From tests/conftest.py (being rewired):
- `async_engine` (:198-219): function-scoped, create_all/drop_all per test, commits a `test-fileserver` seed row (:213-215) — the flake root (D-06)
- `session` (:222-227): fresh `async_sessionmaker(async_engine)` per test
- `client` / `authenticated_client` (:230-236 / :260-280): override `get_session` with `lambda: session` — the single-session funnel to PRESERVE
- `make_file` + sibling factories (:415-445, :448+): call `await session.commit()` — the in-test commits create_savepoint must intercept-yet-rollback; `make_file` hardcodes `agent_id="test-fileserver"` (:431)
- `DB_FIXTURES` (:45) + `pytest_collection_modifyitems` auto-marking (:145-164)
- imports (:10-13): `from sqlalchemy.ext.asyncio import AsyncSession, async_sessionmaker, create_async_engine`

Recommended async fixture shape: 92-RESEARCH.md:107-142 (SQLAlchemy 2.0 docs, cited).
Placement analog: tests/shared/test_conftest_dsn_coercion.py (infra test of conftest).
Behavioral analog (commit-then-independent-read): tests/review/routers/test_duplicates.py:298-347.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire conftest to session-scoped engine + create_savepoint session + shared verify fixture</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py:1-20 (imports) and :145-164 (auto-marking) and :198-280 (async_engine/session/client fixtures) and :415-460 (make_file + factories)
    - .planning/phases/92-milestone-close-tech-debt-cleanup/92-RESEARCH.md:95-142 (CLEAN-02 mechanism + recommended fixture shape) and Pitfall 3/4/5/6
    - .planning/phases/92-milestone-close-tech-debt-cleanup/92-PATTERNS.md (tests/conftest.py CLEAN-02 section + the 🔴 LOAD-BEARING LANDMINE box)
    - MEMORY project_get_session_never_commits (the invariant to preserve)
  </read_first>
  <action>
    Rewire the fixtures per D-06/D-07 using the SQLAlchemy 2.0 built-in — do NOT hand-roll the `after_transaction_end` / `begin_nested()` event listener (RESEARCH: `create_savepoint` IS that recipe):

    1. `async_engine` → `scope="session"`: create schema ONCE via `run_sync(Base.metadata.create_all)`; seed the stable FK-parent `test-fileserver` Agent ONCE, committed for real via a throwaway `async_sessionmaker(engine)` (durably, OUTSIDE per-test txns) so `make_file`'s `agent_id="test-fileserver"` FK target survives all tests; `drop_all` + `dispose` on session teardown.
    2. New function-scoped `_db_connection` fixture: `async with async_engine.connect() as conn: yield conn`.
    3. `session` → function-scoped, bound to `_db_connection`: begin a per-test outer transaction (`outer = await _db_connection.begin()`), construct `AsyncSession(bind=_db_connection, join_transaction_mode="create_savepoint")`, yield it, and on teardown `await s.close()` then `await outer.rollback()` (discards ALL in-test commits, including savepoint-release commits from mutating routers).
    4. New shared `verify` fixture (function-scoped) that yields an independent read session BOUND TO THE SAME `_db_connection` (e.g. a factory `async_sessionmaker(bind=_db_connection, ...)` or a second `AsyncSession(bind=_db_connection, join_transaction_mode="create_savepoint")`). This is the migration target for plan 92-04's 23 verify-session call sites — reads through it see in-test commits because they share the one outer-transaction connection (RESEARCH Pitfall 3 / CRITICAL wiring corollary). Give it a clear name and docstring.
    5. Keep the `client` / `authenticated_client` `get_session` override yielding the SAME `session` object (`lambda: session`) — the single-connection funnel (:234) MUST stay intact.
    6. Decide whether the new `_db_connection`/`verify` fixtures join `DB_FIXTURES` (:45) for auto-marking; keep the integration/migration auto-mark behavior (:145-164) unchanged.

    RESEARCH Pitfall 4: several pipeline functions use `session.begin_nested()`; under create_savepoint they create a nested SAVEPOINT — PostgreSQL/SQLAlchemy support this, but the full-suite gate (plan 92-05) is what confirms they stay green. Do not special-case them here.

    Do NOT modify `tests/integration/conftest.py` or anything under `tests/integration/**` / `tests/test_migrations/**` — they build their own engines against `MIGRATIONS_TEST_DATABASE_URL` / a real broker and are auto-marked `integration` (RESEARCH A4 / Pitfall 3). Grep-verify none of them consume the top-level `async_engine`/`session` fixtures.
  </action>
  <verify>
    <automated>grep -q 'join_transaction_mode="create_savepoint"' tests/conftest.py && grep -q "scope=\"session\"" tests/conftest.py && uv run pytest tests/shared -q -k "conftest or dsn" </automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` `async_engine` fixture is `scope="session"` and calls `create_all` exactly once; the `test-fileserver` seed is committed once in session setup, not per test.
    - A `session` fixture constructs `AsyncSession(bind=_db_connection, join_transaction_mode="create_savepoint")` and teardown calls `await outer.rollback()`.
    - A shared `verify` (independent-read) fixture bound to the same `_db_connection` exists with a docstring.
    - The `client`/`authenticated_client` `get_session` override still yields the same `session` object (funnel intact).
    - `grep -rn "async_engine\|from tests.conftest import session\|\bsession\b" tests/integration tests/test_migrations` confirms those suites do NOT consume the top-level fixtures (documented in SUMMARY).
    - `just test-bucket shared` runs (the shared bucket has no verify-session call sites) — records pass/fail; the existing `test_conftest_dsn_coercion.py` still passes.
  </acceptance_criteria>
  <done>conftest rewired to session-scoped engine + per-test create_savepoint transaction + shared verify fixture + seed-once fileserver; single-session funnel preserved; integration/migration suites untouched and grep-verified.</done>
</task>

<task type="auto">
  <name>Task 2: Add mutation-safe fixture-contract hermeticity test</name>
  <files>tests/shared/test_conftest_hermeticity.py</files>
  <read_first>
    - tests/shared/test_conftest_dsn_coercion.py:1-13 (header/import template for an infra test of conftest)
    - tests/review/routers/test_duplicates.py:298-347 (commit-then-independent-session-read assertion shape)
    - .planning/phases/92-milestone-close-tech-debt-cleanup/92-VALIDATION.md (Wave 0 Requirements — the fixture-contract test) and 92-RESEARCH.md Wave-0 Gaps
    - MEMORY feedback_mutation_test_guard_tests (a green fixture proves nothing — break it, watch it fail, restore)
  </read_first>
  <action>
    Create `tests/shared/test_conftest_hermeticity.py` proving the create_savepoint mechanism actually isolates. It must assert three things (RESEARCH Wave-0 Gap):
    1. An in-test `commit()` (e.g. add + commit an Agent like `test-agent-01` via the `session` fixture) IS visible to a sibling read on the SAME connection — read it back through the `verify` fixture and assert present.
    2. The NEXT test sees a clean DB — a second test asserts that `test-agent-01` (committed in test 1) is ABSENT, proving teardown rollback discarded it (no surviving row → no `pk_agents` collision). Order-independence: make both tests assert-then-create so either ordering proves rollback.
    3. Mutation-safety: document in the module docstring the exact manual mutation that must make the test FAIL — revert `async_engine` to function-scoped `create_all`/`drop_all` with a committed seed and re-run; the seed-collision / surviving-row must return. (Encode as a docstring recipe the reviewer can run; do not leave a permanently-broken variant in the file.)

    Place in the `shared` bucket. Use `pytest_asyncio` async tests. This is the mutation-safe proof that CLEAN-02 isolates for an intermittent flake.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/test_conftest_hermeticity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/shared/test_conftest_hermeticity.py` exists with ≥2 async tests: one asserting commit-visible-to-sibling-`verify`-read, one asserting the committed row is absent in a subsequent test.
    - The module docstring contains the explicit mutation recipe (revert to function-scoped create_all → collision returns) per feedback_mutation_test_guard_tests.
    - `uv run pytest tests/shared/test_conftest_hermeticity.py -q` exits 0.
    - `just test-bucket shared` is green.
  </acceptance_criteria>
  <done>Mutation-safe hermeticity contract test green in the shared bucket; proves commit-visibility + rollback-isolation with a documented break-recipe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture ↔ shared DB connection | Test isolation boundary; a leak here is a hermeticity failure, not a runtime security surface (test-infra only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-03-ISO | Tampering (test-state leak) | conftest transactional fixture | mitigate | create_savepoint + per-test outer-transaction rollback discards all in-test commits; mutation-safe contract test proves it; production code untouched |
| T-92-03-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in Phase 92 (RESEARCH §Standard Stack) — no supply-chain surface |
</threat_model>

<verification>
- `just test-bucket shared` green (shared bucket has no verify-session call sites).
- Contract test green; mutation recipe documented.
- Integration/migration suites grep-verified as not consuming the rewired fixtures.
- Intermediate red in analyze/review/agents/discovery buckets is EXPECTED here and closed by plan 92-04.
</verification>

<success_criteria>
- conftest uses session-scoped engine + per-test create_savepoint rollback + shared verify fixture (D-06/D-07).
- get_session-never-commits invariant preserved (single-connection funnel).
- Hermeticity proven by a mutation-safe contract test.
</success_criteria>

<output>
Create `.planning/phases/92-milestone-close-tech-debt-cleanup/92-03-SUMMARY.md` when done.
</output>
