---
phase: 50-push-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - src/phaze/tasks/_shared/deterministic_key.py
  - src/phaze/services/pipeline_counters.py
  - src/phaze/services/enqueue_router.py
  - src/phaze/tasks/reenqueue.py
  - tests/test_reenqueue.py
autonomous: true
requirements: [CLOUDPIPE-01, CLOUDPIPE-05]

must_haves:
  truths:
    - "push_file is keyed, counted, and routable — the three totality guards pass together"
    - "A push_file enqueue dedups on push_file:<file_id> (double-tick collapses to a no-op)"
    - "Recovery classifies PUSHING/PUSHED as NOT done (re-drivable); a re-driven push_file routes to a fileserver agent"
    - "process_file done-set stays {ANALYZED, ANALYSIS_FAILED} (PUSHED is not analyze-done)"
  artifacts:
    - path: "src/phaze/tasks/_shared/deterministic_key.py"
      provides: "_KEY_BUILDERS['push_file'] = lambda k: str(k['file_id'])"
      contains: "push_file"
    - path: "src/phaze/services/pipeline_counters.py"
      provides: "PIPELINE_FUNCTIONS includes push_file"
      contains: "push_file"
    - path: "src/phaze/services/enqueue_router.py"
      provides: "AGENT_TASKS includes push_file"
      contains: "push_file"
    - path: "src/phaze/tasks/reenqueue.py"
      provides: "push_file domain-completed predicate + fileserver-routed re-drive partition"
      contains: "push_file"
  key_links:
    - from: "src/phaze/tasks/reenqueue.py"
      to: "select_active_agent(kind=fileserver)"
      via: "push_file re-drive partition mirrors held_agent_rows"
      pattern: "kind=.fileserver."
---

<objective>
Register `push_file` across the three totality guards (deterministic key, pipeline counters, agent-task router) in one plan so the guard tests pass together, and classify the new PUSHING/PUSHED states in ledger-driven recovery so orphaned pushes are re-driven (to a fileserver) while a PUSHED file remains eligible for analysis.

Purpose: Adding `push_file` trips four completeness guards at once (RESEARCH §3); landing them separately leaves the suite red. This plan also wires CLOUDPIPE-05 idempotency (deterministic-key dedup) and the recovery predicate (D-10).
Output: `push_file` keyed/counted/routable; recovery classification + fileserver re-drive partition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-push-pipeline/50-PATTERNS.md
@.planning/phases/50-push-pipeline/50-RESEARCH.md
@src/phaze/tasks/_shared/deterministic_key.py
@src/phaze/services/pipeline_counters.py
@src/phaze/services/enqueue_router.py
@src/phaze/tasks/reenqueue.py

<interfaces>
<!-- From 50-01: FileState.PUSHING / FileState.PUSHED already exist. -->
_KEY_BUILDERS (deterministic_key.py L74-83): add "push_file": lambda k: str(k["file_id"]); update the "Exactly 8 entries" comment → 9.
PIPELINE_FUNCTIONS (pipeline_counters.py L33-42): append "push_file"; update the "8 pipeline functions" comment → 9.
AGENT_TASKS (enqueue_router.py L60-69): add "push_file" (file-touching → per-agent fileserver queue; routing_for_function derives "agent").
reenqueue.py: _DOMAIN_COMPLETED_STAGES (L107-113) + is_domain_completed (L194-217) + _build_done_sets (L150-156) + held_agent_rows partition (L304-332).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register push_file in all three totality guards</name>
  <files>src/phaze/tasks/_shared/deterministic_key.py, src/phaze/services/pipeline_counters.py, src/phaze/services/enqueue_router.py</files>
  <read_first>
    - src/phaze/tasks/_shared/deterministic_key.py (_KEY_BUILDERS L74-83 + the "Exactly 8 entries" count comment ~L70)
    - src/phaze/services/pipeline_counters.py (PIPELINE_FUNCTIONS L33-42 + the "8 pipeline functions" comment ~L29)
    - src/phaze/services/enqueue_router.py (AGENT_TASKS frozenset L60-69; routing_for_function; resolve_queue_for_task L128-159)
    - tests/test_deterministic_key.py (the keyed-or-exempt + counter-sync drift guards that must go green)
  </read_first>
  <action>
    Add `"push_file": lambda k: str(k["file_id"])` to `_KEY_BUILDERS` (NOT `_UNKEYED_TASKS` — we want push_file:<id> dedup) and bump the "Exactly 8 entries" comment to 9. Append `"push_file"` to `PIPELINE_FUNCTIONS` and bump its "8 pipeline functions" comment to 9. Add `"push_file"` to the `AGENT_TASKS` frozenset (it reads the media mount → per-agent fileserver queue). Do all three in this single task so the keyed-or-exempt guard, the counter-sync drift guard, and router-routability are all satisfied at the same commit.
  </action>
  <verify>
    <automated>uv run pytest tests/test_deterministic_key.py tests/test_pipeline_counters.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_deterministic_key.py tests/test_pipeline_counters.py -q` is green (keyed-universe + counter-sync guards pass).
    - `uv run python -c "from phaze.tasks._shared.deterministic_key import _KEY_BUILDERS; assert _KEY_BUILDERS['push_file']({'file_id':'abc'})=='abc'"` exits 0.
    - `grep -c 'push_file' src/phaze/services/enqueue_router.py` ≥ 1 and `push_file` is inside the AGENT_TASKS set.
  </acceptance_criteria>
  <done>push_file is keyed, counted, and routable; all drift/totality guards pass at one commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify push_file in recovery + fileserver re-drive partition</name>
  <files>src/phaze/tasks/reenqueue.py, tests/test_reenqueue.py</files>
  <read_first>
    - src/phaze/tasks/reenqueue.py (_DOMAIN_COMPLETED_STAGES L107-113; is_domain_completed L194-217; _build_done_sets L150-156; held_agent_rows AWAITING_CLOUD partition L304-332; _select_done_analyze_ids L165-167)
    - .planning/phases/50-push-pipeline/50-PATTERNS.md (reenqueue.py section — predicate + fileserver re-drive)
    - .planning/phases/50-push-pipeline/50-RESEARCH.md (§"Recovery classification of PUSHING/PUSHED")
    - tests/test_tasks/test_controller_reenqueue.py (existing recovery test style/fixtures)
  </read_first>
  <behavior>
    - A push_file ledger row whose FileRecord.state ∈ {PUSHED, ANALYZED, ANALYSIS_FAILED} is classified done (not re-driven).
    - A push_file ledger row whose FileRecord.state ∈ {PUSHING, AWAITING_CLOUD, DISCOVERED} is classified orphaned → re-driven.
    - A re-driven push_file routes to select_active_agent(kind="fileserver"); with no fileserver online it is skipped with a WARNING (does not raise).
    - process_file done-set is unchanged: still {ANALYZED, ANALYSIS_FAILED} (PUSHED is NOT added).
  </behavior>
  <action>
    Add `"push_file"` to `_DOMAIN_COMPLETED_STAGES` with predicate "done when FileRecord.state ∈ {PUSHED, ANALYZED, ANALYSIS_FAILED}" (mirror the _ANALYZE_DONE build in _build_done_sets and the process_file branch in is_domain_completed). Add a `push_file` re-drive partition mirroring the `held_agent_rows` AWAITING_CLOUD handling but selecting `select_active_agent(session, kind="fileserver")` (push reads the media mount); on NoActiveAgentError skip with a WARNING exactly as the existing partition does (do not raise). Keep `_select_done_analyze_ids` at {ANALYZED, ANALYSIS_FAILED} (D-10 — PUSHED still needs analysis). Add the recovery assertions to `tests/test_reenqueue.py` (convert the relevant Wave-0-adjacent expectations into real tests, `-k pushing`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_reenqueue.py -q -k pushing; uv run pytest tests/test_tasks/test_controller_reenqueue.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_reenqueue.py -q -k pushing` passes with ≥1 real (non-skipped) assertion covering: PUSHING→re-drive, PUSHED→done, fileserver routing, no-fileserver→skip-not-raise.
    - `uv run pytest tests/test_tasks/test_controller_reenqueue.py -q` stays green (recovery totality preserved).
    - `grep -q 'kind="fileserver"' src/phaze/tasks/reenqueue.py` (re-drive routes to a fileserver).
    - `grep -q 'push_file' src/phaze/tasks/reenqueue.py` inside `_DOMAIN_COMPLETED_STAGES`.
  </acceptance_criteria>
  <done>Recovery treats PUSHING/PUSHED as re-drivable, routes a re-driven push_file to a fileserver, never raises on a missing fileserver, and leaves the analyze-done set unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| enqueue chokepoint → SAQ | push_file enqueues pass through the deterministic-key before_enqueue hook; dedup is the idempotency boundary. |
| recovery cron → agents | a re-driven push_file must reach a fileserver (media-mount owner), never the compute agent. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-double-enqueue | Tampering | push_file enqueue / recovery re-drive | mitigate | `push_file:<file_id>` deterministic key collapses a repeat enqueue to a no-op (SAQ incomplete-set dedup); CLOUDPIPE-05. |
| T-50-orphan-leak | Denial of Service | uncleared push_file ledger rows after a crash-before-callback | mitigate | predicate-covered classification: a still-PUSHING file is orphaned → re-driven; a genuinely-lost push re-occupies its existing slot (no new window entry). |
| T-50-misroute | Elevation | re-driven push_file routed to compute instead of fileserver | mitigate | partition selects kind="fileserver"; no-fileserver → WARNING skip, never a raise or a compute enqueue. |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase; supply-chain gate N/A. |
</threat_model>

<verification>
`uv run pytest tests/test_deterministic_key.py tests/test_pipeline_counters.py tests/test_reenqueue.py tests/test_tasks/test_controller_reenqueue.py -q` is green; `uv run ruff check .` and `uv run mypy .` pass.
</verification>

<success_criteria>
push_file is keyed, counted, and routable (guards green together), double-ticks dedup, and recovery re-drives orphaned PUSHING/PUSHED files to a fileserver without touching the analyze-done set.
</success_criteria>

<output>
Create `.planning/phases/50-push-pipeline/50-02-SUMMARY.md` when done.
</output>
