---
phase: 50-push-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/models/file.py
  - src/phaze/schemas/agent_tasks.py
  - src/phaze/schemas/agent_push.py
  - src/phaze/config.py
autonomous: true
requirements: [CLOUDPIPE-01, CLOUDPIPE-02, CLOUDPIPE-03]
user_setup:
  - service: compute-agent-ssh
    why: "rsync-over-SSH push target (operator-provisioned in Phase 51; Phase 50 only adds the config fields)"
    env_vars:
      - name: PHAZE_PUSH_SSH_KEY_FILE
        source: "Operator-provisioned SSH identity key file path (Phase 51 runbook)"
      - name: PHAZE_PUSH_KNOWN_HOSTS_FILE
        source: "Operator-provisioned pinned known_hosts file path (Phase 51 runbook)"

must_haves:
  truths:
    - "FileState has PUSHING and PUSHED members (code-only, no migration)"
    - "ProcessFilePayload carries optional expected_sha256 + scratch_path; absence preserves local-file behavior under extra=forbid"
    - "PushFilePayload exists with file_id, original_path, file_type, agent_id"
    - "Config exposes cloud_max_in_flight (default 2), push_max_attempts (default 3), and the push SSH/scratch knobs with _FILE secret resolution"
  artifacts:
    - path: "src/phaze/models/file.py"
      provides: "FileState.PUSHING / FileState.PUSHED"
      contains: "PUSHING"
    - path: "src/phaze/schemas/agent_tasks.py"
      provides: "ProcessFilePayload.expected_sha256 + scratch_path; PushFilePayload"
      contains: "scratch_path"
    - path: "src/phaze/schemas/agent_push.py"
      provides: "Pushed/PushMismatch request+response schemas for the internal-API callbacks"
      contains: "class"
    - path: "src/phaze/config.py"
      provides: "cloud_max_in_flight, push_max_attempts, push_ssh_host/user, cloud_scratch_dir, push_timeout knobs; push_ssh_key + push_known_hosts as _FILE secrets"
      contains: "cloud_max_in_flight"
  key_links:
    - from: "src/phaze/config.py"
      to: "_resolve_secret_files"
      via: "SECRET_FILE_FIELDS override on AgentSettings"
      pattern: "SECRET_FILE_FIELDS"
---

<objective>
Define the data contracts every other Phase 50 plan builds against: the two new FileState members, the ProcessFilePayload scratch/integrity fields, the new PushFilePayload + push-callback schemas, and all config knobs (window size, attempt cap, SSH/scratch target, _FILE secrets).

Purpose: Interface-first ordering — downstream plans (push task, compute scratch, cron, callbacks) consume these contracts directly instead of exploring the codebase. Pure additive contract layer; no behavior change to existing local-file analysis (D-08, D-11, D-03, D-05, D-07).
Output: New states, payload fields, schema module, and config fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-push-pipeline/50-CONTEXT.md
@.planning/phases/50-push-pipeline/50-PATTERNS.md
@.planning/phases/50-push-pipeline/50-RESEARCH.md
@src/phaze/models/file.py
@src/phaze/schemas/agent_tasks.py
@src/phaze/config.py

<interfaces>
<!-- Concrete contracts to add. Exact line anchors are in 50-PATTERNS.md. -->
FileState: add PUSHING = "pushing", PUSHED = "pushed" (String(30) column already covers length).
ProcessFilePayload (extra="forbid"): add expected_sha256: str | None = None, scratch_path: str | None = None.
PushFilePayload (extra="forbid"): file_id: uuid.UUID, original_path: str, file_type: str, agent_id: str.
ControlSettings: cloud_max_in_flight: int = Field(default=2, gt=0, lt=100, alias PHAZE_CLOUD_MAX_IN_FLIGHT); push_max_attempts: int = Field(default=3, gt=0, lt=20, alias PHAZE_PUSH_MAX_ATTEMPTS).
AgentSettings: push_ssh_host, push_ssh_user, cloud_scratch_dir, push_timeout_sec, push_connect_timeout_sec (Field + AliasChoices(PHAZE_*, ...)); push_ssh_key + push_known_hosts added to SECRET_FILE_FIELDS.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PUSHING/PUSHED FileState members + payload fields</name>
  <files>src/phaze/models/file.py, src/phaze/schemas/agent_tasks.py</files>
  <read_first>
    - src/phaze/models/file.py (FileState StrEnum — AWAITING_CLOUD at ~L40-43 and ANALYSIS_FAILED at ~L37-39 are the exact analogs; state column String(30) at ~L66; ix_files_state at ~L78)
    - src/phaze/schemas/agent_tasks.py (ProcessFilePayload at L28-43; the Phase-44 fine_cap/coarse_cap optional-field additions are the analog)
    - .planning/phases/50-push-pipeline/50-PATTERNS.md (file.py + agent_tasks.py sections — exact edits)
  </read_first>
  <behavior>
    - FileState("pushing") == FileState.PUSHING and FileState("pushed") == FileState.PUSHED.
    - ProcessFilePayload built with only the existing 5 fields still validates and leaves expected_sha256/scratch_path == None (local-file behavior unchanged under extra="forbid").
    - ProcessFilePayload accepts expected_sha256 + scratch_path when supplied.
    - PushFilePayload requires file_id/original_path/file_type/agent_id and rejects extra keys.
  </behavior>
  <action>
    In file.py add `PUSHING = "pushing"` and `PUSHED = "pushed"` beside AWAITING_CLOUD, each with a "code-only StrEnum over String(30) → no migration (D-08)" comment. Confirm the String(30) column needs no change. In agent_tasks.py add to ProcessFilePayload (after the cap fields): `expected_sha256: str | None = None` and `scratch_path: str | None = None` with the comment that `scratch_path is not None` is itself the compute-read/ephemeral signal (no separate boolean per D-11). Add a new `PushFilePayload(BaseModel)` with `model_config = ConfigDict(extra="forbid")` and fields file_id (uuid.UUID), original_path (str), file_type (str), agent_id (str) — copy the ProcessFilePayload class header. Do NOT add a migration.
  </action>
  <verify>
    <automated>uv run pytest tests/test_models tests/test_payload.py -q 2>/dev/null; uv run python -c "from phaze.models.file import FileState; from phaze.schemas.agent_tasks import ProcessFilePayload, PushFilePayload; assert FileState.PUSHING=='pushing' and FileState.PUSHED=='pushed'; assert ProcessFilePayload.model_validate({'file_id':'00000000-0000-0000-0000-000000000000','original_path':'/x','file_type':'mp3','agent_id':'a','models_path':'/m'}).scratch_path is None"</automated>
  </verify>
  <acceptance_criteria>
    - The inline python assertion above exits 0 (states resolve; scratch_path defaults None).
    - `grep -c 'PUSHING\|PUSHED' src/phaze/models/file.py` ≥ 2.
    - `grep -q 'scratch_path' src/phaze/schemas/agent_tasks.py` and `grep -q 'class PushFilePayload' src/phaze/schemas/agent_tasks.py`.
    - No new file under `migrations/` or `alembic/` versions (no migration added).
  </acceptance_criteria>
  <done>Two new states, two new optional payload fields, and PushFilePayload exist; existing local-file payload construction is byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Add push-callback schemas (agent_push.py)</name>
  <files>src/phaze/schemas/agent_push.py</files>
  <read_first>
    - src/phaze/schemas/agent_tasks.py (Pydantic model + ConfigDict(extra="forbid") style)
    - src/phaze/routers/agent_analysis.py (the put_analysis / report_analysis_failed request+response shapes these mirror)
    - .planning/phases/50-push-pipeline/50-RESEARCH.md (§Critical Finding 1 + Open-Q2 — two endpoints: pushed + mismatch)
  </read_first>
  <action>
    Create `src/phaze/schemas/agent_push.py` defining request/response models for the two internal-API push callbacks (consumed by 50-03 client and 50-05 router): `PushedResponse` (e.g. file_id + status="pushed"), and `PushMismatchRequest`/`PushMismatchResponse` (e.g. status="mismatch", and a `cleared: bool` / next-state indicator mirroring the Phase-45 failure-response schemas). All models `extra="forbid"`. Keep field set minimal — file_id flows on the path, not the body (AUTH-01 discipline). No DB imports (schemas are import-safe for the Postgres-free agent client).
  </action>
  <verify>
    <automated>uv run python -c "import phaze.schemas.agent_push as m; assert hasattr(m,'PushedResponse')"; uv run ruff check src/phaze/schemas/agent_push.py; uv run mypy src/phaze/schemas/agent_push.py</automated>
  </verify>
  <acceptance_criteria>
    - Module imports cleanly; `PushedResponse` (and the mismatch schemas) are defined.
    - `uv run mypy src/phaze/schemas/agent_push.py` passes (strict).
    - `grep -L 'phaze.database\|phaze.models\|sqlalchemy' src/phaze/schemas/agent_push.py` returns the file (no DB/ORM imports).
  </acceptance_criteria>
  <done>The callback schema module exists, is strict-typed, and is ORM-free.</done>
</task>

<task type="auto">
  <name>Task 3: Add window/attempt + push/SSH/scratch config knobs with _FILE secrets</name>
  <files>src/phaze/config.py</files>
  <read_first>
    - src/phaze/config.py (cloud_route_threshold_sec on ControlSettings ~L365-371 = Field+AliasChoices analog; AgentSettings analysis knobs ~L433-503; SECRET_FILE_FIELDS override ~L389; _resolve_secret_files before-validator ~L82-145; AgentSettings.kind ~L415-419)
    - .planning/phases/50-push-pipeline/50-PATTERNS.md (config.py section — exact Field signatures + secret names)
    - .planning/phases/50-push-pipeline/50-CONTEXT.md (D-03, D-05, D-07, D-12)
  </read_first>
  <action>
    On ControlSettings add `cloud_max_in_flight: int` default 2 (gt=0, lt=100, AliasChoices("PHAZE_CLOUD_MAX_IN_FLIGHT","cloud_max_in_flight"), description naming it the load-bearing ≤N window, D-03) and `push_max_attempts: int` default 3 (gt=0, lt=20, alias PHAZE_PUSH_MAX_ATTEMPTS, D-12). On AgentSettings add push_ssh_host, push_ssh_user, cloud_scratch_dir (str | None), push_timeout_sec (int, default e.g. 600 — the rsync I/O-stall timeout), push_connect_timeout_sec (int, default e.g. 30), each Field + AliasChoices("PHAZE_*","..."). Add `push_ssh_key` and `push_known_hosts` (SecretStr | None) and extend `AgentSettings.SECRET_FILE_FIELDS` to include both names so the existing `_resolve_secret_files` validator auto-resolves their `<VAR>_FILE` siblings — add NO new resolution code. Bound numeric fields with gt/lt so out-of-range fails fast at startup. These values must NEVER be logged (D-13 secret discipline).
  </action>
  <verify>
    <automated>uv run python -c "from phaze.config import ControlSettings; c=ControlSettings(); assert c.cloud_max_in_flight==2 and c.push_max_attempts==3"; uv run mypy src/phaze/config.py; uv run pytest tests/test_config -q</automated>
  </verify>
  <acceptance_criteria>
    - Inline assertion passes (defaults 2 and 3).
    - `grep -q 'push_ssh_key' src/phaze/config.py` and `push_ssh_key` + `push_known_hosts` both appear inside the AgentSettings SECRET_FILE_FIELDS set.
    - `uv run pytest tests/test_config -q` is green (existing config tests + any _FILE-resolution tests).
    - `uv run mypy src/phaze/config.py` passes.
  </acceptance_criteria>
  <done>All window/attempt/push/scratch knobs exist with bounded Fields and _FILE secret resolution; defaults match D-03/D-12.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator → AgentSettings | SSH identity key + pinned known_hosts enter as `_FILE`-mounted secrets; must never be logged. |
| control payload → compute agent | expected_sha256 + scratch_path are pinned control-side and travel in ProcessFilePayload (validated, extra=forbid). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-secret-leak | Information Disclosure | push_ssh_key / push_known_hosts in config | mitigate | Added to SECRET_FILE_FIELDS (SecretStr, `_FILE` resolution); never logged (D-13 discipline); values provisioned in Phase 51, not committed. |
| T-50-config-oob | Tampering | cloud_max_in_flight / push_max_attempts / timeouts | mitigate | Bounded `Field(gt=…, lt=…)` so an out-of-range value fails fast at startup (cloud_route_threshold_sec precedent). |
| T-50-payload-inject | Tampering | ProcessFilePayload / PushFilePayload | mitigate | `extra="forbid"`; file_id is a server-generated UUID; scratch_path derived control-side from file_id, not from untrusted filenames. |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH audit); supply-chain gate N/A. |
</threat_model>

<verification>
`uv run pytest tests/test_config tests/test_models -q`, `uv run ruff check .`, and `uv run mypy .` are all green. The two new FileState members and payload fields resolve via the inline assertions. No Alembic migration was added (D-08).
</verification>

<success_criteria>
The contract layer is in place: PUSHING/PUSHED states, ProcessFilePayload scratch/integrity fields, PushFilePayload + push-callback schemas, and all config knobs with `_FILE` secrets — with zero behavior change to existing local-file analysis and no migration.
</success_criteria>

<output>
Create `.planning/phases/50-push-pipeline/50-01-SUMMARY.md` when done.
</output>
