---
phase: 50-push-pipeline
plan: 04
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - src/phaze/tasks/functions.py
  - src/phaze/services/analysis_enqueue.py
  - tests/test_process_file_scratch.py
autonomous: true
requirements: [CLOUDPIPE-03, CLOUDPIPE-04]

must_haves:
  truths:
    - "process_file reads payload.scratch_path when set, else payload.original_path (analyzer is path-agnostic)"
    - "When scratch_path + expected_sha256 are set, sha256 is verified off the event loop before analysis"
    - "On sha256 mismatch: scratch deleted + report_push_mismatch called + NO analysis runs"
    - "Scratch is deleted in a finally on success, timeout, crash, generic error, and mismatch (D-13)"
    - "enqueue_process_file threads optional expected_sha256 + scratch_path into ProcessFilePayload; the bulk local producer passing neither is byte-identical"
  artifacts:
    - path: "src/phaze/tasks/functions.py"
      provides: "scratch read-path swap + off-loop sha256 verify + finally cleanup"
      contains: "to_thread"
    - path: "src/phaze/services/analysis_enqueue.py"
      provides: "enqueue_process_file with expected_sha256/scratch_path kwargs"
      contains: "expected_sha256"
  key_links:
    - from: "src/phaze/tasks/functions.py"
      to: "compute_sha256"
      via: "asyncio.to_thread off-loop verify"
      pattern: "to_thread\\(compute_sha256"
---

<objective>
Make `process_file` integrity-verify and analyze the ephemeral scratch copy on the compute agent, clean up scratch in a `finally`, and extend the producer (`enqueue_process_file`) to carry the scratch path + expected sha256.

Purpose: CLOUDPIPE-03 (verify sha256 after transfer, before analysis; mismatch fails cleanly + triggers re-push) and CLOUDPIPE-04 (delete scratch on success OR terminal failure). The analyzer is already path-agnostic, so this is a read-path swap + verify + cleanup, not an analyzer change (D-11, D-12, D-13).
Output: `process_file` scratch handling; `enqueue_process_file` new kwargs. Tests live in `tests/test_process_file_scratch.py` (split from test_push_pipeline.py so this Wave-2 plan owns a disjoint test file from 50-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-push-pipeline/50-PATTERNS.md
@.planning/phases/50-push-pipeline/50-RESEARCH.md
@src/phaze/tasks/functions.py
@src/phaze/services/analysis_enqueue.py
@src/phaze/services/hashing.py

<interfaces>
<!-- From 50-01: ProcessFilePayload now has expected_sha256 + scratch_path (default None). -->
compute_sha256(Path) -> str  (services/hashing.py:10 — chunked stdlib; off-loop via asyncio.to_thread, pattern at scan.py:268)
process_file body: functions.py:139-220; TimeoutError at :169; ProcessExpired at :175; generic except at :179; PUT result at :201
read_path = payload.scratch_path or payload.original_path  (analysis.py is path-agnostic — verified)
mismatch: Path(scratch_path).unlink(missing_ok=True); await api.report_push_mismatch(file_id); return {"status":"push_mismatch"}
finally: if payload.scratch_path: Path(payload.scratch_path).unlink(missing_ok=True)
enqueue_process_file (analysis_enqueue.py:43-101): add keyword-only expected_sha256/scratch_path (mirror fine_cap/coarse_cap at :49-50); thread into ProcessFilePayload(...) at :66-77; key + timeout=7200/retries=2 unchanged.
Wave-0 stubs for the sha256 + cleanup selectors live in tests/test_process_file_scratch.py (created in 50-00).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: process_file scratch read + off-loop sha256 verify + finally cleanup</name>
  <files>src/phaze/tasks/functions.py, tests/test_process_file_scratch.py</files>
  <read_first>
    - src/phaze/tasks/functions.py (process_file L139-220; read-path use of payload.original_path L159-168; TimeoutError L169; ProcessExpired L175; generic except L179; PUT result L201)
    - src/phaze/services/hashing.py (compute_sha256 at L10 — pure stdlib, agent-boundary safe)
    - src/phaze/tasks/scan.py (the off-loop hash pattern `await asyncio.to_thread(compute_sha256, full_path)` at L268)
    - .planning/phases/50-push-pipeline/50-PATTERNS.md (functions.py section — read swap, verify, finally)
    - .planning/phases/50-push-pipeline/50-RESEARCH.md (§"sha256 verify off the event loop"; §"ProcessFilePayload extension + scratch read-path swap")
  </read_first>
  <behavior>
    - payload.scratch_path set → process_file analyzes that path; unset → analyzes payload.original_path (existing behavior).
    - scratch_path + expected_sha256 set and they match → analysis runs normally.
    - scratch_path + expected_sha256 set and they MISMATCH → scratch file unlinked, api.report_push_mismatch(file_id) awaited, analysis NOT invoked, returns a push_mismatch result.
    - scratch deleted in finally on: success, TimeoutError, ProcessExpired, generic except, and the mismatch early-return.
    - A short local file (neither field set) takes none of the scratch branches — behavior byte-identical.
  </behavior>
  <action>
    In process_file, compute `read_path = payload.scratch_path or payload.original_path` and pass it to `run_in_process_pool` (analyzer is path-agnostic). Before analysis, gated on `payload.scratch_path and payload.expected_sha256`, compute `actual = await asyncio.to_thread(compute_sha256, Path(payload.scratch_path))`; if `actual != payload.expected_sha256`: unlink the scratch file (missing_ok=True), `await api.report_push_mismatch(payload.file_id)`, and return a `{"file_id":..., "status":"push_mismatch"}` (do NOT analyze). Wrap the existing try/except analysis body so a `finally` runs `if payload.scratch_path: Path(payload.scratch_path).unlink(missing_ok=True)` on every exit path (success, TimeoutError, ProcessExpired, generic except, mismatch return). Keep the module Postgres-free (compute_sha256 is stdlib-safe). Convert the `-k sha256` and `-k cleanup` stubs in tests/test_process_file_scratch.py into real tests (mock the analyzer + api; assert unlink across exit paths and no-analyze-on-mismatch).
  </action>
  <verify>
    <automated>uv run pytest tests/test_process_file_scratch.py -q -k "sha256 or cleanup"; uv run pytest tests/test_task_split.py -q; uv run mypy src/phaze/tasks/functions.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_process_file_scratch.py -q -k "sha256 or cleanup"` passes: match→analyze; mismatch→unlink+report+no-analyze; finally-unlink on success and on each failure path.
    - `grep -q 'to_thread(compute_sha256' src/phaze/tasks/functions.py` and `grep -q 'report_push_mismatch' src/phaze/tasks/functions.py`.
    - `uv run pytest tests/test_task_split.py -q` green (functions.py still Postgres-free).
    - `uv run mypy src/phaze/tasks/functions.py` passes.
  </acceptance_criteria>
  <done>process_file verifies the scratch copy off-loop, refuses to analyze a corrupt transfer, and always cleans up scratch; local-file analysis is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread expected_sha256 + scratch_path through enqueue_process_file</name>
  <files>src/phaze/services/analysis_enqueue.py, tests/test_process_file_scratch.py</files>
  <read_first>
    - src/phaze/services/analysis_enqueue.py (enqueue_process_file L43-101; fine_cap/coarse_cap keyword-only additions L49-50; ProcessFilePayload construction L66-77; process_file_job_key L32-40; timeout=7200/retries=2 policy L94-99)
    - .planning/phases/50-push-pipeline/50-PATTERNS.md (analysis_enqueue.py section)
  </read_first>
  <behavior>
    - enqueue_process_file(..., expected_sha256=X, scratch_path=Y) builds a ProcessFilePayload carrying X and Y.
    - enqueue_process_file called WITHOUT the new kwargs builds a payload with both fields None (bulk local producer unchanged).
    - The deterministic key and timeout/retries policy are identical regardless of the new kwargs.
  </behavior>
  <action>
    Add keyword-only `expected_sha256: str | None = None` and `scratch_path: str | None = None` to the `enqueue_process_file` signature (mirror fine_cap/coarse_cap) and thread them into the `ProcessFilePayload(...)` construction. Leave `process_file_job_key`, `timeout=7200`, and `retries=2` untouched. Add a unit test in tests/test_process_file_scratch.py asserting the payload carries the values when supplied and stays None otherwise.
  </action>
  <verify>
    <automated>uv run pytest tests/test_process_file_scratch.py -q -k "enqueue or scratch"; uv run mypy src/phaze/services/analysis_enqueue.py</automated>
  </verify>
  <acceptance_criteria>
    - A unit test in tests/test_process_file_scratch.py proves the produced ProcessFilePayload carries expected_sha256+scratch_path when passed and None when omitted.
    - `grep -q 'expected_sha256' src/phaze/services/analysis_enqueue.py` and `grep -q 'scratch_path' src/phaze/services/analysis_enqueue.py`.
    - `uv run mypy src/phaze/services/analysis_enqueue.py` passes; deterministic key/policy unchanged (no edit to process_file_job_key / timeout / retries).
  </acceptance_criteria>
  <done>The producer can pin the scratch path + expected sha256 for a cloud file while leaving the bulk local path byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| transferred bytes → analyzer | a scratch file is untrusted until sha256-verified against the control-pinned expected hash. |
| compute scratch dir → disk | per-job cleanup bounds disk to the in-flight set. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-corrupt | Tampering | corrupt/partial transfer analyzed as valid | mitigate | off-loop sha256 verify before analysis; mismatch → delete + report + no-analyze; re-push driven control-side (CLOUDPIPE-03). |
| T-50-scratch-dos | Denial of Service | scratch accumulation | mitigate | `finally` unlink on every exit path (success/timeout/crash/mismatch) bounds disk to the in-flight set (CLOUDPIPE-04). |
| T-50-loop | Denial of Service | endless re-push on a persistently corrupt source | mitigate | re-push attempt cap is enforced control-side (50-05, push_max_attempts → ANALYSIS_FAILED); this plan only reports the mismatch. |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | No new packages; supply-chain gate N/A. |
</threat_model>

<verification>
`uv run pytest tests/test_process_file_scratch.py tests/test_task_split.py -q` green; `uv run ruff check .`, `uv run mypy .` pass. Short-file local analysis verified unchanged by the byte-identical-producer test.
</verification>

<success_criteria>
A cloud file is sha256-verified before analysis, a mismatch is reported without analyzing, scratch is always cleaned up, and the producer can pin scratch_path + expected_sha256 without altering the local-file path.
</success_criteria>

<output>
Create `.planning/phases/50-push-pipeline/50-04-SUMMARY.md` when done.
</output>
</content>
</invoke>
