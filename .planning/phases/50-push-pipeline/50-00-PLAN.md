---
phase: 50-push-pipeline
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_push_pipeline.py
  - tests/test_process_file_scratch.py
  - tests/test_staging_cron.py
  - tests/test_routing_seam.py
autonomous: true
requirements: [CLOUDPIPE-01, CLOUDPIPE-02, CLOUDPIPE-03, CLOUDPIPE-04, CLOUDPIPE-05]

must_haves:
  truths:
    - "The four new Nyquist test files exist and are collected by pytest"
    - "Every requirement (CLOUDPIPE-01..05) has at least one named test target reserved"
  artifacts:
    - path: "tests/test_push_pipeline.py"
      provides: "Stub tests for rsync argv, exit codes, and the compute-only startup janitor (CLOUDPIPE-02/-04)"
      contains: "import"
    - path: "tests/test_process_file_scratch.py"
      provides: "Stub tests for off-loop sha256 verify + scratch finally-cleanup in process_file (CLOUDPIPE-03/-04)"
      contains: "import"
    - path: "tests/test_staging_cron.py"
      provides: "Stub tests for the ≤N bounded-window controller cron (CLOUDPIPE-01/-05)"
      contains: "import"
    - path: "tests/test_routing_seam.py"
      provides: "Stub tests for the Phase 49 routing-seam reshape (CLOUDPIPE-01)"
      contains: "import"
  key_links:
    - from: "tests/test_push_pipeline.py"
      to: "pytest collection"
      via: "test functions marked xfail/skip until implemented"
      pattern: "def test_"
---

<objective>
Create the four new Nyquist test-stub files so every downstream implementation task has a concrete `<automated>` verify target before any production code is written.

Purpose: Nyquist validation is enabled — implementation tasks must point at a real test file, not "MISSING". This wave establishes the scaffolding (collectible, marked `xfail`/`skip` with reason) so the suite stays green while the contracts land. The push-pipeline selectors are split across two files so the Wave 2 plans that own them (50-03 → push_pipeline, 50-04 → process_file_scratch) never write the same test file in parallel.
Output: `tests/test_push_pipeline.py`, `tests/test_process_file_scratch.py`, `tests/test_staging_cron.py`, `tests/test_routing_seam.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-push-pipeline/50-VALIDATION.md
@.planning/phases/50-push-pipeline/50-RESEARCH.md
@tests/conftest.py
@tests/test_deterministic_key.py

<interfaces>
<!-- Test-file naming is fixed by 50-VALIDATION.md per-area map. Each stub reserves the -k selectors the implementation plans verify against. The push-pipeline selectors are split across two files so 50-03 and 50-04 (both Wave 2) own disjoint test files. -->
tests/test_push_pipeline.py selectors (50-03): -k argv, -k exit_code, -k janitor
tests/test_process_file_scratch.py selectors (50-04): -k sha256, -k cleanup
tests/test_staging_cron.py: ≤N window math (N=2: full→0 staged, 1 slot→1 staged, no-compute→no-op, FIFO order)
tests/test_routing_seam.py: long file → AWAITING_CLOUD only (no direct-to-compute enqueue)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create push-pipeline + process-file-scratch test stubs</name>
  <files>tests/test_push_pipeline.py, tests/test_process_file_scratch.py</files>
  <read_first>
    - .planning/phases/50-push-pipeline/50-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements — the exact -k selectors)
    - tests/conftest.py (existing async fixtures, fake SAQ, DB session — reuse, do NOT add a new framework)
    - tests/test_deterministic_key.py (style for a totality/guard test file)
  </read_first>
  <action>
    Create `tests/test_push_pipeline.py` with stub test functions covering, by name, the 50-03 selectors in 50-VALIDATION.md: `test_rsync_argv_no_shell_pinned_known_hosts` (-k argv), `test_rsync_exit_code_nonzero_raises_no_callback` (-k exit_code), `test_startup_janitor_compute_only_sweep` (-k janitor). Create `tests/test_process_file_scratch.py` with the 50-04 selectors: `test_sha256_mismatch_deletes_scratch_and_reports` (-k sha256), `test_scratch_cleanup_finally_on_all_exit_paths` (-k cleanup). Each body: `pytest.skip("Wave 0 stub — implemented in 50-03/50-04")` (or `@pytest.mark.xfail(reason=..., strict=False)`). The push_pipeline module docstring states these are Nyquist Wave 0 stubs for CLOUDPIPE-02/-04; the process_file_scratch docstring states CLOUDPIPE-03/-04. Do NOT import production modules that do not yet exist (push.py, the scratch read-path); keep imports to stdlib + pytest so collection succeeds.
  </action>
  <verify>
    <automated>uv run pytest tests/test_push_pipeline.py tests/test_process_file_scratch.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_push_pipeline.py tests/test_process_file_scratch.py -q` collects ≥5 tests total and exits 0 (all skipped/xfail, none errored on import).
    - `grep -c "def test_" tests/test_push_pipeline.py` returns ≥3; `grep -c "def test_" tests/test_process_file_scratch.py` returns ≥2.
    - Selectors resolve: `uv run pytest tests/test_push_pipeline.py -q -k argv` and `-k janitor` each collect ≥1 test; `uv run pytest tests/test_process_file_scratch.py -q -k sha256` and `-k cleanup` each collect ≥1 test.
  </acceptance_criteria>
  <done>Both files exist, are collectible, and reserve the five named selectors (argv/exit_code/janitor in push_pipeline; sha256/cleanup in process_file_scratch) with skip/xfail markers.</done>
</task>

<task type="auto">
  <name>Task 2: Create staging-cron + routing-seam test stubs</name>
  <files>tests/test_staging_cron.py, tests/test_routing_seam.py</files>
  <read_first>
    - .planning/phases/50-push-pipeline/50-VALIDATION.md (staging cron ≤N window + routing seam rows)
    - .planning/phases/50-push-pipeline/50-RESEARCH.md (§"Stay one ahead" controller cron; §Critical Finding 2 routing seam)
    - tests/conftest.py (DB session + fake SAQ fixtures)
  </read_first>
  <action>
    Create `tests/test_staging_cron.py` with stub functions `test_window_full_stages_zero`, `test_one_free_slot_stages_one`, `test_no_compute_agent_is_noop`, `test_fifo_oldest_awaiting_cloud_first` (selector default file run). Create `tests/test_routing_seam.py` with stub `test_long_file_routes_to_awaiting_cloud_not_compute` and `test_no_direct_to_compute_enqueue_path`. All bodies `pytest.skip("Wave 0 stub — implemented in 50-06")`. stdlib + pytest imports only (no production imports of not-yet-existing symbols). Docstrings name the requirement IDs (CLOUDPIPE-01/-05).
  </action>
  <verify>
    <automated>uv run pytest tests/test_staging_cron.py tests/test_routing_seam.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Both files collect (exit 0, all skipped) under `uv run pytest tests/test_staging_cron.py tests/test_routing_seam.py -q`.
    - `grep -c "def test_" tests/test_staging_cron.py` ≥4; `grep -c "def test_" tests/test_routing_seam.py` ≥2.
  </acceptance_criteria>
  <done>Both stub files exist, are collectible, and reserve the window-math and routing-seam selectors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| n/a (test scaffolding) | This plan adds no runtime surface; it creates skipped test stubs only. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-00-01 | Tampering | Test stubs masking real gaps | mitigate | Stubs use `skip`/`xfail` with a reason string and are converted to real assertions in 50-03/04/06 — they never assert-pass falsely. |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: zero new deps); supply-chain gate N/A. |
</threat_model>

<verification>
`uv run pytest tests/test_push_pipeline.py tests/test_process_file_scratch.py tests/test_staging_cron.py tests/test_routing_seam.py -q` exits 0 with all tests skipped/xfail and no import errors.
</verification>

<success_criteria>
Four new test files exist, are collectible, reserve every named selector the implementation plans verify against (split so 50-03 and 50-04 own disjoint test files), and keep the suite green.
</success_criteria>

<output>
Create `.planning/phases/50-push-pipeline/50-00-SUMMARY.md` when done.
</output>
</content>
</invoke>
