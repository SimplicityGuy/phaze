---
phase: 50-push-pipeline
plan: 06
type: execute
wave: 3
depends_on: ["50-01", "50-02"]
files_modified:
  - src/phaze/tasks/release_awaiting_cloud.py
  - src/phaze/tasks/controller.py
  - src/phaze/routers/pipeline.py
  - src/phaze/services/pipeline.py
  - tests/test_staging_cron.py
  - tests/test_routing_seam.py
autonomous: true
requirements: [CLOUDPIPE-01, CLOUDPIPE-05]

must_haves:
  truths:
    - "Every cloud-routed long file is held in AWAITING_CLOUD — there is no remaining direct-to-compute enqueue path in the router (the ≤N window cannot be bypassed)"
    - "stage_cloud_window counts the window from FileState IN {PUSHING, PUSHED}, computes slots = cloud_max_in_flight - window, and stages at most `slots` AWAITING_CLOUD files (FIFO by created_at) to push_file"
    - "With no online compute agent the cron is a clean no-op (hold, D-04)"
    - "The window is NEVER exceeded — a 144-file backlog stages ≤ N at a time"
    - "The cron is registered on the controller, replacing the release_awaiting_cloud drain cron; the Phase-42 no-general-auto-advance comment stays intact"
  artifacts:
    - path: "src/phaze/tasks/release_awaiting_cloud.py"
      provides: "stage_cloud_window bounded top-up cron"
      contains: "cloud_max_in_flight"
    - path: "src/phaze/services/pipeline.py"
      provides: "window-count helper over {PUSHING, PUSHED}"
      contains: "PUSHING"
    - path: "src/phaze/routers/pipeline.py"
      provides: "_route_discovered_by_duration long-file branch → AWAITING_CLOUD only"
      contains: "AWAITING_CLOUD"
  key_links:
    - from: "src/phaze/tasks/release_awaiting_cloud.py"
      to: "push_file enqueue on the fileserver queue"
      via: "stage up to `slots` AWAITING_CLOUD files, state→PUSHING"
      pattern: "push_file"
    - from: "src/phaze/tasks/controller.py"
      to: "CronJob(stage_cloud_window, '*/5 * * * *')"
      via: "cron_jobs registration"
      pattern: "stage_cloud_window"
---

<objective>
Make the ≤N window the single, unbypassable entry to the compute pipeline: reshape the Phase-49 routing seam so every cloud-routed long file holds in `AWAITING_CLOUD` (no direct-to-compute enqueue), and evolve `release_awaiting_cloud` into a `stage_cloud_window` cron that tops the window up to `cloud_max_in_flight` by staging `push_file` for the oldest held files.

Purpose: CLOUDPIPE-01 — the load-bearing invariant that a 144-file backlog can never blow up the compute scratch disk. The window is enforced in exactly one place (RESEARCH §Critical Finding 2, single-entry hold-then-stage), counted from committed `FileState` truth (D-08), and gated on an online compute agent (D-04).
Output: reshaped router seam; `stage_cloud_window` cron + registration; window-count helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-push-pipeline/50-PATTERNS.md
@.planning/phases/50-push-pipeline/50-RESEARCH.md
@src/phaze/tasks/release_awaiting_cloud.py
@src/phaze/tasks/controller.py
@src/phaze/routers/pipeline.py
@src/phaze/services/pipeline.py

<interfaces>
<!-- From 50-01: cloud_max_in_flight (ControlSettings), FileState.PUSHING/PUSHED. From 50-02: push_file keyed + routable (AGENT_TASKS). -->
cron template: release_awaiting_cloud.py:44-92 (open async_session; gate on select_active_agent(kind="compute") wrapped in try/except NoActiveAgentError → zero no-op; resolve queue via ctx["task_router"].queue_for(fileserver_agent.id); loop-enqueue; commit once; return {"staged": N, "skipped": M}).
window math (RESEARCH §"Stay one ahead" cron L271-293): window = COUNT(state IN {PUSHING, PUSHED}); slots = cloud_max_in_flight - window; if slots<=0 return zero; SELECT ... WHERE state==AWAITING_CLOUD ORDER BY created_at ASC LIMIT slots [FOR UPDATE SKIP LOCKED optional]; per file state=PUSHING then enqueue push_file (fileserver queue). Do count+select+state=PUSHING in ONE transaction.
push_file staging needs a fileserver agent: select_active_agent(session, kind="fileserver") for the push initiator.
controller registration: controller.py:41 import, :212 functions[], :232 CronJob(..., "*/5 * * * *"); REPLACE the release_awaiting_cloud cron; keep the Phase-42 "DO NOT re-add a general auto-advance cron" comment (:220-231).
router seam: pipeline.py:306-313 long-file branch — drop the compute_agent!=None→cloud_files direct enqueue (:294-299,302,328-331); always file.state=AWAITING_CLOUD for a long file. Fileserver/local path (:314-315,324-327) unchanged.
window-count helper: services/pipeline.py — select(func.count()).where(FileRecord.state.in_([PUSHING, PUSHED])); candidate SELECT reuses get_files_by_state(AWAITING_CLOUD) + ORDER BY created_at ASC LIMIT slots.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reshape the routing seam to single-entry AWAITING_CLOUD hold</name>
  <files>src/phaze/routers/pipeline.py, tests/test_routing_seam.py</files>
  <read_first>
    - src/phaze/routers/pipeline.py (_route_discovered_by_duration L252-339; the long-file branch + compute direct-enqueue L294-302,306-313,328-331; the fileserver/local path L314-315,324-327)
    - .planning/phases/50-push-pipeline/50-RESEARCH.md (§Critical Finding 2 — single-entry hold-then-stage; the two direct-to-compute sites to remove; the AWAITING_CLOUD semantics shift)
    - .planning/phases/50-push-pipeline/50-PATTERNS.md (routers/pipeline.py section — routing seam reshape)
  </read_first>
  <behavior>
    - A long file in _route_discovered_by_duration always gets state = AWAITING_CLOUD, regardless of whether a compute agent is online.
    - There is NO branch in the router that enqueues process_file (or push_file) directly onto a compute queue for a long file.
    - The short/local-file path is unchanged (still enqueues process_file on the fileserver/local path as before).
  </behavior>
  <action>
    In `_route_discovered_by_duration`, replace the long-file branch so a long file is ALWAYS set to `FileState.AWAITING_CLOUD` (held regardless of compute availability — the staging cron stages it). Remove the `compute_agent is not None → cloud_files → _enqueue_analysis_jobs(compute_q, …)` direct-to-compute enqueue and the now-dead `cloud_files`/`compute_q`/`compute_agent` locals. Leave the short-file fileserver/local path untouched. This removes the only direct-to-compute path so the ≤N window cannot be bypassed (Pitfall 1). Convert tests/test_routing_seam.py stubs into real tests (long file → AWAITING_CLOUD; assert no compute enqueue is made).
  </action>
  <verify>
    <automated>uv run pytest tests/test_routing_seam.py -q; uv run pytest tests/test_no_default_queue_producers.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_routing_seam.py -q` passes: a long file routes to AWAITING_CLOUD; no direct compute enqueue is invoked.
    - `grep -c 'cloud_files' src/phaze/routers/pipeline.py` reflects removal of the direct-enqueue locals (no remaining direct-to-compute enqueue in `_route_discovered_by_duration`).
    - Short-file behavior tests stay green (existing pipeline router tests).
  </acceptance_criteria>
  <done>The router holds every long file in AWAITING_CLOUD with no direct-to-compute path; the window is now enforceable in one place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: stage_cloud_window bounded top-up cron + window-count helper</name>
  <files>src/phaze/tasks/release_awaiting_cloud.py, src/phaze/services/pipeline.py, tests/test_staging_cron.py</files>
  <read_first>
    - src/phaze/tasks/release_awaiting_cloud.py (the whole file — Phase 49 cron template; the gate + commit-once + return shape L44-92; the job-is-None dedup → skipped contract L80-89; the CONTROL-ONLY module docstring L1-26)
    - .planning/phases/50-push-pipeline/50-RESEARCH.md (§"Stay one ahead" controller cron L271-293 — window math, FIFO ordering, single-transaction TOCTOU analysis)
    - src/phaze/services/pipeline.py (get_awaiting_cloud_count L805-816; _safe_count L272-289; get_files_by_state L725-737)
    - .planning/phases/50-push-pipeline/50-PATTERNS.md (stage_cloud_window section; services/pipeline.py window-count section)
  </read_first>
  <behavior>
    - window full (COUNT(PUSHING+PUSHED) >= cloud_max_in_flight) → stages 0.
    - one free slot → stages exactly 1 (the oldest AWAITING_CLOUD by created_at).
    - no online compute agent → clean no-op (staged=0), file stays AWAITING_CLOUD.
    - given a 144-file AWAITING_CLOUD backlog and N=2 with an empty window, a single tick stages at most 2 (never the whole backlog).
    - staged files transition AWAITING_CLOUD → PUSHING and enqueue push_file on the fileserver queue; a double-tick collapses via the push_file:<id> deterministic key (counted as skipped).
  </behavior>
  <action>
    Evolve `release_awaiting_cloud.py`: rename/replace the cron function with `stage_cloud_window(ctx)`. Add a window-count helper in `services/pipeline.py` (clone get_awaiting_cloud_count, `state.in_([FileState.PUSHING, FileState.PUSHED])`, `_safe_count`). In the cron: gate on `select_active_agent(session, kind="compute")` (try/except NoActiveAgentError → return {"staged":0,"skipped":0}); compute `window` via the helper and `slots = cloud_max_in_flight - window`; if `slots <= 0` return zero; SELECT up to `slots` AWAITING_CLOUD files `ORDER BY created_at ASC LIMIT slots` (recommend `FOR UPDATE SKIP LOCKED`); resolve the fileserver queue via `select_active_agent(session, kind="fileserver")` + `ctx["task_router"].queue_for(...)`; for each file set `state=FileState.PUSHING` and enqueue push_file (carrying file_id/original_path/file_type/agent_id for PushFilePayload); do the COUNT+SELECT+state=PUSHING in ONE transaction; commit once; return {"staged":N,"skipped":M}. Keep the CONTROL-ONLY docstring + the no-op/dedup contract. Convert tests/test_staging_cron.py stubs into real tests (window full→0, 1 slot→1, no-compute→no-op, FIFO order, 144-backlog→≤N).
  </action>
  <verify>
    <automated>uv run pytest tests/test_staging_cron.py -q; uv run mypy src/phaze/tasks/release_awaiting_cloud.py src/phaze/services/pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_staging_cron.py -q` passes every window-math case incl. the 144-file-backlog → ≤N assertion and FIFO ordering.
    - `grep -q 'in_(\[FileState.PUSHING' src/phaze/services/pipeline.py` (window counted from committed state, not the ledger).
    - `grep -q 'cloud_max_in_flight' src/phaze/tasks/release_awaiting_cloud.py` and the cron sets `state = FileState.PUSHING` before enqueueing push_file.
    - `uv run mypy` passes on both files.
  </acceptance_criteria>
  <done>The cron tops the window up to N from the oldest held files, never exceeds N, holds cleanly when no compute agent is online, and dedups double-ticks.</done>
</task>

<task type="auto">
  <name>Task 3: Register stage_cloud_window on the controller (replace the drain cron)</name>
  <files>src/phaze/tasks/controller.py</files>
  <read_first>
    - src/phaze/tasks/controller.py (release_awaiting_cloud import L41; functions[] L205-212; CronJob(release_awaiting_cloud, "*/5 * * * *") L232; the Phase-42 "DO NOT re-add a general auto-advance cron" comment L220-231)
    - .planning/phases/50-push-pipeline/50-PATTERNS.md (controller.py section)
    - .planning/phases/50-push-pipeline/50-RESEARCH.md (§"State of the Art" — the drain cron is deprecated, replaced by the bounded top-up)
  </read_first>
  <action>
    Import `stage_cloud_window`, add it to `settings["functions"]`, and register `CronJob(stage_cloud_window, cron="*/5 * * * *")` in `cron_jobs`, REPLACING the `release_awaiting_cloud` cron registration (its "drain ALL → process_file" behavior is deprecated). Keep the Phase-42 no-general-auto-advance comment intact — note inline that this cron is scoped only to the bounded cloud-window top-up and is gated on an online compute agent.
  </action>
  <verify>
    <automated>uv run pytest tests/test_tasks -q -k "controller or cron"; uv run python -c "import phaze.tasks.controller"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'stage_cloud_window' src/phaze/tasks/controller.py` in both the functions list and a CronJob registration; the old `release_awaiting_cloud` CronJob registration is gone.
    - `uv run python -c "import phaze.tasks.controller"` imports cleanly.
    - Controller/cron tests stay green; the Phase-42 comment is still present.
  </acceptance_criteria>
  <done>The controller runs exactly one bounded cloud-window cron; the deprecated drain cron is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| operator action (Run analysis / backfill) → pipeline | a large corpus must funnel through one bounded enqueue, not stage N×144 pushes. |
| controller cron → fileserver queue | the single driver that introduces new push work; counted from committed FileState. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-scratch-dos | Denial of Service | exceeding the ≤N window (144-file blowup) | mitigate | single-entry hold-then-stage; window counted from FileState IN {PUSHING,PUSHED}; count+select+state=PUSHING in one transaction; deterministic-key double-tick backstop (CLOUDPIPE-01). |
| T-50-bypass | Tampering | a leftover direct-to-compute enqueue path | mitigate | the router long-file branch is reshaped to AWAITING_CLOUD-only; no router/cron path enqueues compute analysis outside the window. |
| T-50-double-enqueue | Tampering | double cron tick / double click | mitigate | push_file:<id> deterministic key collapses repeats (counted skipped); committed PUSHING transition makes the next tick's count current. |
| T-50-runaway-cron | Denial of Service | a general auto-advance cron creeping back | accept | scoped strictly to the bounded top-up + gated on an online compute agent; Phase-42 guard comment retained (no steady-state auto-enqueue). |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | No new packages; supply-chain gate N/A. |
</threat_model>

<verification>
`uv run pytest tests/test_staging_cron.py tests/test_routing_seam.py tests/test_tasks -q` green; `uv run ruff check .`, `uv run mypy .` pass. The ≤N invariant is proven by the window-math unit tests including the 144-file backlog case.
</verification>

<success_criteria>
Every cloud-routed long file holds in AWAITING_CLOUD with no bypass path, and a single controller cron tops the window up to `cloud_max_in_flight` from the oldest held files — the window is never exceeded regardless of corpus size.
</success_criteria>

<output>
Create `.planning/phases/50-push-pipeline/50-06-SUMMARY.md` when done.
</output>
