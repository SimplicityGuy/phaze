---
phase: 89-legacy-scan-path-deletion-sentinel-reattribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/routers/scan.py
  - src/phaze/services/ingestion.py
  - src/phaze/schemas/scan.py
  - src/phaze/main.py
  - src/phaze/models/file.py
  - src/phaze/models/scan_batch.py
  - tests/conftest.py
  - tests/discovery/routers/test_scan.py
  - tests/discovery/services/test_ingestion.py
  - tests/discovery/test_rescan_preserves_state.py
  - tests/shared/core/test_phase02_gaps.py
  - tests/shared/core/test_no_auto_metadata_enqueue.py
  - tests/metadata/tasks/test_metadata_extraction.py
  - tests/agents/services/test_agent_upsert.py
  - tests/integration/test_stage_progress_buckets.py
  - tests/integration/test_pending_set_divergence.py
  - tests/integration/test_stage_status_equivalence.py
  - tests/integration/test_dedup_divergence.py
  - tests/integration/test_orphan_count.py
  - tests/integration/test_dedup_resolve_undo_shadow.py
  - tests/integration/test_fingerprint_progress.py
  - tests/integration/test_files_page.py
  - tests/integration/test_shadow_compare_skipped.py
  - tests/integration/test_enrich_pending_independence.py
  - tests/integration/test_shadow_compare.py
autonomous: true
requirements: [LEGACY-01, LEGACY-03]

must_haves:
  truths:
    - "No `POST /api/v1/scan` or `GET /api/v1/scan/{batch_id}` route exists; the app boots without the scan router (LEGACY-01, D-04)"
    - "Deleting `GET /api/v1/scan/{batch_id}` is safe: no template/JS/homelab consumer references it — the D-05 contingency was discharged by research (RESEARCH.md §D-05 sweep); a future batch-status API would be built fresh on the pipeline surface, not preserved here (D-05)"
    - "No source references the trio `run_scan`/`discover_and_hash_files`/`bulk_upsert_files` (LEGACY-01, D-04)"
    - "`agent_id` has no Python model `default=`; constructing FileRecord/ScanBatch requires an explicit agent_id (LEGACY-03, D-06)"
    - "The shared test seed provides a real `kind='fileserver'` agent (`test-fileserver`), not the sentinel (LEGACY-03, D-08)"
    - "The full test suite is green after deletion — no dangling imports, no NOT-NULL/FK flush failures (LEGACY-01/03)"
  artifacts:
    - path: "src/phaze/main.py"
      provides: "App router wiring without the legacy scan router"
      contains: "no include_router(scan.router)"
    - path: "src/phaze/models/file.py"
      provides: "agent_id mapped column without default="
    - path: "src/phaze/models/scan_batch.py"
      provides: "agent_id mapped column without default="
    - path: "tests/conftest.py"
      provides: "test-fileserver seed"
      contains: "kind=\"fileserver\""
  key_links:
    - from: "src/phaze/main.py"
      to: "removed routers/scan.py"
      via: "deleted import + deleted include_router"
      pattern: "include_router\\(scan"
    - from: "tests/integration/*"
      to: "test-fileserver agent"
      via: "_LEGACY_AGENT_ID constant repoint (D-08)"
      pattern: "test-fileserver"
---

<objective>
Delete the orphaned legacy scan path wholesale and drop the `agent_id` Python model default, keeping the entire test suite green. This delivers LEGACY-01 (source + test deletion) and the model-code half of LEGACY-03 (default removal). It removes two surviving `FileState`-writing upsert sites (inside the deleted `discover_and_hash_files`/`bulk_upsert_files`), shrinking the Phase 90 migration surface.

Purpose: Retire the dead `POST /api/v1/scan` → `run_scan` ingestion trio and make `agent_id` a required construction argument, so no new row can ever be attributed to `legacy-application-server`. The FK ownership model (`agent_id` = owning fileserver) is preserved.
Output: Three deleted source files, unwired `main.py`, two edited models, and a repointed/pruned test tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-legacy-scan-path-deletion-sentinel-reattribution/89-CONTEXT.md
@.planning/phases/89-legacy-scan-path-deletion-sentinel-reattribution/89-RESEARCH.md
@.planning/phases/89-legacy-scan-path-deletion-sentinel-reattribution/89-PATTERNS.md

<interfaces>
<!-- Verified deletion facts (grep-confirmed). Executor uses these directly. -->
- Only NON-comment importer of `phaze.services.ingestion` in src/ is `routers/scan.py:18` (`from phaze.services.ingestion import run_scan`). `agent_files.py` + `tasks/scan.py` reference "services/ingestion.py" in PROSE COMMENTS only (not imports) — leave those comments.
- Only importer of `phaze.schemas.scan` in src/ is `routers/scan.py:16`.
- `main.py`: `scan,` appears in the router-import tuple at L43; `app.include_router(scan.router)` at L193. Remove both.
- `models/file.py:92` and `models/scan_batch.py:33` each hold `default="legacy-application-server"` on the `agent_id` mapped_column. Drop ONLY the `default=` kwarg — KEEP `nullable=False` and `ForeignKey(..., ondelete="RESTRICT")`.
- `models/agent.py:14` `LEGACY_AGENT_ID` constant — KEEP (still test-referenced + labels historical DB data).
- `tests/conftest.py:17` imports `LEGACY_AGENT_ID`; `:212` seeds `Agent(id=LEGACY_AGENT_ID, name=LEGACY_AGENT_ID, scan_roots=[])`. The current seed OMITS `kind`, leaning on DB `server_default='fileserver'` (agent.py:28) — the new seed must set `kind="fileserver"` EXPLICITLY.
- 13 files under tests/integration/ carry a `_LEGACY_AGENT_ID` constant to repoint to `"test-fileserver"` (D-08). `test_pending_set_divergence.py` + `test_enrich_pending_independence.py` have a sibling-bucket "re-add if missing" guard with a stale comment to update.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint test fixtures and constants for required agent_id (D-08, Pitfall 2)</name>
  <files>tests/conftest.py, tests/integration/test_stage_progress_buckets.py, tests/integration/test_pending_set_divergence.py, tests/integration/test_stage_status_equivalence.py, tests/integration/test_dedup_divergence.py, tests/integration/test_orphan_count.py, tests/integration/test_dedup_resolve_undo_shadow.py, tests/integration/test_fingerprint_progress.py, tests/integration/test_files_page.py, tests/integration/test_shadow_compare_skipped.py, tests/integration/test_enrich_pending_independence.py, tests/integration/test_shadow_compare.py</files>
  <read_first>
    - tests/conftest.py (L17 import, L198-217 seed fixture + docstring)
    - .planning/phases/89-legacy-scan-path-deletion-sentinel-reattribution/89-PATTERNS.md (`tests/conftest.py L198-217` section + Category C list)
    - .planning/phases/89-legacy-scan-path-deletion-sentinel-reattribution/89-RESEARCH.md (Test Blast Radius Categories C/D + Pitfall 2)
    - tests/integration/test_pending_set_divergence.py + tests/integration/test_enrich_pending_independence.py (the re-add-if-missing sibling-bucket guards)
  </read_first>
  <action>
    Do this BEFORE any source/default removal so the suite stays green throughout (repointing to a real fileserver is harmless while the default still exists).
    1. In tests/conftest.py: change the seed at ~L212 from `Agent(id=LEGACY_AGENT_ID, name=LEGACY_AGENT_ID, scan_roots=[])` to `Agent(id="test-fileserver", name="test-fileserver", kind="fileserver", scan_roots=[])` — set `kind` EXPLICITLY (D-08). Update the fixture docstring (~L200-206) which references "the model-level default added in phase 24" (that default is being removed in Task 2). Drop the now-unused `LEGACY_AGENT_ID` from the L17 import if nothing else in the file uses it; otherwise keep it.
    2. Repoint the `_LEGACY_AGENT_ID` module constant to `"test-fileserver"` in all 13 Category-C integration tests listed in PATTERNS.md (test_stage_progress_buckets, test_pending_set_divergence, test_stage_status_equivalence, test_dedup_divergence, test_orphan_count, test_dedup_resolve_undo_shadow, test_fingerprint_progress, test_files_page, test_shadow_compare_skipped, test_enrich_pending_independence, test_shadow_compare — plus any other file grep surfaces). In test_pending_set_divergence.py and test_enrich_pending_independence.py, also update the stale sibling-bucket "re-add the agent if missing" comment to name test-fileserver.
    3. Pitfall 2 sweep: grep the whole tests/ tree for `FileRecord(` and `ScanBatch(` constructions; any that rely on the soon-removed default (no explicit `agent_id`) must pass `agent_id="test-fileserver"` explicitly. Most already pass a constant — repoint those constants too. This prevents NOT-NULL + FK flush failures once Task 2 drops the default.
    Do NOT touch Category-E historical migration tests (test_012..test_037, test_016/017/024) — they pin pre-038 revisions where the sentinel still exists.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_stage_status_equivalence.py tests/integration/test_files_page.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "_LEGACY_AGENT_ID" tests/integration/ | xargs grep -L "test-fileserver"` returns nothing (every Category-C file repointed).
    - `grep -n "test-fileserver" tests/conftest.py` shows the reseeded fileserver with `kind="fileserver"`.
    - `uv run pytest tests/integration/test_stage_status_equivalence.py tests/integration/test_files_page.py -x` exits 0 (suite still green with the default still present).
  </acceptance_criteria>
  <done>Shared seed is a real fileserver; all row-constructing tests pass an explicit agent_id; suite green before any source removal.</done>
</task>

<task type="auto">
  <name>Task 2: Delete legacy scan path + drop model defaults + prune orphaned tests (LEGACY-01, LEGACY-03 model half)</name>
  <files>src/phaze/routers/scan.py, src/phaze/services/ingestion.py, src/phaze/schemas/scan.py, src/phaze/main.py, src/phaze/models/file.py, src/phaze/models/scan_batch.py, tests/discovery/routers/test_scan.py, tests/discovery/services/test_ingestion.py, tests/discovery/test_rescan_preserves_state.py, tests/shared/core/test_phase02_gaps.py, tests/shared/core/test_no_auto_metadata_enqueue.py, tests/metadata/tasks/test_metadata_extraction.py, tests/agents/services/test_agent_upsert.py</files>
  <read_first>
    - src/phaze/routers/scan.py (whole file — being deleted)
    - src/phaze/services/ingestion.py (whole file — being deleted; trio at L47/L94/L128, residue normalize_path/classify_file have no source caller)
    - src/phaze/schemas/scan.py (whole file — being deleted)
    - src/phaze/main.py (L43 import tuple, L193 include_router)
    - src/phaze/models/file.py (L88-95 agent_id column) + src/phaze/models/scan_batch.py (L29-34 agent_id column)
    - tests/agents/services/test_agent_upsert.py (Pitfall 3 twin — must cover agent_files.py ON-CONFLICT state-preservation)
    - .planning/phases/89-legacy-scan-path-deletion-sentinel-reattribution/89-RESEARCH.md (Deletion Completeness Map, Pitfall 3, Test Blast Radius A/B)
  </read_first>
  <action>
    1. Delete wholesale: src/phaze/routers/scan.py, src/phaze/services/ingestion.py (D-recommended wholesale delete — normalize_path/classify_file have zero source callers per A1; tasks/scan.py has its OWN `_classify`), and src/phaze/schemas/scan.py (orphaned once the router goes).
    2. In src/phaze/main.py: remove `scan,` from the router-import tuple (L43) and remove `app.include_router(scan.router)` (L193). Leave pipeline_scans / agent_scan_batches untouched.
    3. In src/phaze/models/file.py (L92) and src/phaze/models/scan_batch.py (L33): drop the `default="legacy-application-server"` kwarg on the `agent_id` mapped_column. KEEP `nullable=False` and the RESTRICT `ForeignKey` (D-06/D-07 — no DB server_default exists, so this is a pure model-code change, no DDL).
    4. Delete the now-orphaned test files (Category A): tests/discovery/routers/test_scan.py, tests/discovery/services/test_ingestion.py, tests/discovery/test_rescan_preserves_state.py. Before deleting test_rescan_preserves_state.py, CONFIRM tests/agents/services/test_agent_upsert.py asserts the surviving ON-CONFLICT state-preservation twin at agent_files.py:136 (Pitfall 3); if it does not, port that assertion into test_agent_upsert.py so the MIG-03 rescan-preserve invariant stays covered.
    5. Trim Category-B tests (remove ONLY the run_scan blocks, keep the rest): tests/shared/core/test_phase02_gaps.py (L22 import, L70-229 run_scan orchestration tests — if no non-run_scan tests remain, delete the file); tests/shared/core/test_no_auto_metadata_enqueue.py (L8, L28, L103-143 run_scan portion; keep the surviving scan_directory/agent portion). In tests/metadata/tasks/test_metadata_extraction.py (L232-235) update/remove the stale run_scan comment only.
    6. KEEP models/agent.py:14 LEGACY_AGENT_ID constant.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/routers/test_pipeline.py tests/agents/services/test_agent_upsert.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/phaze/routers/scan.py || test -f src/phaze/services/ingestion.py || test -f src/phaze/schemas/scan.py` is false (all three deleted).
    - `grep -rn "run_scan\|discover_and_hash_files\|bulk_upsert_files" src/phaze/` returns nothing.
    - `grep -n "include_router(scan.router)" src/phaze/main.py` returns nothing.
    - `grep -rn "legacy-application-server" src/phaze/models/` returns nothing (defaults gone).
    - `uv run python -c "import phaze.main"` exits 0 (no dangling imports).
    - `uv run pytest tests/shared/routers/test_pipeline.py tests/agents/services/test_agent_upsert.py -x` exits 0 (app boots without the scan router; Pitfall-3 twin covered).
    - Full suite: `uv run pytest -q` exits 0 (no NOT-NULL/FK flush failures from the removed default).
  </acceptance_criteria>
  <done>Legacy scan path + both model defaults removed; orphaned tests pruned; app boots and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | `POST /api/v1/scan` was an untrusted HTTP entrypoint; deleting it shrinks the attack surface. The surviving `agent_files` path stamps `agent_id` from the bearer-token auth dep (AUTH-01), never from the body. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01-01 | Elevation/Tampering | deleted `POST /api/v1/scan` legacy trigger | mitigate | Delete the router wholesale (D-04); surviving ingest is auth-gated via `agent_files.py` (agent_id from auth dep, never body). Verified no live consumer (D-05). |
| T-89-01-02 | Tampering | `agent_id` required (default removed) | mitigate | Dropping the Python default makes `agent_id` a required construction arg — no row can silently be attributed to the sentinel; every surviving write site supplies a real agent_id (grep-verified). |
| T-89-01-SC | Tampering | package installs | accept | No packages installed in this plan (pure source deletion + model edit); RESEARCH Package Legitimacy Audit = N/A. |
</threat_model>

<verification>
- App boots without the scan router (`import phaze.main` + test_pipeline app fixture green).
- No dangling references to the deleted trio or router in src/.
- `agent_id` is required at the model level; the full suite passes with the shared test-fileserver seed.
- `pre-commit run --all-files` clean (ruff line-150/py313, mypy strict excludes tests, bandit).
</verification>

<success_criteria>
- LEGACY-01: `POST /api/v1/scan`, `run_scan`, `discover_and_hash_files`, `bulk_upsert_files`, `routers/scan.py`, `services/ingestion.py`, `schemas/scan.py` deleted; `main.py` unwired; no new row can be attributed to `legacy-application-server`.
- LEGACY-03 (model half): `default="legacy-application-server"` dropped from both models; `agent_id` required.
- Full `uv run pytest` green + `pre-commit run --all-files` clean + coverage ≥ 90% (net-neutral: source and its tests removed together).
</success_criteria>

<output>
Create `.planning/phases/89-legacy-scan-path-deletion-sentinel-reattribution/89-01-SUMMARY.md` when done
</output>
