---
phase: 57-shell-dag-rail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/templates/base.html
  - static/vendor/tailwindcss-browser-4.3.2.min.js
  - static/vendor/tailwindcss-browser-4.3.0.min.js
  - tests/test_dead_template_guard.py
autonomous: true
requirements: [SHELL-04, SHELL-05]

must_haves:
  truths:
    - "Browser-delivered htmx is htmx 2.0.10 and Alpine is 3.15.12 with SRI hashes matching the served CDN bytes (no silent script block)"
    - "Vendored Tailwind is 4.3.2; the stale 4.3.0 file is gone"
    - "A dead-template AST guard test exists and is GREEN against the current repo (no orphaned Jinja2 templates)"
  artifacts:
    - path: "src/phaze/templates/base.html"
      provides: "htmx 2.0.10 + Alpine 3.15.12 SRI-pinned script tags; Tailwind 4.3.2 vendored filename"
      contains: "htmx.org@2.0.10"
    - path: "static/vendor/tailwindcss-browser-4.3.2.min.js"
      provides: "vendored Tailwind v4.3.2 browser build"
    - path: "tests/test_dead_template_guard.py"
      provides: "jinja2.meta orphan-template AST guard, seeded green"
      contains: "find_referenced_templates"
  key_links:
    - from: "tests/test_base_html_sri.py"
      to: "src/phaze/templates/base.html"
      via: "static full-semver pin check + live-CDN SHA-384 recompute"
      pattern: "htmx.org@2.0.10"
    - from: "tests/test_dead_template_guard.py"
      to: "src/phaze/routers/*.py + src/phaze/templates/**/*.html"
      via: "entry set = literal name=\"*.html\" in routers; reachable = transitive extends/include closure"
      pattern: "find_referenced_templates"
---

<objective>
Bump the three browser-delivered libs to the ROADMAP-locked versions (htmx 2.0.10 / Alpine 3.15.12 / Tailwind 4.3.2) with recomputed SRI, and seed the dead-template AST guard GREEN. This is the independent foundation wave: no shell code yet, no file overlap with the shell plans.

Purpose: A stale `integrity=` hash silently blocks htmx/Alpine (rail stops swapping, theme dies) with no obvious console error — getting the exact hashes in first de-risks every later plan. The dead-template guard must be seeded green now and watched green through the v7.0 cutover (Phase 62 / CUT-02 removes legacy templates and the guard proves nothing dangles).

Output: base.html with bumped + re-SRI'd scripts, the 4.3.2 vendored asset (4.3.0 deleted), and tests/test_dead_template_guard.py passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-shell-dag-rail/57-RESEARCH.md
@.planning/phases/57-shell-dag-rail/57-PATTERNS.md

<interfaces>
<!-- Exact bumped script tags (from 57-RESEARCH.md "Stack version bumps"). Use verbatim. -->
<!-- base.html:34 htmx → -->
<!--   src="https://unpkg.com/htmx.org@2.0.10/dist/htmx.min.js" -->
<!--   integrity="sha384-H5SrcfygHmAuTDZphMHqBJLc3FhssKjG7w/CeCpFReSfwBWDTKpkzPP8c+cLsK+V" -->
<!-- base.html:40 Alpine → -->
<!--   src="https://cdn.jsdelivr.net/npm/alpinejs@3.15.12/dist/cdn.min.js" -->
<!--   integrity="sha384-LUONAH/vnlbGK96OtMBbN0l0Fcsr7dW3BK7NOImE4oHZAZ/IwIEvvpxyajWxvpaD" -->
<!-- base.html:28 Tailwind (NO integrity attr — vendored local file) → -->
<!--   src="/static/vendor/tailwindcss-browser-4.3.2.min.js" -->
<!-- base.html:37 htmx-ext-sse@2.2.4 → DO NOT TOUCH (not in bump list; SRI test still validates it) -->
<!-- Tailwind download URL: https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4.3.2/dist/index.global.min.js -->

<!-- Dead-template guard implementation is supplied in full in 57-RESEARCH.md "Code Examples". -->
<!-- entry set: re.findall(r'name=["\']([^"\']+\.html)["\']', router_src) over src/phaze/routers/*.py -->
<!-- reachable: transitive jinja2.meta.find_referenced_templates closure; orphan = templates/**/*.html reached by nobody -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump htmx/Alpine SRI + swap vendored Tailwind to 4.3.2</name>
  <files>src/phaze/templates/base.html, static/vendor/tailwindcss-browser-4.3.2.min.js</files>
  <read_first>
    - src/phaze/templates/base.html (lines 21-42: the vendored-Tailwind comment + script L28, htmx L34, htmx-ext-sse L37, Alpine L40)
    - tests/test_base_html_sri.py (the existing guard that validates these — static full-semver pin + live-CDN SHA-384 recompute)
    - .planning/phases/57-shell-dag-rail/57-RESEARCH.md § "Stack version bumps — exact SRI hashes" (the verified hash table + the 3 critical scoping notes)
    - .planning/phases/57-shell-dag-rail/57-PATTERNS.md § "src/phaze/templates/base.html (MOD)" (the 3 edits + the htmx-ext-sse do-not-touch note)
  </read_first>
  <action>
    Download the Tailwind 4.3.2 browser build from https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4.3.2/dist/index.global.min.js into static/vendor/tailwindcss-browser-4.3.2.min.js, then delete static/vendor/tailwindcss-browser-4.3.0.min.js. In base.html: (1) update the L21-28 comment block from 4.3.0 to 4.3.2 and change the L28 `<script src>` to `/static/vendor/tailwindcss-browser-4.3.2.min.js` (NO `integrity=` attr — vendored local file); (2) change the L34 htmx `<script>` src to `https://unpkg.com/htmx.org@2.0.10/dist/htmx.min.js` and its `integrity` to `sha384-H5SrcfygHmAuTDZphMHqBJLc3FhssKjG7w/CeCpFReSfwBWDTKpkzPP8c+cLsK+V`; (3) change the L40 Alpine `<script>` src to `https://cdn.jsdelivr.net/npm/alpinejs@3.15.12/dist/cdn.min.js` and its `integrity` to `sha384-LUONAH/vnlbGK96OtMBbN0l0Fcsr7dW3BK7NOImE4oHZAZ/IwIEvvpxyajWxvpaD`. Leave the L37 `htmx-ext-sse@2.2.4` script and its integrity UNTOUCHED. Do not add or remove the `crossorigin="anonymous"` attributes.
  </action>
  <verify>
    <automated>uv run pytest tests/test_base_html_sri.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_base_html_sri.py` passes (static pin asserts htmx@2.0.10 + alpinejs@3.15.12 full-semver pins; with network it recomputes SHA-384 against the live CDN and matches the new integrity values).
    - `grep -c 'tailwindcss-browser-4.3.0' src/phaze/templates/base.html` returns 0 and the 4.3.0 file no longer exists; `static/vendor/tailwindcss-browser-4.3.2.min.js` exists and is non-empty.
    - `grep -c 'htmx-ext-sse@2.2.4' src/phaze/templates/base.html` returns 1 and its integrity is unchanged (the SSE script was not touched).
    - The htmx `<script>` carries `htmx.org@2.0.10` and integrity `sha384-H5SrcfygHmAuTDZphMHqBJLc3FhssKjG7w/CeCpFReSfwBWDTKpkzPP8c+cLsK+V`; the Tailwind `<script>` has NO `integrity=` attribute.
  </acceptance_criteria>
  <done>htmx 2.0.10 + Alpine 3.15.12 are SRI-pinned to the recomputed hashes, Tailwind is vendored at 4.3.2, the 4.3.0 file is deleted, and the existing SRI test is green.</done>
</task>

<task type="auto">
  <name>Task 2: Seed the dead-template AST guard test (green)</name>
  <files>tests/test_dead_template_guard.py</files>
  <read_first>
    - .planning/phases/57-shell-dag-rail/57-RESEARCH.md § "Code Examples → Dead-template AST guard" (the full implementation + the static-literal-name caveat A4)
    - .planning/phases/57-shell-dag-rail/57-PATTERNS.md § "No Analog Found" (entry-set / reachable-set / orphan definition)
    - tests/conftest.py (test conventions; the guard is a static AST test, needs no fixtures)
    - src/phaze/routers/pipeline.py (sample of how `TemplateResponse(..., name="...")` literals appear — confirms static extraction is sound)
  </read_first>
  <action>
    Create tests/test_dead_template_guard.py implementing the orphan-template AST guard exactly as specified in 57-RESEARCH.md. Entry set = literal `name="...html"` strings extracted via regex from every `src/phaze/routers/*.py`. Reachable set = transitive closure of `jinja2.meta.find_referenced_templates(Environment().parse(src))` over each template's `extends`/`include`/`import` targets, starting from the entry set. Orphan = any `src/phaze/templates/**/*.html` reachable from nobody; assert the orphan set is empty with a message listing the sorted orphans. Use repo-relative paths anchored off the test file location (not a hardcoded CWD). If the guard reports pre-existing orphans (it must not, per RESEARCH), add an explicit named allowlist constant with a comment citing the reason — do NOT weaken the closure logic to force green. The dynamic `{% include stage_partial %}` form introduced by the shell (Plan 02/03) resolves to None in jinja2.meta; that is expected and safe because those targets stay reachable via existing legacy pages this phase.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dead_template_guard.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_dead_template_guard.py` passes (zero orphans) against the current repo.
    - The test references `jinja2.meta.find_referenced_templates` and computes the entry set from `name=` literals in `src/phaze/routers/*.py` (not a hardcoded list).
    - `uv run ruff check tests/test_dead_template_guard.py` and `uv run mypy tests/test_dead_template_guard.py` (or full `uv run mypy .`) are clean — tests/ is mypy-excluded but ruff must pass.
    - If an allowlist exists, every entry carries an inline comment justifying it.
  </acceptance_criteria>
  <done>The dead-template guard exists, runs green, and is structured to stay green as shell templates are added — it will be re-run in every subsequent plan's quick run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN → browser | htmx/Alpine bytes fetched from unpkg/jsDelivr cross into the app's execution context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-SRI | Tampering | htmx/Alpine CDN `<script integrity=>` | mitigate | Pin exact recomputed SHA-384 hashes (RESEARCH-verified); `tests/test_base_html_sri.py` enforces https-only + live-CDN recompute. A stale/forged hash either blocks the script (fail-safe) or is caught by the test. |
| T-57-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install this plan — Tailwind is a vendored static file fetched once over https; htmx/Alpine are SRI-gated CDN scripts. No registry slopcheck surface. |
</threat_model>

<verification>
- `uv run pytest tests/test_base_html_sri.py tests/test_dead_template_guard.py` green.
- `uv run pytest` full suite stays green (no regression from the version bump).
- `uv run ruff check . && uv run mypy .` clean.
</verification>

<success_criteria>
- htmx 2.0.10 / Alpine 3.15.12 SRI-pinned and validated; Tailwind 4.3.2 vendored; 4.3.0 deleted (SHELL-04 stack-bump portion of success criterion 4).
- Dead-template AST guard seeded GREEN (SHELL-05 success criterion 5 portion).
</success_criteria>

<output>
Create `.planning/phases/57-shell-dag-rail/57-01-SUMMARY.md` when done.
</output>
