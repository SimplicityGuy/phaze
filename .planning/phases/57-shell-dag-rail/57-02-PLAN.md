---
phase: 57-shell-dag-rail
plan: 02
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - src/phaze/routers/shell.py
  - src/phaze/templates/shell/shell.html
  - src/phaze/templates/shell/_stage_fragment.html
  - src/phaze/main.py
  - src/phaze/routers/pipeline.py
  - tests/test_shell_routes.py
autonomous: true
requirements: [SHELL-01, SHELL-04]

must_haves:
  truths:
    - "GET / returns 200 and renders the three-column shell with the Analyze rail node the selected stage (no redirect to /pipeline)"
    - "GET /s/<stage> with HX-Request: true returns a bare content fragment with no <html>/<head> (D-01 fragment-only contract)"
    - "GET /s/<unknown> returns 404 (per-stage validation owned by the shell handler, D-02)"
    - "The shell <head> carries the lifted auto/dark/light theme machinery and consumes (does not redefine) Alpine.store('pipeline')"
    - "GET /pipeline/ as a plain (non-HX) request 302-redirects to / (true rename)"
  artifacts:
    - path: "src/phaze/routers/shell.py"
      provides: "GET / + GET /s/{stage}; STAGE_PARTIALS map; _render_stage HX fork"
      exports: ["router"]
      contains: "STAGE_PARTIALS"
    - path: "src/phaze/templates/shell/shell.html"
      provides: "full three-column shell: lifted <head> theme/store/SRI, #stage-workspace swap target, skip-link → #stage-workspace, htmx:historyRestore re-init handler"
      contains: "id=\"stage-workspace\""
    - path: "src/phaze/templates/shell/_stage_fragment.html"
      provides: "bare {% include stage_partial %} — no extends base.html"
    - path: "tests/test_shell_routes.py"
      provides: "SHELL-01/02-fragment/04 behavioral tests"
  key_links:
    - from: "src/phaze/main.py"
      to: "src/phaze/routers/shell.py"
      via: "app.include_router(shell.router)"
      pattern: "include_router\\(shell"
    - from: "src/phaze/routers/shell.py"
      to: "src/phaze/routers/pipeline.py"
      via: "factored dashboard-context builder for the Analyze default (D-01)"
      pattern: "from .pipeline import|import pipeline"
    - from: "src/phaze/templates/shell/shell.html"
      to: "#stage-workspace"
      via: "{% include stage_partial %} inside the swap target"
      pattern: "id=\"stage-workspace\""
---

<objective>
Add the dedicated shell router (`GET /` + `GET /s/{stage}`) and the structural three-column shell template that locks the cross-cutting contracts: the single `#stage-workspace` swap target, the fragment-vs-full HX fork, per-stage validation, the lifted theme/`$store.pipeline` machinery, the skip-link → `#stage-workspace`, and the `htmx:historyRestore` re-init handler. `/` renders with **Analyze selected by default** and embeds the existing pipeline-dashboard content (D-01 bridge). `/pipeline/` becomes a rename-redirect to `/`.

Purpose: This is the load-bearing spine Phases 58–62 build on. Get the swap-target id, fragment-only convention, stage resolution, and theme preservation exactly right; the rail/header/⌘K chrome lands in Plan 03 (which wires into this shell.html), and the legacy redirects land in Plan 04.

Output: shell.py, shell.html, _stage_fragment.html, main.py wiring, pipeline.py dashboard-context factoring + `/pipeline`→`/` redirect, and the SHELL-01/04 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-shell-dag-rail/57-CONTEXT.md
@.planning/phases/57-shell-dag-rail/57-RESEARCH.md
@.planning/phases/57-shell-dag-rail/57-PATTERNS.md
@.planning/phases/57-shell-dag-rail/57-UI-SPEC.md
@docs/superpowers/specs/2026-06-28-ui-redesign-assets/prototype.html
@src/phaze/templates/base.html
@src/phaze/routers/pipeline.py

<interfaces>
<!-- The HX fork to mirror VERBATIM (search.py:73-77, verified): -->
<!--   if request.headers.get("HX-Request") == "true": return TemplateResponse(name="<partial>") -->
<!--   return TemplateResponse(name="<full page>") -->
<!-- shell.py variant: HX-Request → shell/_stage_fragment.html (bare); else shell/shell.html (chrome). -->

<!-- STAGE_PARTIALS keys = rail node ids (prototype RAIL config, lines 97-116; UI-SPEC DAG Rail table): -->
<!--   discover, metadata, fingerprint, analyze(default), trackid, tracklist, propose, -->
<!--   rename, tagwrite, move, dedupe, cue. Each value = the EXISTING content partial to bridge (D-01). -->
<!--   "analyze" embeds the existing pipeline-dashboard content (dag_canvas) — Phase 58 replaces with lane cards. -->
<!-- shell.html passes `stage` (active stage id) to context so rail.html (Plan 03) sets aria-current. -->

<!-- pipeline.py verified anchors: _build_dag_context (:131), dashboard (:435), pipeline_stats_partial (:550), -->
<!--   oob_counts=True (:606). Initial shell render MUST pass oob_counts falsy (Pitfall 5 — duplicate-id collision). -->

<!-- base.html LIFT-VERBATIM ranges (SHELL-04): vendored Tailwind+comment (21-28), htmx/sse/Alpine scripts -->
<!--   (34/37/40 — already bumped in Plan 01), no-FOUC _applyTheme + Alpine.store('theme') (54-82), -->
<!--   Alpine.store('pipeline') seed (106-136 — CONSUME, NEVER REDEFINE), @theme tokens + @custom-variant dark -->
<!--   (140-162), body color classes + .font-jura/.htmx-indicator (164-169). Skip-link (174-176) target changes -->
<!--   to #stage-workspace in shell.html ("Skip to workspace"). The <nav> tab-bar (178-269) is NOT carried over. -->

<!-- conftest client fixture (tests/conftest.py:161-167): create_app() + get_session override + AsyncClient/ASGITransport. REUSE it. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shell router + main.py wiring + pipeline.py dashboard-context factoring + /pipeline→/ redirect</name>
  <files>src/phaze/routers/shell.py, src/phaze/main.py, src/phaze/routers/pipeline.py</files>
  <read_first>
    - src/phaze/routers/search.py (lines 14-16 router header; 73-77 the HX-Request fork to mirror — DROP the prefix for shell.py)
    - src/phaze/routers/pipeline.py (lines 120-160 _build_dag_context; 430-546 dashboard(); 598-606 oob_counts gate — factor the dashboard context so / and /pipeline/ share it)
    - src/phaze/main.py (lines 185-227 the include block; add app.include_router(shell.router))
    - .planning/phases/57-shell-dag-rail/57-PATTERNS.md § "src/phaze/routers/shell.py (NEW)" and § "src/phaze/main.py (MOD)"
    - .planning/phases/57-shell-dag-rail/57-RESEARCH.md § "New / + /s/{stage} shell handlers" and Open Question 2 (factor the dashboard context builder)
  </read_first>
  <action>
    Create src/phaze/routers/shell.py: `router = APIRouter(tags=["shell"])` (prefix-less, like pipeline.py — it must own `/` and `/s/{stage}`). Define `STAGE_PARTIALS: dict[str, str]` mapping each rail node id (discover, metadata, fingerprint, analyze, trackid, tracklist, propose, rename, tagwrite, move, dedupe, cue) to the existing content partial that bridges it (D-01); "analyze" maps to the existing pipeline-dashboard content (dag_canvas). Implement a private `_render_stage(request, stage)` helper holding the HX fork (mirror search.py:73-77): `HX-Request == "true"` → `templates.TemplateResponse(name="shell/_stage_fragment.html", ...)` (bare); else → `name="shell/shell.html"` (full chrome). Pass context including `stage` (active stage id), `stage_partial=STAGE_PARTIALS[stage]`, and `oob_counts=False` (Pitfall 5). `GET /` calls `_render_stage(request, "analyze")` (SHELL-01 — Analyze default, bare root per D-02; NO redirect). `GET /s/{stage}` validates `stage in STAGE_PARTIALS` else `raise HTTPException(status_code=404)` (D-02 per-stage validation; V5 input-validation — never interpolate `stage` into a template path), then calls `_render_stage`. For the Analyze default context, do NOT duplicate dashboard() query logic — factor the dashboard context builder in pipeline.py into a reusable function (e.g. `build_dashboard_context(...)`) that both `dashboard()` (:435) and shell.py's analyze path call (RESEARCH Open Question 2). In pipeline.py also insert the `/pipeline` → `/` rename: at the top of `dashboard()` (:435), before any query work, `if request.headers.get("HX-Request") != "true": return RedirectResponse(url="/", status_code=302)` (conditional form for uniformity; add `from fastapi.responses import RedirectResponse` if missing). Wire `app.include_router(shell.router)` into main.py:185-227 with NO extra `prefix=` (required by _route_introspection). shell.html / _stage_fragment.html do not yet exist — route registration is lazy, so this task verifies registration + types only.
  </action>
  <verify>
    <automated>uv run mypy src/phaze/routers/shell.py src/phaze/routers/pipeline.py src/phaze/main.py && uv run ruff check src/phaze/routers/shell.py && uv run python -c "from tests._route_introspection import effective_route_paths; from phaze.main import create_app; p=effective_route_paths(create_app()); assert '/' in p and '/s/{stage}' in p, p"</automated>
  </verify>
  <acceptance_criteria>
    - mypy clean on shell.py, pipeline.py, main.py; ruff clean on shell.py.
    - A python check confirms `effective_route_paths(create_app())` (via tests/_route_introspection) contains `/` and `/s/{stage}` — e.g. `uv run python -c "from tests._route_introspection import effective_route_paths; from phaze.main import create_app; p=effective_route_paths(create_app()); assert '/' in p and '/s/{stage}' in p, p"`.
    - `STAGE_PARTIALS` contains exactly the 12 rail node ids listed; "analyze" maps to the existing pipeline-dashboard content partial.
    - `dashboard()` factors its context through a shared builder that shell.py's analyze path also calls (no duplicated query logic); `dashboard()` returns a 302 to `/` for a plain (non-HX) GET.
  </acceptance_criteria>
  <done>shell.py registers GET / + GET /s/{stage} with HX fork + 404 stage validation, pipeline.py exposes a shared dashboard-context builder and redirects /pipeline→/, and main.py includes the shell router.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Structural shell.html + _stage_fragment.html (head theme lift, swap target, history/focus handler)</name>
  <files>src/phaze/templates/shell/shell.html, src/phaze/templates/shell/_stage_fragment.html</files>
  <behavior>
    - GET / → 200; response body contains `id="stage-workspace"`, the lifted no-FOUC `_applyTheme` script, `Alpine.store('theme'`, the Jura font link, and the wave-logo SVG; the Analyze pipeline-dashboard content is embedded inside `#stage-workspace`.
    - GET / response does NOT redefine `Alpine.store('pipeline'` more than the single lifted seed (consume, not redefine).
    - GET /s/discover with header HX-Request: true → 200; body has NO `<html>` and NO `<head>` (bare fragment, D-01).
    - GET /s/discover without the HX header → 200; body IS the full shell (contains `id="stage-workspace"`).
    - The shell skip-link href is `#stage-workspace` (not `#proposals-table`).
  </behavior>
  <read_first>
    - src/phaze/templates/base.html (the entire <head>: lines 21-169 to lift verbatim; 174-176 skip-link to retarget; 178-269 the <nav> tab-bar NOT to carry over)
    - docs/superpowers/specs/2026-06-28-ui-redesign-assets/prototype.html (three-column frame: header h-14, rail w-[280px], #stage-workspace, right pane w-[350px]; layout proportions)
    - .planning/phases/57-shell-dag-rail/57-UI-SPEC.md § "Spacing Scale" (fixed load-bearing dims, h-screen overflow-hidden frame) and § "Theme & Brand Preservation" + § "Focus / ARIA Baseline"
    - .planning/phases/57-shell-dag-rail/57-PATTERNS.md § "shell/shell.html (NEW)" (LIFT VERBATIM list, REPLACE list, skip-link change, history re-init handler) and § "_stage_fragment.html (NEW)"
    - .planning/phases/57-shell-dag-rail/57-RESEARCH.md § "Pattern: htmx history + Alpine re-init" (Assumption A1 — confirm the htmx:historyRestore event name against htmx 2.0.10 when wiring)
  </read_first>
  <action>
    Create src/phaze/templates/shell/shell.html: lift VERBATIM into `<head>` the vendored-Tailwind script + comment, the htmx/htmx-ext-sse/Alpine scripts (already bumped in Plan 01), the no-FOUC `_applyTheme` script + `Alpine.store('theme')`, the `Alpine.store('pipeline')` seed (CONSUME, do not redefine), the `@theme` tokens + `@custom-variant dark`, the `.font-jura`/`.htmx-indicator` styles, and the Jura/Inter font links (SHELL-04). Body = `h-screen overflow-hidden flex flex-col` app frame: a placeholder header bar (`h-14` — Plan 03 fills it with header.html include), a row holding a placeholder left rail column (`w-[280px]` — Plan 03 fills it with rail.html include), the center `<main>` with `<div id="stage-workspace" class="overflow-y-auto">{% include stage_partial %}</div>`, and a right pane (`w-[350px]`, overflow-y-auto; bridged content is a later phase — minimal placeholder is fine). Each column scrolls independently. Add the skip-link as the existing sr-only-focus pattern but `href="#stage-workspace"` text "Skip to workspace". Add an inline `<script>` binding `htmx:historyRestore` on `document.body` that runs `Alpine.initTree(document.getElementById('stage-workspace'))` and re-syncs the active rail node (a `syncRailSelection(location.pathname)` stub Plan 03 completes), plus a `htmx:afterSwap` handler on `#stage-workspace` that moves focus to the stage `<h1tabindex="-1">` (focus-to-heading). Confirm the exact event token against htmx 2.0.10 (A1) before finalizing. Create src/phaze/templates/shell/_stage_fragment.html as exactly the bare `{% include stage_partial %}` — NO `{% extends %}`, NO `<html>`/`<head>` (anti-pattern: a fragment extending base.html corrupts the shell). Do NOT include rail.html/header.html/cmdk_modal.html yet (Plan 03) — use inert placeholder markup so the shell renders.
  </action>
  <verify>
    <automated>uv run pytest tests/test_shell_routes.py -k "shell_analyze_default or fragment_is_bare or theme_and_store_preserved"</automated>
  </verify>
  <acceptance_criteria>
    - All five `<behavior>` assertions hold (verified by the Task 3 tests once written; this task's verify runs the subset that exists).
    - `_stage_fragment.html` contains no `{% extends %}` and no `<html`/`<head` tokens.
    - shell.html embeds the theme `<head>` machinery verbatim and the swap target `<div id="stage-workspace">`; the skip-link href is `#stage-workspace`.
    - shell.html binds `htmx:historyRestore` and calls `Alpine.initTree` on `#stage-workspace`.
  </acceptance_criteria>
  <done>The shell renders the full chrome on direct nav and a bare fragment on HX request, with theme/store machinery lifted verbatim, the `#stage-workspace` swap target, retargeted skip-link, and the history/focus re-init handlers in place.</done>
</task>

<task type="auto">
  <name>Task 3: SHELL-01/02-fragment/04 route tests (green)</name>
  <files>tests/test_shell_routes.py</files>
  <read_first>
    - tests/conftest.py (lines 161-167 the `client` fixture to reuse — create_app() + get_session override + AsyncClient/ASGITransport)
    - .planning/phases/57-shell-dag-rail/57-VALIDATION.md § "Per-Task Verification Map" (exact test names + behaviors)
    - .planning/phases/57-shell-dag-rail/57-RESEARCH.md § "Validation Architecture → Phase Requirements → Test Map"
    - .planning/phases/57-shell-dag-rail/57-PATTERNS.md § "tests/test_shell_routes.py (NEW)"
  </read_first>
  <action>
    Fill in the Wave-1 stub `tests/test_shell_routes.py` (created in Plan 57-01) using the conftest `client` fixture — REPLACE the stub bodies, do NOT re-create the file or redeclare the functions. Implement: `test_root_renders_shell_analyze_default` — GET / → 200, body contains `id="stage-workspace"` and the Analyze stage is the selected/active stage (assert the `stage` is "analyze" via a marker the shell renders, e.g. the embedded dashboard content or a data attribute; the `aria-current` assertion against the rail is added in Plan 03 — here assert the shell rendered with stage=analyze and that the response is NOT a redirect). `test_stage_fragment_is_bare` — GET /s/discover with `headers={"HX-Request": "true"}` → 200 and body contains neither `<html` nor `<head`; the same path without the header → 200 and IS the full shell. `test_unknown_stage_404` — GET /s/does-not-exist → 404. `test_theme_and_store_preserved` — GET / body contains the `_applyTheme` script, `Alpine.store('theme'`, the Jura font link, and exactly one `Alpine.store('pipeline'` definition (consumed, not redefined). All green.
  </action>
  <verify>
    <automated>uv run pytest tests/test_shell_routes.py tests/test_dead_template_guard.py tests/test_base_html_sri.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_shell_routes.py` passes with the four named tests present.
    - `test_stage_fragment_is_bare` proves the HX fragment has no `<html>`/`<head>` and the non-HX response is the full shell (the D-01 / fragment-only contract).
    - `test_theme_and_store_preserved` proves SHELL-04 (theme machinery present, `$store.pipeline` not redefined).
    - The dead-template guard (Plan 01) is still GREEN with the new shell templates added.
    - Full suite `uv run pytest` and `uv run ruff check . && uv run mypy .` are green (wave-merge gate).
  </acceptance_criteria>
  <done>SHELL-01 and SHELL-04 (and the SHELL-02 fragment-bare contract) are proven by automated tests; the dead-template guard stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → shell route | `{stage}` path parameter crosses from an untrusted URL into stage resolution |
| browser address bar → /pipeline redirect | `Location` header value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | `GET /s/{stage}` stage resolution | mitigate | Whitelist via `stage in STAGE_PARTIALS` → 404 otherwise (D-02); `stage` is NEVER interpolated into a template path — the partial name comes from the static `STAGE_PARTIALS` dict, preventing template-path injection/reflection (ASVS V5). |
| T-57-02 | Tampering | `/pipeline`→`/` `RedirectResponse` Location | accept | Redirect target is the static internal constant `/`, never user input — no open-redirect surface. |
| T-57-03 | Injection (XSS) | new shell templates | mitigate | No `| safe` / autoescape-off on any user-influenced value; the lifted `$store.pipeline` seed values are server-computed ints (verified pipeline.py coerces to int). Jinja2 autoescape stays on for all new templates. |
| T-57-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan — no new dependency (`@alpinejs/focus` is Phase 61). |
</threat_model>

<verification>
- `uv run pytest tests/test_shell_routes.py tests/test_dead_template_guard.py tests/test_base_html_sri.py -x` green.
- `uv run pytest` full suite green (wave-merge gate).
- `uv run ruff check . && uv run mypy .` clean.
</verification>

<success_criteria>
- `GET /` renders the three-column shell with Analyze as the default selected stage, no `/pipeline` redirect (SHELL-01, success criterion 1 — the rail `aria-current` marker is completed in Plan 03).
- Stage responses are fragment-only on HX request; unknown stages 404 (D-01/D-02 contract).
- Theme/brand machinery + `$store.pipeline` consumption preserved verbatim in the shell `<head>` (SHELL-04, success criterion 4).
- `/pipeline/` rename-redirects to `/`.
</success_criteria>

<output>
Create `.planning/phases/57-shell-dag-rail/57-02-SUMMARY.md` when done.
</output>
