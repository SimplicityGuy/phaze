---
phase: 57-shell-dag-rail
plan: 04
type: execute
wave: 4
depends_on: ["57-02", "57-03"]
files_modified:
  - src/phaze/routers/proposals.py
  - src/phaze/routers/tracklists.py
  - src/phaze/routers/tags.py
  - src/phaze/routers/cue.py
  - src/phaze/routers/duplicates.py
  - src/phaze/routers/preview.py
  - src/phaze/routers/search.py
  - tests/test_redirect_resolution.py
autonomous: true
requirements: [SHELL-05]

must_haves:
  truths:
    - "Each of the 8 legacy canonical (trailing-slash) routes resolves in ≤1 hop to a 200 with the matching rail node pre-selected (D-03)"
    - "A plain (non-HX) GET to a legacy render-in-shell route 302-redirects to its canonical /s/<stage>; /search 302-redirects to /?palette=1"
    - "An in-page filter request (HX-Request: true) on a legacy route still returns its existing filter partial — NOT a redirect (the app stays fully usable, D-01)"
    - "All redirect targets are static internal constants — no open-redirect surface"
  artifacts:
    - path: "src/phaze/routers/proposals.py"
      provides: "conditional redirect → /s/proposals above the existing HX-filter branch"
      contains: "RedirectResponse"
    - path: "tests/test_redirect_resolution.py"
      provides: "8-route ≤1-hop redirect test + HX-filter-not-redirected test (uses _route_introspection)"
      contains: "follow_redirects=False"
  key_links:
    - from: "src/phaze/routers/{proposals,tracklists,tags,cue,duplicates,preview,search}.py"
      to: "/s/<stage> | /?palette=1"
      via: "if request.headers.get('HX-Request') != 'true': RedirectResponse(..., 302) as the first handler statement"
      pattern: "HX-Request.*!=.*true"
    - from: "tests/test_redirect_resolution.py"
      to: "the 8 legacy routes"
      via: "parametrized follow_redirects=False hop count + follow_redirects=True 200"
      pattern: "follow_redirects"
---

<objective>
Make every legacy bookmark resolve into the shell (SHELL-05). Insert a conditional redirect at the top of each of the 6 render-in-shell legacy GET handlers (302 → canonical `/s/<stage>`) and the `/search` rename (→ `/?palette=1`), leaving each handler's existing in-page HX-filter branch untouched so the app stays fully usable through cutover (D-01). The `/pipeline`→`/` rename already landed in Plan 02. Prove all 8 canonical routes resolve in ≤1 hop to a 200, and that HX filters are not redirected.

Purpose: External bookmarks to the old tab URLs must not break. The conditional form (redirect only when `HX-Request` is absent) is the core finding — an unconditional 302 would hijack the in-page filter keystroke and swap the shell into the inner filter div.

Output: conditional redirects across 7 routers + the redirect-resolution test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-shell-dag-rail/57-CONTEXT.md
@.planning/phases/57-shell-dag-rail/57-RESEARCH.md
@.planning/phases/57-shell-dag-rail/57-PATTERNS.md

<interfaces>
<!-- The conditional-redirect pattern (D-03 / SHELL-05) — insert as the FIRST statement of each GET handler, -->
<!-- ABOVE the existing body (including its own `if HX-Request == "true": return <filter partial>` branch): -->
<!--   if request.headers.get("HX-Request") != "true": -->
<!--       return RedirectResponse(url="/s/proposals", status_code=302) -->
<!-- Add `from fastapi.responses import RedirectResponse` where missing. -->

<!-- Per-file insertion point + canonical target (verified line numbers — handlers all take `request: Request`): -->
<!--   proposals.py   list_proposals (@router.get("/"), :114)   existing HX-branch :157   -> /s/proposals  (prefix /proposals) -->
<!--   tracklists.py  list           (:78)                       HX-branch :152          -> /s/tracklist  (prefix /tracklists) -->
<!--   tags.py        GET "/"        (:140)                      HX-branch :201          -> /s/tagwrite   (prefix /tags) -->
<!--   cue.py         list_cue       (:176)                      HX-branch :228          -> /s/cue        (prefix /cue) -->
<!--   duplicates.py  GET "/"        (:79)                       HX-branch :105          -> /s/dedupe     (prefix /duplicates) -->
<!--   preview.py     tree_preview   (@router.get("/preview/"), :36)   NO HX-branch       -> /s/move       (prefix-less, path /preview/) -->
<!--   search.py      search_page    (@router.get("/"), :19)     HX-branch :74           -> /?palette=1    (rename -> ⌘K, D-04/A3) -->
<!-- /pipeline -> / already done in Plan 02 (pipeline.py dashboard). -->

<!-- ≤1-hop caveat (Pitfall 4): redirect_slashes=True (Starlette default). Test the CANONICAL trailing-slash forms -->
<!-- (/proposals/) — they resolve in 1 hop. The no-slash form (/proposals) is a framework-level 2-hop that still terminates. -->

<!-- Test helpers: tests/conftest.py client fixture (:161-167); tests/_route_introspection.iter_effective_routes / -->
<!-- effective_route_paths — NEVER app.routes directly (FastAPI 0.138 lazy-includes routers). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conditional redirects on the 7 legacy routers</name>
  <files>src/phaze/routers/proposals.py, src/phaze/routers/tracklists.py, src/phaze/routers/tags.py, src/phaze/routers/cue.py, src/phaze/routers/duplicates.py, src/phaze/routers/preview.py, src/phaze/routers/search.py</files>
  <read_first>
    - src/phaze/routers/proposals.py (lines 114-160 — handler + existing HX-branch at :157)
    - src/phaze/routers/tracklists.py (:78 handler, :152 HX-branch), src/phaze/routers/tags.py (:140 handler, :201 HX-branch), src/phaze/routers/cue.py (:176 handler, :228 HX-branch), src/phaze/routers/duplicates.py (:79 handler, :105 HX-branch)
    - src/phaze/routers/preview.py (:36 tree_preview — NO existing HX-branch; use the conditional form anyway for uniformity)
    - src/phaze/routers/search.py (:19 search_page, :74 HX-branch — target is /?palette=1, the ⌘K rename)
    - .planning/phases/57-shell-dag-rail/57-PATTERNS.md § "Legacy router conditional-redirect (MOD)" (the per-file table + the import-to-add note)
    - .planning/phases/57-shell-dag-rail/57-RESEARCH.md § "Pattern: Legacy route resolution (the core finding)" (the collision + conditional resolution + true-renames)
  </read_first>
  <action>
    In each of the 7 legacy GET handlers, insert as the FIRST statement (before any query work, above the existing HX-filter branch): `if request.headers.get("HX-Request") != "true": return RedirectResponse(url="<target>", status_code=302)`. Targets: proposals → `/s/proposals`, tracklists → `/s/tracklist`, tags → `/s/tagwrite`, cue → `/s/cue`, duplicates → `/s/dedupe`, preview → `/s/move`, search → `/?palette=1`. Add `from fastapi.responses import RedirectResponse` to any file missing it. Do NOT modify the existing `if request.headers.get("HX-Request") == "true": return <filter partial>` branches — they must keep working so the in-page filter stays functional (D-01, Pitfall 2). All redirect URLs are static internal string literals — never derive a redirect target from a query param or user input (no open-redirect). preview.py has no HX-branch; the conditional form is still used for uniformity (a plain GET redirects; the handler body is otherwise unreachable for non-HX requests).
  </action>
  <verify>
    <automated>uv run mypy src/phaze/routers/ && uv run ruff check src/phaze/routers/</automated>
  </verify>
  <acceptance_criteria>
    - mypy + ruff clean across `src/phaze/routers/`.
    - Each of the 7 routers contains `from fastapi.responses import RedirectResponse` and a `request.headers.get("HX-Request") != "true"` guard returning a 302 to the correct static target (per the interface table).
    - `grep -c "HX-Request"` in each touched handler shows BOTH the new `!= "true"` redirect guard AND the pre-existing `== "true"` filter branch are present (preview.py only the new guard — it had none).
    - No redirect target is built from a query parameter or other user input (all targets are string literals).
  </acceptance_criteria>
  <done>All 7 legacy render-in-shell / rename routes conditionally 302-redirect to their canonical shell URL for plain GETs while preserving the in-page HX-filter branch.</done>
</task>

<task type="auto">
  <name>Task 2: Redirect-resolution test (8-route ≤1-hop + HX-filter-not-redirected)</name>
  <files>tests/test_redirect_resolution.py</files>
  <read_first>
    - tests/conftest.py (lines 161-167 the `client` fixture)
    - tests/_route_introspection.py (lines 37-56 iter_effective_routes / effective_route_paths — use these, NEVER app.routes)
    - .planning/phases/57-shell-dag-rail/57-RESEARCH.md § "Code Examples → Redirect-loop / ≤1-hop test (NEW)" (the full shape + the CANONICAL map)
    - .planning/phases/57-shell-dag-rail/57-VALIDATION.md § "Per-Task Verification Map" (SHELL-05 rows)
    - .planning/phases/57-shell-dag-rail/57-PATTERNS.md § "tests/test_redirect_resolution.py (NEW)"
  </read_first>
  <action>
    Create tests/test_redirect_resolution.py using the conftest `client` fixture (and `iter_effective_routes`/`effective_route_paths` where route enumeration is needed). Parametrize over the 8 canonical trailing-slash legacy routes mapped to their shell targets: `/proposals/`→`/s/proposals`, `/tracklists/`→`/s/tracklist`, `/tags/`→`/s/tagwrite`, `/cue/`→`/s/cue`, `/duplicates/`→`/s/dedupe`, `/preview/`→`/s/move`, `/pipeline/`→`/`, `/search/`→`/` (palette param). For each: with `follow_redirects=False` and a plain GET (no HX header), assert status in (302, 307) and `location.split("?")[0]` equals the target (≤1 hop); then with `follow_redirects=True` assert the final status is 200 (lands on a live shell with the matching rail node pre-selected). Add `test_hx_filter_not_redirected`: GET a filterable legacy route (e.g. `/proposals/`) with `headers={"HX-Request": "true"}` and assert the response is NOT a 302/307 — it returns the existing filter partial (200, body is the partial content, not the full shell). All green.
  </action>
  <verify>
    <automated>uv run pytest tests/test_redirect_resolution.py tests/test_shell_routes.py tests/test_dead_template_guard.py tests/test_base_html_sri.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_redirect_resolution.py` passes: all 8 canonical trailing-slash routes resolve in ≤1 hop to a 200; the parametrized hop-count assertion uses `follow_redirects=False`.
    - `test_hx_filter_not_redirected` proves an `HX-Request: true` request to a legacy route returns its filter partial (NOT a redirect) — the app stays usable (D-01).
    - The test enumerates routes via `tests/_route_introspection`, never `app.routes` directly.
    - Full suite `uv run pytest` green and `uv run ruff check . && uv run mypy .` clean (wave-merge + phase gate).
  </acceptance_criteria>
  <done>SHELL-05 is proven: every legacy bookmark resolves into the shell in ≤1 hop with the matching rail node, and in-page filters are never hijacked by the redirect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser address bar / bookmark → legacy route | a plain GET crosses into a redirect handler that emits a `Location` header |
| in-page filter (HX) → legacy route | same path, distinguished only by the `HX-Request` header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-RD | Tampering | legacy-route `RedirectResponse` Location | mitigate | All 7 redirect targets are static internal string literals (`/s/<stage>`, `/?palette=1`) — never derived from a query param or user input, so there is no open-redirect (`next=`-style) surface. Test asserts the exact canonical target. |
| T-57-FH | Tampering | conditional `HX-Request` branch | mitigate | The redirect fires ONLY when `HX-Request` is absent; the existing `== "true"` filter branch is left intact so a filter keystroke is not hijacked into a shell swap (Pitfall 2). `test_hx_filter_not_redirected` enforces this. |
| T-57-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan — `RedirectResponse` is stdlib (starlette, already a dep). |
</threat_model>

<verification>
- `uv run pytest tests/test_redirect_resolution.py tests/test_shell_routes.py tests/test_dead_template_guard.py tests/test_base_html_sri.py -x` green.
- `uv run pytest` full suite green (phase gate).
- `uv run ruff check . && uv run mypy .` clean.
</verification>

<success_criteria>
- Each of the 8 legacy routes (`/pipeline`, `/proposals`, `/tracklists`, `/tags`, `/cue`, `/duplicates`, `/search`, `/preview`) resolves in ≤1 hop to a 200 with the matching rail node pre-selected (SHELL-05, success criterion 5).
- In-page HX filters on legacy routes still return their filter partials (D-01 — app stays fully usable).
</success_criteria>

<output>
Create `.planning/phases/57-shell-dag-rail/57-04-SUMMARY.md` when done.
</output>
