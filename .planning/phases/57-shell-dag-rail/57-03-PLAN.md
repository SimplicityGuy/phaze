---
phase: 57-shell-dag-rail
plan: 03
type: execute
wave: 3
depends_on: ["57-02"]
files_modified:
  - src/phaze/templates/shell/partials/rail.html
  - src/phaze/templates/shell/partials/header.html
  - src/phaze/templates/shell/partials/cmdk_modal.html
  - src/phaze/templates/shell/shell.html
  - tests/test_shell_routes.py
autonomous: true
requirements: [SHELL-02, SHELL-03]

must_haves:
  truths:
    - "The left DAG rail lists every pipeline stage in the prototype's verbatim order, each navigable node carrying hx-get=/s/<stage>, hx-target=#stage-workspace, hx-swap=innerHTML, hx-push-url=true (SHELL-02)"
    - "The active stage's rail node carries aria-current=\"page\"; / marks the analyze node active"
    - "Rail live counts bind via x-text to EXISTING $store.pipeline keys only; nodes without a backing key render no count (no new store keys)"
    - "The legacy top <nav> tab-bar is absent from the shell; the header shows a ⌘K button, agent status dots, and an Agents link (SHELL-03)"
    - "The ⌘K button + ⌘K/Ctrl+K keybinding open an Alpine skeleton modal (empty body) that closes on ESC and backdrop, returning focus to the trigger (D-04)"
    - "Agent status dots + count ride the existing /pipeline/stats OOB poll (no new poll loop, D-05)"
  artifacts:
    - path: "src/phaze/templates/shell/partials/rail.html"
      provides: "DAG rail nav: prototype-order nodes, HTMX wiring, aria-current, live-count bindings, +Scan CTA, amber Review&Apply group, below-line audit/agents links"
      contains: "hx-target=\"#stage-workspace\""
    - path: "src/phaze/templates/shell/partials/header.html"
      provides: "wave logo (lifted), theme toggle (lifted), ⌘K button, agent status dots + Agents link bound to $store.pipeline.agentOnline"
    - path: "src/phaze/templates/shell/partials/cmdk_modal.html"
      provides: "Alpine skeleton command palette (open/close/ESC/focus contract, empty body)"
    - path: "src/phaze/templates/shell/shell.html"
      provides: "header/rail/cmdk includes wired in; palette query-param auto-open hook"
  key_links:
    - from: "src/phaze/templates/shell/partials/rail.html"
      to: "#stage-workspace"
      via: "hx-get=/s/<stage> hx-target=#stage-workspace hx-push-url=true on every node"
      pattern: "hx-target=\"#stage-workspace\""
    - from: "src/phaze/templates/shell/partials/rail.html"
      to: "$store.pipeline"
      via: "x-text live-count binding to existing keys (discovered, metadataDone, analyzeActive, ...)"
      pattern: "\\$store\\.pipeline"
    - from: "src/phaze/templates/shell/partials/header.html"
      to: "$store.pipeline.agentOnline"
      via: "status dots + Agents link, refreshed by the /pipeline/stats OOB poll"
      pattern: "agentOnline"
---

<objective>
Build the navigation chrome that makes the shell usable: the DAG rail (the nav spine), the header (wave logo · ⌘K affordance · agent status strip · Agents link), and the ⌘K skeleton modal — then wire all three into the Plan 02 shell.html. This delivers SHELL-02 (rail nav + live counts + HTMX swap) and SHELL-03 (tab-bar removed, ⌘K header command bar, header status strip), honoring D-04 (skeleton modal only) and D-05 (minimal status strip riding the existing poll).

Purpose: The rail is how every later phase navigates between workspaces; its HTMX wiring + `aria-current` + `$store.pipeline` count bindings are a locked contract. The ⌘K affordance and status strip establish the header pattern with no new poll loop and no new store keys.

Output: rail.html, header.html, cmdk_modal.html, the shell.html include wiring, and the SHELL-02/03 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-shell-dag-rail/57-CONTEXT.md
@.planning/phases/57-shell-dag-rail/57-RESEARCH.md
@.planning/phases/57-shell-dag-rail/57-PATTERNS.md
@.planning/phases/57-shell-dag-rail/57-UI-SPEC.md
@docs/superpowers/specs/2026-06-28-ui-redesign-assets/prototype.html
@src/phaze/templates/base.html
@src/phaze/templates/pipeline/partials/stats_bar.html
@src/phaze/templates/shell/shell.html

<interfaces>
<!-- Rail order VERBATIM from prototype RAIL config (lines 97-116) — UI-SPEC "DAG Rail" table (lines 189-208): -->
<!--   1 discover (live: discovered)                                                                          -->
<!--   group Enrich · parallel: metadata (live: metadataDone) · fingerprint (live: fingerprintDone)           -->
<!--                            · analyze (live: analyzeActive, blue-pulse dot, = / default)                  -->
<!--   group Identify: trackid (idle, STATIC label — no store key) · tracklist (live: tracklistDone)          -->
<!--   propose (live: proposalsDone)                                                                          -->
<!--   group Review & Apply (AMBER): rename · tagwrite · move · dedupe · cue (all STATIC — no store keys in 57)-->
<!--   below-the-line (border-t): audit → /audit/ , agents → /admin/agents (plain links, NOT /s/ stages)      -->
<!-- Live-vs-static rule: render x-text count ONLY for nodes whose key exists in base.html:106. NEVER add store keys. -->
<!-- Existing $store.pipeline keys (base.html:106-136): discovered, metadataDone, fingerprintDone, analyzeActive, -->
<!--   tracklistDone, proposalsDone, agentOnline, ... -->

<!-- HTMX wiring per navigable node: hx-get="/s/<stage>" hx-target="#stage-workspace" hx-swap="innerHTML" hx-push-url="true" -->
<!-- Active node: aria-current="page" + bg-blue-500/10 + inset-left bar; shell passes `stage` (Plan 02) — compare node id == stage. -->

<!-- LIFT VERBATIM from base.html into header.html: wave logo SVG (183-188, links to /), theme-toggle button -->
<!--   (243-267: auto/moon/sun x-show SVGs + 3-way @click cycle). -->
<!-- Status strip (D-05): agent dots (emerald online / amber degraded / gray offline) + "Agents · {n}" link to -->
<!--   /admin/agents, bound to $store.pipeline.agentOnline, refreshed by the EXISTING /pipeline/stats OOB poll — NO new setInterval. -->

<!-- ⌘K modal (D-04, UI-SPEC "⌘K Command Palette"): x-data{open:false}; @keydown.window.cmd.k.prevent/.ctrl.k; -->
<!--   @keydown.escape; backdrop @click; role="dialog" aria-modal="true"; focus input on open ($nextTick); return -->
<!--   focus to ⌘K button on close. Panel w-[640px] max-w-[92vw] phaze-panel/phaze-border. SKELETON body only (copy in UI-SPEC). -->
<!--   Use CORE Alpine only — @alpinejs/focus is a Phase 61 dep. /search→/?palette=1 (Plan 04): shell Alpine reads `palette` query param to auto-open. -->

<!-- OOB fanout pattern (stats_bar.html:46-104 + pipeline.py:606): rail counts + status dots RIDE this one 5s poll; -->
<!--   initial shell render passes oob_counts falsy (Pitfall 5). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DAG rail partial (rail.html) — nav spine, HTMX wiring, live counts, aria-current</name>
  <files>src/phaze/templates/shell/partials/rail.html</files>
  <read_first>
    - docs/superpowers/specs/2026-06-28-ui-redesign-assets/prototype.html (RAIL config lines 97-116 — node order/ids/labels/groups VERBATIM; .rail-node.sel active treatment)
    - .planning/phases/57-shell-dag-rail/57-UI-SPEC.md § "DAG Rail — node visual states & order" (the full node table, live-vs-static rule, node visual states, HTMX wiring, +Scan CTA copy)
    - src/phaze/templates/base.html (lines 191-240 the nav link loop + aria-current form; 106-136 the $store.pipeline keys available for x-text)
    - src/phaze/templates/pipeline/partials/stats_bar.html (lines 47-68 the x-text / x-init store bindings to mirror)
    - .planning/phases/57-shell-dag-rail/57-PATTERNS.md § "shell/partials/rail.html (NEW)"
  </read_first>
  <action>
    Create src/phaze/templates/shell/partials/rail.html: an `<aside class="w-[280px] ... overflow-y-auto">` with a Jura "Pipeline" eyebrow + the primary "+ Scan" CTA (`bg-blue-600`), then the nodes/groups in the EXACT prototype order (discover; Enrich group: metadata, fingerprint, analyze; Identify group: trackid, tracklist; propose; amber Review & Apply group: rename, tagwrite, move, dedupe, cue; below-the-line border-t: audit→/audit/, agents→/admin/agents). Every navigable stage node carries `hx-get="/s/<id>"` `hx-target="#stage-workspace"` `hx-swap="innerHTML"` `hx-push-url="true"`. The active node (compare node id to the `stage` context var passed by shell.py) gets `aria-current="page"` + `bg-blue-500/10` + the inset-left bar + a focus ring (`ring-2 ring-blue-500`). Bind a live count via `x-text` to the node's `$store.pipeline` key ONLY when that key exists (discover→discovered, metadata→metadataDone, fingerprint→fingerprintDone, analyze→analyzeActive, tracklist→tracklistDone, propose→proposalsDone); trackid and the five Review&Apply items render NO count (static label) — DO NOT add store keys. analyze gets the blue-pulse "live" dot. audit/agents are plain `<a>` links (NOT /s/ stages). Every element carries working `dark:` variants. Counts must be numeric server/store values only (no user-influenced interpolation).
  </action>
  <verify>
    <automated>uv run python -c "from jinja2 import Environment, FileSystemLoader; e=Environment(loader=FileSystemLoader('src/phaze/templates')); e.get_template('shell/partials/rail.html'); print('parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - rail.html parses as a valid Jinja2 template.
    - Every navigable node has all four HTMX attrs (`hx-get="/s/`, `hx-target="#stage-workspace"`, `hx-swap="innerHTML"`, `hx-push-url="true"`); `grep -c 'hx-target="#stage-workspace"'` ≥ the navigable-node count.
    - Node order matches the prototype RAIL config verbatim (discover → Enrich → Identify → propose → Review&Apply → audit/agents below-line).
    - `x-text` count bindings reference ONLY existing `$store.pipeline` keys; no new keys are introduced; trackid + the 5 Review&Apply items have no count binding.
    - audit links to `/audit/` and agents to `/admin/agents` (not `/s/` paths).
  </acceptance_criteria>
  <done>rail.html renders the full DAG nav spine with correct order, HTMX swap wiring, aria-current active state, and live-vs-static count bindings against existing store keys.</done>
</task>

<task type="auto">
  <name>Task 2: header.html (logo · ⌘K button · status strip) + cmdk_modal.html (skeleton)</name>
  <files>src/phaze/templates/shell/partials/header.html, src/phaze/templates/shell/partials/cmdk_modal.html</files>
  <read_first>
    - docs/superpowers/specs/2026-06-28-ui-redesign-assets/prototype.html (header structure; #cmd modal markup/positioning)
    - .planning/phases/57-shell-dag-rail/57-UI-SPEC.md § "Header Status Strip — agent dot states" and § "⌘K Command Palette — skeleton modal" and § "Copywriting Contract" (exact copy strings)
    - src/phaze/templates/base.html (lines 183-188 wave logo SVG; 243-267 theme-toggle button — both LIFT VERBATIM)
    - .planning/phases/57-shell-dag-rail/57-PATTERNS.md § "shell/partials/header.html" and § "shell/partials/cmdk_modal.html"
  </read_first>
  <action>
    Create src/phaze/templates/shell/partials/header.html: an `h-14` header lifting VERBATIM the wave logo SVG (links to `/`) and the auto/moon/sun theme-toggle button + its 3-way `@click` cycle. Add the ⌘K affordance button (Jura/blue search affordance: `flex-1 max-w-md h-9 rounded-lg phaze-panel phaze-border`, muted placeholder per UI-SPEC copy, a `⌘K` mono chip; on click sets the modal open). Add the D-05 status strip: agent status dots (`w-2 h-2 rounded-full` — emerald online / amber degraded / gray offline) + an "Agents · {n}" Jura link to `/admin/agents`, the count bound via `x-text` to `$store.pipeline.agentOnline`, refreshed by the EXISTING `/pipeline/stats` OOB poll — add NO `setInterval`/poll. Create src/phaze/templates/shell/partials/cmdk_modal.html: an Alpine skeleton command palette — `x-data` with `open`, opened by the header button and by `@keydown.window.cmd.k.prevent` / `.ctrl.k`, closed by `@keydown.escape` and backdrop `@click`; `role="dialog" aria-modal="true" aria-label="Command palette"`; on open focus the input via `$nextTick`; on close return focus to the ⌘K trigger button. Panel `w-[640px] max-w-[92vw]` `phaze-panel`/`phaze-border` `rounded-2xl`; header row with search icon + text input (placeholder per UI-SPEC) + an `esc` mono hint; SKELETON body = the single muted placeholder line from the Copywriting Contract — NO search/command wiring (D-04). Use CORE Alpine only (no `@alpinejs/focus`). All elements carry `dark:` variants.
  </action>
  <verify>
    <automated>uv run python -c "from jinja2 import Environment, FileSystemLoader; e=Environment(loader=FileSystemLoader('src/phaze/templates')); e.get_template('shell/partials/header.html'); e.get_template('shell/partials/cmdk_modal.html'); print('parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - header.html and cmdk_modal.html parse as valid Jinja2 templates.
    - header.html contains the lifted wave logo SVG + theme-toggle, a ⌘K button, status dots, and an Agents link to `/admin/agents`; the agent count binds to `$store.pipeline.agentOnline`; there is NO `setInterval` anywhere in the partial.
    - cmdk_modal.html declares `role="dialog"` + `aria-modal="true"`, a `cmd.k`/`ctrl.k` keybinding, an `escape` handler, and a skeleton (empty) body — no fetch / no command list.
    - cmdk_modal.html does not reference `@alpinejs/focus` (core Alpine only).
  </acceptance_criteria>
  <done>The header (logo, ⌘K affordance, D-05 status strip) and the ⌘K skeleton modal (D-04) exist with the full open/close/focus contract and no new poll loop.</done>
</task>

<task type="auto">
  <name>Task 3: Wire header/rail/cmdk into shell.html + palette auto-open + SHELL-02/03 tests</name>
  <files>src/phaze/templates/shell/shell.html, tests/test_shell_routes.py</files>
  <read_first>
    - src/phaze/templates/shell/shell.html (the Plan 02 placeholders for header/rail/cmdk + the syncRailSelection stub to complete)
    - tests/conftest.py (lines 161-167 the `client` fixture)
    - .planning/phases/57-shell-dag-rail/57-VALIDATION.md § "Per-Task Verification Map" (test_rail_nodes_wired, test_tabbar_removed_header_present)
    - .planning/phases/57-shell-dag-rail/57-RESEARCH.md § "Pattern: status strip + rail counts via the existing OOB poll" and Assumption A3 (palette query-param auto-open)
  </read_first>
  <action>
    Edit src/phaze/templates/shell/shell.html: replace the placeholder header bar with `{% include "shell/partials/header.html" %}`, the placeholder rail column with `{% include "shell/partials/rail.html" %}`, and add `{% include "shell/partials/cmdk_modal.html" %}` near the end of the body. Complete the `syncRailSelection(path)` JS to re-apply `aria-current="page"` to the rail node matching the restored path (called by the `htmx:historyRestore` handler from Plan 02). Add the `palette` query-param auto-open hook (A3): on shell load, if the `palette` query param is present (the `/search`→`/?palette=1` redirect target landed by Plan 04), open the ⌘K skeleton modal. Confirm the initial render still passes `oob_counts` falsy (Pitfall 5). Then fill in the remaining Wave-1 stub bodies in tests/test_shell_routes.py (created in Plan 57-01 — REPLACE the stub bodies, do not redeclare the functions): `test_rail_nodes_wired` — GET / body has every navigable rail node carrying `hx-get="/s/<id>"` + `hx-target="#stage-workspace"` + `hx-push-url="true"`, and the analyze node carries `aria-current="page"`; `test_tabbar_removed_header_present` — GET / body does NOT contain the legacy top `<nav>` tab-bar markers, and DOES contain the ⌘K button, the Agents link (`/admin/agents`), and agent status dots. All green.
  </action>
  <verify>
    <automated>uv run pytest tests/test_shell_routes.py tests/test_dead_template_guard.py tests/test_base_html_sri.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `test_rail_nodes_wired` and `test_tabbar_removed_header_present` pass, plus the Plan 02 tests still pass.
    - GET / has the analyze rail node with `aria-current="page"` (completes SHELL-01 success criterion 1's rail-marker).
    - The legacy `<nav>` tab-bar is absent from the shell; the header carries the ⌘K button + Agents link + status dots (SHELL-03).
    - The dead-template guard is still GREEN (rail/header/cmdk are reachable via shell.html includes).
    - Full suite `uv run pytest` and `uv run ruff check . && uv run mypy .` are green (wave-merge gate).
  </acceptance_criteria>
  <done>The rail, header, and ⌘K modal are wired into the shell; rail nav swaps only `#stage-workspace`, the active node shows `aria-current`, the tab-bar is gone, and SHELL-02/03 are proven by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /pipeline/stats poll → DOM | server-computed OOB count seeds interpolated into Alpine `x-init`/`x-text` |
| browser → ⌘K modal | client-only Alpine state; no server input in Phase 57 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-04 | Injection (XSS) | rail count / status-dot `x-text` + OOB `x-init` seeds | mitigate | Every count/dot value is a server-computed `int` from `$store.pipeline` (verified pipeline.py coerces to int); bind via `x-text` to numeric store keys only — never interpolate user-influenced strings. No new store keys, no `| safe`. |
| T-57-05 | Tampering | ⌘K skeleton modal | accept | Client-only Alpine affordance with an empty body — no fetch, no command execution, no user input reaches the server in Phase 57 (D-04). |
| T-57-06 | Spoofing/DoS | status strip refresh | accept | Rides the single existing `/pipeline/stats` 5s poll (D-05); no new poll loop or endpoint is introduced, so no new request-amplification surface. |
| T-57-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan (core Alpine only; `@alpinejs/focus` is Phase 61). |
</threat_model>

<verification>
- `uv run pytest tests/test_shell_routes.py tests/test_dead_template_guard.py tests/test_base_html_sri.py -x` green.
- `uv run pytest` full suite green (wave-merge gate).
- `uv run ruff check . && uv run mypy .` clean.
</verification>

<success_criteria>
- The persistent left DAG rail lists every stage in prototype order with live counts (existing store keys only), and clicking a stage swaps only `#stage-workspace` via HTMX with `hx-push-url` (SHELL-02, success criterion 2).
- The legacy tab-bar is removed; global search is the ⌘K header affordance and agent status shows in the header strip, both riding the single `/pipeline/stats` OOB poll (SHELL-03, success criterion 3).
- The ⌘K skeleton modal opens via button + keybinding and closes on ESC/backdrop with focus return (D-04).
</success_criteria>

<output>
Create `.planning/phases/57-shell-dag-rail/57-03-SUMMARY.md` when done.
</output>
