---
phase: 24
plan: 03
type: execute
wave: 2
depends_on:
  - "24-01"
  - "24-02"
files_modified:
  - alembic/versions/012_add_agents_table_and_backfill.py
  - tests/test_migrations/test_012_upgrade.py
autonomous: true
requirements:
  - DATA-01
  - DATA-03
  - DATA-04
must_haves:
  truths:
    - "Running `alembic upgrade 012` from revision 011 creates the `agents` table with all 8 columns and the slug CHECK constraint"
    - "Migration 012 inserts a `legacy-application-server` agent row whose `scan_roots` JSONB equals `[<value of SCAN_PATH env var>]` (defaulting to `[/data/music]` when unset)"
    - "The legacy agent is born revoked: `token_hash IS NULL` AND `revoked_at IS NOT NULL`"
    - "Migration 012 backfills every pre-existing `files` row with `agent_id = 'legacy-application-server'` and every pre-existing `scan_batches` row likewise"
    - "Migration 012 inserts exactly one LIVE sentinel `scan_batches` row per registered agent with `scan_path = '<watcher>'` and `status = 'live'`"
    - "After 012 completes the partial unique index `uq_scan_batches_agent_id_live` exists with predicate `status = 'live'` and prevents a second LIVE row for the same agent"
    - "After 012 completes the FKs `fk_files_agent_id_agents` and `fk_scan_batches_agent_id_agents` exist with `ON DELETE RESTRICT`"
    - "Migration 012 logs the resolved SCAN_PATH value via `logging.getLogger('alembic.runtime.migration')` for operator audit (per D-05)"
    - "Downgrade 012 -> 011 on a clean DB succeeds and drops everything 012 added (table, columns, FKs, indexes, sentinel)"
  artifacts:
    - path: "alembic/versions/012_add_agents_table_and_backfill.py"
      provides: "Additive migration: create agents, seed legacy + sentinel, add nullable agent_id, FKs, partial UQ, backfill"
      min_lines: 80
      contains: "revision: str = \"012\""
    - path: "tests/test_migrations/test_012_upgrade.py"
      provides: "Integration tests covering DATA-01, DATA-03 (partial), DATA-04 (backfill+legacy+env+sentinel)"
      min_lines: 120
  key_links:
    - from: "alembic/versions/012_add_agents_table_and_backfill.py"
      to: "os.environ SCAN_PATH"
      via: "os.environ.get('SCAN_PATH', '/data/music') inside upgrade()"
      pattern: "os\\.environ\\.get\\(\"SCAN_PATH\""
    - from: "alembic/versions/012_add_agents_table_and_backfill.py"
      to: "Postgres agents table"
      via: "op.create_table('agents', ...) + parameterized INSERT via op.get_bind().execute(sa.text(...), bind_params)"
      pattern: "INSERT INTO agents"
    - from: "alembic/versions/012_add_agents_table_and_backfill.py"
      to: "Postgres files/scan_batches backfill"
      via: "op.execute(sa.text('UPDATE ... SET agent_id = ... WHERE agent_id IS NULL'))"
      pattern: "UPDATE (files|scan_batches) SET agent_id = 'legacy-application-server'"
    - from: "tests/test_migrations/test_012_upgrade.py"
      to: "tests/test_migrations/conftest.py::migrated_engine"
      via: "fixture parameter on each async test"
      pattern: "async def test_.*\\(migrated_engine"
---

<objective>
Land migration 012 — the additive + backfill half of the two-step shape. Creates the `agents` table (with slug CHECK constraint matching the model declaration byte-for-byte), seeds the `legacy-application-server` agent (born revoked) with `scan_roots` resolved from the `SCAN_PATH` env var, adds nullable `agent_id` columns to `files` and `scan_batches`, adds FK constraints with `ON DELETE RESTRICT`, adds the plain index on `scan_batches.agent_id`, runs backfill UPDATEs, inserts the legacy agent's LIVE sentinel scan_batch, and creates the partial unique index that guarantees one-LIVE-per-agent. Writes integration tests in `tests/test_migrations/test_012_upgrade.py` that exercise the migration against a real Postgres DB via the `migrated_engine` fixture from Plan 01.

Purpose: Migration 012 is the safe-additive step of the two-step shape (D-13). It can be paused after running 012 on a real 200K-file dataset before applying 013. Independently revertable. Plan 04 will run 013 on top and tighten constraints.

Output: New `alembic/versions/012_add_agents_table_and_backfill.py`; new `tests/test_migrations/test_012_upgrade.py` covering verification rows #08–#16 from VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-schema-foundation-agent-registry/24-CONTEXT.md
@.planning/phases/24-schema-foundation-agent-registry/24-RESEARCH.md
@.planning/phases/24-schema-foundation-agent-registry/24-PATTERNS.md
@.planning/phases/24-schema-foundation-agent-registry/24-VALIDATION.md
@.planning/phases/24-schema-foundation-agent-registry/24-02-SUMMARY.md
@alembic/versions/011_add_tag_write_log.py
@alembic/versions/002_add_scan_batches_and_unique_path.py
@alembic/versions/009_add_search_vectors.py
@alembic/versions/005_add_metadata_columns.py
@alembic/env.py
@src/phaze/models/agent.py
@src/phaze/models/scan_batch.py
@tests/test_migrations/conftest.py
@CLAUDE.md

<interfaces>
Plan 01 created fixture/helpers in tests/test_migrations/conftest.py:

  MIGRATIONS_TEST_DATABASE_URL = "postgresql+asyncpg://phaze:phaze@localhost:5432/phaze_migrations_test"
  _build_alembic_config(database_url) -> Config
  upgrade_to(cfg, revision) -> None
  downgrade_to(cfg, revision) -> None
  @pytest_asyncio.fixture
  async def migrated_engine() -> AsyncGenerator  # upgrades to head, yields, downgrades to base

Canonical literals that MUST match Plan 02 model declarations byte-for-byte:

  AGENT_ID_REGEX       = ^[a-z0-9]+(-[a-z0-9]+)*$
  LIVE_PREDICATE       = status = 'live'
  LEGACY_AGENT_ID      = legacy-application-server
  SENTINEL_SCAN_PATH   = <watcher>  (literal with angle brackets)
  SCAN_PATH_DEFAULT    = /data/music  (per RESEARCH Summary + Pattern 3; matches config.py:24 and docker-compose.yml:12 — NOT /music)

Constraint and index names emitted by op.create_table / op.create_index (fully-qualified — op.create_table does NOT consult Base.metadata.naming_convention):

  pk_agents
  ck_agents_id_charset
  fk_files_agent_id_agents
  fk_scan_batches_agent_id_agents
  ix_scan_batches_agent_id
  uq_scan_batches_agent_id_live
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 012 — additive + backfill</name>
  <files>alembic/versions/012_add_agents_table_and_backfill.py</files>
  <read_first>
    - alembic/versions/011_add_tag_write_log.py (full file — exact header/imports/revision-id pattern; copy lines 1–19 verbatim with revision IDs swapped to "012"/"011")
    - alembic/versions/002_add_scan_batches_and_unique_path.py (FK creation + index creation + downgrade ordering — closest analog)
    - alembic/versions/009_add_search_vectors.py (raw op.execute SQL emission pattern; project precedent for embedded SQL in migrations)
    - alembic/versions/005_add_metadata_columns.py (simple op.add_column shape)
    - alembic/env.py (async runner contract — Config gets sqlalchemy.url from set_main_option per Plan 01)
    - src/phaze/models/agent.py (Plan 02 file — regex literal must be byte-exact match)
    - src/phaze/models/scan_batch.py (Plan 02 file — partial-index predicate must be byte-exact match)
    - .planning/phases/24-schema-foundation-agent-registry/24-PATTERNS.md (section on alembic/versions/012_add_agents_table_and_backfill.py — full target skeleton, 8-step upgrade order, reverse-order downgrade)
    - .planning/phases/24-schema-foundation-agent-registry/24-RESEARCH.md (Pattern 3 env var read; Pattern 4 upgrade skeleton; Pitfall 3 regex literal drift)
    - .planning/phases/24-schema-foundation-agent-registry/24-CONTEXT.md (Decisions D-01..D-16)
    - .planning/phases/24-schema-foundation-agent-registry/24-02-SUMMARY.md (canonical regex + predicate literals recorded by Plan 02)
  </read_first>
  <action>
    Create `alembic/versions/012_add_agents_table_and_backfill.py` following PATTERNS.md verbatim. Structure:

    HEADER (mirror 011_add_tag_write_log.py lines 1–19 byte-for-byte with IDs swapped):

      Module docstring: "Add agents table, agent_id columns, FKs, and backfill legacy agent."
      Lines 2–5: Revision ID: 012, Revises: 011, Create Date: 2026-05-11
      Imports (stdlib block first, then third-party, then first-party — two blank lines after):
        from collections.abc import Sequence
        import json
        import logging
        import os
        import uuid

        import sqlalchemy as sa
        from sqlalchemy.dialects import postgresql

        from alembic import op
      revision identifiers:
        revision: str = "012"
        down_revision: str | Sequence[str] | None = "011"
        branch_labels: str | Sequence[str] | None = None
        depends_on: str | Sequence[str] | None = None
      Module-level: logger = logging.getLogger("alembic.runtime.migration")

    isort order is mandatory (CLAUDE.md ruff config): stdlib → third-party → first-party. Two blank lines after imports. DO NOT use the alembic/script.py.mako boilerplate (from typing import Sequence, Union) — existing 001–011 already use `from collections.abc import Sequence` + PEP 604 unions.

    upgrade() — 8 steps in this exact order (rationale and patterns from PATTERNS.md + RESEARCH Pattern 4):

      1. op.create_table("agents", ...) with these columns in order:
         - sa.Column("id", sa.String(64), primary_key=True)
         - sa.Column("name", sa.String(128), nullable=False)
         - sa.Column("token_hash", sa.String(128), nullable=True)
         - sa.Column("scan_roots", postgresql.JSONB, nullable=False, server_default=sa.text("'[]'::jsonb"))
         - sa.Column("last_seen_at", sa.DateTime(timezone=True), nullable=True)
         - sa.Column("revoked_at", sa.DateTime(timezone=True), nullable=True)
         - sa.Column("created_at", sa.DateTime(timezone=True), server_default=sa.func.now(), nullable=False)
         - sa.Column("updated_at", sa.DateTime(timezone=True), server_default=sa.func.now(), nullable=False)
         Then two constraint clauses:
         - sa.PrimaryKeyConstraint("id", name="pk_agents")
         - sa.CheckConstraint("id ~ '^[a-z0-9]+(-[a-z0-9]+)*$'", name="ck_agents_id_charset")

      2. Resolve SCAN_PATH from env, log resolution, seed legacy agent (D-05, D-06, D-07):
         raw_scan_path = os.environ.get("SCAN_PATH", "/data/music")
         scan_roots_json = json.dumps([raw_scan_path])
         logger.info("phaze-024: resolved legacy-application-server scan_roots=%s (SCAN_PATH=%r)", scan_roots_json, raw_scan_path)
         op.get_bind().execute(
             sa.text(
                 "INSERT INTO agents (id, name, token_hash, scan_roots, revoked_at, created_at, updated_at) "
                 "VALUES (:id, :name, NULL, CAST(:scan_roots AS jsonb), NOW(), NOW(), NOW())"
             ),
             {"id": "legacy-application-server", "name": "legacy-application-server", "scan_roots": scan_roots_json},
         )

      3. Add nullable agent_id columns (per 005_add_metadata_columns.py shape):
         op.add_column("files", sa.Column("agent_id", sa.String(64), nullable=True))
         op.add_column("scan_batches", sa.Column("agent_id", sa.String(64), nullable=True))

      4. FKs with ON DELETE RESTRICT (D-03; mirror 002 line 43):
         op.create_foreign_key("fk_files_agent_id_agents", "files", "agents", ["agent_id"], ["id"], ondelete="RESTRICT")
         op.create_foreign_key("fk_scan_batches_agent_id_agents", "scan_batches", "agents", ["agent_id"], ["id"], ondelete="RESTRICT")

      5. Plain index on scan_batches.agent_id (D-15: NO separate index on files.agent_id — composite UQ covers it):
         op.create_index("ix_scan_batches_agent_id", "scan_batches", ["agent_id"])

      6. Backfill (D-08, D-14: pure raw SQL via sa.text — no model imports):
         op.execute(sa.text("UPDATE files SET agent_id = 'legacy-application-server' WHERE agent_id IS NULL"))
         op.execute(sa.text("UPDATE scan_batches SET agent_id = 'legacy-application-server' WHERE agent_id IS NULL"))

      7. Sentinel LIVE scan_batch for legacy agent (D-11; UUID generated in Python per RESEARCH Pattern 4):
         sentinel_id = uuid.uuid4()
         op.get_bind().execute(
             sa.text(
                 "INSERT INTO scan_batches (id, agent_id, scan_path, status, total_files, processed_files, "
                 "created_at, updated_at) "
                 "VALUES (:id, :agent_id, '<watcher>', 'live', 0, 0, NOW(), NOW())"
             ),
             {"id": sentinel_id, "agent_id": "legacy-application-server"},
         )

      8. Partial UQ for sentinel (D-12) — AFTER the INSERT so the first run cannot violate it:
         op.create_index(
             "uq_scan_batches_agent_id_live",
             "scan_batches",
             ["agent_id"],
             unique=True,
             postgresql_where=sa.text("status = 'live'"),
         )

    Critical invariants (all grep-verifiable):
    - Regex `^[a-z0-9]+(-[a-z0-9]+)*$` MUST appear in the sa.CheckConstraint exactly — byte-for-byte match with src/phaze/models/agent.py (Plan 02)
    - Predicate `status = 'live'` MUST appear in the postgresql_where exactly — byte-for-byte match with src/phaze/models/scan_batch.py (Plan 02)
    - Sentinel scan_path value is literal SQL string `'<watcher>'` (with angle brackets, per D-10)
    - Sentinel status value is lowercase `'live'` (RESEARCH Example 2 callout)
    - `os.environ.get("SCAN_PATH", "/data/music")` — env var name is SCAN_PATH (NOT PHAZE_SCAN_PATH); fallback `/data/music` (NOT `/music`)
    - The CHECK constraint name in op.create_table is the FULLY-QUALIFIED `ck_agents_id_charset` (NOT short `id_charset`) — op.create_table does NOT consult naming_convention
    - Constraint/index names match this inventory exactly: pk_agents, ck_agents_id_charset, fk_files_agent_id_agents, fk_scan_batches_agent_id_agents, ix_scan_batches_agent_id, uq_scan_batches_agent_id_live
    - DO NOT import from phaze.models (D-14)
    - DO NOT use op.bulk_insert() for the legacy agent
    - DO NOT use print() — use logger.info(...) (ruff T20)
    - DO NOT use op.get_context().autocommit_block() — ScanStatus is VARCHAR(20), no ENUM type involved
    - DO NOT use f-string interpolation for SQL values — only sa.text(...) with bind params
    - DO NOT use unicode arrows (use ASCII `->` in any comments/strings — ruff/yamllint convention)

    downgrade() — reverse order, drop everything 012 created (per PATTERNS.md downgrade pattern):

      1. op.drop_index("uq_scan_batches_agent_id_live", table_name="scan_batches")
      2. op.execute(sa.text("DELETE FROM scan_batches WHERE status = 'live'"))
      3. op.drop_index("ix_scan_batches_agent_id", table_name="scan_batches")
      4. op.drop_constraint("fk_scan_batches_agent_id_agents", "scan_batches", type_="foreignkey")
      5. op.drop_constraint("fk_files_agent_id_agents", "files", type_="foreignkey")
      6. op.drop_column("scan_batches", "agent_id")
      7. op.drop_column("files", "agent_id")
      8. op.drop_table("agents")

    Backfill UPDATEs do not need to be reversed — once the column is dropped the values disappear with it.

    Quality gates:
    - uv run ruff check alembic/versions/012_add_agents_table_and_backfill.py
    - uv run ruff format alembic/versions/012_add_agents_table_and_backfill.py
    - uv run mypy alembic/versions/012_add_agents_table_and_backfill.py  (alembic/ is NOT excluded from mypy; full annotations required on `def upgrade() -> None:` and `def downgrade() -> None:`)
    - Manual smoke: `uv run alembic upgrade 012` against a fresh `phaze_migrations_test` DB exits 0

    Operator pre-condition: the `phaze_migrations_test` DB on localhost:5432 must exist (per Plan 01 conftest docstring). If absent, create it with `docker compose up -d postgres` then `PGPASSWORD=phaze psql -h localhost -U phaze -d phaze -c 'CREATE DATABASE phaze_migrations_test'`.
  </action>
  <verify>
    <automated>uv run ruff check alembic/versions/012_add_agents_table_and_backfill.py &amp;&amp; uv run mypy alembic/versions/012_add_agents_table_and_backfill.py &amp;&amp; grep -F "id ~ '^[a-z0-9]+(-[a-z0-9]+)*$'" alembic/versions/012_add_agents_table_and_backfill.py &amp;&amp; grep -F "status = 'live'" alembic/versions/012_add_agents_table_and_backfill.py &amp;&amp; grep -F 'os.environ.get("SCAN_PATH", "/data/music")' alembic/versions/012_add_agents_table_and_backfill.py</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `grep -c 'revision: str = "012"' alembic/versions/012_add_agents_table_and_backfill.py` returns 1
    - `grep -c 'down_revision: str | Sequence\[str\] | None = "011"' alembic/versions/012_add_agents_table_and_backfill.py` returns 1
    - `grep -F "id ~ '^[a-z0-9]+(-[a-z0-9]+)*$'"` finds the exact regex literal (byte-match with model)
    - `grep -F "status = 'live'"` finds the predicate at least twice (in postgresql_where AND in the INSERT VALUES clause for the sentinel)
    - `grep -F "'<watcher>'"` finds the sentinel scan_path literal
    - `grep -c "ck_agents_id_charset" alembic/versions/012_add_agents_table_and_backfill.py` returns 1
    - `grep -c "fk_files_agent_id_agents" alembic/versions/012_add_agents_table_and_backfill.py` returns 1
    - `grep -c "fk_scan_batches_agent_id_agents" alembic/versions/012_add_agents_table_and_backfill.py` returns 1
    - `grep -c "ondelete=\"RESTRICT\"" alembic/versions/012_add_agents_table_and_backfill.py` returns 2 (both FKs)
    - `grep -c "uq_scan_batches_agent_id_live" alembic/versions/012_add_agents_table_and_backfill.py` returns 1 (created in upgrade; dropped in downgrade) — actually 2 occurrences expected (create + drop)
    - `grep -v '^#' alembic/versions/012_add_agents_table_and_backfill.py | grep -c "from phaze.models"` returns 0 (no model imports in migration, per D-14)
    - `grep -c "op.bulk_insert" alembic/versions/012_add_agents_table_and_backfill.py` returns 0 (forbidden pattern)
    - `grep -c "print(" alembic/versions/012_add_agents_table_and_backfill.py` returns 0 (T20)
    - `grep -c "autocommit_block" alembic/versions/012_add_agents_table_and_backfill.py` returns 0 (no ENUM type involved)
    - `uv run ruff check alembic/versions/012_add_agents_table_and_backfill.py` passes
    - `uv run mypy alembic/versions/012_add_agents_table_and_backfill.py` reports Success
    - Pre-commit clean: `pre-commit run --files alembic/versions/012_add_agents_table_and_backfill.py`
    - Smoke: `uv run alembic upgrade 012` against a fresh `phaze_migrations_test` DB exits 0; `uv run alembic downgrade 011` then exits 0; `uv run alembic upgrade 012` again exits 0 (idempotency check on a clean schema)
  </acceptance_criteria>
  <done>Migration 012 exists, all 8 upgrade steps + 8 downgrade steps present, literals match Plan 02 byte-for-byte, mypy/ruff clean, manual upgrade smoke succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests for migration 012 — backfill, legacy agent shape, sentinel, partial UQ</name>
  <files>tests/test_migrations/test_012_upgrade.py</files>
  <read_first>
    - tests/test_migrations/conftest.py (Plan 01 fixture — confirm the exact name of helpers and the fixture signature)
    - tests/test_models/test_tag_write_log.py (lines 1–80 — model-test class style)
    - tests/test_services/test_ingestion.py (lines 200–300 — async DB integration pattern with `@pytest.mark.asyncio`, `async with engine.connect() as conn`, `await conn.execute(text(...))`)
    - alembic/versions/012_add_agents_table_and_backfill.py (Task 1 output — confirm exact column names, INSERT shape, constraint names)
    - .planning/phases/24-schema-foundation-agent-registry/24-PATTERNS.md (section on tests/test_migrations/test_012_upgrade.py — full test-coverage map and pattern excerpts)
    - .planning/phases/24-schema-foundation-agent-registry/24-VALIDATION.md (rows #08–#16 — the 9 verification points this file owns)
    - .planning/phases/24-schema-foundation-agent-registry/24-RESEARCH.md (Validation Architecture table; special note on env-var tests at the end of "Test coverage map")
  </read_first>
  <behavior>
    Test file covers VALIDATION.md verification rows #08–#16 plus DATA-01 SQL-level CHECK constraint behavior. Each test uses `@pytest.mark.asyncio` and consumes `migrated_engine` from the conftest — except env-var tests which need to control SCAN_PATH BEFORE the alembic upgrade runs (the migrated_engine fixture upgrades at fixture-setup time).

    Tests required:

    1. `test_agents_table_columns(migrated_engine) -> None` — DATA-01
       After head upgrade, query `information_schema.columns WHERE table_name='agents'`; assert column set equals {id, name, token_hash, scan_roots, last_seen_at, revoked_at, created_at, updated_at}.

    2. `test_scan_roots_is_jsonb(migrated_engine) -> None` — DATA-01
       Query `information_schema.columns WHERE table_name='agents' AND column_name='scan_roots'`; assert `data_type` is 'jsonb'.

    3. `test_id_charset_check(migrated_engine) -> None` — DATA-01 (T-V5-01 mitigation)
       Attempt INSERTs with hostile slugs and assert each raises IntegrityError (or check constraint violation):
         - "UPPER" (uppercase letters)
         - "--double" (leading hyphen + double hyphen)
         - "-leading" (leading hyphen)
         - "trailing-" (trailing hyphen)
         - "under_score" (underscore)
       Then INSERT "valid-slug-123" and assert it succeeds. Use `async with engine.begin()` + `await conn.execute(text("INSERT INTO agents (id, name, scan_roots) VALUES (:id, :name, CAST('[]' AS jsonb))"), {...})`. Wrap each forbidden insert in `with pytest.raises(IntegrityError):` (import from sqlalchemy.exc).

    4. `test_token_hash_nullable(migrated_engine) -> None` — DATA-01 (T-V6-01 mitigation)
       Assert via information_schema that `agents.token_hash` is_nullable = 'YES'.

    5. `test_legacy_agent_born_revoked(migrated_engine) -> None` — DATA-04 (T-V4-01 mitigation)
       Query `SELECT token_hash, revoked_at FROM agents WHERE id = 'legacy-application-server'`. Assert token_hash IS NULL and revoked_at IS NOT NULL.

    6. `test_legacy_agent_scan_roots_fallback() -> None` — DATA-04
       Special handling needed because migrated_engine pre-upgrades. Two options (planner picks one — recommend option A in the task action below): (A) build a dedicated cfg in the test, upgrade to 011 first on a sub-test DB, set SCAN_PATH=None via env unset, then upgrade to 012, then query; (B) skip the fixture and call _build_alembic_config + command.upgrade in the test body. Whichever is chosen, assert that without SCAN_PATH set, scan_roots == ["/data/music"].

    7. `test_legacy_agent_scan_roots_from_env() -> None` — DATA-04
       Same pattern as test 6 but with `os.environ["SCAN_PATH"] = "/custom/path"` set BEFORE `command.upgrade(cfg, "012")`; assert scan_roots == ["/custom/path"].

    8. `test_sentinel_scan_path_literal(migrated_engine) -> None` — DATA-04
       Query the legacy agent's LIVE sentinel: `SELECT scan_path FROM scan_batches WHERE agent_id = 'legacy-application-server' AND status = 'live'`. Assert scan_path == "<watcher>" (with angle brackets).

    9. `test_legacy_sentinel_exists(migrated_engine) -> None` — DATA-03
       `SELECT COUNT(*) FROM scan_batches WHERE agent_id = 'legacy-application-server' AND status = 'live'`. Assert count == 1.

    10. `test_partial_uq_rejects_dup_live(migrated_engine) -> None` — DATA-03
        Attempt to INSERT a second LIVE scan_batch for the legacy agent and assert IntegrityError. Use `async with engine.begin()` so the violation rolls back cleanly.

    11. `test_partial_uq_allows_multiple_non_live(migrated_engine) -> None` — DATA-03
        INSERT two RUNNING (or COMPLETED) scan_batches for the legacy agent with the same agent_id; assert both succeed (the partial index only enforces uniqueness when status = 'live').

    12. `test_backfill_files(migrated_engine) -> None` — DATA-04 (T-V5-02 parameterized SQL)
        Special pre-state needed: this test needs pre-existing files rows before migration 012 runs. Two implementation paths:
        (i) The migrated_engine fixture upgrades to head, so this test runs against an empty `files` table. Insert a row that simulates pre-existing data using SQL that bypasses the FK (e.g., insert via a transaction that suspends FK checks isn't acceptable). Better: insert against the legacy agent which exists.
        Realistic approach: insert a `files` row with agent_id explicitly set to 'legacy-application-server'; the test then asserts that the row exists with that value. The migration-time backfill IS exercised by tests 6+7 (which run upgrade from 011→012 with pre-existing fixtures), but to actually test the WHERE agent_id IS NULL UPDATE we need a from-011 upgrade flow:
        - Build cfg, upgrade to "011", insert a files row using ONLY pre-012 columns (no agent_id), upgrade to "012", query and assert agent_id == "legacy-application-server".
        Use `upgrade_to(cfg, "011")` and `upgrade_to(cfg, "012")` helpers from Plan 01.

    13. `test_backfill_scan_batches(migrated_engine) -> None` — DATA-04
        Same pattern as test_backfill_files but for scan_batches. Pre-existing scan_batch row gets backfilled to legacy-application-server.

    Test framework conventions (from PATTERNS.md and existing tests):
    - `@pytest.mark.asyncio` decorator on every async test (auto-mode is enabled in pyproject.toml)
    - `# type: ignore[no-untyped-def]` after the def line for tests taking fixture parameters (matches tests/conftest.py:28 convention)
    - `from sqlalchemy import text` for raw SQL
    - `from sqlalchemy.exc import IntegrityError` for the rejection tests
    - `import pytest`, `import pytest_asyncio` as needed
    - Tests 6, 7, 12, 13 do NOT use the migrated_engine fixture (which auto-upgrades to head); instead they build their own cfg via `_build_alembic_config` imported from `tests.test_migrations.conftest`, control the upgrade sequence with `upgrade_to`/`downgrade_to`, and create their own engine for queries
    - For tests 6/7, use `monkeypatch` fixture (pytest built-in) to set/unset SCAN_PATH; this is cleaner than `os.environ` mutation + `patch.dict`
  </behavior>
  <action>
    Step 1 — TDD RED: write all 13 tests in `tests/test_migrations/test_012_upgrade.py` per the behavior block above. Run `uv run pytest tests/test_migrations/test_012_upgrade.py -x -v` and confirm collection succeeds. The tests will execute against the migration from Task 1 and should pass — but if the migration has any bugs (wrong regex, wrong predicate, missing constraint), the relevant tests will fail.

    Step 2 — File structure:

    Header docstring: `"""Tests for migration 012: agents table + backfill."""`

    Imports (stdlib first, third-party, first-party — isort order, 2 blank lines after):
      from collections.abc import AsyncGenerator
      import os
      import uuid

      import pytest
      import pytest_asyncio
      from sqlalchemy import text
      from sqlalchemy.exc import IntegrityError
      from sqlalchemy.ext.asyncio import create_async_engine

      from tests.test_migrations.conftest import (
          MIGRATIONS_TEST_DATABASE_URL,
          _build_alembic_config,
          upgrade_to,
          downgrade_to,
      )
      from alembic import command

    (If a small helper test-local fixture is needed for tests 6/7/12/13, define a `clean_db` fixture inside this file that runs `command.downgrade(cfg, "base")` then `command.upgrade(cfg, "011")` to give a clean revision-011 starting point.)

    Step 3 — implement each test per behavior block. Specific shape for the IntegrityError test (#3):

      @pytest.mark.asyncio
      async def test_id_charset_check(migrated_engine) -> None:  # type: ignore[no-untyped-def]
          """CHECK constraint rejects hostile slugs (uppercase, leading/trailing/double hyphens, underscore)."""
          hostile = ["UPPER", "--double", "-leading", "trailing-", "under_score"]
          for bad in hostile:
              with pytest.raises(IntegrityError):
                  async with migrated_engine.begin() as conn:
                      await conn.execute(
                          text(
                              "INSERT INTO agents (id, name, scan_roots, created_at, updated_at) "
                              "VALUES (:id, :name, CAST('[]' AS jsonb), NOW(), NOW())"
                          ),
                          {"id": bad, "name": "test"},
                      )
          # Sanity: a valid slug succeeds
          async with migrated_engine.begin() as conn:
              await conn.execute(
                  text(
                      "INSERT INTO agents (id, name, scan_roots, created_at, updated_at) "
                      "VALUES (:id, :name, CAST('[]' AS jsonb), NOW(), NOW())"
                  ),
                  {"id": "valid-slug-123", "name": "valid"},
              )

    For the backfill tests (#12, #13), the structure is:

      @pytest.mark.asyncio
      async def test_backfill_files() -> None:
          """Existing files rows from revision 011 get agent_id = 'legacy-application-server' after 012."""
          cfg = _build_alembic_config(MIGRATIONS_TEST_DATABASE_URL)
          downgrade_to(cfg, "base")
          upgrade_to(cfg, "011")
          # Insert a pre-012 files row (no agent_id column exists yet)
          engine = create_async_engine(MIGRATIONS_TEST_DATABASE_URL)
          try:
              async with engine.begin() as conn:
                  await conn.execute(
                      text(
                          "INSERT INTO files (id, sha256_hash, original_path, original_filename, current_path, "
                          "file_type, file_size, state, created_at, updated_at) "
                          "VALUES (:id, :hash, :path, :name, :path, 'mp3', 1000, 'discovered', NOW(), NOW())"
                      ),
                      {"id": uuid.uuid4(), "hash": "a" * 64, "path": "/music/x.mp3", "name": "x.mp3"},
                  )
              upgrade_to(cfg, "012")
              async with engine.connect() as conn:
                  result = await conn.execute(text("SELECT agent_id FROM files WHERE original_path = '/music/x.mp3'"))
                  row = result.one()
              assert row.agent_id == "legacy-application-server"
          finally:
              await engine.dispose()
              downgrade_to(cfg, "base")

    For env-var tests (#6, #7), use the pytest `monkeypatch` fixture:

      @pytest.mark.asyncio
      async def test_legacy_agent_scan_roots_from_env(monkeypatch: pytest.MonkeyPatch) -> None:
          """SCAN_PATH env var flows into legacy agent's scan_roots JSONB array."""
          monkeypatch.setenv("SCAN_PATH", "/custom/path")
          cfg = _build_alembic_config(MIGRATIONS_TEST_DATABASE_URL)
          downgrade_to(cfg, "base")
          upgrade_to(cfg, "012")
          engine = create_async_engine(MIGRATIONS_TEST_DATABASE_URL)
          try:
              async with engine.connect() as conn:
                  result = await conn.execute(text("SELECT scan_roots FROM agents WHERE id = 'legacy-application-server'"))
                  row = result.one()
              assert row.scan_roots == ["/custom/path"]
          finally:
              await engine.dispose()
              downgrade_to(cfg, "base")

      @pytest.mark.asyncio
      async def test_legacy_agent_scan_roots_fallback(monkeypatch: pytest.MonkeyPatch) -> None:
          """SCAN_PATH absent yields fallback ['/data/music'] in legacy agent's scan_roots."""
          monkeypatch.delenv("SCAN_PATH", raising=False)
          cfg = _build_alembic_config(MIGRATIONS_TEST_DATABASE_URL)
          downgrade_to(cfg, "base")
          upgrade_to(cfg, "012")
          engine = create_async_engine(MIGRATIONS_TEST_DATABASE_URL)
          try:
              async with engine.connect() as conn:
                  result = await conn.execute(text("SELECT scan_roots FROM agents WHERE id = 'legacy-application-server'"))
                  row = result.one()
              assert row.scan_roots == ["/data/music"]
          finally:
              await engine.dispose()
              downgrade_to(cfg, "base")

    Critical invariants:
    - Tests that DO use `migrated_engine` MUST NOT manually call command.upgrade/downgrade (the fixture handles teardown)
    - Tests that DON'T use `migrated_engine` MUST call `downgrade_to(cfg, "base")` in a `finally` block so subsequent tests start clean
    - DO NOT share state between tests via module-level engines — every test that creates an engine MUST `await engine.dispose()` in a `finally` block
    - DO NOT use `assert row[0]` indexing — use `.scalars()` or column-name access (e.g., `row.scan_roots`) for readability and SQLAlchemy 2.0 compatibility

    Step 4 — TDD GREEN: `uv run pytest tests/test_migrations/test_012_upgrade.py -x -v` passes all 13 tests against the migration from Task 1.

    Step 5 — Quality gates: `uv run ruff check tests/test_migrations/test_012_upgrade.py`, `uv run ruff format`, pre-commit run on the file.
  </action>
  <verify>
    <automated>uv run pytest tests/test_migrations/test_012_upgrade.py -x -v 2>&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_migrations/test_012_upgrade.py` exists with at least 13 test functions
    - `grep -c '^async def test_' tests/test_migrations/test_012_upgrade.py` returns at least 13
    - All test names from VALIDATION.md rows #08–#16 present plus the four model-shape tests (#02, #04 from VALIDATION.md DATA-01 group): grep finds `test_agents_table_columns`, `test_scan_roots_is_jsonb`, `test_id_charset_check`, `test_token_hash_nullable`, `test_legacy_agent_born_revoked`, `test_legacy_agent_scan_roots_fallback`, `test_legacy_agent_scan_roots_from_env`, `test_sentinel_scan_path_literal`, `test_legacy_sentinel_exists`, `test_partial_uq_rejects_dup_live`, `test_partial_uq_allows_multiple_non_live`, `test_backfill_files`, `test_backfill_scan_batches`
    - `uv run pytest tests/test_migrations/test_012_upgrade.py -x -v` passes all tests
    - `grep -c "@pytest.mark.asyncio" tests/test_migrations/test_012_upgrade.py` matches the number of async test defs
    - `grep -c "engine.dispose()" tests/test_migrations/test_012_upgrade.py` is at least as many as the number of tests that build their own engine (#6, #7, #12, #13 minimum)
    - `grep -c "monkeypatch" tests/test_migrations/test_012_upgrade.py` is at least 2 (env-var tests)
    - `uv run ruff check tests/test_migrations/test_012_upgrade.py` passes
    - Pre-commit clean
    - Quick suite check: `uv run pytest tests/test_models/test_agent.py tests/test_migrations/ -x -q` passes in under 30s (per VALIDATION.md Sampling Rate)
  </acceptance_criteria>
  <done>All 13 integration tests pass against migration 012, covering DATA-01 (CHECK + nullable + column shape), DATA-03 (sentinel + partial UQ behavior), DATA-04 (legacy agent shape + env var resolution + backfill).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env (SCAN_PATH) -> migration -> Postgres JSONB | The migration reads SCAN_PATH at runtime and embeds the string into `agents.scan_roots`. Path-traversal payloads (e.g., `../../etc`) flow into JSONB as audit data only; no filesystem operation consumes the value in Phase 24. Phase 27 watcher will validate consumed paths. |
| operator slug -> migration -> agents table | Phase 25 will INSERT operator-provided slugs. The CHECK constraint declared in this migration is the durable last line of defense. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03-T | Tampering | SQL injection via slug in migration INSERT | mitigate | All INSERTs use `op.get_bind().execute(sa.text("..."), bind_params)` — never f-string interpolation. Legacy slug is hardcoded; no operator input flows here in Phase 24. Pattern matters for Phase 25 extending the migration approach. |
| T-24-03-T | Tampering | Backfill UPDATE accidentally widens scope to non-NULL rows | mitigate | `WHERE agent_id IS NULL` predicate restricts UPDATEs to rows that lack attribution. Idempotent on re-run. |
| T-24-03-T | Tampering | SCAN_PATH env var with hostile path persists to JSONB | accept | Audit-only field in Phase 24; Phase 27 validates filesystem operations. D-05 logging contract makes operator audit trivial: `Resolved scan_roots = ...` log line. |
| T-24-03-E | Elevation | Hostile slug crafted by operator bypasses application validation (e.g., `; DROP TABLE`) | mitigate | DB-level `CHECK (id ~ '^[a-z0-9]+(-[a-z0-9]+)*$')` rejects at INSERT time regardless of application path. Plan 03 Test #3 exercises five concrete hostile forms. |
| T-24-03-R | Repudiation | Operator cannot determine what scan_roots the migration used after the fact | mitigate | `logger.info("phaze-024: resolved legacy-application-server scan_roots=...")` writes to the alembic.runtime.migration logger which is operator-visible via `just db-upgrade` output. |
</threat_model>

<verification>
After both tasks complete:
- `uv run alembic upgrade 012` against a fresh `phaze_migrations_test` DB exits 0
- `uv run alembic downgrade 011` then `uv run alembic upgrade 012` exit 0 (roundtrip works)
- `uv run pytest tests/test_migrations/test_012_upgrade.py -x -v` reports 13/13 pass
- `uv run pytest tests/test_migrations/ tests/test_models/test_agent.py -x -q` (the quick-feedback set from VALIDATION.md Sampling Rate) exits 0 in under 30s
- Full suite: `uv run pytest -x -q` passes (no regressions from Plan 02 model edits or migration 012)
- Grep contract:
  - `grep -F "id ~ '^[a-z0-9]+(-[a-z0-9]+)*$'" alembic/versions/012_add_agents_table_and_backfill.py` exits 0
  - `grep -F "id ~ '^[a-z0-9]+(-[a-z0-9]+)*$'" src/phaze/models/agent.py` exits 0
  - `grep -F "status = 'live'" alembic/versions/012_add_agents_table_and_backfill.py` exits 0 (twice — postgresql_where + INSERT VALUES)
  - `grep -F "status = 'live'" src/phaze/models/scan_batch.py` exits 0
- `uv run mypy alembic/versions/012_add_agents_table_and_backfill.py` reports Success
- Pre-commit run --files passes on both files
</verification>

<success_criteria>
- Migration 012 created with correct revision/down_revision identifiers, full 8-step upgrade (table create → seed legacy agent → add columns → FKs → plain index → backfill → sentinel insert → partial UQ), full 8-step downgrade
- Regex literal and partial-index predicate match Plan 02 declarations byte-for-byte
- 13 integration tests in `test_012_upgrade.py` covering DATA-01 (CHECK + nullable + column inventory), DATA-03 (sentinel + partial UQ behavior), DATA-04 (env var resolution + legacy agent shape + backfill of pre-existing rows)
- Manual smoke: alembic upgrade 012 → downgrade 011 → upgrade 012 against `phaze_migrations_test` succeeds
- mypy + ruff + pre-commit pass on both files
- Quick test suite executes in <30s; full suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/24-schema-foundation-agent-registry/24-03-SUMMARY.md` recording: any deviation from the upgrade/downgrade step ordering in PATTERNS.md, the exact log line format chosen for the SCAN_PATH audit (D-05), and whether any IntegrityError-shape was different than expected (some Postgres versions surface CHECK violations as different SQLSTATE — verify SQLAlchemy maps them all to IntegrityError). Plan 04 must read this summary before writing migration 013 to ensure the downgrade ordering remains symmetric.
</output>
