---
phase: 13-ai-destination-paths
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/prompts/naming.md
  - src/phaze/services/proposal.py
  - tests/test_services/test_proposal.py
autonomous: true
requirements:
  - PATH-01
user_setup: []

must_haves:
  truths:
    - "LLM prompt includes directory path generation rules with 3-step decision tree"
    - "FileProposalResponse model accepts proposed_path field"
    - "store_proposals persists proposed_path with normalization (strip slashes, collapse doubles)"
  artifacts:
    - path: "src/phaze/prompts/naming.md"
      provides: "Path generation rules section with directory convention templates"
      contains: "## Directory Path Rules"
    - path: "src/phaze/services/proposal.py"
      provides: "Extended FileProposalResponse with proposed_path, updated store_proposals"
      exports: ["FileProposalResponse", "store_proposals"]
    - path: "tests/test_services/test_proposal.py"
      provides: "Tests for proposed_path in response model, path normalization, store_proposals"
  key_links:
    - from: "src/phaze/prompts/naming.md"
      to: "src/phaze/services/proposal.py"
      via: "load_prompt_template reads naming.md, LLM returns proposed_path in response"
      pattern: "proposed_path"
    - from: "src/phaze/services/proposal.py"
      to: "src/phaze/models/proposal.py"
      via: "store_proposals persists proposed_path to RenameProposal.proposed_path column"
      pattern: "proposed_path=path_raw"
---

<objective>
Extend the LLM prompt template and proposal pipeline to generate destination paths alongside filenames.

Purpose: PATH-01 requires the LLM to produce proposed_path using the v1.0 naming format directory conventions. This plan adds path generation rules to the prompt, extends the Pydantic response model, and updates store_proposals to persist normalized paths.
Output: Updated naming.md prompt, extended FileProposalResponse, path-aware store_proposals with normalization.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ai-destination-paths/13-CONTEXT.md
@.planning/phases/13-ai-destination-paths/13-RESEARCH.md
@.planning/phases/13-ai-destination-paths/13-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/phaze/services/proposal.py:
```python
class FileProposalResponse(BaseModel):
    file_index: int
    proposed_filename: str
    confidence: float
    artist: str | None = None
    event_name: str | None = None
    venue: str | None = None
    date: str | None = None
    source_type: str | None = None
    stage: str | None = None
    day_number: int | None = None
    b2b_partners: list[str] = []
    reasoning: str

class BatchProposalResponse(BaseModel):
    proposals: list[FileProposalResponse]

async def store_proposals(
    session: AsyncSession,
    file_ids: list[str],
    batch_response: BatchProposalResponse,
    files_context: list[dict[str, Any]],
) -> int:
```

From src/phaze/models/proposal.py:
```python
class RenameProposal(TimestampMixin, Base):
    proposed_path: Mapped[str | None] = mapped_column(Text, nullable=True)
```

From src/phaze/prompts/naming.md:
- Template uses `{files_json}` placeholder for file data
- Output Instructions section defines response fields (file_index, proposed_filename, confidence, metadata fields, reasoning)
- No existing path generation section
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend FileProposalResponse and store_proposals with proposed_path</name>
  <files>src/phaze/services/proposal.py, tests/test_services/test_proposal.py</files>
  <read_first>
    - src/phaze/services/proposal.py (current FileProposalResponse model and store_proposals function)
    - src/phaze/models/proposal.py (RenameProposal model with existing proposed_path column)
    - tests/test_services/test_proposal.py (existing test patterns for proposal service)
  </read_first>
  <behavior>
    - Test 1: FileProposalResponse accepts proposed_path as optional str (None default) -- `FileProposalResponse(file_index=0, proposed_filename="test.mp3", confidence=0.9, reasoning="test", proposed_path="performances/artists/Disclosure")` succeeds, `resp.proposed_path == "performances/artists/Disclosure"`
    - Test 2: FileProposalResponse defaults proposed_path to None -- `FileProposalResponse(file_index=0, proposed_filename="test.mp3", confidence=0.9, reasoning="test")` has `resp.proposed_path is None`
    - Test 3: store_proposals persists proposed_path on RenameProposal -- mock session, verify `RenameProposal` created with `proposed_path="performances/artists/Disclosure"` when proposal has that path
    - Test 4: store_proposals normalizes path by stripping leading/trailing slashes -- input `/performances/artists/Disclosure/` becomes `performances/artists/Disclosure`
    - Test 5: store_proposals collapses double slashes -- input `performances//artists//Disclosure` becomes `performances/artists/Disclosure`
    - Test 6: store_proposals leaves proposed_path as None when proposal has None path
  </behavior>
  <action>
    1. Add `proposed_path: str | None = None` field to `FileProposalResponse` class in `src/phaze/services/proposal.py`, placed after `proposed_filename` and before `confidence`. Per D-06, this is the Pydantic structured output field the LLM will populate.

    2. Update `store_proposals()` function in `src/phaze/services/proposal.py` to:
       - Extract `path_raw = proposal.proposed_path` after the confidence clamping
       - Normalize the path if not None: strip leading/trailing slashes, collapse double slashes
       - Pass `proposed_path=path_raw` to the `RenameProposal(...)` constructor (the column already exists per D-06)

    Normalization code (insert before `record = RenameProposal(...)` around line 282):
    ```python
    path_raw = proposal.proposed_path
    if path_raw:
        path_raw = path_raw.strip("/")
        while "//" in path_raw:
            path_raw = path_raw.replace("//", "/")
    ```

    Then add to the RenameProposal constructor:
    ```python
    proposed_path=path_raw,
    ```

    3. Add tests to `tests/test_services/test_proposal.py` covering the 6 behaviors above. Follow existing test patterns in the file (class-based, direct model imports, MagicMock for session).
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/phaze && uv run pytest tests/test_services/test_proposal.py -x -k "path" -v</automated>
  </verify>
  <acceptance_criteria>
    - src/phaze/services/proposal.py contains `proposed_path: str | None = None` in FileProposalResponse class
    - src/phaze/services/proposal.py contains `path_raw = proposal.proposed_path` in store_proposals function
    - src/phaze/services/proposal.py contains `path_raw.strip("/")` for normalization
    - src/phaze/services/proposal.py contains `proposed_path=path_raw` in RenameProposal constructor
    - tests/test_services/test_proposal.py contains at least 4 test methods with "path" in name
    - `uv run pytest tests/test_services/test_proposal.py -x -k "path"` exits 0
  </acceptance_criteria>
  <done>FileProposalResponse accepts proposed_path, store_proposals persists and normalizes it, all path tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Extend naming.md prompt with directory path rules</name>
  <files>src/phaze/prompts/naming.md</files>
  <read_first>
    - src/phaze/prompts/naming.md (current prompt template structure)
    - .planning/phases/13-ai-destination-paths/13-CONTEXT.md (D-01 through D-05 for path generation decisions)
    - .planning/phases/13-ai-destination-paths/13-RESEARCH.md (Pattern 1: Prompt Template Extension section)
  </read_first>
  <action>
    Per D-01, D-02, D-03, D-04, D-05: extend the existing `naming.md` prompt template with path generation rules. Single LLM call produces both filename and path.

    1. Add a new `## Directory Path Rules` section AFTER the `## Confidence Scoring` section and BEFORE the `## Metadata Extraction` section. Content:

    ```markdown
    ## Directory Path Rules

    For each file, also propose a destination directory path. Use this 3-step decision tree:

    ### Step 1: Determine Category
    - Album tracks (identified by track number, album name, or studio recording indicators) -> `music/`
    - DJ sets, live performances, festival recordings, concert bootlegs, radio shows -> `performances/`

    ### Step 2: Determine Subcategory

    For `performances/`:
    - Artist DJ sets and live sets -> `performances/artists/{Artist Name}/`
    - Festival recordings -> `performances/festivals/{Festival Name} {Year}/`
    - Concert recordings -> `performances/concerts/{Concert Name} {Year}/`
    - Radio shows -> `performances/radioshows/{Radioshow Name}/`

    For `music/`:
    - Album tracks -> `music/{Artist}/{Album}/`

    ### Step 3: Year Handling (festivals and concerts only)
    - If year is known, include in the directory name: `performances/festivals/Coachella 2024/`
    - If year is unknown, omit it: `performances/festivals/Coachella/`

    ### Path Confidence
    - If you cannot determine a reasonable path from available metadata, set `proposed_path` to null.
    - A null path means the file stays in its current location during execution.
    - It is better to leave the path null than to guess incorrectly.
    ```

    2. Update the `## Output Instructions` section to include `proposed_path` in the response field list. Add this line after the `proposed_filename` bullet:
    ```
    - `proposed_path`: The destination directory path (e.g. "performances/artists/Disclosure") or null if uncertain
    ```

    Do NOT change the `{files_json}` placeholder, the naming rules, the date format, or any other existing sections.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/phaze && grep -c "Directory Path Rules" src/phaze/prompts/naming.md && grep -c "proposed_path" src/phaze/prompts/naming.md && uv run pytest tests/test_services/test_proposal.py -x -k "load_prompt"</automated>
  </verify>
  <acceptance_criteria>
    - src/phaze/prompts/naming.md contains "## Directory Path Rules"
    - src/phaze/prompts/naming.md contains "performances/artists/{Artist Name}/"
    - src/phaze/prompts/naming.md contains "performances/festivals/{Festival Name} {Year}/"
    - src/phaze/prompts/naming.md contains "performances/concerts/{Concert Name} {Year}/"
    - src/phaze/prompts/naming.md contains "performances/radioshows/{Radioshow Name}/"
    - src/phaze/prompts/naming.md contains "music/{Artist}/{Album}/"
    - src/phaze/prompts/naming.md contains "`proposed_path`" in the Output Instructions section
    - src/phaze/prompts/naming.md still contains "{files_json}" placeholder (not removed)
    - src/phaze/prompts/naming.md still contains "## Naming Rules" (existing section preserved)
  </acceptance_criteria>
  <done>Prompt template includes directory path rules with 3-step decision tree, album vs performance separation, and proposed_path in output instructions</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_services/test_proposal.py -x` -- all proposal tests pass including new path tests
- `uv run ruff check src/phaze/services/proposal.py` -- no lint errors
- `uv run mypy src/phaze/services/proposal.py` -- no type errors
- `grep "proposed_path" src/phaze/services/proposal.py` -- field exists in model and store function
- `grep "Directory Path Rules" src/phaze/prompts/naming.md` -- prompt section exists
</verification>

<success_criteria>
- FileProposalResponse has proposed_path: str | None = None
- store_proposals normalizes and persists proposed_path
- naming.md has Directory Path Rules with performances/ and music/ trees
- All existing tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/13-ai-destination-paths/13-01-SUMMARY.md`
</output>
