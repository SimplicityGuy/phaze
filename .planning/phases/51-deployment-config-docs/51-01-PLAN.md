---
phase: 51-deployment-config-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/config.py
  - src/phaze/routers/pipeline.py
  - src/phaze/tasks/release_awaiting_cloud.py
  - tests/test_config/test_cloud_burst_toggle.py
  - tests/test_routing_seam.py
  - tests/test_staging_cron.py
  - tests/test_routers/test_pipeline.py
autonomous: true
requirements: [CLOUDDEPLOY-04, CLOUDDEPLOY-02]
user_setup: []

must_haves:
  truths:
    - "With cloud_burst_enabled=False, a long (>= threshold) file routes to the local/fileserver queue and is NEVER set to AWAITING_CLOUD"
    - "With cloud_burst_enabled=True, a long file is held in AWAITING_CLOUD (existing Phase 49/50 behavior, unchanged)"
    - "With cloud_burst_enabled=False, stage_cloud_window returns {staged:0, skipped:0} without taking the advisory lock or staging any push_file job"
    - "With cloud_burst_enabled=False, trigger_backfill_cloud early-returns and mutates zero file.state rows (no ANALYSIS_FAILED -> DISCOVERED reset)"
    - "cloud_burst_enabled parses from PHAZE_CLOUD_BURST_ENABLED, defaults to False"
  artifacts:
    - path: "src/phaze/config.py"
      provides: "cloud_burst_enabled bool Field on ControlSettings"
      contains: "cloud_burst_enabled"
    - path: "src/phaze/routers/pipeline.py"
      provides: "toggle-gated routing seam + backfill early-return"
      contains: "cloud_burst_enabled"
    - path: "src/phaze/tasks/release_awaiting_cloud.py"
      provides: "stage_cloud_window toggle no-op gate"
      contains: "cloud_burst_enabled"
  key_links:
    - from: "src/phaze/routers/pipeline.py"
      to: "src/phaze/config.py::ControlSettings.cloud_burst_enabled"
      via: "module-level settings singleton read"
      pattern: "settings\\.cloud_burst_enabled"
    - from: "src/phaze/tasks/release_awaiting_cloud.py"
      to: "src/phaze/config.py::ControlSettings.cloud_burst_enabled"
      via: "get_settings() read at top of stage_cloud_window"
      pattern: "cloud_burst_enabled"
---

<objective>
Add the single net-new code path of Phase 51: the `cloud_burst_enabled` master toggle (D-01) and the THREE exact short-circuit sites that make OFF revert to pure all-local analysis (D-02, D-03, D-04). This is CLOUDDEPLOY-04, and the new field also satisfies the master-toggle slice of CLOUDDEPLOY-02.

Purpose: a fresh v5.0 deploy must behave all-local (no cloud activity anywhere) until the operator provisions the A1 and explicitly opts in. One flag ⇒ zero cloud activity at every entry point.
Output: `cloud_burst_enabled` Field on `ControlSettings`; gated routing seam, staging cron, and backfill trigger; unit tests proving OFF and ON behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-deployment-config-docs/51-CONTEXT.md
@.planning/phases/51-deployment-config-docs/51-RESEARCH.md
@.planning/phases/51-deployment-config-docs/51-PATTERNS.md
@.planning/phases/51-deployment-config-docs/51-VALIDATION.md

<interfaces>
<!-- Verified current signatures the executor edits. Source: src/phaze/routers/pipeline.py -->
_route_discovered_by_duration(app_state, session, files_with_duration: list[tuple[FileRecord, float|None]], threshold_sec: int, models_path: str) -> dict[str,int]
  - line 308: `is_long = duration is not None and duration >= threshold_sec`
  - called by trigger_analysis (~:364), trigger_analysis_ui (~:596), trigger_backfill_cloud (~:676)
trigger_backfill_cloud(request) — early-return template shape at :662-667 ({"request", "count": 0})
stage_cloud_window(ctx) — tasks/release_awaiting_cloud.py:109; reads `max_in_flight = get_settings().cloud_max_in_flight  # type: ignore[attr-defined]` at :121
config.py:292 enable_saq_ui — the exact bool-Field pattern to mirror; ControlSettings starts at :338; cloud knobs block at :376-416
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cloud_burst_enabled field to ControlSettings</name>
  <files>src/phaze/config.py, tests/test_config/test_cloud_burst_toggle.py</files>
  <read_first>
    - src/phaze/config.py lines 288-296 (enable_saq_ui pattern) and 376-416 (ControlSettings cloud knob block)
    - tests/test_config/test_cloud_route_threshold.py (the alias/default/bounds test pattern for a cloud knob)
    - 51-PATTERNS.md "src/phaze/config.py" section (exact field to add + why ControlSettings not BaseSettings)
  </read_first>
  <behavior>
    - Test: ControlSettings() default cloud_burst_enabled is False
    - Test: env PHAZE_CLOUD_BURST_ENABLED=true → cloud_burst_enabled is True
    - Test: bare-name env cloud_burst_enabled=true also parses (AliasChoices dual form)
  </behavior>
  <action>
    Add a `cloud_burst_enabled: bool` Field to `ControlSettings` (NOT BaseSettings) placed alongside the existing cloud knob block near `cloud_route_threshold_sec` (config.py:376). Use `default=False`, `validation_alias=AliasChoices("PHAZE_CLOUD_BURST_ENABLED", "cloud_burst_enabled")`, and a `description=` that states it is the master switch and that False reverts to all-local analysis (Phase 51, CLOUDDEPLOY-04). Plain bool — NO `gt=`/`lt=` bounds (unlike the int knobs). Do NOT touch `SECRET_FILE_FIELDS` (not secret-bearing). Mirror the `Phase NN D-NN:` provenance-comment style of the surrounding cloud knobs. Create `tests/test_config/test_cloud_burst_toggle.py` asserting the default-False, PHAZE_CLOUD_BURST_ENABLED alias, and bare-name parsing using the monkeypatch.setenv pattern from test_cloud_route_threshold.py.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config/test_cloud_burst_toggle.py -q</automated>
  </verify>
  <acceptance_criteria>
    - config.py contains "cloud_burst_enabled" with `default=False` inside ControlSettings
    - config.py contains `AliasChoices("PHAZE_CLOUD_BURST_ENABLED", "cloud_burst_enabled")`
    - `uv run pytest tests/test_config/test_cloud_burst_toggle.py -q` exits 0
    - `uv run mypy src/phaze/config.py` exits 0
  </acceptance_criteria>
  <done>The toggle field exists on ControlSettings, defaults False, parses from both env forms, and is type-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate the routing seam and the staging cron</name>
  <files>src/phaze/routers/pipeline.py, src/phaze/tasks/release_awaiting_cloud.py, tests/test_routing_seam.py, tests/test_staging_cron.py</files>
  <read_first>
    - src/phaze/routers/pipeline.py lines 254-320 (_route_discovered_by_duration) and 344-372, 574-602 (the two callers)
    - src/phaze/tasks/release_awaiting_cloud.py lines 109-135 (stage_cloud_window top + GATE 1 no-op contract)
    - tests/test_routing_seam.py (existing call sites at :71 and :100 — both must be updated to the new signature)
    - tests/test_staging_cron.py lines 42-104 (the _FakeSettings stand-in + get_settings monkeypatch + no-op assertion pattern)
    - 51-PATTERNS.md sections "src/phaze/routers/pipeline.py" (Site 1) and "src/phaze/tasks/release_awaiting_cloud.py"
  </read_first>
  <behavior>
    - Test (routing): cloud_enabled=False + long file → file.state stays DISCOVERED-routed-local, NOT AWAITING_CLOUD
    - Test (routing): cloud_enabled=True + long file → file.state == AWAITING_CLOUD (existing behavior, regression)
    - Test (cron): cloud_burst_enabled False on the stand-in settings → stage_cloud_window returns {"staged":0,"skipped":0} and stages nothing
    - Test (cron): cloud_burst_enabled True → existing staging behavior unchanged (regression)
  </behavior>
  <action>
    Site 1 — `_route_discovered_by_duration`: thread a new `cloud_enabled: bool` parameter (place it adjacent to `threshold_sec` for parity with how `threshold_sec` is already passed) and change line 308 to `is_long = cloud_enabled and duration is not None and duration >= threshold_sec`. Update all three call sites to pass `settings.cloud_burst_enabled`: `trigger_analysis` (~:364), `trigger_analysis_ui` (~:596), and `trigger_backfill_cloud` (~:676). When OFF, no row reaches `FileState.AWAITING_CLOUD`; short and long files alike append to `local_files`. Site 2 — `stage_cloud_window` (release_awaiting_cloud.py:109): add a top-of-function gate BEFORE the `max_in_flight = get_settings().cloud_max_in_flight` read at :121 — read `cfg = get_settings()`, then `if not cfg.cloud_burst_enabled:  # type: ignore[attr-defined]` return `{"staged": 0, "skipped": 0}` (NEVER raise — match the GATE 1 no-op contract, T-50-cron-raise discipline), then read `max_in_flight = cfg.cloud_max_in_flight  # type: ignore[attr-defined]`. This single cron gate satisfies D-03 clause (c) — the Phase 49 `release_awaiting_cloud` cron no longer exists; Phase 50 replaced it with `stage_cloud_window` in this same file. Update existing tests in test_routing_seam.py (new positional arg) and add the OFF/ON cases; extend test_staging_cron.py's `_FakeSettings` to carry `cloud_burst_enabled` and add the disabled-no-op case plus an enabled regression.
  </action>
  <verify>
    <automated>uv run pytest tests/test_routing_seam.py tests/test_staging_cron.py -q</automated>
  </verify>
  <acceptance_criteria>
    - pipeline.py line for is_long reads `cloud_enabled and duration is not None and duration >= threshold_sec`
    - All three `_route_discovered_by_duration(...)` call sites pass `settings.cloud_burst_enabled`
    - release_awaiting_cloud.py stage_cloud_window returns `{"staged": 0, "skipped": 0}` on the disabled branch (grep confirms the early `if not cfg.cloud_burst_enabled` before line ~121)
    - `uv run pytest tests/test_routing_seam.py tests/test_staging_cron.py -q` exits 0
    - `uv run mypy src/phaze/routers/pipeline.py src/phaze/tasks/release_awaiting_cloud.py` exits 0
  </acceptance_criteria>
  <done>OFF routes every long file local and no-ops the staging cron; ON preserves Phase 49/50 behavior; tests prove both.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gate the backfill trigger with an explicit early-return</name>
  <files>src/phaze/routers/pipeline.py, tests/test_routers/test_pipeline.py</files>
  <read_first>
    - src/phaze/routers/pipeline.py lines 640-710 (trigger_backfill_cloud — the count==0 early-return template at :662-667 is the structural template)
    - tests/test_routers/test_pipeline.py (existing backfill test(s) — search for "backfill")
    - 51-RESEARCH.md "Pitfall 2" and "Master Toggle Wiring" site #3 (why the routing-seam gate alone is insufficient)
  </read_first>
  <behavior>
    - Test: cloud_burst_enabled=False → POST backfill returns the backfill_response partial with count 0 and mutates ZERO file.state rows (no ANALYSIS_FAILED → DISCOVERED reset)
    - Test: cloud_burst_enabled=True → existing backfill behavior unchanged (regression — candidates reset + routed)
  </behavior>
  <action>
    In `trigger_backfill_cloud`, add an explicit early-return guard ABOVE the `count = await count_backfill_candidates(...)` line (~:660), mirroring the existing `count == 0` early-return shape at :662-667: `if not settings.cloud_burst_enabled:` return `templates.TemplateResponse(request=request, name="pipeline/partials/backfill_response.html", context={"request": request, "count": 0, "disabled": True})`. Reuse the SAME `backfill_response.html` partial — add the optional `disabled` context flag only; do NOT create a new template and do NOT mutate any `file.state` on this path. This prevents the OFF feature from silently resetting the 144 ANALYSIS_FAILED long files to DISCOVERED and re-routing them local to re-time-out (Pitfall 2). Add the disabled-path test and the enabled regression to test_routers/test_pipeline.py.
  </action>
  <verify>
    <automated>uv run pytest tests/test_routers/test_pipeline.py -k backfill -q</automated>
  </verify>
  <acceptance_criteria>
    - pipeline.py trigger_backfill_cloud contains `if not settings.cloud_burst_enabled:` BEFORE the count_backfill_candidates call
    - The disabled branch returns context including `"disabled": True` and performs no `file.state =` assignment
    - `uv run pytest tests/test_routers/test_pipeline.py -k backfill -q` exits 0
  </acceptance_criteria>
  <done>Backfill is a no-op when cloud is OFF (zero state mutations); enabled path unchanged; tests prove both.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config → control-plane routing | `cloud_burst_enabled` controls whether files leave the local trust boundary for a cloud target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Tampering / Elevation | master toggle OFF state | mitigate | Gate ALL THREE cloud entry points (routing seam, staging cron, backfill); unit tests assert OFF produces zero AWAITING_CLOUD rows, zero staged push jobs, zero backfill state mutations — proving OFF cannot leak any file to a cloud target. |
| T-51-02 | Repudiation / silent scope drift | backfill path | mitigate | Explicit early-return (not routing-seam-only) so a disabled feature never silently resets 144 ANALYSIS_FAILED files to DISCOVERED (Pitfall 2). |
| T-51-03 | Insecure default | new toggle | accept | Default `False` (D-01): the feature ships dormant; opt-in only after provisioning. No residual risk — safe state is the default. |
| T-51-SC | Tampering | package installs | accept | No package installs in this plan (config field + gates + tests only). RESEARCH Package Legitimacy Audit: N/A. |
</threat_model>

<verification>
- `uv run pytest tests/test_config/test_cloud_burst_toggle.py tests/test_routing_seam.py tests/test_staging_cron.py tests/test_routers/test_pipeline.py -q` green
- `uv run pytest --cov --cov-report=term-missing` ≥ 85%
- `pre-commit run --all-files` clean (ruff, mypy, etc.)
</verification>

<success_criteria>
- CLOUDDEPLOY-04: a single config toggle (`cloud_burst_enabled`) disables the entire cloud-burst feature, reverting to all-local with no other change.
- Master-toggle slice of CLOUDDEPLOY-02: `cloud_burst_enabled` configurable via pydantic-settings (`PHAZE_CLOUD_BURST_ENABLED`).
- OFF and ON behaviors at all three sites proven by automated unit tests.
</success_criteria>

<output>
Create `.planning/phases/51-deployment-config-docs/51-01-SUMMARY.md` when done.
</output>
