---
phase: 51-deployment-config-docs
plan: 03
type: execute
wave: 2
depends_on: ["51-01", "51-02", "51-04"]
files_modified:
  - docs/cloud-burst.md
  - docs/configuration.md
  - docs/deployment.md
  - docs/README.md
autonomous: true
requirements: [CLOUDDEPLOY-02, CLOUDDEPLOY-03, CLOUDDEPLOY-04]
user_setup: []

must_haves:
  truths:
    - "docs/configuration.md documents EVERY cloud-burst knob (knob / env var / default / _FILE?) including cloud_burst_enabled, threshold, max in-flight, scratch dirs, push SSH target/timeouts, the _FILE secrets, WORKER_MAX_JOBS (agent concurrency), and PHAZE_AGENT_QUEUE (cloud queue name)"
    - "docs/configuration.md documents the master-toggle semantics (OFF = all-local) and that flipping it requires a control-plane restart"
    - "docs/cloud-burst.md is the single feature home: compose/deploy walkthrough + runbook (ACL JSON + PG role SQL + OpenTofu reference copies) + smoke test"
    - "docs/deployment.md points to cloud-burst.md and lists docker-compose.cloud-agent.yml as a deployment target"
    - "docs/README.md indexes cloud-burst.md under Operations"
    - "Implements 51-CONTEXT.md decisions D-12 (full cloud-burst knob table in configuration.md) and D-13 (one new cloud-burst.md feature home + deployment.md pointer + README.md index); carries the D-09/D-10/D-11 ACL JSON + PG role SQL + OpenTofu reference copies into cloud-burst.md, and the D-04 master-toggle OFF=all-local docs note"
  artifacts:
    - path: "docs/cloud-burst.md"
      provides: "cloud-burst feature home (deploy walkthrough + runbook + smoke test)"
      contains: "phaze_broker"
    - path: "docs/configuration.md"
      provides: "cloud-burst config knob table + _FILE secret rows"
      contains: "cloud_burst_enabled"
  key_links:
    - from: "docs/deployment.md"
      to: "docs/cloud-burst.md"
      via: "pointer section + Deployment Targets row"
      pattern: "cloud-burst\\.md"
    - from: "docs/README.md"
      to: "docs/cloud-burst.md"
      via: "Operations index row"
      pattern: "cloud-burst\\.md"
---

<objective>
Document the cloud-burst feature surface (CLOUDDEPLOY-02 docs, plus the CLOUDDEPLOY-03 reference copies and the CLOUDDEPLOY-04 toggle semantics): the full config-knob table in `docs/configuration.md`, a new `docs/cloud-burst.md` feature home (compose/deploy walkthrough + runbook + smoke test), and pointers from `docs/deployment.md` and the `docs/README.md` index.

Purpose: every cloud-burst parameter and the deploy runbook must be discoverable in one place; satisfies the docs-up-to-date and README-per-surface project rules.
Output: one new doc + three modified docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-deployment-config-docs/51-CONTEXT.md
@.planning/phases/51-deployment-config-docs/51-RESEARCH.md
@.planning/phases/51-deployment-config-docs/51-PATTERNS.md
@.planning/phases/51-deployment-config-docs/51-HOMELAB-CHANGE-PROMPT.md
@docker-compose.cloud-agent.yml

<interfaces>
<!-- Source-of-truth for the doc content -->
51-RESEARCH.md "Config Knobs Audit" — the full 14-row knob table + the two criterion-named knobs (WORKER_MAX_JOBS = agent concurrency; PHAZE_AGENT_QUEUE = cloud queue name, a raw import-time env read, document don't refactor)
51-RESEARCH.md "Tailscale Grants ACL" / "Least-Privilege Postgres Role" / "OCI Always-Free A1 OpenTofu Spec" — the copies cloud-burst.md embeds
src/phaze/config.py — Field(...) descriptions are the source for each knob Description
docs/configuration.md:19-48 (_FILE conventions + dual-form) and :66-76 (worker table style)
docs/deployment.md:13-19 (Deployment Targets table) and :81-270 (numbered Step N walkthrough style)
docs/README.md:24-28 (Operations index table)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the cloud-burst config table to docs/configuration.md</name>
  <files>docs/configuration.md</files>
  <read_first>
    - docs/configuration.md (full file — line-1 marker, _FILE table at :21-48, worker/task-queue table style at :66-76, dual-form convention at :19)
    - 51-RESEARCH.md "Config Knobs Audit" (the full enumeration + the two criterion-named knobs + the _FILE-bearing rows)
    - src/phaze/config.py (the Field(...) description text for each cloud knob — source the Description column from these, including the new cloud_burst_enabled from Plan 01)
  </read_first>
  <action>
    Add a new `## Cloud-burst settings` section using the existing table style. Per D-12, columns: Knob / Env var / Default / `_FILE`-secret? / Description. Include every row from the RESEARCH Config Knobs Audit: `cloud_burst_enabled` (PHAZE_CLOUD_BURST_ENABLED, False, no), `cloud_route_threshold_sec` (5400), `cloud_max_in_flight` (2), `push_max_attempts` (3), `compute_scratch_dir`, `cloud_scratch_dir` (note: MUST match compute_scratch_dir), `push_ssh_host`, `push_ssh_user`, `push_timeout_sec` (600), `push_connect_timeout_sec` (30), `push_ssh_key` (_FILE, whitespace-preserved), `push_known_hosts` (_FILE, whitespace-preserved), `agent_token` (_FILE), plus the two criterion-named knobs: `WORKER_MAX_JOBS` (agent concurrency; document recommended =1 on the 12 GB A1) and `PHAZE_AGENT_QUEUE` (the cloud queue name; document that it is an operator env var read at SAQ import time, NOT a pydantic field — structural, per RESEARCH A2). Also extend the existing `_FILE` table at :21-48 with any cloud `_FILE`-bearing rows not already present (push_ssh_key, push_known_hosts, queue_url, redis_url, agent_token). Add a short note documenting the master-toggle semantics: OFF = all-local analysis with no other change; flipping it requires a control-plane restart (startup-read). Add an EXPLICIT exception note (a sentence or callout, not just a table cell) stating that `PHAZE_AGENT_QUEUE` is the SINGLE structural exception to CLOUDDEPLOY-02's "all parameters via pydantic-settings" coverage: it is read as a raw env var at SAQ module import time (agent_worker.py), BEFORE pydantic settings (get_settings()) are constructed, so it cannot be a pydantic-settings field — it remains a required operator env var. Use the dual-form convention from :19 (e.g. `PHAZE_CLOUD_BURST_ENABLED (or cloud_burst_enabled)`). Source each Description from the config.py Field text. Keep the line-1 `generated-by: gsd-doc-writer` marker.
  </action>
  <verify>
    <automated>grep -q "cloud_burst_enabled" docs/configuration.md && grep -q "WORKER_MAX_JOBS" docs/configuration.md && grep -q "PHAZE_AGENT_QUEUE" docs/configuration.md && grep -q "cloud_route_threshold_sec" docs/configuration.md && grep -q "push_known_hosts" docs/configuration.md && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - docs/configuration.md has a `## Cloud-burst settings` section with a table row for each of the 13 knobs plus WORKER_MAX_JOBS and PHAZE_AGENT_QUEUE
    - The _FILE-bearing knobs (push_ssh_key, push_known_hosts, agent_token, queue_url, redis_url) are marked as _FILE-capable
    - The OFF=all-local + restart-required semantics are documented
    - docs/configuration.md contains "PHAZE_AGENT_QUEUE" AND an exception note containing "import time" (or equivalent) that names it as the one knob NOT configurable via pydantic-settings, with the SAQ-import-before-settings rationale
    - `pre-commit run --files docs/configuration.md` exits 0
  </acceptance_criteria>
  <done>Every cloud-burst parameter is documented with env var, default, _FILE flag, and description sourced from config.py.</done>
</task>

<task type="auto">
  <name>Task 2: Author docs/cloud-burst.md (feature home: walkthrough + runbook + smoke test)</name>
  <files>docs/cloud-burst.md</files>
  <read_first>
    - docs/deployment.md lines 81-270 (numbered `## Step N — ...` walkthrough style) and docs/arm64-agent-image.md:189-194 (the -arm64 pin rationale)
    - 51-RESEARCH.md sections "Tailscale Grants ACL", "Least-Privilege Postgres Role", "OCI Always-Free A1 OpenTofu Spec", "Deploy ordering", "Runtime State Inventory", "Common Pitfalls"
    - .planning/phases/51-deployment-config-docs/51-HOMELAB-CHANGE-PROMPT.md (the homelab deliverable cloud-burst.md references — keep the copies consistent)
    - docker-compose.cloud-agent.yml (the compose file the walkthrough deploys; reference its exact filename)
  </read_first>
  <action>
    Create `docs/cloud-burst.md` starting with the line-1 `generated-by: gsd-doc-writer` marker and a title. Structure it as the single feature home using numbered `## Step N — ...` operator headings mirroring deployment.md. Cover: (a) overview of cloud burst (long-file analysis offloaded to a free OCI A1 arm64 compute agent over Tailscale via rsync push — NO object storage); (b) the compose/deploy walkthrough for `docker-compose.cloud-agent.yml` (worker-only, -arm64, named scratch, host tailscaled, the required .env: PHAZE_QUEUE_URL broker DSN, PHAZE_REDIS_URL passworded, PHAZE_AGENT_API_URL https, PHAZE_AGENT_KIND=compute, PHAZE_AGENT_QUEUE, PHAZE_AGENT_TOKEN_FILE, PHAZE_CLOUD_SCRATCH_DIR matching control's PHAZE_COMPUTE_SCRATCH_DIR, WORKER_MAX_JOBS=1, MODELS_PATH, CA file — call out the two production guards: https + passworded redis); (c) the runbook referencing the homelab OpenTofu deliverable (51-HOMELAB-CHANGE-PROMPT.md) and embedding reference COPIES of the Tailscale grants ACL JSON and the least-privilege phaze_broker PG role SQL (the empirically-verified CREATE ON SCHEMA finding is load-bearing — keep the explanatory comment); (d) the 7-step deploy ordering; (e) a smoke-test step (doc checklist: confirm the compute worker registers, drains a cloud process_file, scratch is cleaned); (f) the master-toggle semantics + runtime-state notes: OFF ships the feature dormant, flip PHAZE_CLOUD_BURST_ENABLED=true after provisioning, pre-existing AWAITING_CLOUD rows release once enabled, flipping requires a control-plane restart (Pitfall 6), nox's PHAZE_PUSH_KNOWN_HOSTS must include the A1 host key. Link to docs/configuration.md for the canonical knob table (do NOT duplicate the full table here). Placeholders only — never real secrets/IPs.
  </action>
  <verify>
    <automated>test -f docs/cloud-burst.md && grep -q "docker-compose.cloud-agent.yml" docs/cloud-burst.md && grep -q "phaze_broker" docs/cloud-burst.md && grep -q "tag:cloud-agent" docs/cloud-burst.md && grep -q "PHAZE_CLOUD_BURST_ENABLED" docs/cloud-burst.md && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - docs/cloud-burst.md line 1 is the gsd-doc-writer generated-by marker
    - It contains the compose walkthrough (references docker-compose.cloud-agent.yml + the -arm64 image + WORKER_MAX_JOBS=1), the Tailscale ACL JSON copy, the phaze_broker PG role SQL copy (with CREATE ON SCHEMA public), the 7-step deploy ordering, and a smoke-test checklist
    - It documents OFF=dormant + restart-required + AWAITING_CLOUD release-on-enable
    - No real secrets/IPs (placeholders only)
    - `pre-commit run --files docs/cloud-burst.md` exits 0
  </acceptance_criteria>
  <done>cloud-burst.md is the complete single feature home: compose deploy, runbook, smoke test, toggle semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Pointer from docs/deployment.md + index entry in docs/README.md</name>
  <files>docs/deployment.md, docs/README.md</files>
  <read_first>
    - docs/deployment.md lines 13-19 (Deployment Targets table) and the "See also" section near :461
    - docs/README.md lines 24-28 (the Operations index table + emoji-prefixed-purpose convention)
    - 51-PATTERNS.md sections "docs/deployment.md" and "docs/README.md" (exact rows to add)
  </read_first>
  <action>
    In docs/deployment.md: add a row to the Deployment Targets table (:13-19) for `docker-compose.cloud-agent.yml` — columns matching the existing `| File | Host | Services | Notes |` shape: `| docker-compose.cloud-agent.yml | OCI A1 (cloud) | worker (agent role, kind=compute) | arm64 image, no media, named scratch. See cloud-burst.md. |`. Add a short `## Cloud-burst compute agent` pointer section (or a See-also bullet) linking to docs/cloud-burst.md — do NOT inline the vendor runbook (D-13 keeps deployment.md from bloating). In docs/README.md: add a row to the Operations table (:24-28) mirroring the Deployment Guide row exactly, with an emoji-prefixed purpose: `| **[Cloud Burst](cloud-burst.md)** | ☁️ OCI A1 compute-agent deploy, Tailscale ACL, broker role, master toggle |`. Keep both files' line-1 generated-by markers.
  </action>
  <verify>
    <automated>grep -q "docker-compose.cloud-agent.yml" docs/deployment.md && grep -q "cloud-burst.md" docs/deployment.md && grep -q "cloud-burst.md" docs/README.md && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - docs/deployment.md Deployment Targets table includes the docker-compose.cloud-agent.yml row and a pointer to cloud-burst.md
    - docs/README.md Operations table includes the Cloud Burst index row linking cloud-burst.md
    - `pre-commit run --files docs/deployment.md docs/README.md` exits 0
  </acceptance_criteria>
  <done>Both index/pointer docs link to cloud-burst.md; the vendor runbook stays out of deployment.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → operator action | Docs instruct secret handling; must not leak real secrets or encourage insecure config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-11 | Information disclosure | secrets in docs | mitigate | All ACL/SQL/.env examples use placeholders only (no real passwords/IPs/OCIDs); the config table flags which fields are `_FILE`-secret-bearing so operators mount them via files, never plaintext. |
| T-51-12 | Tampering / misconfiguration | runbook completeness | mitigate | Docs carry the load-bearing CREATE ON SCHEMA finding + the production guards (https + passworded redis) + scratch-dir-match warning so operators do not deploy an insecure or broken stack. |
| T-51-SC | Tampering | package installs | accept | No package installs (docs-only). RESEARCH Package Legitimacy Audit: N/A. |
</threat_model>

<verification>
- All `grep` acceptance checks pass
- `pre-commit run --all-files` clean (markdown/yaml/whitespace hooks)
- All four docs retain their line-1 `generated-by` markers
</verification>

<success_criteria>
- CLOUDDEPLOY-02 (docs): every cloud-burst parameter is documented with env var, default, and _FILE support in docs/configuration.md.
- CLOUDDEPLOY-03 (docs): docs/cloud-burst.md carries the runbook with ACL JSON + PG role SQL copies and references the homelab OpenTofu deliverable.
- CLOUDDEPLOY-04 (docs): the master-toggle OFF=all-local + restart-required semantics are documented.
</success_criteria>

<output>
Create `.planning/phases/51-deployment-config-docs/51-03-SUMMARY.md` when done.
</output>
