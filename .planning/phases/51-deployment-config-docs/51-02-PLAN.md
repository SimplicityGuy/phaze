---
phase: 51-deployment-config-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.cloud-agent.yml
  - tests/test_deployment/test_cloud_agent_compose.py
  - justfile
autonomous: true
requirements: [CLOUDDEPLOY-01]
user_setup: []

must_haves:
  truths:
    - "docker-compose.cloud-agent.yml brings up exactly one service (worker), worker-only, no watcher/audfprint/panako"
    - "The compose file has NO media mount (no SCAN_PATH / /data/music) and NO DATABASE_URL / POSTGRES_* env (DIST-04)"
    - "The worker image ends with -arm64 and preserves the ${PHAZE_IMAGE_TAG:-latest} convention"
    - "Scratch is a named docker volume (not a host bind); MODELS mount is rw, CA mount is ro"
    - "worker carries PHAZE_ROLE=agent and PHAZE_AGENT_KIND=compute"
  artifacts:
    - path: "docker-compose.cloud-agent.yml"
      provides: "OCI A1 compute-agent compose stack"
      contains: "PHAZE_AGENT_KIND=compute"
    - path: "tests/test_deployment/test_cloud_agent_compose.py"
      provides: "YAML-parse invariant assertions for the cloud compose"
      contains: "-arm64"
  key_links:
    - from: "docker-compose.cloud-agent.yml"
      to: "ghcr.io/simplicityguy/phaze (-arm64 tag, Phase 47)"
      via: "image: line with ${PHAZE_IMAGE_TAG:-latest}-arm64"
      pattern: "ghcr\\.io/simplicityguy/phaze:.*-arm64"
    - from: "docker-compose.cloud-agent.yml"
      to: "host tailscaled (D-05)"
      via: "network_mode: host"
      pattern: "network_mode:\\s*host"
---

<objective>
Author the net-new `docker-compose.cloud-agent.yml` — a worker-only, Tailscale-connected, no-media, arm64 compute-agent stack for the OCI A1 (CLOUDDEPLOY-01) — plus a parallel YAML-parse invariant test mirroring `tests/test_deployment/test_agent_compose.py`.

Purpose: give the operator a single compose file that brings up the compute agent with host-Tailscale connectivity, a named scratch volume, no media bind, and the mandatory `-arm64` image (D-05..D-08).
Output: the compose file, its invariant test, and a justfile convenience recipe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-deployment-config-docs/51-CONTEXT.md
@.planning/phases/51-deployment-config-docs/51-RESEARCH.md
@.planning/phases/51-deployment-config-docs/51-PATTERNS.md

<interfaces>
<!-- Analog files the executor must mirror -->
docker-compose.agent.yml — fileserver-agent compose (worker+watcher+audfprint+panako, media-bound). Strip to worker-only.
tests/test_deployment/test_agent_compose.py — pure yaml.safe_load, no docker daemon; helpers _load_agent_compose, _env_to_strs; asserts raw ${VAR} tokens.
docs/arm64-agent-image.md:189-194 — the -arm64 suffix tag scheme (NOT multi-arch); production pins <version>-arm64.
config.py:470 — kind Literal["fileserver","compute"]; kind=compute relaxes the empty-scan-roots gate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docker-compose.cloud-agent.yml</name>
  <files>docker-compose.cloud-agent.yml, justfile</files>
  <read_first>
    - docker-compose.agent.yml (full file — banner/invariant comment block at :1-29, worker service at :32-42, named-volume block at :78-80)
    - 51-PATTERNS.md "docker-compose.cloud-agent.yml" section (worker service to copy + target shape + invariants)
    - 51-RESEARCH.md "Compose Pattern" (the recommended file body + the .env enumeration + the two production guards)
    - justfile (find any existing agent-compose up/down/test recipe to mirror)
  </read_first>
  <action>
    Create `docker-compose.cloud-agent.yml` with a single `worker` service mirroring docker-compose.agent.yml's worker but: image `ghcr.io/simplicityguy/phaze:${PHAZE_IMAGE_TAG:-latest}-arm64` (the `-arm64` suffix is MANDATORY, D-08); `command: uv run saq phaze.tasks.agent_worker.settings`; `network_mode: host` (D-05, host tailscaled + MagicDNS); `env_file: .env`; `environment:` `PHAZE_ROLE=agent` and `PHAZE_AGENT_KIND=compute` (relaxes the empty-scan-roots gate, config.py:470); volumes = a NAMED volume `cloud_scratch:${PHAZE_CLOUD_SCRATCH_DIR:?PHAZE_CLOUD_SCRATCH_DIR required}:rw` (D-07), `${MODELS_PATH:-./models}:/models:rw`, `${CA_PATH:-./certs}:/certs:ro`; `restart: unless-stopped`; and a top-level `volumes:` block declaring `cloud_scratch:`. REMOVE entirely: any watcher/audfprint/panako service, any `SCAN_PATH`/`/data/music` media bind, any `DATABASE_URL`/`POSTGRES_*` env, any postgres/redis service or depends_on (DIST-04 — the agent reaches Postgres only via PHAZE_QUEUE_URL for saq_jobs + the HTTP API). Copy the header banner + "Invariants (asserted by tests/...)" comment block style from agent.yml:1-29, adapted for the cloud stack. Add a justfile recipe (e.g. `cloud-agent-up` / `cloud-agent-down`) mirroring any existing agent-compose recipe convention; keep it minimal and grouped with the other deployment recipes.
  </action>
  <verify>
    <automated>uv run python -c "import yaml; d=yaml.safe_load(open('docker-compose.cloud-agent.yml')); assert set(d['services'])=={'worker'}; img=d['services']['worker']['image']; assert img.endswith('-arm64') and 'PHAZE_IMAGE_TAG' in img; assert 'cloud_scratch' in d.get('volumes',{}); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.cloud-agent.yml` services key is exactly `{worker}`
    - The worker image string matches `ghcr.io/simplicityguy/phaze:${PHAZE_IMAGE_TAG:-latest}-arm64`
    - The file has NO `/data/music`, NO `SCAN_PATH`, NO `DATABASE_URL`, NO `POSTGRES_`, NO `postgres`/`redis` service
    - `network_mode: host` present; `cloud_scratch` declared under top-level `volumes:`; MODELS mount `:rw`, CA mount `:ro`
    - justfile gains a cloud-agent up/down recipe; `pre-commit run yamllint --files docker-compose.cloud-agent.yml` exits 0
  </acceptance_criteria>
  <done>The compose file is a clean worker-only, no-media, arm64, host-Tailscale, named-scratch stack; yamllint clean; justfile updated.</done>
</task>

<task type="auto">
  <name>Task 2: Author the cloud-agent compose invariant test</name>
  <files>tests/test_deployment/test_cloud_agent_compose.py</files>
  <read_first>
    - tests/test_deployment/test_agent_compose.py (full file — module scaffold :32-58, service-list :61-66, no-postgres :69-84, role :87-91, scan-path failfast :94-110, ghcr image :113-137)
    - docker-compose.cloud-agent.yml (the file produced in Task 1)
    - 51-PATTERNS.md "tests/test_deployment/test_cloud_agent_compose.py" section (the exact assertions to add, including the net-new -arm64 + named-scratch + no-media checks)
  </read_first>
  <action>
    Create `tests/test_deployment/test_cloud_agent_compose.py` mirroring test_agent_compose.py: pure `yaml.safe_load` against `Path(__file__).resolve().parents[2] / "docker-compose.cloud-agent.yml"`, no docker daemon, asserting raw `${VAR}` tokens (no interpolation). Reuse the `_env_to_strs` list-or-dict helper. Assertions: (1) `set(services) == {"worker"}`; (2) copy the no-Postgres/DIST-04 assertion verbatim (no DATABASE_URL, no POSTGRES_*, no depends_on postgres); (3) worker env contains both `PHAZE_ROLE=agent` AND `PHAZE_AGENT_KIND=compute`; (4) NEW image assertion — worker image startswith `ghcr.io/simplicityguy/phaze:`, contains `PHAZE_IMAGE_TAG`, and ENDS with `-arm64` (D-08, Pitfall 3); (5) NEW named-scratch assertion — the scratch volume's left side is a named-volume ref `cloud_scratch` (no leading `/` or `.`) and `cloud_scratch` is declared under top-level `volumes:`; (6) NEW no-media assertion — NO volume string contains `SCAN_PATH` or `/data/music`; (7) MODELS mount `:rw`, CA mount `:ro`; (8) `network_mode: host` present on worker.
  </action>
  <verify>
    <automated>uv run pytest tests/test_deployment/test_cloud_agent_compose.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The test file asserts the single-service, no-DB, role+kind, `-arm64`, named-scratch, no-media, mount-mode, and network_mode invariants
    - `uv run pytest tests/test_deployment/test_cloud_agent_compose.py -q` exits 0
    - `uv run pytest tests/test_deployment/ -q` exits 0 (no regression to the agent compose test)
  </acceptance_criteria>
  <done>Every CLOUDDEPLOY-01 invariant is asserted by an automated YAML-parse test that needs no docker daemon.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compute agent → lux Postgres | The compute agent must reach ONLY saq_jobs via PHAZE_QUEUE_URL, never the app ORM (DIST-04) |
| OCI A1 host → tailnet | host tailscaled is the network trust boundary; the container uses host networking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-04 | Information disclosure | cloud compose env | mitigate | Test asserts NO DATABASE_URL / POSTGRES_* / postgres service (DIST-04) — the agent cannot reach app data through the compose surface. |
| T-51-05 | Tampering (wrong arch image) | worker image tag | mitigate | Test asserts the image ends `-arm64` (no multi-arch manifest exists; an x86 image would not run on the A1 — Pitfall 3). |
| T-51-06 | Information disclosure (secrets in compose) | _FILE secrets | mitigate | Compose mounts `*_FILE` paths via the existing `_FILE` machinery; no plaintext secrets inlined in the compose file. |
| T-51-SC | Tampering | package installs | accept | No package installs (new compose YAML + test only). The compose references an already-published GHCR image. RESEARCH Package Legitimacy Audit: N/A. |
</threat_model>

<verification>
- `uv run pytest tests/test_deployment/ -q` green
- `pre-commit run --all-files` clean (yamllint, check-jsonschema, etc.)
</verification>

<success_criteria>
- CLOUDDEPLOY-01: a cloud-agent compose file brings up the compute agent with Tailscale connectivity (host networking), no media mount, a named scratch volume, and the arm64 image — all proven by automated invariant tests.
</success_criteria>

<output>
Create `.planning/phases/51-deployment-config-docs/51-02-SUMMARY.md` when done.
</output>
