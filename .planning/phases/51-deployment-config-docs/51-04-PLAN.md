---
phase: 51-deployment-config-docs
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/51-deployment-config-docs/51-HOMELAB-CHANGE-PROMPT.md
autonomous: true
requirements: [CLOUDDEPLOY-03]
user_setup: []

must_haves:
  truths:
    - "The homelab change prompt specifies the OpenTofu OCI Always-Free A1 module (2 OCPU / 12 GB, Ubuntu 24.04 arm64, boot volume, SSH key, VCN/subnet/IGW/route-table/security-list)"
    - "It carries the EXACT Tailscale grants ACL JSON scoping A1 -> lux:{5432,6379,8000} + nox -> A1:22, default-deny"
    - "It carries the EXACT least-privilege phaze_broker PG role SQL including GRANT USAGE, CREATE ON SCHEMA public (empirically required by SAQ init_db) and ZERO app-table grants"
    - "It lists the 7-step deploy ordering and uses placeholders only (no real secrets)"
  artifacts:
    - path: ".planning/phases/51-deployment-config-docs/51-HOMELAB-CHANGE-PROMPT.md"
      provides: "ready-to-paste homelab spec for OCI A1 + Tailscale ACL + broker role"
      contains: "phaze_broker"
  key_links:
    - from: "51-HOMELAB-CHANGE-PROMPT.md"
      to: "homelab repo agent (OpenTofu + Tailscale + lux Postgres)"
      via: "paste-the-section-below blockquote + numbered change sections"
      pattern: "CREATE ON SCHEMA public"
---

<objective>
Author the cross-repo deliverable that lets the homelab repo agent provision the live cloud-burst infrastructure (CLOUDDEPLOY-03): a ready-to-paste change prompt carrying the OpenTofu OCI A1 module spec, the exact Tailscale grants ACL JSON, and the empirically-verified least-privilege Postgres broker role SQL. phaze is the source-of-truth spec; all live infra lives in homelab (workspace boundary, D-09/D-10).

Purpose: the OCI A1 + Tailscale ACL + broker role must be authored as IaC/SQL in the homelab repo, not in phaze. This plan emits the spec the homelab agent applies.
Output: `.planning/phases/51-deployment-config-docs/51-HOMELAB-CHANGE-PROMPT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-deployment-config-docs/51-CONTEXT.md
@.planning/phases/51-deployment-config-docs/51-RESEARCH.md
@.planning/phases/51-deployment-config-docs/51-PATTERNS.md

<interfaces>
<!-- The authoritative spec content lives in 51-RESEARCH.md — copy it, do not re-derive -->
51-RESEARCH.md "OCI Always-Free A1 OpenTofu Spec" — hcl block, 2 OCPU/12 GB, capacity gotcha
51-RESEARCH.md "Tailscale Grants ACL" — jsonc grants block (default-deny, tagOwners, hosts, grants)
51-RESEARCH.md "Least-Privilege Postgres Role" — empirically-verified sql block (CREATE ON SCHEMA required)
51-RESEARCH.md "Deploy ordering" — the 7-step sequence (datum@nox / datum@lux)
Analog: .planning/milestones/v4.0-phases/36-pipeline-queue-backend-migration-redis-to-postgres-saq/36-HOMELAB-CHANGE-PROMPT.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OpenTofu OCI A1 module spec + Tailscale grants ACL sections</name>
  <files>.planning/phases/51-deployment-config-docs/51-HOMELAB-CHANGE-PROMPT.md</files>
  <read_first>
    - .planning/milestones/v4.0-phases/36-pipeline-queue-backend-migration-redis-to-postgres-saq/36-HOMELAB-CHANGE-PROMPT.md (full file — the exact structure: line-1 marker, paste-the-section blockquote, Context section, numbered change sections, deploy ordering)
    - 51-RESEARCH.md sections "OCI Always-Free A1 OpenTofu Spec" and "Tailscale Grants ACL"
    - 51-CONTEXT.md D-09, D-10 (workspace boundary; phaze specs, homelab applies)
  </read_first>
  <action>
    Create the file starting with the line-1 marker `generated-by: gsd-executor` (HTML-comment form, matching the Phase 36 analog) and `# Homelab Change Prompt — Phaze v5.0 Cloud Burst (OCI A1 + Tailscale + broker role)`. Add the paste-the-section-below blockquote intro and a `## Context for the homelab agent` summary (v5.0 Cloud Burst: a free OCI Ampere A1 runs the arm64 compute agent over Tailscale; phaze emits the spec, homelab applies). Then add numbered change sections. Section `## 1. Provision the OCI Always-Free A1 (OpenTofu)` — embed the hcl block from RESEARCH (oci_core_instance VM.Standard.A1.Flex, shape_config ocpus 2 / memory_in_gbs 12, Canonical Ubuntu 24.04 Minimal aarch64 image OCID var, assign_public_ip true, ssh_authorized_keys var), plus the required VCN/subnet/internet-gateway/route-table/security-list note and a cloud-init note installing tailscaled + rsync; include the capacity gotcha (2 OCPU/12 GB current June-2026 Always-Free limit; verify at provision time; retry across ADs/regions on Out of Capacity); security-list = outbound-mostly (Tailscale is the real ACL). Section `## 2. Apply the Tailscale grants ACL` — embed the jsonc grants block from RESEARCH verbatim (tagOwners tag:cloud-agent, hosts lux/nox, grants A1 to lux on tcp:5432/tcp:6379/tcp:8000 + nox to A1 on tcp:22, default-deny). Use placeholder tailnet IPs (100.x.x.x) — never real IPs.
  </action>
  <verify>
    <automated>grep -q "VM.Standard.A1.Flex" .planning/phases/51-deployment-config-docs/51-HOMELAB-CHANGE-PROMPT.md && grep -q "tcp:5432" .planning/phases/51-deployment-config-docs/51-HOMELAB-CHANGE-PROMPT.md && grep -q "tag:cloud-agent" .planning/phases/51-deployment-config-docs/51-HOMELAB-CHANGE-PROMPT.md && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - File line 1 is the gsd-executor generated-by HTML-comment marker
    - Section 1 contains an hcl block with `VM.Standard.A1.Flex`, `ocpus = 2`, `memory_in_gbs = 12`, and the Ubuntu 24.04 aarch64 + ssh_authorized_keys references
    - Section 2 contains a default-deny grants block with `tcp:5432`, `tcp:6379`, `tcp:8000` (A1 to lux) and `tcp:22` (nox to A1) and `tag:cloud-agent`
    - No real IPs/secrets — placeholders only (100.x.x.x, var.*)
  </acceptance_criteria>
  <done>Sections 1 and 2 carry the OCI A1 OpenTofu spec and the exact Tailscale grants ACL, placeholders only.</done>
</task>

<task type="auto">
  <name>Task 2: Least-privilege broker role SQL + deploy ordering + verification</name>
  <files>.planning/phases/51-deployment-config-docs/51-HOMELAB-CHANGE-PROMPT.md</files>
  <read_first>
    - .planning/phases/51-deployment-config-docs/51-HOMELAB-CHANGE-PROMPT.md (the file from Task 1)
    - 51-RESEARCH.md sections "Least-Privilege Postgres Role" (the empirical finding + verified SQL) and "OCI Always-Free A1 OpenTofu Spec" subsection "Deploy ordering"
    - 51-RESEARCH.md "Runtime State Inventory" + "Common Pitfalls" (Pitfall 1, 6; the off-by-default behavior note)
  </read_first>
  <action>
    Append section `## 3. Create the least-privilege phaze_broker Postgres role (lux)` — embed the sql block from RESEARCH verbatim: CREATE ROLE phaze_broker LOGIN PASSWORD placeholder; GRANT USAGE, CREATE ON SCHEMA public TO phaze_broker (with the load-bearing comment that SAQ init_db runs an unconditional CREATE TABLE IF NOT EXISTS saq_versions and PG checks schema-CREATE BEFORE the IF-NOT-EXISTS short-circuit — empirically verified, so CREATE is unavoidable even with tables pre-created); GRANT SELECT, INSERT, UPDATE, DELETE ON saq_jobs, saq_stats, saq_versions; GRANT USAGE, SELECT ON SEQUENCE saq_jobs_lock_key_seq; and the DIST-04 verification probe (SET ROLE phaze_broker; SELECT * FROM files LIMIT 1; MUST ERROR with permission denied). Add the prerequisite note that the full phaze role must boot FIRST so SAQ has already created/migrated the saq tables, plus the PG15+ schema-CREATE note. Mention the optional dedicated-saq-schema hardening as document-don't-default. Then append section `## 4. Deploy ordering` with the 7-step sequence from RESEARCH (OpenTofu apply -> tailnet ACL -> broker role SQL -> phaze v5.0.x release publishes GHCR -arm64 -> A1 .env then docker compose -f docker-compose.cloud-agent.yml up -d -> set PHAZE_CLOUD_BURST_ENABLED=true then restart control plane -> smoke test), using datum@nox / datum@lux for the SSH-target convention (Phase 36 precedent). Add a closing note: off-by-default ships the cloud feature dormant; flipping the toggle requires a control-plane restart (startup-read, Pitfall 6); nox's PHAZE_PUSH_KNOWN_HOSTS must be re-provisioned with the A1 host key after the A1 is up. Placeholders only — never real secrets.
  </action>
  <verify>
    <automated>grep -q "CREATE ON SCHEMA public" .planning/phases/51-deployment-config-docs/51-HOMELAB-CHANGE-PROMPT.md && grep -q "saq_jobs_lock_key_seq" .planning/phases/51-deployment-config-docs/51-HOMELAB-CHANGE-PROMPT.md && grep -q "PHAZE_CLOUD_BURST_ENABLED=true" .planning/phases/51-deployment-config-docs/51-HOMELAB-CHANGE-PROMPT.md && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Section 3 SQL contains `GRANT USAGE, CREATE ON SCHEMA public`, the three saq tables DML grant, `saq_jobs_lock_key_seq` USAGE, and the `SELECT * FROM files` DIST-04 probe expected to ERROR
    - Section 3 contains NO grant on any app ORM table (files/metadata/agents)
    - Section 4 lists the 7 ordered deploy steps including `PHAZE_CLOUD_BURST_ENABLED=true` + control-plane restart, via datum@nox / datum@lux
    - `pre-commit run --files .planning/phases/51-deployment-config-docs/51-HOMELAB-CHANGE-PROMPT.md` exits 0 (trailing whitespace / EOF / markdown hooks)
  </acceptance_criteria>
  <done>Sections 3 and 4 carry the empirically-verified broker role SQL and the 7-step deploy ordering; placeholders only; hooks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compute agent (A1) -> lux Postgres | Broker role must reach saq_jobs ONLY, never app ORM tables (DIST-04) |
| A1 host -> tailnet | Tailscale ACL is the network trust boundary; default-deny grants |
| nox -> A1 | SSH push channel; only nox:22 inbound to A1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-07 | Information disclosure | phaze_broker DB role | mitigate | Role gets table-DML on the three saq tables + sequence USAGE + CREATE ON SCHEMA only; ZERO grants on app ORM tables; spec includes the `SELECT * FROM files` probe that MUST ERROR (DIST-04). |
| T-51-08 | Elevation of privilege | broker role over-broad | accept (with note) | `CREATE ON SCHEMA public` is empirically unavoidable (SAQ init_db unconditional CREATE TABLE IF NOT EXISTS). Residual risk: the role can create new objects in public. Documented; optional dedicated-saq-schema hardening offered for strict lockdown. |
| T-51-09 | Spoofing / Tampering / lateral movement | Tailscale ACL scope | mitigate | Default-deny grants give the A1 exactly lux:{5432,6379,8000} outbound + nox:22 inbound and nothing else — no broader tailnet reach. |
| T-51-10 | Information disclosure | secrets in the change prompt | mitigate | Placeholders only (passwords, IPs, OCIDs as var.* / 100.x.x.x); never real secrets — matches the Phase 36 placeholder discipline. |
| T-51-SC | Tampering | package installs | accept | No package installs (spec/markdown deliverable only). OpenTofu/oci-provider/Tailscale tooling live in the homelab repo, not phaze. RESEARCH Package Legitimacy Audit: N/A. |
</threat_model>

<verification>
- `grep` acceptance checks above pass
- `pre-commit run --files .planning/phases/51-deployment-config-docs/51-HOMELAB-CHANGE-PROMPT.md` clean
</verification>

<success_criteria>
- CLOUDDEPLOY-03: a runbook/spec documents OCI Always-Free A1 provisioning + a Tailscale ACL scoping the A1 to exactly lux:{5432,6379,8000} + nox->A1:22 + a least-privilege Postgres broker role — authored as a homelab change prompt with phaze as source-of-truth.
</success_criteria>

<output>
Create `.planning/phases/51-deployment-config-docs/51-04-SUMMARY.md` when done.
</output>
