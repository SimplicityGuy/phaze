---
phase: 83-cloud-routing-sidecar-cutover
plan: 06
type: execute
wave: 3
depends_on: ["83-01", "83-02", "83-05"]
files_modified:
  - src/phaze/services/pipeline.py
  - src/phaze/services/backend_selection.py
  - tests/integration/test_drain_double_dispatch.py
  - tests/shared/routers/test_pipeline.py
autonomous: true
requirements: [SIDECAR-01]
must_haves:
  truths:
    - "D-05: get_cloud_staging_candidates excludes locally-dispatched/terminally-failed files by a PREDICATE CONJUNCT (cloud_job.status=='awaiting' AND ~inflight_clause(Stage.ANALYZE) AND ~domain_completed_clause(Stage.ANALYZE)) composing the stage_status.py:150/:170 builders VERBATIM — NOT row deletion (deletion fails the rolled-back-tick path where the committed ledger row + a restored awaiting row would double-dispatch, which SC#3 forbids); LocalBackend.dispatch stays a no-cloud_job-row writer"
    - "D-06: the drain locks with with_for_update(of=CloudJob, skip_locked=True) over an INNER join to cloud_job — the candidacy predicate lives on cloud_job, so Postgres EvalPlanQual re-checks the locked table's WHERE; locking only files would read the deciding column stale and the tick advisory lock does not cover the callback routers / reconcile cron"
    - "D-07: FIFO stays on FileRecord.created_at (immutable discovery order, byte-identical to today); the cloud_route_max_wait_sec staleness clock moves from file.updated_at to cloud_job.updated_at (surfaced from the candidate query / per-candidate read and passed into select_backend), so Phase 90's removal of the dual-written file.state does not silently break the spill clock"
    - "D-15: get_awaiting_cloud_count derives from the SAME clause the drain uses (COUNT(cloud_job) WHERE status='awaiting' AND ~inflight_clause(ANALYZE) AND ~domain_completed_clause(ANALYZE)), kept _safe_count-wrapped, so the card and the drain cannot disagree; get_pushing_count/get_pushed_count are NOT touched (unowned Phase-90 blocker)"
    - "SC#3 (D-08 hard gate): a hermetic two-tick integration test proves each file is dispatched exactly once and never to a cloud backend after a local dispatch, across (a) local dispatch, (b) rolled-back tick with a committed ledger row, (c) terminally-failed local analyze"
  artifacts:
    - path: "tests/integration/test_drain_double_dispatch.py"
      provides: "SC#3 two-tick double-dispatch HARD GATE"
      contains: "stage_cloud_window"
    - path: "src/phaze/services/pipeline.py"
      provides: "drain-candidate query cutover (D-05/D-06/D-07) + get_awaiting_cloud_count (D-15)"
      contains: "inflight_clause"
    - path: "src/phaze/services/backend_selection.py"
      provides: "staleness clock on cloud_job.updated_at (D-07)"
  key_links:
    - from: "src/phaze/services/pipeline.py get_cloud_staging_candidates"
      to: "cloud_job (INNER join + status='awaiting' conjunct + FOR UPDATE OF cloud_job SKIP LOCKED)"
      via: "composed inflight_clause/domain_completed_clause"
      pattern: "with_for_update\\(of="
    - from: "src/phaze/services/pipeline.py get_awaiting_cloud_count"
      to: "the same drain conjunct"
      via: "_safe_count over COUNT(cloud_job)"
      pattern: "inflight_clause"
---

<objective>
Cut the drain reader over to the sidecar — the sharpest-risk change in the phase. Rewrite
`get_cloud_staging_candidates` to exclude in-flight/terminally-failed local analyze by a predicate conjunct
(D-05), lock the candidacy table (D-06), keep FIFO on `created_at` while moving the staleness clock to
`cloud_job.updated_at` (D-07). Re-anchor `get_awaiting_cloud_count` on the same clause (D-15). Gate the whole
cutover behind the SC#3 two-tick double-dispatch integration test — a ROADMAP-designated HARD GATE, not a
recommendation.

Purpose: This closes SC#1 (no `FileRecord.state` routing read in the drain) without introducing the
rolled-back-tick double-dispatch the deletion variant would (SC#3). It runs AFTER the writer (83-05) and
migration (83-02) so no held file strands.

Output: cut-over drain query; staleness-clock move; re-anchored count card; SC#3 HARD GATE integration test;
`get_awaiting_cloud_count` unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-cloud-routing-sidecar-cutover/83-CONTEXT.md
@.planning/phases/83-cloud-routing-sidecar-cutover/83-RESEARCH.md
@.planning/phases/83-cloud-routing-sidecar-cutover/83-PATTERNS.md

<interfaces>
From src/phaze/services/stage_status.py — REUSE VERBATIM, never re-spell (DERIV-04 equivalence test):
  def inflight_clause(stage: Stage) -> ColumnElement[bool]        # :150
  def domain_completed_clause(stage: Stage) -> ColumnElement[bool] # :170  (DONE ∨ (FAILED ∧ FAILURE_IS_TERMINAL[stage]))
From src/phaze/enums/stage.py:
  Stage.ANALYZE ; FAILURE_IS_TERMINAL[ANALYZE] == True
From src/phaze/services/backend_selection.py:
  select_backend(...)  # currently reads file.updated_at at :108 as the spill staleness clock
Drain tick driver: src/phaze/tasks/release_awaiting_cloud.py — stage_cloud_window (advisory lock :135,
  candidate call :173, single post-loop commit / whole-tick rollback :257-264)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the SC#3 two-tick double-dispatch HARD GATE test FIRST (RED)</name>
  <files>tests/integration/test_drain_double_dispatch.py</files>
  <read_first>
    - tests/integration/test_release_awaiting_cloud.py — the scaffold to reuse: `_make_ctx(async_engine, router)`,
      the `DedupFakeQueue`/`DedupFakeTaskRouter`/`seed_active_agent` fakes, and the fake `Backend` impl with a
      stub `dispatch`. This is how you drive `stage_cloud_window` hermetically.
    - src/phaze/tasks/release_awaiting_cloud.py — stage_cloud_window: the advisory lock at :135, the candidate
      call at :173, and the single post-loop commit / whole-tick rollback at :257-264 (the mechanism the (b)
      rolled-back case exercises).
    - .planning/phases/83-cloud-routing-sidecar-cutover/83-RESEARCH.md — §"Pitfall 1" (the rolled-back-tick
      double-dispatch) and §"D-08" (the required test shape); §"Validation Architecture" the SC#3 row.
    - .planning/phases/83-cloud-routing-sidecar-cutover/83-VALIDATION.md — the SC#3 (HARD GATE) row and the
      Wave 0 fixture-reuse list.
  </read_first>
  <behavior>
    - Case (a) local dispatch: a held file (awaiting row) is dispatched to LocalBackend on tick 1; on tick 2 it
      is NOT re-picked (its committed scheduling_ledger row makes ~inflight_clause(ANALYZE) exclude it), so it
      is dispatched exactly once and never to a cloud backend.
    - Case (b) rolled-back tick with a committed ledger row: tick 1 dispatches locally (ledger row committed by
      the before_enqueue hook's own session) but the tick then rolls back (poisoned txn); on tick 2 the file is
      still excluded by the committed ledger row → dispatched exactly once, never cloud. (This is the case
      row-deletion would fail; the conjunct survives it.)
    - Case (c) terminally-failed local analyze: a file whose analyze failed (FAILURE_IS_TERMINAL[ANALYZE]=True,
      domain_completed) is excluded by ~domain_completed_clause(ANALYZE) → never re-picked.
    - Across all cases: assert each file's dispatch count == 1 and no cloud dispatch follows a local dispatch.
    - (Optional, MEDIUM value) EXPLAIN the drain SELECT and assert `ix_cloud_job_awaiting` usage — guard as
      non-fatal (plan shape varies); the D-14 reaper is the real degradation defense.
  </behavior>
  <action>
    Create `tests/integration/test_drain_double_dispatch.py` reusing the `test_release_awaiting_cloud.py`
    scaffold. Drive TWO sequential `stage_cloud_window(ctx)` ticks across the three outcomes above, using the
    fake Backend to record dispatch calls per file and per backend kind. Seed cloud_job awaiting rows and the
    scheduling_ledger / analysis-failed markers directly via fixtures. This test is written to FAIL against the
    current `state`-based drain query (RED) — do NOT modify `services/pipeline.py` in this task. Assert each
    file dispatched exactly once and never cloud-dispatched after a local dispatch. Must live in the
    `integration` bucket.
  </action>
  <acceptance_criteria>
    - `tests/integration/test_drain_double_dispatch.py` exists, drives two sequential `stage_cloud_window`
      ticks, and covers cases (a), (b) rolled-back-tick-with-committed-ledger-row, and (c) terminally-failed
      analyze.
    - Run against current source, the test FAILS (RED) — confirm via `just test-bucket integration` and record
      in the commit message.
    - `uv run ruff check tests/integration/test_drain_double_dispatch.py` passes.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check tests/integration/test_drain_double_dispatch.py</automated>
  </verify>
  <done>SC#3 hard-gate test committed RED, covering all three outcomes across two ticks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cut over the drain query (D-05/D-06/D-07) + move the staleness clock (GREEN)</name>
  <files>src/phaze/services/pipeline.py, src/phaze/services/backend_selection.py</files>
  <read_first>
    - src/phaze/services/pipeline.py — the file being modified. Read `get_cloud_staging_candidates` at
      :1248-1264 (the current `FileRecord.state == AWAITING_CLOUD` read + `.with_for_update(skip_locked=True)`
      + ORDER BY created_at). Confirm imports for `CloudJob`, `CloudJobStatus`, `Stage`, `inflight_clause`,
      `domain_completed_clause`.
    - src/phaze/services/stage_status.py — :150 (`inflight_clause`), :170 (`domain_completed_clause`) — compose
      VERBATIM; re-spelling breaks the DERIV-04 equivalence test (`tests/integration/test_stage_status_equivalence.py`).
    - src/phaze/services/backend_selection.py — :80-119 (`select_backend`), :108 (the `file.updated_at`
      staleness read) and :27-31 (how `cloud_attempts` is already passed explicitly — mirror that for updated_at).
    - src/phaze/tasks/release_awaiting_cloud.py — :88-95 and :205 (`_cloud_attempts_for` per-candidate read —
      the place to also surface `cloud_job.updated_at`), :210-214 (where file.updated_at is read today).
    - .planning/phases/83-cloud-routing-sidecar-cutover/83-RESEARCH.md — the D-05 drain query code example
      (lines 292-309), §"Pitfall 2" (INNER join + FOR UPDATE OF cloud_job) and §"Pitfall 3" (correlated EXISTS).
    - .planning/phases/83-cloud-routing-sidecar-cutover/83-PATTERNS.md — the "get_cloud_staging_candidates"
      and "backend_selection.py staleness clock move" sections.
  </read_first>
  <behavior>
    - The drain query returns FileRecord entities via an INNER join to cloud_job where
      cloud_job.status=='awaiting' AND ~inflight_clause(ANALYZE) AND ~domain_completed_clause(ANALYZE),
      ordered by FileRecord.created_at ASC, limited, with `with_for_update(of=CloudJob, skip_locked=True)`.
    - No `FileRecord.state` predicate remains in the drain (SC#1).
    - select_backend's spill decision reads the awaiting row's `cloud_job.updated_at` (passed explicitly),
      not `file.updated_at`; FIFO ordering is unchanged (still FileRecord.created_at).
    - The SC#3 test (Task 1) goes green: exactly-once dispatch, never cloud after local, across all three cases.
  </behavior>
  <action>
    Rewrite `get_cloud_staging_candidates` per the D-05 code example: `select(FileRecord).join(CloudJob,
    CloudJob.file_id == FileRecord.id).where(CloudJob.status == CloudJobStatus.AWAITING.value,
    ~inflight_clause(Stage.ANALYZE), ~domain_completed_clause(Stage.ANALYZE)).order_by(FileRecord.created_at.asc())
    .limit(limit).with_for_update(of=CloudJob, skip_locked=True)`. Use an INNER join (Postgres rejects FOR
    UPDATE on the nullable side of an outer join). Compose the two clause builders VERBATIM — do not hand-write
    the ledger/analysis EXISTS. Surface `cloud_job.updated_at` for each candidate (extra returned column or via
    a per-candidate read like `_cloud_attempts_for`) and pass it into `select_backend`. In
    `backend_selection.py`, change the staleness read at :108 from `file.updated_at` to the explicitly-passed
    `cloud_job.updated_at` (mirror how `cloud_attempts` is already passed). Keep FIFO on FileRecord.created_at
    (do NOT move it to cloud_job.created_at — that changes ordering). Do not alter the tick advisory lock or the
    single post-loop commit in release_awaiting_cloud.py.
  </action>
  <acceptance_criteria>
    - `just test-bucket integration` passes IN ISOLATION — the Task 1 SC#3 HARD GATE goes GREEN (all three
      cases, exactly-once, never-cloud-after-local). Command: `just test-bucket integration`.
    - `just test-bucket analyze` passes IN ISOLATION (drain/dispatch/backend_selection coverage) — command:
      `just test-bucket analyze`.
    - `get_cloud_staging_candidates` contains `.join(CloudJob`, `CloudJob.status == CloudJobStatus.AWAITING.value`,
      `~inflight_clause(Stage.ANALYZE)`, `~domain_completed_clause(Stage.ANALYZE)`, and
      `with_for_update(of=CloudJob, skip_locked=True)`; it contains NO `FileRecord.state` predicate.
    - `tests/integration/test_stage_status_equivalence.py` (DERIV-04) still passes — the clause builders were
      composed, not re-spelled.
    - `backend_selection.py` no longer reads `file.updated_at` for the spill clock; it uses the passed
      `cloud_job.updated_at`. FIFO remains `FileRecord.created_at`.
    - `uv run mypy src/phaze/services/pipeline.py src/phaze/services/backend_selection.py` and `uv run ruff check`
      on both pass.
  </acceptance_criteria>
  <verify>
    <automated>just test-bucket integration &amp;&amp; just test-bucket analyze</automated>
  </verify>
  <done>Drain cut over to the conjunct + FOR UPDATE OF cloud_job; staleness clock on cloud_job.updated_at; SC#3 green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Re-anchor get_awaiting_cloud_count on the drain clause (D-15) + test</name>
  <files>src/phaze/services/pipeline.py, tests/shared/routers/test_pipeline.py</files>
  <read_first>
    - src/phaze/services/pipeline.py — `get_awaiting_cloud_count` at :1112-1123 (the `_safe_count` wrapper +
      the current `FileRecord.state == AWAITING_CLOUD` read to re-anchor). Do NOT touch `get_pushing_count`
      (:1206) / `get_pushed_count` (:1224) — unowned Phase-90 blocker.
    - src/phaze/services/pipeline.py — the drain query from Task 2 (the same conjunct to reuse).
    - tests/shared/routers/test_pipeline.py — existing `/pipeline` count-card tests (e.g. the split-counts test)
      to mirror.
    - .planning/phases/83-cloud-routing-sidecar-cutover/83-PATTERNS.md — the "get_awaiting_cloud_count (D-15)"
      section; §"D-15" in RESEARCH.
  </read_first>
  <behavior>
    - get_awaiting_cloud_count returns COUNT(cloud_job) WHERE status='awaiting' AND ~inflight_clause(ANALYZE)
      AND ~domain_completed_clause(ANALYZE) — the count of genuinely-parked (non-locally-dispatched) awaiting
      rows, matching exactly what the drain would pick.
    - A LOCAL_ANALYZING long file that still carries its awaiting row is excluded from BOTH the count and the
      drain (card and drain cannot disagree).
    - The count stays _safe_count-wrapped (degrade-safe; the 5s poll never 500s).
  </behavior>
  <action>
    Re-anchor `get_awaiting_cloud_count` to `select(func.count(CloudJob.id)).where(CloudJob.status ==
    CloudJobStatus.AWAITING.value, ~inflight_clause(Stage.ANALYZE), ~domain_completed_clause(Stage.ANALYZE))`,
    keeping the `_safe_count(..., node="awaiting_cloud")` wrapper. Compose the clause builders verbatim (same as
    the drain). Add a test in `tests/shared/routers/test_pipeline.py` asserting the count derives from the drain
    clause: seed an awaiting file that is in-flight-analyze (LOCAL_ANALYZING) and assert it is excluded from the
    count, and a genuinely-parked awaiting file and assert it is counted. Do NOT modify get_pushing_count /
    get_pushed_count.
  </action>
  <acceptance_criteria>
    - `just test-bucket shared` passes IN ISOLATION — command: `just test-bucket shared`.
    - `get_awaiting_cloud_count` composes `~inflight_clause(Stage.ANALYZE)` and
      `~domain_completed_clause(Stage.ANALYZE)` over `CloudJob.status == 'awaiting'`, still wrapped by
      `_safe_count(..., node="awaiting_cloud")`; it contains NO `FileRecord.state` predicate.
    - The new test asserts a locally-analyzing awaiting file is excluded from the count and a parked awaiting
      file is counted (card and drain agree).
    - `get_pushing_count` / `get_pushed_count` are unmodified (grep: no diff to those functions).
    - `uv run mypy src/phaze/services/pipeline.py` and `uv run ruff check` pass.
  </acceptance_criteria>
  <verify>
    <automated>just test-bucket shared</automated>
  </verify>
  <done>Count card derives from the drain clause; card and drain cannot disagree; pushing/pushed untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drain tick (`*/5` cron) ↔ Postgres | The advisory-locked tick selects candidates and dispatches; concurrent callback routers (`/uploaded`, `/pushed`, `/upload-failed`) and the reconcile cron mutate `cloud_job` outside the tick's lock |
| control-plane ↔ display poll (`/pipeline/stats`) | The count card is a display read on the hot 5s poll — degrade-safe, never 500s |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-06-DBLDISP | Tampering | rolled-back-tick double-dispatch (a file with analysis in flight re-picked and cloud-dispatched) | mitigate | D-05 predicate conjunct (~inflight_clause survives the whole-tick rollback via the independently-committed ledger row) instead of row deletion. **Proven by** the SC#3 HARD GATE `tests/integration/test_drain_double_dispatch.py` case (b) (Task 1 RED → Task 2 GREEN) |
| T-83-06-STALE | Tampering | stale read of the candidacy column | mitigate | D-06 `with_for_update(of=CloudJob, skip_locked=True)` over an INNER join — Postgres EvalPlanQual re-checks the locked table's WHERE; locking only `files` would read `cloud_job.status` stale |
| T-83-06-CLOCK | Tampering | spill staleness clock silently breaks under Phase 90 | mitigate | D-07 moves the clock to `cloud_job.updated_at` (durable past the Phase-90 state-write removal); FIFO stays on immutable `FileRecord.created_at` |
| T-83-SC | Tampering | package installs | accept | No new packages installed (zero-new-deps milestone constraint) |
</threat_model>

<verification>
- SC#3 HARD GATE: `just test-bucket integration` green IN ISOLATION (test_drain_double_dispatch.py all three cases).
- `just test-bucket analyze` and `just test-bucket shared` green in isolation.
- DERIV-04 equivalence test (`tests/integration/test_stage_status_equivalence.py`) still green (clauses composed, not re-spelled).
- Grep audit: no `FileRecord.state` predicate in `get_cloud_staging_candidates` or `get_awaiting_cloud_count`;
  `with_for_update(of=CloudJob`; `backend_selection.py` reads `cloud_job.updated_at` not `file.updated_at`;
  `get_pushing_count`/`get_pushed_count` unchanged.
- `uv run mypy` clean on both services.
</verification>

<success_criteria>
- SC#1 met in the drain and count card: no `FileRecord.state` routing/display predicate; reads the sidecar + derived in_flight.
- SC#3 met: the two-tick HARD GATE proves exactly-once dispatch and no cloud-after-local across all three outcomes.
- D-06 lock on the candidacy table; D-07 FIFO preserved and staleness clock on `cloud_job.updated_at`.
- D-15 count card and drain cannot disagree; the unowned pushing/pushed cards are untouched.
</success_criteria>

<output>
Create `.planning/phases/83-cloud-routing-sidecar-cutover/83-06-SUMMARY.md` when done.
</output>
