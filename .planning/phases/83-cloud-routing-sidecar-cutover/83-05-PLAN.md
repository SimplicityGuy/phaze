---
phase: 83-cloud-routing-sidecar-cutover
plan: 05
type: execute
wave: 2
depends_on: ["83-01", "83-02"]
files_modified:
  - src/phaze/routers/pipeline.py
  - tests/integration/test_shadow_compare.py
autonomous: true
requirements: [SIDECAR-01]
must_haves:
  truths:
    - "D-01: trigger_analysis's long-file hold stops writing a bare file.state=AWAITING_CLOUD and instead calls hold_awaiting_cloud(session, file) (attempts=0), so every go-forward hold now carries a cloud_job(status='awaiting') sidecar row — closing the missing-writer gap that made the hard shadow invariant violate on every held file since 032"
    - "D-00d: the Phase-79 shadow-compare gate stays GREEN — with the go-forward writer (D-01) plus the 034 repair, the hard awaiting_cloud invariant (shadow_compare.py:131, AWAITING_CLOUD ⇒ cloud_job(status='awaiting')) passes on backfilled fixtures where it is violated at HEAD; contract stays implication-not-equality (no converse invariant added, no shadow_compare.py source change)"
  artifacts:
    - path: "src/phaze/routers/pipeline.py"
      provides: "trigger_analysis hold path cut over to the shared writer"
      contains: "hold_awaiting_cloud"
    - path: "tests/integration/test_shadow_compare.py"
      provides: "awaiting_cloud hard invariant green on a held-file fixture carrying its awaiting row"
  key_links:
    - from: "src/phaze/routers/pipeline.py trigger_analysis"
      to: "services/backends.hold_awaiting_cloud"
      via: "await call inside the hold loop; existing post-loop commit retained"
      pattern: "hold_awaiting_cloud"
---

<objective>
Cut the go-forward hold path over to the shared writer: `trigger_analysis` (`routers/pipeline.py:341-346`)
replaces its bare `file.state = FileState.AWAITING_CLOUD` with `await hold_awaiting_cloud(session, file)`
(D-01). This is the go-forward half of closing the D-01 discovery (the migration `034` from 83-02 is the
existing-corpus half). Prove the Phase-79 shadow gate stays green (D-00d) now that every held file carries
its sidecar row.

Purpose: Without this, new long-file holds keep creating sidecar-less `AWAITING_CLOUD` files, re-opening the
hard-invariant violation `034` just repaired — and the drain reader (83-06) would strand them.

Output: `trigger_analysis` cut over to `hold_awaiting_cloud`; a shadow-compare integration test asserting the
`awaiting_cloud` hard invariant is green on a held-file fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-cloud-routing-sidecar-cutover/83-CONTEXT.md
@.planning/phases/83-cloud-routing-sidecar-cutover/83-RESEARCH.md
@.planning/phases/83-cloud-routing-sidecar-cutover/83-PATTERNS.md

<interfaces>
From src/phaze/services/backends.py (created in 83-01):
  async def hold_awaiting_cloud(session: AsyncSession, file: FileRecord, *, attempts: int = 0) -> None
  # stamps file.state=AWAITING_CLOUD + upserts cloud_job(status='awaiting'); NEVER commits — the caller commits.
From src/phaze/services/shadow_compare.py:
  Invariant("awaiting_cloud", ..., _cloud_awaiting, soft=False)  # hard: AWAITING_CLOUD ⇒ cloud_job status=='awaiting'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cut trigger_analysis's hold path over to hold_awaiting_cloud (D-01)</name>
  <files>src/phaze/routers/pipeline.py</files>
  <read_first>
    - src/phaze/routers/pipeline.py — the file being modified. Read `trigger_analysis`'s hold loop at
      :339-356: the bare `file.state = FileState.AWAITING_CLOUD` at :346 (the D-01 gap — the file never imports
      CloudJob) and the existing post-loop `await session.commit()` at :356 (the writer's own commit boundary —
      it STAYS; this is the top of the hold, not a dispatch loop).
    - .planning/phases/83-cloud-routing-sidecar-cutover/83-PATTERNS.md — the "routers/pipeline.py —
      trigger_analysis hold path (D-01)" section (the call-site swap; attempts defaults to 0 for a fresh hold).
    - src/phaze/services/backends.py — `hold_awaiting_cloud` (83-01) to import.
  </read_first>
  <behavior>
    - After trigger_analysis holds a long file, that file has file.state==AWAITING_CLOUD AND a
      cloud_job(status='awaiting', attempts=0) row (previously it had only the state, no row).
    - The existing post-loop commit still commits the batch of holds (the helper itself never commits).
    - Non-long files are unaffected (no cloud_job row, no state change from this path).
  </behavior>
  <action>
    In `trigger_analysis`, replace `file.state = FileState.AWAITING_CLOUD` at :346 with
    `await hold_awaiting_cloud(session, file)` (attempts defaults to 0 — a fresh hold has spent no budget).
    Import `hold_awaiting_cloud` from `phaze.services.backends`. Keep the existing post-loop
    `await session.commit()` at :356 unchanged (the helper does not commit). Do not otherwise change the loop
    or the long-file threshold logic. Preserve type hints, double quotes, 150-char lines.
  </action>
  <acceptance_criteria>
    - `trigger_analysis` in `routers/pipeline.py` calls `await hold_awaiting_cloud(session, file)` and no
      longer contains a bare `file.state = FileState.AWAITING_CLOUD` in the hold loop.
    - `routers/pipeline.py` imports `hold_awaiting_cloud`; the post-loop `await session.commit()` is retained.
    - `just test-bucket shared` passes IN ISOLATION (the bucket covering `tests/shared/routers/test_pipeline.py`
      and the trigger_analysis route). Command: `just test-bucket shared`.
    - `uv run mypy src/phaze/routers/pipeline.py` and `uv run ruff check src/phaze/routers/pipeline.py` pass.
  </acceptance_criteria>
  <verify>
    <automated>just test-bucket shared</automated>
  </verify>
  <done>trigger_analysis holds via the shared writer; every go-forward hold carries its awaiting row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shadow-compare — awaiting_cloud hard invariant green on a held-file fixture (D-00d)</name>
  <files>tests/integration/test_shadow_compare.py</files>
  <read_first>
    - tests/integration/test_shadow_compare.py — the file being modified. Read how existing fixtures seed files
      and how the shadow-compare invariants are driven/asserted; the `awaiting_cloud` invariant is
      `shadow_compare.py:131` (`_cloud_awaiting`, soft=False).
    - src/phaze/services/shadow_compare.py — :82 (`_cloud_awaiting` checks status=='awaiting' exactly), :131
      (the hard invariant), :151 (the LOCAL_ANALYZING soft allowlist) — do NOT modify this source (no converse
      invariant is added; implication-not-equality, 79 D-04).
    - .planning/phases/83-cloud-routing-sidecar-cutover/83-VALIDATION.md — the "SC#3 (shadow green)" row.
  </read_first>
  <behavior>
    - A fixture seeding an AWAITING_CLOUD file WITH its cloud_job(status='awaiting') row (as trigger_analysis
      now produces, and 034 backfills) yields ZERO divergence on the hard awaiting_cloud invariant.
    - A LOCAL_ANALYZING file carrying an awaiting row is NOT a violation (soft allowlist / implication-only).
  </behavior>
  <action>
    Extend `tests/integration/test_shadow_compare.py`: add/adjust a fixture seeding an AWAITING_CLOUD file that
    carries its `awaiting` cloud_job row, and assert the hard `awaiting_cloud` invariant reports zero
    divergence (it is violated at HEAD without the writer + 034). Add a case that a LOCAL_ANALYZING file still
    carrying an awaiting row (post-D-13, pre-D-14 reap) does NOT violate any hard invariant. Make NO change to
    `services/shadow_compare.py`.
  </action>
  <acceptance_criteria>
    - `just test-bucket integration` passes IN ISOLATION, including the new awaiting_cloud-green assertion.
      Command: `just test-bucket integration`.
    - The test asserts zero divergence on the hard `awaiting_cloud` invariant for a held file carrying its
      awaiting row, and no hard-invariant violation for a LOCAL_ANALYZING file carrying an awaiting row.
    - `services/shadow_compare.py` is unmodified (grep: no diff to that file in this plan).
    - `uv run ruff check tests/integration/test_shadow_compare.py` passes.
  </acceptance_criteria>
  <verify>
    <automated>just test-bucket integration</automated>
  </verify>
  <done>Shadow gate green on the held-file fixture; implication-not-equality preserved; no source change to shadow_compare.py.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/API → control-plane (`trigger_analysis`) | Operator triggers analysis; the hold path is trusted control-plane logic writing files + cloud_job in one txn — no new external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-INVAR | Tampering | hard shadow invariant `AWAITING_CLOUD ⇒ status='awaiting'` | mitigate | D-01 go-forward writer + 034 repair make the invariant hold for all held files; Task 2 proves it green (it is violated at HEAD) |
| T-83-COMMIT | Denial of Service | hold-loop commit boundary | mitigate | The helper never commits; the existing post-loop commit is retained (Task 1) — no lock/txn regression |
| T-83-SC | Tampering | package installs | accept | No new packages installed (zero-new-deps milestone constraint) |
</threat_model>

<verification>
- `just test-bucket shared` and `just test-bucket integration` green in isolation.
- Grep audit: `trigger_analysis` uses `hold_awaiting_cloud`; no bare `file.state = FileState.AWAITING_CLOUD`
  remains in the hold loop; `services/shadow_compare.py` unchanged.
- `uv run mypy src/phaze/routers/pipeline.py` clean.
</verification>

<success_criteria>
- Every go-forward long-file hold carries its `cloud_job(status='awaiting')` sidecar row (D-01).
- The Phase-79 shadow gate stays green with implication-not-equality preserved (D-00d).
- No change to `shadow_compare.py` source.
</success_criteria>

<output>
Create `.planning/phases/83-cloud-routing-sidecar-cutover/83-05-SUMMARY.md` when done.
</output>
