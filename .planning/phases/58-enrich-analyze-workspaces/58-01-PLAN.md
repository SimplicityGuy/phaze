---
phase: 58-enrich-analyze-workspaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_enrich_analyze_workspaces.py
  - src/phaze/templates/shell/shell.html
  - .planning/phases/58-enrich-analyze-workspaces/58-UI-SPEC.md
autonomous: true
requirements: [WORK-05]
must_haves:
  truths:
    - "The v7.0 shell fires exactly ONE /pipeline/stats request per 5s from persistent chrome (not from any swappable workspace fragment) — WORK-05 / R-2"
    - "Polling is shed while the tab is backgrounded (document.hidden) and resumes on foreground — R-3"
    - "A Phase-58 test file exists asserting single-poll discipline + fragment bareness, with stubs for the four workspace tests"
    - "58-UI-SPEC.md records that EXTRACT SELECTED + row-checkboxes are deferred (D-02:)"
  artifacts:
    - path: "src/phaze/templates/shell/shell.html"
      provides: "Persistent #pipeline-stats poll element in chrome + visibilitychange shed"
      contains: "/pipeline/stats"
    - path: "tests/test_enrich_analyze_workspaces.py"
      provides: "Phase-58 test scaffold (WORK-01..05 + fragment bareness)"
      exports: ["test_single_poll_discipline", "test_stage_fragment_is_bare"]
  key_links:
    - from: "src/phaze/templates/shell/shell.html"
      to: "/pipeline/stats"
      via: "hx-get every 5s in persistent chrome"
      pattern: "hx-get=\"/pipeline/stats\""
---

<objective>
Wire the single live-refresh poll into the v7.0 shell chrome and create the Phase-58 test scaffold.

Phase 58 is an IA/presentation rewrite over EXISTING routers/services/endpoints — NO backend behavior change. This plan adds ZERO new endpoints, payloads, or stage semantics; it reuses the existing `GET /pipeline/stats` poll verbatim.

**Load-bearing gap this plan closes:** the v7.0 shell (`shell/shell.html`, Phase 57) currently contains NO live `/pipeline/stats` poll element — the only poll element in the codebase lives in the legacy `pipeline/dashboard.html`, which the shell never renders. So today the shell never refreshes live. This plan wires the single persistent poll into shell chrome (WORK-05 / R-2) and adds the `visibilitychange` shed (R-3, "the one NEW client behavior Phase 58 adds to the Phase-57 poll"). The shared scaffold/table/seed-target partials follow in Plan 02 (so they are reachable the moment Discover includes them — keeping the dead-template guard green).

**Upstream (already shipped):** Phase 57 (shell + `#stage-workspace` swap target + `$store.pipeline` + `STAGE_PARTIALS` whitelist) and Phase 57.1 (incremental mid-flight `analysis.fine_windows_analyzed/total` signal, PR #184). This plan depends on those being present on `main`.

Purpose: make every downstream workspace live-refreshable through ONE poll with ZERO second loop.
Output: chrome poll + visibilitychange shed; the new test file; the D-02 reconciliation note in 58-UI-SPEC.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-enrich-analyze-workspaces/58-CONTEXT.md
@.planning/phases/58-enrich-analyze-workspaces/58-RESEARCH.md
@.planning/phases/58-enrich-analyze-workspaces/58-PATTERNS.md
@.planning/phases/58-enrich-analyze-workspaces/58-UI-SPEC.md
@.planning/phases/58-enrich-analyze-workspaces/58-VALIDATION.md

<interfaces>
<!-- Reuse VERBATIM — do not author new endpoints/poll loops. -->
Existing poll endpoint (reuse): GET /pipeline/stats → pipeline.py:571 pipeline_stats_partial(); returns pipeline/partials/stats_bar.html with oob_counts=True (pipeline.py:628), which emits the OOB store seeds + cloud-card swaps (stats_bar.html:46-103).
Legacy poll element to MIRROR (do NOT reuse the legacy page): pipeline/dashboard.html:50 — `<div id="pipeline-stats" hx-get="/pipeline/stats" hx-trigger="every 5s" hx-swap="innerHTML">`.
Shell chrome anchors: shell.html:186 `<div id="stage-workspace" ...>{% include stage_partial %}</div>`; shell.html:190 empty right `<aside aria-label="Detail pane">` (stays empty — D-06 / Phase 61); shell.html ~211-256 existing htmx:afterSwap / htmx:historyRestore handler &lt;script&gt; block (add the visibilitychange listener here).
Fragment-bareness analog: tests/test_shell_routes.py:66 test_stage_fragment_is_bare (assert no `<html`/`<head` in HX response).
Conftest: tests/conftest.py:162 `client` (AsyncClient), :150 `session`, :171 `seed_test_agent`.
$store.pipeline keys (base.html:107-138) for the workspace-test stubs: discovered, analyzed, metadataExtracted, metadataDone/Total, fingerprintDone/Total, analyzeActive, agentOnline, analyzeBusy, etc.
Models for seed helpers: src/phaze/models/cloud_job.py (file_id, cloud_phase), src/phaze/models/analysis.py:28-29 (fine_windows_analyzed/total).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0 (Wave 0): Create the Phase-58 test scaffold + record the D-02 reconciliation note</name>
  <read_first>
    - tests/test_shell_routes.py (copy fixture/seam conventions: @pytest.mark.asyncio, client: AsyncClient, response.text substring asserts; test_stage_fragment_is_bare at :66)
    - tests/conftest.py (confirm `client`, `session`, `seed_test_agent`; check whether a CloudJob / AnalysisResult row can be seeded — if a helper is missing, add a small module-level seed helper in the new test file, NOT a backend change)
    - src/phaze/models/cloud_job.py, src/phaze/models/analysis.py (column shapes for the lane/window seed fixtures Plans 02-04 will need)
    - .planning/phases/58-enrich-analyze-workspaces/58-VALIDATION.md (Per-Task Verification Map — the 6 test names)
    - .planning/phases/58-enrich-analyze-workspaces/58-UI-SPEC.md (Copywriting "Primary actions" table — where EXTRACT SELECTED is listed)
  </read_first>
  <action>
    Create `tests/test_enrich_analyze_workspaces.py` as the single Phase-58 test file (Wave 0 gap from 58-VALIDATION.md). Define the full test surface; FILL the foundation test now and leave the four workspace tests as `pytest.mark.xfail(reason="lands in Plan 02/03/04", strict=False)` stubs that later plans convert to real assertions:
    - `test_stage_fragment_is_bare` (R-5): GET /s/discover with header HX-Request=true → 200 and `"<html"`/`"<head"` absent (reuse the test_shell_routes.py:66 pattern).
    - `test_single_poll_discipline` (WORK-05): GET / (full shell) → response.text contains EXACTLY ONE occurrence of `hx-get="/pipeline/stats"` and contains `visibilitychange`; AND GET /s/discover, /s/metadata, /s/fingerprint, /s/analyze (HX-Request) each contain NO `hx-trigger="every"` and NO `setInterval`.
    - xfail stubs (names exact, per 58-VALIDATION.md): `test_discover_workspace`, `test_metadata_trigger_all_wired`, `test_lane_cards_states`, `test_analyze_file_table_lane_and_windows`.
    Add module-level async seed helpers as needed (e.g. `_seed_analysis(session, file_id, fine_done, fine_total)`, `_seed_cloud_job(session, file_id, cloud_phase)`) using ONLY ORM inserts — test fixtures, not production code.
    Then append a one-line reconciliation note to `58-UI-SPEC.md` (cite `D-02`): EXTRACT SELECTED + per-row checkboxes are DEFERRED from Phase 58 (would need a subset-enqueue backend endpoint); see CONTEXT Deferred Ideas. Place it near the Copywriting "Primary actions" table.
  </action>
  <verify>
    <automated>uv run pytest tests/test_enrich_analyze_workspaces.py::test_stage_fragment_is_bare -x</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_enrich_analyze_workspaces.py` exists with all 6 test names from 58-VALIDATION.md; the four workspace tests are xfail stubs that collect cleanly.
    - test_single_poll_discipline is implemented (it FAILS until Task 1 lands the chrome poll — expected at Task 0; assert collection succeeds).
    - 58-UI-SPEC.md contains a line citing `D-02` with the words EXTRACT SELECTED + deferred.
  </acceptance_criteria>
  <done>The test file collects; the foundation test is implemented; the D-02 deferral is recorded in the UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 1: Wire the single persistent /pipeline/stats poll into shell chrome + visibilitychange shed</name>
  <read_first>
    - src/phaze/templates/shell/shell.html (chrome layout ~175-260; the existing handler &lt;script&gt; block; the right &lt;aside&gt; at :190 stays empty)
    - src/phaze/templates/pipeline/dashboard.html:50 (the legacy `#pipeline-stats` poll element to mirror — do NOT touch dashboard.html)
    - src/phaze/templates/pipeline/partials/stats_bar.html:34-103 (what the poll returns: oob_counts gate, dag-seed loop, cloud-card OOB includes — reused unchanged)
    - .planning/phases/58-enrich-analyze-workspaces/58-UI-SPEC.md (R-2 single-poll discipline; R-3 visibilitychange shed)
  </read_first>
  <action>
    In `shell/shell.html`, add ONE persistent poll element to the chrome (OUTSIDE `#stage-workspace`, so it survives every rail swap and is never duplicated/stopped by navigation): a hidden `<div id="pipeline-stats" hx-get="/pipeline/stats" hx-trigger="every 5s" hx-swap="innerHTML">` placed in the shell body (e.g. just before the existing handler `<script>`). This reuses the EXISTING endpoint verbatim — no new route. Its `hx-swap="innerHTML"` replaces #pipeline-stats with stats_bar.html each tick; the OOB seeds inside the response fan out to `$store.pipeline` and the cloud-card targets (those land on whatever workspace is mounted — Plans 02-04 provide the targets).
    Implement R-3 `visibilitychange` shed in the existing chrome `<script>`: a listener that on `document.hidden` parks the poll (e.g. set the element's `hx-trigger` to `none` and call `htmx.process(el)`) and on foreground restores `every 5s` (+ `htmx.process`). It MUST remain a single poll element — do NOT add a second loop.
    Do NOT edit stats_bar.html. Do NOT add OOB-seed placeholders to chrome here — those persistent targets ride each workspace (Plan 02 scaffold) so they never collide with the analyze stage's own targets.
  </action>
  <verify>
    <automated>uv run pytest tests/test_enrich_analyze_workspaces.py::test_single_poll_discipline tests/test_shell_routes.py -x</automated>
  </verify>
  <acceptance_criteria>
    - GET / renders exactly one `hx-get="/pipeline/stats"` and the string `visibilitychange`.
    - The poll element is in chrome, NOT inside `#stage-workspace` (a rail swap must not re-render or remove it).
    - dashboard.html and stats_bar.html are untouched; existing shell-route tests stay green.
  </acceptance_criteria>
  <done>The shell live-polls once per 5s from persistent chrome and sheds polling when backgrounded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → shell route | rail navigation + the 5s poll request cross here; `stage` is the only path-shaped input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | shell.py STAGE_PARTIALS stage resolution | mitigate | Unchanged this plan; `stage` stays whitelisted, never spliced into a template path |
| T-58-POLL | DoS (self-inflicted) | chrome poll | mitigate | Exactly one `/pipeline/stats` poll per 5s + `visibilitychange` shed (R-2/R-3); structural test asserts no second loop in any fragment |
| T-58-SC | Tampering | npm/pip installs | accept | Phase 58 installs NO packages (RESEARCH Package Legitimacy Audit: N/A); no registry interaction |
</threat_model>

<verification>
- `uv run pytest tests/test_enrich_analyze_workspaces.py tests/test_shell_routes.py -x`
- `uv run ruff check . && uv run mypy .`
- GET / renders one `hx-get="/pipeline/stats"` + `visibilitychange`; the four `/s/<stage>` fragments contain no `hx-trigger="every"` / `setInterval`.
</verification>

<success_criteria>
- Shell live-polls once per 5s from persistent chrome with a visibilitychange shed (WORK-05 / R-2 / R-3).
- Phase-58 test file exists; D-02 reconciliation note recorded in 58-UI-SPEC.md.
- Full suite + ruff + mypy green.
</success_criteria>

<output>
Create `.planning/phases/58-enrich-analyze-workspaces/58-01-SUMMARY.md` when done.
</output>
