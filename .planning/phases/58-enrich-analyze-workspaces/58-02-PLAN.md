---
phase: 58-enrich-analyze-workspaces
plan: 02
type: execute
wave: 2
depends_on: ["58-01"]
files_modified:
  - src/phaze/templates/pipeline/partials/_workspace_scaffold.html
  - src/phaze/templates/pipeline/partials/_file_table.html
  - src/phaze/templates/pipeline/partials/_workspace_poll_seeds.html
  - src/phaze/templates/pipeline/partials/discover_workspace.html
  - src/phaze/routers/shell.py
  - src/phaze/routers/pipeline.py
  - src/phaze/templates/base.html
  - tests/test_enrich_analyze_workspaces.py
autonomous: true
requirements: [WORK-01, WORK-05]
must_haves:
  truths:
    - "A reusable workspace scaffold (one <h1 tabindex=-1> focus target + live sub-count + secondary action row + body slot), a reusable file table (empty state + inert D-06: rows + path-safe cells), and a persistent OOB seed-target host exist for the four workspaces"
    - "The OOB store-key seeds emitted by stats_bar.html land on every stage because the seed-target placeholders ride each workspace via the shared scaffold — including dag-seed-notYetEnriched, pre-mounted even though notYetEnriched joins base.html later in this same plan"
    - "Selecting Discover shows recent scans + a live discovered / not-yet-enriched sub-count + a scan trigger (WORK-01)"
    - "The Discover workspace reuses the existing recent-scans data with its self-poll STRIPPED — no second loop (WORK-05 / Pitfall 4 / A3)"
    - "SCAN and RECOVER wire to existing scan endpoints; RECOVER carries a confirm + busy-disable guard (R-4)"
    - "not_yet_enriched is a read-only derived int (discovered − metadataExtracted, clamped ≥0) seeded onto the dag dict — no new query path"
  artifacts:
    - path: "src/phaze/templates/pipeline/partials/_workspace_scaffold.html"
      provides: "Shared workspace shell (Jura h1 focus target + sub-count + action row + body slot + poll-seed include)"
      contains: "tabindex"
    - path: "src/phaze/templates/pipeline/partials/_file_table.html"
      provides: "Reusable file/queue table with empty state, inert cursor-pointer rows, font-mono path cells"
      contains: "cursor-pointer"
    - path: "src/phaze/templates/pipeline/partials/_workspace_poll_seeds.html"
      provides: "Persistent hidden OOB seed-target placeholders (dag-seed-<key> incl. dag-seed-notYetEnriched + legacy *-files-ready / *-busy-seed ids)"
      contains: "dag-seed-notYetEnriched"
    - path: "src/phaze/templates/pipeline/partials/discover_workspace.html"
      provides: "Discover workspace fragment (scaffold + recent-scans table + sub-count + SCAN/RECOVER)"
      contains: "_workspace_scaffold.html"
  key_links:
    - from: "src/phaze/templates/pipeline/partials/_workspace_scaffold.html"
      to: "src/phaze/templates/pipeline/partials/_workspace_poll_seeds.html"
      via: "{% include %} so OOB store seeds always have a target"
      pattern: "_workspace_poll_seeds.html"
    - from: "src/phaze/templates/pipeline/partials/stats_bar.html dag.items() OOB loop"
      to: "src/phaze/templates/pipeline/partials/_workspace_poll_seeds.html dag-seed-notYetEnriched"
      via: "OOB swap targets a placeholder id that MUST pre-exist in the mounted workspace"
      pattern: "dag-seed-notYetEnriched"
    - from: "src/phaze/routers/shell.py"
      to: "build_recent_scans"
      via: "_render_stage discover context load"
      pattern: "recent_scans"
    - from: "src/phaze/routers/pipeline.py"
      to: "$store.pipeline.notYetEnriched"
      via: "derived int key on the dag dict (rides the existing dag.items() OOB loop)"
      pattern: "notYetEnriched"
---

<objective>
Build the shared presentation partials every workspace uses, and ship the first real workspace — Discover (WORK-01) — rendered as a content-only fragment into `#stage-workspace`.

NO backend behavior change: reuse the existing recent-scans data (`build_recent_scans`) and the existing scan endpoints verbatim. The ONE new server datum is a read-only DERIVED int (`notYetEnriched = discovered − metadataExtracted`) added to the existing `dag` dict so it rides the existing OOB seed loop with zero new poll and zero new query.

The shared partials (scaffold / file-table / poll-seed-target host) are created HERE so they are reachable the moment `discover_workspace.html` includes them — keeping the Phase-57 dead-template AST guard green. The poll-seed-target host makes the Plan-01 chrome poll's OOB store seeds land on every workspace (so workspace sub-counts refresh live).

Purpose: give the operator the Discover surface — recent scans + the not-yet-enriched backlog count + scan/recover triggers — live via the Plan-01 chrome poll, and lay the shared partials for Plans 03-04.
Output: `_workspace_scaffold.html`, `_file_table.html`, `_workspace_poll_seeds.html`, `discover_workspace.html`; shell.py discover context + STAGE_PARTIALS value; the `notYetEnriched` derived seed + base.html default + its `dag-seed-notYetEnriched` placeholder; real `test_discover_workspace`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-enrich-analyze-workspaces/58-CONTEXT.md
@.planning/phases/58-enrich-analyze-workspaces/58-RESEARCH.md
@.planning/phases/58-enrich-analyze-workspaces/58-PATTERNS.md
@.planning/phases/58-enrich-analyze-workspaces/58-UI-SPEC.md
@.planning/phases/58-enrich-analyze-workspaces/58-01-SUMMARY.md

<interfaces>
Shell dispatch: shell.py:51-64 STAGE_PARTIALS (currently `discover: _STAGE_PLACEHOLDER`); shell.py:67-97 _render_stage (only the `analyze` branch loads DB context today — Pitfall 5: discover has NO context; add it).
Recent scans (reuse): pipeline_scans.py:229 build_recent_scans(session) → list[ScanBatch] with _agent_name/_elapsed_seconds/_seconds_since_progress/_is_stalled attached (the SAME helper build_dashboard_context uses).
Recent-scans table partial: templates/pipeline/partials/recent_scans_table.html — SELF-POLLS at :17-19 (`hx-get="/pipeline/scans/recent" hx-trigger="every 5s" hx-swap="outerHTML"`). MUST be stripped on reuse (A3 / Pitfall 4) or re-authored into the workspace without the self-poll; also the source for the empty-state block + font-mono path cell (`title=`, no `| safe`).
Scaffold header analog: templates/shell/partials/_stage_placeholder.html (h1 tabindex=-1 focus target token string).
Derived-seed idiom: pipeline.py:131-217 _build_dag_context builds the `dag` dict of server-computed ints (e.g. dag["metadataBusy"] = int(...)); every key auto-rides the stats_bar.html dag.items() OOB loop + needs a base.html store default. stats_bar.html does NOT need editing.
OOB store-seed contract (stats_bar.html:46-67): hidden `<p id="dag-seed-<key>" hx-swap-oob="true" x-init="$store.pipeline.<key> = <int>">` ; legacy ids: `analyze-files-ready`, `fingerprint-files-ready`, `proposals-files-ready`, `agent-busy-seed`, `controller-busy-seed`. htmx OOB swaps apply ONLY to ids already present in the DOM — so the placeholder host must pre-exist on the mounted workspace. CRITICAL: notYetEnriched is added to base.html only in Task 2, but the placeholder host is authored in Task 1 "mirroring base.html"; explicitly include the dag-seed-notYetEnriched placeholder in Task 1 anyway so the seed lands once Task 2 adds the key (same OOB-target rule that bites Plan 04's computeOnline).
$store.pipeline keys (base.html:107-138): discovered, metadataExtracted already present; notYetEnriched is NEW.
Scan endpoints (reuse, already exist): POST /pipeline/scans (Start Scan) + POST /pipeline/recover (Phase 42). Wire SCAN/RECOVER to these — no new endpoints.
Build-time Tailwind (PR #181): the stylesheet is PRE-COMPILED, not JIT-in-browser — `assets/src/app.css` (`@source "../../src/phaze/templates"` already globs `pipeline/partials/`) is compiled to `src/phaze/static/css/app.css` by `just tailwind`. NEW utility/arbitrary-value classes these partials introduce (e.g. `tracking-[0.15em]`, `h-9`, `rounded-lg`, `hover:bg-white/5`) render ONLY after a `just tailwind` rebuild — otherwise the workspace renders unstyled while structural tests still pass green. No `assets/src/app.css` edit needed (glob already covers the new templates); `app.css` is a gitignored build artifact — regenerate, do NOT commit, do NOT add to files_modified.
UI-SPEC: Pattern 1 scaffold (Jura 18 h1 uppercase tracking-[0.15em]; muted Inter-14 sub-count; secondary `h-9 px-3 rounded-lg bg phaze-panel border phaze-border hover:bg-white/5 text-gray-300` action buttons); Pattern 2 file table (thead Jura text-[11px] uppercase tracking-[0.2em] text-gray-500; tbody divide-y phaze-border; row hover:bg-white/5 cursor-pointer; cells px-6 py-3); Discover sub-count "{discovered} files · last scan {when} · {not_yet_enriched} not yet enriched"; actions SCAN · RECOVER (RECOVER = R-4 bulk guard); empty "No scans yet".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared scaffold + file-table + poll-seed-target partials</name>
  <read_first>
    - src/phaze/templates/shell/partials/_stage_placeholder.html (h1 tabindex=-1 focus-target token string to reuse verbatim)
    - src/phaze/templates/pipeline/partials/recent_scans_table.html (table shell + empty-state block + font-mono path cell `title=` no `| safe`)
    - src/phaze/templates/base.html:107-138 ($store.pipeline key list — the seed-target placeholders must mirror these keys)
    - src/phaze/templates/pipeline/partials/stats_bar.html:46-67 (the dag-seed-<key> + agent-busy-seed / controller-busy-seed / *-files-ready ids the host must provide)
    - .planning/phases/58-enrich-analyze-workspaces/58-UI-SPEC.md (Pattern 1 scaffold; Pattern 2 file table; R-5 fragment correctness; R-1 inert row; D-06; Color/Typography/Spacing tokens)
  </read_first>
  <action>
    Create three content-only partials under `templates/pipeline/partials/` (NEVER `{% extends %}`, no `<html>`/`<head>`/second skip-link — R-5; they become reachable via discover_workspace's `{% include %}` in Task 3, keeping the dead-template AST guard green):
    1. `_workspace_scaffold.html` (UI-SPEC Pattern 1): a `<section>` root with a `px-6 py-4 border-b phaze-border` header containing one `<h1 tabindex="-1">` (Jura 18 uppercase `tracking-[0.15em]`, the htmx:afterSwap focus landing target) bound to a `title` var, a muted Inter-14 sub-count `<p>` bound via `x-text` (caller supplies the expression / falls back to a `subcount` var), and a right-aligned `flex gap-2` action-row slot. Use Jinja blocks or documented context vars (`title`, `subcount`, `actions`, `body`) so the four workspaces compose against it. Stage-action buttons use the Phase-57 SECONDARY style (NOT blue primary). Include `_workspace_poll_seeds.html` at the end so every workspace carries the OOB targets.
    2. `_file_table.html` (UI-SPEC Pattern 2): copy the `recent_scans_table.html` empty-state + thead/tbody structure retokened to C3. D-06: rows carry `cursor-pointer` + a stable row markup/id per R-1 but NO click binding (no hx-get, no selected-state). File-path / tag cells render `font-mono text-xs truncate` with `title="..."` and NEVER `| safe` (XSS V5). Status cell is a colored text span that ALWAYS includes a word (never hue-only). Accept generic `columns` + `rows` context so each workspace feeds its own column set.
    3. `_workspace_poll_seeds.html`: a single `class="hidden"` wrapper containing one placeholder `<p id="dag-seed-<key>" class="hidden">` for EVERY `$store.pipeline` key (mirror base.html:107-138) PLUS `<p id="agent-busy-seed">`, `<p id="controller-busy-seed">`, `<p id="analyze-files-ready">`, `<p id="fingerprint-files-ready">`, `<p id="proposals-files-ready">`. ALSO explicitly add `<p id="dag-seed-notYetEnriched" class="hidden">` even though notYetEnriched is not added to base.html until Task 2 of THIS plan — htmx OOB swaps apply ONLY to ids already present in the DOM, so the placeholder MUST be pre-mounted or the derived seed no-ops forever (Warning A / same OOB-target class as Plan 04's computeOnline). OOB *targets* only (no x-init, no hx-trigger) — the poll's emitted seeds replace each tick. NO `hx-trigger`/`setInterval` here.
  </action>
  <verify>
    <automated>uv run ruff check src/phaze 2>&1 | tail -3; test -f src/phaze/templates/pipeline/partials/_workspace_scaffold.html && test -f src/phaze/templates/pipeline/partials/_file_table.html && test -f src/phaze/templates/pipeline/partials/_workspace_poll_seeds.html && grep -q 'dag-seed-notYetEnriched' src/phaze/templates/pipeline/partials/_workspace_poll_seeds.html && echo PARTIALS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `_workspace_scaffold.html` has exactly one `tabindex="-1"` h1 and includes `_workspace_poll_seeds.html`; no `{% extends %}`, no `<html`/`<head`.
    - `_file_table.html` rows carry `cursor-pointer` and NO `hx-get`/selected-state (D-06); path cells use `font-mono` + `title=` + no `| safe`.
    - `_workspace_poll_seeds.html` contains a `dag-seed-` placeholder for every base.html store key AND an explicit `dag-seed-notYetEnriched` placeholder (added in this plan's Task 2) AND the five legacy seed ids; contains no `hx-trigger`/`setInterval`.
    - (Dead-template guard is run in Task 3 once discover_workspace makes these reachable.)
  </acceptance_criteria>
  <done>The three shared partials exist, fragment-correct and XSS-safe, with the notYetEnriched seed target pre-mounted, ready for Discover to compose.</done>
</task>

<task type="auto">
  <name>Task 2: Add the notYetEnriched derived seed + Discover per-stage context</name>
  <read_first>
    - src/phaze/routers/pipeline.py:131-217 (_build_dag_context — the derived-int idiom + how every key rides dag.items()) and :434-543 (build_dashboard_context — get_pipeline_stats gives discovered/metadata_extracted)
    - src/phaze/templates/base.html:107-138 ($store.pipeline init — where to add the notYetEnriched default)
    - src/phaze/routers/shell.py:67-97 (_render_stage — extend with a discover context branch mirroring the analyze branch)
    - src/phaze/routers/pipeline_scans.py:229 (build_recent_scans signature)
  </read_first>
  <action>
    In `pipeline.py` `_build_dag_context`, add ONE read-only derived int key: `dag["notYetEnriched"] = max(int(discovered) - int(metadata_extracted), 0)`, sourcing the two counts consistently with how the builder reads stage/stats today (read get_pipeline_stats or pass through). Keep it a plain server-computed int (safe for x-init). It auto-rides the existing `dag.items()` OOB loop — NO stats_bar.html edit — and lands on the `dag-seed-notYetEnriched` placeholder pre-mounted in Task 1.
    In `base.html`, add `notYetEnriched: 0` to the `Alpine.store('pipeline', {...})` init.
    In `shell.py` `_render_stage`, add a `discover` branch that loads `context["recent_scans"] = await build_recent_scans(session)` (degrade-safe at the service layer; no router try/except). Keep `oob_counts=False` on the stage render (Pitfall 3) and the static-whitelist invariant (`stage` never spliced into a template path — T-57-01).
  </action>
  <verify>
    <automated>uv run pytest tests/test_pipeline_dag_context.py tests/test_shell_routes.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `_build_dag_context` returns `notYetEnriched` as a non-negative int; existing dag-context tests stay green (extend them if they assert the key set).
    - base.html `$store.pipeline` includes `notYetEnriched: 0`.
    - shell.py `_render_stage` populates `recent_scans` for stage `discover`; `oob_counts` stays False on the stage render.
  </acceptance_criteria>
  <done>notYetEnriched rides the existing OOB loop onto its pre-mounted seed target; Discover has its recent-scans context.</done>
</task>

<task type="auto">
  <name>Task 3: Build discover_workspace.html, wire STAGE_PARTIALS['discover'], make shared partials reachable</name>
  <read_first>
    - src/phaze/templates/pipeline/partials/_workspace_scaffold.html + _file_table.html (Task 1 — compose against them)
    - src/phaze/templates/pipeline/partials/recent_scans_table.html (data columns + the empty-state block; STRIP its :17-19 self-poll on reuse)
    - src/phaze/routers/shell.py:51-64 (STAGE_PARTIALS — flip the `discover` value to a static literal)
    - .planning/phases/58-enrich-analyze-workspaces/58-UI-SPEC.md (Pattern 2 Discover columns; sub-count + SCAN/RECOVER copy; R-4 bulk guard; empty state)
    - tests/test_enrich_analyze_workspaces.py (convert the test_discover_workspace xfail stub)
  </read_first>
  <action>
    Create `templates/pipeline/partials/discover_workspace.html` as a content-only fragment composing `_workspace_scaffold.html`: title "DISCOVER"; sub-count `x-text` rendering "{discovered} files · {notYetEnriched} not yet enriched" from `$store.pipeline` (the static "last scan {when}" frame may come from the most-recent `recent_scans` row server-side); action row with secondary-style `SCAN` (wires to the existing scan trigger — POST /pipeline/scans / open the existing scan form) and `RECOVER` (POST /pipeline/recover) — RECOVER carries `hx-confirm` + a `:disabled` busy-gate (R-4). Body renders the recent scans by reusing the recent-scans table markup WITHOUT the self-poll (strip `hx-get`/`hx-trigger="every 5s"`/`hx-swap` — A3/Pitfall 4); rows inert (D-06). Empty state uses the locked copy ("No scans yet").
    Then run `just tailwind` to recompile `src/phaze/static/css/app.css` so the new workspace utility/arbitrary-value classes introduced by this plan (scaffold + file-table + discover) are emitted into the built stylesheet (PR #181 build-time Tailwind; the `@source` glob already covers `pipeline/partials/`, so no `assets/src/app.css` edit — regenerate only, do NOT commit `app.css`, it is gitignored).
    In `shell.py`, set `STAGE_PARTIALS["discover"] = "pipeline/partials/discover_workspace.html"` (static literal — T-57-01).
    Convert `test_discover_workspace` to real assertions (WORK-01): GET /s/discover (HX) → 200; contains the recent-scans table OR the "No scans yet" empty state; sub-count present; SCAN + RECOVER present; contains NO `hx-trigger="every"` (self-poll stripped); contains the `dag-seed-notYetEnriched` placeholder id (the sub-count's derived seed has a landing target).
  </action>
  <verify>
    <automated>uv run pytest tests/test_enrich_analyze_workspaces.py::test_discover_workspace tests/test_enrich_analyze_workspaces.py::test_single_poll_discipline tests/test_dead_template_guard.py -x && just tailwind && grep -q 'tracking-\[0.15em\]' src/phaze/static/css/app.css && echo CSS_REBUILT_OK</automated>
  </verify>
  <acceptance_criteria>
    - GET /s/discover (HX) renders the scaffold (one `tabindex="-1"` h1), the recent-scans surface or empty state, the live sub-count, and SCAN + RECOVER.
    - The discover fragment contains NO `hx-trigger="every"` / `setInterval` (self-poll stripped) and DOES contain the `dag-seed-notYetEnriched` placeholder id.
    - RECOVER has `hx-confirm` + `:disabled` busy-gate; rows are inert (no row `hx-get`).
    - STAGE_PARTIALS["discover"] points at the new partial; dead-template guard green (scaffold/table/poll-seeds now reachable via the include chain).
    - `just tailwind` was run after the new partials exist and `src/phaze/static/css/app.css` contains a class introduced by this plan (e.g. `grep -q 'tracking-\[0.15em\]' src/phaze/static/css/app.css`); `app.css` is NOT committed (gitignored build artifact).
  </acceptance_criteria>
  <done>Discover stage shows live recent scans + not-yet-enriched count + scan/recover, refreshing via the single chrome poll; shared partials are reachable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /s/discover + POST scans/recover | rail nav + scan/recover actions cross here; `stage` whitelisted |
| DB → render | scan paths / agent names flow into the recent-scans table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | shell.py STAGE_PARTIALS['discover'] | mitigate | Static literal value; `stage` never spliced into a template path |
| T-58-XSS | Tampering/Info-disclosure | scan_path / agent name cells + new file-table cells | mitigate | Jinja autoescape; `font-mono` + `title=`; no `| safe` (recent_scans_table precedent) |
| T-58-POLL | DoS (self-inflicted) | reused recent-scans self-poll | mitigate | Strip the partial's `hx-trigger="every 5s"` (A3/Pitfall 4); single chrome poll only |
| T-58-SEED | Tampering (UX integrity) | notYetEnriched OOB seed target | mitigate | Pre-mount `dag-seed-notYetEnriched` placeholder so the derived seed lands (else sub-count sticks at 0) |
| T-58-ENQ | DoS (self-inflicted) | RECOVER bulk re-enqueue | mitigate | R-4: `hx-confirm` + `:disabled` busy-gate; reuses the existing recover endpoint (no new path) |
| T-58-SC | Tampering | installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `uv run pytest tests/test_enrich_analyze_workspaces.py tests/test_shell_routes.py tests/test_pipeline_dag_context.py tests/test_dead_template_guard.py -x`
- `uv run ruff check . && uv run mypy .`
</verification>

<success_criteria>
- Shared scaffold / file-table / poll-seed-target partials exist and are reachable.
- Discover workspace delivers WORK-01 (recent scans + not-yet-enriched count + scan trigger), live via the single poll, with no second loop.
- notYetEnriched is a read-only derived seed that lands on a pre-mounted placeholder; no new endpoint/query/payload.
- Full suite + ruff + mypy green.
</success_criteria>

<output>
Create `.planning/phases/58-enrich-analyze-workspaces/58-02-SUMMARY.md` when done.
</output>
