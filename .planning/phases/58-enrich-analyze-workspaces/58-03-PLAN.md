---
phase: 58-enrich-analyze-workspaces
plan: 03
type: execute
wave: 3
depends_on: ["58-01", "58-02"]
files_modified:
  - src/phaze/templates/pipeline/partials/metadata_workspace.html
  - src/phaze/templates/pipeline/partials/fingerprint_workspace.html
  - src/phaze/routers/shell.py
  - tests/test_enrich_analyze_workspaces.py
autonomous: true
requirements: [WORK-02, WORK-05]
must_haves:
  truths:
    - "Selecting Metadata shows its file queue + an EXTRACT ALL trigger wired VERBATIM to POST /pipeline/extract-metadata (D-01:)"
    - "Selecting Fingerprint shows its file queue + a FINGERPRINT ALL trigger wired VERBATIM to POST /pipeline/fingerprint (D-01:)"
    - "Both ALL triggers carry a confirm + busy-disable guard (R-4) and target a response div consuming trigger_response.html branches"
    - "There is NO EXTRACT SELECTED button and NO per-row checkbox/selection state anywhere (D-02:)"
    - "Both workspaces refresh live via the single chrome poll — no second loop (WORK-05)"
  artifacts:
    - path: "src/phaze/templates/pipeline/partials/metadata_workspace.html"
      provides: "Metadata workspace (scaffold + queue table + EXTRACT ALL)"
      contains: "/pipeline/extract-metadata"
    - path: "src/phaze/templates/pipeline/partials/fingerprint_workspace.html"
      provides: "Fingerprint workspace (scaffold + queue table + FINGERPRINT ALL)"
      contains: "/pipeline/fingerprint"
    - path: "src/phaze/routers/shell.py"
      provides: "STAGE_PARTIALS metadata/fingerprint values + per-stage queue context"
      contains: "metadata_workspace.html"
  key_links:
    - from: "src/phaze/templates/pipeline/partials/metadata_workspace.html"
      to: "POST /pipeline/extract-metadata"
      via: "hx-post on EXTRACT ALL"
      pattern: "hx-post=\"/pipeline/extract-metadata\""
    - from: "src/phaze/templates/pipeline/partials/fingerprint_workspace.html"
      to: "POST /pipeline/fingerprint"
      via: "hx-post on FINGERPRINT ALL"
      pattern: "hx-post=\"/pipeline/fingerprint\""
---

<objective>
Replace the Phase-57 placeholders for the Metadata and Fingerprint stages with their real workspaces (WORK-02) — sibling fragments of identical shape, different endpoint.

NO backend behavior change: D-01: ALL-only triggers wired VERBATIM to the EXISTING `POST /pipeline/extract-metadata` (pipeline.py:959) and `POST /pipeline/fingerprint` (pipeline.py:1045), which enqueue ALL pending files via `get_metadata_pending_files` / `get_fingerprint_pending_files`. D-02: NO EXTRACT SELECTED, NO row-selection state (that needs a subset-enqueue endpoint = forbidden backend change; deferred, noted in 58-UI-SPEC.md by Plan 01).

Purpose: surface each manual enrich stage's queue + its existing bulk trigger, gated so it cannot double-enqueue, live via the single poll.
Output: `metadata_workspace.html` + `fingerprint_workspace.html`; shell.py STAGE_PARTIALS + per-stage queue context; real `test_metadata_trigger_all_wired`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-enrich-analyze-workspaces/58-CONTEXT.md
@.planning/phases/58-enrich-analyze-workspaces/58-RESEARCH.md
@.planning/phases/58-enrich-analyze-workspaces/58-PATTERNS.md
@.planning/phases/58-enrich-analyze-workspaces/58-UI-SPEC.md
@.planning/phases/58-enrich-analyze-workspaces/58-01-SUMMARY.md
@.planning/phases/58-enrich-analyze-workspaces/58-02-SUMMARY.md

<interfaces>
Metadata trigger (reuse VERBATIM): POST /pipeline/extract-metadata → pipeline.py:959 trigger_extraction_ui; pending set get_metadata_pending_files (services/pipeline.py:1038); returns pipeline/partials/trigger_response.html with context {action:"metadata extraction", count, no_active_agent}.
Fingerprint trigger (reuse VERBATIM): POST /pipeline/fingerprint → pipeline.py:1045 trigger_fingerprint_ui; pending set get_fingerprint_pending_files (services/pipeline.py:1052, METADATA_EXTRACTED ∪ failed-retry deduped); returns trigger_response.html with action="fingerprinting".
trigger_response.html branches: no_active_agent (amber held copy) / count>0 ("Enqueued N files") / else ("No files ready").
Queue listing: services/pipeline.py:727 get_files_by_state(session, state) → list[FileRecord]; pending helpers get_metadata_pending_files / get_fingerprint_pending_files for the count + list.
Busy store keys (already seeded by _build_dag_context): metadataBusy, fingerprintBusy in $store.pipeline.
Sub-count copy (UI-SPEC): Metadata "{metadataDone} extracted · {pending} pending · manual stage"; Fingerprint "{fingerprintDone} fingerprinted · chromaprint / fpcalc".
Columns (UI-SPEC Pattern 2): Metadata File · Format · Size · Existing tags · State; Fingerprint File · Duration · Chromaprint · AcoustID.
Shell dispatch: shell.py:51-64 STAGE_PARTIALS; :67-97 _render_stage (add metadata + fingerprint context branches — Pitfall 5; they have none today).
Shared partials (Plan 02): pipeline/partials/_workspace_scaffold.html, _file_table.html, _workspace_poll_seeds.html.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Metadata + Fingerprint per-stage queue context to shell.py</name>
  <read_first>
    - src/phaze/routers/shell.py:67-97 (_render_stage — add metadata + fingerprint branches mirroring the analyze branch's context-load shape)
    - src/phaze/services/pipeline.py:727 get_files_by_state, :1038 get_metadata_pending_files, :1052 get_fingerprint_pending_files (degrade-safe read helpers to reuse)
    - src/phaze/models/file.py (FileState members: METADATA_EXTRACTED etc. — choose the queue state set for each stage)
  </read_first>
  <action>
    In `shell.py` `_render_stage`, add `metadata` and `fingerprint` branches that load each stage's queue context (Pitfall 5 — neither has context today). For metadata: set `context["metadata_files"] = await get_metadata_pending_files(session)` (the pending list the EXTRACT ALL would enqueue) — or `get_files_by_state` for the displayed queue, whichever matches the UI-SPEC "stage file queue" intent; capture what the table needs (file, format, size, existing-tags, state). For fingerprint: set `context["fingerprint_files"] = await get_fingerprint_pending_files(session)`. Reuse the existing degrade-safe service helpers (no router try/except; services own the never-500 degrade). Keep `oob_counts=False` on the stage render and the static-whitelist invariant (T-57-01).
    Do NOT add new service functions — these are existing reads. Do NOT change any enqueue behavior.
  </action>
  <verify>
    <automated>uv run pytest tests/test_shell_routes.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `_render_stage` populates a queue list for stages `metadata` and `fingerprint` using EXISTING service reads.
    - `oob_counts` stays False on the stage render; no new service function added.
  </acceptance_criteria>
  <done>Both enrich workspaces have their queue data available to render.</done>
</task>

<task type="auto">
  <name>Task 2: Build metadata_workspace.html + fingerprint_workspace.html and wire STAGE_PARTIALS</name>
  <read_first>
    - src/phaze/templates/pipeline/partials/_workspace_scaffold.html + _file_table.html (Plan 02 shared partials — compose against them)
    - src/phaze/templates/pipeline/partials/trigger_response.html (the response branches each ALL button targets)
    - src/phaze/templates/pipeline/partials/dag_canvas.html (existing trigger-button pattern: hx-post + hx-target + :disabled busy gate — copy the idiom, NOT the canvas)
    - src/phaze/routers/shell.py:51-64 (STAGE_PARTIALS — flip metadata + fingerprint)
    - .planning/phases/58-enrich-analyze-workspaces/58-UI-SPEC.md (Pattern 2 columns; sub-count + button copy; R-4 bulk guard; empty states "Nothing pending" / "Nothing to fingerprint")
    - tests/test_enrich_analyze_workspaces.py (convert test_metadata_trigger_all_wired)
  </read_first>
  <action>
    Create `metadata_workspace.html`: content-only fragment composing `_workspace_scaffold.html` — title "METADATA"; sub-count `x-text` "{metadataDone} extracted · … · manual stage"; action row with ONE secondary-style `EXTRACT ALL` button: `hx-post="/pipeline/extract-metadata"`, `hx-target="#metadata-trigger-response"`, `hx-confirm="Enqueue metadata extraction for all pending files?"` (R-4), `:disabled="$store.pipeline.metadataBusy > 0"`; plus an empty `<div id="metadata-trigger-response"></div>`. Body = `_file_table.html` fed the metadata queue (columns File · Format · Size · Existing tags · State), inert rows (D-06), empty state "Nothing pending". D-02: NO EXTRACT SELECTED button, NO checkboxes, NO selection state.
    Create `fingerprint_workspace.html`: the sibling — title "FINGERPRINT"; sub-count "{fingerprintDone} fingerprinted · chromaprint / fpcalc"; ONE `FINGERPRINT ALL` button `hx-post="/pipeline/fingerprint"`, `hx-target="#fingerprint-trigger-response"`, `hx-confirm=...` (R-4), `:disabled="$store.pipeline.fingerprintBusy > 0"`; response div `#fingerprint-trigger-response`; body `_file_table.html` (columns File · Duration · Chromaprint · AcoustID), empty state "Nothing to fingerprint".
    In `shell.py`, set `STAGE_PARTIALS["metadata"]` and `STAGE_PARTIALS["fingerprint"]` to the new partial paths (static literals — T-57-01).
    Convert `test_metadata_trigger_all_wired` to real assertions (WORK-02 / D-01 / D-02): GET /s/metadata (HX) contains `hx-post="/pipeline/extract-metadata"`, contains NO "EXTRACT SELECTED" and NO `type="checkbox"`; GET /s/fingerprint contains `hx-post="/pipeline/fingerprint"`; both fragments contain NO `hx-trigger="every"`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_enrich_analyze_workspaces.py::test_metadata_trigger_all_wired tests/test_enrich_analyze_workspaces.py::test_single_poll_discipline tests/test_dead_template_guard.py -x</automated>
  </verify>
  <acceptance_criteria>
    - /s/metadata renders the scaffold + queue table + EXTRACT ALL posting to `/pipeline/extract-metadata` with hx-confirm + `:disabled` on metadataBusy; `#metadata-trigger-response` present.
    - /s/fingerprint renders FINGERPRINT ALL posting to `/pipeline/fingerprint` with the busy gate; `#fingerprint-trigger-response` present.
    - NO "EXTRACT SELECTED", NO `type="checkbox"`, NO row-selection state anywhere (D-02).
    - Neither fragment contains `hx-trigger="every"` / `setInterval`; rows inert (D-06).
    - STAGE_PARTIALS metadata/fingerprint point at the new partials; dead-template guard green.
  </acceptance_criteria>
  <done>Metadata + Fingerprint stages show their queue + their existing bulk trigger, double-enqueue-guarded, live via the single poll.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /s/{metadata,fingerprint} + POST triggers | rail nav + bulk-enqueue actions cross here |
| DB → render | file paths / tags flow into the queue tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | shell.py STAGE_PARTIALS metadata/fingerprint | mitigate | Static literal values; `stage` never spliced into a template path |
| T-58-XSS | Tampering/Info-disclosure | file/tag cells in the queue tables | mitigate | Jinja autoescape; `font-mono` + `title=`; no `| safe` |
| T-58-ENQ | DoS (self-inflicted) | EXTRACT ALL / FINGERPRINT ALL bulk enqueue | mitigate | R-4: `:disabled` busy-gate (metadataBusy/fingerprintBusy) + `hx-confirm`; reuses the existing dedup-keyed endpoints (no new enqueue path) |
| T-58-SC | Tampering | installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `uv run pytest tests/test_enrich_analyze_workspaces.py tests/test_shell_routes.py tests/test_dead_template_guard.py -x`
- `uv run ruff check . && uv run mypy .`
</verification>

<success_criteria>
- Metadata + Fingerprint workspaces deliver WORK-02 (queue + existing manual trigger), D-01 verbatim endpoints, D-02 no selected/checkboxes, live via the single poll.
- Full suite + ruff + mypy green.
</success_criteria>

<output>
Create `.planning/phases/58-enrich-analyze-workspaces/58-03-SUMMARY.md` when done.
</output>
