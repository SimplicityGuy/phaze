---
phase: 58-enrich-analyze-workspaces
plan: 04
type: execute
wave: 4
depends_on: ["58-01", "58-03"]
files_modified:
  - src/phaze/templates/pipeline/partials/analyze_workspace.html
  - src/phaze/templates/pipeline/partials/_lane_card.html
  - src/phaze/services/pipeline.py
  - src/phaze/routers/pipeline.py
  - src/phaze/routers/shell.py
  - src/phaze/templates/base.html
  - tests/test_enrich_analyze_workspaces.py
autonomous: true
requirements: [WORK-03, WORK-04, WORK-05]
must_haves:
  truths:
    - "The Analyze workspace ALWAYS renders all three lane cards local / A1 / k8s; a down/unconfigured lane renders greyed with an explicit 'offline' vs 'not configured' label and 0 capacity (D-05:)"
    - "The k8s lane distinguishes healthy quota-wait from a genuine fault: admission_state_card.html has NO role=alert; inadmissible_card.html / localqueue_card.html carry role=alert (WORK-03)"
    - "ONE table below the lane grid lists ALL in-stage Analyze files (queued · running · awaiting-cloud · done), each with a status column + a local/A1/k8s lane badge (D-03:)"
    - "Per-file lane is DERIVED (no cloud_job → local; cloud_phase IS NULL → A1; cloud_phase set → k8s); windowed progress reads analysis.fine_windows_analyzed/total — completed rows show full coverage, in-flight rows show the 57.1 mid-flight N/M + running (D-04:)"
    - "Row-click is UNBOUND — inert-but-present rows only; no record fetch / pane content (D-06:)"
    - "All cloud counts ride the existing /pipeline/stats OOB card swaps — no new poll, no new backend behavior (WORK-05)"
  artifacts:
    - path: "src/phaze/templates/pipeline/partials/analyze_workspace.html"
      provides: "Analyze workspace: 3 lane cards + reused cloud cards + all-in-stage file table"
      contains: "_lane_card.html"
    - path: "src/phaze/templates/pipeline/partials/_lane_card.html"
      provides: "Reusable lane summary card (always-render, labeled offline/not-configured)"
      contains: "not configured"
    - path: "src/phaze/services/pipeline.py"
      provides: "Read-only multi-state Analyze file query (LEFT JOIN cloud_job + analysis)"
      exports: ["get_analyze_stage_files"]
  key_links:
    - from: "src/phaze/templates/pipeline/partials/analyze_workspace.html"
      to: "pipeline/partials/admission_state_card.html, inadmissible_card.html, localqueue_card.html, awaiting_cloud_card.html, staged_pushing_card.html, analyzing_cloud_card.html"
      via: "{% include %} verbatim under the lane grid (they already ride the OOB poll)"
      pattern: "admission_state_card.html"
    - from: "src/phaze/services/pipeline.py get_analyze_stage_files"
      to: "cloud_job + analysis"
      via: "LEFT JOIN for per-file lane + window coverage"
      pattern: "cloud_phase"
---

<objective>
Replace the Phase-57 analyze placeholder (currently `dag_canvas.html`) with the real Analyze workspace (WORK-03 + WORK-04): three execution-lane cards (local / A1 / k8s) with live capacity + Kueue quota-wait-vs-Inadmissible state, plus ONE table of ALL in-stage Analyze files with a per-file lane badge + windowed progress.

NO backend behavior change: reuse the EXISTING degrade-safe cloud-count services + the six existing cloud-card partials verbatim; the ONE new server datum is a read-only multi-state SELECT (`get_analyze_stage_files`) joining `cloud_job` + `analysis`, plus a read-only derived `computeOnline` seed. Phase 58 only READS the Phase-57.1 mid-flight windowed signal (`analysis.fine_windows_analyzed/total`) — no schema/query semantics change.

Purpose: give the operator the full routing picture (all three lanes always present, faults surfaced) and per-file lane + window progress, live via the single chrome poll.
Output: `analyze_workspace.html` + `_lane_card.html`; `get_analyze_stage_files` service; build_dashboard_context list + computeOnline seed + base.html default; STAGE_PARTIALS analyze value; real `test_lane_cards_states` + `test_analyze_file_table_lane_and_windows`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-enrich-analyze-workspaces/58-CONTEXT.md
@.planning/phases/58-enrich-analyze-workspaces/58-RESEARCH.md
@.planning/phases/58-enrich-analyze-workspaces/58-PATTERNS.md
@.planning/phases/58-enrich-analyze-workspaces/58-UI-SPEC.md
@.planning/phases/58-enrich-analyze-workspaces/58-01-SUMMARY.md
@.planning/phases/58-enrich-analyze-workspaces/58-03-SUMMARY.md

<interfaces>
Analyze already gets DB context: shell.py:89-93 _render_stage `analyze` branch calls build_dashboard_context; STAGE_PARTIALS["analyze"] is currently "pipeline/partials/dag_canvas.html" — flip to analyze_workspace.html.
build_dashboard_context (pipeline.py:434-543) already returns: awaiting_cloud_count, pushing_count, analyzing_cloud_count, inadmissible_count, localqueue_unreachable, queued_behind_quota_count/admitted_count/running_count/finished_count, **dag_ctx, recent_scans. ADD the analyze file list here.
Cloud-card partials (reuse VERBATIM, do NOT restyle) + their OOB ids: awaiting_cloud_card.html (#awaiting-cloud-card, A1 held, sky), staged_pushing_card.html (#staged-pushing-card, A1 push, amber), analyzing_cloud_card.html (#analyzing-cloud-card, A1 in-analysis, violet), admission_state_card.html (#admission-state-card, k8s healthy progression — NO role=alert), inadmissible_card.html (#inadmissible-card, k8s fault — role="alert"), localqueue_card.html (#localqueue-card, k8s fault — role="alert"). Carrier-always/body-conditional; oob=True flips them to OOB (stats_bar.html includes them on the poll).
Lane capacity sources: local ← $store.pipeline.analyzeBusy / analyzeActive / agentOnline; A1 online ← compute-agent presence (select_active_agent kind="compute", enqueue_router.py:96) — NO kind-split count exists, so add a derived `computeOnline` seed; k8s ← queued_behind_quota + admitted (pending) / running, from get_cloud_phase_counts (services/pipeline.py:864).
"not configured" (D-05) ← config.py:406 settings.cloud_target Literal["local","a1","k8s"]: A1 not-configured when cloud_target != "a1"; k8s not-configured when cloud_target != "k8s". "offline" = configured-but-no-online-agent (A1) or localqueue_unreachable True (k8s).
Per-file lane derivation (no cloud_target file column): no cloud_job row → local; cloud_job.cloud_phase IS NULL → A1; cloud_job.cloud_phase set → k8s. cloud_job model: cloud_job.py file_id:72 (unique), cloud_phase:91.
Window coverage: analysis.fine_windows_analyzed / fine_windows_total (models/analysis.py:28-29). Post-57.1 an in-flight file has an analysis row that increments mid-flight → in-flight row shows "N/M windows" + running; completed (ANALYZED) shows full coverage.
Multi-state query analog: services/pipeline.py:727 get_files_by_state (single-state SELECT to extend to WHERE state IN (...) + LEFT JOIN cloud_job + LEFT JOIN analysis). Candidate Analyze-stage states (CONFIRM against models/file.py + analysis pipeline): ANALYZED, AWAITING_CLOUD, PUSHING, PUSHED, ANALYSIS_FAILED, plus in-flight (mid-flight analysis row present, not yet ANALYZED).
Cloud_job lifecycle (CONFIRM the A1 assumption A1): read src/phaze/services/release_awaiting_cloud.py + src/phaze/services/cloud_staging.py so a transient cloud_job row cannot mislabel a file that ran local.
Shared partials (Plan 02): _workspace_scaffold.html, _file_table.html, _workspace_poll_seeds.html.
UI-SPEC: Pattern 3 lane cards (grid grid-cols-3 gap-4 p-6; capacity numeral font-medium Inter-500; local emerald-400 / A1 blue-400 / k8s amber-400 border-amber-500/30); Pattern 4 per-file lane badge (🖥️ local text-emerald-300 / ☁️ A1 text-blue-300 / ⎈ k8s text-amber-300) + State (running/window X/Y blue, pending(quota) amber); sub-count "{analyzeActive} files in flight across 3 lanes"; empty "No files in flight".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the read-only multi-state Analyze file query + computeOnline seed + context plumbing</name>
  <read_first>
    - src/phaze/services/pipeline.py:727 get_files_by_state (the single-state SELECT shape to extend) + :702 count_active_agents + :864 get_cloud_phase_counts + :807 get_awaiting_cloud_count (degrade-safe idiom — SAVEPOINT / _safe_count, never-500)
    - src/phaze/services/enqueue_router.py:96 select_active_agent(session, kind=...) (the canonical compute-online liveness seam — do NOT invent a new rule)
    - src/phaze/services/release_awaiting_cloud.py + src/phaze/services/cloud_staging.py (CONFIRM cloud_job lifecycle vs FileState — assumption A1)
    - src/phaze/models/file.py (FileState members — pin the Analyze-stage state set), src/phaze/models/cloud_job.py (cloud_phase), src/phaze/models/analysis.py:28-29 (fine_windows columns)
    - src/phaze/routers/pipeline.py:434-543 build_dashboard_context (where to add the file list) and :131-217 _build_dag_context (where to add computeOnline)
    - src/phaze/templates/base.html:107-138 ($store.pipeline init — add computeOnline default)
  </read_first>
  <action>
    Add `get_analyze_stage_files(session)` to `services/pipeline.py`: ONE read-only SELECT over FileRecord WHERE state IN the Analyze-stage set (confirm the set against models/file.py + the analysis pipeline; include the done/cloud/failed buckets and in-flight files) with `LEFT JOIN cloud_job ON cloud_job.file_id == FileRecord.id` and `LEFT JOIN analysis ON analysis.file_id == FileRecord.id`, returning per file the fields the table needs: file path/name, duration (join FileMetadata if needed), the derived lane signal (cloud_job present? cloud_phase value), and `fine_windows_analyzed`/`fine_windows_total`. Make it degrade-safe in the existing style (SAVEPOINT + return [] on any error — mirror count_active_agents / get_awaiting_cloud_count); it must NEVER 500 the hot poll. This is a pure read — no behavior change.
    In `pipeline.py` `_build_dag_context`, add `dag["computeOnline"] = int(await count_active_agents(session, kind="compute"))` if count_active_agents supports a kind arg; otherwise add a small degrade-safe kind-scoped count using the select_active_agent liveness definition. It rides the existing dag.items() OOB loop (no stats_bar.html edit).
    In `pipeline.py` `build_dashboard_context`, add `"analyze_files": await get_analyze_stage_files(session)` to the returned dict (shell.py's analyze branch already merges this context).
    In `base.html`, add `computeOnline: 0` to the `$store.pipeline` init.
  </action>
  <verify>
    <automated>uv run pytest tests/test_services/test_pipeline.py tests/test_pipeline_dag_context.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `get_analyze_stage_files` returns a per-file structure carrying lane signal (cloud_job/cloud_phase) + window coverage; degrades to [] on DB error (never raises into the poll).
    - `computeOnline` is a non-negative int on the dag dict; base.html has `computeOnline: 0`.
    - build_dashboard_context returns `analyze_files`.
    - The cloud_job-lifecycle confirmation (A1) is noted in the SUMMARY (lane derivation cannot mislabel a local file).
  </acceptance_criteria>
  <done>The Analyze workspace has its per-file list + lane/window data + lane-capacity seeds, all read-only.</done>
</task>

<task type="auto">
  <name>Task 2: Build _lane_card.html + analyze_workspace.html and wire STAGE_PARTIALS['analyze']</name>
  <read_first>
    - src/phaze/templates/pipeline/partials/awaiting_cloud_card.html + admission_state_card.html + inadmissible_card.html + localqueue_card.html + staged_pushing_card.html + analyzing_cloud_card.html (reuse verbatim; note their ids + role=alert vs none + carrier-always/body-conditional pattern)
    - src/phaze/templates/pipeline/partials/_workspace_scaffold.html + _file_table.html (Plan 02 shared partials)
    - src/phaze/routers/shell.py:51-64 STAGE_PARTIALS (flip analyze) + :89-93 (analyze branch already merges build_dashboard_context — keep it)
    - .planning/phases/58-enrich-analyze-workspaces/58-UI-SPEC.md (Pattern 3 lane cards + lane identity colors + Kueue state colors; Pattern 4 lane badge + window cell; D-05 always-render labels; Color note amber-not-red for faults)
    - tests/test_enrich_analyze_workspaces.py (convert test_lane_cards_states + test_analyze_file_table_lane_and_windows)
  </read_first>
  <action>
    Create `_lane_card.html` (UI-SPEC Pattern 3): a `rounded-xl bg phaze-panel border {lane-border} p-4` card with a title row (emoji + LANE · NODE in lane identity color), a `mono font-medium` capacity numeral, an `h-1.5` capacity bar, and a sub-label. Parameterize by lane (local/A1/k8s) so analyze_workspace renders three. D-05: ALWAYS render the card; when the lane is unavailable, render it greyed with 0 capacity and an explicit label — "offline" (configured-but-no-online-agent: A1 with computeOnline==0, or k8s with localqueue_unreachable) vs "not configured" (cloud_target != lane). Bind capacity to EXISTING `$store.pipeline` keys (analyzeBusy/analyzeActive/agentOnline/computeOnline) + the cloud counts already in context; do NOT invent store keys for data a card partial already carries (Pitfall 2). Lane/state never hue-only — always a word + glyph (WCAG 1.4.1).
    Create `analyze_workspace.html`: content-only fragment composing `_workspace_scaffold.html` — title "ANALYZE"; sub-count "{analyzeActive} files in flight across 3 lanes"; action row with secondary `ROUTE RULES` + `PAUSE` (PAUSE is reversible — no confirm, R-4). Body: (a) `grid grid-cols-3 gap-4 p-6` of three `_lane_card.html` (local/A1/k8s, always present); (b) BELOW the grid, `{% include %}` the six cloud-card partials VERBATIM (admission_state_card, inadmissible_card, localqueue_card, awaiting_cloud_card, staged_pushing_card, analyzing_cloud_card) — they carry their own ids so the poll's OOB swaps land here, and they encode the quota-wait-vs-Inadmissible alert distinction; (c) ONE `_file_table.html` listing ALL `analyze_files` (D-03): columns File · Duration · Lane · State. Lane cell = emoji + label in lane color derived per file (no cloud_job → 🖥️ local; cloud_phase NULL → ☁️ A1; cloud_phase set → ⎈ k8s). State/window cell: completed rows show `window {fine_windows_analyzed}/{fine_windows_total}` (mono); in-flight rows show `running` + the mid-flight `N/M windows` (57.1 signal); quota-pending rows show `pending (quota)` amber (D-04). Rows inert (D-06 — cursor-pointer, NO hx-get, no selected-state, no pane fetch). Empty state "No files in flight".
    In `shell.py`, set `STAGE_PARTIALS["analyze"] = "pipeline/partials/analyze_workspace.html"` (static literal — T-57-01). The analyze context branch already merges build_dashboard_context (now carrying analyze_files); keep it.
    Convert the two tests: `test_lane_cards_states` (WORK-03 / D-05) — all 3 lane cards render; a not-configured lane shows "not configured"; an offline lane shows "offline"; inadmissible_card carries `role="alert"` while admission_state_card does NOT. `test_analyze_file_table_lane_and_windows` (WORK-04 / D-03 / D-04) — seed a completed file (analysis aggregate) → row shows its lane badge + `window {a}/{total}`; seed an in-flight file → `running`; seed a cloud_job with cloud_phase NULL → ☁️ A1 badge, with cloud_phase set → ⎈ k8s badge; assert rows have NO `hx-get` (D-06).
  </action>
  <verify>
    <automated>uv run pytest tests/test_enrich_analyze_workspaces.py tests/test_shell_routes.py tests/test_dead_template_guard.py -x</automated>
  </verify>
  <acceptance_criteria>
    - GET /s/analyze (HX) renders exactly three lane cards (local/A1/k8s) always; not-configured/offline labels appear for unavailable lanes with 0 capacity.
    - The fragment includes admission_state_card.html (no `role="alert"`) and inadmissible_card.html / localqueue_card.html (with `role="alert"`) verbatim.
    - The all-in-stage file table renders File · Duration · Lane · State; lane badge derived per the cloud_job rule; completed rows show `window {a}/{total}`, in-flight show `running`.
    - Rows have NO `hx-get` and no selected-state (D-06); fragment has NO `hx-trigger="every"` / `setInterval` (WORK-05).
    - STAGE_PARTIALS["analyze"] points at analyze_workspace.html; dead-template guard green (dag_canvas.html stays reachable via legacy dashboard.html — superseded-in-place, NOT deleted; that is CUT-02 / Phase 62).
  </acceptance_criteria>
  <done>The Analyze stage shows 3 always-present lanes with live capacity + fault distinction and a full per-file lane + window table, live via the single poll.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /s/analyze + 5s poll | rail nav + live cloud-card OOB swaps cross here |
| DB → render | file paths + window counts + cloud_phase flow into the lane cards + file table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | shell.py STAGE_PARTIALS['analyze'] | mitigate | Static literal value; `stage` never spliced into a template path |
| T-58-XSS | Tampering/Info-disclosure | file-path cell in the all-in-stage table | mitigate | Jinja autoescape; `font-mono` + `title=`; no `| safe`; lane/window values are server-computed ints/static strings |
| T-58-DEGRADE | DoS (self-inflicted) | get_analyze_stage_files on the hot poll | mitigate | Degrade-safe SAVEPOINT read returning [] on any error (never-500), mirroring count_active_agents / get_awaiting_cloud_count |
| T-58-ALERT | Tampering (UX integrity) | Kueue quota-wait vs Inadmissible | mitigate | Reuse inadmissible_card/localqueue_card (role="alert") vs admission_state_card (no alert) verbatim — the fault distinction cannot be collapsed |
| T-58-SC | Tampering | installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `uv run pytest tests/test_enrich_analyze_workspaces.py tests/test_shell_routes.py tests/test_services/test_pipeline.py tests/test_pipeline_dag_context.py tests/test_dead_template_guard.py -x`
- `uv run pytest --cov --cov-report=term-missing` (≥85%)
- `uv run ruff check . && uv run mypy .`
</verification>

<success_criteria>
- Analyze workspace delivers WORK-03 (3 always-present lanes + live capacity + quota-wait-vs-Inadmissible) and WORK-04 (per-file lane badge + windowed progress), reading the 57.1 mid-flight signal, live via the single poll.
- D-03/D-04/D-05/D-06 all honored; NO backend behavior change (one read-only query + one derived seed).
- Full suite + ruff + mypy green; coverage ≥85%.
</success_criteria>

<output>
Create `.planning/phases/58-enrich-analyze-workspaces/58-04-SUMMARY.md` when done.
</output>
