---
phase: 82-counts-pending-set-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/services/stage_status.py
  - tests/integration/test_stage_status_equivalence.py
autonomous: true
requirements: [READ-01]
must_haves:
  truths:
    - "eligible_clause(stage) SQL agrees with the Python eligible() for every enrich stage across the fixture matrix"
    - "A failed analyze is ineligible (absent from the analyze eligible set) — the ELIG-03 44.5K over-enqueue guard"
    - "A failed metadata and a failed fingerprint stay eligible (auto-retry preserved)"
  artifacts:
    - path: "src/phaze/services/stage_status.py"
      provides: "eligible_clause(stage) — the SQL twin of enums.stage.eligible() for the three enrich stages"
      contains: "def eligible_clause"
    - path: "tests/integration/test_stage_status_equivalence.py"
      provides: "ELIGIBLE_CASES matrix + test_eligible_sql_equals_python (DERIV-04 drift lock)"
      contains: "ELIGIBLE_CASES"
  key_links:
    - from: "eligible_clause"
      to: "ELIGIBLE_AFTER_FAILURE"
      via: "table-driven analyze ~failed_clause conjunct (never inlined)"
      pattern: "ELIGIBLE_AFTER_FAILURE\\[stage\\]"
    - from: "test_eligible_sql_equals_python"
      to: "eligible()"
      via: "three-way assert sql == py == expected"
      pattern: "eligible\\("
---

<objective>
Add the single new SQL builder this phase needs — `eligible_clause(stage)` in `services/stage_status.py` — the SQL twin of the pure-Python `enums.stage.eligible()`, defined only for the three enrich stages, and drift-lock it against `eligible()` by extending Phase-78's DERIV-04 equivalence harness with an additive `ELIGIBLE_CASES` matrix.

Purpose: Every enrich pending-set cutover in Plan 02 composes this one predicate, so eligibility lives in exactly one place with a parametrized SQL==Python lock — the drift class the milestone exists to prevent (D-01, D-00d). This plan is purely additive: no reader is wired yet, so it is safe to land first behind the standing shadow-compare gate.

Output: `eligible_clause(stage)` + the `ELIGIBLE_CASES` matrix and `test_eligible_sql_equals_python`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-counts-pending-set-cutover/82-CONTEXT.md
@.planning/phases/82-counts-pending-set-cutover/82-RESEARCH.md
@.planning/phases/82-counts-pending-set-cutover/82-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs — extracted from main. Use directly, no exploration. -->

From src/phaze/enums/stage.py (the Python truth being mirrored):
  eligible(status_map, stage, has_approved_proposal=False) -> bool
  # for an enrich stage: status not in (Status.DONE, Status.IN_FLIGHT)
  #                      and (status != Status.FAILED or ELIGIBLE_AFTER_FAILURE[stage])
  ELIGIBLE_AFTER_FAILURE = {Stage.ANALYZE: False, Stage.METADATA: True, Stage.FINGERPRINT: True}
  # ELIGIBLE_AFTER_FAILURE is enrich-only-keyed (three keys); has_approved_proposal is APPLY-only.

From src/phaze/services/stage_status.py (LOCKED sibling builders to compose verbatim):
  done_clause(stage) -> ColumnElement[bool]        # correlated ~exists on FileRecord.id
  failed_clause(stage) -> ColumnElement[bool]      # analyze: AnalysisResult.failed_at IS NOT NULL
  inflight_clause(stage) -> ColumnElement[bool]    # scheduling_ledger "<fn>:<file_id>" key only (LEDGER-ONLY)
  domain_completed_clause(stage)                   # the enrich-only-guard analog to copy (raises ValueError)
  # Module already imports: and_, not_, exists, false (sqlalchemy); FAILURE_IS_TERMINAL, Stage, Status.

From tests/integration/test_stage_status_equivalence.py (harness to extend — reuse all fixtures/seeds):
  db_session fixture; _new_file; _seed_ledger; seed_metadata_none/done/failed_only/inflight;
  seed_fp_none/success/success_and_failed/failed_only/inflight;
  seed_analysis_none/partial/completed/failed/failed_inflight; load_scalars; resolve_status.
  DOMAIN_COMPLETED_CASES block + eval_sql_domain_completed lazy-import idiom (copy shape for ELIGIBLE_CASES).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the DERIV-04 harness with ELIGIBLE_CASES (RED-first)</name>
  <read_first>
    - tests/integration/test_stage_status_equivalence.py — copy the DOMAIN_COMPLETED_CASES block (~:445) and the eval_sql_domain_completed lazy-import helper (~:463) verbatim; reuse db_session, _new_file, every seed_* fn, load_scalars, resolve_status.
    - src/phaze/enums/stage.py — eligible(), ELIGIBLE_AFTER_FAILURE (the Python truth the SQL must match).
    - .planning/phases/82-counts-pending-set-cutover/82-RESEARCH.md — Pattern 2 (lines 160-199), the exact ELIGIBLE_CASES matrix.
    - .planning/phases/82-counts-pending-set-cutover/82-PATTERNS.md — the "ADD ELIGIBLE_CASES" section.
  </read_first>
  <behavior>
    - metadata: (seed_metadata_none, True), (seed_metadata_done, False), (seed_metadata_failed_only, True), (seed_metadata_inflight, False)
    - fingerprint: (seed_fp_none, True), (seed_fp_success, False), (seed_fp_success_and_failed, False), (seed_fp_failed_only, True), (seed_fp_inflight, False)
    - analyze: (seed_analysis_none, True), (seed_analysis_partial, True), (seed_analysis_completed, False), (seed_analysis_failed, False) [the load-bearing ELIG-03 cell], (seed_analysis_failed_inflight, False)
    - Each case asserts sql_eligible == py_eligible == expected (three-way).
  </behavior>
  <action>
    Add a third parametrized matrix `ELIGIBLE_CASES` to `tests/integration/test_stage_status_equivalence.py`, mirroring the existing `DOMAIN_COMPLETED_CASES` structure exactly. Add an `eval_sql_eligible(session, stage, file_id)` helper that lazy-imports `eligible_clause` from `phaze.services.stage_status` inside the function body (keeps `pytest --co` green while `eligible_clause` does not yet exist — the same RED-state idiom as `eval_sql_domain_completed`), executes `select(eligible_clause(stage)).where(FileRecord.id == file_id)`, and returns the scalar bool. Add `test_eligible_sql_equals_python(db_session, stage, seed_fn, expected)` that seeds the file, computes the SQL side via `eval_sql_eligible`, computes the Python side via `eligible({stage: resolve_status(stage, await load_scalars(...))}, stage)`, and asserts `sql_eligible == py_eligible == expected`. Reuse the existing seed fns verbatim — no new fixtures. Do NOT add `has_approved_proposal` to the harness (APPLY-only, irrelevant to enrich).
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_stage_status_equivalence.py -q --co 2>&1 | grep -q eligible</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --co` (collection only) stays GREEN — the lazy import means collection does not fail on the missing `eligible_clause`.
    - Running `test_eligible_sql_equals_python` at this point fails at execution (RED) with an ImportError/AttributeError on `eligible_clause` — proving the test drives the implementation, not the reverse.
    - `ELIGIBLE_CASES` contains exactly the 14 cells listed in the behavior block, including `(Stage.ANALYZE, seed_analysis_failed, False)`.
    - No new seed fixtures added; every `seed_*` referenced already exists in the file.
  </acceptance_criteria>
  <done>The ELIGIBLE_CASES matrix + eval_sql_eligible + test_eligible_sql_equals_python exist; collection is green; execution is RED pending Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement eligible_clause(stage) in stage_status.py (GREEN)</name>
  <read_first>
    - src/phaze/services/stage_status.py — `domain_completed_clause` (:196-228, the enrich-only-guard + table-driven analog to copy) and `awaiting_candidate_clause` (:231-262, the correlated-exists join contract to document).
    - src/phaze/enums/stage.py — `eligible()` and `ELIGIBLE_AFTER_FAILURE` (mirror exactly).
    - .planning/phases/82-counts-pending-set-cutover/82-RESEARCH.md — Pattern 1 (lines 124-158): the proposed body and the equivalence proof.
  </read_first>
  <action>
    Add `eligible_clause(stage: Stage) -> ColumnElement[bool]` to `services/stage_status.py`, next to `domain_completed_clause`. Guard on `ELIGIBLE_AFTER_FAILURE` (enrich-only): if `stage not in ELIGIBLE_AFTER_FAILURE`, raise `ValueError` in the same message shape as `domain_completed_clause` (using `getattr(stage, "value", stage)` and `sorted(s.value for s in ELIGIBLE_AFTER_FAILURE)`). Compose `conjuncts = [not_(inflight_clause(stage)), not_(done_clause(stage))]`; append `not_(failed_clause(stage))` ONLY when `not ELIGIBLE_AFTER_FAILURE[stage]` (analyze — the ELIG-03 terminal-failed carve-out); return `and_(*conjuncts)`. Drive the analyze carve-out off the table, NEVER an inline `if stage is Stage.ANALYZE`. Add `ELIGIBLE_AFTER_FAILURE` to the existing `from phaze.enums.stage import ...` line. Write a docstring documenting: the Python-truth equivalence (`status not in (DONE, IN_FLIGHT)` ⇔ `~inflight ∧ ~done` under the CASE precedence `in_flight ≻ done ≻ failed ≻ not_started`), that it is enrich-only, and the correlated-`~exists(... == FileRecord.id)` join contract (enclosing query must select-from/join FileRecord).
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_stage_status_equivalence.py -k eligible -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_eligible_sql_equals_python` passes GREEN for all 14 ELIGIBLE_CASES cells.
    - `grep -n "def eligible_clause" src/phaze/services/stage_status.py` returns one match; `grep -c "if stage is Stage.ANALYZE" ` within the eligible_clause body is 0 (table-driven, not inlined).
    - `eligible_clause(Stage.PROPOSE)` (a non-enrich stage) raises ValueError (enrich-only guard) — assert via a quick unit or the harness's negative behavior.
    - MUTATION CHECK (record in SUMMARY): temporarily drop the analyze `not_(failed_clause(stage))` conjunct → the `(Stage.ANALYZE, seed_analysis_failed, False)` cell goes RED; restore → GREEN. Proves the ELIG-03 guard has teeth.
    - `uv run ruff check src/phaze/services/stage_status.py` and `uv run mypy src/phaze/services/stage_status.py` pass.
  </acceptance_criteria>
  <done>eligible_clause exists, is table-driven off ELIGIBLE_AFTER_FAILURE, and the full ELIGIBLE_CASES matrix is green; the ELIG-03 mutation check is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure internal predicate builder + test; no external input crosses any boundary this plan touches. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01 | Tampering | eligible_clause SQL construction | mitigate | Composed only from LOCKED `ColumnElement` builders + `ELIGIBLE_AFTER_FAILURE` table lookup; no f-string / interpolated operator input (project rule T-42-03). |
| T-82-02 | Denial of Service (over-enqueue/cost) | analyze eligibility | mitigate | The analyze `~failed_clause` conjunct (driven off `ELIGIBLE_AFTER_FAILURE[ANALYZE]=False`) keeps a failed analyze terminal (ELIG-03); the `(ANALYZE, seed_analysis_failed, False)` anti-drift cell + mutation check guard the 44.5K over-enqueue class. |
| T-82-DDL | Tampering | schema | accept | No DDL / no Alembic revision in this plan (additive code + test only); nothing to migrate. |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_stage_status_equivalence.py -q` fully green (all three matrices).
- Shadow-compare gate unaffected (no reader wired yet): `just shadow-compare` still green.
- ruff + mypy clean on the touched files; pre-commit passes.
</verification>

<success_criteria>
- `eligible_clause(stage)` exists for the three enrich stages, table-driven off `ELIGIBLE_AFTER_FAILURE`, drift-locked SQL==Python==expected across the ELIGIBLE_CASES matrix (READ-01 foundation).
- The ELIG-03 mutation check is demonstrated and recorded in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/82-counts-pending-set-cutover/82-01-SUMMARY.md` when done
</output>
