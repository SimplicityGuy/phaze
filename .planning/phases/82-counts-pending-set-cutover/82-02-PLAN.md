---
phase: 82-counts-pending-set-cutover
plan: 02
type: execute
wave: 2
depends_on: [82-01]
files_modified:
  - src/phaze/services/pipeline.py
  - tests/integration/test_enrich_pending_independence.py
  - tests/integration/test_pending_set_divergence.py
  - tests/shared/test_pending_set_source_scan.py
  - tests/shared/services/test_pipeline.py
autonomous: true
requirements: [READ-01]
must_haves:
  truths:
    - "A single file completes all three enrich stages in ANY order — each enrich pending set is independent, with no upstream and no FileRecord.state read"
    - "A cloud-dispatched analyze file (cloud_job status awaiting/pushing/pushed) is ABSENT from the analyze pending set — no local+cloud double-dispatch"
    - "A dedup-resolved file is absent from all three enrich pending sets"
    - "The three pending helpers contain zero FileState READ occurrences of DISCOVERED/METADATA_EXTRACTED/FINGERPRINTED"
    - "The pre-existing tests/shared/services/test_pipeline.py stays green: the stale state-gated pending-set assertions are removed (their derived successors live in the new integration files), no contradictory coverage remains"
  artifacts:
    - path: "src/phaze/services/pipeline.py"
      provides: "get_metadata_pending_files / get_fingerprint_pending_files / get_discovered_files_with_duration cut over to the derived layer"
      contains: "eligible_clause"
    - path: "tests/integration/test_enrich_pending_independence.py"
      provides: "SC#1 all-orderings independence test + the A1 cloud-exclusion regression"
      contains: "def test_"
    - path: "tests/shared/test_pending_set_source_scan.py"
      provides: "mutation-tested AST source scan — no FileState READ in the 3 pending helpers"
      contains: "FileState"
    - path: "tests/integration/test_pending_set_divergence.py"
      provides: "behavioral state/derived-disagreement guard (derived reader wins)"
      contains: "MUTATION"
    - path: "tests/shared/services/test_pipeline.py"
      provides: "pre-existing unit tests reconciled to derived pending-set semantics (stale state-gated assertions deleted)"
      contains: "get_metadata_pending_files"
  key_links:
    - from: "the three pending helpers"
      to: "eligible_clause / dedup_resolved_clause"
      via: "eligible_clause(stage) ∧ ~dedup_resolved_clause() ∧ file_type IN MUSIC_VIDEO_TYPES"
      pattern: "eligible_clause\\(Stage\\.(METADATA|FINGERPRINT|ANALYZE)\\)"
    - from: "get_discovered_files_with_duration"
      to: "cloud_job active-status exclusion"
      via: "ledger-survival trace OR explicit ~exists(active cloud_job) conjunct"
      pattern: "cloud_job|CloudJob"
---

<objective>
Cut the three enrich pending sets over from `FileRecord.state` to the derived layer, so metadata / fingerprint / analyze each surface every not-done, not-in-flight file independent of the others — the cross-stage deadlock dissolves (READ-01). Resolve the A1 double-dispatch landmine explicitly (a cloud-dispatched analyze file must never re-enter the local analyze set). Lock the cutover with a mutation-tested AST source guard, a behavioral divergence guard, and the SC#1 all-orderings test. Reconcile the pre-existing `tests/shared/services/test_pipeline.py` unit tests so the suite stays green under the new derived semantics.

Purpose: This is the milestone's thesis made executable. All three helpers are the single shared "pending" definition consumed by BOTH the manual trigger endpoints AND the Phase-80 recovery producer (`reenqueue.py`), so narrowing the helper narrows manual/API/recovery in lockstep (Phase-42 anti-drift).

Output: three cut-over pending helpers + three new test files (all-orderings/A1, divergence, AST source scan) + the reconciled pre-existing `test_pipeline.py` unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-counts-pending-set-cutover/82-CONTEXT.md
@.planning/phases/82-counts-pending-set-cutover/82-RESEARCH.md
@.planning/phases/82-counts-pending-set-cutover/82-PATTERNS.md
@.planning/phases/82-counts-pending-set-cutover/82-01-SUMMARY.md

<interfaces>
<!-- Contracts extracted from main + Plan 01. Use directly. -->

From src/phaze/services/stage_status.py:
  eligible_clause(stage) -> ColumnElement[bool]     # NEW in Plan 01 — enrich-only, correlated ~exists on FileRecord.id
  dedup_resolved_clause() -> ColumnElement[bool]    # Phase 84 — file-level, correlated ~exists on FileRecord.id
  inflight_clause(stage) -> ColumnElement[bool]     # LEDGER-ONLY: scheduling_ledger "<fn>:<file_id>" key. Does NOT read cloud_job.

From src/phaze/services/pipeline.py (current bodies — the cutover targets):
  MUSIC_VIDEO_TYPES  (:45)
  get_discovered_files_with_duration(session)  (:1098)  # select(FileRecord, FileMetadata.duration).outerjoin(...).where(state==DISCOVERED); KEEP the LEFT JOIN
  get_metadata_pending_files(session)          (:1370)  # currently select(FileRecord).where(file_type.in_(MUSIC_VIDEO_TYPES)) — ALL music/video
  get_fingerprint_pending_files(session)       (:1403)  # currently get_files_by_state(METADATA_EXTRACTED) UNION failed-retry + manual de-dup loop
  get_metadata_failed_files(session)           (:1384)  # SEPARATE operator-retry set — DO NOT touch

From src/phaze/enums/stage.py: Stage.METADATA / Stage.FINGERPRINT / Stage.ANALYZE

Pre-existing tests exercising the cutover targets (tests/shared/services/test_pipeline.py — MUST be reconciled here, not left RED):
  Module-level import block (:21-51) imports the pending helpers by name.
  test_get_metadata_pending_files_returns_only_music_video (:843)                                  # STAYS GREEN under derived semantics — KEEP untouched
  test_get_fingerprint_pending_files_unions_metadata_extracted_and_failed_retry (:857)             # stale union/state semantics — DELETE (Task 3)
  test_get_fingerprint_pending_files_dedups_metadata_extracted_with_failed_result (:886)           # guards a UNION that no longer exists — DELETE (Task 3)
  test_get_discovered_files_with_duration_joins_duration (:1449)                                    # DISCOVERED file, no analysis row → still returned — KEEP untouched
  test_get_discovered_files_with_duration_outerjoin_null (:1466)                                    # KEEP untouched (LEFT JOIN still returns the file)
  test_get_discovered_files_with_duration_excludes_other_states (:1481)                             # asserts an ANALYZED-state file (no AnalysisResult row) is excluded — RED under derived (NOT_STARTED → eligible) — DELETE (Task 2)
  # NOTE: the get_pipeline_stats import + its three tests are Plan 03's concern (that plan deletes the function).

A1 landmine facts (CONTEXT D-00c CORRECTED):
  inflight_clause(ANALYZE) reads ONLY scheduling_ledger key "process_file:<file_id>" (STAGE_TO_FUNCTION["analyze"]=="process_file").
  tasks/reconcile_cloud_jobs.py:122-123 states the cloud re-drive "Writes NO scheduling-ledger row ... never a process_file ledger seed."
  services/backends.py:113 — hold_awaiting_cloud spill-mode is a no-op: "no FileRecord write, no cleanup, no ledger clear."
  So a cloud file is auto-excluded from the analyze set ONLY IF its original local process_file:<id> ledger row survives the cloud hand-off.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the three cutover guard tests (RED-first)</name>
  <files>tests/integration/test_enrich_pending_independence.py, tests/integration/test_pending_set_divergence.py, tests/shared/test_pending_set_source_scan.py</files>
  <read_first>
    - tests/integration/test_dedup_divergence.py — copy the db_session fixture + _TARGET_DB endswith("_test") destructive guard + _file(session, *, sha256, state) seed helper + the per-reader MUTATION-comment discipline verbatim.
    - tests/integration/test_stage_status_equivalence.py — reuse the seed fns for writing OUTPUT rows (metadata / fingerprint-success / analysis-with-completed_at) instead of mutating state.
    - tests/shared/test_dedup_fingerprint_source_scan.py — copy the AST helper battery verbatim: _filestate_occurrences (docstring-blind attribute-chain match), _in_compare, _in_where_arg (walks Call.args positionally AND Call.keywords incl. keyword.arg is None splat), _classify.
    - .planning/phases/82-counts-pending-set-cutover/82-RESEARCH.md — OQ3 (all-orderings, lines 302-312), OQ6 (both guards, lines 360-370), Pitfall 2 (A1, lines 391-393).
    - .planning/phases/82-counts-pending-set-cutover/82-PATTERNS.md — the three NEW-test sections + the cloud-exclusion cell note.
  </read_first>
  <behavior>
    - test_enrich_pending_independence.py: for each of the 6 permutations of {metadata, fingerprint, analyze}, drive one music/video file through the stages by writing the stage OUTPUT row (never mutating state to advance); after each partial: the completed stage's pending set EXCLUDES the file, the not-yet-done stages STILL INCLUDE it; after all three, all three exclude it.
    - Deadlock-detection cell (RED pre-cutover): write a metadata row AND set state='metadata_extracted' (dual-write reality), then assert the file is STILL in the analyze pending set (get_discovered_files_with_duration) and STILL in the fingerprint pending set. RED against current state-gated code; GREEN post-cutover.
    - A1 cloud-exclusion cell (mandatory): seed a file with cloud_job(status='pushing') (and a variant with 'pushed' and 'awaiting'), no analysis row, and assert it is ABSENT from the analyze pending set.
    - dedup-exclusion cells: a dedup-resolved file is ABSENT from all three pending sets.
    - test_pending_set_divergence.py: File A metadata-row present but state='discovered' → EXCLUDED from metadata set; File B no fingerprint row but state='analyzed' → INCLUDED in fingerprint set; File C no analysis row but state='fingerprinted' → INCLUDED in analyze set. Each with a MUTATION: comment naming the state-based revert that inverts it.
    - test_pending_set_source_scan.py: ZERO FileState READ occurrences of DISCOVERED/METADATA_EXTRACTED/FINGERPRINTED inside the three pending-helper function bodies; crafted-string mutations (positional .where read, keyword .filter_by read, **splat read) each flagged RED; a docstring mention stays GREEN.
  </behavior>
  <action>
    Create `tests/integration/test_enrich_pending_independence.py` (SC#1 all-orderings + A1 cloud-exclusion + dedup-exclusion), `tests/integration/test_pending_set_divergence.py` (behavioral derived-wins guard, all three sets), and `tests/shared/test_pending_set_source_scan.py` (AST source scan scoped to the three pending-helper function bodies in `services/pipeline.py`). Reuse the real-PG harness (`psycopg` connectivity-probe → `pytest.skip`, `Base.metadata.create_all`, legacy-agent FK seed, rollback teardown) and the AST helper battery from the analogs verbatim. Write output rows to advance stages; set `state` to whatever the real writer would stamp (dual-write reality) so each assertion proves the derived reader IGNORES state. Encode the crafted-string mutation cases as permanent tests (mutate STRINGS, not files). Because the pending helpers are pre-cutover here, several cells are expected RED now and turn GREEN after Task 2/Task 3 — this is the intended TDD RED state.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_enrich_pending_independence.py tests/integration/test_pending_set_divergence.py tests/shared/test_pending_set_source_scan.py -q --co</automated>
  </verify>
  <acceptance_criteria>
    - All three test files collect cleanly (`--co` green).
    - Running them now: the deadlock/independence/divergence cells and the source-scan are RED against pre-cutover source (proving the tests drive the cutover); record which cells are RED in the task notes.
    - The A1 cell exists for all three active cloud states (awaiting, pushing, pushed).
    - The source-scan mutation cases (positional, keyword, `**` splat, docstring-GREEN) are all present, mirroring the Phase-84 analog helper names.
  </acceptance_criteria>
  <done>Three guard/behavior test files exist and collect; the RED cells that Task 2/3 must turn green are enumerated.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the A1 landmine + cut over the analyze pending set</name>
  <files>src/phaze/services/pipeline.py, tests/shared/services/test_pipeline.py</files>
  <read_first>
    - src/phaze/services/pipeline.py:1098 (get_discovered_files_with_duration — KEEP the outerjoin FileMetadata) and :1259-1320 (the ledger-scoped candidate + before_enqueue ledger-commit discipline notes).
    - src/phaze/tasks/reconcile_cloud_jobs.py:110-130 (the "writes NO scheduling-ledger row" cloud re-drive).
    - src/phaze/services/backends.py:100-130 (hold_awaiting_cloud spill-mode no-op — no ledger clear) and the Phase-83 hold-path dispatch (services/cloud_staging.py, routers/agent_push.py `/pushed`).
    - src/phaze/services/stage_status.py:176-193 (inflight_clause — ledger-only) + :231-262 (awaiting_candidate_clause cloud-join note).
    - tests/shared/services/test_pipeline.py:1481 (test_get_discovered_files_with_duration_excludes_other_states — the stale state-gated assertion to delete) and :1449-1477 (the two sibling discovered-files tests that STAY green).
    - .planning/phases/83-cloud-routing-sidecar-cutover/83-CONTEXT.md — the hold/dispatch writer path and cloud_job status lifecycle.
    - .planning/phases/82-counts-pending-set-cutover/82-CONTEXT.md — D-00c (corrected) and D-03; .planning/phases/82-counts-pending-set-cutover/82-RESEARCH.md OQ2c + Pitfall 1/2.
  </read_first>
  <action>
    First, TRACE the local `process_file:<file_id>` scheduling-ledger row lifecycle across the Phase-83 cloud hand-off: does the row survive from local enqueue until cloud analysis reports terminal (done/failed), or is it cleared when the file is routed to cloud (AWAITING_CLOUD/PUSHING/PUSHED)? Follow the enqueue chokepoint (before_enqueue ledger commit), the cloud routing branch (`stage_cloud_window` / `hold_awaiting_cloud` / dispatch), and every ledger clear site (`clear_ledger_entry`, the after_process completion hook). Record the finding in the SUMMARY. Then cut over `get_discovered_files_with_duration`: KEEP the `.outerjoin(FileMetadata, FileMetadata.file_id == FileRecord.id)` (the cloud duration-router reads `FileMetadata.duration`); replace `.where(FileRecord.state == FileState.DISCOVERED)` with `.where(FileRecord.file_type.in_(MUSIC_VIDEO_TYPES), eligible_clause(Stage.ANALYZE), ~dedup_resolved_clause())`. The `file_type` scope is NEWLY required (the old state-gated query was file-type-agnostic — Pitfall 1). If the trace does NOT prove the local ledger row survives until cloud-terminal, ALSO add an explicit active-cloud-job exclusion conjunct composed at THIS query level (like `~dedup_resolved_clause()`, NOT inside `eligible_clause`): a correlated `~exists(select(CloudJob.id).where(CloudJob.file_id == FileRecord.id, CloudJob.status.in_(('awaiting','pushing','pushed'))))`. Default to adding the conjunct unless survival is conclusively proven — it is provably safe (an active cloud file is never a legitimate local-analyze candidate; a terminal cloud file is already excluded by ~done/~failed). Use the `CloudJobStatus` enum values, not string literals, if the codebase exposes them. THEN reconcile the pre-existing unit test: DELETE `test_get_discovered_files_with_duration_excludes_other_states` from `tests/shared/services/test_pipeline.py` (it asserts an ANALYZED-state file with no AnalysisResult row is excluded — under derived ANALYZE eligibility that file is NOT_STARTED and is now legitimately included; its derived successor is the divergence/independence coverage in the new integration files, so keeping it would be contradictory). Leave `test_get_discovered_files_with_duration_joins_duration` and `_outerjoin_null` UNTOUCHED (both stay green — the DISCOVERED file has no analysis row, so it remains eligible and returned).
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_enrich_pending_independence.py tests/shared/services/test_pipeline.py -k "discovered_files_with_duration or pending" -q</automated>
  </verify>
  <acceptance_criteria>
    - The A1 cloud-exclusion cell (cloud_job status in awaiting/pushing/pushed → ABSENT from the analyze pending set) is GREEN for all three statuses.
    - A non-music discovered file is ABSENT from the analyze set (the new file_type scope — Pitfall 1 regression).
    - The deadlock-detection cell (metadata-done + state='metadata_extracted' → STILL in the analyze set) is GREEN.
    - `test_get_discovered_files_with_duration_excludes_other_states` is removed; `_joins_duration` and `_outerjoin_null` still pass unchanged (`uv run pytest tests/shared/services/test_pipeline.py -k discovered_files_with_duration -q` green with two tests).
    - The ledger-survival trace finding is written to the SUMMARY, and the SUMMARY states explicitly whether the explicit cloud conjunct was added or the survival guarantee relied on (with the evidence).
    - `get_discovered_files_with_duration` retains its LEFT OUTER JOIN on FileMetadata (duration still returned).
    - `uv run mypy src/phaze/services/pipeline.py` passes.
  </acceptance_criteria>
  <done>The analyze pending set is derived, file-type-scoped, and provably excludes cloud-in-flight files; the stale exclude-other-states unit test is removed; the A1 trace is recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Cut over the metadata + fingerprint pending sets; align manual/recovery; verify guards</name>
  <files>src/phaze/services/pipeline.py, tests/shared/services/test_pipeline.py</files>
  <read_first>
    - src/phaze/services/pipeline.py:1370 (get_metadata_pending_files) and :1403-1435 (get_fingerprint_pending_files — the UNION + manual de-dup-by-id loop that collapses) and :1359-1367 (the "ONE definition of pending per stage" shared-helper note).
    - src/phaze/routers/pipeline.py — trigger_metadata_extraction (:1236), trigger_extraction_ui (:1262), trigger_fingerprint (:1315), trigger_fingerprint_ui (:1354) — confirm they route through the shared helpers (no per-endpoint edit needed).
    - tests/shared/test_dedup_fingerprint_source_scan.py — the mutation-test discipline to reproduce.
    - tests/shared/services/test_pipeline.py:857 (test_get_fingerprint_pending_files_unions_metadata_extracted_and_failed_retry) and :886 (test_get_fingerprint_pending_files_dedups_metadata_extracted_with_failed_result — the two stale union/dedup tests to delete) and :843 (test_get_metadata_pending_files_returns_only_music_video — STAYS green, keep).
    - .planning/phases/82-counts-pending-set-cutover/82-RESEARCH.md — OQ2a/b (lines 252-276), OQ2d (manual alignment, 296-300).
  </read_first>
  <action>
    Cut over `get_metadata_pending_files` to `select(FileRecord).where(FileRecord.file_type.in_(MUSIC_VIDEO_TYPES), eligible_clause(Stage.METADATA), ~dedup_resolved_clause())`. Cut over `get_fingerprint_pending_files` to the same single-WHERE shape with `Stage.FINGERPRINT`, COLLAPSING the `get_files_by_state(METADATA_EXTRACTED)` UNION and the manual de-dup-by-id loop (eligible_clause(FINGERPRINT) = `~inflight ∧ ~done` already subsumes the failed-retry set, since `ELIGIBLE_AFTER_FAILURE[FINGERPRINT]=True` drops the `~failed` conjunct). Add the `eligible_clause` / `dedup_resolved_clause` / `Stage` imports to `pipeline.py`. Do NOT touch `get_metadata_failed_files` (the separate operator-retry set). Do NOT add per-endpoint filters — the shared helpers narrow manual + API + recovery in lockstep (Phase-42). THEN reconcile the pre-existing unit tests in `tests/shared/services/test_pipeline.py`: DELETE `test_get_fingerprint_pending_files_unions_metadata_extracted_and_failed_retry` (:857) and `test_get_fingerprint_pending_files_dedups_metadata_extracted_with_failed_result` (:886) — both encode the now-collapsed UNION/dedup-loop semantics (the first also asserts a plain DISCOVERED file and a failed-only FINGERPRINTED file are excluded, both RED under derived FINGERPRINT eligibility; the second guards a duplicate-row hazard the single WHERE cannot produce). Their derived successors are the `[failed]`-only-included and independence assertions in the new integration files, so keeping them would be contradictory coverage. Leave `test_get_metadata_pending_files_returns_only_music_video` (:843) UNTOUCHED (a music/video file with no metadata row is NOT_STARTED → eligible → still returned; a non-music file is still excluded by file_type — stays green). Then run the mutation checks on the AST source-scan guard and the divergence guard: break each pending helper's predicate back to a `FileRecord.state` filter, watch the corresponding guard/divergence test go RED, restore, watch GREEN — record in the SUMMARY.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_enrich_pending_independence.py tests/integration/test_pending_set_divergence.py tests/shared/test_pending_set_source_scan.py tests/shared/services/test_pipeline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All three new test files GREEN (independence, divergence, source-scan) AND the full `tests/shared/services/test_pipeline.py` file GREEN (collects + passes) — the stale union/dedup pending tests are gone, `returns_only_music_video` still passes.
    - `get_fingerprint_pending_files` is a single `.where(...)` — the UNION and the de-dup-by-id loop are gone (`grep -c "get_files_by_state" ` within the function body is 0).
    - A `[failed]`-only fingerprint file is returned by the new fingerprint query (the collapse proof) — asserted in the divergence or independence test.
    - MUTATION CHECK recorded: reverting each of the three helpers to a `FileRecord.state` filter turns the AST source-scan RED (state read reintroduced) AND inverts the matching divergence assertion; restore → GREEN. Also confirm the guard does NOT false-positive on retained readers like `FileRecord.file_type.in_(...)`.
    - Manual/recovery alignment: a test (or reasoned assertion in SUMMARY) confirms the manual trigger endpoints and `reenqueue.py` enqueue the SAME set (Phase-42 precedent) because they share the helpers.
    - `just shadow-compare` stays green (no new hard divergence, D-00e). `uv run ruff check` + `uv run mypy` clean on pipeline.py.
  </acceptance_criteria>
  <done>All three enrich pending sets are derived, the fingerprint UNION collapsed, both guards mutation-proven, manual/recovery aligned, the pre-existing test_pipeline.py reconciled + green, shadow gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recovery/manual trigger → SAQ enqueue | The pending helpers define what gets enqueued for analysis/metadata/fingerprint; an over-broad set drives cost/over-enqueue. |
| cloud dispatch ↔ local analyze pending set | A file dispatched to a compute agent must not simultaneously be a local analyze candidate (double-dispatch). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-A1 | Denial of Service (double-dispatch / cost) | get_discovered_files_with_duration vs cloud_job | mitigate | Prove local ledger survival OR add explicit `~exists(cloud_job status in awaiting/pushing/pushed)` conjunct; mandatory PUSHING/PUSHED-absent regression (Task 2). |
| T-82-OE | Denial of Service (44.5K over-enqueue class) | analyze eligibility | mitigate | `eligible_clause(ANALYZE)` keeps a failed analyze terminal (ELIG-03); analyze set never auto-re-enqueues a failed row. |
| T-82-03 | Tampering | pending-set SQL | mitigate | Pure `ColumnElement` composition + bound params; no f-string SQL (T-42-03). AST source scan forbids reintroducing a `FileState` read. |
| T-82-DRIFT | Repudiation (silent reader drift) | 3 pending helpers | mitigate | Mutation-tested AST source scan + behavioral divergence guard; a green guard is re-proven RED-on-break-restore. |
| T-82-DDL | Tampering | schema | accept | No DDL / no migration in this plan (pure reader cutover, D-02). |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_enrich_pending_independence.py tests/integration/test_pending_set_divergence.py tests/shared/test_pending_set_source_scan.py -q` green.
- `uv run pytest tests/shared/services/test_pipeline.py -q` green (the pre-existing file collects + passes after the stale state-gated pending tests are deleted — no ImportError, no RED assertion).
- `just test-bucket integration` (or `just integration-test`) green after the wave.
- `just shadow-compare` green (D-00e — no new hard divergence).
- D-02 note carried to Plan 04 VERIFICATION: the analyze pending-set flip is trusted only once the deploy target is at Alembic ≥036 AND `COUNT(files WHERE state='analyzed' AND analysis_completed_at IS NULL AND failed_at IS NULL) = 0` (live/shadow gate; prod currently ~031 — deployment-gated).
</verification>

<success_criteria>
- All three enrich pending sets derived from `stage_status` (eligible_clause ∧ ~dedup_resolved_clause ∧ file_type), independent, no `FileRecord.state` read (READ-01).
- A cloud-in-flight analyze file is provably excluded from the analyze set (A1 resolved, regression green).
- Both anti-drift guards are mutation-proven; manual/API/recovery stay aligned via the shared helpers.
- The pre-existing `tests/shared/services/test_pipeline.py` is reconciled to the derived semantics and green (no stale/contradictory pending-set coverage).
</success_criteria>

<output>
Create `.planning/phases/82-counts-pending-set-cutover/82-02-SUMMARY.md` when done. Note the semantic shift (per-stage pending counts jump as the deadlock dissolves — this is the fix, not a regression), the A1 trace finding, and which pre-existing test_pipeline.py tests were deleted vs kept.
</output>
</content>
