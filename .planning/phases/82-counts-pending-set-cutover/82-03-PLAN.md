---
phase: 82-counts-pending-set-cutover
plan: 03
type: execute
wave: 3
depends_on: [82-02]
files_modified:
  - src/phaze/services/pipeline.py
  - src/phaze/routers/pipeline.py
  - src/phaze/templates/pipeline/partials/stats_bar.html
  - tests/integration/test_stage_progress_buckets.py
  - tests/shared/routers/test_pipeline_stats.py
autonomous: true
requirements: [READ-02]
must_haves:
  truths:
    - "get_stage_progress reports four buckets (not_started/in_flight/done/failed) for each enrich stage, summing to total, with a visible failed count"
    - "get_pipeline_stats is removed — no GROUP BY FileRecord.state survives in the stats path"
    - "The three former get_pipeline_stats callers + stats_bar.html read derived counts from get_stage_progress"
  artifacts:
    - path: "src/phaze/services/pipeline.py"
      provides: "_safe_bucket_counts + four-bucket enrich nodes in get_stage_progress; get_pipeline_stats deleted"
      contains: "_safe_bucket_counts"
    - path: "src/phaze/routers/pipeline.py"
      provides: "notYetEnriched / build_dashboard_context / pipeline_stats_partial re-expressed off stage_progress"
      contains: "stage_progress"
    - path: "src/phaze/templates/pipeline/partials/stats_bar.html"
      provides: "key remap off the derived stats dict; Alpine $store.pipeline.* keys stable"
    - path: "tests/integration/test_stage_progress_buckets.py"
      provides: "four-bucket-sums-to-total invariant + visible failed count"
      contains: "def test_"
  key_links:
    - from: "get_stage_progress enrich nodes"
      to: "stage_status_case(stage)"
      via: "one GROUP BY stage_status_case per enrich stage (degrade-safe)"
      pattern: "stage_status_case"
    - from: "routers/pipeline.py callers"
      to: "get_stage_progress"
      via: "derived re-expression of the seven former stats keys"
      pattern: "stage_progress\\["
---

<objective>
Extend `get_stage_progress`'s three enrich nodes from `{done, total}` to the four-bucket `{not_started, in_flight, done, failed, total}` via one degrade-safe `GROUP BY stage_status_case(stage)` per enrich stage, and remove `get_pipeline_stats`'s linear `GROUP BY FileRecord.state` entirely — re-expressing its seven consumed keys and its three router callers + the `stats_bar.html` partial off the derived counts (READ-02, D-04, D-05).

Purpose: the DAG surfaces a visible failed count per enrich stage and the stats path no longer reads `FileRecord.state`. This front-runs nothing in Phase 86 (it changes count derivation, not proposals-status authority).

Output: `_safe_bucket_counts` + four-bucket `get_stage_progress`, `get_pipeline_stats` deleted, three callers + template migrated, sums-to-total test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-counts-pending-set-cutover/82-CONTEXT.md
@.planning/phases/82-counts-pending-set-cutover/82-RESEARCH.md
@.planning/phases/82-counts-pending-set-cutover/82-PATTERNS.md
@.planning/phases/82-counts-pending-set-cutover/82-02-SUMMARY.md

<interfaces>
<!-- Contracts extracted from main. Use directly. -->

From src/phaze/services/stage_status.py:
  stage_status_case(stage) -> ColumnElement[str]   # LOCKED 4-way CASE (in_flight ≻ done ≻ failed ≻ not_started); reuse, do NOT author a fresh CASE
From src/phaze/enums/stage.py: Status  (values: not_started/in_flight/done/failed)

From src/phaze/services/pipeline.py:
  get_pipeline_stats(session)  (:61)   # REMOVE — body: select(FileRecord.state, func.count(FileRecord.id)).group_by(FileRecord.state)
  get_stage_progress(session)  (:302)  # EXTEND — already derives every DAG node from output tables via _safe_count; music_video_total computed at ~:333; enrich nodes return {done, total} at ~:373-388
  _safe_count(...)             (:282)  # degrade discipline: try → log → guarded rollback → return 0
  queue_progress_percent(done, busy)  (:264)  # signature unchanged; only first-arg source changes
  MUSIC_VIDEO_TYPES  (:45)

From src/phaze/routers/pipeline.py (the three get_pipeline_stats callers — the ONLY importers):
  _build_dag_context       (:240)  # notYetEnriched = max(stats["discovered"] - stats["metadata_extracted"], 0)
  build_dashboard_context  (:485)  # feeds stats_bar.html + queue_progress_percent(stats["analyzed"], ...) at :506
  pipeline_stats_partial   (:629)  # the /pipeline/stats HTMX poll; queue_progress_percent(stats["analyzed"], ...) at :636
  (import at :46)

Seven former keys → derived re-expression (all from get_stage_progress):
  discovered→discovery.done · metadata_extracted→metadata.done · fingerprinted→fingerprint.done ·
  analyzed→analyze.done · proposal_generated→proposals.done · approved→execute.total · executed→execute.done
  notYetEnriched = metadata.total - metadata.done

From src/phaze/templates/pipeline/partials/stats_bar.html:
  Six visible cards read stats.<key> at lines 3,7,11,15,19,23.
  Three OOB x-init store writes at lines 47-49 push into $store.pipeline.discovered / .metadataExtracted / .analyzed — KEEP these Alpine keys stable.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Four-bucket-sums-to-total + stats-removal tests (RED-first)</name>
  <read_first>
    - tests/integration/test_stage_status_equivalence.py — reuse db_session + seed fns to build a consistent mixed corpus.
    - tests/shared/routers/ — the existing router test layout + fixtures for exercising _build_dag_context / build_dashboard_context / pipeline_stats_partial (grep for existing get_pipeline_stats / get_stage_progress tests to extend rather than duplicate).
    - .planning/phases/82-counts-pending-set-cutover/82-RESEARCH.md — Pattern 3 (four-bucket, lines 201-223), OQ4 (consumer inventory + key table, lines 314-338), Pitfall 3 (degrade sum invariant) + Pitfall 4 (OOB store keys).
  </read_first>
  <behavior>
    - test_stage_progress_buckets.py: on a healthy mixed music/video corpus, for each enrich stage (metadata/fingerprint/analyze) get_stage_progress returns keys {not_started,in_flight,done,failed,total} and not_started+in_flight+done+failed == total; a stage with ≥1 failed row reports failed ≥ 1 (visible failed count).
    - Degrade edge: assert the sum invariant ONLY on a healthy query (document the fail-safe-to-zero degrade; never a runtime assertion in the poll path).
    - test_pipeline_stats: the three callers render with the derived counts; notYetEnriched == metadata.total - metadata.done; queue_progress_percent is fed stage_progress["analyze"]["done"]; the /pipeline/stats poll partial still emits the three OOB store ids (discovered/metadataExtracted/analyzed).
    - A guard asserting get_pipeline_stats no longer exists / no GROUP BY FileRecord.state remains in the stats path.
  </behavior>
  <action>
    Create `tests/integration/test_stage_progress_buckets.py` for the four-bucket-sums-to-total invariant + visible-failed-count, reusing the real-PG harness and seed fns. Extend (or create) `tests/shared/routers/test_pipeline_stats.py` to assert the three callers re-express the seven keys off `get_stage_progress`, that `notYetEnriched` equals `metadata.total - metadata.done`, that the poll partial still emits the three OOB store ids, and that no `GROUP BY FileRecord.state` remains in the stats path (assert `get_pipeline_stats` is removed — e.g. `not hasattr(pipeline_module, "get_pipeline_stats")`). These fail RED against current code (four-bucket keys absent, get_pipeline_stats still present).
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_stage_progress_buckets.py tests/shared/routers/test_pipeline_stats.py -q --co</automated>
  </verify>
  <acceptance_criteria>
    - Both test files collect cleanly.
    - Running now: the four-bucket keys assertion and the get_pipeline_stats-removed assertion are RED (drive the implementation).
    - The sum-invariant test is scoped to a healthy corpus only (Pitfall 3).
    - The OOB-store-id assertion names the three stable Alpine keys (Pitfall 4).
  </acceptance_criteria>
  <done>Four-bucket + stats-removal tests exist, collect, and are RED pending Task 2/3.</done>
</task>

<task type="auto">
  <name>Task 2: _safe_bucket_counts + four-bucket enrich nodes; remove get_pipeline_stats</name>
  <read_first>
    - src/phaze/services/pipeline.py:282-299 (_safe_count degrade discipline to copy verbatim), :302-413 (get_stage_progress body + music_video_total at ~:333 + enrich node returns ~:373-388), :61-71 (get_pipeline_stats to delete).
    - src/phaze/services/stage_status.py:265-284 (stage_status_case — reuse, do not re-author).
    - .planning/phases/82-counts-pending-set-cutover/82-PATTERNS.md — the get_stage_progress four-bucket + get_pipeline_stats removal sections.
  </read_first>
  <action>
    Add `_safe_bucket_counts(session, stage) -> dict[str, int]` to `pipeline.py` mirroring the `_safe_count` degrade discipline: zero-fill `{s.value: 0 for s in Status}`, run `select(stage_status_case(stage).label(...), func.count(FileRecord.id)).where(FileRecord.file_type.in_(MUSIC_VIDEO_TYPES)).group_by(stage_status_case(stage))`, populate the dict from the rows, and on any exception log a warning, guarded-rollback, and return the all-zero dict (never 500 the poll). Reuse `stage_status_case` — do NOT author a fresh CASE (D-04). In `get_stage_progress`, change the three enrich nodes (metadata/fingerprint/analyze) to return the flat shape `{not_started, in_flight, done, failed, total}` where the four buckets come from `_safe_bucket_counts` and `total` stays `music_video_total`. Leave downstream nodes (scan_search/scrape/match/proposals/execute) at `{done, total}` untouched. Delete `get_pipeline_stats` in full (D-05, SC#2 — no `state`-keyed GROUP BY survives).
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_stage_progress_buckets.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The four-bucket-sums-to-total test is GREEN for all three enrich stages; a failed row yields failed ≥ 1.
    - `grep -n "def get_pipeline_stats" src/phaze/services/pipeline.py` returns nothing (removed).
    - `grep -n "group_by(FileRecord.state)" src/phaze/services/pipeline.py` returns nothing.
    - `_safe_bucket_counts` uses `stage_status_case` (not a fresh CASE) — `grep -c "case(" ` inside the new function body is 0.
    - Degrade path preserved: a forced query error returns all-zero and rolls back (mirror the `_safe_count` test if one exists).
    - `uv run mypy src/phaze/services/pipeline.py` passes.
  </acceptance_criteria>
  <done>get_stage_progress enrich nodes are four-bucket, degrade-safe; get_pipeline_stats is gone.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the three callers + stats_bar.html key remap</name>
  <read_first>
    - src/phaze/routers/pipeline.py:46 (import), :240-241 (_build_dag_context/notYetEnriched), :485-506 (build_dashboard_context), :629-636 (pipeline_stats_partial).
    - src/phaze/templates/pipeline/partials/stats_bar.html — the six cards (lines 3,7,11,15,19,23) + three OOB x-init writes (47-49).
    - .planning/phases/82-counts-pending-set-cutover/82-RESEARCH.md — the seven-key table (325-334) + stats_bar remap guidance (336) + Pitfall 4.
  </read_first>
  <action>
    In `routers/pipeline.py`: remove the `get_pipeline_stats` import; in `_build_dag_context` set `notYetEnriched = max(stage_progress["metadata"]["total"] - stage_progress["metadata"]["done"], 0)`; in `build_dashboard_context` and `pipeline_stats_partial` feed `queue_progress_percent` its first arg from `stage_progress["analyze"]["done"]`. Build a small derived `stats` dict (the seven keys re-expressed per the interface table) in the two context builders so `stats_bar.html` needs only a mechanical value-source remap. In `stats_bar.html`: point the six visible cards and the three OOB `x-init` writes at the derived dict; KEEP the Alpine `$store.pipeline.discovered / .metadataExtracted / .analyzed` store keys STABLE (only the server-side source changes) — they drive the DAG canvas bindings + button `:disabled` gating. Leave the `dag.items()` OOB loop and per-card includes untouched.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/routers/test_pipeline_stats.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All three callers render with derived counts; no reference to `get_pipeline_stats` remains (`grep -rn "get_pipeline_stats" src/phaze/` returns nothing).
    - `notYetEnriched` equals `metadata.total - metadata.done`.
    - `stats_bar.html` still emits exactly the three OOB store ids writing into `$store.pipeline.discovered/.metadataExtracted/.analyzed` (Pitfall 4) — asserted by the poll-partial test.
    - The stats-bar renders the six card values from the derived dict (no `stats.<filestate>` key referencing a removed FileState-derived source).
    - Full router test suite green; `uv run ruff check` clean; `just shadow-compare` still green.
  </acceptance_criteria>
  <done>The three callers + template read derived counts; the OOB store contract is intact; no get_pipeline_stats reference remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 5s /pipeline/stats poll → DB | Hot poll fans out many counting reads; a query that errors or scans the whole corpus degrades the operator console. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-04 | Denial of Service | _safe_bucket_counts / get_stage_progress on the 5s poll | mitigate | Every read wrapped in the `_safe_count` degrade discipline (log → guarded rollback → all-zero); never 500s the poll (INFLIGHT-02). |
| T-82-05 | Tampering | four-bucket GROUP BY SQL | mitigate | Reuses the LOCKED `stage_status_case` `ColumnElement`; no f-string SQL (T-42-03). |
| T-82-06 | Repudiation (silent count drift) | stats_bar OOB store keys | mitigate | Alpine store keys kept stable; poll-partial test asserts the three OOB ids still emit (Pitfall 4). |
| T-82-DDL | Tampering | schema | accept | No DDL / no migration (pure reader/count cutover). |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_stage_progress_buckets.py tests/shared/routers/test_pipeline_stats.py -q` green.
- `just test-bucket <pipeline/integration>` green after the wave.
- `grep -rn "get_pipeline_stats\|group_by(FileRecord.state)" src/phaze/` returns nothing.
- `just shadow-compare` green.
</verification>

<success_criteria>
- `get_pipeline_stats` removed; four-bucket per-enrich-stage counts sum to total with a visible failed count (READ-02).
- All three former callers + `stats_bar.html` read derived counts; the Alpine OOB store contract is preserved.
</success_criteria>

<output>
Create `.planning/phases/82-counts-pending-set-cutover/82-03-SUMMARY.md` when done. Flag the semantic shift: `metadata.done` counts every file with a metadata row (not the transient linear METADATA_EXTRACTED state) — these numbers legitimately differ, this is the deadlock dissolving.
</output>
