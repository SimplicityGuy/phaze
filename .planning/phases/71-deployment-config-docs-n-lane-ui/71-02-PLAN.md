---
phase: 71-deployment-config-docs-n-lane-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/models/route_control.py
  - src/phaze/models/__init__.py
  - alembic/versions/031_add_route_control.py
  - src/phaze/services/route_control.py
  - src/phaze/tasks/release_awaiting_cloud.py
  - src/phaze/routers/pipeline.py
  - tests/integration/test_migrations/test_migration_031_route_control.py
  - tests/shared/routers/test_routing.py
  - tests/analyze/core/test_staging_cron.py
autonomous: true
requirements: [BEUI-02]
must_haves:
  truths:
    - "D-09: a one-row route_control DB table (id PK default 'global', force_local bool default false) persists the force-local override, mirroring the pipeline_stage_control control-table pattern; migration 031 creates it and seeds the single default-false row"
    - "D-08: when force-local is engaged, the drain stage_cloud_window is a clean {staged:0,skipped:0} no-op AND the duration router routes new long files LOCAL (not held in AWAITING_CLOUD) — behaving exactly like the existing cloud_enabled=False all-local path; select_backend stays pure (gated at the two callers, not inside it)"
    - "get_route_control(session) degrades to False (cloud-enabled) on any DB error/absent row so the hot poll and the routing gate never 500"
    - "already-held AWAITING_CLOUD files stay held while forced (drain no-op) — documented behavior for the runbook (A4)"
  artifacts:
    - path: "src/phaze/models/route_control.py"
      provides: "RouteControl single-row control model"
      contains: "class RouteControl"
    - path: "alembic/versions/031_add_route_control.py"
      provides: "create_table route_control + seed 'global' row; down_revision 030"
      contains: "route_control"
    - path: "src/phaze/services/route_control.py"
      provides: "get_route_control degrade-safe reader"
      contains: "async def get_route_control"
  key_links:
    - from: "tasks/release_awaiting_cloud.py stage_cloud_window"
      to: "get_route_control"
      via: "early no-op read after session opens, before advisory lock"
      pattern: "get_route_control"
    - from: "routers/pipeline.py _route_discovered_by_duration callers"
      to: "get_route_control"
      via: "effective cloud_enabled = cloud_enabled AND NOT force_local"
      pattern: "get_route_control"
---

<objective>
Build the force-local mechanism: a persisted DB control row plus every place the routing path reads it.
Mirrors the shipped per-stage pause/priority control plane VERBATIM. This is the BEUI-02 routing-behavior
change — the DB row + the two gate reads. The control SURFACE (thin endpoint + header pill) is Plan 04.

Purpose: One runtime-mutable flag that, when engaged, makes all routing behave exactly like an all-local
registry (`cloud_enabled=False`) with no redeploy — incident-response revert.
Output: `RouteControl` model + migration 031 + `get_route_control` service + drain gate + duration-router gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-deployment-config-docs-n-lane-ui/71-CONTEXT.md
@.planning/phases/71-deployment-config-docs-n-lane-ui/71-RESEARCH.md
@.planning/phases/71-deployment-config-docs-n-lane-ui/71-PATTERNS.md

<interfaces>
<!-- Copy-from analogs. -->

From src/phaze/models/pipeline_stage_control.py (the model template):
```python
class PipelineStageControl(TimestampMixin, Base):
    __tablename__ = "pipeline_stage_control"
    stage: Mapped[str] = mapped_column(String(32), primary_key=True)
    paused: Mapped[bool] = mapped_column(Boolean, nullable=False, server_default=text("false"))
    priority: Mapped[int] = mapped_column(SmallInteger, nullable=False, server_default=text("50"))
```

From src/phaze/services/pipeline.py:416-446 (get_stage_controls — the degrade shape to mirror for get_route_control).

From src/phaze/tasks/release_awaiting_cloud.py:
```python
# :110-113  cloud_enabled early no-op (BEFORE session/advisory lock):
if not cfg.cloud_enabled:
    return {"staged": 0, "skipped": 0}
# :123      async with ctx["async_session"]() as session:
# :127      await session.execute(text("SELECT pg_advisory_xact_lock(:lock_key)"), ...)
```

From src/phaze/routers/pipeline.py:
```python
async def _route_discovered_by_duration(..., cloud_enabled: bool, ...)   # :278 ; is_long gate at :335
# 3 callers pass settings.cloud_enabled: :391, :698, :791 (trigger_analysis / trigger_analysis_ui / the shared helper)
# backend_selection.select_backend is PURE (backend_selection.py:80-119) — do NOT inject the flag there
```

Last migration on disk: alembic/versions/030_add_cloud_job_staging_bucket.py → down_revision = "030".
Migration test convention: tests/integration/test_migrations/test_migration_0NN_*.py (conftest.py:147 auto-marks integration).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RouteControl model + migration 031 + register</name>
  <files>src/phaze/models/route_control.py, src/phaze/models/__init__.py, alembic/versions/031_add_route_control.py, tests/integration/test_migrations/test_migration_031_route_control.py</files>
  <read_first>
    - src/phaze/models/pipeline_stage_control.py (verbatim model template — TimestampMixin, PK, Boolean server_default)
    - src/phaze/models/__init__.py:12,20-34 (where PipelineStageControl is imported + __all__)
    - alembic/versions/020_add_pipeline_stage_control.py (create_table + bound-param seed shape) and alembic/versions/030_add_cloud_job_staging_bucket.py (latest revision id / head)
    - tests/integration/test_migrations/test_migration_030_staging_bucket.py (migration-test convention to mirror)
    - 71-PATTERNS.md §models/route_control.py + §alembic/versions/031_add_route_control.py
  </read_first>
  <action>
    Create `src/phaze/models/route_control.py` with `class RouteControl(TimestampMixin, Base)`, `__tablename__ = "route_control"`, `id: Mapped[str] = mapped_column(String(32), primary_key=True)` (single-row, value `'global'`), `force_local: Mapped[bool] = mapped_column(Boolean, nullable=False, server_default=text("false"))`. Do NOT redeclare created_at/updated_at (TimestampMixin owns them). Register it in `src/phaze/models/__init__.py` (import + add `"RouteControl"` to `__all__`). Create `alembic/versions/031_add_route_control.py` with `down_revision = "030"`: `op.create_table("route_control", ...)` with the four columns + `PrimaryKeyConstraint("id", name=op.f("pk_route_control"))`, then seed ONE row via `bind.execute(sa.text("INSERT INTO route_control (id, force_local, created_at, updated_at) VALUES (:id, false, NOW(), NOW())"), {"id": "global"})` (bound params, no interpolation — T-37-01); `downgrade()` drops the table. Add `tests/integration/test_migrations/test_migration_031_route_control.py` asserting upgrade creates the table with the seeded `'global'` row (force_local false) and downgrade drops it.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_migrations/test_migration_031_route_control.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class RouteControl" src/phaze/models/route_control.py` == 1 and `"RouteControl"` appears in `src/phaze/models/__init__.py` `__all__`
    - `grep -c 'down_revision = "030"' alembic/versions/031_add_route_control.py` == 1
    - migration test asserts table exists + single seeded `id='global'` row with `force_local=false` after upgrade, table gone after downgrade
    - `uv run pytest tests/integration/test_migrations/test_migration_031_route_control.py -x` exits 0
  </acceptance_criteria>
  <done>route_control table + model + migration 031 land; register + migration test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: get_route_control degrade-safe reader</name>
  <files>src/phaze/services/route_control.py, tests/shared/routers/test_routing.py</files>
  <read_first>
    - src/phaze/services/pipeline.py:416-446 (get_stage_controls — the degrade-to-default shape) and :275-292 (_safe_count guarded rollback)
    - src/phaze/models/route_control.py (from Task 1)
    - 71-PATTERNS.md §services/route_control.py (copy-from excerpt) + 71-RESEARCH.md §Code Examples (get_route_control)
  </read_first>
  <behavior>
    - get_route_control(session) → True iff the 'global' row has force_local True; absent row → False; any DB exception → guarded rollback → False.
    - Never raises (the hot poll + routing gate depend on it).
  </behavior>
  <action>
    Create `src/phaze/services/route_control.py` with `async def get_route_control(session) -> bool` per the copy-from excerpt: `row = await session.get(RouteControl, "global")`, return `bool(row.force_local) if row is not None else False`, wrapped in try/except → log `route_control_degraded` + guarded double-rollback → `False`. Mirror the `get_stage_controls` degrade discipline exactly.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/routers/test_routing.py -k route_control_degrades -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async def get_route_control" src/phaze/services/route_control.py` == 1
    - test asserts: seeded row false → False; row set true → True; forced DB error → False (no raise)
    - `uv run pytest tests/shared/routers/test_routing.py -k route_control_degrades -x` exits 0
  </acceptance_criteria>
  <done>Degrade-safe reader returns the flag, defaults False on error/absent; unit test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire the two routing gates (drain no-op + duration router routes-local)</name>
  <files>src/phaze/tasks/release_awaiting_cloud.py, src/phaze/routers/pipeline.py, tests/analyze/core/test_staging_cron.py, tests/shared/routers/test_routing.py</files>
  <read_first>
    - src/phaze/tasks/release_awaiting_cloud.py:110-127 (the cloud_enabled early no-op + session-open + advisory-lock ordering — insert the gate JUST AFTER the session opens, before the advisory lock)
    - src/phaze/routers/pipeline.py:278-345 (_route_discovered_by_duration + :335 is_long gate) and :391,:698,:791 (the 3 callers passing settings.cloud_enabled)
    - src/phaze/services/route_control.py (from Task 2)
    - tests/analyze/core/test_staging_cron.py:335 (the cloud-local drain no-op test to mirror for forced-local)
    - tests/shared/routers/test_pipeline.py:882 (test_backfill_disabled_when_cloud_local — the routes-local assertion pattern)
    - 71-PATTERNS.md §drain force-local gate + §duration-router force-local gate (A4 recommendation: gate BOTH)
  </read_first>
  <behavior>
    - Drain: with force-local engaged, stage_cloud_window returns {"staged":0,"skipped":0} as a clean no-op (no advisory lock taken, no dispatch), exactly like cloud_enabled=False.
    - Duration router: with force-local engaged, a new long file (duration ≥ threshold) routes LOCAL and is NOT held in AWAITING_CLOUD; when not forced, behavior is unchanged.
    - select_backend is untouched (pure) — the flag is read only at the two callers that already read cloud_enabled.
  </behavior>
  <action>
    In `tasks/release_awaiting_cloud.py`, JUST AFTER `async with ctx["async_session"]() as session:` (before the `pg_advisory_xact_lock`), add `if await get_route_control(session): return {"staged": 0, "skipped": 0}` — the same no-op shape as the `cloud_enabled` early return. In `routers/pipeline.py`, thread the force-local read into the duration-router callers: at each of the two UI/analysis callers (`trigger_analysis`, `trigger_analysis_ui` — they hold a session), compute the effective flag `settings.cloud_enabled and not await get_route_control(session)` and pass THAT as the `cloud_enabled` arg to `_route_discovered_by_duration` (the third caller is the shared helper — route the effective flag through it consistently). Do NOT modify `_route_discovered_by_duration`'s `is_long` line or `backend_selection.select_backend`. Extend `tests/analyze/core/test_staging_cron.py` with a forced-local drain no-op test (mirror the `:335` cloud-local test). Add to `tests/shared/routers/test_routing.py` a `route_forced_local_no_hold` test asserting a long file routes local (not AWAITING_CLOUD) when forced. Note in the SUMMARY that already-held AWAITING_CLOUD files stay held under force-local (A4) — this feeds the Plan 05 runbook.
  </action>
  <verify>
    <automated>uv run pytest tests/analyze/core/test_staging_cron.py -k forced_local tests/shared/routers/test_routing.py -k "route_forced_local_no_hold or route_control_degrades" -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "get_route_control" src/phaze/tasks/release_awaiting_cloud.py` ≥ 1 and `grep -c "get_route_control" src/phaze/routers/pipeline.py` ≥ 1
    - `grep -c "get_route_control" src/phaze/services/backend_selection.py` == 0 (select_backend stays pure)
    - drain-no-op test asserts `{"staged":0,"skipped":0}` when forced; router test asserts a long file lands local, not AWAITING_CLOUD, when forced
    - `uv run pytest tests/analyze/core/test_staging_cron.py -k forced_local tests/shared/routers/test_routing.py -x` exits 0; ruff + mypy clean
  </acceptance_criteria>
  <done>Both gates behave exactly like the all-local path when forced; select_backend untouched; gate tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| control row → routing decision | the force_local flag steers whether cloud/Kueue dispatch happens |
| DB read → hot 5s poll + drain cron | a raising read would 500 the poll or crash the cron |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-03 | Denial of service | get_route_control raising into poll/drain | mitigate | degrade to `False` on any error with guarded double-rollback (mirror get_stage_controls); never raises |
| T-71-04 | Tampering | migration seed / control row | mitigate | bound-param INSERT (no interpolation, T-37-01); Boolean column with `server_default false`; single 'global' PK row |
| T-71-08 | Elevation of privilege (unintended cloud dispatch) | forgetting a gate site | mitigate | gate BOTH the drain (primary) AND the duration router (secondary, A4) at the callers already reading cloud_enabled; select_backend stays pure — asserted by a grep gate |
| T-71-SC | Tampering | package installs | accept | no packages installed this phase (zero-new-deps) |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_migrations/test_migration_031_route_control.py tests/shared/routers/test_routing.py tests/analyze/core/test_staging_cron.py -k "route or forced_local or 031" -x`
- Run new tests in isolation via `just test-bucket` (Pitfall 5)
- `uv run ruff check . && uv run mypy .` clean
</verification>

<success_criteria>
A persisted force-local flag, degrade-safe to read, gates both the drain and the duration router so engaging it makes all routing behave exactly like an all-local registry. Ready for Plan 04 to add the write endpoint + header pill.
</success_criteria>

<output>
Create `.planning/phases/71-deployment-config-docs-n-lane-ui/71-02-SUMMARY.md` when done (note the A4 held-file behavior for the runbook).
</output>
