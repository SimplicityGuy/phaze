---
phase: 71-deployment-config-docs-n-lane-ui
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/configuration.md
  - docs/runbook.md
  - docs/cloud-burst.md
  - docs/k8s-burst.md
  - tests/shared/core/test_docs_beui03.py
autonomous: true
requirements: [BEUI-03]
must_haves:
  truths:
    - "D-11: a new docs/runbook.md covers operator ops — the force-local master-toggle / incident-revert procedure, how to read the N lanes (rank order = dispatch preference), spillover behavior + the A4 held-file note, and per-backend _FILE secrets; docs/configuration.md covers the backends: schema + _FILE secrets"
    - "D-12: docs state cloud_target was REMOVED in Phase 67 (not 'deprecated but still works') and show the trivial 1:1 cloud_target→backends equivalence; no migration runbook, no removal date"
    - "D-13: deprecation coverage is DOCS-ONLY — NO startup deprecation-log code (there is no cloud_target shim; PHAZE_CLOUD_TARGET is already silently ignored via extra='ignore'); no new runtime code in this plan"
    - "the internal contradiction in configuration.md is reconciled: the :131 table row and :200-210 'Cloud target' section that still describe PHAZE_CLOUD_TARGET as a live selector are corrected to match the :123 'removed with no shim' banner"
    - "cloud-burst.md / k8s-burst.md gain pointers to the unified backends model without reintroducing the 'one shared bucket' framing (superseded by REG-05)"
  artifacts:
    - path: "docs/runbook.md"
      provides: "operator runbook: toggle/incident-revert, reading N lanes, spillover + held-file note, _FILE secrets"
      contains: "force"
    - path: "tests/shared/core/test_docs_beui03.py"
      provides: "hermetic docs-content guard for BEUI-03 coverage"
      contains: "runbook"
  key_links:
    - from: "docs/configuration.md"
      to: "backends: registry"
      via: "1:1 cloud_target→backends equivalence + removed-in-Phase-67 statement"
      pattern: "backends"
---

<objective>
Close BEUI-03: a new operator runbook plus reconciled configuration docs. DOCS-ONLY — no runtime code (D-13
dropped the startup-log idea; there is no `cloud_target` shim to warn about). Fix the internal contradiction
in `configuration.md` where `PHAZE_CLOUD_TARGET` is still described as a live selector despite Phase 67
removing it.

Purpose: Operators can follow a runbook for the `backends:` schema, `_FILE` secrets, the force-local toggle,
reading N lanes, and the (trivial) `cloud_target`→`backends` migration.
Output: `docs/runbook.md` (new) + reconciled `docs/configuration.md` + pointers + a hermetic docs guard test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-deployment-config-docs-n-lane-ui/71-CONTEXT.md
@.planning/phases/71-deployment-config-docs-n-lane-ui/71-RESEARCH.md
@.planning/phases/71-deployment-config-docs-n-lane-ui/71-PATTERNS.md

<interfaces>
Facts to encode (from RESEARCH §Assumptions A1 + Pitfall 1 — verified):
- Phase 67 REMOVED `cloud_target` outright, NO back-compat shim (configuration.md:91 + :123 already state this).
- `config.py` `model_config = extra="ignore"` (config.py:87) → a stale `PHAZE_CLOUD_TARGET` env var is SILENTLY DROPPED, not read. Nothing in `src/phaze/` references `cloud_target`.
- The `backends:` registry (`backends.toml`, `PHAZE_BACKENDS_CONFIG_FILE`, default `/etc/phaze/backends.toml`) is the SOLE cloud config surface; per-backend inline `*_file` secret pointers + control-plane `<VAR>_FILE` env secrets.
- 1:1 equivalence to show: old `cloud_target=local` → an all-local registry (implicit single `kind=local` when backends.toml is absent); `cloud_target=a1` → one `[[backends]] kind="compute"` entry; `cloud_target=k8s` → one `[[backends]] kind="kueue"` entry (+ its bucket).

The CONTRADICTION to reconcile in configuration.md:
- :89-118 `## Backend registry` — CORRECT (registry is sole surface, flat knobs removed).
- :121-131 `## Cloud-burst settings` table + :131 `cloud_target` row — STALE (still calls it "Master routing selector … local|a1|k8s").
- :200-210 `### Cloud target (PHAZE_CLOUD_TARGET)` — STALE (describes it as the live single selector + "changing the target requires a restart").
- :154-156 kube knobs say "Required when cloud_target=k8s" — restate against the registry (kueue backend requires kube_* in its submodel).

Force-local runbook facts (from Plans 02/04):
- The header pill (`FORCED LOCAL` amber engaged / `CLOUD ROUTING` normal) writes a route_control DB row via POST /pipeline/routing/force-local; reversible, no redeploy.
- A4 held-file behavior: while forced-local is engaged, already-held AWAITING_CLOUD files STAY held (the drain no-ops); new long files route local. Document this so an operator isn't surprised.

docs-drift guard (justfile:97 → tests/shared/core/test_requirements_traceability.py) tracks only
REQUIREMENTS/ROADMAP — it does NOT cover runbook content. This plan adds its own hermetic docs-content test.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile configuration.md + cloud-burst/k8s-burst pointers</name>
  <files>docs/configuration.md, docs/cloud-burst.md, docs/k8s-burst.md</files>
  <read_first>
    - docs/configuration.md:21-59 (the _FILE convention section), :89-118 (## Backend registry — the correct anchor), :121-131 (## Cloud-burst settings + the stale cloud_target row), :147-165 (kube/S3 sections with "Required when cloud_target=k8s"), :196-211 (### Cloud target — the stale live-selector section)
    - docs/cloud-burst.md + docs/k8s-burst.md (structure/tone; the ASCII→mermaid intro blocks — add a pointer, do NOT reintroduce "one shared bucket")
    - 71-PATTERNS.md §Docs — BEUI-03 + 71-RESEARCH.md §Assumptions A1 / Pitfall 1
  </read_first>
  <action>
    In `docs/configuration.md`: correct the `### Cloud target (PHAZE_CLOUD_TARGET)` section (:200-211) and the `cloud_target` table row (:131) to state that `cloud_target` was **REMOVED in Phase 67 (2026.7.1) — use the `backends:` registry** (NOT "deprecated but still works"); note that a stale `PHAZE_CLOUD_TARGET` env var is silently ignored (`extra="ignore"`). Add a short **1:1 `cloud_target`→`backends` equivalence** table (local→implicit/all-local registry; a1→one `kind="compute"` entry; k8s→one `kind="kueue"` entry + its bucket) for anyone reading old configs — no migration runbook, no removal date (D-12). Restate the kube-knob "Required when `cloud_target=k8s`" phrasing (:154-156) against the registry (the `kueue` backend submodel requires its kube_* fields). Confirm the `## Backend registry` section already documents the `backends:` schema + per-backend `_FILE`/inline `*_file` secrets; extend if any gap. In `docs/cloud-burst.md` and `docs/k8s-burst.md`, add a one-line pointer to the unified `backends` model in configuration.md; do NOT reintroduce the "one shared bucket" framing (superseded by REG-05). NO code changes — this is docs-only (D-13).
  </action>
  <verify>
    <automated>uv run pytest tests/shared/core/test_docs_beui03.py -k configuration -x</automated>
  </verify>
  <acceptance_criteria>
    - `docs/configuration.md` no longer describes `PHAZE_CLOUD_TARGET` as a live "single routing selector" that chooses where analysis runs; the `### Cloud target` section states it was removed in Phase 67 and points to `backends:`
    - a 1:1 `cloud_target`→`backends` equivalence appears in configuration.md
    - `grep -c "one shared bucket" docs/cloud-burst.md docs/k8s-burst.md` == 0 (framing not reintroduced) and both files point to the backends model
    - `grep -c "cloud_target" src/phaze/` (via `grep -rc`) confirms zero code changes — no runtime edits in this plan
    - `uv run pytest tests/shared/core/test_docs_beui03.py -k configuration -x` exits 0
  </acceptance_criteria>
  <done>configuration.md contradiction resolved with the removed-in-Phase-67 statement + 1:1 equivalence; pointers added; no code touched.</done>
</task>

<task type="auto">
  <name>Task 2: New docs/runbook.md + hermetic docs-content guard</name>
  <files>docs/runbook.md, tests/shared/core/test_docs_beui03.py</files>
  <read_first>
    - docs/cloud-burst.md / docs/k8s-burst.md (structure/tone to mirror for the new runbook)
    - docs/configuration.md:21-59 (_FILE secrets — the runbook cross-references, never restates secret values)
    - .planning/phases/71-deployment-config-docs-n-lane-ui/71-02-SUMMARY.md (the A4 held-file behavior recorded by Plan 02, if present) + 71-UI-SPEC.md §BEUI-02 (pill states/copy for the toggle procedure)
    - tests/shared/core/test_requirements_traceability.py (the hermetic docs-test convention: `_REPO_ROOT`/`Path` reads, no DB fixture) to mirror for the new guard
  </read_first>
  <action>
    Create `docs/runbook.md` covering: (1) **Force-local incident revert** — what the header pill does, how to engage/revert (one click, reversible, no redeploy), that it writes the route_control row and gates both the drain and the duration router, and the **A4 held-file note** (already-held `AWAITING_CLOUD` files stay held while forced; new long files route local); (2) **Reading the N lanes** — rank ascending = dispatch preference (top-left = used first), in_flight/cap, available/offline, and the per-lane Kueue quota-wait-vs-Inadmissible caption; (3) **Spillover behavior** across backends by rank/cap; (4) **Per-backend `_FILE` secrets** — cross-reference configuration.md, never print secret values (T-71-11). Add `tests/shared/core/test_docs_beui03.py`: a hermetic file-content test (mirror the traceability-guard `Path` read pattern, no DB) asserting `docs/runbook.md` exists and contains the required sections (force-local/incident, lane-reading, spillover, `_FILE` secrets) AND that `docs/configuration.md` states `cloud_target` was removed + contains the `backends`-equivalence. This is the plan's automated BEUI-03 verify.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/core/test_docs_beui03.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `docs/runbook.md` exists and contains sections for the force-local toggle/incident revert (incl. the held-file note), reading the N lanes, spillover, and `_FILE` secrets
    - `grep -ci "held" docs/runbook.md` ≥ 1 (A4 held-file behavior documented) and no literal secret values appear
    - `tests/shared/core/test_docs_beui03.py` asserts runbook presence+sections AND configuration.md removed-statement+equivalence, runs with NO DB fixture (hermetic, generic bucket)
    - `uv run pytest tests/shared/core/test_docs_beui03.py -x` exits 0; `just docs-drift` still green
  </acceptance_criteria>
  <done>Operator runbook shipped covering toggle/lanes/spillover/held-files/_FILE secrets; hermetic guard green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → operator | docs describe secret handling but must never embed secret values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-11 | Information disclosure | runbook / configuration docs | mitigate | describe the `_FILE` secret convention and per-backend `*_file` pointers by NAME only; never print a token/key/DSN value in docs or examples |
| T-71-12 | Tampering (stale guidance) | contradictory cloud_target docs | mitigate | reconcile the internal contradiction so operators don't set a silently-ignored `PHAZE_CLOUD_TARGET` and assume routing changed; hermetic guard locks the corrected statement |
| T-71-SC | Tampering | package installs | accept | no packages installed this phase (docs + one test only) |
</threat_model>

<verification>
- `uv run pytest tests/shared/core/test_docs_beui03.py -x` (hermetic docs guard)
- `just docs-drift` still green (REQUIREMENTS/ROADMAP traceability unaffected)
- `grep -rc "cloud_target" src/phaze/` == 0 change (no runtime edits — docs-only, D-13)
</verification>

<success_criteria>
Operators have a runbook for the backends schema, `_FILE` secrets, the force-local toggle (with held-file
behavior), reading N lanes, and spillover; configuration.md no longer contradicts itself about `cloud_target`
and shows the trivial 1:1 `backends` equivalence — all with zero runtime code change.
</success_criteria>

<output>
Create `.planning/phases/71-deployment-config-docs-n-lane-ui/71-05-SUMMARY.md` when done.
</output>
