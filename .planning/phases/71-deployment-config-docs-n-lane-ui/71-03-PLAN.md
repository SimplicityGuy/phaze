---
phase: 71-deployment-config-docs-n-lane-ui
plan: 03
type: execute
wave: 2
depends_on: ["71-01", "71-02"]
files_modified:
  - src/phaze/routers/pipeline.py
  - src/phaze/templates/pipeline/partials/_lane_card.html
  - src/phaze/templates/pipeline/partials/_analyze_lanes.html
  - src/phaze/templates/pipeline/partials/analyze_workspace.html
  - src/phaze/templates/pipeline/partials/stats_bar.html
  - tests/shared/routers/test_pipeline.py
  - tests/shared/routers/test_enrich_analyze_workspaces.py
autonomous: true
requirements: [BEUI-01]
must_haves:
  truths:
    - "D-04: the lane list is seeded IDENTICALLY into build_dashboard_context() (full page) AND pipeline_stats_partial() (5s OOB re-push) — one existing poll, no second loop, no new read endpoint"
    - "D-05: analyze_workspace.html replaces the fixed grid grid-cols-3 with a responsive wrapping grid looping _lane_card.html over N lanes; card box model (p-4/mt-3/mt-2/h-1.5/rounded-xl) is FROZEN"
    - "D-06: cards render rank-ascending (server pre-sorted; template loops verbatim), each showing RANK {n} + {in_flight}/{cap} + per-lane Kueue quota-wait-vs-Inadmissible caption, with word+glyph state labels (WCAG 1.4.1, never hue-only)"
    - "D-07: the 6 global cloud-state cards stay VERBATIM as a cross-lane roll-up below the grid (unchanged ids + OOB swaps) — lean keep-as-roll-up"
    - "BEUI-01: no operator-facing copy hardcodes a lane count — the Analyze workspace subcount is lane-count-agnostic (interpolated from the seeded lanes or without a fixed number) and the stale THREE-execution-lane comments are updated to the N-lane loop"
    - "the transitional cloud_lane_kind context key is retired from build_dashboard_context + analyze_workspace.html (replaced by the lanes list); resolved_non_local_kind itself is NOT deleted (other callers use it)"
  artifacts:
    - path: "src/phaze/templates/pipeline/partials/_analyze_lanes.html"
      provides: "OOB-swappable #analyze-lanes grid looping _lane_card.html + empty/degrade panel"
      contains: "analyze-lanes"
    - path: "src/phaze/routers/pipeline.py"
      provides: "lanes seeded in both context builders; cloud_lane_kind key removed"
      contains: "get_backend_lane_snapshot"
  key_links:
    - from: "routers/pipeline.py build_dashboard_context + pipeline_stats_partial"
      to: "get_backend_lane_snapshot"
      via: "seeded identically as context['lanes']"
      pattern: "get_backend_lane_snapshot"
    - from: "stats_bar.html"
      to: "_analyze_lanes.html"
      via: "OOB include with oob=True inside the oob_counts gate"
      pattern: "_analyze_lanes.html"
---

<objective>
Render the N-lane grid. Seed the Plan-01 snapshot into both context builders, extract the grid into an
OOB-swappable partial, loop `_lane_card.html` rank-ascending with the three new per-card data points, keep
the 6 global cards as a roll-up, and retire the transitional `cloud_lane_kind` key. Rides the existing 5s
poll — no second loop.

Purpose: Generalize Phase 58's fixed 3-card grid to N registry-derived lanes (BEUI-01).
Output: seeded `lanes` context + `_analyze_lanes.html` + extended `_lane_card.html` + updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-deployment-config-docs-n-lane-ui/71-CONTEXT.md
@.planning/phases/71-deployment-config-docs-n-lane-ui/71-RESEARCH.md
@.planning/phases/71-deployment-config-docs-n-lane-ui/71-PATTERNS.md
@.planning/phases/71-deployment-config-docs-n-lane-ui/71-UI-SPEC.md

<interfaces>
Plan 01 provides: `get_backend_lane_snapshot(session) -> list[dict]` where each dict =
`{id, kind, rank, cap, in_flight, available, quota_wait, inadmissible}`, rank-ascending, `[]` on error.

Seed sites (routers/pipeline.py):
- `build_dashboard_context(app_state, session)` returns its dict around :550 (cloud counts seeded here)
- `pipeline_stats_partial(...)` builds context around :652 (same cloud counts seeded)
- `cloud_lane_kind` currently seeded at :576 via `resolved_non_local_kind(settings)` — RETIRE this key

Template OOB idiom (admission_state_card.html:25-27): `{% if oob %}hx-swap-oob="true"{% endif %}` on `<section id>`.
stats_bar.html:46 `{% if oob_counts %}` block wraps the 6 cloud-card `{% with oob = True %}...{% endwith %}` includes (:73-103).
analyze_workspace.html:41 `<div id="analyze-lanes" class="grid grid-cols-3 gap-4 p-6">` includes _lane_card 3× (:53,:66,:81); the 6 global cards follow at :87-94.

UI-SPEC locks (do not deviate): grid `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4 p-6`;
lane colors by kind (local=emerald/🖥️, compute=blue/☁️, kueue=amber/⎈); RANK {n} = Jura 500 text-xs uppercase
tracking-wider text-gray-400 dark:text-gray-500; numeral = Inter 500 mono (NOT semibold); Kueue caption
`{quota_wait} waiting · {inadmissible} inadmissible` (inadmissible segment amber + role="alert" when >0);
empty state heading "Lane status unavailable".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seed lanes in both context builders + retire cloud_lane_kind</name>
  <files>src/phaze/routers/pipeline.py, tests/shared/routers/test_pipeline.py</files>
  <read_first>
    - src/phaze/routers/pipeline.py:455-578 (build_dashboard_context, cloud-count seeds + :576 cloud_lane_kind) and :599-668 (pipeline_stats_partial context)
    - src/phaze/routers/shell.py:164-174 (oob_counts=False initial-render discipline — confirm the lanes seed follows it)
    - tests/shared/routers/test_pipeline.py:1026-1050 (test_dashboard_context_binds_cloud_lane_kind — the test to REPLACE with a lanes assertion)
    - 71-PATTERNS.md §routers/pipeline.py + shell.py seed + §Retirement
  </read_first>
  <behavior>
    - build_dashboard_context returns `lanes` = await get_backend_lane_snapshot(session), and no longer returns `cloud_lane_kind`.
    - pipeline_stats_partial seeds the SAME `lanes` key identically (D-04).
    - `resolved_non_local_kind` remains defined and importable (other callers at pipeline.py:811 keep using it) — only the context key is removed.
  </behavior>
  <action>
    Add `"lanes": await get_backend_lane_snapshot(session)` to the return dict of `build_dashboard_context` AND to the context of `pipeline_stats_partial`, seeded identically (mirror the cloud-count seeded-identically comment discipline). Remove the `"cloud_lane_kind": resolved_non_local_kind(settings)` key from `build_dashboard_context` (:576). Keep the `resolved_non_local_kind` import — it is still used at :811. Update `tests/shared/routers/test_pipeline.py:1026` (`test_dashboard_context_binds_cloud_lane_kind`): rename/rework to assert the new `lanes` key is present and identical in BOTH builders and that `cloud_lane_kind` is absent; keep the existing `assert "cloud_target" not in ctx` guard. Use `monkeypatch.setattr(settings, "backends", [...])` for registry fixtures (Pitfall 5).
  </action>
  <verify>
    <automated>uv run pytest tests/shared/routers/test_pipeline.py -k "lanes or dashboard_context" -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "get_backend_lane_snapshot" src/phaze/routers/pipeline.py` == 2 (both builders)
    - `grep -c 'cloud_lane_kind' src/phaze/routers/pipeline.py` == 0 (key retired; string gone)
    - `grep -c "resolved_non_local_kind" src/phaze/routers/pipeline.py` ≥ 1 (still used elsewhere, not deleted)
    - test asserts `lanes` present in both builders + `cloud_lane_kind`/`cloud_target` absent; exits 0
  </acceptance_criteria>
  <done>lanes seeded identically in both context builders; cloud_lane_kind retired; test updated + green.</done>
</task>

<task type="auto">
  <name>Task 2: _lane_card extend + _analyze_lanes grid + workspace/stats_bar wiring</name>
  <files>src/phaze/templates/pipeline/partials/_lane_card.html, src/phaze/templates/pipeline/partials/_analyze_lanes.html, src/phaze/templates/pipeline/partials/analyze_workspace.html, src/phaze/templates/pipeline/partials/stats_bar.html, tests/shared/routers/test_enrich_analyze_workspaces.py</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/_lane_card.html:31-52 (current card contract — box model to FREEZE, word+glyph state labels)
    - src/phaze/templates/pipeline/partials/analyze_workspace.html:1-94 (the stale "THREE execution-lane cards" comments at :9/:40, the hardcoded subcount string at :31 `across 3 lanes`, the 3× include block to replace + the 6 global cards to KEEP verbatim)
    - src/phaze/templates/pipeline/partials/stats_bar.html:46-103 (the oob_counts gate + the 6 cloud-card OOB includes to mirror)
    - src/phaze/templates/pipeline/partials/admission_state_card.html:25-27 (the {% if oob %}hx-swap-oob gate to copy onto #analyze-lanes)
    - 71-UI-SPEC.md §BEUI-01 (grid class, per-card data, states, empty state) + 71-PATTERNS.md §_lane_card.html / §_analyze_lanes.html / §analyze_workspace.html / §stats_bar.html
  </read_first>
  <action>
    Extend `_lane_card.html` to render from a `lane` dict: `RANK {{ lane.rank }}` micro-label inline after the title (Jura 500 text-xs uppercase tracking-wider text-gray-400 dark:text-gray-500), the capacity numeral as `{{ lane.in_flight }}/{{ lane.cap }}` (Inter 500 mono, kind-color), bar fill % = in_flight/cap clamped 0–100, and for kueue lanes a caption `{{ lane.quota_wait }} waiting · {{ lane.inadmissible }} inadmissible` with the inadmissible segment amber + `role="alert"` when `> 0`. Keep the `available`/`offline` word+glyph contract (retire "not configured"). Derive lane color/glyph from `lane.kind` (local=emerald/🖥️, compute=blue/☁️, kueue=amber/⎈; kueue keeps border-amber-500/30). FREEZE the box model (p-4/mt-3/mt-2/h-1.5/rounded-xl). If any `lane` value reaches an Alpine JS context use `|tojson` not `|e` (T-71-05 XSS lesson). Create `_analyze_lanes.html`: `<div id="analyze-lanes" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4 p-6" {% if oob %}hx-swap-oob="true"{% endif %}>` looping `{% for lane in lanes %}{% include "pipeline/partials/_lane_card.html" %}{% endfor %}`, with an empty/degrade branch when `lanes` is falsy rendering a single full-width muted panel (rounded-xl border phaze-border p-4, heading "Lane status unavailable" + the UI-SPEC body copy) — never a 500, never a collapsed layout. In `analyze_workspace.html` replace the hand-written `#analyze-lanes` 3-card block with `{% include "pipeline/partials/_analyze_lanes.html" %}` (no oob → initial render) and KEEP the 6 global cards (:87-94) VERBATIM below (D-07 roll-up, byte-stable ids). In `stats_bar.html`, inside the `{% if oob_counts %}` block, add `{% with oob = True %}{% include "pipeline/partials/_analyze_lanes.html" %}{% endwith %}` (mirror the cloud-card OOB includes). Make the workspace subcount lane-count-agnostic: replace the hardcoded `across 3 lanes` string at `analyze_workspace.html:31` — drop the literal count or interpolate the seeded lane count (e.g. `{{ lanes|length }} lanes`, consistent with the Task-1 `lanes` context key) so N≠3 backends never render "across 3 lanes". Update the stale `THREE execution-lane cards` header/body comments (~:9 and ~:40) to describe the N-lane `{% for lane in lanes %}` loop (no fixed count).
  </action>
  <verify>
    <automated>uv run pytest tests/shared/routers/test_enrich_analyze_workspaces.py -k "lane" -x</automated>
  </verify>
  <acceptance_criteria>
    - `_analyze_lanes.html` contains `id="analyze-lanes"` and `{% if oob %}hx-swap-oob` and a `{% for lane in lanes %}` loop
    - `grep -c "grid-cols-3\"" src/phaze/templates/pipeline/partials/analyze_workspace.html` == 0 (fixed 3-col grid replaced) and the 6 global card includes are still present
    - template-render test (mirror test_enrich_analyze_workspaces.py:396): a stats render with N seeded lanes emits N lane cards under `#analyze-lanes` with `hx-swap-oob`, rank order, word-labelled states, and no `cloud_target` string
    - `grep -c "3 lanes" src/phaze/templates/pipeline/partials/analyze_workspace.html` == 0 (no hardcoded lane count) and `grep -ci "THREE execution-lane" src/phaze/templates/pipeline/partials/analyze_workspace.html` == 0 (stale comment updated)
    - render test with N≠3 seeded lanes: the workspace subcount contains no literal `3 lanes` and reflects the actual lane count (or omits a fixed count)
    - `uv run pytest tests/shared/routers/test_enrich_analyze_workspaces.py -x` exits 0
  </acceptance_criteria>
  <done>N-lane grid renders rank-ascending with new per-card data, OOB-swaps on the poll, degrades to the empty panel, keeps the 6-card roll-up; template test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry-declared lane values → rendered HTML / Alpine JS | `id`/`kind` and counts flow into the card template |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-05 | Tampering / XSS | lane `id`/`kind` in `_lane_card.html` | mitigate | Jinja autoescape on; if a value reaches an Alpine JS context use `|tojson` not `|e` (Phase-60 `_diff_row.html` lesson); registry ids are operator-declared |
| T-71-01 | Information disclosure | card render | mitigate | render ONLY `{id,kind,rank,cap,in_flight,available,quota_wait,inadmissible}` — the snapshot already excludes secrets (SP-5) |
| T-71-06 | Denial of service | initial render emitting OOB / missing DOM target | mitigate | gate `hx-swap-oob` behind `oob`; initial include passes no oob (mirror `oob_counts=False`, Pitfall 4); `#analyze-lanes` present on the Analyze render so the poll swap lands |
| T-71-SC | Tampering | package installs | accept | no packages installed this phase |
</threat_model>

<verification>
- `uv run pytest tests/shared/routers/test_pipeline.py tests/shared/routers/test_enrich_analyze_workspaces.py -x`
- Run touched tests in isolation via `just test-bucket` (Pitfall 5)
- `uv run ruff check . && uv run mypy .` clean
- Manual (Plan-level, deferred to UAT): boot local uvicorn + fresh phaze_uat DB with a backends.toml holding ≥2 lanes; confirm the grid wraps rank-ascending on the 5s poll
</verification>

<success_criteria>
The Analyze workspace renders N rank-ascending per-backend lane cards (rank + in_flight/cap + per-lane Kueue
admission) on the existing 5s poll with word-labelled states, degrading to a muted panel on `[]`, with the
6-card global roll-up intact and `cloud_lane_kind` retired.
</success_criteria>

<output>
Create `.planning/phases/71-deployment-config-docs-n-lane-ui/71-03-SUMMARY.md` when done.
</output>
