---
phase: 69-tiered-drain-scheduler
plan: 04
type: execute
wave: 3
depends_on: [69-01, 69-02]
files_modified:
  - src/phaze/tasks/reenqueue.py
  - src/phaze/routers/agent_push.py
  - src/phaze/routers/agent_s3.py
  - tests/analyze/tasks/test_recovery.py
autonomous: true
requirements: [SCHED-03, SCHED-05]

must_haves:
  truths:
    - "SCHED-05: recover_orphaned_work excludes any ledger row whose file has an in-flight cloud_job row (any backend_id) — the backend reconcile/callback is the single owner, so compute-backed cloud files gain no second recovery path (no replay of the 44.5k over-enqueue incident class)"
    - "SCHED-05: files with NO in-flight cloud_job keep the existing AWAITING_CLOUD held-file recovery path"
    - "SCHED-03: a compute push/analysis failure at its cap spills the file back to AWAITING_CLOUD (not ANALYSIS_FAILED) and marks its cloud budget spent so the next drain tick routes it to local (D-04)"
    - "SCHED-03: the compute cloud_job row is terminalized (FAILED, in-flight decremented) on spill so in_flight_count stays honest"
  artifacts:
    - path: "src/phaze/tasks/reenqueue.py"
      provides: "in-flight-cloud_job exclusion set in the orphan comprehension"
      contains: "in_flight"
    - path: "src/phaze/routers/agent_push.py"
      provides: "compute push-cap failure spill to AWAITING_CLOUD"
    - path: "src/phaze/routers/agent_s3.py"
      provides: "kueue upload-cap failure spill to AWAITING_CLOUD"
  key_links:
    - from: "src/phaze/tasks/reenqueue.py"
      to: "src/phaze/models/cloud_job.py"
      via: "SELECT cloud_job.file_id WHERE status IN in-flight set"
      pattern: "CloudJob.file_id"
---

<objective>
Close the SCHED-05 double-owner vector and finish the SCHED-03 compute-spill uniformity. (1) Extend `recover_orphaned_work`'s orphan predicate with an `in_flight_cloud_job_file_ids` exclusion set (mirroring `_get_awaiting_cloud_ids`) so a file that already has an in-flight `cloud_job` row is owned solely by its backend reconcile/`/pushed` callback — never recovered a second time by the ledger. (2) Flip the compute (`agent_push`) and kueue-upload (`agent_s3`) at-cap terminal callbacks from `ANALYSIS_FAILED` to spill-back to `AWAITING_CLOUD` with the cloud budget marked spent, so a cloud-flaky long file falls to local (D-04) instead of hard-failing — the least-developed path RESEARCH flags as a distinct task.

Purpose: Guarantee exactly one recovery owner per backend kind (no 44.5k-incident replay) and make every cloud-failure path (reconcile + both callbacks) spill to local rather than hard-fail.
Output: ledger single-owner guard + compute/kueue callback spill + recovery tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-tiered-drain-scheduler/69-CONTEXT.md
@.planning/phases/69-tiered-drain-scheduler/69-RESEARCH.md
@.planning/phases/69-tiered-drain-scheduler/69-PATTERNS.md
@.planning/phases/69-tiered-drain-scheduler/69-VALIDATION.md
@.planning/phases/69-tiered-drain-scheduler/69-01-SUMMARY.md
@.planning/phases/69-tiered-drain-scheduler/69-02-SUMMARY.md

<interfaces>
<!-- Recovery + callback substrate. -->

src/phaze/tasks/reenqueue.py:
  _get_awaiting_cloud_ids(session) -> set[str]  (lines 190-199) — the EXACT mirror for the new in-flight set
  recover_orphaned_work: orphan comprehension at line 318:
    orphaned = [r for r in rows if r.key not in live and not is_domain_completed(r, done_sets)]
  _natural_id(row) -> str | None (line 202) — file-id from ledger payload

src/phaze/models/cloud_job.py: CloudJob.file_id, CloudJob.status; the in-flight status set
  IN_FLIGHT = {UPLOADING, UPLOADED, SUBMITTED, RUNNING} (src/phaze/services/backends.py:72-77) — import or re-declare

src/phaze/routers/agent_push.py `report_push_mismatch` at-cap branch (lines 184-203):
  currently: FileRecord -> ANALYSIS_FAILED + CloudJob -> FAILED + clear_ledger_entry (cap = settings.push_max_attempts)
src/phaze/routers/agent_s3.py `report_upload_failed` at-cap branch (lines 174-188):
  currently: CloudJob -> FAILED + FileRecord -> ANALYSIS_FAILED + abort_multipart + delete_staged_object + clear_ledger (cap = settings.push_max_attempts)

D-04 confirmed reading: the attempt signal select_backend consumes is `cloud_job.attempts` (total-cloud budget). These callbacks use a SEPARATE ledger-payload counter (push_attempt / s3_upload_attempt) — so on spill they must set `cloud_job.attempts` to at least `cloud_submit_max_attempts` to route to local next tick.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: In-flight-cloud_job exclusion in recover_orphaned_work (SCHED-05)</name>
  <files>src/phaze/tasks/reenqueue.py, tests/analyze/tasks/test_recovery.py</files>
  <read_first>
    - src/phaze/tasks/reenqueue.py:180-349 (`_get_awaiting_cloud_ids`, `_natural_id`, `is_domain_completed`, `recover_orphaned_work` orphan comprehension + the held-agent-rows partition)
    - 69-RESEARCH.md § "Novel Mechanism 5" (lines 357-388 — the double-owner danger + the plan-ready resolution points 2-3)
    - 69-PATTERNS.md § "MOD src/phaze/tasks/reenqueue.py" (lines 151-172 — the `_in_flight_cloud_job_ids` helper + the comprehension edit)
    - src/phaze/services/backends.py:70-77 (the IN_FLIGHT status set to import/re-declare)
    - tests/analyze/tasks/test_recovery.py (`test_held_process_file_row_*` cells ~lines 609-671; helpers `_seed_ledger`/`_patch_inflight`/`_patch_live_keys`)
  </read_first>
  <action>
    Add an async helper `_in_flight_cloud_job_ids(session) -> set[str]` to reenqueue.py mirroring `_get_awaiting_cloud_ids`: `SELECT CloudJob.file_id WHERE CloudJob.status IN [s.value for s in IN_FLIGHT]`, returning `{str(fid) for fid ...}`. Import the in-flight status set from `phaze.services.backends` (or the cloud_job model) rather than re-hardcoding the enum values if a shared symbol exists. In `recover_orphaned_work`, read the set once (alongside `live`/`done_sets`), then extend the orphan comprehension (line 318) so a row is excluded when `_natural_id(r)` is in the in-flight set: `orphaned = [r for r in rows if r.key not in live and not is_domain_completed(r, done_sets) and _natural_id(r) not in in_flight]`. This makes the backend reconcile/`/pushed` callback the single owner for any file with a live cloud_job row (compute now writes one per Phase-68 BACK-03), preventing the 44.5k double-recovery class. KEEP the existing AWAITING_CLOUD held-file path (lines 335-340) for files with NO in-flight cloud_job — those are genuinely orphaned and correctly re-route to the drain. Extend tests/analyze/tasks/test_recovery.py with a `single_owner` case: a compute file with an in-flight cloud_job row AND a process_file/push_file ledger row is skipped by `recover_orphaned_work` (recovered by exactly one path); a file with NO in-flight cloud_job still recovers via the held path.
  </action>
  <verify>
    <automated>uv run pytest tests/analyze/tasks/test_recovery.py -k single_owner -x</automated>
  </verify>
  <acceptance_criteria>
    - `recover_orphaned_work` excludes ledger rows whose file has an in-flight cloud_job (grep shows `_natural_id(r) not in in_flight` in the comprehension)
    - a compute file with an in-flight cloud_job + a ledger row is NOT re-enqueued by recovery (owned by its callback/reconcile)
    - a held AWAITING_CLOUD file with no cloud_job still recovers via the existing path (no regression)
    - `uv run mypy src/phaze/tasks/reenqueue.py` clean
  </acceptance_criteria>
  <done>Exactly one recovery owner per backend kind; compute-backed cloud files gain no second recovery path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compute/kueue callback at-cap spill to AWAITING_CLOUD (SCHED-03/D-04)</name>
  <files>src/phaze/routers/agent_push.py, src/phaze/routers/agent_s3.py, tests/analyze/tasks/test_recovery.py</files>
  <behavior>
    - report_push_mismatch at cap: FileRecord -> AWAITING_CLOUD (NOT ANALYSIS_FAILED); its cloud_job -> FAILED; cloud_job.attempts marked spent (>= cloud_submit_max_attempts); ledger cleared; response still cleared=True
    - report_upload_failed at cap: FileRecord -> AWAITING_CLOUD (NOT ANALYSIS_FAILED); cloud_job -> FAILED + cloud_phase None; cloud_job.attempts marked spent; multipart aborted + staged object deleted + ledger cleared (cleanup preserved)
    - under-cap re-drive paths for both callbacks are UNCHANGED
    - a spilled file, on the next drain tick, is routed to local by select_backend (attempts >= cap) — assert the state is AWAITING_CLOUD with attempts>=cap so the drain will pick local
  </behavior>
  <read_first>
    - src/phaze/routers/agent_push.py:150-230 (`report_push_mismatch` — the at-cap branch lines 184-203 to flip; under-cap re-drive to leave alone)
    - src/phaze/routers/agent_s3.py:145-200 (`report_upload_failed` — the at-cap branch lines 174-188 to flip; preserve abort_multipart + delete_staged_object + clear_ledger)
    - 69-RESEARCH.md § "Novel Mechanism 3: The increment site + compute uniformity" point 2 (lines 329-334 — flip terminal to AWAITING_CLOUD + mark cloud budget) + Pitfall 3
    - 69-PATTERNS.md § "MOD src/phaze/routers/agent_push.py / agent_s3.py" (lines 198-200)
    - src/phaze/config.py `cloud_submit_max_attempts` (the exclusion cap select_backend reads)
  </read_first>
  <action>
    In agent_push.py `report_push_mismatch` at the `if next_attempt > settings.push_max_attempts:` branch: change the FileRecord terminal from `state=FileState.ANALYSIS_FAILED` to `state=FileState.AWAITING_CLOUD`; keep the `CloudJob -> FAILED` terminalization (decrements in-flight) and the `clear_ledger_entry` + commit; ADD marking the file's cloud budget spent so select_backend routes it to local next tick — set the file's `cloud_job.attempts` to at least `settings.cloud_submit_max_attempts` in the same UPDATE (D-04 total-cloud budget: this backend exhausted its `push_max_attempts` re-drives, so cloud is done -> local). Reword the warning to "push cap reached -> spill to AWAITING_CLOUD (routes to local)". In agent_s3.py `report_upload_failed` at-cap branch: same flip (`ANALYSIS_FAILED` -> `AWAITING_CLOUD`), keep `CloudJob -> FAILED, cloud_phase=None`, keep `abort_multipart_upload` + `delete_staged_object` + `clear_ledger_entry` cleanup, and set `cloud_job.attempts >= settings.cloud_submit_max_attempts`. Do NOT change the under-cap re-drive paths. NEVER 500 — the callbacks stay 200 with their existing degrade behavior. Add tests (extend test_recovery.py or the existing callback tests — locate the current at-cap callback tests via grep of `push_max_attempts` in tests) covering every <behavior> case; name them so `-k "spill or compute"` binds.
  </action>
  <verify>
    <automated>uv run pytest -k "push_mismatch or upload_failed or compute_spill" -x</automated>
  </verify>
  <acceptance_criteria>
    - Both callbacks at cap set `FileState.AWAITING_CLOUD` (grep confirms neither at-cap branch references `ANALYSIS_FAILED`)
    - On spill, the file's `cloud_job` is FAILED with `attempts >= cloud_submit_max_attempts` so the next drain tick routes it to local
    - agent_s3 cleanup (abort multipart + delete staged object + ledger clear) is preserved on the spill path
    - under-cap re-drive behavior unchanged; callbacks never 500
    - `just test-bucket analyze` green; `uv run mypy src/phaze/routers/agent_push.py src/phaze/routers/agent_s3.py` clean
  </acceptance_criteria>
  <done>Every cloud-failure path (reconcile + both callbacks) spills to AWAITING_CLOUD and falls to local; ANALYSIS_FAILED comes only from local failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent callback (bearer-authed) -> control DB | compute/kueue agents POST failure callbacks that mutate FileState/cloud_job |
| ledger recovery vs backend reconcile | both could claim ownership of a cloud file's re-drive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-04-01 | Elevation/DoS (double recovery -> queue detonation) | recover_orphaned_work orphan set | mitigate | Exclude any file with an in-flight cloud_job from the ledger orphan set — single owner per kind; prevents the 44.5k over-enqueue incident class (SCHED-05) |
| T-69-04-02 | DoS (unbounded cloud retry) | callback at-cap spill | mitigate | Spill marks `cloud_job.attempts >= cloud_submit_max_attempts` (bounded gt=0 lt=20) so select_backend forces local — the file cannot re-thrash cloud; local failure is the terminal (D-04) |
| T-69-04-03 | Tampering (SQLi) | callback UPDATEs + in-flight query | mitigate | ORM `update(...).values(...)` + bound params; `CloudJob.status.in_([...])` bound list; no f-string SQL |
| T-69-04-04 | Spoofing | agent callbacks | accept | Callbacks require the existing bearer-token agent dependency (AUTH-01, untouched); `file_id` is a path UUID, no free-text |
| T-69-04-05 | Info disclosure | callback logging | accept | Logs file_id/agent_id/attempt/cap only; no secrets (Phase-68 discipline) |
</threat_model>

<verification>
- `uv run pytest tests/analyze/tasks/test_recovery.py -x` green
- `just test-bucket analyze` green
- `grep -n "ANALYSIS_FAILED" src/phaze/routers/agent_push.py src/phaze/routers/agent_s3.py` shows the at-cap branches no longer hard-fail
- `just integration-test` full suite green (2566+ baseline, no test lost), 85% coverage floor
- `uv run mypy src/phaze` clean; `pre-commit run --files <changed>` clean (never --no-verify)
</verification>

<success_criteria>
The ledger recovery has exactly one owner per backend kind (SCHED-05, no double-recovery), and the compute + kueue-upload callbacks spill cloud failures back to AWAITING_CLOUD to fall to local (SCHED-03) instead of hard-failing — completing the Phase-69 tiered scheduler.
</success_criteria>

<output>
Create `.planning/phases/69-tiered-drain-scheduler/69-04-SUMMARY.md` when done.
</output>
