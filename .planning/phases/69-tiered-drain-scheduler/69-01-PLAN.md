---
phase: 69-tiered-drain-scheduler
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/config.py
  - src/phaze/services/backend_selection.py
  - tests/analyze/services/test_backend_selection.py
  - tests/shared/config/test_cloud_spill_to_local.py
autonomous: true
requirements: [SCHED-01, SCHED-03, SCHED-04]

must_haves:
  truths:
    - "select_backend picks the available lowest-rank backend whose remaining slot > 0, per candidate file (SCHED-01)"
    - "D-01: local (rank-99) is NOT eligible while higher-rank backends are online-but-FULL until the file has waited past the staleness threshold"
    - "D-03: when every non-local backend is OFFLINE, local becomes eligible immediately (offline->local path is NOT staleness-gated)"
    - "D-04: a file whose cloud_attempts >= cloud_submit_max_attempts is excluded from cloud/kueue candidates and routes to local only"
    - "D-06: stateless re-rank — select_backend carries no per-file 'last-failed backend' memory; a failed backend may be re-picked, bounded only by the D-04 attempt-count eligibility filter"
    - "SCHED-04: equal-rank backends are tie-broken by lowest in_flight/cap utilization then stable id"
    - "select_backend returns None (hold, never raises) when no candidate is eligible"
    - "D-02: cloud_spill_to_local_after_seconds config knob exists, default 900, bounded gt=0/lt=86400"
  artifacts:
    - path: "src/phaze/services/backend_selection.py"
      provides: "pure select_backend selection policy"
      contains: "def select_backend"
      min_lines: 40
    - path: "src/phaze/config.py"
      provides: "cloud_spill_to_local_after_seconds bounded int knob"
      contains: "cloud_spill_to_local_after_seconds"
    - path: "tests/analyze/services/test_backend_selection.py"
      provides: "select_backend unit suite (rank-first, staleness, attempt-exclusion, tie-break)"
    - path: "tests/shared/config/test_cloud_spill_to_local.py"
      provides: "config default/env-alias/bounds test"
  key_links:
    - from: "src/phaze/services/backend_selection.py"
      to: "src/phaze/config.py"
      via: "reads cfg.cloud_spill_to_local_after_seconds + cfg.cloud_submit_max_attempts"
      pattern: "cloud_spill_to_local_after_seconds"
    - from: "src/phaze/services/backend_selection.py"
      to: "src/phaze/services/backends.py"
      via: "isinstance(slot backend, LocalBackend) local detection"
      pattern: "LocalBackend"
---

<objective>
Build the pure, synchronous, fully-typed `select_backend` decision function (the single genuinely-new artifact of Phase 69) plus the one new config knob it consumes. This is the selection *policy* over the Phase-68 `Backend` substrate: rank-first eligible dispatch (SCHED-01), the D-01/D-02/D-03 staleness gate on local spill, the D-04 attempt-exclusion, and the SCHED-04 utilization+id tie-break. No I/O — the drain (Plan 02) supplies a once-per-tick snapshot and each candidate's cloud attempt count.

Purpose: Isolate the load-bearing routing logic in a pure function so it is exhaustively unit-testable with no DB, before it is wired into the drain tick (Plan 02 consumes it).
Output: `src/phaze/services/backend_selection.py`, the `cloud_spill_to_local_after_seconds` knob, and two new unit suites (Wave-0 for SCHED-01/03/04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-tiered-drain-scheduler/69-CONTEXT.md
@.planning/phases/69-tiered-drain-scheduler/69-RESEARCH.md
@.planning/phases/69-tiered-drain-scheduler/69-PATTERNS.md
@.planning/phases/69-tiered-drain-scheduler/69-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase; use directly, no exploration. -->

The snapshot the drain (Plan 02) will pass, one entry per resolved backend (define a TypedDict/dataclass `BackendSlot`; keep it in backend_selection.py and export it so Plan 02 imports it):
  BackendSlot = { "backend": Backend, "available": bool, "remaining": int, "cap": int }
  snapshot: dict[str, BackendSlot]   # keyed by backend.id

Backend attributes available (src/phaze/services/backends.py `_BaseBackend`): `.id: str`, `.rank: int`, `.cap: int`.
LocalBackend is a concrete subclass — detect local by `isinstance(slot["backend"], LocalBackend)`, NOT `rank == 99` (RESEARCH Open Q4, line 519). Import LocalBackend at runtime; backends.py does NOT import backend_selection, so there is no cycle.

FileRecord.updated_at (src/phaze/models/base.py `TimestampMixin`): `server_default=func.now(), onupdate=func.now()` — for a file parked in AWAITING_CLOUD this equals its entry timestamp (RESEARCH Q2).

Config idiom to copy verbatim (src/phaze/config.py:562-568 `cloud_route_threshold_sec`):
  Field(default=..., gt=0, lt=86400, validation_alias=AliasChoices("PHAZE_...", "..."), description=...)
`cloud_submit_max_attempts` already exists (config.py:593-599, default 3, gt=0 lt=20) — reuse it as the cloud attempt budget; do NOT add a new attempts knob.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cloud_spill_to_local_after_seconds config knob (D-02)</name>
  <files>src/phaze/config.py, tests/shared/config/test_cloud_spill_to_local.py</files>
  <read_first>
    - src/phaze/config.py:555-599 (the `cloud_route_threshold_sec` + `cloud_submit_max_attempts` bounded-int Field idioms — copy the exact shape)
    - tests/shared/config/test_cloud_route_threshold.py (the 4-cell pure config-test analog: default / env-alias / rejects-zero / rejects-too-large)
    - 69-PATTERNS.md § "MOD src/phaze/config.py" (lines 182-194 — the exact Field spec)
  </read_first>
  <action>
    Add a new bounded int field `cloud_spill_to_local_after_seconds` to `ControlSettings` in src/phaze/config.py, mirroring `cloud_route_threshold_sec` verbatim: `default=900` (15 min), `gt=0`, `lt=86400`, `validation_alias=AliasChoices("PHAZE_CLOUD_SPILL_TO_LOCAL_AFTER_SECONDS", "cloud_spill_to_local_after_seconds")`, and a description stating it is the seconds a long file waits in AWAITING_CLOUD while higher-rank backends are FULL before slow local becomes an eligible spill target (Phase 69, D-02; offline backends spill immediately per D-03). Place it next to `cloud_submit_max_attempts`. Do NOT touch or re-add `cloud_max_in_flight` — it was already retired in Phase 67 (REG-04, D-05); confirm via grep it does not reappear. Write tests/shared/config/test_cloud_spill_to_local.py mirroring test_cloud_route_threshold.py: asserts the default is 900, the `PHAZE_CLOUD_SPILL_TO_LOCAL_AFTER_SECONDS` env alias overrides it, a value of 0 raises ValidationError, and a value >= 86400 raises ValidationError.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/config/test_cloud_spill_to_local.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `ControlSettings().cloud_spill_to_local_after_seconds == 900`
    - Setting env `PHAZE_CLOUD_SPILL_TO_LOCAL_AFTER_SECONDS=1200` yields 1200
    - Values 0 and 86400 both raise pydantic ValidationError (bounds enforced fail-fast)
    - `grep -n "cloud_max_in_flight" src/phaze/config.py` returns nothing (D-05 retirement intact)
    - `uv run mypy src/phaze/config.py` clean
  </acceptance_criteria>
  <done>The staleness knob exists, is bounded and env-aliased, and its pure config test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure select_backend selection policy (SCHED-01/03/04)</name>
  <files>src/phaze/services/backend_selection.py, tests/analyze/services/test_backend_selection.py</files>
  <behavior>
    - rank-first: given two available backends rank 0 (remaining>0) and rank 5, returns the rank-0 backend
    - spill-when-full: rank-0 backend remaining==0, rank-5 remaining>0 -> returns rank-5 (SCHED-01 per-candidate spill)
    - D-03 offline->local immediate: all non-local backends unavailable, local available, file just entered AWAITING_CLOUD (updated_at == now) -> returns local (no wait)
    - D-01 full->local gated: non-local backends available-but-remaining==0, file waited < threshold -> local NOT returned (returns None/hold); same file waited >= threshold -> returns local
    - D-04 attempt-exclusion: cloud_attempts >= cfg.cloud_submit_max_attempts -> only local eligible (returns local even with cloud slots free)
    - D-06 stateless re-rank: select_backend receives no last-failed-backend argument and holds no per-file failure state; given identical inputs it re-picks the same lowest-rank-available backend (a previously-failed backend is re-picked unless the D-04 attempt filter excludes it)
    - SCHED-04 tie-break: two available equal-rank backends, one at lower in_flight/cap utilization -> returns the lower-utilization one; equal utilization -> returns the lexicographically-smaller stable id
    - hold: no eligible backend -> returns None (never raises)
  </behavior>
  <read_first>
    - 69-RESEARCH.md § "Pattern 2: Rank-first eligible selection" (lines 147-182 — the canonical algorithm, staleness full-vs-offline subtlety) and § "Novel Mechanism 3" (lines 295-343 — attempt-exclusion as an eligibility filter, Reading 1 total-cloud budget)
    - 69-PATTERNS.md § "NEW src/phaze/services/backend_selection.py" (lines 29-52 — module-header/import idiom, local-detection subtlety, snapshot shape)
    - src/phaze/tasks/reenqueue.py:202-240 (`_natural_id`/`is_domain_completed` — the repo idiom for a pure, synchronous, fully-typed decision helper with no I/O)
    - src/phaze/services/backends.py:143-208 (`_BaseBackend` id/rank/cap + `LocalBackend` — the isinstance target)
    - tests/analyze/services/test_backends.py (backend construction helpers `_local()`/`_compute()`/`_kueue()` to build snapshot fixtures)
    - tests/shared/config/test_cloud_route_threshold.py (pure-no-DB test structure)
  </read_first>
  <action>
    Create src/phaze/services/backend_selection.py with the module header + import idiom from PATTERNS (from __future__ import annotations; type-only imports of ControlSettings/FileRecord/Backend under TYPE_CHECKING; structlog logger). Define and export `BackendSlot` (a TypedDict with keys `backend: Backend`, `available: bool`, `remaining: int`, `cap: int`) and the pure function `select_backend(file: FileRecord, cloud_attempts: int, snapshot: dict[str, BackendSlot], now: datetime, cfg: ControlSettings) -> Backend | None`. NOTE the signature deviates from RESEARCH's `file.cloud_attempts` pseudocode: attempts live on `cloud_job`, not FileRecord, so the drain passes `cloud_attempts` explicitly (mypy-strict clean) — the function reads `file.updated_at` off the model and `cloud_attempts` off the parameter. The function is STATELESS (D-06): it takes no "last-failed backend" argument and stores nothing per-file across ticks — a failed backend is re-picked normally, bounded only by the D-04 attempt filter. Implement exactly the RESEARCH Pattern-2 steps: (1) eligible = slots that are available AND remaining>0; (2) D-04 attempt-exclusion — if `cloud_attempts >= cfg.cloud_submit_max_attempts`, restrict eligible to local-only (`isinstance(slot["backend"], LocalBackend)`), local is never excluded; (3) D-01/D-03 staleness gate — compute `any_non_local_online` (any non-local slot available); `waited = (now - file.updated_at).total_seconds() >= cfg.cloud_spill_to_local_after_seconds`; local is eligible only when `(not any_non_local_online) or waited or cloud_attempts >= cfg.cloud_submit_max_attempts`, otherwise drop all local slots from eligible; (4) if eligible empty return None (hold); else sort by `(rank, (cap - remaining)/cap, id)` and return `eligible[0]["backend"]`. Detect local via a runtime `from phaze.services.backends import LocalBackend` (no cycle — backends.py does not import this module). NEVER raise; every no-decision path returns None. Fully type-annotated for mypy strict. Write tests/analyze/services/test_backend_selection.py as the RED-first suite covering every case in <behavior> (build snapshots with the `_local/_compute/_kueue` helpers; construct FileRecord stubs with controllable `updated_at`; use test names containing `rank`, `spill`, `stale`, `attempt`, `stateless`, `tiebreak`, `hold` so the VALIDATION `-k` filters bind).
  </action>
  <verify>
    <automated>uv run pytest tests/analyze/services/test_backend_selection.py -x</automated>
  </verify>
  <acceptance_criteria>
    - All <behavior> cases pass; `-k stale`, `-k attempt`, `-k tiebreak` each select at least one test
    - `select_backend` performs no `await`/DB access (pure sync function) and returns `Backend | None`
    - D-06 stateless: `select_backend`'s signature has no last-failed/history parameter and the module holds no module-level per-file state; a `-k stateless` test proves identical inputs yield the identical pick after a simulated prior failure
    - Local detection uses `isinstance(..., LocalBackend)`, not a `rank == 99` literal (`grep -n "== 99" src/phaze/services/backend_selection.py` returns nothing)
    - `uv run mypy src/phaze/services/backend_selection.py` clean; `uv run ruff check src/phaze/services/backend_selection.py` clean
  </acceptance_criteria>
  <done>select_backend encodes rank-first + staleness (D-01/D-03) + attempt-exclusion (D-04) + stateless re-rank (D-06) + tie-break (SCHED-04) as a pure function, fully covered by a green unit suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config -> ControlSettings | the one new bounded int knob crosses here at startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01-01 | Tampering (V5 Input Validation) | `cloud_spill_to_local_after_seconds` | mitigate | Bounded pydantic Field (gt=0, lt=86400) fails fast at startup — mirrors existing cloud knobs; no unbounded/negative wait reaches selection |
| T-69-01-02 | DoS (config-driven retry storm) | attempt-exclusion in select_backend | mitigate | Exclusion filter keys off the already-bounded `cloud_submit_max_attempts` (gt=0, lt=20); an exhausted file is deterministically forced to local, terminating cloud thrash |
| T-69-01-03 | Info disclosure | selection logging | accept | Pure function logs nothing sensitive; only `{id, kind, rank, cap}` ever logged downstream (Phase-68 discipline). No secrets in scope. |
</threat_model>

<verification>
- `uv run pytest tests/analyze/services/test_backend_selection.py tests/shared/config/test_cloud_spill_to_local.py -x` green
- `uv run mypy src/phaze/config.py src/phaze/services/backend_selection.py` clean
- `pre-commit run --files src/phaze/config.py src/phaze/services/backend_selection.py tests/analyze/services/test_backend_selection.py tests/shared/config/test_cloud_spill_to_local.py` clean (frozen SHAs, never --no-verify)
</verification>

<success_criteria>
select_backend is a pure, mypy-strict, fully-unit-tested decision function encoding SCHED-01/03/04 + D-01..D-04 + D-06, and the D-02 staleness knob exists bounded — ready for Plan 02 to consume in the drain tick.
</success_criteria>

<output>
Create `.planning/phases/69-tiered-drain-scheduler/69-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
