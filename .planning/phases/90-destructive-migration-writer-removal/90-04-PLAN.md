---
phase: 90-destructive-migration-writer-removal
plan: 04
type: execute
wave: 4
depends_on: [90-01, 90-02, 90-03]
files_modified:
  - src/phaze/services/shadow_compare.py
  - src/phaze/cli/shadow_compare.py
  - src/phaze/models/file.py
  - src/phaze/models/__init__.py
  - src/phaze/config.py
  - tests/shared/test_no_filestate_guard.py
  - tests/buckets.json
  - tests/integration/test_migrations/test_migration_039_drop_files_state_column.py
autonomous: true
requirements: [MIG-04]

must_haves:
  truths:
    - "The FileState enum class, the `state` mapped column, and Index('ix_files_state','state') are deleted from models/file.py; models/__init__ no longer re-exports FileState; config.py comment tidied. mypy + ruff + import are the PRIMARY anti-drift guard (D-08) and MUST be green after removal — no src/phaze code references FileState."
    - "The now-dead shadow_compare subsystem is fully removed: services/shadow_compare.py + cli/shadow_compare.py + their dedicated tests and the FileState-mutating divergence tests (test_pending_set_divergence, test_dedup_divergence, test_drain_double_dispatch, and the shadow arm of test_dedup_resolve_undo_shadow). Its purpose (compare legacy files.state vs derived) is void once the column is dropped; its HARD invariants already live FROZEN inside 039's guard (D-07), so removal loses no safety."
    - "The ~90 dependent test files are migrated off FileState/`state=` seeds to the derived authority PR-A/PR-B established (a central make_file/seed lever + import swaps) — never by deleting coverage. Full suite green at the 90% floor."
    - "D-08: ONE mutation-tested source-grep guard (tests/shared/test_no_filestate_guard.py) forbids FileState / files.state / .state = / .values(state= (incl. the multi-line and .values(**splat) forms) from reappearing in src/phaze, with a planted-match self-test AND a documented manual mutation run that went RED then restored (a GREEN guard proves nothing — memory feedback_mutation_test_guard_tests). Registered in tests/buckets.json (shared bucket)."
    - "test_039_autogenerate_diff_is_empty_for_dropped_objects flips GREEN once the model column/index are removed — models and migration 039 are consistent (alembic autogenerate empty diff for files.state / ix_files_state)."
  artifacts:
    - path: "src/phaze/models/file.py"
      provides: "FileState class + state column + ix_files_state index removed"
    - path: "tests/shared/test_no_filestate_guard.py"
      provides: "Mutation-tested source-grep anti-drift guard (D-08)"
      contains: "FileState"
  key_links:
    - from: "tests/shared/test_no_filestate_guard.py"
      to: "src/phaze/**"
      via: "source-grep forbidding FileState/files.state/.state =/.values(state="
      pattern: "FileState"
---

<objective>
90-04 completes PR-C (MIG-04): the model/enum retirement + subsystem cleanup that was split out of
90-03 at a decision checkpoint when its real blast radius (90 test files, 718 FileState refs, 328
`state=` seed sites, plus a 14-file shadow_compare subsystem) proved 4-5x the original plan's budget.
Migration 039 (the irreversible drop) already landed in 90-03 — this plan removes the now-dead
`FileState` enum, `state` column, and `ix_files_state` index from the ORM, retires the dead
`shadow_compare` subsystem, migrates the ~90 dependent test files to derived sources, adds the
mutation-tested anti-drift guard, and flips the one expected-RED autogenerate test to GREEN.

Depends on 90-03 (migration 039 must be present so model↔DB end consistent). This is a large but
mostly-mechanical structural change; the one judgement call — full removal of shadow_compare — is
justified because the column it shadows is gone and its hard invariants are frozen into 039's guard.

**Optional enrichment:** this plan was authored by the execute-phase orchestrator from the 90-03
checkpoint. Run `/gsd:plan-phase 90` (or `/gsd:discuss-phase 90`) first if you want research /
pattern-map / plan-check rigor or want to reconsider the shadow_compare full-removal default.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-destructive-migration-writer-removal/90-CONTEXT.md
@.planning/phases/90-destructive-migration-writer-removal/90-RESEARCH.md
@.planning/phases/90-destructive-migration-writer-removal/90-PATTERNS.md
@.planning/phases/90-destructive-migration-writer-removal/90-01-SUMMARY.md
@.planning/phases/90-destructive-migration-writer-removal/90-02-SUMMARY.md
@.planning/phases/90-destructive-migration-writer-removal/90-03-SUMMARY.md

<facts>
<!-- Verified at the 90-03 checkpoint. -->
- Migration 039 is LANDED (alembic/versions/039_drop_files_state_column.py) — do NOT touch it. Its guard
  transcribed the shadow_compare HARD invariants inline (D-07), so those invariants persist frozen.
- Blast radius (measured on phase HEAD): `grep -rl FileState src/phaze | wc -l` ≈ 18 src files;
  `grep -rl FileState tests | wc -l` ≈ 90 test files; shadow_compare subsystem ≈ 14 files
  (`grep -rl 'shadow_compare\|shadow-compare' src/phaze tests`).
- models/file.py: FileState(enum.StrEnum) class, `state: Mapped[str] = mapped_column(String(30), ...)`,
  and Index("ix_files_state","state") in __table_args__. models/__init__.py re-exports FileState (import + __all__).
  config.py has a comment-only FileState reference.
- The one currently-RED test: tests/integration/test_migrations/test_migration_039_drop_files_state_column.py
  ::test_039_autogenerate_diff_is_empty_for_dropped_objects — RED until the model column/index are removed here.
- Test-DB footgun (memory reference_migrations_test_db_port): `just test-bucket` does NOT export the DB URLs.
  Export both at :5433 with the +asyncpg scheme before any DB test:
    export TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_test"
    export MIGRATIONS_TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_migrations_test"
  Bare postgresql:// fails with ModuleNotFoundError: psycopg2. `just test-db` provisions the containers.
- Anti-drift analog: tests/shared/test_no_raw_state_render.py (vacuous-glob assert, planted-match self-test,
  comment-strip). The guard MUST cover the multi-line `.values(\n state=...)` and `.values(**splat)` forms
  (memory feedback_mutation_test_guard_tests — Phase 83 shipped two toothless guards; mutate every syntactic form).
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire the dead shadow_compare subsystem</name>
  <files>src/phaze/services/shadow_compare.py, src/phaze/cli/shadow_compare.py</files>
  <read_first>
    - src/phaze/services/shadow_compare.py (INVARIANTS + the state-vs-derived comparison — now void)
    - src/phaze/cli/shadow_compare.py (CLI entrypoint + any registration in the CLI group)
    - all shadow_compare tests: `grep -rl 'shadow_compare\|shadow-compare' tests`
    - the FileState-mutating divergence tests: test_pending_set_divergence.py, test_dedup_divergence.py,
      test_drain_double_dispatch.py, and the shadow arm of tests/integration/test_dedup_resolve_undo_shadow.py
  </read_first>
  <action>
    Remove services/shadow_compare.py and cli/shadow_compare.py and any CLI/registration wiring that references
    them (typer/click command registration, __init__ exports, docs/runbook references in code comments). The
    subsystem compares the legacy files.state column against derived sources; with the column dropped (039) it is
    dead. Its HARD invariants are already frozen inside 039's guard (D-07), so nothing is lost. Delete the
    subsystem's dedicated tests and retire the divergence tests that exist ONLY to exercise the shadow mechanism
    by mutating FileRecord.state (test_pending_set_divergence, test_dedup_divergence, test_drain_double_dispatch,
    and the shadow-compare arm of test_dedup_resolve_undo_shadow — keep any non-shadow assertions in that file that
    validate the derived resolve/undo behavior PR-A established). Do NOT delete coverage of live behavior — only the
    shadow-vs-legacy comparison that no longer has a legacy column to compare. Update tests/buckets.json if any
    removed test file was individually listed.
  </action>
  <verify>
    <automated>test ! -f src/phaze/services/shadow_compare.py && test ! -f src/phaze/cli/shadow_compare.py && echo "shadow_compare removed"; uv run ruff check . 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - services/shadow_compare.py and cli/shadow_compare.py are gone; no src/phaze import references them (`grep -rn 'shadow_compare' src/phaze` == 0).
    - The CLI still loads (`uv run python -c "import phaze.cli"` or the project's CLI entrypoint import) with no dangling command registration.
    - Removed only shadow-vs-legacy tests; live resolve/undo/derived coverage preserved.
  </acceptance_criteria>
  <done>The dead shadow_compare service + CLI + shadow-only tests are removed; no dangling references; ruff green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete FileState enum + state column + ix_files_state; clean residual src refs</name>
  <files>src/phaze/models/file.py, src/phaze/models/__init__.py, src/phaze/config.py</files>
  <read_first>
    - src/phaze/models/file.py (FileState class; state mapped_column; Index("ix_files_state","state") in __table_args__)
    - src/phaze/models/__init__.py (FileState in the file import + __all__)
    - src/phaze/config.py (comment-only FileState reference)
  </read_first>
  <action>
    Delete from models/file.py: the entire `class FileState(enum.StrEnum)`, the `state` mapped_column, and the
    `Index("ix_files_state","state")` entry in __table_args__ (pairs 1:1 with 039's op.drop_index). In
    models/__init__.py remove FileState from the `from phaze.models.file import ...` line and from __all__. Tidy the
    comment-only FileState reference in config.py. Then remove every residual `FileState` import/reference across
    src/phaze/** — run `uv run mypy . && uv run ruff check .` and clean iteratively until green (the files_modified
    list is a FLOOR; touch whatever the compiler flags). After PR-A/PR-B there should be no LIVE use, so residuals
    are dead imports/annotations.
  </action>
  <verify>
    <automated>uv run mypy . && uv run ruff check . && echo "src clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class FileState" src/phaze/models/file.py` == 0; the state column + ix_files_state index are gone.
    - `grep -rn "FileState" src/phaze | grep -v '#'` == 0 (no live src reference; comments allowed only where the guard strips them).
    - `uv run mypy .` and `uv run ruff check .` both clean.
  </acceptance_criteria>
  <done>FileState/state column/ix_files_state deleted, re-export removed, all residual src refs cleaned, mypy+ruff green.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the ~90 dependent test files to derived sources; flip the 039 autogen test GREEN</name>
  <files>tests/integration/test_migrations/test_migration_039_drop_files_state_column.py</files>
  <read_first>
    - 90-01-SUMMARY.md + 90-02-SUMMARY.md (the derived authority: stage markers, cloud_job status, analysis_completed_at/failed_at, DedupResolution marker — how PR-A/PR-B migrated their own test fallout)
    - the central test seed helper (make_file / factory) the suite uses — extend it as the single lever
    - `grep -rl FileState tests` (the ~90 files to migrate)
  </read_first>
  <action>
    Migrate the ~90 test files off `FileState` / `state=` seeds to the derived sources PR-A/PR-B established.
    Prefer a CENTRAL lever: if a shared make_file/factory seeds `state=`, remove that param and seed the derived
    marker(s) once there, so most call sites need only an import swap or nothing. For per-file literals, replace
    `state=FileState.X`/`state="x"` seeds with the equivalent derived-source seed (marker rows / cloud_job status /
    analysis timestamps) — following exactly how PR-A/PR-B migrated their fallout (do NOT delete assertions; assert
    the DERIVED result). Drop now-unused `FileState` imports. Run the full suite bucket-by-bucket (export the :5433
    DB URLs) until green at the 90% floor. The autogen-diff test (test_039_autogenerate_diff_is_empty_for_dropped_objects)
    goes GREEN automatically once Task 2 removed the model column/index — confirm it passes.
  </action>
  <verify>
    <automated>export TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_test"; export MIGRATIONS_TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_migrations_test"; uv run pytest tests/integration/test_migrations/test_migration_039_drop_files_state_column.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl FileState tests | wc -l` == 0 (all test references migrated or removed).
    - test_039_autogenerate_diff_is_empty_for_dropped_objects passes GREEN.
    - Every bucket green with the :5433 DB URLs exported; combined coverage ≥ 90% floor.
  </acceptance_criteria>
  <done>All ~90 test files migrated to derived sources, no FileState reference remains in tests, autogen test GREEN, full suite green at 90%.</done>
</task>

<task type="auto">
  <name>Task 4: Mutation-tested anti-drift guard (D-08) + buckets.json</name>
  <files>tests/shared/test_no_filestate_guard.py, tests/buckets.json</files>
  <read_first>
    - tests/shared/test_no_raw_state_render.py (vacuous-glob assert; planted-match self-test; comment-strip)
    - memory feedback_mutation_test_guard_tests (Phase 83 shipped toothless guards; cover multi-line + .values(**splat))
  </read_first>
  <action>
    Create tests/shared/test_no_filestate_guard.py modeled on test_no_raw_state_render.py: resolve the src/phaze root,
    assert the scanned glob is NON-EMPTY (no vacuous pass), strip comments/docstrings, then `test_no_filestate_in_src`
    asserting ZERO matches for the forbidden forms — `FileState`, `files\.state`, `\.state\s*=`, the multi-line
    `\.values\(\s*state\s*=` form, AND `.values(**...)` splat forms that could smuggle a state key — across
    src/phaze/**. Add `test_guard_flags_planted_matches`: assert the regex set MATCHES a planted string of EACH
    forbidden form (including a multi-line `.values(\n    state=...)` and a `.values(**payload)` where payload holds
    state) and does NOT match legitimate lookalikes (`stage_status`, `cloud_job.status`, unrelated `.state`).
    MANUAL mutation check (document in SUMMARY per feedback_mutation_test_guard_tests): temporarily reintroduce a
    real `.values(state=...)` into a src file, RE-RUN the guard, CONFIRM RED, RESTORE, confirm GREEN. Register the
    new test in tests/buckets.json (shared bucket).
  </action>
  <verify>
    <automated>uv run pytest tests/shared/test_no_filestate_guard.py -q && uv run python -c "import json; b=json.load(open('tests/buckets.json')); assert 'shared' in b" && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - test_no_filestate_guard.py passes; its planted-match self-test proves the regex catches FileState / files.state / .state = / multi-line .values(state=) / .values(**splat).
    - A documented manual mutation run went RED then GREEN on restore (recorded in SUMMARY).
    - The guard is registered so `just test-bucket shared` runs it; buckets.json still valid JSON.
  </acceptance_criteria>
  <done>A mutation-verified anti-drift guard forbids any FileState/state reappearance in src; registered in buckets.json.</done>
</task>

</tasks>

<verification>
- `uv run mypy . && uv run ruff check .` clean; no FileState reference in src/phaze or tests.
- shadow_compare subsystem fully removed with no dangling imports/CLI registration.
- `export ...:5433 DB URLs; uv run pytest` (bucket-by-bucket) full suite green at the 90% floor, incl. the now-GREEN
  test_039_autogenerate_diff_is_empty_for_dropped_objects.
- test_no_filestate_guard.py green incl. planted-match self-test; documented manual mutation run RED→GREEN.
- `uv run alembic check` / autogenerate shows an empty diff for files.state / ix_files_state (models ⇔ 039 consistent).
</verification>

<success_criteria>
FileState enum + state column + ix_files_state index + re-export deleted; the dead shadow_compare subsystem retired
without losing frozen-invariant safety; ~90 test files migrated to derived sources (no coverage deleted); the
mutation-tested anti-drift guard forbids any reappearance; the 039 autogenerate test is GREEN; full suite green at
90%. Phase 90 (MIG-04) is then complete and ready for verification.
</success_criteria>

<output>
Create `.planning/phases/90-destructive-migration-writer-removal/90-04-SUMMARY.md` when done.
</output>
</content>
</invoke>
