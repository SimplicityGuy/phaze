---
phase: 90-destructive-migration-writer-removal
plan: 03
type: execute
wave: 3
depends_on: [90-01, 90-02]
files_modified:
  - alembic/versions/039_drop_files_state_column.py
  - tests/integration/test_migrations/test_migration_039_drop_files_state_column.py
  - tests/shared/test_no_filestate_guard.py
  - src/phaze/models/file.py
  - src/phaze/models/__init__.py
  - src/phaze/config.py
autonomous: true
requirements: [MIG-04]

must_haves:
  truths:
    - "The destructive migration 039 runs in ONE transaction: archives files.state into files_state_archive, applies an idempotent delta top-up for anything changed since 032, then drops ix_files_state and files.state under a lock_timeout + savepoint-retry wrapper so the ACCESS EXCLUSIVE lock aborts-and-retries instead of queuing behind the 5s /pipeline/stats poll (ROADMAP success-criterion 1)."
    - "D-06: 039.upgrade() self-guards ONLY when data exists — it RAISEs (aborts the txn) on mid-flight rows (files.state IN ('pushing','uploading') OR non-terminal cloud_job) OR on shadow-compare implication violations, but runs clean on an empty/fresh DB (avoids the Phase-89 038 CR-02 fresh-DB-abort footgun)."
    - "D-07: the shadow-compare precondition is INLINE sync SQL transcribed from shadow_compare.INVARIANTS (hard invariants only; fingerprinted/local_analyzing soft-allowlisted) — the migration does NOT import services/shadow_compare.py and never references saq_jobs or the scheduling ledger."
    - "D-10: downgrade() restores files.state VERBATIM from files_state_archive (lossless primary); D-04/D-05 derived furthest-along + marker-override reconstruction is the documented FALLBACK for rows absent from the archive (created after 039), with lossy transient cases enumerated in the docstring."
    - "The FileState enum class, the state column mapping, and Index('ix_files_state','state') are deleted from models/file.py; models/__init__ no longer re-exports FileState; config.py:619 comment tidied; no src/ code references FileState (mypy/ruff/import are the primary anti-drift guard, D-08)."
    - "D-08: ONE mutation-tested source-grep guard forbids FileState / files.state / .state = / .values(state= from reappearing in src/, with a planted-match self-test that goes RED (a GREEN guard proves nothing)."
    - "Deploy ordering respected: prod is at Alembic 031 (032-038 unreleased); runbook order is deploy through 038 -> shadow-compare green on the drained live corpus -> THEN 039. 039 is never the first migration prod sees."
  artifacts:
    - path: "alembic/versions/039_drop_files_state_column.py"
      provides: "Destructive migration: archive + delta top-up + guarded DDL drop + lossless-archive downgrade"
      contains: "files_state_archive"
    - path: "tests/integration/test_migrations/test_migration_039_drop_files_state_column.py"
      provides: "upgrade guard (violation->raise, empty->pass, mid-flight->raise) + archive populated + DDL gone + downgrade restores"
    - path: "tests/shared/test_no_filestate_guard.py"
      provides: "Mutation-tested source-grep anti-drift guard"
    - path: "src/phaze/models/file.py"
      provides: "FileState class + state column + ix_files_state index removed"
  key_links:
    - from: "alembic/versions/039_drop_files_state_column.py upgrade()"
      to: "files / cloud_job / analysis / metadata / dedup_resolution / proposals"
      via: "inline anti-join COUNT guard SQL (sa.text + bindparams, no saq_jobs)"
      pattern: "files_state_archive"
    - from: "039 downgrade()"
      to: "files_state_archive"
      via: "UPDATE files SET state FROM archive (verbatim restore, D-10)"
      pattern: "files_state_archive"
    - from: "tests/shared/test_no_filestate_guard.py"
      to: "src/phaze/**"
      via: "source-grep forbidding FileState/files.state/.state =/.values(state="
      pattern: "FileState"
---

<objective>
PR-C, the irreversible finale (MIG-04). Now that PR-A made all readers derived and PR-B removed all writers,
the `files.state` column is truly dead. Write the destructive `039` migration (archive + delta top-up +
guarded, lock-timeout-wrapped DDL drop + lossless archive-restore downgrade), delete the `FileState` enum /
state column / `ix_files_state` index, and add the mutation-tested anti-drift guard. This is the ONLY
irreversible action in the phase and is quarantined behind two independently-green PRs.

Purpose: retire the last vestige of the linear FileState model; satisfy ROADMAP success-criteria 1-3.
Output: `039` migration + its integration test + the source-grep guard + model/enum deletion.

**Deploy gate (runbook, note in SUMMARY):** prod is at Alembic 031; 032-038 are unreleased. `039` must NOT be
prod's first migration — deploy through 038, run shadow-compare green on the DRAINED live corpus
(`--profile drain`), THEN run `039`. Rehearsal against a real-corpus restore (RESEARCH Pattern 5 layer 2) is a
manual operator step recorded in VERIFICATION.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-destructive-migration-writer-removal/90-CONTEXT.md
@.planning/phases/90-destructive-migration-writer-removal/90-RESEARCH.md
@.planning/phases/90-destructive-migration-writer-removal/90-PATTERNS.md
@.planning/phases/90-destructive-migration-writer-removal/90-01-SUMMARY.md
@.planning/phases/90-destructive-migration-writer-removal/90-02-SUMMARY.md

<migration_facts>
<!-- Verified this planning session. -->
- Head is 038; next revision id = "039", down_revision = "038". files.state is String(30) (models/file.py:86) — NOT a PG enum, so NO DROP TYPE; drop is just op.drop_index('ix_files_state', table_name='files') + op.drop_column('files','state').
- alembic/env.py does NOT set transaction_per_migration; a single revision runs inside ONE outer begin_transaction() (env.py:67). A per-attempt begin_nested() SAVEPOINT correctly scopes SET LOCAL lock_timeout and rolls back ONLY the failed attempt, leaving the outer txn usable (resolves RESEARCH Assumption A6).
- Analog template: alembic/versions/038_retire_legacy_sentinel.py (raw sa.text + bindparams + raise-to-rollback + -x override + "empty autogenerate diff" contract test). NO phaze.services.* imports; NEVER reference saq_jobs / scheduling_ledger. Table is `analysis` (not analysis_results — memory).
- Guard tables allowed: files, cloud_job, analysis, metadata, dedup_resolution, proposals, execution_log. HARD shadow-compare invariants to transcribe (RESEARCH Pattern 3): metadata_extracted, analyzed, analysis_failed, proposal_generated, awaiting_cloud, pushing, pushed, duplicate_resolved, approved, rejected, executed, failed, moved(->executed), unchanged(->failed). SOFT (never gate): fingerprinted, local_analyzing.
- Test harness: tests/integration/test_migrations/conftest.py exposes MIGRATIONS_TEST_DATABASE_URL, _build_alembic_config, _reset_schema, upgrade_to, downgrade_to. FOOTGUN: `just test-bucket integration` does NOT export MIGRATIONS_TEST_DATABASE_URL — export it at :5433 (memory reference_migrations_test_db_port).
- Anti-drift analog: tests/shared/test_no_raw_state_render.py (vacuous-glob assert, planted-match self-test, comment-strip). Guard must also cover the multi-line .values(state=...) form (memory feedback_mutation_test_guard_tests).
</migration_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the 039 migration integration test FIRST (RED)</name>
  <files>tests/integration/test_migrations/test_migration_039_drop_files_state_column.py</files>
  <read_first>
    - tests/integration/test_migrations/test_migration_038_retire_legacy_sentinel.py (footgun header :21-28; path-load helper :81-88; static DB-free asserts :96-117; drive loop :154-178; seed helpers :69-73/:131-146)
    - tests/integration/test_migrations/conftest.py (MIGRATIONS_TEST_DATABASE_URL, _build_alembic_config, _reset_schema, upgrade_to, downgrade_to)
    - RESEARCH Pattern 5 layer 1 + Pattern 3 (guard cases) + Pattern 2 (archive) + Pattern 4 (downgrade restore)
    - PATTERNS "test_migration_039_*.py (test) — NEW, PR-C"
  </read_first>
  <action>
    Create tests/integration/test_migrations/test_migration_039_drop_files_state_column.py modeled on the 038 test. Include the FOOTGUN header comment (export MIGRATIONS_TEST_DATABASE_URL at :5433) and the DB-free static asserts (bare-number revision ids "039"/"038"; a saq_jobs-never-referenced body grep; a "never f-string interpolated operand" body scan). Integration drive loop: `_reset_schema` -> `upgrade_to(cfg,"038")` -> seed representative rows for EVERY durable state (metadata_extracted, analyzed, analysis_failed, proposal_generated, approved, rejected, executed, duplicate_resolved) plus soft/transient cases -> assert cases: (a) HAPPY: on a shadow-compare-consistent seeded corpus `upgrade_to("039")` succeeds, files.state column + ix_files_state are GONE, and files_state_archive is populated with one row per file; (b) EMPTY: on a fresh/empty files table `upgrade_to("039")` passes cleanly (D-06 — no fresh-DB abort); (c) VIOLATION: seed a shadow-compare implication violation (e.g. files.state='analyzed' with no analysis.analysis_completed_at) and assert `upgrade_to("039")` raises RuntimeError; (d) MID-FLIGHT: seed files.state IN ('pushing','uploading') or a non-terminal cloud_job and assert RuntimeError; (e) DOWNGRADE: after a successful upgrade, `downgrade_to(cfg,"038")` recreates the column + ix_files_state and restores durable states VERBATIM from files_state_archive (D-10). Reuse the raw-`text` seed INSERT shape; import helpers from conftest; tear down via `_reset_schema` in `finally`. This task lands the test RED (039 does not exist yet) — that is expected.
  </action>
  <verify>
    <automated>uv run python -c "import ast,pathlib; ast.parse(pathlib.Path('tests/integration/test_migrations/test_migration_039_drop_files_state_column.py').read_text())" && echo "test parses"</automated>
  </verify>
  <acceptance_criteria>
    - The test file exists and parses; collection succeeds (`uv run pytest tests/integration/test_migrations/test_migration_039_drop_files_state_column.py --collect-only` lists the upgrade/downgrade/guard cases).
    - It asserts all five behaviors: happy-drop, empty-DB clean pass, shadow-compare-violation raise, mid-flight raise, downgrade-restore-from-archive.
    - It exports/documents MIGRATIONS_TEST_DATABASE_URL at :5433 and contains a saq_jobs-never-referenced static assert.
    - Running it now fails (RED) because migration 039 is absent — recorded as expected.
  </acceptance_criteria>
  <done>The 039 integration test exists (RED), covering guard/archive/DDL/downgrade per D-06/D-07/D-10.</done>
</task>

<task type="auto">
  <name>Task 2: Write the 039 destructive migration (GREEN)</name>
  <files>alembic/versions/039_drop_files_state_column.py</files>
  <read_first>
    - alembic/versions/038_retire_legacy_sentinel.py (header :60-71; SQL consts + bindparams :78-116; -x read :125; raise-to-rollback body :141-167; docstring constraints :42-53)
    - alembic/env.py (:59-68 — single outer begin_transaction, no transaction_per_migration)
    - src/phaze/services/shadow_compare.py INVARIANTS (reference to TRANSCRIBE, do NOT import)
    - RESEARCH Pattern 1 (lock_timeout + begin_nested savepoint-retry), Pattern 2 (archive + delta top-up), Pattern 3 (inline guard SQL), Pattern 4 (downgrade CASE) + Code Examples
    - PATTERNS "alembic/versions/039_*.py (migration) — NEW, PR-C" (NEW body / No Analog table)
  </read_first>
  <action>
    Create alembic/versions/039_drop_files_state_column.py (revision "039", down_revision "038"), sync, modeled on 038. NO phaze.services.* imports; NEVER reference saq_jobs or scheduling_ledger; parameterize all SQL via sa.text(...).bindparams(...) — the only literal is the fixed lock_timeout constant (bandit -s B608 / S-rules). upgrade() order, ONE transaction: (1) D-06/D-07 GUARD FIRST — an inline mid-flight COUNT (files.state IN ('pushing','uploading') + non-terminal cloud_job status IN ('uploading','submitted','running')) and one anti-join COUNT summing the HARD shadow-compare implication invariants transcribed from shadow_compare.INVARIANTS (analyzed/metadata_extracted/analysis_failed/duplicate_resolved/awaiting_cloud/pushing+pushed/proposal_generated/approved+rejected+executed+failed via proposals with the moved->executed, unchanged->failed CASE mapping); RAISE RuntimeError if either count > 0; because every COUNT is 0 on an empty DB, a fresh DB passes cleanly (D-06 — no CR-02 fresh-DB abort). Do NOT gate on fingerprinted/local_analyzing (soft allowlist). (2) ARCHIVE (Pattern 2, D-10): CREATE TABLE files_state_archive(file_id uuid PRIMARY KEY, state varchar(30) NOT NULL, archived_at timestamptz NOT NULL DEFAULT now()); INSERT ... SELECT id, state FROM files. (3) DELTA top-up: idempotent `INSERT ... ON CONFLICT DO NOTHING` for the three 032-backfilled markers (dedup_resolution, analysis.failed_at, cloud_job awaiting) — on a guard-green corpus this finds nothing (the guard subsumes the delta per RESEARCH Pattern 2). (4) DROP DDL under the wrapper: loop MAX_ATTEMPTS with `with bind.begin_nested(): bind.execute(sa.text("SET LOCAL lock_timeout='2s'")); op.drop_index('ix_files_state', table_name='files'); op.drop_column('files','state')`, catching OperationalError (LockNotAvailable 55P03) and backing off, re-raising on the last attempt (env.py's single-txn mode makes the per-attempt savepoint the correct scope — migration_facts). Optionally read a `-x force=1` escape hatch via context.get_x_argument to skip the guard (Claude's discretion). downgrade() (Pattern 4, D-10 primary + D-04/D-05 fallback): recreate the column (`op.add_column('files', sa.Column('state', sa.String(30), nullable=False, server_default='discovered'))`) + `op.create_index('ix_files_state','files',['state'])`; RESTORE VERBATIM from files_state_archive via `UPDATE files f SET state = a.state FROM files_state_archive a WHERE a.file_id = f.id`; for rows absent from the archive (created after 039) apply the derived FALLBACK `UPDATE ... SET state = CASE ...` (markers-override-then-furthest-along per D-04/D-05); drop the server_default after backfill. Write a docstring enumerating the lossy fallback cases (LOCAL_ANALYZING/PUSHING/PUSHED/AWAITING_CLOUD transients collapse to nearest durable; rollback-FINGERPRINTED reconstructs to its true derived stage; MOVED/UNCHANGED reconstruct as executed/failed) — the round-trip test asserts only the durable set. Make the Task-1 test GREEN.
  </action>
  <verify>
    <automated>MIGRATIONS_TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_migrations_test" uv run pytest tests/integration/test_migrations/test_migration_039_drop_files_state_column.py -x</automated>
  </verify>
  <acceptance_criteria>
    - The migration file exists with `revision = "039"` / `down_revision = "038"`; `grep -c "saq_jobs" alembic/versions/039_drop_files_state_column.py` counting non-comment lines == 0 (`grep -v '^\s*#' ... | grep -c saq_jobs == 0`).
    - `grep -c "files_state_archive" alembic/versions/039_drop_files_state_column.py` >= 3 (create + insert + downgrade restore).
    - `grep -c "begin_nested\|lock_timeout" alembic/versions/039_drop_files_state_column.py` >= 2 (DDL wrapper present).
    - `grep -c "import phaze\|from phaze" alembic/versions/039_drop_files_state_column.py == 0` (frozen-in-time, D-07).
    - The Task-1 integration test passes GREEN for all five cases (happy/empty/violation/mid-flight/downgrade) with MIGRATIONS_TEST_DATABASE_URL at :5433.
    - `uv run alembic upgrade head` autogenerate diff is empty against the post-PR-C models (checked after Task 3).
  </acceptance_criteria>
  <done>039 archives+guards+drops in one txn under a lock_timeout retry and restores verbatim from archive; the integration test is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Delete FileState / state column / index + import cleanup + mutation-tested anti-drift guard (D-08)</name>
  <files>src/phaze/models/file.py, src/phaze/models/__init__.py, src/phaze/config.py, tests/shared/test_no_filestate_guard.py, tests/buckets.json</files>
  <read_first>
    - src/phaze/models/file.py (state column :86; Index('ix_files_state','state') :97; FileState(enum.StrEnum) class :20-71)
    - src/phaze/models/__init__.py (:9 import FileRecord, FileState; :36 "FileState" in __all__)
    - src/phaze/config.py (:619 comment-only FileState.AWAITING_CLOUD reference)
    - tests/shared/test_no_raw_state_render.py (vacuous-glob assert :81; planted-match self-test :87-101; comment-strip :59-64)
    - PATTERNS "models/file.py + models/__init__.py — PR-C schema deletion" + "test_no_filestate_guard.py — NEW, PR-C, D-08"
  </read_first>
  <action>
    Delete from models/file.py: the `state: Mapped[str] = mapped_column(String(30), ...)` column (:86), the `Index("ix_files_state", "state")` entry in `__table_args__` (:97, pairs 1:1 with 039's op.drop_index), and the entire `class FileState(enum.StrEnum)` (:20-71). In models/__init__.py drop `FileState` from the `from phaze.models.file import ...` line (:9) and from `__all__` (:36). Tidy the comment-only `FileState.AWAITING_CLOUD` reference in config.py (:619). Then remove every residual `FileState` import across src/phaze/** — after PR-A/PR-B there should be no live use; mypy/ruff/import (the D-08 PRIMARY guard) surface any missed reference immediately, so run them and clean until green. Create tests/shared/test_no_filestate_guard.py modeled on test_no_raw_state_render.py: resolve the src/phaze root, assert the scanned file glob is non-empty (no vacuous pass), strip comments/docstrings so retired symbols can be named in prose, then a `test_no_filestate_in_src` asserting ZERO matches for the forbidden forms `FileState`, `files\.state`, `\.state\s*=`, and the multi-line `\.values\(\s*state\s*=` form across src/phaze/**; PLUS a `test_guard_flags_planted_matches` self-test asserting the regex set MATCHES a planted string of each forbidden form and does NOT match legitimate lookalikes (e.g. `stage_status`, `cloud_job.status`, a `.state` attribute on an unrelated object if any). Mutation-test manually per memory feedback_mutation_test_guard_tests: temporarily add a fake `.values(state=...)` line to a src file, confirm the guard goes RED, then restore. Register the new test file in tests/buckets.json (shared bucket) per the partition guard.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/test_no_filestate_guard.py -x && uv run mypy . && uv run ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "FileState" src/phaze | grep -v ':0' | grep -c ':'` == 0 (no src/ file references FileState — comment-stripped where the guard allows prose).
    - `grep -c "ix_files_state\|String(30)" src/phaze/models/file.py` reflects the column + index removed; `grep -c "class FileState" src/phaze/models/file.py == 0`.
    - `grep -c "FileState" src/phaze/models/__init__.py == 0`.
    - test_no_filestate_guard.py passes AND its planted-match self-test proves the regex set catches `FileState`, `files.state`, `.state =`, and `.values(state=`; a documented mutation run went RED then restored.
    - `uv run mypy . && uv run ruff check .` clean; `uv run pytest` full suite green at the 90% floor.
    - `uv run alembic check` (or autogenerate) shows an empty diff — models and 039 are consistent.
  </acceptance_criteria>
  <done>FileState/state column/ix_files_state deleted, re-export removed, no src reference remains, mutation-tested guard green, mypy/ruff/full-suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration SQL → live Postgres catalog | destructive DDL + guard/archive SQL execute against the live corpus |
| operator `-x` args → migration | optional force-skip escape hatch crosses into the guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-guard | Tampering/DoS | 039 upgrade drops files.state on a mid-flight / shadow-compare-inconsistent corpus | mitigate | D-06 inline guard (mid-flight COUNT + transcribed HARD-invariant anti-join) RAISEs to roll back the txn before any drop; deploy under `--profile drain`; files_state_archive is a forensic snapshot. |
| T-90-sqli | Tampering | injection via migration / guard SQL | mitigate | All operands parameterized via sa.text().bindparams(); the only literal is the fixed lock_timeout constant; a static test scans the body for f-string interpolation (038 discipline, bandit -s B608). |
| T-90-loss | Info disclosure / data loss | downgrade() cannot reconstruct the exact scalar | mitigate | D-10 downgrade restores VERBATIM from files_state_archive (lossless primary); derived CASE is fallback only for post-039 rows; lossy cases enumerated in the docstring; round-trip test asserts the durable set. |
| T-90-pii | Info disclosure | migration/guard/test output leaks paths | mitigate | Guard + rehearsal emit counts + file_id UUIDs only, never original_path/original_filename (shadow-compare T-79-02 precedent). |
| T-90-frozen | Tampering | migration coupling to mutable app code | mitigate | D-07: NO phaze.services.* imports; never references saq_jobs/scheduling_ledger; a static test asserts both. |
| T-90-SC | Tampering | npm/pip/cargo installs | N/A | Zero new dependencies (design §8); no package installs; no legitimacy gate required. |
</threat_model>

<verification>
- `MIGRATIONS_TEST_DATABASE_URL=...:5433/phaze_migrations_test uv run pytest tests/integration/test_migrations/test_migration_039_drop_files_state_column.py -x` green (all five cases).
- `uv run pytest tests/shared/test_no_filestate_guard.py -x` green incl. planted-match self-test.
- `uv run mypy . && uv run ruff check .` clean; `uv run pytest` full suite green at the 90% floor.
- `uv run alembic upgrade head` then autogenerate/`alembic check` shows an empty diff (models ⇔ 039 consistent).
- MANUAL (operator, runbook — record in VERIFICATION): rehearsal against a real-corpus restore — restore prod snapshot, apply 032-038, run shadow-compare green (drained), run 039, assert column/index gone + files_state_archive row count matches, downgrade, assert durable states restored, record lock-acquisition/DDL timing (RESEARCH Pattern 5 layer 2, memory reference_lux_readonly_pg_probe).
</verification>

<success_criteria>
039 archives files.state + delta-tops-up + drops ix_files_state and files.state in one guarded, lock-timeout-
wrapped transaction; guard aborts on mid-flight / shadow-compare violation and passes clean on empty DB;
downgrade restores verbatim from the archive with documented lossy fallback; FileState enum + column + index +
re-export are deleted; the mutation-tested anti-drift guard forbids any reappearance; full suite green. The
runbook deploy order (through 038 -> shadow-compare green drained -> 039) is documented.
</success_criteria>

<output>
Create `.planning/phases/90-destructive-migration-writer-removal/90-03-SUMMARY.md` when done.
</output>
