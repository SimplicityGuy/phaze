---
phase: 70-multi-kueue-n-clusters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/models/cloud_job.py
  - alembic/versions/030_add_cloud_job_staging_bucket.py
  - src/phaze/services/s3_staging.py
  - src/phaze/config_backends.py
  - pyproject.toml
  - tests/integration/test_migrations/test_migration_030_staging_bucket.py
  - tests/analyze/services/test_s3_staging.py
autonomous: true
requirements: [MKUE-01, MKUE-02, MKUE-04]

must_haves:
  truths:
    - "cloud_job carries a nullable staging_bucket column recording which BucketConfig.id staged the current object (D-01/MKUE-04)"
    - "cloud_job stays one-row-per-file: unique(file_id) is unchanged (D-02/MKUE-04)"
    - "pick_bucket maps a file_id to a stable, restart-invariant bucket id from a sorted bucket-id list (D-06/MKUE-02)"
    - "KubeConfig carries a context field so the kubeconfig+context auth path can select a non-default context (A1/MKUE-01)"
  artifacts:
    - path: "alembic/versions/030_add_cloud_job_staging_bucket.py"
      provides: "additive nullable staging_bucket migration mirroring 029, no backfill"
      contains: "staging_bucket"
    - path: "src/phaze/models/cloud_job.py"
      provides: "staging_bucket Mapped[str | None] column"
      contains: "staging_bucket"
    - path: "src/phaze/services/s3_staging.py"
      provides: "pick_bucket pure D-06 helper (sha256 of file_id bytes mod len(sorted(bucket_ids)))"
      contains: "def pick_bucket"
    - path: "src/phaze/config_backends.py"
      provides: "KubeConfig.context field"
      contains: "context"
  key_links:
    - from: "alembic/versions/030_add_cloud_job_staging_bucket.py"
      to: "cloud_job.staging_bucket"
      via: "op.add_column"
      pattern: "add_column.*staging_bucket"
    - from: "pick_bucket"
      to: "hashlib.sha256(file_id.bytes)"
      via: "stable digest mod sorted bucket ids"
      pattern: "sha256"
---

<objective>
Lay the additive, non-breaking foundation every later Phase-70 plan builds on: the
`cloud_job.staging_bucket` column + migration 030 (D-01/D-02, MKUE-04), the pure `pick_bucket`
deterministic per-file bucket selector (D-06, MKUE-02), and the `KubeConfig.context` field
(A1, MKUE-01). Nothing here changes runtime behavior — all three are additive so the full suite
stays green while later plans (02–05) lift the module-global reads onto these primitives.

Purpose: de-risk the phase's tightly-coupled param-lifting by landing the schema/helper/config
surface first, in isolation, with its own tests.
Output: migration 030 + column, `pick_bucket` + its unit tests, `KubeConfig.context`, PyYAML
declared explicitly in `pyproject.toml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-multi-kueue-n-clusters/70-CONTEXT.md
@.planning/phases/70-multi-kueue-n-clusters/70-RESEARCH.md
@.planning/phases/70-multi-kueue-n-clusters/70-PATTERNS.md
@.planning/phases/70-multi-kueue-n-clusters/70-VALIDATION.md

<interfaces>
<!-- Extracted from HEAD; executor uses these directly, no exploration needed. -->

From src/phaze/config_backends.py (the KubeConfig submodel to extend — A1):
  class KubeConfig(BaseModel):
    api_url: str | None = None
    namespace: str | None = None
    local_queue: str | None = None
    job_image / cpu_request / memory_request: str | None = None
    workload_api_version: str = "kueue.x-k8s.io/v1beta1"
    ca_secret_name / env_configmap_name / env_secret_name: str  (Field default)
    kubeconfig: SecretStr | None = None
    sa_token: SecretStr | None = None
    # ADD: context: str | None = None   (plain str like `namespace`, NOT a secret)

From src/phaze/models/cloud_job.py (the additive column analog is backend_id, L94-98):
    backend_id: Mapped[str | None] = mapped_column(String(255), nullable=True)
    # unique(file_id) FK lives on L72 — DO NOT touch (D-02)

From alembic/versions/029_add_cloud_job_backend_id.py (mirror for 030):
    revision = "029"; down_revision = "028"
    upgrade: op.add_column("cloud_job", sa.Column("backend_id", sa.String(255), nullable=True))
    downgrade: op.drop_column("cloud_job", "backend_id")
    # CRITICAL banner "never reference saq_jobs" — keep it in 030

From src/phaze/services/s3_staging.py (where pick_bucket lands, near the L77 TRANSITIONAL marker):
    class S3StagingError(RuntimeError): ...
    def staged_object_key(file_id: uuid.UUID) -> str  # pure
    # ADD: def pick_bucket(file_id: uuid.UUID, bucket_ids: list[str]) -> str
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cloud_job.staging_bucket column + migration 030 (D-01/D-02, MKUE-04)</name>
  <files>src/phaze/models/cloud_job.py, alembic/versions/030_add_cloud_job_staging_bucket.py, tests/integration/test_migrations/test_migration_030_staging_bucket.py</files>
  <read_first>
    - src/phaze/models/cloud_job.py (the backend_id column at L94-98 is the exact analog; unique(file_id) at L72 is preserved)
    - alembic/versions/029_add_cloud_job_backend_id.py (mirror the revision header + upgrade/downgrade shape; keep the CRITICAL "never reference saq_jobs" banner)
    - tests/integration/test_migrations/test_migration_029_backend_id.py (mirror verbatim; drop the 029-only s3_key NOT-NULL leg)
    - .planning/phases/70-multi-kueue-n-clusters/70-PATTERNS.md (§migration 030 + §cloud_job.py)
  </read_first>
  <behavior>
    - Test: after upgrade, information_schema.columns shows cloud_job.staging_bucket exists with is_nullable == "YES"
    - Test: after downgrade, cloud_job.staging_bucket is absent
    - Test: revision id is the bare string "030", down_revision is "029"
    - Test: the migration source contains no reference to saq_jobs
  </behavior>
  <action>
    Add `staging_bucket: Mapped[str | None] = mapped_column(String(255), nullable=True)` to `CloudJob`
    immediately after `backend_id` (L98), with a docstring noting it records which `BucketConfig.id`
    staged the current object (D-01/D-06), nullable, no backfill, plain free-text (no CHECK/enum), and
    that `unique(file_id)` is preserved (D-02). Do NOT modify `__table_args__` or the `unique(file_id)` FK.
    Create `alembic/versions/030_add_cloud_job_staging_bucket.py` mirroring 029: `revision = "030"`,
    `down_revision = "029"`, `branch_labels`/`depends_on = None`, the CRITICAL "touches ONLY cloud_job /
    never reference saq_jobs" banner. `upgrade` = single `op.add_column("cloud_job", sa.Column("staging_bucket",
    sa.String(255), nullable=True))`; `downgrade` = single `op.drop_column("cloud_job", "staging_bucket")`.
    No `s3_key` leg (029-only), no CHECK change, no backfill (029's "~zero live rows" rationale applies).
    Create the migration test by mirroring `test_migration_029_backend_id.py`: the `_MIGRATION_PATH` +
    skipif guard, the importlib loader, `test_revision_identifiers_are_bare_numbers` asserting "030"/"029",
    `test_migration_never_references_saq_jobs`, the `_seed_file` FK helper, and the upgrade/downgrade
    round-trip probing `information_schema.columns` for `staging_bucket` existence + `is_nullable == "YES"`.
  </action>
  <acceptance_criteria>
    - `src/phaze/models/cloud_job.py` contains `staging_bucket: Mapped[str | None]` and STILL contains `unique=True` on file_id
    - `alembic/versions/030_add_cloud_job_staging_bucket.py` contains `add_column` + `staging_bucket` and the string `saq_jobs` appears only inside the "never reference" banner comment
    - `uv run pytest tests/integration/test_migrations/test_migration_030_staging_bucket.py -x` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/integration/test_migrations/ -k "030 or staging_bucket" -x</automated>
  </verify>
  <done>staging_bucket column exists, migration 030 round-trips nullable with no backfill, unique(file_id) untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add pure pick_bucket deterministic selector (D-06, MKUE-02)</name>
  <files>src/phaze/services/s3_staging.py, tests/analyze/services/test_s3_staging.py</files>
  <read_first>
    - src/phaze/services/s3_staging.py (the L77 TRANSITIONAL marker names MKUE-02 as the landing spot; S3StagingError at L50; staged_object_key at L59 is the pure-helper precedent)
    - .planning/phases/70-multi-kueue-n-clusters/70-RESEARCH.md (§Pattern 2, verified-sound reference impl)
    - .planning/phases/70-multi-kueue-n-clusters/70-PATTERNS.md (§s3_staging.py — the NEW pure helper block)
    - tests/analyze/services/test_s3_staging.py (test module conventions; pick_bucket cases need no moto)
  </read_first>
  <behavior>
    - Test: pick_bucket(file_id, ["b","a","c"]) == pick_bucket(file_id, ["c","b","a"]) (sorted → order-independent)
    - Test: pick_bucket returns the same id across a simulated process restart (compute the sha256-mod by hand for a fixed UUID and assert equality — proves NOT Python's salted hash())
    - Test: pick_bucket(file_id, []) raises S3StagingError
    - Test: over many random UUIDs against a 3-bucket set, every returned id is a member of the set
  </behavior>
  <action>
    Add a module-level pure function `pick_bucket(file_id: uuid.UUID, bucket_ids: list[str]) -> str` to
    `s3_staging.py` near the L77 marker. Body: `ordered = sorted(bucket_ids)`; raise `S3StagingError`
    with the "kueue backend resolves to an empty bucket set" message when `ordered` is empty; compute
    `digest = hashlib.sha256(file_id.bytes).digest()`; `index = int.from_bytes(digest, "big") % len(ordered)`;
    return `ordered[index]`. Add `import hashlib` (and `import uuid` if not already imported at runtime —
    the module currently imports uuid only under TYPE_CHECKING, so add a runtime `import uuid` guarded by
    the existing style, or use `uuid.UUID` only in the annotation). Docstring must state: stable hash of the
    UUID bytes (NOT Python's salted `hash()`), `sorted()` for stable order, and that the chosen id is the
    AUTHORITATIVE value recorded on `cloud_job.staging_bucket` — cleanup/presign READ it, never re-derive
    (D-01/D-06). Do NOT parameterize any other verb here (that is Plan 02); keep `_staging_config()` intact.
    Keep the module ORM-free.
  </action>
  <acceptance_criteria>
    - `src/phaze/services/s3_staging.py` contains `def pick_bucket` and `hashlib.sha256` and `sorted(`
    - `s3_staging.py` still has NO `import phaze.models` (ORM-free purity preserved)
    - `uv run pytest tests/analyze/services/test_s3_staging.py -k "pick_bucket" -x` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/analyze/services/test_s3_staging.py -k "pick_bucket" -x</automated>
  </verify>
  <done>pick_bucket is deterministic, restart-stable (sha256, not salted hash), empty-set raises, output always in the set.</done>
</task>

<task type="auto">
  <name>Task 3: Add KubeConfig.context field + declare PyYAML explicitly (A1, MKUE-01)</name>
  <files>src/phaze/config_backends.py, pyproject.toml</files>
  <read_first>
    - src/phaze/config_backends.py (KubeConfig fields L144-157; `namespace: str | None = None` at L145 is the shape to copy for a plain non-secret string field)
    - .planning/phases/70-multi-kueue-n-clusters/70-RESEARCH.md (§Assumptions A1 + §Open Questions Q1 — planner adds context)
    - CLAUDE.md (pyproject.toml section order: [build-system] → [project] → [project.scripts] → [tool.*] → [dependency-groups]; dependencies alphabetically sorted)
  </read_first>
  <action>
    Add `context: str | None = None` to `KubeConfig` (config_backends.py), placed with the other plain
    string fields (near `namespace`/`workload_api_version`), with a one-line comment: "REG-05 per-cluster
    kubeconfig/context; selects a non-default context, defaults to current-context when None (MKUE-01/A1)".
    It is NOT a secret — plain `str | None`, never a `SecretStr`. Do NOT alter the `_resolve_inline_secret_files`
    validator. Then declare `PyYAML` explicitly under `[project].dependencies` in `pyproject.toml` in
    alphabetical position (it is already a resolved transitive dependency of the pinned kr8s — this is
    hygiene so a future kr8s dropping PyYAML cannot silently break the kubeconfig-dict parse Plan 03 adds;
    NOT a new dependency in the milestone sense). Use a floor at/above the currently-resolved version in
    `uv.lock` and respect the `[tool.uv] exclude-newer = "7 days"` cooldown (pick a floor already ≥7 days old).
  </action>
  <acceptance_criteria>
    - `src/phaze/config_backends.py` KubeConfig contains `context: str | None = None`
    - `uv run python -c "from phaze.config_backends import KubeConfig; KubeConfig(context='prod')"` exits 0
    - `pyproject.toml` lists PyYAML under [project].dependencies (alphabetical) and `uv sync` resolves cleanly
    - `uv run ruff check src/phaze/config_backends.py` and `uv run mypy src/phaze/config_backends.py` pass
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from phaze.config_backends import KubeConfig; assert 'context' in KubeConfig.model_fields; KubeConfig(context='prod')"</automated>
  </verify>
  <done>KubeConfig accepts an optional context; PyYAML is an explicit, cooldown-satisfying project dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config → control plane | `KubeConfig` fields (incl. new `context`) are operator-supplied via `backends.toml`; secrets are `SecretStr` |
| control plane → Postgres | the additive DDL migration runs control-side only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01-01 | Information Disclosure | new KubeConfig.context field | accept | `context` is a plain non-secret string (a kubeconfig context name), never `SecretStr`, never logged; kubeconfig/sa_token remain `SecretStr` |
| T-70-04-01 | Tampering | migration 030 DDL | mitigate | additive nullable column only; downgrade drops cleanly; migration touches ONLY cloud_job, never saq_jobs (asserted by test) |
| T-70-02-01 | Tampering | pick_bucket determinism | mitigate | pure sha256-of-UUID-bytes (not salted hash) so cleanup/presign can independently re-agree via the RECORDED staging_bucket (no re-derive drift); unit-proven restart-stable |
| T-70-SC | Tampering | PyYAML dependency declaration | accept | no new install — PyYAML already resolved transitively via pinned kr8s (PyPI 15+ yrs, ~300M/mo); explicit declaration is hygiene; `exclude-newer` cooldown respected |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_migrations/ -k "030 or staging_bucket" tests/analyze/services/test_s3_staging.py -k "pick_bucket or 030 or staging_bucket" -x`
- `uv run python -c "from phaze.config_backends import KubeConfig; assert 'context' in KubeConfig.model_fields"`
- `uv run ruff check . && uv run mypy .` pass; `pre-commit run --all-files` green
</verification>

<success_criteria>
- cloud_job has a nullable staging_bucket column; migration 030 round-trips; unique(file_id) unchanged
- pick_bucket is deterministic + restart-stable + empty-set-raises; s3_staging stays ORM-free
- KubeConfig has an optional context field; PyYAML declared explicitly
- No runtime behavior changed (all edits additive); full suite green
</success_criteria>

<output>
Create `.planning/phases/70-multi-kueue-n-clusters/70-01-SUMMARY.md` when done
</output>
