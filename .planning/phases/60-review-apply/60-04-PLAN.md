---
phase: 60-review-apply
plan: 04
type: execute
wave: 4
depends_on: [60-03]
files_modified:
  - src/phaze/templates/pipeline/partials/dedupe_workspace.html
  - src/phaze/templates/pipeline/partials/_dupe_group.html
  - src/phaze/templates/pipeline/partials/cue_workspace.html
  - src/phaze/templates/pipeline/partials/_cue_preview.html
  - src/phaze/services/review.py
  - src/phaze/routers/shell.py
  - tests/test_review_apply_workspaces.py
autonomous: true
requirements: [REVIEW-03, REVIEW-04, REVIEW-05]
must_haves:
  truths:
    - "D-07: keeper radio posts POST /duplicates/{group.sha256_hash}/resolve with Form field canonical_id (NOT group_id/keeper_id); auto-keep bulk posts POST /duplicates/resolve-all (page-scoped)"
    - "REVIEW-05: dedupe UNDO posts POST /duplicates/{sha256_hash}/undo with Form file_states = the JSON blob round-tripped from the resolve response (load-bearing — undo reconstructs from that JSON, not a fresh query)"
    - "D-08: Cue APPROVE posts POST /cue/{tracklist.id}/generate (there is NO /cue/{id}/approve route — generate IS the approve/write); ineligible cards render opacity-60 'awaiting tracklist match…' with NO approve control"
    - "Cue preview <pre> is built IN-MEMORY at render via _build_cue_tracks + generate_cue_content (NO write_cue_file, NO disk write) — a read-time reuse of generation helpers"
    - "GET /s/dedupe and /s/cue return bare fragments; STAGE_PARTIALS values are static literals (T-57-01); no second poll; KEEP/archive + eligible/gated never hue-only (WCAG 1.4.1)"
  artifacts:
    - path: "src/phaze/templates/pipeline/partials/dedupe_workspace.html"
      provides: "Dedupe keeper-group workspace (D-07) + auto-keep bulk"
      min_lines: 18
    - path: "src/phaze/templates/pipeline/partials/cue_workspace.html"
      provides: "Cue preview workspace (D-08) — eligible + gated cards"
      min_lines: 18
    - path: "src/phaze/services/review.py"
      provides: "get_dedupe_groups + get_cue_review_cards degrade-safe read helpers"
      contains: "get_dedupe_groups"
  key_links:
    - from: "src/phaze/routers/shell.py"
      to: "get_dedupe_groups"
      via: "_render_stage dedupe branch"
      pattern: "get_dedupe_groups"
    - from: "src/phaze/templates/pipeline/partials/_dupe_group.html"
      to: "/duplicates/{group_hash}/resolve"
      via: "keeper radio canonical_id form"
      pattern: "canonical_id"
---

<objective>
Supersede the final two placeholders — `dedupe` (keeper-select groups, REVIEW-03) and `cue` (preview + approve gated on a matched tracklist, REVIEW-04) — over existing endpoints with zero backend change, completing the Review & Apply gate.

Purpose: REVIEW-03 (duplicate groups + keeper radio + auto-keep bulk + reversible), REVIEW-04 (cue preview + approve gate), REVIEW-05 (dedupe reversibility via the stateful file_states undo round-trip). All wiring rides existing `duplicates.py` / `cue.py` routes; the ONLY correctness risk is copying the UI-SPEC sketch (which shows `group_id`/`keeper_id` and `/cue/{id}/approve` — all WRONG).

Output: `dedupe_workspace.html` + `_dupe_group.html` + `cue_workspace.html` + `_cue_preview.html` + `get_dedupe_groups`/`get_cue_review_cards` in `services/review.py` + shell.py dedupe/cue wiring + converted tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-review-apply/60-CONTEXT.md
@.planning/phases/60-review-apply/60-UI-SPEC.md
@.planning/phases/60-review-apply/60-PATTERNS.md
@.planning/phases/60-review-apply/60-03-SUMMARY.md

<interfaces>
<!-- VERIFIED contracts. The UI-SPEC sketch is WRONG on dedupe (group_id/keeper_id) and cue (/approve) — use THESE. -->

Dedupe (duplicates.py — all POST):
  keeper radio -> POST /duplicates/{group_hash}/resolve  Form: canonical_id: uuid = Form(...)   (:144)  ← group key = group["sha256_hash"], field = canonical_id (NOT group_id/keeper_id)
      returns duplicates/partials/resolve_response.html carrying resolved_file_states = json.dumps(...)
  auto-keep bulk -> POST /duplicates/resolve-all  Form: page/page_size  (:199)  ← PAGE-SCOPED; score_group sets group["canonical_id"]
  UNDO -> POST /duplicates/{group_hash}/undo  Form: file_states: str = Form(...)  (:169)  ← the JSON blob from the resolve response, round-tripped (undo reconstructs prior state FROM this JSON)
  undo-all -> POST /duplicates/undo-all  Form: file_states + page  (:233)
  reads: find_duplicate_groups_with_metadata(session, limit, offset) (services/dedup.py:118) + score_group(group) (dedup.py:32) sets group["canonical_id"]
  group dict keys: sha256_hash, canonical_id, files[] (each {id, file_size, bitrate, duration, tag_filled, ...})

Cue (cue.py):
  APPROVE -> POST /cue/{tracklist_id}/generate  (:239)  ← generate IS the approve/write (validates gates, writes .cue); there is NO /cue/{id}/approve route. card key = tracklist.id
      NOTE: generate branches on HX-Target starting "tracklist-"; the new workspace target won't match → it falls through to the cue_row.html branch (A2 — confirm it renders acceptably; if not, target an id the row branch handles or accept the toast)
  eligibility gate: _get_eligible_tracklist_query(session) (:31) — status=="approved" + file_id set + FileRecord.state==EXECUTED + ≥1 timestamped track
  in-memory preview (NO disk write): _build_cue_tracks(session, tracklist.latest_version_id) (:120) + generate_cue_content(audio_name, file_type, cue_tracks) (services/cue_generator.py) — do NOT call write_cue_file

get_dedupe_groups(session) -> list[dict]  (NEW review.py, degrade-safe): find_duplicate_groups_with_metadata + score_group per group; return dicts with sha256_hash, group_name, files[] (id, name, quality string, keeper=bool from canonical_id). Wrap session.begin_nested(); [] on error.
get_cue_review_cards(session) -> list[dict]  (NEW review.py, degrade-safe): for each eligible (Tracklist, FileRecord) build cue_text in-memory via _build_cue_tracks+generate_cue_content; also surface the ineligible/gated sets; dict {tracklist_id, set_name, eligible: bool, cue_text|None}. Wrap session.begin_nested(); [] on error.

shell.py: STAGE_PARTIALS["dedupe"]/["cue"] -> static literals; _render_stage:
  elif stage == "dedupe": context["dedupe_groups"] = await get_dedupe_groups(session)
  elif stage == "cue":    context["cue_cards"] = await get_cue_review_cards(session)
  oob_counts stays False.

Copy (UI-SPEC): dedupe h1 "Review & Apply · Dedupe" subcount "{n} duplicate groups · pick the keeper, others archived"; bulk "AUTO-KEEP HIGHEST QUALITY";
  auto-keep confirm "Auto-keep the highest-quality copy in every duplicate group and archive the rest? {n} groups · reversible via undo."; empty "No duplicates found" / "Fingerprinting surfaced no duplicate groups. Nothing to reconcile."
  cue h1 "Review & Apply · Cue sheets" subcount "{n} generated from matched tracklists"; gated card body "awaiting tracklist match…"; empty "No cue sheets yet" / "Cue sheets generate once a set has a matched, approved tracklist with timestamped tracks."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dedupe_workspace.html + _dupe_group.html + get_dedupe_groups</name>
  <files>src/phaze/templates/pipeline/partials/dedupe_workspace.html, src/phaze/templates/pipeline/partials/_dupe_group.html, src/phaze/services/review.py</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/analyze_workspace.html (the grid-card structure `grid grid-cols-3 gap-4 p-6` + _lane_card shape to mirror)
    - src/phaze/routers/duplicates.py (resolve :144 canonical_id; undo :169 file_states round-trip; resolve-all :199; score_group usage; the resolve_response.html it returns)
    - src/phaze/services/dedup.py (find_duplicate_groups_with_metadata :118; score_group :32 sets group["canonical_id"])
    - src/phaze/services/pipeline.py:769-835 (SAVEPOINT degrade-safe read pattern)
    - .planning/phases/60-review-apply/60-UI-SPEC.md (NEW Pattern 4 keeper-group markup + Copywriting; WCAG never-hue-only)
    - .planning/phases/60-review-apply/60-PATTERNS.md (§dedupe — the ⚠️ verified wiring + file_states round-trip)
  </read_first>
  <action>
    In services/review.py add `get_dedupe_groups(session) -> list[dict[str, Any]]`: inside `session.begin_nested()`, call `find_duplicate_groups_with_metadata(session)`, run `score_group(group)` on each (sets group["canonical_id"]), map to dicts {sha256_hash, group_name (a short label, e.g. first file name + "· N copies"), files: [{id, name, quality (e.g. "{bitrate} kbps · {size}"), keeper: (id == canonical_id)}]}; `except Exception: logger.warning(...); return []`. Create _dupe_group.html (one card per group): `rounded-xl bg-phaze-panel border phaze-border p-4`; group title (Jura uppercase + "· {n} copies" suffix); `space-y-1.5` list of `<label>` radio rows — each `<input type="radio" name="group-{{ group.sha256_hash }}" value="{{ f.id }}" {{ 'checked' if f.keeper }} hx-post="/duplicates/{{ group.sha256_hash }}/resolve" hx-vals='{"canonical_id":"{{ f.id }}"}' hx-target="#dupe-group-{{ group.sha256_hash }}" hx-swap="outerHTML">` (⚠️ Form field name is canonical_id via hx-vals; group key is sha256_hash), keeper row emerald-tinted with a **KEEP** text tag, others neutral with an **archive** text tag (text tags, NOT hue-only). Wrap the card in `<div id="dupe-group-{{ group.sha256_hash }}">`. Provide an UNDO affordance on the resolved state that posts `/duplicates/{{ group.sha256_hash }}/undo` with `file_states` carried from the resolve response (the resolve returns resolve_response.html which already holds resolved_file_states — the UNDO in that response partial round-trips it; ensure the workspace's resolved card includes the hidden file_states so undo can fire — mirror duplicates/partials/resolve_response.html). Create dedupe_workspace.html: import scaffold; subcount per UI-SPEC; `actions` = ONE amber "AUTO-KEEP HIGHEST QUALITY" button `hx-post="/duplicates/resolve-all"` (Form page/page_size via hx-vals) `hx-confirm` (UI-SPEC copy) `hx-disabled-elt="this"`; `{% call ws.workspace(...) %}` looping `_dupe_group.html` over dedupe_groups in a `grid` (or space-y) layout; empty-state per UI-SPEC. NO `<html`/`<head`, NO second poll, NO `hx-swap-oob` on any group card, NO `| safe`. Run `just tailwind` if new classes appear.
  </action>
  <verify>
    <automated>uv run pytest tests/test_review_apply_workspaces.py::test_review_fragments_are_bare tests/test_review_apply_workspaces.py::test_review_single_poll_discipline -x && uv run mypy src/phaze/services/review.py</automated>
  </verify>
  <acceptance_criteria>
    - get_dedupe_groups is SAVEPOINT-wrapped, [] on error, sets keeper from canonical_id
    - _dupe_group.html radio posts `/duplicates/{sha256_hash}/resolve` with `canonical_id` (grep shows `canonical_id`, and NO `group_id`/`keeper_id`)
    - auto-keep bulk posts `/duplicates/resolve-all`; UNDO posts `/duplicates/{sha256_hash}/undo` carrying `file_states`
    - KEEP/archive rendered as text tags (not hue-only); fragment has NO `<html`/`<head`/`hx-trigger="every"`/`hx-swap-oob` on cards/`| safe`
    - `uv run mypy src/phaze/services/review.py` clean
  </acceptance_criteria>
  <done>Dedupe renders keeper-group cards with the verified canonical_id resolve wiring, page-scoped auto-keep, and the stateful file_states undo round-trip.</done>
</task>

<task type="auto">
  <name>Task 2: Create cue_workspace.html + _cue_preview.html + get_cue_review_cards</name>
  <files>src/phaze/templates/pipeline/partials/cue_workspace.html, src/phaze/templates/pipeline/partials/_cue_preview.html, src/phaze/services/review.py</files>
  <read_first>
    - src/phaze/routers/cue.py (_get_eligible_tracklist_query :31; _build_cue_tracks :120; generate_cue_content import; generate_cue :239 — the HX-Target "tracklist-" branch + cue_row.html fall-through, A2)
    - src/phaze/services/cue_generator.py (generate_cue_content signature + CueTrackData; write_cue_file — which we do NOT call)
    - src/phaze/templates/pipeline/partials/analyze_workspace.html (grid card layout to mirror; two-column grid)
    - src/phaze/services/pipeline.py:769-835 (SAVEPOINT degrade-safe read pattern)
    - .planning/phases/60-review-apply/60-UI-SPEC.md (NEW Pattern 5 cue preview + gated card + Copywriting)
    - .planning/phases/60-review-apply/60-PATTERNS.md (§cue — verified wiring, in-memory preview, omit EXPORT/EDIT)
  </read_first>
  <action>
    In services/review.py add `get_cue_review_cards(session) -> list[dict[str, Any]]`: inside `session.begin_nested()`, call `_get_eligible_tracklist_query(session)` (import from cue.py or replicate the query) for eligible (Tracklist, FileRecord) pairs; for each build cue_text IN-MEMORY via `_build_cue_tracks(session, tracklist.latest_version_id)` + `generate_cue_content(audio_name, file_type, cue_tracks)` — do NOT call write_cue_file, do NOT write disk; also surface the ineligible/gated sets (approved+EXECUTED but no timestamps) for the gated cards; map to dicts {tracklist_id, set_name, eligible: bool, cue_text: str|None}; `except Exception: logger.warning(...); return []`. Create _cue_preview.html (one card): eligible → `rounded-xl bg-phaze-panel border phaze-border p-4` with `{{ set_name }}.cue` title, a `<pre class="mono text-[11px] text-gray-400 leading-relaxed bg-phaze-bg rounded-lg p-3 overflow-x-auto">{{ cue_text }}</pre>` (autoescaped, NEVER `| safe`), and an emerald APPROVE button `hx-post="/cue/{{ tracklist_id }}/generate"` `hx-target="#cue-card-{{ tracklist_id }}"` `hx-swap="outerHTML"` (⚠️ generate IS approve — NO /approve route); gated → `opacity-60` card with body "awaiting tracklist match…" and NO approve control. Omit EDIT and the prototype's EXPORT APPROVED (no endpoint). Wrap each card in `<div id="cue-card-{{ tracklist_id }}">`. Create cue_workspace.html: import scaffold; subcount per UI-SPEC; NO bulk header (REVIEW-04 requires none); `{% call ws.workspace(...) %}` looping `_cue_preview.html` over cue_cards in `grid grid-cols-2 gap-4 p-6`; empty-state per UI-SPEC. NO `<html`/`<head`, NO second poll, NO `| safe`. Run `just tailwind` if new classes appear.
  </action>
  <verify>
    <automated>uv run pytest tests/test_review_apply_workspaces.py::test_review_fragments_are_bare -x && uv run mypy src/phaze/services/review.py</automated>
  </verify>
  <acceptance_criteria>
    - get_cue_review_cards builds cue_text in-memory (grep shows `generate_cue_content` and NO `write_cue_file`), SAVEPOINT-wrapped, [] on error
    - _cue_preview.html eligible card renders the `<pre>` preview + APPROVE `hx-post="/cue/{tracklist_id}/generate"` (NO `/approve`); gated card is `opacity-60` with "awaiting tracklist match…" and NO approve button
    - cue_workspace.html has NO bulk header; fragment has NO `<html`/`<head`/`hx-trigger="every"`/`| safe`
    - `uv run mypy src/phaze/services/review.py` clean
  </acceptance_criteria>
  <done>Cue renders in-memory .cue previews with the generate-as-approve wiring and visibly gated ineligible cards, no disk write at render.</done>
</task>

<task type="auto">
  <name>Task 3: Wire shell.py dedupe + cue stages + convert tests</name>
  <files>src/phaze/routers/shell.py, tests/test_review_apply_workspaces.py</files>
  <read_first>
    - src/phaze/routers/shell.py (STAGE_PARTIALS + _render_stage + review import; the rename/move/propose/tagwrite branches to mirror)
    - src/phaze/templates/pipeline/partials/dedupe_workspace.html + cue_workspace.html (context keys consumed)
    - tests/test_review_apply_workspaces.py (stubs to convert: test_dedupe_keeper_resolve_wiring, test_cue_gate_and_preview)
    - tests/test_dead_template_guard.py + tests/test_shell_routes.py (guards)
  </read_first>
  <action>
    In shell.py replace `"dedupe": _STAGE_PLACEHOLDER,` and `"cue": _STAGE_PLACEHOLDER,` with static literals `"pipeline/partials/dedupe_workspace.html"` / `"pipeline/partials/cue_workspace.html"` (T-57-01) + the Phase-60 comment convention. Extend the review import with `get_dedupe_groups, get_cue_review_cards`. In `_render_stage` add `elif stage == "dedupe": context["dedupe_groups"] = await get_dedupe_groups(session)` and `elif stage == "cue": context["cue_cards"] = await get_cue_review_cards(session)`; oob_counts stays False. Do NOT delete legacy templates — this is the last workspace, but CUT-02/dead-template removal is Phase 62; the guard must still be green. Convert the two behavior tests: test_dedupe_keeper_resolve_wiring seeds a duplicate group, GETs `/s/dedupe` (HX), asserts a keeper radio posts `/duplicates/{sha256}/resolve` with `canonical_id` and that a resolved card round-trips `file_states` on UNDO; test_cue_gate_and_preview seeds one eligible + one ineligible cue set, GETs `/s/cue` (HX), asserts the eligible card shows a `<pre>` preview + APPROVE `hx-post="/cue/{id}/generate"` and the ineligible card is `opacity-60` with no approve control. Run the module + guard tests + full suite.
  </action>
  <verify>
    <automated>uv run pytest tests/test_review_apply_workspaces.py tests/test_dead_template_guard.py tests/test_shell_routes.py -x && uv run mypy src/phaze/routers/shell.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "dedupe\|cue" src/phaze/routers/shell.py` shows both STAGE_PARTIALS values are static literals
    - `_render_stage` has `elif stage == "dedupe":` and `elif stage == "cue":` branches; oob_counts unchanged (False)
    - GET `/s/dedupe` renders keeper radios (`canonical_id` resolve); GET `/s/cue` renders eligible `<pre>` + generate-approve and a gated opacity-60 card
    - test_dedupe_keeper_resolve_wiring + test_cue_gate_and_preview pass (xfail removed); all six workspaces now live
    - dead-template + shell-route guards green; `uv run mypy src/phaze/routers/shell.py` clean; full suite ≥85% cov
  </acceptance_criteria>
  <done>/s/dedupe and /s/cue serve their real workspaces; the keeper-resolve + cue-gate tests pass; all six Review & Apply placeholders are superseded and guards are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /s/{stage} | `stage` whitelisted; never a template path |
| DB → dedupe/cue cell | file names/quality strings + cue text cross into rendered HTML |
| client → resolve/undo/generate | keeper canonical_id + undo file_states + cue generate cross to existing routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | shell.py STAGE_PARTIALS dedupe/cue | mitigate | static string literals; `stage` never spliced into a template path |
| T-60-XSS | Tampering / Info-disclosure | cue `<pre>` text + dupe file cells | mitigate | Jinja autoescape; NEVER `\| safe` on cue text/file names/paths (the cue `<pre>` is DB+filesystem-derived and MUST autoescape) |
| T-60-03 | Repudiation | dedupe reversibility (file_states) | mitigate | UNDO round-trips the resolve response's file_states JSON to `/duplicates/{hash}/undo`; reversal is authoritative from that blob (REVIEW-05); resolve writes exactly one resolution (asserted in Plan 60-01 audit test) |
| T-60-CUE | Tampering | cue in-memory preview render | mitigate | get_cue_review_cards builds preview via generate_cue_content ONLY (no write_cue_file) — render never mutates disk; the write happens only on explicit APPROVE→/cue/{id}/generate |
| T-60-DOS | Denial of Service | _render_stage dedupe/cue branches | mitigate | get_dedupe_groups + get_cue_review_cards SAVEPOINT-wrapped, [] on error; no router try/except; oob_counts False |
| T-60-SC | Tampering | package installs | accept | Phase 60 installs nothing |
</threat_model>

<verification>
- `uv run pytest tests/test_review_apply_workspaces.py tests/test_dead_template_guard.py tests/test_shell_routes.py -x` green
- `uv run mypy src/phaze/routers/shell.py src/phaze/services/review.py` clean
- `uv run ruff check src/phaze/routers/shell.py src/phaze/services/review.py tests/test_review_apply_workspaces.py` clean
- `just tailwind` clean if new utility classes were introduced
- Full suite: `uv run pytest --cov --cov-report=term-missing` ≥ 85%
- pre-commit hooks pass (never --no-verify)
</verification>

<success_criteria>
- Dedupe wires keeper radio → /duplicates/{sha256_hash}/resolve (canonical_id), auto-keep → resolve-all, UNDO → the file_states round-trip (REVIEW-03 + REVIEW-05); KEEP/archive never hue-only
- Cue renders in-memory .cue previews, APPROVE → /cue/{id}/generate (no /approve route), ineligible cards visibly gated (REVIEW-04)
- /s/dedupe and /s/cue are bare fragments with static-literal STAGE_PARTIALS; no second poll; all six placeholders superseded; guards green
</success_criteria>

<output>
Create `.planning/phases/60-review-apply/60-04-SUMMARY.md` when done
</output>
