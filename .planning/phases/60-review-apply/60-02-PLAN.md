---
phase: 60-review-apply
plan: 02
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - src/phaze/templates/pipeline/partials/_diff_row.html
  - src/phaze/templates/pipeline/partials/rename_workspace.html
  - src/phaze/templates/pipeline/partials/move_workspace.html
  - src/phaze/services/review.py
  - src/phaze/routers/shell.py
  - tests/test_review_apply_workspaces.py
autonomous: true
requirements: [REVIEW-01, REVIEW-02]
must_haves:
  truths:
    - "D-06: ONE shared partial _diff_row.html serves both Rename (proposed_filename facet) and Move (proposed_path facet) over the SAME RenameProposal source"
    - "Per-row Approve/Skip/Undo use hx-PATCH (NOT hx-post) → /proposals/{id}/approve|reject|undo; Skip = reject (D discretion)"
    - "Inline Edit = Alpine LOCAL x-data island; SAVE EDIT hx-patch → /proposals/{id}/edit targets ONLY its own row (hx-swap=outerHTML, R-6); DISCARD sends no request"
    - "Bulk header APPROVE ALL >=90% CONFIDENCE = amber-attention + hx-confirm + busy-gate, hx-patch → /proposals/bulk-approve-high-confidence with NO id-list/threshold param (D-02)"
    - "BEFORE value rose+line-through, AFTER value emerald; row-body click UNBOUND (R-1); every cell autoescaped, never | safe"
    - "GET /s/rename and /s/move return bare fragments; STAGE_PARTIALS values are static string literals (T-57-01); rename+move are two separate rail-node workspaces (D-06 LOCKED)"
  artifacts:
    - path: "src/phaze/templates/pipeline/partials/_diff_row.html"
      provides: "shared before→after diff row (rename/move/tag facets)"
      min_lines: 25
    - path: "src/phaze/templates/pipeline/partials/rename_workspace.html"
      provides: "Rename/Path review diff workspace + bulk header"
      min_lines: 20
    - path: "src/phaze/templates/pipeline/partials/move_workspace.html"
      provides: "Move-files review diff workspace (proposed_path facet)"
      min_lines: 20
    - path: "src/phaze/services/review.py"
      provides: "get_pending_proposal_rows degrade-safe read helper"
      contains: "get_pending_proposal_rows"
  key_links:
    - from: "src/phaze/routers/shell.py"
      to: "get_pending_proposal_rows"
      via: "_render_stage rename + move branches"
      pattern: "get_pending_proposal_rows"
    - from: "src/phaze/templates/pipeline/partials/rename_workspace.html"
      to: "_diff_row.html"
      via: "per-row include over pending proposals"
      pattern: "_diff_row.html"
---

<objective>
Supersede the `rename` and `move` placeholders with their before→after review diff workspaces, and create the ONE shared `_diff_row.html` partial (D-06) that Tag-write (Plan 60-03) also reuses.

Purpose: REVIEW-01 (per-file Approve/Edit/Skip over a diff) + REVIEW-02 (bulk approve-high-confidence) for the two `RenameProposal` facets. This is the load-bearing Review interaction; every wiring detail must match the VERIFIED routes, not the UI-SPEC sketch (which shows `hx-post` where the routes are `hx-patch`).

Output: `_diff_row.html` (shared) + `rename_workspace.html` + `move_workspace.html` + `services/review.py` with the degrade-safe `get_pending_proposal_rows` + shell.py rename/move wiring + converted diff/edit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-review-apply/60-CONTEXT.md
@.planning/phases/60-review-apply/60-UI-SPEC.md
@.planning/phases/60-review-apply/60-PATTERNS.md
@.planning/phases/60-review-apply/60-01-SUMMARY.md

<interfaces>
<!-- VERIFIED contracts — wire against THESE (the UI-SPEC sketch HTML is illustrative and WRONG on verbs). -->

Per-row (Rename/Move), from proposals.py — ALL PATCH:
  APPROVE -> hx-patch="/proposals/{id}/approve"   SKIP -> hx-patch="/proposals/{id}/reject"   UNDO -> hx-patch="/proposals/{id}/undo"
  SAVE EDIT -> hx-patch="/proposals/{id}/edit"  (D-05, from Plan 60-01) form field name="proposed" (+ optional facet="path" for move)
Bulk header (Rename & Move share it):
  hx-patch="/proposals/bulk-approve-high-confidence"  (D-02) — id-less, threshold-less; predicate fixed 0.9 server-side

get_pending_proposal_rows(session) -> list[dict]  (NEW in services/review.py, this plan) per-row keys:
  id, filename (original_filename), original_path (current_path), proposed_filename, proposed_path, confidence  — degrade-safe ([] on DB error)
  Reuse services/proposal_queries.get_proposals_page(session, status="pending", page_size=...) inside a SAVEPOINT; map ORM -> plain dicts.

shell.py wiring (src/phaze/routers/shell.py):
  STAGE_PARTIALS["rename"]/["move"] = _STAGE_PLACEHOLDER  -> replace with static literals "pipeline/partials/rename_workspace.html" / "move_workspace.html" (T-57-01)
  _render_stage: add `elif stage == "rename": context["rename_proposals"] = await get_pending_proposal_rows(session)` and the move sibling; oob_counts stays False (Pitfall 5); fragment-vs-full fork UNCHANGED.

_workspace_scaffold.html macro (import as ws): {% call ws.workspace(title, subcount='', actions='') %} — one <h1 tabindex="-1">; actions = a {% set actions %}...{% endset %} block; auto-includes _workspace_poll_seeds.html.

UI-SPEC diff tints (dark / light):
  BEFORE bg-rose-500/10 text-rose-300 line-through  (light bg-rose-50 text-rose-600 line-through)
  AFTER  bg-emerald-500/10 text-emerald-300         (light bg-emerald-50 text-emerald-600)
  grid grid-cols-[1fr_auto_1fr] gap-3 ; arrow → text-gray-600 ; per-row control height h-7 ; action cluster flex gap-1.5 shrink-0 ml-4
  Bulk button: h-9 px-3 border-amber-500/40 text-amber-200 hover:bg-amber-500/10 font-jura uppercase tracking-wider
Copy (UI-SPEC Copywriting):
  rename h1 "Review & Apply · Rename / Path" subcount "{n} awaiting approval · proposed filename + destination"
  move   h1 "Review & Apply · Move files"    subcount "{n} awaiting approval · physical file moves on the file server"
  bulk button "APPROVE ALL ≥90% CONFIDENCE"; confirm "Approve all pending proposals at ≥90% confidence? The server re-checks confidence at submit — {n} match now. Every apply is audited and reversible."
  empty rename: "Nothing to review" / "No pending rename proposals. Generate proposals in Propose, or approvals are all cleared."
  empty move: "Nothing to move" / "No pending file moves. Approve rename proposals to queue moves."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared _diff_row.html partial (D-06)</name>
  <files>src/phaze/templates/pipeline/partials/_diff_row.html, tests/test_review_apply_workspaces.py</files>
  <read_first>
    - .planning/phases/60-review-apply/60-UI-SPEC.md (NEW Pattern 1 — copy the tokenized markup; Color § diff tints; Inline Edit affordance; Interaction R-1/R-6)
    - src/phaze/templates/pipeline/partials/_file_table.html (the cell autoescape/never-| safe trust boundary + stable-id + cursor-pointer idiom to match)
    - src/phaze/templates/pipeline/partials/fingerprint_workspace.html (the secondary-button class string _btn to reuse for EDIT/SKIP/UNDO)
    - .planning/phases/60-review-apply/60-PATTERNS.md (§_diff_row.html — per-facet before/after/approve-wire table; the ⚠️ verb corrections)
    - .planning/phases/60-review-apply/60-RESEARCH.md (§Code Examples verified inline-edit island; Anti-Patterns to Avoid)
  </read_first>
  <action>
    Create src/phaze/templates/pipeline/partials/_diff_row.html — ONE partial, parameterized by facet vars the caller passes: `row_id_prefix` (e.g. "rename-row"/"move-row"), `pid` (proposal id), `file` (display name), `original_path` (title tooltip), `before` (current value), `after` (proposed value), `approve_url`/`skip_url`/`undo_url`/`edit_url` (the VERIFIED PATCH routes), `edit_facet` ("filename" or "path"). Root `<div id="{{ row_id_prefix }}-{{ pid }}" class="px-6 py-3 border-b phaze-border hover:bg-white/5 cursor-pointer" x-data="{ editing:false, val:'{{ after|e }}' }">` — row-body click UNBOUND (R-1). Top line: truncating file name (`title="{{ original_path }}"`) + a `flex gap-1.5 shrink-0 ml-4` action cluster with two x-show groups: pending group (x-show="!editing") = APPROVE (emerald-600 fill, `hx-patch="{{ approve_url }}"` `hx-target="#{{ row_id_prefix }}-{{ pid }}"` `hx-swap="outerHTML"`), EDIT (secondary, `@click="editing=true; $nextTick(()=>$refs.edit.focus())"`), SKIP (secondary, `hx-patch="{{ skip_url }}"` `hx-confirm="Skip this proposal?"` same target/swap); edit group (x-show="editing") = SAVE EDIT (emerald fill, `hx-patch="{{ edit_url }}"` `hx-include="closest div"` same target/swap), DISCARD (secondary, `@click="editing=false; val='{{ after|e }}'"`, no request). Body: `grid grid-cols-[1fr_auto_1fr] gap-3 items-center text-xs` — BEFORE `<code>` with the rose+line-through tint, `→` arrow, then `<template x-if="!editing"><code>` AFTER emerald tint `</code></template>` and `<template x-if="editing"><input x-ref="edit" x-model="val" name="proposed" ...></template>`; include a hidden `<input type="hidden" name="facet" value="{{ edit_facet }}">` inside the row so SAVE EDIT posts the facet. All h-7 dense controls per UI-SPEC. NO `| safe` on any cell; NO hx-post (all PATCH); NO hx-trigger/setInterval; NO hx-swap-oob on the row container. Run `just tailwind` if any new utility class appears (app.css is a gitignored build artifact). Then convert the xfail stub `test_diff_row_before_after` to assert (via a rename fragment once Task 2/3 exist, OR by rendering the partial directly with a stub context): rose-struck before + emerald after, `hx-patch` on APPROVE, the `x-data` edit island, and a `name="proposed"` input.
  </action>
  <verify>
    <automated>uv run pytest tests/test_review_apply_workspaces.py::test_review_fragments_are_bare tests/test_review_apply_workspaces.py::test_review_single_poll_discipline -x</automated>
  </verify>
  <acceptance_criteria>
    - _diff_row.html exists; contains `hx-patch` on approve/skip/undo/save-edit and ZERO `hx-post`
    - `grep -c "| safe" src/phaze/templates/pipeline/partials/_diff_row.html` is 0
    - `grep -Ec "hx-trigger=\"every|setInterval|hx-swap-oob" src/phaze/templates/pipeline/partials/_diff_row.html` is 0
    - Contains `grid-cols-[1fr_auto_1fr]`, a rose+`line-through` BEFORE cell, an emerald AFTER cell, `x-data="{ editing`, and `name="proposed"`
  </acceptance_criteria>
  <done>The shared diff-row partial renders a verified before→after row with correct PATCH verbs and an isolated inline-edit island.</done>
</task>

<task type="auto">
  <name>Task 2: Create rename_workspace.html + move_workspace.html + review.py helper</name>
  <files>src/phaze/templates/pipeline/partials/rename_workspace.html, src/phaze/templates/pipeline/partials/move_workspace.html, src/phaze/services/review.py</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/fingerprint_workspace.html (scaffold import + actions block + trigger-response div idiom to mirror)
    - src/phaze/templates/pipeline/partials/_diff_row.html (Task 1 — the include contract + facet vars)
    - src/phaze/services/pipeline.py:769-835 (get_analyze_stage_files — the SAVEPOINT begin_nested + []-on-error degrade-safe read pattern to copy for review.py)
    - src/phaze/services/proposal_queries.py (get_proposals_page signature to wrap)
    - .planning/phases/60-review-apply/60-UI-SPEC.md (NEW Pattern 3 bulk header + Copywriting titles/subcounts/confirm/empty states)
  </read_first>
  <action>
    Create src/phaze/services/review.py with `get_pending_proposal_rows(session) -> list[dict[str, Any]]`: inside `async with session.begin_nested():` call `get_proposals_page(session, status="pending", page_size=200)`, map each RenameProposal (with selectinload'd file) to a plain dict {id, filename=file.original_filename, original_path=file.current_path, proposed_filename, proposed_path, confidence}; `except Exception: logger.warning(...); return []`. Module-level structlog logger; return plain dicts (never ORM). Create rename_workspace.html: import `_workspace_scaffold.html` as ws; set subcount JS string binding `$store.pipeline` (e.g. approved/proposalsTotal where it fits) OR a static server numeral `{{ rename_proposals|length }}` per UI-SPEC copy "{n} awaiting approval · proposed filename + destination"; build the `actions` block = ONE amber-attention bulk button "APPROVE ALL ≥90% CONFIDENCE" `hx-patch="/proposals/bulk-approve-high-confidence"` `hx-target="#rename-trigger-response"` `hx-swap="innerHTML"` `hx-confirm` (the UI-SPEC confirm copy) `hx-disabled-elt="this"` (R-4 no-double-fire, no id-list/threshold param); `{% call ws.workspace(title="Review & Apply · Rename / Path", subcount=..., actions=actions) %}`; inside, a `<div id="rename-trigger-response" class="px-6 pt-4 empty:hidden"></div>` then `{% for p in rename_proposals %}{% include "pipeline/partials/_diff_row.html" with context %}{% endfor %}` passing `row_id_prefix="rename-row"`, `pid=p.id`, `file=p.filename`, `original_path=p.original_path`, `before=p.filename`, `after=p.proposed_filename`, `approve_url="/proposals/"+p.id+"/approve"` (etc.), `edit_facet="filename"`; render the empty-state ("Nothing to review" / body) when the list is empty. Create move_workspace.html as the sibling: identical shape, title "Review & Apply · Move files", `row_id_prefix="move-row"`, `before=p.original_path`, `after=p.proposed_path`, `edit_facet="path"`, empty-state "Nothing to move". Both fragments: NO `<html`/`<head`/`<header`/`{% extends`; NO second poll; NO `| safe`; NO legacy proposals/partials pill styling. Run `just tailwind` if new utility classes appear.
  </action>
  <verify>
    <automated>uv run pytest tests/test_review_apply_workspaces.py::test_review_fragments_are_bare -x && uv run mypy src/phaze/services/review.py</automated>
  </verify>
  <acceptance_criteria>
    - review.py `get_pending_proposal_rows` wraps get_proposals_page in `session.begin_nested()` and returns `[]` on exception (degrade-safe); returns plain dicts
    - rename_workspace.html and move_workspace.html each import the scaffold, render exactly one bulk button `hx-patch="/proposals/bulk-approve-high-confidence"` with `hx-confirm` + `hx-disabled-elt`, and loop `_diff_row.html`
    - move uses proposed_path facet (before=original_path, after=proposed_path, edit_facet="path"); rename uses filename facet
    - Neither fragment contains `<html`/`<head`/`hx-trigger="every"`/`| safe`
    - `uv run mypy src/phaze/services/review.py` clean
  </acceptance_criteria>
  <done>Rename and Move diff workspaces render pending proposals through the shared partial with the amber bulk header, backed by a degrade-safe read helper.</done>
</task>

<task type="auto">
  <name>Task 3: Wire shell.py rename + move stages + convert behavior tests</name>
  <files>src/phaze/routers/shell.py, tests/test_review_apply_workspaces.py</files>
  <read_first>
    - src/phaze/routers/shell.py (STAGE_PARTIALS dict :64-94, _render_stage branches :119-167, import block :33-42 — mirror the Phase-58/59 metadata/fingerprint/trackid comment convention)
    - src/phaze/templates/pipeline/partials/rename_workspace.html + move_workspace.html (the context keys they consume)
    - tests/test_review_apply_workspaces.py (the stubs to convert: test_diff_row_before_after, test_bulk_approve_high_confidence_server_predicate wiring assertion, test_edit_patch_targets_own_row wiring assertion)
    - tests/test_dead_template_guard.py + tests/test_shell_routes.py (the guards that must stay green)
  </read_first>
  <action>
    In src/phaze/routers/shell.py replace `"rename": _STAGE_PLACEHOLDER,` and `"move": _STAGE_PLACEHOLDER,` with the static string literals `"pipeline/partials/rename_workspace.html"` and `"pipeline/partials/move_workspace.html"` (T-57-01 — never an f-string/format), each with the Phase-60 comment convention (cite phase + REVIEW-01/02 + T-57-01 + supersede-in-place). Add `from phaze.services.review import get_pending_proposal_rows` (combine-as-imports, force-sort-within-sections). In `_render_stage` append `elif stage == "rename": context["rename_proposals"] = await get_pending_proposal_rows(session)` and `elif stage == "move": context["move_proposals"] = await get_pending_proposal_rows(session)` after the tracklist branch; keep `oob_counts` False (no router try/except — the helper is degrade-safe). Do NOT delete any legacy template (supersede-in-place; CUT-02 is Phase 62). Convert the three tests to real assertions: test_diff_row_before_after GETs `/s/rename` (HX) with seeded pending proposals and asserts the rose/emerald before→after cells + `hx-patch` approve + edit island + `name="proposed"`; test_bulk_approve_high_confidence_server_predicate additionally asserts the rename fragment's bulk button carries `hx-patch="/proposals/bulk-approve-high-confidence"` and NO `proposal_ids`/id-list markup; test_edit_patch_targets_own_row asserts SAVE EDIT `hx-patch` targets the row id with `hx-swap="outerHTML"` (R-6). Also assert `/s/move` renders the proposed_path facet.
  </action>
  <verify>
    <automated>uv run pytest tests/test_review_apply_workspaces.py tests/test_dead_template_guard.py tests/test_shell_routes.py -x && uv run mypy src/phaze/routers/shell.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "rename\|move" src/phaze/routers/shell.py` shows both STAGE_PARTIALS values are static literals (no f-string/format/concat)
    - `_render_stage` has `elif stage == "rename":` and `elif stage == "move":` branches calling `get_pending_proposal_rows`; oob_counts unchanged (False)
    - GET `/s/rename` and `/s/move` (HX) return bare fragments (no `<html`/`<head`) showing the seeded diff rows; move shows the path facet
    - test_diff_row_before_after / test_bulk_approve_high_confidence_server_predicate / test_edit_patch_targets_own_row pass (xfail removed)
    - `tests/test_dead_template_guard.py` and `tests/test_shell_routes.py` green; `uv run mypy src/phaze/routers/shell.py` clean
  </acceptance_criteria>
  <done>/s/rename and /s/move serve the real diff workspaces; the diff/bulk/edit behavior tests pass; guards stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /s/{stage} | `stage` is whitelisted against STAGE_PARTIALS; must never reach a template path |
| DB → diff cell | filenames/paths cross into rendered before/after HTML |
| client → bulk/edit controls | the fragment must post the server-predicate bulk (no id-list) and the own-row edit only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | shell.py STAGE_PARTIALS rename/move | mitigate | values are STATIC string literals; `stage` matched against dict keys, never spliced into a template path (unknown stage 404s) |
| T-60-01 | Tampering / EoP | rename/move bulk header button | mitigate | button posts the id-less/threshold-less `/proposals/bulk-approve-high-confidence`; the server re-queries the fixed 0.9 predicate (Plan 60-01) — the fragment carries NO client id-list, so the counts-only poll (R-2) cannot corrupt a mass-apply |
| T-60-XSS | Tampering / Info-disclosure | _diff_row.html before/after/file cells | mitigate | all DB-sourced values autoescaped via Jinja; NEVER `\| safe`; color is a caller class string, not user data |
| T-60-R6 | Tampering | inline-edit swap scope | mitigate | SAVE EDIT `hx-target` = own row id + `hx-swap="outerHTML"`; Alpine LOCAL state; never re-renders siblings and the poll never touches it (R-2) |
| T-60-DOS | Denial of Service | _render_stage rename/move branches | mitigate | get_pending_proposal_rows is SAVEPOINT-wrapped, returns [] on DB error; no router try/except; oob_counts stays False (Pitfall 5) |
| T-60-SC | Tampering | package installs | accept | Phase 60 installs nothing — no supply-chain surface |
</threat_model>

<verification>
- `uv run pytest tests/test_review_apply_workspaces.py tests/test_dead_template_guard.py tests/test_shell_routes.py -x` green
- `uv run mypy src/phaze/routers/shell.py src/phaze/services/review.py` clean
- `uv run ruff check src/phaze/routers/shell.py src/phaze/services/review.py tests/test_review_apply_workspaces.py` clean
- `just tailwind` runs cleanly if any new utility class was introduced (app.css is a gitignored build artifact)
- pre-commit hooks pass (never --no-verify)
</verification>

<success_criteria>
- One shared `_diff_row.html` serves rename (filename) and move (path) facets over one RenameProposal source (D-06), with verified PATCH verbs and an isolated inline-edit island (R-6)
- Bulk header is the id-less server-predicate approve (D-02, REVIEW-02); per-row Approve/Edit/Skip/Undo wire to the verified proposals PATCH routes (REVIEW-01)
- /s/rename and /s/move are bare fragments with static-literal STAGE_PARTIALS (T-57-01); rows inert (R-1); no second poll; dead-template + shell-route guards green
</success_criteria>

<output>
Create `.planning/phases/60-review-apply/60-02-SUMMARY.md` when done
</output>
