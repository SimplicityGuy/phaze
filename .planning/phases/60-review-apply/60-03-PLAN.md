---
phase: 60-review-apply
plan: 03
type: execute
wave: 3
depends_on: [60-02]
files_modified:
  - src/phaze/templates/pipeline/partials/propose_workspace.html
  - src/phaze/templates/pipeline/partials/tagwrite_workspace.html
  - src/phaze/services/review.py
  - src/phaze/routers/shell.py
  - tests/test_review_apply_workspaces.py
autonomous: true
requirements: [REVIEW-01, REVIEW-02]
must_haves:
  truths:
    - "D-01: Propose is a thin GENERATION view over the SAME RenameProposal source as Rename — NOT a diff; GENERATE ALL wires verbatim to POST /pipeline/proposals (the existing UI trigger), reusing _file_table.html"
    - "Tag-write reuses the shared _diff_row.html (tag facet) but its per-row APPROVE wires to POST /tags/{file_id}/write (NOT a proposals PATCH) and per-row UNDO to POST /tags/{file_id}/undo (before_tags restore, Plan 60-01)"
    - "Tag bulk header APPROVE ALL WITH NO DISCREPANCIES wires to POST /tags/bulk-write-no-discrepancies (D-03 server-predicate, no id-list)"
    - "Tag queue shows ONLY EXECUTED files (Pitfall 3 — an empty queue while files await move is correct, not a bug)"
    - "GET /s/propose and /s/tagwrite return bare fragments; STAGE_PARTIALS values are static literals (T-57-01); Propose Model column = configured settings.llm_model (A1), not a per-row value"
  artifacts:
    - path: "src/phaze/templates/pipeline/partials/propose_workspace.html"
      provides: "Propose generation view (D-01) — _file_table + GENERATE ALL"
      min_lines: 20
    - path: "src/phaze/templates/pipeline/partials/tagwrite_workspace.html"
      provides: "Tag-write diff workspace (tag facet) + tag-bulk header"
      min_lines: 20
    - path: "src/phaze/services/review.py"
      provides: "get_tagwrite_review_rows degrade-safe read helper"
      contains: "get_tagwrite_review_rows"
  key_links:
    - from: "src/phaze/routers/shell.py"
      to: "get_tagwrite_review_rows"
      via: "_render_stage tagwrite branch"
      pattern: "get_tagwrite_review_rows"
    - from: "src/phaze/templates/pipeline/partials/propose_workspace.html"
      to: "/pipeline/proposals"
      via: "GENERATE ALL trigger (existing endpoint)"
      pattern: "/pipeline/proposals"
---

<objective>
Supersede the `propose` and `tagwrite` placeholders: Propose = a thin generation view (D-01) over the same `RenameProposal` source as Rename; Tag-write = the diff workspace over the computed tag comparison, applying via the existing tag-write path and the Plan-60-01 tag-bulk/tag-undo routes.

Purpose: REVIEW-01 (Tag-write per-file Approve/Edit/Skip diff) + REVIEW-02 (Tag bulk no-discrepancies) + D-01 (fold Propose generation into the gate). Tag-write's apply wiring differs from Rename/Move — the diff partial is shared but APPROVE posts to `/tags/{id}/write` and UNDO to `/tags/{id}/undo`, NOT proposals PATCH routes.

Output: `propose_workspace.html` + `tagwrite_workspace.html` + `get_tagwrite_review_rows` in `services/review.py` + shell.py propose/tagwrite wiring + converted tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-review-apply/60-CONTEXT.md
@.planning/phases/60-review-apply/60-UI-SPEC.md
@.planning/phases/60-review-apply/60-PATTERNS.md
@.planning/phases/60-review-apply/60-02-SUMMARY.md

<interfaces>
<!-- VERIFIED contracts. Tag apply wiring is DIFFERENT from rename/move — do not use proposals PATCH for tags. -->

Propose (D-01) — generation view, NOT a diff:
  GENERATE ALL -> hx-post="/pipeline/proposals" (pipeline.py:913 trigger_proposals_ui) -> returns trigger_response.html into #propose-trigger-response
  busy-gate :disabled="$store.pipeline.proposalsBusy > 0"; confirm "Generate AI proposals for all {n} pending files? This enqueues {n} litellm jobs."
  Reuse _file_table.html columns ["File","Proposed name","Proposed path","Model","Conf"]; Model = settings.llm_model (config.py:363, A1 — configured value, NOT per-row); Conf tier: >=90 emerald-300 / 70-89 amber-300 / <70 gray-500
  NOTE: proposalsDone is a BATCH counter (not files) — use the DB pending count for the sub-count numeral.

Tag-write — diff facet via shared _diff_row.html, DIFFERENT apply wiring:
  per-row APPROVE -> hx-post="/tags/{file_id}/write" (tags.py:309 — the write IS the apply; there is NO tag pending→approved status)
  per-row UNDO   -> hx-post="/tags/{file_id}/undo"  (Plan 60-01 — before_tags restore)
  bulk header    -> hx-post="/tags/bulk-write-no-discrepancies" (Plan 60-01, D-03 server-predicate, no id-list)
  EDIT: tag inline-edit is OUT of the initial cut (tags are computed, not a stored row; existing PUT /tags/{id}/edit/{field} returns a display span and does NOT persist) — render APPROVE/SKIP/UNDO only for tag rows (no SAVE-EDIT), OR omit EDIT via a caller flag on _diff_row.
  Tag row before/after come from _build_comparison per file: current tags vs proposed tags.

get_tagwrite_review_rows(session) -> list[dict]  (NEW in services/review.py) degrade-safe; per row:
  file_id, filename, before_summary (current tags joined e.g. "artist: — · title: …"), after_summary (proposed joined), changed_count, has_blanking (any field current!=None & proposed==None)
  Source: query EXECUTED FileRecords w/ metadata (mirror list_tags :153-159), for each compute compute_proposed_tags(...) + _build_comparison + _count_changes. Wrap in session.begin_nested(); return [] on error.

shell.py: STAGE_PARTIALS["propose"]/["tagwrite"] -> static literals; _render_stage:
  elif stage == "propose": context["propose_proposals"] = await get_pending_proposal_rows(session); context["llm_model"] = <settings.llm_model>
  elif stage == "tagwrite": context["tagwrite_files"] = await get_tagwrite_review_rows(session)
  oob_counts stays False.

Copy (UI-SPEC): propose h1 "Propose" subcount "{n} proposals ready · litellm"; empty "Nothing to propose" / "Every discovered file already has a rename proposal. Run enrichment first to surface more."
  tagwrite h1 "Review & Apply · Tag write" subcount "{n} awaiting approval · mutagen will write these tags"; bulk button "APPROVE ALL WITH NO DISCREPANCIES";
  bulk confirm "Approve all pending tag-writes with no discrepancies? The server re-checks at submit — {n} match now. Reversible via undo."
  empty tagwrite "No tag changes pending" / "Every file's tags already match its proposed metadata."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create propose_workspace.html (D-01 generation view)</name>
  <files>src/phaze/templates/pipeline/partials/propose_workspace.html, src/phaze/services/review.py</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/fingerprint_workspace.html (the scaffold + single trigger + _file_table analog to copy — swap endpoint to /pipeline/proposals)
    - src/phaze/templates/pipeline/partials/_file_table.html (column/cell-dict contract, inert rows, empty-state)
    - src/phaze/services/review.py (the get_pending_proposal_rows helper from Plan 60-02 — Propose reuses it)
    - src/phaze/config.py (near :363 — the settings.llm_model source for the Model column, A1)
    - .planning/phases/60-review-apply/60-UI-SPEC.md (NEW Pattern 2 Propose columns + Copywriting)
  </read_first>
  <action>
    Create propose_workspace.html: import `_workspace_scaffold.html` as ws; subcount per UI-SPEC ("{n} proposals ready · litellm"); build an `actions` block with ONE secondary GENERATE ALL button `hx-post="/pipeline/proposals"` `hx-target="#propose-trigger-response"` `hx-swap="innerHTML"` `hx-confirm` (UI-SPEC generate-all copy) `:disabled="$store.pipeline.proposalsBusy > 0"` `hx-indicator` (R-4 bulk-enqueue guard); `{% call ws.workspace(title="Propose", subcount=..., actions=actions) %}` with a `<div id="propose-trigger-response" class="px-6 pt-4 empty:hidden"></div>` then a `<div class="p-6">` building `_file_table.html` with columns ["File","Proposed name","Proposed path","Model","Conf"] over `propose_proposals` (reusing get_pending_proposal_rows): File cell `{text: filename, mono, title: original_path}`, Proposed name `{text: proposed_filename}`, Proposed path `{text: proposed_path, mono}`, Model `{text: llm_model, mono, color: gray}`, Conf `{text: "{n}%" or "—", mono, color: tier}`; `row_id_prefix="propose-row"` (inert); empty-state per UI-SPEC. NOT a diff, NO per-row approve here (approval lives on Rename/Move). Also in services/review.py: no new helper needed for propose (reuses get_pending_proposal_rows) — but if the Conf tier / Model value needs shaping, keep it in the template. NO `<html`/`<head`, NO second poll, NO `| safe`, NO legacy proposals pill styling. Run `just tailwind` if new classes appear.
  </action>
  <verify>
    <automated>uv run pytest tests/test_review_apply_workspaces.py::test_review_fragments_are_bare tests/test_review_apply_workspaces.py::test_review_single_poll_discipline -x</automated>
  </verify>
  <acceptance_criteria>
    - propose_workspace.html exists; ONE GENERATE ALL button `hx-post="/pipeline/proposals"` with `hx-confirm` + `:disabled` busy-gate; reuses `_file_table.html`
    - Columns are exactly File · Proposed name · Proposed path · Model · Conf; Model cell renders `llm_model` (configured value, not per-row)
    - Fragment has NO `<html`/`<head`/`hx-trigger="every"`/`| safe`; rows inert (`row_id_prefix="propose-row"`, no hx-get on rows)
  </acceptance_criteria>
  <done>Propose renders the generation view (proposals list + Model + Conf + GENERATE ALL) over the shared RenameProposal read, reusing the file table.</done>
</task>

<task type="auto">
  <name>Task 2: Create tagwrite_workspace.html + get_tagwrite_review_rows helper</name>
  <files>src/phaze/templates/pipeline/partials/tagwrite_workspace.html, src/phaze/services/review.py</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/_diff_row.html (Plan 60-02 — the shared partial; how to pass tag-facet vars + suppress EDIT for tags)
    - src/phaze/routers/tags.py (list_tags :140-160 EXECUTED-only query to mirror; _build_comparison :102; _count_changes :128; compute_proposed_tags usage :343)
    - src/phaze/services/pipeline.py:769-835 (the SAVEPOINT degrade-safe read pattern to copy)
    - .planning/phases/60-review-apply/60-UI-SPEC.md (NEW Pattern 3 tag bulk + Copywriting; state/status words "no discrepancies"/"N discrepancies")
    - .planning/phases/60-review-apply/60-RESEARCH.md (Pitfall 3 EXECUTED-only; §No Analog — tag apply/undo wiring)
  </read_first>
  <action>
    In services/review.py add `get_tagwrite_review_rows(session) -> list[dict[str, Any]]`: inside `session.begin_nested()`, query EXECUTED FileRecords with `selectinload(file_metadata)` (mirror tags.list_tags), and for each load its tracklist + accepted discogs link (reuse the tags.py helpers or inline the same queries), compute `compute_proposed_tags(...)` then `_build_comparison(...)`; keep only rows the review needs (pending = no COMPLETED TagWriteLog); map to a dict {file_id, filename, before_summary, after_summary, changed_count=_count_changes(cmp), has_blanking}; `except Exception: logger.warning(...); return []`. Create tagwrite_workspace.html: import scaffold; subcount per UI-SPEC ("{n} awaiting approval · mutagen will write these tags"); `actions` = ONE amber-attention bulk button "APPROVE ALL WITH NO DISCREPANCIES" `hx-post="/tags/bulk-write-no-discrepancies"` `hx-target="#tagwrite-trigger-response"` `hx-swap="innerHTML"` `hx-confirm` (UI-SPEC copy) `hx-disabled-elt="this"`; `{% call ws.workspace(...) %}` with a `<div id="tagwrite-trigger-response" class="px-6 pt-4 empty:hidden"></div>` then a per-file loop including the shared `_diff_row.html` with tag-facet vars: `row_id_prefix="tagwrite-row"`, `pid=f.file_id`, `file=f.filename`, `before=f.before_summary`, `after=f.after_summary`, `approve_url="/tags/"+f.file_id+"/write"`, `undo_url="/tags/"+f.file_id+"/undo"`, and a caller flag (e.g. `show_edit=false`) so _diff_row renders APPROVE/SKIP/UNDO but NO SAVE-EDIT for tags (tag inline-edit is out of the initial cut). If _diff_row does not yet support a `show_edit` flag or a tag apply verb (it was built POST-style for proposals in 60-02), extend _diff_row minimally to accept `approve_method`/`skip_method` (default "patch") + `show_edit` (default true) so tag rows can POST — keep the change backward-compatible for rename/move. Render the discrepancy state word per UI-SPEC. Empty-state per UI-SPEC. NO `<html`/`<head`, NO second poll, NO `| safe`. Run `just tailwind` if new classes appear.
  </action>
  <verify>
    <automated>uv run pytest tests/test_review_apply_workspaces.py::test_review_fragments_are_bare -x && uv run mypy src/phaze/services/review.py</automated>
  </verify>
  <acceptance_criteria>
    - get_tagwrite_review_rows wraps its read in `session.begin_nested()`, returns `[]` on error, and only surfaces EXECUTED files without a COMPLETED TagWriteLog (Pitfall 3)
    - tagwrite_workspace.html bulk button posts `/tags/bulk-write-no-discrepancies`; per-row APPROVE posts `/tags/{id}/write` and UNDO posts `/tags/{id}/undo` (NOT proposals PATCH)
    - Tag rows render NO SAVE-EDIT control (tag inline-edit out of cut); `_diff_row.html` change stays backward-compatible for rename/move (they still hx-patch)
    - Fragment has NO `<html`/`<head`/`hx-trigger="every"`/`| safe`; `uv run mypy src/phaze/services/review.py` clean
  </acceptance_criteria>
  <done>Tag-write renders the computed tag diff with EXECUTED-only rows, the D-03 bulk header, and per-row write/undo wired to the tag routes.</done>
</task>

<task type="auto">
  <name>Task 3: Wire shell.py propose + tagwrite stages + convert tests</name>
  <files>src/phaze/routers/shell.py, tests/test_review_apply_workspaces.py</files>
  <read_first>
    - src/phaze/routers/shell.py (STAGE_PARTIALS + _render_stage + import block; the rename/move branches added in Plan 60-02 to mirror)
    - src/phaze/config.py (how settings/llm_model is obtained in a router — e.g. request.app.state or a get_settings())
    - src/phaze/templates/pipeline/partials/propose_workspace.html + tagwrite_workspace.html (context keys consumed)
    - tests/test_review_apply_workspaces.py (the tag-predicate wiring stub + any propose assertions; the foundation guards)
  </read_first>
  <action>
    In shell.py replace `"propose": _STAGE_PLACEHOLDER,` and `"tagwrite": _STAGE_PLACEHOLDER,` with static literals `"pipeline/partials/propose_workspace.html"` / `"pipeline/partials/tagwrite_workspace.html"` (T-57-01) + the Phase-60 comment convention. Extend the review import to also bring `get_tagwrite_review_rows`. In `_render_stage` add `elif stage == "propose": context["propose_proposals"] = await get_pending_proposal_rows(session); context["llm_model"] = <configured settings.llm_model>` (obtain the model id the same way other routers read settings — confirm via config.py; do NOT hardcode) and `elif stage == "tagwrite": context["tagwrite_files"] = await get_tagwrite_review_rows(session)`; oob_counts stays False. Do NOT delete legacy templates. Convert the tagwrite behavior test: GET `/s/tagwrite` (HX) with seeded EXECUTED files renders the tag diff rows with APPROVE `hx-post="/tags/{id}/write"` and the bulk button `hx-post="/tags/bulk-write-no-discrepancies"`; assert `/s/propose` renders the GENERATE ALL button + the Model column value. Keep the REVIEW-02 tag-predicate assertion (the endpoint behavior) satisfied by the Plan-60-01 test — this task asserts the UI WIRING to it.
  </action>
  <verify>
    <automated>uv run pytest tests/test_review_apply_workspaces.py tests/test_dead_template_guard.py tests/test_shell_routes.py -x && uv run mypy src/phaze/routers/shell.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "propose\|tagwrite" src/phaze/routers/shell.py` shows both STAGE_PARTIALS values are static literals
    - `_render_stage` has `elif stage == "propose":` (sets propose_proposals + llm_model) and `elif stage == "tagwrite":` (sets tagwrite_files) branches; oob_counts unchanged
    - GET `/s/propose` renders GENERATE ALL + Model column; GET `/s/tagwrite` renders tag diff rows with `/tags/{id}/write` + the bulk `/tags/bulk-write-no-discrepancies` wiring
    - dead-template + shell-route guards green; `uv run mypy src/phaze/routers/shell.py` clean
  </acceptance_criteria>
  <done>/s/propose and /s/tagwrite serve their real workspaces; propose generation + tag apply/bulk wiring is asserted; guards green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /s/{stage} | `stage` whitelisted; never a template path |
| DB → tag diff cell | current/proposed tag values cross into rendered HTML |
| client → tag bulk/write controls | must post the server-predicate tag-bulk (no id-list) and per-file write/undo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | shell.py STAGE_PARTIALS propose/tagwrite | mitigate | static string literals; `stage` never spliced into a template path |
| T-60-01 | Tampering / EoP | tag bulk header button | mitigate | posts the id-less `/tags/bulk-write-no-discrepancies`; server re-queries the fixed no-blank/≥1-change predicate (Plan 60-01) — no client id-list, counts-only poll cannot corrupt it |
| T-60-XSS | Tampering / Info-disclosure | tag diff cells + propose table cells | mitigate | Jinja autoescape; NEVER `\| safe` on filenames/tags/paths/model id |
| T-60-DOS | Denial of Service | _render_stage propose/tagwrite branches | mitigate | get_pending_proposal_rows + get_tagwrite_review_rows are SAVEPOINT-wrapped, [] on error; no router try/except; oob_counts False |
| T-60-SC | Tampering | package installs | accept | Phase 60 installs nothing |
</threat_model>

<verification>
- `uv run pytest tests/test_review_apply_workspaces.py tests/test_dead_template_guard.py tests/test_shell_routes.py -x` green
- `uv run mypy src/phaze/routers/shell.py src/phaze/services/review.py` clean
- `uv run ruff check src/phaze/routers/shell.py src/phaze/services/review.py tests/test_review_apply_workspaces.py` clean
- `just tailwind` clean if new utility classes were introduced
- pre-commit hooks pass (never --no-verify)
</verification>

<success_criteria>
- Propose is the D-01 generation view (GENERATE ALL → /pipeline/proposals, Model = configured llm_model) over the shared RenameProposal read — not a diff
- Tag-write reuses the shared diff partial but applies via /tags/{id}/write + /tags/{id}/undo and bulk via /tags/bulk-write-no-discrepancies (D-03); EXECUTED-only (Pitfall 3)
- /s/propose and /s/tagwrite are bare fragments with static-literal STAGE_PARTIALS; no second poll; guards green
</success_criteria>

<output>
Create `.planning/phases/60-review-apply/60-03-SUMMARY.md` when done
</output>
