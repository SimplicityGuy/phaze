---
phase: 67-backend-registry-config-model
plan: 05
type: execute
wave: 3
depends_on: ["67-02"]
files_modified:
  - src/phaze/routers/agent_s3.py
  - src/phaze/routers/agent_push.py
  - src/phaze/tasks/controller.py
  - tests/agents/routers/test_agent_s3.py
  - tests/agents/routers/test_agent_push.py
  - tests/shared/tasks/test_controller_startup_localqueue.py
autonomous: true
requirements: [REG-04]
must_haves:
  truths:
    - "The S3 post-staging callback and the push callback read the transitional `active_cloud_kind`/`active_compute_scratch_dir` instead of `cloud_target`/`compute_scratch_dir` (REG-04, D-14 Class B)."
    - "The controller LocalQueue probe gate reads `active_cloud_kind == 'kueue'` and keeps its own boot-safety try/except intact (REG-04, D-14 Class B)."
    - "controller.startup calls `settings.log_effective_registry()` so the resolved registry (id/kind/rank/cap only) is logged at boot (REG-04 startup-log)."
  artifacts:
    - path: "src/phaze/routers/agent_s3.py"
      provides: "post-staging seam reads active_cloud_kind == 'kueue'"
    - path: "src/phaze/routers/agent_push.py"
      provides: "scratch_path built from active_compute_scratch_dir"
    - path: "src/phaze/tasks/controller.py"
      provides: "LocalQueue probe gate on active_cloud_kind + wired log_effective_registry() startup call"
  key_links:
    - from: "controller.startup"
      to: "settings.log_effective_registry()"
      via: "boot-time registry log"
      pattern: "log_effective_registry"
    - from: "agent_push.report_pushed"
      to: "settings.active_compute_scratch_dir"
      via: "transitional accessor"
      pattern: "active_compute_scratch_dir"
---

<objective>
Rewire the remaining Class-B call sites — the S3 post-staging callback (agent_s3.py), the rsync push
callback (agent_push.py), and the controller LocalQueue probe gate (controller.py) — off `cloud_target`
and `compute_scratch_dir` onto the Plan-02 transitional accessors, and wire the REG-04 startup-log call
into `controller.startup`. Config-model-only: transitional accessors only, no Backend protocol
(Pitfall 1). `cloud_target` still exists (removed in Plan 06), so this lands green.

Purpose: REG-04 call-site rewire (callback + probe seam) + REG-04 boot-time registry log.
Output: rewired agent_s3.py + agent_push.py + controller.py + updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-backend-registry-config-model/67-CONTEXT.md
@.planning/phases/67-backend-registry-config-model/67-RESEARCH.md
@.planning/phases/67-backend-registry-config-model/67-PATTERNS.md
@.planning/phases/67-backend-registry-config-model/67-02-SUMMARY.md

<interfaces>
Transitional accessors + log method on ControlSettings (Plan 02):
- `active_cloud_kind: Literal["compute","kueue"] | None`
- `active_compute_scratch_dir: str | None`
- `log_effective_registry(self) -> None` (secret-free id/kind/rank/cap projection; CALL is wired here)

Call sites to rewire (grep + line-verified):
- agent_s3.py:112 `if settings.cloud_target == "k8s":` (post-staging seam) → `if settings.active_cloud_kind == "kueue":`
- agent_push.py:121 `settings.compute_scratch_dir` (scratch_path build) → `settings.active_compute_scratch_dir`
- controller.py:167 `if cfg.cloud_target == "k8s":` (LocalQueue probe gate) → `if cfg.active_cloud_kind == "kueue":` — keep the OWN broad try/except boot-safety block (D-05: a kube/Redis blip must never abort boot)

Startup-log wire: add `cfg.log_effective_registry()` in controller.startup (the natural home alongside
the existing llm-config / LocalQueue-probe boot steps).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire the S3 callback + push-scratch reads</name>
  <files>src/phaze/routers/agent_s3.py, src/phaze/routers/agent_push.py, tests/agents/routers/test_agent_s3.py, tests/agents/routers/test_agent_push.py</files>
  <read_first>
    - src/phaze/routers/agent_s3.py:105-120 (post-staging k8s seam)
    - src/phaze/routers/agent_push.py:115-128 (scratch_path build)
    - .planning/phases/67-backend-registry-config-model/67-PATTERNS.md ("agent_s3.py ... agent_push.py — Class B transitional")
  </read_first>
  <action>
    agent_s3.py:112 `if settings.cloud_target == "k8s":` → `if settings.active_cloud_kind == "kueue":`
    (keep the rowcount-guarded idempotent PUSHING→PUSHED flip below it unchanged). agent_push.py:121
    `scratch_path = f"{settings.compute_scratch_dir}/{file_id}.{file.file_type}"` →
    `f"{settings.active_compute_scratch_dir}/{file_id}.{file.file_type}"`. Mark both reads
    `# TRANSITIONAL — Phase 68`. Rewrite test_agent_s3.py and test_agent_push.py to drive the registry
    via the conftest `backends_toml_env` fixture (one kueue backend for the S3 callback path; one
    compute backend with `scratch_dir` for the push path) instead of `PHAZE_CLOUD_TARGET` /
    `PHAZE_COMPUTE_SCRATCH_DIR`; assert the idempotent flip and the built scratch_path are unchanged.
  </action>
  <verify>
    <automated>uv run pytest tests/agents/routers/test_agent_s3.py tests/agents/routers/test_agent_push.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c cloud_target src/phaze/routers/agent_s3.py` == 0; `grep -c "settings.compute_scratch_dir" src/phaze/routers/agent_push.py` == 0.
    - agent_s3.py reads `active_cloud_kind == "kueue"`; agent_push.py reads `active_compute_scratch_dir`.
    - `uv run pytest tests/agents/routers/test_agent_s3.py tests/agents/routers/test_agent_push.py -x` exits 0.
  </acceptance_criteria>
  <done>Both callbacks read the transitional accessors; the idempotent PUSHING→PUSHED flip and scratch_path construction are behavior-preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire the controller LocalQueue probe gate + wire the startup registry log</name>
  <files>src/phaze/tasks/controller.py, tests/shared/tasks/test_controller_startup_localqueue.py</files>
  <read_first>
    - src/phaze/tasks/controller.py:160-175 (LocalQueue probe gate + its own try/except boot-safety)
    - .planning/phases/67-backend-registry-config-model/67-PATTERNS.md ("controller.py (line 167) — Class B transitional")
    - .planning/phases/67-backend-registry-config-model/67-02-SUMMARY.md (log_effective_registry contract)
  </read_first>
  <action>
    controller.py:167 `if cfg.cloud_target == "k8s":` → `if cfg.active_cloud_kind == "kueue":` — keep
    the OWN broad try/except boot-safety block below it exactly (D-05: a transient kube/mesh/Redis blip
    MUST NEVER abort controller boot; the WARNING still names only the env var, never the SA token / kube
    DSN). Mark the read `# TRANSITIONAL — Phase 68`. Add a `cfg.log_effective_registry()` call in
    `controller.startup` at boot (alongside the existing llm-config / probe steps) so the resolved
    registry (id/kind/rank/cap only) is logged once at startup (REG-04). Update
    test_controller_startup_localqueue.py to drive the probe gate off a one-kueue-backend registry
    (conftest `backends_toml_env`) instead of `PHAZE_CLOUD_TARGET`; add/extend a caplog assertion that
    the startup emits the id/kind/rank/cap registry line and no secret material.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/tasks/test_controller_startup_localqueue.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c cloud_target src/phaze/tasks/controller.py` == 0.
    - controller.startup calls `log_effective_registry` (grep present).
    - The probe gate keeps its broad try/except (grep: `try:` block around the `get_local_queue()` call still present).
    - A test asserts the startup log includes the registry projection and excludes secret values.
    - `uv run pytest tests/shared/tasks/test_controller_startup_localqueue.py -x` exits 0.
  </acceptance_criteria>
  <done>The LocalQueue probe gates on the registry, keeps boot-safety, and the resolved registry is logged secret-free at startup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config layer → controller boot / agent callbacks | Registry accessors gate the LocalQueue probe + callback dispatch; effective registry logged at boot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-05-01 | Denial of service | kube/Redis blip aborting controller boot | mitigate | Preserve the LocalQueue probe's own broad try/except boot-safety (D-05); the gate swap changes only the predicate source |
| T-67-05-02 | Information disclosure | secret material in the startup registry log | mitigate | Wire `log_effective_registry()` (Plan-02 projection = id/kind/rank/cap only); the probe WARNING keeps naming only the env var, never the SA token / kube DSN (T-56-LOG) |
| T-67-05-03 | Elevation of privilege / scope creep | Backend-protocol dispatch leaking into Phase 67 | mitigate | Transitional accessor reads only; no Backend type introduced (Pitfall 1) |
| T-67-05-SC | Tampering | package installs | accept | Zero new dependencies; no install task |
</threat_model>

<verification>
- `uv run pytest tests/agents/routers/test_agent_s3.py tests/agents/routers/test_agent_push.py tests/shared/tasks/test_controller_startup_localqueue.py -x` green.
- `grep -c cloud_target src/phaze/routers/agent_s3.py src/phaze/tasks/controller.py` == 0 each.
- `uv run mypy src/phaze/routers/agent_s3.py src/phaze/routers/agent_push.py src/phaze/tasks/controller.py` clean.
- Per-wave: `uv run pytest --cov --cov-report=term-missing`.
</verification>

<success_criteria>
The S3/push callbacks and the LocalQueue probe read the transitional accessors, boot-safety is
preserved, the resolved registry is logged secret-free at startup, and the suite stays green with
`cloud_target` still present (removed in Plan 06).
</success_criteria>

<output>
Create `.planning/phases/67-backend-registry-config-model/67-05-SUMMARY.md` when done.
</output>
