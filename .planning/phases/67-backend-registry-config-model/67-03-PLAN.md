---
phase: 67-backend-registry-config-model
plan: 03
type: execute
wave: 3
depends_on: ["67-02"]
files_modified:
  - src/phaze/routers/pipeline.py
  - src/phaze/tasks/release_awaiting_cloud.py
  - src/phaze/templates/pipeline/partials/analyze_workspace.html
  - src/phaze/templates/pipeline/partials/_lane_card.html
  - src/phaze/templates/pipeline/partials/backfill_response.html
  - tests/shared/routers/test_pipeline.py
  - tests/shared/core/test_routing_seam.py
  - tests/analyze/core/test_staging_cron.py
  - tests/shared/core/test_enrich_analyze_workspaces.py
autonomous: true
requirements: [REG-04]
must_haves:
  truths:
    - "The routing seam + backfill guards read the registry-derived `cloud_enabled` instead of `cloud_target == 'local'`, byte-identical for the all-local deploy (REG-04, D-14 Class A)."
    - "The staging cron (stage_cloud_window) forks on the transitional `active_cloud_kind`/`active_cap` instead of `cloud_target`/`cloud_max_in_flight`, preserving every no-op early-return (D-14 Class B)."
    - "The Analyze lane-card templates still render: pipeline.py hands them a transitional legacy-shaped cloud value ('local' when all-local, else active_cloud_kind) (D-14 Class C)."
  artifacts:
    - path: "src/phaze/routers/pipeline.py"
      provides: "cloud_enabled routing-arg + backfill guards (395/699/763), transitional k8s ledger fork (805), Class-C template context (572)"
    - path: "src/phaze/tasks/release_awaiting_cloud.py"
      provides: "cloud_enabled gate (126) + active_cap (128) + active_cloud_kind forks (142/177)"
  key_links:
    - from: "release_awaiting_cloud.stage_cloud_window"
      to: "cfg.cloud_enabled / cfg.active_cloud_kind / cfg.active_cap"
      via: "transitional registry accessors"
      pattern: "active_cloud_kind"
    - from: "pipeline.py analyze context"
      to: "analyze_workspace.html lane cards"
      via: "transitional cloud value"
      pattern: "cloud_target|active_cloud_kind"
---

<objective>
Rewire the routing / staging-cron / backfill / presentation call sites off `cloud_target` and the flat
`cloud_max_in_flight` onto the Plan-02 registry-derived reads — WITHOUT dragging in dispatch logic
(config-model-only, Pitfall 1). Class A (on/off gates) → `cloud_enabled`; Class B (a1/k8s forks) →
the `# TRANSITIONAL — Phase 68` accessors; Class C (template context) → a transitional legacy-shaped
value so the dashboard keeps rendering. `cloud_target` still EXISTS (removed in Plan 06), so this plan
lands green; the tests are rewritten to drive the registry instead of `PHAZE_CLOUD_TARGET`.

Purpose: REG-04 call-site rewire — the routing/staging/backfill/presentation seam.
Output: rewired pipeline.py + release_awaiting_cloud.py + 3 templates + updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-backend-registry-config-model/67-CONTEXT.md
@.planning/phases/67-backend-registry-config-model/67-RESEARCH.md
@.planning/phases/67-backend-registry-config-model/67-PATTERNS.md
@.planning/phases/67-backend-registry-config-model/67-02-SUMMARY.md

<interfaces>
Registry-derived reads on ControlSettings (Plan 02):
- `cloud_enabled: bool` (property) — True iff any non-local backend
- `active_cloud_kind: Literal["compute","kueue"] | None` (# TRANSITIONAL — Phase 68)
- `active_cap: int` (# TRANSITIONAL — Phase 68) — was cloud_max_in_flight
- `active_compute_scratch_dir: str | None`, `active_kube: KubeConfig | None`, `active_bucket: BucketConfig | None`

Call sites to rewire (grep + line-verified):
- release_awaiting_cloud.py:126 `if cfg.cloud_target == "local":` → `if not cfg.cloud_enabled:` (Class A no-op)
- release_awaiting_cloud.py:128 `cfg.cloud_max_in_flight` → `cfg.active_cap` (Class B)
- release_awaiting_cloud.py:142 `if cfg.cloud_target == "a1":` (GATE-1 compute probe) → `if cfg.active_cloud_kind == "compute":` (Class B)
- release_awaiting_cloud.py:177 `if cfg.cloud_target == "k8s":` (S3-vs-rsync fork) → `if cfg.active_cloud_kind == "kueue":` (Class B)
- pipeline.py:395 & :699 `settings.cloud_target != "local"` (bool arg to _route_discovered_by_duration) → `settings.cloud_enabled` (Class A)
- pipeline.py:763 `if settings.cloud_target == "local":` backfill no-op guard → `if not settings.cloud_enabled:` (Class A)
- pipeline.py:805 `if settings.cloud_target == "k8s":` backfill ledger fork → `if settings.active_cloud_kind == "kueue":` (Class B; also the 791-805 mypy-narrowing comment block that assumed static a1/k8s)
- pipeline.py:572 `"cloud_target": settings.cloud_target` → a transitional legacy value ("local" when not cloud_enabled, else active_cloud_kind) (Class C)

Templates referencing cloud_target (keep rendering; Phase 71 owns the N-lane redesign):
- analyze_workspace.html:25,54,60,67,75 ; _lane_card.html:12 ; backfill_response.html:15
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire the staging cron (release_awaiting_cloud.py)</name>
  <files>src/phaze/tasks/release_awaiting_cloud.py, tests/analyze/core/test_staging_cron.py</files>
  <read_first>
    - src/phaze/tasks/release_awaiting_cloud.py:120-193 (the whole stage_cloud_window body)
    - .planning/phases/67-backend-registry-config-model/67-PATTERNS.md ("release_awaiting_cloud.py (Class A + Class B)")
    - tests/analyze/core/test_staging_cron.py (current PHAZE_CLOUD_TARGET-driven setup)
  </read_first>
  <action>
    In `stage_cloud_window`: line 126 `if cfg.cloud_target == "local":` → `if not cfg.cloud_enabled:`
    (keep the exact `{"staged":0,"skipped":0}` no-op — the T-50-cron-raise no-op contract is
    load-bearing); line 128 `cfg.cloud_max_in_flight` → `cfg.active_cap`; line 142 `if cfg.cloud_target
    == "a1":` (GATE-1 compute-agent probe) → `if cfg.active_cloud_kind == "compute":`; line 177
    `if cfg.cloud_target == "k8s":` (the S3-vs-rsync dispatch fork) → `if cfg.active_cloud_kind ==
    "kueue":`. Keep or migrate the `# type: ignore[attr-defined]` comments to the new accessor names;
    do NOT change any early-return semantics. Rewrite test_staging_cron.py to drive the registry via
    the `backends_toml_env` conftest fixture (a single compute backend for the a1 path, a single kueue
    backend + a bucket for the k8s path) instead of `PHAZE_CLOUD_TARGET`.
  </action>
  <verify>
    <automated>uv run pytest tests/analyze/core/test_staging_cron.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c cloud_target src/phaze/tasks/release_awaiting_cloud.py` == 0.
    - The function reads `cfg.cloud_enabled`, `cfg.active_cap`, `cfg.active_cloud_kind` (grep each present).
    - `uv run pytest tests/analyze/core/test_staging_cron.py -x` exits 0.
    - A test asserts the implicit-local (cloud_enabled False) path returns the `{"staged":0,"skipped":0}` no-op.
  </acceptance_criteria>
  <done>The staging cron forks on the registry accessors with every no-op early-return preserved; no `cloud_target` read remains in the module.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire the routing seam + backfill (pipeline.py)</name>
  <files>src/phaze/routers/pipeline.py, tests/shared/routers/test_pipeline.py, tests/shared/core/test_routing_seam.py</files>
  <read_first>
    - src/phaze/routers/pipeline.py:388-400, :560-576, :695-705, :760-840
    - .planning/phases/67-backend-registry-config-model/67-PATTERNS.md ("pipeline.py (Class A + Class C)")
  </read_first>
  <action>
    Rewire: lines 395 & 699 `settings.cloud_target != "local"` (the bool arg passed to
    `_route_discovered_by_duration`) → `settings.cloud_enabled`; line 763 `if settings.cloud_target ==
    "local":` backfill no-op guard → `if not settings.cloud_enabled:`; line 805 `if settings.cloud_target
    == "k8s":` (the held-file ledger-seed fork) → `if settings.active_cloud_kind == "kueue":` and update
    the 791-805 mypy-narrowing comment block (it assumed `cloud_target` is statically 'a1'/'k8s' after
    the local early-return; the `True` literal passed to the router stays correct because the
    `not cloud_enabled` guard already short-circuited). For the Class-C context at line 572, replace
    `"cloud_target": settings.cloud_target` with a transitional legacy-shaped value: `"local"` when
    `not settings.cloud_enabled` else `settings.active_cloud_kind` (bind it under the same
    `cloud_target` context key the templates read — Task 3 keeps the key). Update test_pipeline.py and
    test_routing_seam.py to drive the registry (conftest `backends_toml_env`) rather than
    `PHAZE_CLOUD_TARGET`; assert the backfill k8s ledger-skip vs a1 ledger-seed behavior is unchanged.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/routers/test_pipeline.py tests/shared/core/test_routing_seam.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cloud_target ==" src/phaze/routers/pipeline.py` == 0 (no equality reads of the removed field remain).
    - pipeline.py passes `settings.cloud_enabled` to `_route_discovered_by_duration` at both call sites.
    - The backfill ledger fork reads `settings.active_cloud_kind == "kueue"`.
    - `uv run pytest tests/shared/routers/test_pipeline.py tests/shared/core/test_routing_seam.py -x` exits 0.
  </acceptance_criteria>
  <done>The duration router arg, both backfill guards, and the k8s ledger fork read the registry; the a1-vs-k8s backfill ledger behavior is preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Keep the Analyze lane-card templates rendering on the transitional cloud value</name>
  <files>src/phaze/templates/pipeline/partials/analyze_workspace.html, src/phaze/templates/pipeline/partials/_lane_card.html, src/phaze/templates/pipeline/partials/backfill_response.html, tests/shared/core/test_enrich_analyze_workspaces.py</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/analyze_workspace.html:25,54-75
    - src/phaze/templates/pipeline/partials/_lane_card.html:12 ; backfill_response.html:15
    - .planning/phases/67-backend-registry-config-model/67-PATTERNS.md ("Templates (3 files, Class C)")
  </read_first>
  <action>
    The three partials read a `cloud_target` context var to derive the A1/k8s lane "not configured"
    labels. Task 2 now binds that same `cloud_target` context key to the transitional legacy value
    ('local' | 'compute'... — note: the templates compare against `'a1'`/`'k8s'`). Reconcile the
    template comparisons to the transitional value the router supplies: since the registry kinds are
    `compute`/`kueue` (not `a1`/`k8s`), update the template conditionals to compare against
    `'compute'`/`'kueue'` (or have Task 2 map kind→legacy label — pick ONE mapping and keep it
    consistent across all three partials and the router). Do NOT redesign the lanes (BEUI-01 N-lane UI
    is Phase 71) — the minimal change is to keep the existing 3-lane render working with the new value.
    Refresh the comment at analyze_workspace.html:25 (drop the stale `config.py:406` reference). Update
    test_enrich_analyze_workspaces.py to render with a registry-driven context and assert the lane
    states still resolve.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/core/test_enrich_analyze_workspaces.py -x</automated>
  </verify>
  <acceptance_criteria>
    - The three partials no longer break on the removed `cloud_target=='a1'|'k8s'` literals — they compare against the transitional value the router supplies (grep the chosen mapping is consistent across router + 3 templates).
    - `uv run pytest tests/shared/core/test_enrich_analyze_workspaces.py -x` exits 0.
    - A render test asserts the Analyze workspace renders (no template exception) for the implicit-local registry.
  </acceptance_criteria>
  <done>The dashboard Analyze lanes render on the transitional cloud value with no template exception; no lane redesign.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config layer → router/task tiers | Registry-derived `cloud_enabled` / transitional accessors consumed by routing, staging cron, backfill, template context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-03-01 | Elevation of privilege / scope creep | dispatch logic leaking into Phase 67 | mitigate | Rewire reads ONLY (`cloud_enabled` / transitional accessors); import NO Backend protocol or new dispatch type (Pitfall 1); grep the plan added no `services/kube_staging`/`services/s3_staging` import to pipeline.py or release_awaiting_cloud.py |
| T-67-03-02 | Denial of service | staging cron losing a no-op early-return → unbounded push work | mitigate | Preserve every `{"staged":0,"skipped":0}` no-op; the `not cloud_enabled` gate is byte-identical to the old `cloud_target=="local"` for the all-local deploy |
| T-67-03-03 | Tampering | template render breaks on removed literal → dashboard 500 | mitigate | Router supplies a transitional value under the existing `cloud_target` context key; templates compare against the consistent mapping (Class C, Assumption A4 verified at plan-time) |
| T-67-03-SC | Tampering | package installs | accept | Zero new dependencies; no install task |
</threat_model>

<verification>
- `uv run pytest tests/analyze/core/test_staging_cron.py tests/shared/routers/test_pipeline.py tests/shared/core/test_routing_seam.py tests/shared/core/test_enrich_analyze_workspaces.py -x` green.
- `grep -c cloud_target src/phaze/tasks/release_awaiting_cloud.py` == 0.
- `uv run mypy src/phaze/routers/pipeline.py src/phaze/tasks/release_awaiting_cloud.py` clean.
- Per-wave: `uv run pytest --cov --cov-report=term-missing`.
</verification>

<success_criteria>
The routing/staging/backfill/presentation seam reads the registry-derived gates and transitional
accessors, all-local behavior is byte-identical, the dashboard renders, and the suite stays green with
`cloud_target` still present (removed in Plan 06).
</success_criteria>

<output>
Create `.planning/phases/67-backend-registry-config-model/67-03-SUMMARY.md` when done.
</output>
