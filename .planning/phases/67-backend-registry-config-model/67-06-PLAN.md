---
phase: 67-backend-registry-config-model
plan: 06
type: execute
wave: 4
depends_on: ["67-02", "67-03", "67-04", "67-05"]
files_modified:
  - src/phaze/config.py
  - tests/shared/config/test_cloud_target.py
  - tests/shared/config/test_kube_settings.py
  - tests/shared/config/test_s3_settings.py
  - tests/shared/core/test_config_role_split.py
  - tests/BUCKETS.md
  - .env.example
  - docs/configuration.md
autonomous: true
requirements: [REG-04]
must_haves:
  truths:
    - "`cloud_target`, `cloud_max_in_flight`, `compute_scratch_dir`, the flat `s3_*` connection/cred fields, and the flat `kube_*` connection/manifest fields no longer exist on ControlSettings (REG-04, D-12)."
    - "The three `_enforce_s3_config_when_k8s` / `_enforce_compute_scratch_dir_when_a1` / `_enforce_kube_config_when_k8s` validators are removed (their per-variant equivalents live on the submodels) (REG-02, D-12)."
    - "SECRET_FILE_FIELDS no longer lists the per-entry secrets (s3_access_key_id/s3_secret_access_key/kube_kubeconfig/kube_sa_token); control-plane secrets (database_url/redis_url/queue_url/openai/anthropic) stay on the env `_FILE` path (D-05)."
    - "No back-compat shim exists — backends.toml is the sole config surface; the implicit-local default is the only compatibility path (D-11, D-12)."
    - "`.env.example` and docs describe the backends.toml surface; the removed cloud env vars are gone with a loud breaking callout."
  artifacts:
    - path: "src/phaze/config.py"
      provides: "flat-field + validator removal; trimmed SECRET_FILE_FIELDS; kept D-15 global knobs"
    - path: ".env.example"
      provides: "backends.toml surface documented; removed cloud vars deleted with breaking callout"
  key_links:
    - from: "ControlSettings"
      to: "backends.toml (sole config surface)"
      via: "no flat cloud fields, no shim"
      pattern: "backends"
---

<objective>
The removal wave. Every reader of the flat fields was rewired to registry-derived reads in Waves 2-3,
so now delete `cloud_target`, `cloud_max_in_flight`, `compute_scratch_dir`, the flat `s3_*`
connection/credential fields, and the flat `kube_*` connection/manifest fields from `ControlSettings`,
drop the three `_enforce_*_when_*` validators, trim `SECRET_FILE_FIELDS` to control-plane secrets only,
delete the now-invalid config tests, and update `.env.example`/docs to the `backends.toml` surface.
NO back-compat shim (D-11/D-12) — the implicit-local default is the sole compatibility path. Keep the
D-15 global tuning knobs and the transitional accessors (accessors are deleted in Phase 68).

Purpose: REG-04 removal (`cloud_target` + flat fields + 3 validators, no shim) + docs maintenance.
Output: trimmed config.py + deleted/updated tests + refreshed `.env.example`/docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-backend-registry-config-model/67-CONTEXT.md
@.planning/phases/67-backend-registry-config-model/67-RESEARCH.md
@.planning/phases/67-backend-registry-config-model/67-PATTERNS.md
@.planning/phases/67-backend-registry-config-model/67-03-SUMMARY.md
@.planning/phases/67-backend-registry-config-model/67-04-SUMMARY.md
@.planning/phases/67-backend-registry-config-model/67-05-SUMMARY.md

<interfaces>
Removals in config.py (line-verified against the pre-Wave-2 file; recompute after Plan 02's additions):
- `cloud_target` (406-410), `cloud_max_in_flight` (417-423), `compute_scratch_dir` (453-457)
- flat s3_* connection/creds: s3_endpoint_url/s3_bucket/s3_region/s3_addressing_style/s3_access_key_id/s3_secret_access_key (466-495) + `_validate_s3_endpoint_url` (597-613, now lifted onto BucketConfig in Plan 01)
- flat kube_* (534-595): api_url/namespace/local_queue/job_image/job_cpu_request/job_memory_request/workload_api_version/ca_secret_name/env_configmap_name/env_secret_name/kubeconfig/sa_token
- validators: `_enforce_s3_config_when_k8s` (615-634), `_enforce_compute_scratch_dir_when_a1` (636-655), `_enforce_kube_config_when_k8s` (657-681)
- SECRET_FILE_FIELDS trim (348-355): drop s3_access_key_id/s3_secret_access_key/kube_kubeconfig/kube_sa_token; KEEP openai_api_key/anthropic_api_key + inherited database_url/redis_url/queue_url (D-05)

KEEP (D-15 global tuning knobs — do NOT remove): push_max_attempts, cloud_submit_max_attempts,
cloud_route_threshold_sec, s3_presign_put_ttl_sec, s3_presign_get_ttl_sec, s3_lifecycle_ttl_days,
s3_multipart_part_size_bytes. KEEP the Plan-02 registry fields, container validator, cloud_enabled,
and ALL transitional accessors (removed only in Phase 68).

Tests to DELETE (assert removed fields): tests/shared/config/test_cloud_target.py,
test_kube_settings.py, test_s3_settings.py.
Test to UPDATE: tests/shared/core/test_config_role_split.py:257 `test_env_example_documents_cloud_target`
→ assert the backends.toml surface (drop the a1/k8s literal assertions; keep the no-legacy-token gate).
Doc to UPDATE: tests/BUCKETS.md; .env.example:171-200 (the PHAZE_CLOUD_TARGET / PHAZE_S3_* / PHAZE_KUBE_* block);
docs/configuration.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the flat fields + 3 validators + trim SECRET_FILE_FIELDS from config.py</name>
  <files>src/phaze/config.py</files>
  <read_first>
    - src/phaze/config.py:338-355 (SECRET_FILE_FIELDS), :397-681 (the block to prune), and the Plan-02-added registry section
    - .planning/phases/67-backend-registry-config-model/67-PATTERNS.md ("src/phaze/config.py (MODIFIED — ControlSettings registry)", "KEEP unchanged (D-15 global tuning knobs)")
  </read_first>
  <action>
    Delete from `ControlSettings`: `cloud_target`, `cloud_max_in_flight`, `compute_scratch_dir`, the six
    flat `s3_*` connection/credential fields, all twelve flat `kube_*` fields, the `_validate_s3_endpoint_url`
    field_validator (its logic now lives per-bucket in config_backends), and the three
    `_enforce_s3_config_when_k8s` / `_enforce_compute_scratch_dir_when_a1` / `_enforce_kube_config_when_k8s`
    model validators. Trim `SECRET_FILE_FIELDS` to drop `s3_access_key_id`, `s3_secret_access_key`,
    `kube_kubeconfig`, `kube_sa_token` — KEEP `openai_api_key`/`anthropic_api_key` and the inherited
    `database_url`/`redis_url`/`queue_url` (D-05: control-plane secrets stay on env `_FILE`). Do NOT
    remove the D-15 global tuning knobs (push_max_attempts, cloud_submit_max_attempts,
    cloud_route_threshold_sec, s3_presign_*_ttl_sec, s3_lifecycle_ttl_days, s3_multipart_part_size_bytes),
    the Plan-02 registry fields/validator/cloud_enabled, or any transitional accessor. Introduce NO
    back-compat alias/shim for the removed names (D-11).
  </action>
  <verify>
    <automated>uv run pytest tests/shared/config/ -x --ignore=tests/shared/config/test_cloud_target.py --ignore=tests/shared/config/test_kube_settings.py --ignore=tests/shared/config/test_s3_settings.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cloud_target" src/phaze/config.py` == 0.
    - `grep -cE "s3_endpoint_url|s3_bucket\b|s3_access_key_id|kube_api_url|kube_sa_token|kube_kubeconfig|compute_scratch_dir|cloud_max_in_flight|_enforce_s3_config_when_k8s|_enforce_kube_config_when_k8s|_enforce_compute_scratch_dir_when_a1" src/phaze/config.py` == 0.
    - `grep -c "s3_multipart_part_size_bytes\|s3_presign_put_ttl_sec\|cloud_route_threshold_sec" src/phaze/config.py` ≥ 3 (D-15 knobs kept).
    - `uv run mypy .` exits 0 (all rewired call sites resolve; no dangling flat-field read).
  </acceptance_criteria>
  <done>The flat cloud config surface and its three validators are gone with no shim; control-plane secrets and D-15 global knobs remain; the registry is the sole surface.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the stale config tests + update the role-split + BUCKETS doc</name>
  <files>tests/shared/config/test_cloud_target.py, tests/shared/config/test_kube_settings.py, tests/shared/config/test_s3_settings.py, tests/shared/core/test_config_role_split.py, tests/BUCKETS.md</files>
  <read_first>
    - tests/shared/core/test_config_role_split.py:257-275 (the .env.example cloud_target assertion to rewrite)
    - .planning/phases/67-backend-registry-config-model/67-RESEARCH.md (Wave 0 Gaps: DELETE/REWRITE list)
  </read_first>
  <action>
    Delete tests/shared/config/test_cloud_target.py, test_kube_settings.py, test_s3_settings.py (they
    assert removed fields — RESEARCH Wave 0 note; deleting avoids a false-positive green). Rewrite
    `test_env_example_documents_cloud_target` in test_config_role_split.py to assert the new
    `backends.toml` surface is documented in `.env.example` (e.g. `PHAZE_BACKENDS_CONFIG_FILE` is
    mentioned) and to KEEP the no-dead-token gate (assert the removed `PHAZE_CLOUD_TARGET` /
    `PHAZE_CLOUD_BURST_ENABLED` / `cloud_burst` tokens are NOT present as live settings); drop the
    `("local","a1","k8s")` literal assertions. Update tests/BUCKETS.md to describe the `[[buckets]]`
    registry + scope semantics instead of the flat single-global S3 config.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/core/test_config_role_split.py -x</automated>
  </verify>
  <acceptance_criteria>
    - The three deleted test files no longer exist (`test -e` fails for each).
    - `test_config_role_split.py` no longer asserts `PHAZE_CLOUD_TARGET` is present; it asserts the backends.toml surface + the no-dead-token gate.
    - `uv run pytest tests/shared/core/test_config_role_split.py -x` exits 0.
  </acceptance_criteria>
  <done>The removed-field tests are gone; the role-split test enforces the new surface and the no-dead-token gate; BUCKETS.md reflects the bucket registry.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh .env.example + docs to the backends.toml surface</name>
  <files>.env.example, docs/configuration.md</files>
  <read_first>
    - .env.example:168-201 (the PHAZE_CLOUD_TARGET / PHAZE_COMPUTE_SCRATCH_DIR / PHAZE_KUBE_* / PHAZE_S3_* block)
    - .planning/phases/67-backend-registry-config-model/67-RESEARCH.md (Runtime State Inventory: "Update .env.example / compose docs to the backends.toml surface")
    - .planning/phases/67-backend-registry-config-model/67-CONTEXT.md (Specific Ideas: illustrative backends.toml)
  </read_first>
  <action>
    In `.env.example`, delete the removed cloud env vars (`PHAZE_CLOUD_TARGET`,
    `PHAZE_COMPUTE_SCRATCH_DIR`, `PHAZE_CLOUD_MAX_IN_FLIGHT`, `PHAZE_KUBE_*`, `PHAZE_S3_*`
    connection/creds) and replace the block with the `PHAZE_BACKENDS_CONFIG_FILE` pointer (default
    `/etc/phaze/backends.toml`) + a short pointer to the backends.toml surface, with a LOUD breaking
    callout that the old `PHAZE_CLOUD_TARGET` / flat vars are removed with no shim (mirror the existing
    "BREAKING RENAME" style so the operator deletes the dead vars; do NOT re-introduce the forbidden
    `PHAZE_CLOUD_BURST_ENABLED` / `cloud_burst` tokens). KEEP the D-15 global knobs that still exist
    (`PHAZE_S3_PRESIGN_*`, `PHAZE_S3_LIFECYCLE_TTL_DAYS`, `PHAZE_S3_MULTIPART_PART_SIZE_BYTES`,
    `PHAZE_PUSH_MAX_ATTEMPTS`, `PHAZE_CLOUD_SUBMIT_MAX_ATTEMPTS`, `PHAZE_CLOUD_ROUTE_THRESHOLD_SEC`) as
    documented env vars. In docs/configuration.md, add a `backends.toml` section describing the
    array-of-tables schema (backends id/kind/rank/cap + per-kind connection config + inline `*_file`
    secrets; `[[buckets]]` id/scope/endpoint/creds; scope cardinality), and note the implicit-local
    zero-config default. Do NOT rewrite docs/k8s-burst.md / docs/cloud-burst.md deeply (BEUI-03 full
    runbook is Phase 71) — a minimal cross-reference is sufficient.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/core/test_config_role_split.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PHAZE_CLOUD_TARGET\|PHAZE_KUBE_API_URL\|PHAZE_S3_BUCKET\|PHAZE_COMPUTE_SCRATCH_DIR" .env.example` == 0.
    - `.env.example` contains `PHAZE_BACKENDS_CONFIG_FILE` and a breaking-removal callout; `cloud_burst` / `PHAZE_CLOUD_BURST_ENABLED` do NOT appear.
    - docs/configuration.md contains a `backends.toml` section mentioning `[[backends]]`, `[[buckets]]`, and `scope`.
  </acceptance_criteria>
  <done>Operator-facing config docs describe the backends.toml surface; the removed cloud env vars are gone with a loud callout and no shim reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env/.env → config layer | Removed cloud env vars must not silently linger as dead config; sole surface is backends.toml |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-06-01 | Repudiation / operator confusion | dead `PHAZE_CLOUD_TARGET`/flat vars silently ignored after removal | mitigate | `.env.example` carries a loud breaking-removal callout; the role-split test's no-dead-token gate fails if a forbidden legacy token reappears |
| T-67-06-02 | Tampering | accidental removal of a D-15 global knob or a control-plane secret from SECRET_FILE_FIELDS | mitigate | Acceptance grep asserts the D-15 knobs remain and that database_url/redis_url/queue_url/openai/anthropic stay in SECRET_FILE_FIELDS; `uv run mypy .` catches any dangling read |
| T-67-06-03 | Denial of service | a missed flat-field reader breaking at runtime post-removal | mitigate | Removal is gated behind Waves 3 (all readers rewired); `uv run mypy .` + full suite must be green before merge |
| T-67-06-SC | Tampering | package installs | accept | Zero new dependencies; no install task |
</threat_model>

<verification>
- Full suite green: `uv run pytest --cov --cov-report=term-missing` (≥85% coverage, CLAUDE.md).
- `uv run mypy .` clean (excludes tests/ per config).
- `grep -rc "cloud_target" src/phaze` == 0 across the package.
- No back-compat alias for the removed names (grep: no `cloud_target =` assignment survives).
- `pre-commit run --all-files` passes (never `--no-verify`).
</verification>

<success_criteria>
`cloud_target` and every flat `s3_*`/`kube_*`/`compute_*` field + the three validators are removed with
no shim; SECRET_FILE_FIELDS holds only control-plane secrets; the D-15 global knobs and the registry
remain; `.env.example`/docs describe the backends.toml surface; the full suite + mypy + pre-commit are
green. The implicit-local default is the sole compatibility path (D-03) — the live all-local deploy
needs zero config edits.
</success_criteria>

<output>
Create `.planning/phases/67-backend-registry-config-model/67-06-SUMMARY.md` when done.
</output>
