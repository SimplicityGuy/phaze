---
phase: 84-dedup-fingerprint-progress-cutover
plan: 06
type: execute
wave: 4
depends_on: [84-03, 84-04, 84-05]
files_modified: []
autonomous: false
requirements: [SIDECAR-02]
must_haves:
  truths:
    - "D-16: SC#3 proven way 2 — a live-corpus `just shadow-compare --database-url <restore>` run AFTER 035, BEFORE merge, asserting exit code 0 and TOTALS: hard_fail_total=0 with the duplicate_resolved invariant line reading 0 divergent"
    - "D-00b: the live run is the only proof that 035 repaired the real post-032 resolved-without-marker rows — the CI synthetic corpus cannot contain them (which is exactly why D-01 went unnoticed)"
    - "The before/after GET /api/v1/fingerprint/progress readings are recorded so the completed-jump / failed-drop is documented as the fix, not read as breakage"
  artifacts:
    - path: ".planning/phases/84-dedup-fingerprint-progress-cutover/84-06-SUMMARY.md"
      provides: "Recorded live shadow-compare TOTALS + before/after fingerprint-progress counts"
      contains: "hard_fail_total"
  key_links:
    - from: "just shadow-compare --database-url <restore>"
      to: "services/shadow_compare.py:135 (hard duplicate_resolved invariant)"
      via: "python -m phaze.cli.shadow_compare (exit 1 iff any hard invariant diverged)"
      pattern: "hard_fail_total"
---

<objective>
Run the standing Phase-79 shadow-compare gate against a restore of the live corpus AFTER migration `035`
has applied and BEFORE merge (D-16.2), proving `035` repaired the real post-`032`
`state='duplicate_resolved'`-without-marker rows that the CI synthetic corpus cannot contain. Record the
`hard_fail_total=0` result and the before/after `get_fingerprint_progress` counts in the SUMMARY.

Purpose: Phase 79 built the gate re-runnable but deferred the live run (79 D-02) — which is exactly why
D-01 (the missing writer) went unnoticed. This plan closes that gap. It is operator-manual because CI
cannot see the production rows; it cannot be automated in this phase.
Output: recorded live-run result in `84-06-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-dedup-fingerprint-progress-cutover/84-CONTEXT.md
@.planning/phases/84-dedup-fingerprint-progress-cutover/84-VALIDATION.md

@src/phaze/cli/shadow_compare.py
@src/phaze/services/shadow_compare.py
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Live-corpus shadow-compare run after 035 (D-16.2)</name>
  <files>.planning/phases/84-dedup-fingerprint-progress-cutover/84-06-SUMMARY.md</files>
  <action>
    Operator-manual (no CLI/API the executor can run in CI — the production rows only exist on a live
    restore). Follow the numbered steps in how-to-verify: restore into a `_test`-suffixed DB, apply
    `035` via `alembic upgrade head`, run `just shadow-compare --database-url <restore-dsn>`, and record
    the invariant TOTALS + before/after `get_fingerprint_progress` counts (NO DSN) in the SUMMARY.
  </action>
  <what-built>
    Migration `035` (84-01), the dedup marker cutover (84-03), the fingerprint-progress cutover (84-04),
    and the AST guard (84-05) are all in place. The Phase-79 `just shadow-compare` runner already exists
    (`justfile:482-485` → `python -m phaze.cli.shadow_compare`; `--database-url` is password-safe via
    `make_url`; exit code 1 iff any HARD invariant diverged).
  </what-built>
  <how-to-verify>
    1. Restore a recent live DB snapshot into a database whose name ends in `_test` (the destructive-write
       guard refuses any other name; this run is read-only but reuse a safe DB).
    2. `alembic upgrade head` against the restore (applies `035`; `auto_migrate`/`PHAZE_AUTO_MIGRATE`
       otherwise applies it at app boot).
    3. Capture the BEFORE `get_fingerprint_progress` reading against the restore via the curl recipe
       (`justfile:500`) or `GET /api/v1/fingerprint/progress`.
    4. Run `just shadow-compare --database-url <restore-dsn>`. Assert exit code `0` and the render shows
       `TOTALS: hard_fail_total=0`; the `duplicate_resolved` invariant line reads `0 divergent`.
    5. Capture the AFTER `get_fingerprint_progress` reading. Record BEFORE/AFTER in the SUMMARY (the
       `completed` jump and `failed` drop are the fix, D-11 — not a regression).
    6. Do NOT paste the `--database-url` DSN (it carries a password) into the SUMMARY, CI logs, or the PR;
       record only the invariant TOTALS and the count deltas. `make_url` keeps the DSN password-safe in
       the tool's own output — do not defeat that by echoing the raw DSN.
    If `hard_fail_total > 0`: the `duplicate_resolved` divergences are files `035` did not repair — do NOT
    merge; return to 84-01 to widen the reconcile before proceeding.
  </how-to-verify>
  <acceptance_criteria>
    - The run exited `0` with `hard_fail_total=0` and the `duplicate_resolved` invariant line `0 divergent`, recorded verbatim (counts only, no DSN) in `84-06-SUMMARY.md`.
    - BEFORE/AFTER `get_fingerprint_progress` counts recorded with the fix framing.
    - No DSN/password appears in the SUMMARY, PR body, or any committed log.
  </acceptance_criteria>
  <verify>
    <human-check>Operator confirms `just shadow-compare --database-url <restore>` exited 0 with `TOTALS: hard_fail_total=0` and the `duplicate_resolved` invariant line `0 divergent`, recorded in the SUMMARY (no DSN).</human-check>
  </verify>
  <done>Live-corpus shadow-compare recorded with hard_fail_total=0 and before/after fingerprint-progress counts; no DSN leaked.</done>
  <resume-signal>Type "approved" with the recorded `hard_fail_total=0` result, or paste the divergence report if it failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator shell → live-corpus restore DB | `just shadow-compare --database-url <dsn>` connects to a restore; the DSN carries a password. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-06-01 | Information Disclosure | the `--database-url` DSN password leaking into logs/SUMMARY/PR | mitigate | `make_url` renders the DSN password-safe in the tool's own output; the run is operator-manual and off-CI; the acceptance criteria forbid pasting the raw DSN — record only TOTALS + count deltas. |
| T-84-06-02 | Tampering | accidental write to the live restore | mitigate | The shadow-compare core is read-only; the destructive-write guard refuses any DB whose name does not end in `_test`; restore into a `_test`-suffixed DB. |
| T-84-06-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies (reuses the existing Phase-79 runner). |
</threat_model>

<verification>
- Live `just shadow-compare` exits 0 with `hard_fail_total=0`, `duplicate_resolved` line `0 divergent`.
- Before/after fingerprint-progress counts recorded; no DSN leaked.
</verification>

<success_criteria>
The live-corpus shadow-compare run proves `035` repaired the real resolved-without-marker rows
(`hard_fail_total=0`), closing the D-01 gap that Phase 79's deferred live run left open, and the
fingerprint-progress count changes are documented as the intended fix.
</success_criteria>

<output>
Create `.planning/phases/84-dedup-fingerprint-progress-cutover/84-06-SUMMARY.md` when done, recording the
live TOTALS and the before/after counts (no DSN).
</output>
