---
phase: 53-s3-object-staging-leg
plan: 04
type: execute
wave: 3
depends_on: ["53-01", "53-02", "53-03"]
files_modified:
  - src/phaze/services/cloud_staging.py
  - src/phaze/routers/agent_s3.py
  - src/phaze/main.py
  - tests/test_services/test_cloud_staging.py
  - tests/test_routers/test_agent_s3.py
autonomous: true
requirements: [KSTAGE-01, KSTAGE-04]

must_haves:
  truths:
    - "A control-plane producer can stage a file: create the cloud_job row, initiate the multipart upload, presign parts, and enqueue upload_file_s3 through the single enqueue seam"
    - "The upload-complete callback completes the multipart upload control-side (never the agent) and flips cloud_job UPLOADING→UPLOADED idempotently"
    - "The upload-failure callback re-drives the upload up to a bounded cap, then terminally fails it — aborting the multipart and deleting the staged object"
    - "The agent_s3 router is mounted on the app"
    - "The producer/re-drive enqueue is unit-tested but NOT wired into stage_cloud_window (Phase 55 owns that)"
  artifacts:
    - path: "src/phaze/services/cloud_staging.py"
      provides: "stage_file_to_s3 producer + re-drive helper (control-plane orchestration)"
      contains: "async def stage_file_to_s3"
    - path: "src/phaze/routers/agent_s3.py"
      provides: "upload-complete + upload-failure control-side callbacks"
      contains: "/{file_id}/uploaded"
  key_links:
    - from: "src/phaze/routers/agent_s3.py"
      to: "src/phaze/services/s3_staging.py"
      via: "complete_multipart_upload / abort_multipart_upload / delete_staged_object (control-side)"
      pattern: "complete_multipart_upload"
    - from: "src/phaze/services/cloud_staging.py"
      to: "enqueue_router single seam"
      via: "select_active_agent + task_router.queue_for + enqueue upload_file_s3"
      pattern: "select_active_agent"
    - from: "src/phaze/main.py"
      to: "agent_s3.router"
      via: "app.include_router"
      pattern: "agent_s3"
---

<objective>
Build the control-side orchestration: a `cloud_staging` producer that stages a file (cloud_job row +
multipart init + part presign + enqueue through the single seam), and the `agent_s3` callbacks that
complete the multipart upload control-side, flip cloud_job state idempotently, and run the bounded
re-drive / terminal-cleanup loop. Mount the router.

Purpose: KSTAGE-01 (the control plane completes the multipart — never the agent) + KSTAGE-04
(terminal cleanup: abort + delete). The producer is the upload-trigger seam built and unit-tested
here; Phase 55 wires it into stage_cloud_window.
Output: `cloud_staging.py`, `agent_s3.py`, main.py registration, and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-s3-object-staging-leg/53-CONTEXT.md
@.planning/phases/53-s3-object-staging-leg/53-PATTERNS.md
@.planning/phases/53-s3-object-staging-leg/53-01-SUMMARY.md
@.planning/phases/53-s3-object-staging-leg/53-02-SUMMARY.md
@.planning/phases/53-s3-object-staging-leg/53-03-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase. Use these directly — no exploration needed. -->

agent_push callbacks — the EXACT control-side pattern to mirror (src/phaze/routers/agent_push.py):
- router = APIRouter(prefix="/api/internal/agent/push", tags=["agent-internal"])
- auth: agent: Annotated[Agent, Depends(get_authenticated_agent)]; session: Annotated[AsyncSession, Depends(get_session)]
- idempotent state flip with rowcount guard (lines 103-118): res = cast("CursorResult[Any]", await session.execute(update(...).where(... state==EXPECTED).values(...))); if res.rowcount == 0: commit + idempotent 200
- ledger re-drive loop (lines 141-233): read attempt counter from SchedulingLedger.payload JSONB (default 0), compare to ControlSettings.push_max_attempts cap, terminal at cap (clear ledger), else re-enqueue on selected agent queue with explicit SAQ timeout
- clean 200 hold when select_active_agent raises NoActiveAgentError (never 500)

select_active_agent (src/phaze/services/enqueue_router.py:94): select_active_agent(session, kind="fileserver"|"compute") → Agent; raises NoActiveAgentError
queue resolution: request.app.state.task_router.queue_for(agent.id) → SAQ Queue; await queue.connect(); await queue.enqueue("s3_upload", key=ledger_key, timeout=UPLOAD_FILE_SAQ_TIMEOUT_SEC, **dumped)
clear_ledger_entry(session, key) — src/phaze/services/scheduling_ledger.py

s3_staging (Plan 02): create_multipart_upload(file_id)->upload_id, presign_upload_parts(file_id, upload_id, part_count)->list[str], complete_multipart_upload(file_id, upload_id, parts), abort_multipart_upload(file_id, upload_id), delete_staged_object(file_id), staged_object_key(file_id)
UploadFileS3Payload + UploadedRequest/UploadedResponse + UploadFailedRequest/UploadFailedResponse (Plan 03)
UPLOAD_FILE_SAQ_TIMEOUT_SEC (src/phaze/tasks/s3_upload.py, Plan 03)
CloudJob / CloudJobStatus (Plan 01): file_id (unique), s3_key, status, upload_id
FileRecord (src/phaze/models/file.py): file_size: int (BigInteger), original_path, file_type, sha256_hash

main.py router registration (src/phaze/main.py:201-207): app.include_router(agent_files.router) ... — add agent_s3 here.
ControlSettings.push_max_attempts (config.py:413) — reuse as the upload re-drive cap (or add s3_upload_max_attempts if cleaner; Plan 01 may already provide one).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cloud_staging producer + re-drive helper</name>
  <files>src/phaze/services/cloud_staging.py, tests/test_services/test_cloud_staging.py</files>
  <read_first>
    - src/phaze/routers/agent_push.py:120-130, 203-216 (producer enqueue idiom: queue_for + connect + enqueue with explicit timeout)
    - src/phaze/services/enqueue_router.py:94-119 (select_active_agent kind-scoping)
    - src/phaze/services/s3_staging.py (Plan 02 — create_multipart_upload, presign_upload_parts, staged_object_key)
    - src/phaze/models/cloud_job.py (Plan 01 — CloudJob, CloudJobStatus)
    - src/phaze/schemas/agent_s3.py (Plan 03 — UploadFileS3Payload)
    - .planning/phases/53-s3-object-staging-leg/53-CONTEXT.md §Decisions (D-01 multipart, upload-trigger-seam discretion)
  </read_first>
  <behavior>
    - Test (moto + DB): stage_file_to_s3 creates a cloud_job row (status=uploading, s3_key, upload_id set), computes part_count = ceil(file_size / s3_multipart_part_size_bytes), presigns that many part URLs, and enqueues exactly one "s3_upload" job carrying UploadFileS3Payload with those URLs + part_size + file_id
    - Test: the enqueue uses the deterministic key "s3_upload:<file_id>" and the explicit UPLOAD_FILE_SAQ_TIMEOUT_SEC
    - Test: a second stage_file_to_s3 for the same file_id is idempotent (cloud_job unique FK; no duplicate row)
    - Test: with no fileserver agent online the producer raises/holds cleanly (NoActiveAgentError surfaced, no half-written cloud_job committed)
  </behavior>
  <action>
    Create `src/phaze/services/cloud_staging.py` with `async def stage_file_to_s3(session, file, task_router) -> None` (the upload-trigger seam; built + unit-tested here, NOT called by stage_cloud_window — Phase 55 wires it). Steps, mirroring the agent_push producer idiom:
    1. Resolve the fileserver agent via `select_active_agent(session, kind="fileserver")` (let `NoActiveAgentError` propagate for a clean hold).
    2. `upload_id = await s3_staging.create_multipart_upload(file.id)`; compute `part_count = math.ceil(file.file_size / cfg.s3_multipart_part_size_bytes)` (min 1); `part_urls = await s3_staging.presign_upload_parts(file.id, upload_id, part_count)`.
    3. Upsert the `CloudJob` row (`file_id=file.id`, `s3_key=s3_staging.staged_object_key(file.id)`, `status=CloudJobStatus.UPLOADING`, `upload_id=upload_id`) — use `pg_insert(...).on_conflict_do_update(index_elements=["file_id"], ...)` so a re-stage is idempotent against the unique FK.
    4. Build `UploadFileS3Payload(file_id=file.id, original_path=file.original_path, part_urls=part_urls, part_size_bytes=cfg.s3_multipart_part_size_bytes, agent_id=agent.id)`; `queue = task_router.queue_for(agent.id)`; `await queue.connect()`; `await queue.enqueue("s3_upload", key=f"s3_upload:{file.id}", timeout=UPLOAD_FILE_SAQ_TIMEOUT_SEC, **payload.model_dump(mode="json"))`.
    5. `await session.commit()`.

    Add a thin `async def redrive_upload(session, file, task_router) -> None` (used by Plan 04 Task 2's failure callback): `await s3_staging.abort_multipart_upload(file.id, old_upload_id)` (best-effort), then re-run the stage steps with a fresh multipart. Read `cfg = cast("ControlSettings", get_settings())`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_services/test_cloud_staging.py -x</automated>
    <automated>uv run ruff check src/phaze/services/cloud_staging.py && uv run mypy src/phaze/services/cloud_staging.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async def stage_file_to_s3" src/phaze/services/cloud_staging.py` returns 1
    - `grep -c "s3_upload:" src/phaze/services/cloud_staging.py` returns >= 1 (deterministic key)
    - `grep -c "UPLOAD_FILE_SAQ_TIMEOUT_SEC" src/phaze/services/cloud_staging.py` returns >= 1
    - `grep -c "stage_cloud_window" src/phaze/services/cloud_staging.py` returns 0 (NOT wired — Phase 55)
    - `uv run pytest tests/test_services/test_cloud_staging.py` exits 0
    - `uv run mypy src/phaze/services/cloud_staging.py` exits 0
  </acceptance_criteria>
  <done>The producer stages a file end-to-end (cloud_job + multipart + presign + enqueue through the single seam), idempotent on file_id, unwired from the live routing seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: agent_s3 callbacks (complete/re-drive) + router mount</name>
  <files>src/phaze/routers/agent_s3.py, src/phaze/main.py, tests/test_routers/test_agent_s3.py</files>
  <read_first>
    - src/phaze/routers/agent_push.py (full file — report_pushed idempotent flip + report_push_mismatch re-drive/terminal)
    - src/phaze/routers/agent_analysis.py:188-234 (ledger clear + commit ordering)
    - src/phaze/services/cloud_staging.py (Plan 04 Task 1 — redrive_upload)
    - src/phaze/services/s3_staging.py (complete_multipart_upload, abort_multipart_upload, delete_staged_object)
    - src/phaze/schemas/agent_s3.py (UploadedRequest/UploadedResponse, UploadFailedRequest/UploadFailedResponse)
    - src/phaze/main.py:201-207 (include_router block)
    - tests/conftest.py:170-215 (authenticated_client, seed_test_agent)
  </read_first>
  <behavior>
    - Test: POST /api/internal/agent/s3/{file_id}/uploaded with the parts body completes the multipart (control-side), flips cloud_job UPLOADING→UPLOADED, returns 200
    - Test: a duplicate /uploaded callback (cloud_job already UPLOADED) is an idempotent 200 no-op (rowcount guard; does NOT re-complete the multipart)
    - Test: the agent body never carries file_id/agent_id (extra=forbid) — file_id from the path only
    - Test: POST /{file_id}/failed under the cap re-drives (redrive_upload called; cloud_job stays uploading) → cleared=False
    - Test: POST /{file_id}/failed at the cap sets cloud_job FAILED, aborts the multipart, deletes the staged object, returns cleared=True
    - Test: unauthenticated callback returns 401
    - Test: agent_s3.router is registered (the route resolves through the app)
  </behavior>
  <action>
    Create `src/phaze/routers/agent_s3.py` with `router = APIRouter(prefix="/api/internal/agent/s3", tags=["agent-internal"])` (auth + path-only file_id discipline like agent_push).

    `@router.post("/{file_id}/uploaded", response_model=UploadedResponse)` `report_uploaded(file_id, body: UploadedRequest, agent=Depends(get_authenticated_agent), session=Depends(get_session))`: load the `CloudJob` row by file_id; complete the multipart CONTROL-SIDE via `await s3_staging.complete_multipart_upload(file_id, cloud_job.upload_id, [(p.part_number, p.etag) for p in body.parts])` (DIST-01/KSTAGE-01 — the control plane completes, never the agent); then the idempotent flip: `res = cast("CursorResult[Any]", await session.execute(update(CloudJob).where(CloudJob.file_id==file_id, CloudJob.status==CloudJobStatus.UPLOADING).values(status=CloudJobStatus.UPLOADED)))`; if `res.rowcount == 0` commit + idempotent 200 (do NOT re-complete). `await session.commit()`; return `UploadedResponse(file_id=file_id)`.

    `@router.post("/{file_id}/failed", response_model=UploadFailedResponse)` `report_upload_failed(file_id, body: UploadFailedRequest, request, agent, session)`: read the `s3_upload:<file_id>` attempt counter from the SchedulingLedger payload JSONB (default 0; mirror report_push_mismatch). At/over `ControlSettings.push_max_attempts` (or the Plan-01 s3 cap): set `CloudJob.status=FAILED`, `await s3_staging.abort_multipart_upload(...)`, `await s3_staging.delete_staged_object(file_id)`, `clear_ledger_entry(session, "s3_upload:<id>")`, commit, return `cleared=True`. Under the cap: `await cloud_staging.redrive_upload(session, file, request.app.state.task_router)`, stamp the incremented attempt back on the ledger payload, commit, return `cleared=False`. No fileserver online → clean 200 hold (NoActiveAgentError caught), never 500.

    Mount the router in `src/phaze/main.py`: add `agent_s3` to the import block (line ~27) and `app.include_router(agent_s3.router)` alongside the other `agent_*` routers (line ~201-207).
  </action>
  <verify>
    <automated>uv run pytest tests/test_routers/test_agent_s3.py -x</automated>
    <automated>uv run ruff check src/phaze/routers/agent_s3.py src/phaze/main.py && uv run mypy src/phaze/routers/agent_s3.py src/phaze/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/{file_id}/uploaded\|/{file_id}/failed" src/phaze/routers/agent_s3.py` returns >= 2
    - `grep -c "complete_multipart_upload" src/phaze/routers/agent_s3.py` returns >= 1 (control completes, not the agent)
    - `grep -cE "abort_multipart_upload|delete_staged_object" src/phaze/routers/agent_s3.py` returns >= 2 (terminal cleanup, KSTAGE-04)
    - `grep -c "rowcount == 0\|rowcount==0" src/phaze/routers/agent_s3.py` returns >= 1 (idempotent guard)
    - `grep -c "agent_s3" src/phaze/main.py` returns >= 2 (import + include_router)
    - `uv run pytest tests/test_routers/test_agent_s3.py` exits 0
  </acceptance_criteria>
  <done>The control plane completes the multipart upload itself, flips cloud_job idempotently, runs a bounded re-drive, and cleans up (abort+delete) on terminal failure; the router is mounted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → control plane | token-authed upload-complete/upload-failed callbacks |
| control plane → S3 | the control plane completes/aborts/deletes (DIST-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-15 | Tampering | duplicate/late callback clobbering state | mitigate | rowcount-guarded cloud_job flip (UPLOADING→UPLOADED); duplicate is idempotent 200, no re-complete |
| T-53-16 | Denial of Service | unbounded upload re-drive loop | mitigate | attempt counter in ledger JSONB capped at push_max_attempts → terminal FAILED |
| T-53-17 | Denial of Service | orphaned in-flight multipart / staged object leak | mitigate | terminal path calls abort_multipart_upload + delete_staged_object (KSTAGE-04); lifecycle TTL backstop (Plan 02) |
| T-53-18 | Spoofing | forged identity in callback body | mitigate | file_id on path; identity from token; UploadedRequest carries no identity (AUTH-01) |
| T-53-19 | Repudiation | unmounted route / silent 500 | mitigate | router mounted + tested; NoActiveAgentError → clean 200 hold, never 500 |
</threat_model>

<verification>
- `uv run pytest tests/test_services/test_cloud_staging.py tests/test_routers/test_agent_s3.py` passes (moto + DB + respx as needed)
- `uv run ruff check . && uv run mypy .` clean for touched files
- The producer is NOT referenced from stage_cloud_window (Phase 55 wires it)
</verification>

<success_criteria>
- Control plane completes the multipart upload (never the agent) — KSTAGE-01
- Bounded re-drive then terminal cleanup (abort + delete) on upload failure — KSTAGE-04
- Producer seam built + unit-tested, unwired from the live routing seam
</success_criteria>

<output>
Create `.planning/phases/53-s3-object-staging-leg/53-04-SUMMARY.md` when done.
</output>
