---
phase: 53-s3-object-staging-leg
plan: 03
type: execute
wave: 2
depends_on: ["53-01"]
files_modified:
  - src/phaze/schemas/agent_s3.py
  - src/phaze/services/agent_client.py
  - src/phaze/tasks/s3_upload.py
  - src/phaze/tasks/agent_worker.py
  - src/phaze/services/enqueue_router.py
  - tests/test_task_split.py
  - tests/test_schemas/test_agent_s3.py
  - tests/test_tasks/test_s3_upload.py
  - tests/test_services/test_agent_client_upload.py
autonomous: true
requirements: [KSTAGE-02]

must_haves:
  truths:
    - "The file-server agent uploads file bytes by PUTting parts to presigned URLs over httpx"
    - "The agent collects each part's ETag and reports the ordered (part_number, etag) list to the control plane"
    - "No S3 SDK, botocore, or bucket credentials live on the agent (httpx-only)"
    - "The upload task carries no phaze.database/phaze.models/sqlalchemy/aioboto3/botocore import (Postgres-free + SDK-free boundary)"
    - "s3_upload is registered in both AGENT_TASKS and agent_worker functions (single enqueue seam)"
  artifacts:
    - path: "src/phaze/tasks/s3_upload.py"
      provides: "agent-side httpx multipart-PUT upload task"
      contains: "async def upload_file_s3"
    - path: "src/phaze/schemas/agent_s3.py"
      provides: "UploadFileS3Payload + UploadedResponse + UploadFailedRequest/Response"
      contains: "class UploadFileS3Payload"
  key_links:
    - from: "src/phaze/tasks/s3_upload.py"
      to: "presigned part URLs"
      via: "httpx PUT per part, collecting ETag from response headers"
      pattern: "httpx"
    - from: "src/phaze/services/enqueue_router.py"
      to: "s3_upload"
      via: "AGENT_TASKS frozenset membership"
      pattern: "s3_upload"
---

<objective>
Build the agent-side upload leg: the `agent_s3` schemas (upload payload + callback bodies), the
`upload_file_s3` httpx-multipart-PUT task that streams parts to presigned URLs and reports ETags,
the agent_client upload callbacks, and the single-enqueue-seam registration.

Purpose: KSTAGE-02 — the agent transfers bytes over httpx with no S3 SDK or credentials. This is
the byte-transfer half that the control-plane presign (Plan 02) and callbacks (Plan 04) bracket.
Output: schemas, the task, agent_client methods, AGENT_TASKS + worker registration, and the
extended import-boundary test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-s3-object-staging-leg/53-CONTEXT.md
@.planning/phases/53-s3-object-staging-leg/53-PATTERNS.md
@.planning/phases/53-s3-object-staging-leg/53-01-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase. Use these directly — no exploration needed. -->

push_file task — the exact shape to adapt (src/phaze/tasks/push.py):
- Postgres-free import boundary (lines 28-40): ONLY stdlib + phaze.config + phaze.schemas + ctx["api_client"]
- _agent_settings() narrows get_settings() to AgentSettings (lines 65-77)
- Three-layer timeout constants (lines 50-62): _OUTER_TIMEOUT_BUFFER_SEC=30, _SAQ_JOB_TIMEOUT_MARGIN_SEC=30, PUSH_FILE_SAQ_TIMEOUT_SEC=600+30+30
- _STDERR_SNIPPET_MAX=500 bounds error snippet crossing into SAQ error
- async def push_file(ctx, **kwargs): validate payload → fail-fast config → transfer → catch (TimeoutError, asyncio.CancelledError) reap → non-success RuntimeError (SAQ retry) → api.report_pushed → return status dict

PushFilePayload precedent (src/phaze/schemas/agent_tasks.py:54-84): extra="forbid", file_id present
(deterministic-key builder reads it), @field_validator on agent-supplied paths.

agent_client report_pushed/report_push_mismatch (src/phaze/services/agent_client.py:326-359):
- lazy `from phaze.schemas.agent_s3 import ...  # noqa: PLC0415`
- await self._request("POST", f"/api/internal/agent/s3/{file_id}/uploaded", json=...)
- Model.validate(response.json()); inherits tenacity 5xx-retry/4xx-surface; token never logged; file_id on path

AGENT_TASKS frozenset (src/phaze/services/enqueue_router.py:60-70) — MUST mirror agent_worker functions.
agent_worker functions list (src/phaze/tasks/agent_worker.py:270-277) — process_file, ..., push_file.

Import-boundary test (tests/test_task_split.py:33-116): subprocess imports phaze.tasks.agent_worker,
asserts forbidden = ("phaze.database","phaze.tasks.session","sqlalchemy.ext.asyncio") absent from sys.modules.

PresignDownloadResponse note: the agent does NOT call presign-download (that is the pod). The agent
receives presigned PART URLs in the upload task payload.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: agent_s3 schemas + agent_client upload callbacks</name>
  <files>src/phaze/schemas/agent_s3.py, src/phaze/services/agent_client.py, tests/test_schemas/test_agent_s3.py, tests/test_services/test_agent_client_upload.py</files>
  <read_first>
    - src/phaze/schemas/agent_push.py (PushedResponse/PushMismatchRequest/PushMismatchResponse — extra=forbid, identity-never-in-body)
    - src/phaze/schemas/agent_tasks.py:54-84 (PushFilePayload + field validators)
    - src/phaze/services/agent_client.py:326-359 (report_pushed/report_push_mismatch funnel to mirror)
    - .planning/phases/53-s3-object-staging-leg/53-PATTERNS.md §"agent_s3.py" + §"agent_client.py"
  </read_first>
  <behavior>
    - Test: UploadFileS3Payload requires file_id + an ordered list of presigned part URLs + part_size_bytes + agent_id; extra fields rejected (extra=forbid)
    - Test: a part URL that is not http(s) is rejected by a field validator
    - Test: UploadedRequest carries the ordered (part_number, etag) list and NO file_id/agent_id (AUTH-01); extra rejected
    - Test: UploadedResponse/UploadFailedResponse echo file_id + a Literal status
    - Test: agent_client.report_upload_complete POSTs to /api/internal/agent/s3/{file_id}/uploaded with the parts body and parses UploadedResponse
    - Test: agent_client.report_upload_failed POSTs to /api/internal/agent/s3/{file_id}/failed and parses UploadFailedResponse
  </behavior>
  <action>
    Create `src/phaze/schemas/agent_s3.py` (ORM-free, import-safe across the agent boundary; every model `model_config = ConfigDict(extra="forbid")`):
    - `class UploadFileS3Payload(BaseModel)`: `file_id: uuid.UUID`, `original_path: str` (the media-mount source the agent reads — `@field_validator` requires absolute path, mirror PushFilePayload), `part_urls: list[str]` (ordered presigned PUT URLs; `@field_validator` requires each is http(s)), `part_size_bytes: int`, `agent_id: str`. file_id present so the deterministic-key builder reads it.
    - `class UploadedPart(BaseModel)`: `part_number: int = Field(ge=1)`, `etag: str`.
    - `class UploadedRequest(BaseModel)`: `parts: list[UploadedPart]` (upload metadata, NOT identity — file_id stays on the path).
    - `class UploadedResponse(BaseModel)`: `file_id: uuid.UUID`, `status: Literal["uploaded"]="uploaded"`.
    - `class UploadFailedRequest(BaseModel)`: `detail: str | None = Field(default=None, max_length=2000)`.
    - `class UploadFailedResponse(BaseModel)`: `file_id: uuid.UUID`, `status: Literal["failed"]="failed"`, `cleared: bool`.

    Add to `src/phaze/services/agent_client.py` (mirror report_pushed exactly, lazy import `# noqa: PLC0415`):
    - `async def report_upload_complete(self, file_id, parts) -> UploadedResponse`: POST `/api/internal/agent/s3/{file_id}/uploaded` with `UploadedRequest(parts=...).model_dump(mode="json")`.
    - `async def report_upload_failed(self, file_id, detail=None) -> UploadFailedResponse`: POST `/api/internal/agent/s3/{file_id}/failed` with `UploadFailedRequest(detail=detail).model_dump(mode="json")`.
    Both inherit the `_request` tenacity policy; file_id on path; token never logged.
  </action>
  <verify>
    <automated>uv run pytest tests/test_schemas/test_agent_s3.py tests/test_services/test_agent_client_upload.py -x</automated>
    <automated>uv run ruff check src/phaze/schemas/agent_s3.py src/phaze/services/agent_client.py && uv run mypy src/phaze/schemas/agent_s3.py src/phaze/services/agent_client.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "class (UploadFileS3Payload|UploadedRequest|UploadedResponse|UploadFailedRequest|UploadFailedResponse)" src/phaze/schemas/agent_s3.py` returns 5
    - `grep -c "extra=\"forbid\"\|extra='forbid'" src/phaze/schemas/agent_s3.py` returns >= 5
    - `grep -cE "async def report_upload_(complete|failed)" src/phaze/services/agent_client.py` returns 2
    - `grep -c "from phaze.models\|import sqlalchemy\|aioboto3\|botocore" src/phaze/schemas/agent_s3.py` returns 0
    - `uv run pytest tests/test_schemas/test_agent_s3.py tests/test_services/test_agent_client_upload.py` exits 0
  </acceptance_criteria>
  <done>Upload payload + callback schemas exist (identity on the path only), and the agent client can report upload success/failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: upload_file_s3 task + single-enqueue-seam registration + import-boundary guard</name>
  <files>src/phaze/tasks/s3_upload.py, src/phaze/tasks/agent_worker.py, src/phaze/services/enqueue_router.py, tests/test_task_split.py, tests/test_tasks/test_s3_upload.py</files>
  <read_first>
    - src/phaze/tasks/push.py (full file — the exact task shape: import boundary, timeout layering, TERMINAL-vs-retryable, callback)
    - src/phaze/tasks/agent_worker.py:260-280 (functions list registration)
    - src/phaze/services/enqueue_router.py:60-76 (AGENT_TASKS frozenset + the mirror invariant)
    - tests/test_task_split.py:33-116 (subprocess import-boundary test to extend)
    - src/phaze/schemas/agent_s3.py (UploadFileS3Payload, from Task 1)
    - .planning/phases/53-s3-object-staging-leg/53-PATTERNS.md §"s3_upload.py" + §"Postgres-free agent import boundary"
  </read_first>
  <behavior>
    - Test (respx): upload_file_s3 PUTs each part URL with the correct byte slice and collects the ETag from each response header, then calls api.report_upload_complete with the ordered (part_number, etag) list and returns a status dict
    - Test (respx): a non-2xx part PUT raises RuntimeError (SAQ retry path), no report_upload_complete call
    - Test: an outer timeout / asyncio.CancelledError is re-raised after reaping (no swallow)
    - Test: missing/unreadable original_path is a TERMINAL RuntimeError (no local fallback, no callback)
    - Test (test_task_split extension): phaze.tasks.agent_worker import graph contains NO aioboto3 and NO botocore
  </behavior>
  <action>
    Create `src/phaze/tasks/s3_upload.py` adapting `push.py` (swap rsync-over-SSH for httpx multipart PUT). Keep the Postgres-free import boundary: ONLY stdlib + `phaze.config` (AgentSettings narrowing via `_agent_settings()`) + `phaze.schemas.agent_s3` + `ctx["api_client"]`. ABSOLUTELY NO `aioboto3`/`botocore`/`phaze.database`/`phaze.models`/`sqlalchemy` import (KSTAGE-02). Reproduce the three-layer timeout constants as `UPLOAD_FILE_SAQ_TIMEOUT_SEC = 600 + _OUTER_TIMEOUT_BUFFER_SEC + _SAQ_JOB_TIMEOUT_MARGIN_SEC` and the `_STDERR_SNIPPET_MAX`-style snippet bound.

    `async def upload_file_s3(ctx, **kwargs) -> dict[str, Any]`: `payload = UploadFileS3Payload.model_validate(kwargs)`; `api = ctx["api_client"]`; open the file at `payload.original_path` (TERMINAL RuntimeError if missing — never local fallback); stream it in `payload.part_size_bytes` chunks, PUTting chunk N to `payload.part_urls[N-1]` over an httpx AsyncClient (stream parts to bound memory — Claude's Discretion on concurrency; a bounded `asyncio.Semaphore` is acceptable). Collect each part's `ETag` from the response headers (strip surrounding quotes). Wrap the transfer in `asyncio.wait_for(..., timeout=cfg.<upload_timeout>+_OUTER_TIMEOUT_BUFFER_SEC)` and `except (TimeoutError, asyncio.CancelledError): <reap> raise`. Any non-2xx part → `RuntimeError` (SAQ retry). On success: `await api.report_upload_complete(payload.file_id, parts)` and `return {"file_id": str(payload.file_id), "status": "uploaded"}`.

    Register `upload_file_s3` in `src/phaze/tasks/agent_worker.py` `settings["functions"]` AND add `"s3_upload"` (the task NAME used at enqueue) to `AGENT_TASKS` in `src/phaze/services/enqueue_router.py` so the trigger routes through the single enqueue seam (Phase 30 invariant). Keep the two lists in sync.

    Extend `tests/test_task_split.py`: add `aioboto3` and `botocore` to the forbidden-modules assertion for the agent_worker import graph (the SDK-free half of KSTAGE-02).
  </action>
  <verify>
    <automated>uv run pytest tests/test_tasks/test_s3_upload.py tests/test_task_split.py -x</automated>
    <automated>uv run ruff check src/phaze/tasks/s3_upload.py && uv run mypy src/phaze/tasks/s3_upload.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async def upload_file_s3" src/phaze/tasks/s3_upload.py` returns 1
    - `grep -cE "aioboto3|botocore|phaze.database|phaze.models|sqlalchemy" src/phaze/tasks/s3_upload.py` returns 0
    - `grep -c "s3_upload" src/phaze/services/enqueue_router.py` returns >= 1
    - `grep -c "upload_file_s3" src/phaze/tasks/agent_worker.py` returns >= 1
    - `grep -cE "aioboto3|botocore" tests/test_task_split.py` returns >= 1 (boundary extended)
    - `uv run pytest tests/test_tasks/test_s3_upload.py tests/test_task_split.py` exits 0
  </acceptance_criteria>
  <done>The agent uploads bytes over httpx with deterministic timeout layering, reports ETags, routes through the single enqueue seam, and the import-boundary test bans the S3 SDK from the agent graph.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent worker → S3 | the agent transfers bytes via presigned PUT URLs only (no creds) |
| agent → control plane | token-authed upload-complete/upload-failed callbacks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-11 | Elevation of Privilege | S3 SDK/creds leaking onto the agent | mitigate | import-boundary test bans aioboto3/botocore from agent_worker graph; agent holds no bucket creds (KSTAGE-02) |
| T-53-12 | Denial of Service | runaway error snippet / unbounded memory on huge files | mitigate | _STDERR_SNIPPET_MAX bound; streamed part chunks (bounded memory); bounded timeout layering inner<outer<SAQ-net |
| T-53-13 | Tampering | malicious presigned-URL or path in payload | mitigate | extra=forbid + field validators (absolute original_path, http(s) part URLs); shell-free httpx |
| T-53-14 | Spoofing | forged identity in callback body | mitigate | file_id on URL path; agent identity from token; UploadedRequest carries no identity (AUTH-01) |
</threat_model>

<verification>
- `uv run pytest tests/test_schemas/test_agent_s3.py tests/test_tasks/test_s3_upload.py tests/test_services/test_agent_client_upload.py tests/test_task_split.py` passes (respx-backed, no live S3)
- `uv run ruff check . && uv run mypy .` clean for touched files
- AGENT_TASKS and agent_worker functions both contain the upload task
</verification>

<success_criteria>
- Agent uploads file bytes over httpx to presigned PUT URLs, reports ETags, holds no S3 SDK/creds (KSTAGE-02)
- Upload task routes through the single enqueue seam and the import boundary is test-enforced
</success_criteria>

<output>
Create `.planning/phases/53-s3-object-staging-leg/53-03-SUMMARY.md` when done.
</output>
