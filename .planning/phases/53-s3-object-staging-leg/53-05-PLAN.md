---
phase: 53-s3-object-staging-leg
plan: 05
type: execute
wave: 3
depends_on: ["53-01", "53-02"]
files_modified:
  - src/phaze/routers/agent_analysis.py
  - tests/test_routers/test_agent_analysis_inline_delete.py
autonomous: true
requirements: [KSTAGE-04]

must_haves:
  truths:
    - "When an analysis result lands (success), the staged S3 object is deleted inline before commit"
    - "When an analysis failure lands, the staged S3 object is deleted inline before commit"
    - "The inline delete is a no-op for all-local files (no cloud_job row → zero S3 calls)"
  artifacts:
    - path: "src/phaze/routers/agent_analysis.py"
      provides: "inline staged-object delete hook on both result paths (D-02)"
      contains: "delete_staged_object"
  key_links:
    - from: "src/phaze/routers/agent_analysis.py"
      to: "src/phaze/services/s3_staging.py"
      via: "delete_staged_object(file_id) guarded on a cloud_job row existing"
      pattern: "delete_staged_object"
---

<objective>
Hook the inline staged-object delete (D-02) into the analysis-result callback on both the success
(`put_analysis`) and failure (`report_analysis_failed`) paths — the moment the object is provably no
longer needed. Guard it on a `cloud_job` row existing so the all-local path makes zero S3 calls.

Purpose: KSTAGE-04 — objects are deleted on every terminal outcome the result callback represents.
The bucket lifecycle TTL (Plan 02) is the backstop for the no-callback / eviction case (Phase 54's
reconcile may invoke the same delete).
Output: the inline-delete hook + tests covering success, failure, and all-local no-op.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-s3-object-staging-leg/53-CONTEXT.md
@.planning/phases/53-s3-object-staging-leg/53-PATTERNS.md
@.planning/phases/53-s3-object-staging-leg/53-01-SUMMARY.md
@.planning/phases/53-s3-object-staging-leg/53-02-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase. Use these directly — no exploration needed. -->

agent_analysis result paths — the hook points (src/phaze/routers/agent_analysis.py):
- put_analysis (success, lines 94-199): ends with `await clear_ledger_entry(session, f"process_file:{file_id}")` (line 196) then `await session.commit()` (line 198)
- report_analysis_failed (failure, lines 202-234): ends with clear_ledger_entry (line 225) then commit (line 226)
- both take file_id on the PATH and a session dep (AUTH-01)

s3_staging.delete_staged_object(file_id) (Plan 02) — idempotent S3 delete by file_id-scoped key; pure aioboto3.
CloudJob (Plan 01) — query by file_id to detect a staged object exists.

D-02 hook directive (53-PATTERNS.md §agent_analysis.py inline delete): add the delete to BOTH the
success path (after the line-196 clear_ledger_entry) and the failure path (after line-225), BEFORE
commit. Make it a no-op when no cloud_job row exists so the all-local path is unaffected.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: inline staged-object delete on both result paths</name>
  <files>src/phaze/routers/agent_analysis.py, tests/test_routers/test_agent_analysis_inline_delete.py</files>
  <read_first>
    - src/phaze/routers/agent_analysis.py:94-234 (put_analysis success path + report_analysis_failed failure path; ledger-clear + commit ordering)
    - src/phaze/services/s3_staging.py (Plan 02 — delete_staged_object idempotency, client build requires S3 config)
    - src/phaze/models/cloud_job.py (Plan 01 — CloudJob query by file_id)
    - .planning/phases/53-s3-object-staging-leg/53-PATTERNS.md §"agent_analysis.py — INLINE OBJECT DELETE"
    - tests/conftest.py:170-215 (authenticated_client, seed_test_agent)
  </read_first>
  <behavior>
    - Test: a successful put_analysis for a file WITH a cloud_job row calls delete_staged_object exactly once (object deleted)
    - Test: a report_analysis_failed for a file WITH a cloud_job row calls delete_staged_object exactly once
    - Test (the all-local guard): put_analysis / report_analysis_failed for a file with NO cloud_job row makes ZERO S3 calls (no client built, no error even when S3 config is absent)
    - Test: the delete happens in the same request as the result write; a delete error does not corrupt the recorded result (record-first discipline)
  </behavior>
  <action>
    In `src/phaze/routers/agent_analysis.py`, add a small private helper `async def _delete_staged_object_if_cloud(session, file_id) -> None`: query `select(CloudJob.id).where(CloudJob.file_id == file_id)`; if no row, return immediately (all-local no-op — CRITICAL so deploys with no S3 config never build a client or raise); otherwise `await s3_staging.delete_staged_object(file_id)`. Wrap the delete in a guard that logs-and-swallows transient S3 errors (the recorded analysis result must not be lost if cleanup blips — the lifecycle TTL backstops a missed delete), but never swallow on the all-local path (there is no call there).

    Call `await _delete_staged_object_if_cloud(session, file_id)` in `put_analysis` immediately after the `clear_ledger_entry(session, f"process_file:{file_id}")` (line ~196) and BEFORE `session.commit()`, and identically in `report_analysis_failed` after its `clear_ledger_entry` (line ~225) and before commit. Import `CloudJob` from `phaze.models` and `s3_staging` from `phaze.services`. file_id is the PATH value only (AUTH-01).
  </action>
  <verify>
    <automated>uv run pytest tests/test_routers/test_agent_analysis_inline_delete.py -x</automated>
    <automated>uv run ruff check src/phaze/routers/agent_analysis.py && uv run mypy src/phaze/routers/agent_analysis.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "delete_staged_object\|_delete_staged_object_if_cloud" src/phaze/routers/agent_analysis.py` returns >= 3 (helper def + 2 call sites)
    - `grep -c "CloudJob" src/phaze/routers/agent_analysis.py` returns >= 1 (existence guard)
    - the all-local no-op test (no cloud_job row → zero S3 calls, no S3 config needed) passes
    - `uv run pytest tests/test_routers/test_agent_analysis_inline_delete.py` exits 0
    - `uv run mypy src/phaze/routers/agent_analysis.py` exits 0
  </acceptance_criteria>
  <done>Staged objects are deleted inline on both result paths; all-local files trigger zero S3 calls; a cleanup blip never loses the recorded result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pod/agent → control plane | result callback that triggers the inline delete |
| control plane → S3 | the delete (DIST-01, control-side) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-20 | Denial of Service | staged object leak after analysis | mitigate | inline delete on success AND failure result paths (KSTAGE-04); lifecycle TTL backstops a missed callback |
| T-53-21 | Information Disclosure | a delete error losing the analysis result | mitigate | result is recorded first; the delete is log-and-swallow; TTL covers a missed delete |
| T-53-22 | Tampering | inline delete firing on all-local files (no S3 config) | mitigate | cloud_job existence guard → zero S3 calls / no client build on the local path |
| T-53-23 | Spoofing | file_id from body redirecting the delete | mitigate | file_id from the URL PATH only (AUTH-01) |
</threat_model>

<verification>
- `uv run pytest tests/test_routers/test_agent_analysis_inline_delete.py` passes (moto + DB)
- `uv run ruff check . && uv run mypy .` clean for the touched file
- all-local regression: existing agent_analysis tests still pass (zero S3 calls when no cloud_job row)
</verification>

<success_criteria>
- Staged object deleted inline on every result-callback terminal outcome (success + failure) — KSTAGE-04
- All-local path unaffected (no cloud_job row → no S3 calls)
</success_criteria>

<output>
Create `.planning/phases/53-s3-object-staging-leg/53-05-SUMMARY.md` when done.
</output>
