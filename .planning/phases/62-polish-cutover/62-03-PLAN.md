---
phase: 62-polish-cutover
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/architecture.md
  - docs/project-structure.md
  - docs/quick-start.md
  - tests/test_docs_ia_current.py
autonomous: true
requirements: [CUT-03]
must_haves:
  truths:
    - "README, docs/architecture.md, and docs/project-structure.md describe the DAG-centric shell IA (three-column shell, rail-as-nav, /s/<stage> HTMX stages, Cmd-K palette, header status strip, per-file record slide-in, Agents page)"
    - "docs/quick-start.md no longer instructs the operator to navigate to the removed legacy pages; nav steps point at the shell"
    - "A docs-currency guard proves each doc carries the new-IA vocabulary and lacks the specific stale legacy-nav instructions"
  artifacts:
    - path: "docs/architecture.md"
      provides: "New UI/IA section describing the DAG-centric shell"
      contains: "/s/"
    - path: "docs/project-structure.md"
      provides: "Template/router-to-workspace mapping section"
      contains: "templates/shell"
    - path: "tests/test_docs_ia_current.py"
      provides: "CUT-03 docs-currency structural guard"
      min_lines: 40
  key_links:
    - from: "tests/test_docs_ia_current.py"
      to: "README.md"
      via: "read_text new-IA vocabulary assertion"
      pattern: "command palette"
    - from: "tests/test_docs_ia_current.py"
      to: "docs/quick-start.md"
      via: "read_text stale-nav-absence assertion"
      pattern: "proposals"
---

<objective>
Deliver CUT-03: refresh the README and user-facing docs to describe the v7.0 DAG-centric information architecture instead of the retired tab-sprawl UI. Add UI/IA sections where none exist (architecture.md, project-structure.md), rewrite the IA-describing README sections, correct now-wrong nav steps in quick-start.md, and lock currency with a docs guard.

Purpose: CUT-03 requires user-facing docs plus the per-service README to describe the new IA.
Output: updated README.md + docs/architecture.md + docs/project-structure.md + docs/quick-start.md; tests/test_docs_ia_current.py.

Decisions implemented (locked — do not re-litigate):
- D-06: Refresh README.md + docs/architecture.md + docs/project-structure.md for the DAG-centric IA; NO screenshots. Correct now-wrong nav steps in docs/quick-start.md inline, but no full walkthrough rewrite. Rejected: README-only; full sweep + screenshots.

Presentation-only phase — describe existing capabilities under the new IA; do NOT document new/changed backend behavior (there is none). Keep README badges on one line (project convention).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-polish-cutover/62-CONTEXT.md
@.planning/phases/62-polish-cutover/62-PATTERNS.md
@docs/superpowers/specs/2026-06-28-ui-redesign-dag-console-design.md

<interfaces>
<!-- Doc anchors from 62-PATTERNS.md section Docs (CUT-03). -->
README.md          — refresh "Architecture Overview" (:37) + "Key Features" (:166). Keep badges one-line.
docs/architecture.md      — no UI/frontend section today (only "Per-agent task routing" :274). ADD a UI/IA section.
docs/project-structure.md — no template/router section today. ADD one.
docs/quick-start.md       — stale nav steps at :136 and :162-164 reference the removed legacy pages. Fix inline.

New-IA vocabulary: three-column shell, DAG rail (navigation spine), /s/<stage> HTMX stage swap, command palette (Cmd-K), header status strip, per-file record slide-in, Agents page.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the CUT-03 docs-currency guard</name>
  <files>tests/test_docs_ia_current.py</files>
  <read_first>
    - tests/test_dead_template_guard.py (filesystem-only path-constant idiom)
    - tests/test_base_html_sri.py (regex-over-read_text idiom)
    - README.md, docs/architecture.md, docs/project-structure.md, docs/quick-start.md (current state)
    - .planning/phases/62-polish-cutover/62-VALIDATION.md (task 62-03-01 row)
  </read_first>
  <behavior>
    - readme_new_ia: README.md contains "command palette" and "DAG".
    - architecture_ui_section: docs/architecture.md contains "/s/" and "record slide-in".
    - structure_mapping: docs/project-structure.md contains "templates/shell" and "/s/".
    - quickstart_no_stale_nav: docs/quick-start.md contains no navigation instruction pointing at the removed legacy page paths.
  </behavior>
  <action>
    Create tests/test_docs_ia_current.py as a pure-filesystem guard mirroring test_dead_template_guard.py (path constants) and test_base_html_sri.py (read_text substring checks). Docstring explains CUT-03 keeps the docs describing the current DAG-centric IA. Define a repo-root path constant and one assertion function per behavior above using read_text substring checks. Keep the stale-nav check targeted to the specific legacy path strings so it is not brittle. This guard is expected RED until Task 2 edits the docs; do not use any DB/client fixture (fast lane).
  </action>
  <verify>
    <automated>uv run pytest tests/test_docs_ia_current.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_docs_ia_current.py exists, uses no DB/client fixture, runs in the fast lane.
    - One assertion function per behavior above is present.
    - The suite is RED at this point (docs not yet edited); ruff + mypy on the new file are clean.
  </acceptance_criteria>
  <done>The docs-currency guard exists in the repo idiom and defines the CUT-03 currency contract; red pending Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README + docs UI/IA sections and fix quick-start nav</name>
  <files>README.md, docs/architecture.md, docs/project-structure.md, docs/quick-start.md</files>
  <read_first>
    - README.md (Architecture Overview :37, Key Features :166, badge block near top)
    - docs/architecture.md (existing section style; "Per-agent task routing" :274)
    - docs/project-structure.md (existing structure/style)
    - docs/quick-start.md (:130-170 — the legacy nav steps)
    - docs/superpowers/specs/2026-06-28-ui-redesign-dag-console-design.md (authoritative IA to describe)
    - .planning/phases/62-polish-cutover/62-PATTERNS.md section Docs (CUT-03)
  </read_first>
  <action>
    Update the four docs to describe the DAG-centric shell IA (per D-06). No screenshots.
    1. README.md: rewrite "Architecture Overview" and "Key Features" so the UI is described as the three-column DAG-centric shell — DAG rail as the navigation spine with live per-stage counts, /s/<stage> HTMX stage swaps (no tab bar), Cmd-K command palette (replaces the old global search tab), header status strip for compute/agent liveness, per-file record slide-in, and the Agents + Audit pages reachable from the rail. Preserve the one-line badge block verbatim; do not re-add removed badges.
    2. docs/architecture.md: ADD a "User interface / information architecture" section (match the file's section style) describing the shell layout, the rail-as-nav swap contract (single #stage-workspace target, /pipeline/stats poll), the command palette, and the record slide-in. State explicitly that v7.0 changed IA/presentation only — no backend behavior change over the existing routers/services.
    3. docs/project-structure.md: ADD a section mapping the template tree — templates/shell/ (shell.html, partials/rail.html, partials/cmdk_modal.html, partials/record_host.html) and the /s/<stage> router-to-workspace-partial relationship (e.g. /s/propose maps to propose_workspace.html).
    4. docs/quick-start.md: correct the now-wrong nav steps inline — replace the "visit the pipeline page" step with opening / (the shell home; Analyze selected by default) and the "visit the proposals page" step with selecting the Propose/Review stages from the DAG rail (or pressing Cmd-K). Do NOT rewrite the whole walkthrough.
    Use exact new-IA vocabulary consistently (command palette, DAG rail, /s/<stage>, record slide-in, status strip) so the Task 1 guard passes.
  </action>
  <verify>
    <automated>uv run pytest tests/test_docs_ia_current.py -q</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains "command palette" and "DAG"; badge block still one line.
    - docs/architecture.md contains a UI/IA section with "/s/" and "record slide-in".
    - docs/project-structure.md contains the template-tree mapping with "templates/shell" and "/s/".
    - docs/quick-start.md no longer instructs navigating to the removed legacy pages.
    - uv run pytest tests/test_docs_ia_current.py -q is fully green.
  </acceptance_criteria>
  <done>All four docs describe the DAG-centric IA and the docs-currency guard is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs → readers | Docs are static markdown; no code, endpoint, input, or data flow is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-03-NA | (n/a) | new endpoints/inputs/auth/data | accept | None introduced — pure documentation content. Correct assessment: no attack surface. The only risk is documentation drift, mitigated by the tests/test_docs_ia_current.py currency guard. |
</threat_model>

<verification>
- uv run pytest tests/test_docs_ia_current.py -q — green (fast lane, no DB).
- uv run ruff check . + uv run mypy . — clean.
- pre-commit run --all-files — clean (markdownlint/yamllint as configured).
</verification>

<success_criteria>
- README + docs/architecture.md + docs/project-structure.md describe the DAG-centric shell IA; quick-start nav no longer points at removed pages.
- The docs-currency guard is green and will catch future drift.
- No screenshots, no new dependency, no backend/logic change.
</success_criteria>

<output>
Create `.planning/phases/62-polish-cutover/62-03-SUMMARY.md` when done.
</output>
