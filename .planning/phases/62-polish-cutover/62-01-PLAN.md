---
phase: 62-polish-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/templates/shell/partials/cmdk_modal.html
  - src/phaze/templates/shell/shell.html
  - tests/test_a11y_guards.py
autonomous: true
requirements: [CUT-01]
must_haves:
  truths:
    - "The ⌘K command-palette input has an accessible name (screen readers announce it, not just a placeholder)"
    - "A pytest structural guard proves skip-link, DAG-rail landmarks, ⌘K combobox/listbox, and record-slide-in dialog ARIA are all present — without a browser or new dependency"
    - "The dead empty right detail-pane <aside> (superseded by the Phase 61 record slide-in) is gone from the shell"
    - "The a11y guard runs in the fast lane (no DB, no integration marker) and is green"
  artifacts:
    - path: "tests/test_a11y_guards.py"
      provides: "CUT-01 filesystem structural a11y guard (skip-link, rail, cmdk, record)"
      min_lines: 60
    - path: "src/phaze/templates/shell/partials/cmdk_modal.html"
      provides: "⌘K palette with aria-label on the combobox input"
      contains: 'aria-label="Search files and commands"'
    - path: "src/phaze/templates/shell/shell.html"
      provides: "Shell layout with the dead Detail-pane aside removed"
  key_links:
    - from: "tests/test_a11y_guards.py"
      to: "src/phaze/templates/shell/partials/cmdk_modal.html"
      via: "read_text() + substring assertion"
      pattern: 'aria-label="Search files and commands"'
    - from: "tests/test_a11y_guards.py"
      to: "src/phaze/templates/shell/shell.html"
      via: "read_text() + skip-link-first-focusable assertion"
      pattern: 'href="#stage-workspace"'
---

<objective>
Complete and lock the CUT-01 accessibility baseline. Phase 57/61 already built the hard ARIA (skip link, rail landmarks + `aria-current`, ⌘K combobox/listbox/option + `aria-activedescendant`/`aria-expanded`, record slide-in `role="dialog" aria-modal` + `x-trap`). This plan closes the ONE real gap — the ⌘K combobox input has no accessible name — removes the dead detail-pane `<aside>` deferred from Phase 61, and codifies the whole baseline with a pure-filesystem pytest structural guard so it cannot silently regress.

Purpose: CUT-01 requires baseline WCAG 2.1 AA at parity-or-better, proven by repo-style pytest guards with no browser/axe dependency.
Output: `tests/test_a11y_guards.py` (new guard), a one-attribute fix in `cmdk_modal.html`, and the dead `<aside>` removed from `shell.html`.

Decisions implemented (locked — do not re-litigate):
- D-01: Target WCAG 2.1 AA; enforce with pytest structural guards (rendered-HTML assertions in the `test_dead_template_guard.py`/`test_base_html_sri.py` idiom); no new runtime dep, no browser audit.
- D-01a: CUT-01 is audit-and-close-gaps, NOT an ARIA rebuild. The one confirmed real fix is adding `aria-label` to the ⌘K combobox input. Do NOT rework rail keyboard-nav, add roving-tabindex, or broaden ARIA to new surfaces (rejected as scope creep).
- D-02: Parity-or-better is the floor, WCAG 2.1 AA the ceiling; the four named surfaces (rail keyboard nav, ⌘K, focus states, DAG ARIA + skip link) are non-negotiable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-polish-cutover/62-CONTEXT.md
@.planning/phases/62-polish-cutover/62-RESEARCH.md
@.planning/phases/62-polish-cutover/62-UI-SPEC.md
@.planning/phases/62-polish-cutover/62-PATTERNS.md
@.planning/phases/62-polish-cutover/62-VALIDATION.md

<interfaces>
<!-- Extracted this session; executor should use these directly — no codebase exploration needed. -->

Existing ARIA already present (verify + assert, do NOT rebuild):
- shell.html:153  <a href="#stage-workspace" class="sr-only focus:not-sr-only ...">Skip to workspace</a>  (first focusable in <body>)
- shell.html:165  <div id="stage-workspace" data-stage="{{ stage }}" ...>  (skip-link target id)
- shell.html:169  <aside class="w-[350px] ... " aria-label="Detail pane"></aside>  ← DEAD, remove (superseded by Phase 61 record slide-in)
- rail.html:25   <aside ... aria-label="Pipeline navigation">
- rail.html:35   <nav aria-label="Pipeline stages">   (+ every node: {% if stage == '...' %}aria-current="page"{% endif %}, focus-visible:ring-2 focus-visible:ring-blue-500)
- cmdk_modal.html:35  role="dialog" aria-modal="true" aria-label="Command palette"
- cmdk_modal.html:50  <input x-ref="input" name="q" ... role="combobox" aria-expanded="true" aria-controls="cmdk-results" :aria-activedescendant="activeId" ...>  ← GAP: no accessible name
- cmdk_modal.html:75  role="listbox" aria-label="Search and command results"  (id="cmdk-results")
- palette_results.html  rows carry role="option"
- record_host.html:61-64  role="dialog" aria-modal="true" aria-label="File record" x-trap.inert.noscroll.noreturn="open"

Guard idiom (copy from these):
- tests/test_dead_template_guard.py:43-45  _REPO_ROOT / _TEMPLATES path constants (filesystem-only; no DB fixture ⇒ conftest.py:134 will NOT auto-mark integration)
- tests/test_base_html_sri.py:44-67  regex-over-template-text: `html = path.read_text()` then assert substrings/regex
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the CUT-01 filesystem a11y structural guard</name>
  <files>tests/test_a11y_guards.py</files>
  <read_first>
    - tests/test_dead_template_guard.py (path-constant + filesystem-only idiom; lines 43-45)
    - tests/test_base_html_sri.py (regex-over-read_text() assertion style; lines 44-67)
    - src/phaze/templates/shell/shell.html (skip link :153, target :165)
    - src/phaze/templates/shell/partials/rail.html (landmarks :25/:35, aria-current + focus-visible node idiom)
    - src/phaze/templates/shell/partials/cmdk_modal.html (dialog :35, input :50, listbox :75)
    - src/phaze/templates/shell/partials/record_host.html (dialog :61-64 + x-trap)
    - .planning/phases/62-polish-cutover/62-UI-SPEC.md § "Interaction & Accessibility Contract (CUT-01)"
  </read_first>
  <behavior>
    - skip_link: shell.html contains an sr-only skip link with href="#stage-workspace" appearing before any other focusable element in <body>, and an element with id="stage-workspace" exists in shell.html.
    - rail_landmarks: rail.html has <aside> with a non-empty aria-label AND <nav> with a non-empty aria-label; every navigable rail button uses the `aria-current="page"` conditional idiom; every navigable node carries a `focus-visible:` class.
    - cmdk_combobox: cmdk_modal.html input block has role="combobox", aria-controls, aria-expanded, :aria-activedescendant AND an accessible name (aria-label). (This assertion is RED until Task 2.)
    - cmdk_listbox_dialog: cmdk_modal.html has role="listbox" with an aria-label, and a role="dialog" aria-modal="true" wrapper with an aria-label.
    - record_dialog: record_host.html panel has role="dialog", aria-modal="true", an aria-label, and an x-trap directive.
    - no_dead_aside: shell.html does NOT contain aria-label="Detail pane". (RED until Task 2.)
  </behavior>
  <action>
    Create `tests/test_a11y_guards.py` as a pure-filesystem structural guard mirroring the idiom of `test_dead_template_guard.py` (path constants) and `test_base_html_sri.py` (regex over `read_text()`). Open with a multi-paragraph docstring explaining this guard proves WCAG-2.1-AA-relevant ARIA is present without a browser dependency (D-01), and that CUT-01 is audit-and-close-gaps not an ARIA rebuild (D-01a). Define `_TEMPLATES = Path(__file__).resolve().parents[1] / "src" / "phaze" / "templates"`.

    Write one test function per behavior above, each reading the relevant template with `read_text()` and asserting plain substrings / small regexes. For the skip-link "first focusable" assertion, locate the `<body>` open then assert the skip-link `<a href="#stage-workspace"` occurrence index precedes the index of any other `href=`/`<button`/`<input` in the body slice. For the rail per-node assertions, iterate the node blocks (split on `data-rail-stage` or `rail-node`) and assert each carries `focus-visible:` and the `aria-current="page"` idiom. Assert the ⌘K input accessible name via `aria-label="Search files and commands"` (net-new — will be added in Task 2). Assert `aria-label="Detail pane"` is absent from shell.html (removed in Task 2). Do NOT use a DB/client fixture (keep it in the fast lane). Do NOT assert on compiled CSS — assert on source class strings only. Two assertions (cmdk aria-label, no dead aside) are expected RED at end of this task; the rest green.
  </action>
  <verify>
    <automated>uv run pytest tests/test_a11y_guards.py -q -k "skip_link or rail or listbox or record" </automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_a11y_guards.py` exists, imports no DB/client fixture, and is collected without the `integration` marker.
    - The skip-link, rail-landmark, cmdk listbox/dialog, and record-dialog assertions PASS against current source.
    - The cmdk-accessible-name and no-dead-aside assertions exist and are RED (they turn green in Task 2).
    - `uv run ruff check tests/test_a11y_guards.py` and `uv run mypy tests/test_a11y_guards.py` are clean.
  </acceptance_criteria>
  <done>The guard file exists in the established repo idiom; present-today ARIA assertions are green; the two gap assertions are written and red, pending Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Close the CUT-01 gaps — add ⌘K accessible name + remove dead detail-pane aside</name>
  <files>src/phaze/templates/shell/partials/cmdk_modal.html, src/phaze/templates/shell/shell.html</files>
  <read_first>
    - src/phaze/templates/shell/partials/cmdk_modal.html (input block :50)
    - src/phaze/templates/shell/shell.html (dead aside :169; note <script integrity=...> tags live lower — do NOT touch them)
    - .planning/phases/62-polish-cutover/62-PATTERNS.md § cmdk_modal.html + § shell.html
    - .planning/phases/62-polish-cutover/62-UI-SPEC.md § "⌘K command palette" (the one gap) + § Build/Verification (SRI + XSS notes)
  </read_first>
  <action>
    Two source edits, no logic change (D-01a — the only CUT-01 code fixes):
    1. In `cmdk_modal.html`, add the attribute `aria-label="Search files and commands"` to the `<input x-ref="input">` combobox (the one gap; placeholder is not an accessible name per WAI-ARIA APG). Do NOT touch the `hx-get="/search/"`, focus-trap, or keydown wiring.
    2. In `shell.html`, delete the dead empty right detail-pane element `<aside ... aria-label="Detail pane"></aside>` (:169) — it was superseded by the Phase 61 record slide-in and its removal was explicitly deferred to Phase 62 (per 62-CONTEXT.md). Leave every `<script integrity=...>` line untouched so `test_base_html_sri.py` stays green (it guards shell.html). Do NOT introduce `|e` in any Alpine JS-attribute context (use `|tojson`; record aria-label already uses `textContent`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_a11y_guards.py tests/test_base_html_sri.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cmdk_modal.html` contains `aria-label="Search files and commands"` on the combobox input.
    - `shell.html` no longer contains `aria-label="Detail pane"`.
    - `uv run pytest tests/test_a11y_guards.py -q` is fully green (all functions).
    - `uv run pytest tests/test_base_html_sri.py -q` remains green (no SRI drift on base.html/shell.html).
  </acceptance_criteria>
  <done>The ⌘K combobox has an accessible name, the dead aside is gone, and the full a11y guard plus the SRI guard are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → server-rendered HTML | Only net-new markup is static ARIA attributes (`aria-label`) — no new input, endpoint, auth, or data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01-XSS | Tampering/Info-disclosure | shell.html / cmdk_modal.html edits near Alpine JS contexts | mitigate | No new dynamic value is emitted; only a static `aria-label` string and an element deletion. Do not introduce `|e` in a JS-attribute context (use `|tojson`; record aria-label already uses `textContent`). |
| T-62-01-SRI | Tampering | shell.html `<script integrity=...>` pins | mitigate | Edits are far from the script tags; `test_base_html_sri.py` guards both base.html and shell.html and must stay green. |
| T-62-01-NA | (n/a) | new endpoints/inputs/auth | accept | None introduced — presentation-only a11y attributes. Correct assessment: no new attack surface. |
</threat_model>

<verification>
- `uv run pytest tests/test_a11y_guards.py -q` — green (fast lane, no DB).
- `uv run pytest tests/test_base_html_sri.py -q` — green (no SRI drift).
- `uv run ruff check .` + `uv run mypy .` — clean.
</verification>

<success_criteria>
- CUT-01 baseline is codified by an automated, browser-free structural guard that is green.
- The one real ARIA gap (⌘K combobox accessible name) is closed; the dead detail-pane aside is removed.
- No SRI drift, no new dependency, no backend/logic change.
</success_criteria>

<output>
Create `.planning/phases/62-polish-cutover/62-01-SUMMARY.md` when done.
</output>
