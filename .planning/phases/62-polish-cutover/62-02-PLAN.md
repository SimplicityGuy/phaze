---
phase: 62-polish-cutover
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/templates/shell/partials/rail.html
  - tests/test_rail_narrow_width.py
autonomous: true
requirements: [CUT-04]
must_haves:
  truths:
    - "Below 1024px the DAG rail collapses to a 64px icon-only strip; at >=1024px it is unchanged"
    - "Every rail node shows a distinct per-stage inline-SVG glyph in both the expanded and collapsed states"
    - "When collapsed, node labels stay in the DOM as sr-only (screen-reader-readable) NOT display:none, and each node carries a title tooltip; numeric counts hide"
    - "Active-state tint/inset-bar and the focus-visible ring are preserved verbatim in both states"
    - "A pytest structural guard proves the collapse classes, the sr-only labels, and the per-node glyphs are present in source"
  artifacts:
    - path: "src/phaze/templates/shell/partials/rail.html"
      provides: "Collapsible icon rail with per-stage inline-SVG glyphs"
      contains: "max-lg:w-16"
    - path: "tests/test_rail_narrow_width.py"
      provides: "CUT-04 filesystem structural guard for the collapse contract"
      min_lines: 50
  key_links:
    - from: "tests/test_rail_narrow_width.py"
      to: "src/phaze/templates/shell/partials/rail.html"
      via: "read_text() + collapse-contract assertions"
      pattern: "max-lg:sr-only"
    - from: "src/phaze/templates/shell/partials/rail.html"
      to: "assets/src/app.css"
      via: "@source template scan + just tailwind rebuild"
      pattern: "max-lg:"
---

<objective>
Deliver CUT-04: the three-column shell degrades at narrow widths by collapsing the 280px DAG rail to a 64px icon-only strip via a pure-CSS Tailwind breakpoint (no JS, no persistence), and add the per-stage inline-SVG icon set the collapse requires (today the rail uses status dots + text, so an icon-only strip needs real glyphs). Lock the collapse contract with a filesystem structural guard.

Purpose: CUT-04 requires the rail to collapse to icons at narrow widths for the single-user desktop tool, while staying screen-reader-navigable.
Output: rail.html with max-lg collapse classes + 15 inline-SVG glyphs + per-node title tooltips; a locally-rebuilt (gitignored) app.css; tests/test_rail_narrow_width.py.

Decisions implemented (locked — do not re-litigate):
- D-07: Auto-collapse the 280px rail to an icon-only strip via a CSS breakpoint (max-lg = below 1024px), pure CSS, no persistence, no JS toggle. Labels/counts hide; icons + aria-label/tooltip remain. Verify the record slide-in and Cmd-K palette overlays remain usable at narrow width. Collapsed width max-lg:w-16 (64px) is Claude's discretion.
- D-08: Add real per-stage icons as inline SVG (no new dep, no icon font), one glyph per rail node, aria-hidden="true" with the node's existing label as the accessible name.

Hard contract (CUT-04 to CUT-01 join): collapsed labels use max-lg:sr-only, NEVER max-lg:hidden — the collapsed strip must stay screen-reader-navigable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-polish-cutover/62-CONTEXT.md
@.planning/phases/62-polish-cutover/62-RESEARCH.md
@.planning/phases/62-polish-cutover/62-UI-SPEC.md
@.planning/phases/62-polish-cutover/62-PATTERNS.md

<interfaces>
<!-- Extracted this session; use directly. -->

Current rail node shape (rail.html:38-46, top-level Discover — the pattern all 12 navigable nodes + 2 below-line links follow):
  button data-rail-stage="discover" hx-get="/s/discover" hx-target="#stage-workspace" ...
    {% if stage == 'discover' %}aria-current="page"{% endif %}
    class="rail-node ... px-3 py-2.5 flex items-center gap-3 ... focus:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 aria-[current=page]:bg-blue-500/10 aria-[current=page]:shadow-[inset_3px_0_0_var(--color-blue-500)]"
    span (status dot ✓) w-5 h-5 ...            <- replace-with / precede-by the glyph
    span (label) flex-1 text-sm ...            <- add max-lg:sr-only
    span (count) x-text="$store.pipeline...."  <- add max-lg:hidden

Rail structure anchors:
  rail.html:25   aside class="w-[280px] ..." aria-label="Pipeline navigation"   <- add max-lg:w-16
  rail.html:35   nav aria-label="Pipeline stages"
  group boxes / eyebrows around :49-50, :87-88, :124-125                        <- add max-lg:hidden
  rail.html:147-150  below-the-line plain a links: /audit/ and /admin/agents (KEPT, D-04) — same collapse treatment

Inline-SVG idiom to copy (base.html:237-247 theme icons, header.html:17-21 wave logo):
  svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"
      stroke-linecap="round" stroke-linejoin="round" class="w-5 h-5 shrink-0" aria-hidden="true"  path d="..."

Glyph -> node map (UI-SPEC Glyph assignment; heroicons v2 OUTLINE, MIT, inlined verbatim — 15 rows):
  +Scan(CTA)=plus · Discover=folder-open · Metadata=document-text · Fingerprint=finger-print · Analyze=beaker ·
  Track-ID=identification · Tracklist=list-bullet · Propose=sparkles · Rename/Path=pencil-square · Tag write=tag ·
  Move files=folder-arrow-down · Dedupe=document-duplicate · Cue sheets=musical-note ·
  Audit log=clipboard-document-list · Compute/agents=cpu-chip

Build: assets/src/app.css:15 has @source "../../src/phaze/templates" — new max-lg:* + svg classes compile on rebuild.
  Recipe: just tailwind (justfile:63; pinned v4.3.2 standalone binary, no Node). Compiled src/phaze/static/css/app.css is GITIGNORED — do NOT commit it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-stage inline-SVG glyphs + max-lg collapse classes to the rail</name>
  <files>src/phaze/templates/shell/partials/rail.html, assets/src/app.css</files>
  <read_first>
    - src/phaze/templates/shell/partials/rail.html (full file — every navigable node + the 2 below-line links + group boxes/eyebrows)
    - src/phaze/templates/base.html (theme-icon inline SVG idiom :237-247)
    - src/phaze/templates/shell/partials/header.html (wave-logo inline SVG :17-21)
    - .planning/phases/62-polish-cutover/62-UI-SPEC.md section "Net-New Visual Surface (CUT-04)" (breakpoint, transform rules, SVG wrapper contract, glyph table, spacing/hit-target exception)
    - .planning/phases/62-polish-cutover/62-PATTERNS.md section rail.html (CUT-04 transform)
    - justfile (tailwind recipe :63)
  </read_first>
  <action>
    Edit rail.html with class additions + one inline-SVG glyph per node (per D-07/D-08 and the UI-SPEC contract). Additive only: do NOT re-grid the existing vertical rhythm and do NOT change hx-get / aria-current / focus-visible wiring.
    1. Aside (:25): add max-lg:w-16 to the existing w-[280px].
    2. Per navigable node (all 12 stage buttons + the 2 below-line a links + the + Scan CTA): insert the heroicons v2 OUTLINE glyph for that node using the exact SVG wrapper contract (viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="w-5 h-5 shrink-0" aria-hidden="true"), copying the outline path d="..." verbatim from heroicons v2. Place the glyph where the status dot sits today (leading). The glyph must render in BOTH states (it is the only visual when collapsed). Use the glyph-to-node map in the interfaces block.
    3. Label span of each node: add max-lg:sr-only (HARD contract — NOT max-lg:hidden; keeps it in the a11y tree).
    4. Numeric count span (x-text=...): add max-lg:hidden (visual-only data).
    5. Group boxes / eyebrows (the "Enrich · parallel" / "Identify" / "Review & Apply" chrome): add max-lg:hidden.
    6. Node inner layout: add max-lg:justify-center max-lg:px-0 (centers the glyph in the 64px strip, ~40px hit target; keep py-2.5).
    7. Each navigable node + the 2 below-line links + the Scan CTA: add native title="{label}" using the SAME label text (single source of truth — identical to the visible/sr-only label; do not invent new wording).
    8. Preserve aria-[current=page] tint + inset bar and focus-visible:ring-2 focus-visible:ring-blue-500 verbatim.
    Then rebuild CSS locally with just tailwind to confirm the new classes compile. Do NOT stage or commit src/phaze/static/css/app.css (gitignored; Docker rebuilds on image build).
  </action>
  <verify>
    <automated>uv run python -c "import pathlib; h=pathlib.Path('src/phaze/templates/shell/partials/rail.html').read_text(); assert 'max-lg:w-16' in h; assert h.count('aria-hidden=\"true\"')>=15; assert h.count('max-lg:sr-only')>=14; assert 'flex-1 text-sm' in h and 'max-lg:hidden' in h; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - rail.html aside carries max-lg:w-16.
    - At least 15 inline-SVG glyphs present, each with aria-hidden="true".
    - At least 14 label spans carry max-lg:sr-only; NO label span uses max-lg:hidden.
    - Every navigable node carries a title attribute; aria-current + focus-visible classes are unchanged.
    - just tailwind runs and regenerates src/phaze/static/css/app.css locally; app.css is NOT staged/committed (git status shows it ignored).
    - uv run ruff check . stays clean (template not linted, but no accidental Python touched).
  </acceptance_criteria>
  <done>The rail carries the max-lg collapse classes and a distinct inline-SVG glyph per node with sr-only labels + title tooltips; CSS compiles; nothing gitignored is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the CUT-04 narrow-width structural guard</name>
  <files>tests/test_rail_narrow_width.py</files>
  <read_first>
    - src/phaze/templates/shell/partials/rail.html (post-Task-1 state)
    - tests/test_dead_template_guard.py (path-constant + filesystem-only idiom)
    - tests/test_base_html_sri.py (regex-over-read_text() assertion style)
    - .planning/phases/62-polish-cutover/62-VALIDATION.md (task 62-02-01 secure-behavior row)
  </read_first>
  <behavior>
    - collapse_width: rail.html aside contains both w-[280px] and max-lg:w-16.
    - labels_sr_only: every rail node label span carries max-lg:sr-only; assert NO occurrence of a label span using max-lg:hidden (labels must stay in the a11y tree, D-08 hard contract).
    - counts_hidden: numeric count spans (x-text) carry max-lg:hidden.
    - glyphs_present: at least 15 inline svg elements with aria-hidden="true" (one per navigable node + below-line links + Scan CTA).
    - titles_present: each navigable node carries a title attribute.
    - focus_preserved: every navigable node still carries a focus-visible: class and the aria-current="page" idiom (guards against a regression during the CUT-04 edit).
  </behavior>
  <action>
    Create tests/test_rail_narrow_width.py as a pure-filesystem structural guard mirroring test_dead_template_guard.py / test_base_html_sri.py (no DB/client fixture, fast lane). Open with a docstring explaining it proves the CUT-04 collapse contract in source without a browser or CSS build (the guard checks class strings in the HTML, not compiled CSS). Read rail.html with read_text() and write one assertion function per behavior above. For per-node checks, split on the node markers (data-rail-stage / rail-node / the below-line a links) and assert per-block. Keep it grep-comment-safe: rail.html has few Jinja comments, but assert on attribute substrings, not raw line counts of comment-prone regions.
  </action>
  <verify>
    <automated>uv run pytest tests/test_rail_narrow_width.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_rail_narrow_width.py exists, uses no DB/client fixture, runs in the fast lane.
    - All behaviors above pass green against the Task-1 rail.html.
    - The labels_sr_only assertion explicitly fails if any label span switches to max-lg:hidden.
    - uv run ruff check tests/test_rail_narrow_width.py and uv run mypy tests/test_rail_narrow_width.py are clean.
  </acceptance_criteria>
  <done>The CUT-04 collapse contract is codified by an automated, browser-free structural guard that is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> server-rendered HTML | Only net-new markup is static inline SVG (decorative, aria-hidden), Tailwind class strings, and static title tooltips. No new input, endpoint, auth, or data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-02-SUP | Tampering | inline SVG paths copied from heroicons v2 | mitigate | Paths are static geometry copied verbatim from a known MIT source and inlined (no package install, no fetch). No executable content in an SVG path d attribute. |
| T-62-02-SRI | Tampering | rail.html near shell script pins | accept | rail.html contains no script/integrity tags; the SRI guard targets base.html/shell.html only. No drift possible from this file. |
| T-62-02-NA | (n/a) | new endpoints/inputs/auth | accept | None introduced — pure CSS breakpoint + decorative icons. Correct assessment: no new attack surface. |
</threat_model>

<verification>
- uv run pytest tests/test_rail_narrow_width.py -q — green (fast lane, no DB).
- just tailwind regenerates app.css locally; git status confirms app.css is ignored (not committed).
- uv run ruff check . + uv run mypy . — clean.
- Manual (deferred to UAT, per VALIDATION.md): resize viewport <1024px, confirm rail shows icons only with labels SR-readable, tooltips intact, and the record + palette overlays remain operable.
</verification>

<success_criteria>
- The rail collapses to a 64px icon strip below 1024px and is unchanged at >=1024px.
- Each node has a distinct inline-SVG glyph; labels remain sr-only (not display:none) when collapsed; counts hide.
- Active/focus states preserved; the CUT-04 contract is guard-locked; no new dependency and no committed compiled CSS.
</success_criteria>

<output>
Create `.planning/phases/62-polish-cutover/62-02-SUMMARY.md` when done.
</output>
