---
phase: 62-polish-cutover
plan: 04
type: execute
wave: 2
depends_on: ["62-01", "62-02", "62-03"]
files_modified:
  - src/phaze/routers/proposals.py
  - src/phaze/routers/tracklists.py
  - src/phaze/routers/tags.py
  - src/phaze/routers/cue.py
  - src/phaze/routers/duplicates.py
  - src/phaze/routers/preview.py
  - src/phaze/routers/pipeline.py
  - src/phaze/templates/base.html
  - tests/test_dead_template_guard.py
  - tests/test_dag_canvas_render.py
  - tests/test_template_helpers/test_cross_fs_fingerprint_notice.py
  - tests/test_routers/test_proposals.py
autonomous: true
requirements: [CUT-02]
must_haves:
  truths:
    - "The 8 legacy tab-era wrapper templates and every partial that only they referenced are deleted from disk"
    - "For the 5 content routers only the final unreachable non-HX list.html tail is removed (the live HX pagination/filter/sort branch stays); /pipeline/ becomes a pure redirect; the SHELL-05 302 redirects still resolve so old bookmarks keep working"
    - "The dead-template guard _ALLOWLIST is drained to an empty frozenset and test_no_orphan_templates is green with the closure logic untouched"
    - "base.html keeps the wave-logo home link + theme toggle but the legacy top tab-bar nav block is gone; the audit/agents pages that extend it are not dead-ends"
    - "The full test suite, ruff, and mypy are green after the cutover"
  artifacts:
    - path: "tests/test_dead_template_guard.py"
      provides: "CUT-02 arbiter with a drained allowlist"
      contains: "frozenset()"
    - path: "src/phaze/templates/base.html"
      provides: "Reduced header (logo + theme toggle) with the tab-bar nav block removed"
      contains: 'aria-label="Phaze home"'
  key_links:
    - from: "src/phaze/routers/proposals.py"
      to: "shell /s/propose"
      via: "302 RedirectResponse for non-HX GET (SHELL-05, retained)"
      pattern: "RedirectResponse"
    - from: "src/phaze/routers/proposals.py"
      to: "proposals/partials/proposal_content.html"
      via: "live HX-Request branch (KEPT — serves shell pagination/filter/sort)"
      pattern: "proposal_content.html"
    - from: "tests/test_dead_template_guard.py"
      to: "src/phaze/templates"
      via: "jinja2 meta closure over router .html literals"
      pattern: "frozenset()"
---

<objective>
Deliver CUT-02, the dependency-strict LAST work of v7.0: delete the dead tab-era UI now that every stage is superseded in the shell. Research proved every legacy top-level GET already 302-redirects non-HX into the shell (Phase 57 SHELL-05), so the full-page wrapper render tails are unreachable dead code. This plan removes only that dead code (the final `return ...list.html` tail in the 5 content routers, plus pipeline.py's whole dead dashboard render path), deletes the 8 wrapper templates + every partial that only they referenced, strips the legacy tab-bar nav block from base.html (keeping the logo + theme toggle), drains the dead-template guard allowlist to empty, and cleans up the companion tests — all while KEEPING the live per-router HX branches (they serve the shell workspaces' pagination/filter/sort), keeping the guard closure logic untouched, and keeping the redirects intact.

Purpose: CUT-02 requires no orphaned old-UI code, with the Phase 57 dead-template guard green and an empty allowlist.
Output: 5 content routers with the unreachable list.html tail removed (live HX branch KEPT) + pipeline as pure redirect; base.html reduced header; deleted wrappers + orphaned partials; drained _ALLOWLIST; updated companion tests.

Decisions implemented (locked — do not re-litigate):
- D-03: Full purge — supersede-then-delete. Redirects already exist; this is pure deletion of the unreachable full-page wrappers + their dead render tails + orphaned partials, then empty _ALLOWLIST; guard green. Rejected: minimal (delete only the 7 allowlisted).
- D-03a: Exact deletion worklist (8 wrappers + a 6-partial baseline cascade + the 7 allowlisted files), simulated GREEN against the real guard.
- D-03b (CORRECTED 2026-07-01): The per-router in-page HX branches are LIVE, not dead — proposals.py:164-165 renders proposals/partials/proposal_content.html, and identically tags.py / cue.py / tracklists.py / duplicates.py serve the shell workspaces' pagination/filter/sort fragments. KEEP them. For these 5 content routers the ONLY dead code is the final unreachable `return ...list.html` tail line (e.g. proposals.py:167) — delete just that line. The one genuinely-dead render path is pipeline.py's (→ dashboard.html, which has NO live shell fragment) — remove that whole path (make /pipeline/ a pure redirect). Stripping the live HX branches would break live shell pagination/filter/sort — a functional regression violating REQUIREMENTS.md line 82; that alternative is now known to be wrong.
- D-04: /audit/ and /admin/agents are KEPT (rail plain links; Agents = RECORD-03). execution/audit_log.html + admin/* templates and everything they transitively reference stay reachable.
- D-04a: base.html is KEPT but its legacy tab-bar nav block is removed — strip to the wave-logo home link + theme toggle only. Rejected: adding a separate "back to app" affordance.
- D-05: Supersession verified before deletion; deletion is already safe (redirects persist). Surface any supersession gap; do NOT silently drop a capability and do NOT add new capability to close it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-polish-cutover/62-CONTEXT.md
@.planning/phases/62-polish-cutover/62-RESEARCH.md
@.planning/phases/62-polish-cutover/62-PATTERNS.md

<interfaces>
<!-- Extracted this session; use directly. RESEARCH simulated GREEN against the real guard. -->

Reference redirect shape (search.py:39-40, already replicated in all 6 target routers — the redirect STAYS):
  if request.headers.get("HX-Request") != "true":
      return RedirectResponse(url="/?palette=1", status_code=302)

Per-router: non-HX redirect ALREADY at (keep) | LIVE HX branch (keep) | dead non-HX tail literal to delete:
  proposals.py:130  | proposals.py:164-165 (proposal_content.html, KEEP) | proposals.py:167  ("proposals/list.html", DELETE)
  tracklists.py:89  | tracklists.py HX branch (tracklist_list.html, KEEP)  | tracklists.py:160 ("tracklists/list.html", DELETE)
  tags.py:167       | tags.py HX branch (tag_list.html, KEEP)             | tags.py:226       ("tags/list.html", DELETE)
  cue.py:186        | cue.py HX branch (cue_list.html, KEEP)              | cue.py:236        ("cue/list.html", DELETE)
  duplicates.py:89  | duplicates.py HX branch (group_list.html, KEEP)     | duplicates.py:113 ("duplicates/list.html", DELETE)
  preview.py:45     | (no live shell HX consumer)                        | preview.py:55-64  ("preview/tree.html", DELETE whole dead render)
  pipeline.py:598   | pipeline.py HX branch → dashboard.html (DEAD, no live shell fragment) | pipeline.py:601 ("pipeline/dashboard.html") -> make /pipeline/ a PURE redirect (drop the HX branch too; nothing hx-gets /pipeline/; shell uses /s/analyze + /pipeline/stats)

8 wrapper templates to DELETE:
  proposals/list.html, tracklists/list.html, tags/list.html, cue/list.html, duplicates/list.html,
  preview/tree.html, pipeline/dashboard.html, search/page.html

6-partial baseline cascade to DELETE (orphaned once wrappers go):
  _partials/cross_fs_fingerprint_notice.html, pipeline/partials/dag_canvas.html,
  preview/partials/tree_node.html, tags/partials/pagination.html,
  tracklists/partials/filter_tabs.html, tracklists/partials/stats_header.html
  NOTE (D-03b, corrected): the 5 content routers' HX branches STAY LIVE, so their partials stay reachable — expect ZERO additional orphans beyond this baseline (+ the 6 remaining allowlisted search/* partials). pipeline.py's dashboard render path is the only genuinely-dead one, and its dag_canvas.html is already in this cascade. Task 2 runs the recompute as a guard, not an expected-churn step.

Current _ALLOWLIST (7 entries, tests/test_dead_template_guard.py:56-77) to DELETE from disk AND drain to frozenset():
  tracklists/partials/toast.html, search/page.html, search/partials/{results_content,results_row,results_table,search_form,summary_counts}.html

Guard mechanics (test_dead_template_guard.py): entry set = every "...html" literal in routers/*.py (regex :50);
  reachable = transitive jinja2.meta.find_referenced_templates closure (:99-108); orphan = all - reachable - _ALLOWLIST (:110).
  A template stays reachable while its literal survives in ANY router branch -> delete BOTH the literal AND the file.

KEEP (do NOT delete) — live shell/record fragments (RESEARCH "Partials that STAY"):
  proposals/partials/{proposal_content,row_detail}.html, analysis_timeline.html (reused by record view record_body.html),
  tags/partials/tag_list.html, cue/partials/cue_list.html, tracklists/partials/tracklist_list.html,
  duplicates/partials/group_list.html, pipeline/partials/{_diff_row,stats_bar}.html, all *_workspace.html, _lane_card.html, cloud cards.

base.html: KEEP wave-logo home link (:164-171, aria-label="Phaze home") + theme toggle (:224-249, aria-label="Toggle theme").
  DELETE the 8 legacy tab <a> links block (:173-222). base.html SURVIVES — admin/agents.html + execution/audit_log.html extend it (D-04 KEPT).
  Skip-link follow-through: base.html:157 targets #proposals-table (legacy). If the KEPT pages no longer render that id, correct the target to a valid id / the page main.

Companion tests that break on cascade deletion (update or delete in the SAME commit; verify what each asserts first):
  tests/test_dag_canvas_render.py (renders dag_canvas.html), tests/test_template_helpers/test_cross_fs_fingerprint_notice.py.
Audit for non-HX-200-with-body assertions -> expect 302:
  tests/test_routers/test_{proposals,tags,cue,tracklists,duplicates,preview}.py, tests/test_pipeline.py.
Redirect contract already guarded: tests/test_redirect_resolution.py, tests/test_shell_routes.py (must stay green; need test DB).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unreachable non-HX render tails, make /pipeline/ a pure redirect, and strip the base.html tab-bar nav</name>
  <files>src/phaze/routers/proposals.py, src/phaze/routers/tracklists.py, src/phaze/routers/tags.py, src/phaze/routers/cue.py, src/phaze/routers/duplicates.py, src/phaze/routers/preview.py, src/phaze/routers/pipeline.py, src/phaze/templates/base.html</files>
  <read_first>
    - src/phaze/routers/search.py (:34-77 — the canonical redirect-then-fragment target shape)
    - src/phaze/routers/proposals.py (:128-167), tracklists.py (:89,:160), tags.py (:167,:226), cue.py (:186,:236), duplicates.py (:89,:113), preview.py (:45,:55-64), pipeline.py (:598,:601)
    - src/phaze/templates/base.html (:157 skip link, :161-251 nav block, :164-171 logo, :224-249 theme toggle)
    - src/phaze/templates/shell/partials/header.html (reduced-header reference shape)
    - .planning/phases/62-polish-cutover/62-PATTERNS.md sections routers + base.html
  </read_first>
  <action>
    Delete unreachable code only; the SHELL-05 non-HX redirects at the top of each handler STAY, and the LIVE HX branches of the 5 content routers STAY.
    1. In proposals.py, tracklists.py, tags.py, cue.py, duplicates.py, preview.py: delete ONLY the final unreachable `return TemplateResponse(... list.html/tree.html)` line. Because non-HX already redirected at the top, the `if HX-Request == "true"` guard just above that tail is tautological — collapse it to a bare return of the HX fragment (e.g. proposals returns proposal_content.html unconditionally). KEEP the live HX fragment render itself intact — it serves the shell workspaces' pagination/filter/sort and deleting it would break live functionality (REQUIREMENTS.md line 82). Do NOT touch the non-HX redirect at the top.
    2. In pipeline.py: make `/pipeline/` a PURE redirect — this router's HX branch renders dashboard.html which has NO live shell fragment (the shell uses /s/analyze + /pipeline/stats, and nothing hx-gets /pipeline/), so drop the HX branch too and delete the pipeline/dashboard.html literal at :601.
    3. In base.html: DELETE the legacy tab-bar nav block containing the 8 tab <a> links (:173-222). KEEP the wave-logo home link (aria-label="Phaze home") and the theme toggle (aria-label="Toggle theme") so the KEPT audit/agents pages are not dead-ends (D-04a). Do NOT strip the whole <nav> chrome; do NOT touch any <script integrity=...> line (test_base_html_sri.py guards base.html).
    4. Skip-link follow-through: if base.html:157 targets #proposals-table and the KEPT pages no longer render that id, correct the target to a valid id (or the page main element) — supersession hygiene (D-05), not a rebuild.
    Do NOT delete any template file in this task (Task 2 owns deletions + the reach recompute). Do NOT relax the guard closure logic.
  </action>
  <verify>
    <automated>uv run pytest tests/test_redirect_resolution.py tests/test_shell_routes.py tests/test_base_html_sri.py -q</automated>
  </verify>
  <acceptance_criteria>
    - No router still contains a `list.html`/`tree.html`/`dashboard.html` string literal (grep of routers/*.py returns none for those 8 wrapper names).
    - The non-HX 302 redirects remain in all 6 routers; the live HX fragment renders remain in proposals/tags/cue/tracklists/duplicates (proposal_content.html, tag_list.html, cue_list.html, tracklist_list.html, group_list.html still referenced in router source); /pipeline/ returns a 302 for both HX and non-HX.
    - base.html no longer renders the 8 legacy tab links; the wave-logo home link + theme toggle remain; no SRI line changed.
    - tests/test_redirect_resolution.py, tests/test_shell_routes.py, tests/test_base_html_sri.py are green (needs test DB at 5433/6380 for the first two).
  </acceptance_criteria>
  <done>The unreachable list.html tails are gone from the 5 content routers (live HX branch KEPT), /pipeline/ is a pure redirect, base.html is reduced to logo + theme, and the redirect + SRI guards are green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete wrapper + orphaned partial templates, recompute the reach cascade as a guard, and drain the allowlist</name>
  <files>tests/test_dead_template_guard.py, src/phaze/templates (8 wrappers + orphaned partials deleted)</files>
  <read_first>
    - tests/test_dead_template_guard.py (full — _ALLOWLIST :56-77, closure :80-111; the arbiter)
    - .planning/phases/62-polish-cutover/62-RESEARCH.md section "Exact deletion worklist" + Pitfall 2/3 (cascade + reach recompute)
    - .planning/phases/62-polish-cutover/62-PATTERNS.md section test_dead_template_guard.py (files to delete / files that STAY)
  </read_first>
  <action>
    1. Delete the 8 wrapper templates and the 6 baseline cascade partials and the 7 allowlisted files listed in the interfaces block. KEEP every partial in the "KEEP" list (live shell/record fragments).
    2. D-03b guard recompute (corrected premise — NOT an expected-churn step): the 5 content routers' HX branches STAY LIVE (Task 1 removed only their unreachable list.html tail), so their partials remain reachable. EXPECTED result: zero additional orphans beyond the D-03a 6-partial baseline plus the 6 remaining allowlisted search/* partials — because pipeline.py's dashboard.html render path is the one genuinely-dead path, and its dag_canvas.html is already in the baseline cascade. Run the recompute as a GUARD: write a throwaway scratchpad/reach.py that reuses the guard's entry-set regex + jinja2.meta closure (RESEARCH Pitfall 3) to list all current orphans. Time-box this to a single recompute pass — the D-03b scope shrank to pipeline.py + the deterministic cascade. If a partial in the KEEP list surfaces as orphaned, DO NOT delete it: that is a supersession signal (a still-live shell/record fragment lost its last reference) — stop and re-check the Task-1 edit rather than delete a live fragment. Delete only non-KEEP orphans.
    3. Drain _ALLOWLIST to `frozenset()` in tests/test_dead_template_guard.py. Do NOT modify the closure logic (:80-111) — CONTEXT forbids relaxing it.
    Iterate delete -> run guard -> recompute until test_no_orphan_templates is green with the empty allowlist.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dead_template_guard.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The 8 wrappers + 6 baseline cascade partials + 7 allowlisted files are removed from disk (and the D-03b recompute surfaced no additional orphans beyond that baseline — if it did, re-check Task 1 before deleting anything on the KEEP list).
    - No KEEP-listed live fragment was deleted (proposals row_detail/proposal_content, analysis_timeline, *_workspace.html, _diff_row, stats_bar, tag_list, cue_list, tracklist_list, group_list all still on disk).
    - tests/test_dead_template_guard.py:56-77 is `_ALLOWLIST: frozenset[str] = frozenset()`; the closure logic is byte-for-byte unchanged.
    - uv run pytest tests/test_dead_template_guard.py -q is GREEN.
    - scratchpad/reach.py is NOT committed (throwaway).
  </acceptance_criteria>
  <done>All dead wrappers + orphaned partials are deleted, the allowlist is empty, no live fragment was removed, and the guard is green with untouched closure logic.</done>
</task>

<task type="auto">
  <name>Task 3: Fix companion tests and run the full phase gate</name>
  <files>tests/test_dag_canvas_render.py, tests/test_template_helpers/test_cross_fs_fingerprint_notice.py, tests/test_routers/test_proposals.py</files>
  <read_first>
    - tests/test_dag_canvas_render.py (renders the deleted dag_canvas.html)
    - tests/test_template_helpers/test_cross_fs_fingerprint_notice.py (renders the deleted partial)
    - tests/test_routers/test_proposals.py, test_tags.py, test_cue.py, test_tracklists.py, test_duplicates.py, test_preview.py, tests/test_pipeline.py (audit for non-HX-200-with-body assertions)
    - .planning/phases/62-polish-cutover/62-RESEARCH.md section "Testing & Regression Surface"
  </read_first>
  <action>
    1. Update or delete tests/test_dag_canvas_render.py and tests/test_template_helpers/test_cross_fs_fingerprint_notice.py — their target partials are deleted. Verify what each asserts first; if the only subject is the deleted partial, delete the test; if it asserts broader behavior, retarget it to a surviving template. Do this in the same change set as the deletions.
    2. Audit tests/test_routers/test_{proposals,tags,cue,tracklists,duplicates,preview}.py + tests/test_pipeline.py for any assertion expecting a non-HX 200 with a list.html/tree.html/dashboard.html body; change those to expect the 302 redirect (or remove if now redundant with test_redirect_resolution.py). Since redirects were already live and the content routers still return their live HX fragment for HX requests, most already use HX headers or follow_redirects — expect few changes.
    3. Run the fast-lane filesystem guards first, then the full phase gate.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dead_template_guard.py tests/test_a11y_shell.py tests/test_a11y_cmdk.py tests/test_a11y_record.py tests/test_rail_narrow_width.py tests/test_docs_ia_current.py -q</automated>
    <automated>uv run pytest -q</automated>
    <!-- Primary = the sub-second fast-lane filesystem/rendered-HTML guards above (Nyquist low-latency signal).
         Secondary/final-gate = the full suite; colima flake risk on DB-heavy subsets — re-run any failing subset in isolation; test DB must be up at 5433/6380. -->
  </verify>
  <acceptance_criteria>
    - No test references a deleted template; test_dag_canvas_render.py + test_cross_fs_fingerprint_notice.py are updated or removed.
    - Router tests expect the 302 for the legacy non-HX GETs and still pass for the live HX fragment path (no assertion on deleted wrapper bodies).
    - The fast-lane guard command (test_dead_template_guard + the a11y/rail/docs guards) is green in seconds.
    - uv run pytest -q is green (test DB up at 5433/6380; re-run any colima-flaky DB subset in isolation).
    - uv run ruff check . and uv run mypy . are clean; pre-commit run --all-files passes (never use --no-verify).
    - Coverage stays at or above the 85% gate (deleting dead branches raises coverage).
  </acceptance_criteria>
  <done>Companion tests are reconciled with the deletions and the fast-lane guards + full suite + ruff + mypy + pre-commit are green — v7.0 dead-code cutover complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → legacy routes | Untrusted GETs to /proposals, /tracklists, /tags, /cue, /duplicates, /preview, /pipeline, /search still cross here; they must keep 302-redirecting into the shell (SHELL-05) after the dead tails are removed, and the content routers must keep serving their live HX fragment for HX requests. |
| template include graph | Deleting a still-referenced partial would 500 a live page at render time — the dead-template guard + full suite are the boundary check; the KEEP-list protects the live shell/record fragments. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-04-DEL | Denial of Service (broken render) | template deletions | mitigate | Delete only templates the guard proves orphaned; KEEP-list protects live shell/record fragments; tests/test_dead_template_guard.py + full suite gate every deletion. If a KEEP partial appears orphaned, stop (supersession signal), do not delete. |
| T-62-04-RDR | Spoofing/broken links | legacy route redirects | mitigate | Redirects are RETAINED (only the dead render tail is removed) and the live HX branches of the 5 content routers are RETAINED; tests/test_redirect_resolution.py + test_shell_routes.py assert every legacy route still 302s into the shell, preserving bookmarks. |
| T-62-04-REG | Tampering (functional regression) | live HX pagination/filter/sort branches | mitigate | The 5 content routers' HX fragment renders (proposal_content/tag_list/cue_list/tracklist_list/group_list) are LIVE and KEPT; removing them would break shell pagination/filter/sort (REQUIREMENTS.md line 82). Only pipeline.py's HX branch (dead dashboard render) is removed. |
| T-62-04-SRI | Tampering | base.html script pins | mitigate | The tab-bar nav edit is far from the <script integrity=...> lines; test_base_html_sri.py guards base.html and must stay green. |
| T-62-04-NA | (n/a) | new endpoints/inputs/auth | accept | None introduced — this is deletion of dead presentation code. Correct assessment: no net-new attack surface; the surface strictly shrinks. |
</threat_model>

<verification>
- uv run pytest tests/test_dead_template_guard.py -q — green with an empty allowlist and untouched closure.
- uv run pytest tests/test_redirect_resolution.py tests/test_shell_routes.py tests/test_base_html_sri.py -q — green (redirects + live HX branches + SRI intact; needs test DB).
- uv run pytest -q — full suite green (final gate; re-run any colima-flaky DB subset isolated).
- uv run ruff check . + uv run mypy . + pre-commit run --all-files — clean.
</verification>

<success_criteria>
- No orphaned old-UI code: 8 wrappers + all orphaned partials deleted, _ALLOWLIST empty, dead-template guard green with unchanged closure logic.
- Legacy routes still 302-redirect into the shell (bookmarks preserved) and the 5 content routers still serve their live HX pagination/filter/sort fragment; base.html reduced to logo + theme; audit/agents pages are not dead-ends.
- Full suite + ruff + mypy + pre-commit green; coverage at or above 85%.
</success_criteria>

<output>
Create `.planning/phases/62-polish-cutover/62-04-SUMMARY.md` when done.
</output>
</content>
</invoke>
