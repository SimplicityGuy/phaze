---
phase: 49-duration-routing-backfill
plan: 04
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - src/phaze/tasks/release_awaiting_cloud.py
  - src/phaze/tasks/controller.py
  - src/phaze/services/enqueue_router.py
  - tests/test_tasks/test_release_awaiting_cloud.py
  - tests/test_tasks/test_recovery.py
autonomous: true
requirements: [CLOUDROUTE-02]
must_haves:
  truths:
    - "release_awaiting_cloud scans state=AWAITING_CLOUD; when a compute agent is online it enqueues process_file to the compute queue AND resets each released file to DISCOVERED (committed)"
    - "When no compute agent is online, release_awaiting_cloud is a clean no-op (enqueues nothing, raises nothing)"
    - "release_awaiting_cloud is registered as CronJob(*/5) on the controller worker — a NEW narrow cron scoped only to AWAITING_CLOUD→compute (NOT a general pipeline auto-advance)"
    - "An AWAITING_CLOUD file is NOT in the analyze done-set {ANALYZED, ANALYSIS_FAILED} and is NOT treated as domain-completed (stays pending, D-04)"
    - "Release dedups via the process_file:<id> deterministic key against any concurrent enqueue"
  artifacts:
    - path: "src/phaze/tasks/release_awaiting_cloud.py"
      provides: "state-driven held-file release producer"
      contains: "release_awaiting_cloud"
    - path: "src/phaze/tasks/controller.py"
      provides: "CronJob(release_awaiting_cloud, '*/5 * * * *') registration"
      contains: "release_awaiting_cloud"
    - path: "tests/test_tasks/test_recovery.py"
      provides: "D-04 AWAITING_CLOUD-stays-pending regression test"
      contains: "AWAITING_CLOUD"
  key_links:
    - from: "src/phaze/tasks/release_awaiting_cloud.py"
      to: "src/phaze/services/enqueue_router.py"
      via: "select_active_agent(session, kind='compute')"
      pattern: "select_active_agent\\(.*kind=.compute"
    - from: "src/phaze/tasks/release_awaiting_cloud.py"
      to: "src/phaze/services/analysis_enqueue.py"
      via: "enqueue_process_file onto the compute queue"
      pattern: "enqueue_process_file"
    - from: "src/phaze/tasks/controller.py"
      to: "src/phaze/tasks/release_awaiting_cloud.py"
      via: "cron_jobs registration"
      pattern: "CronJob\\(release_awaiting_cloud"
---

<objective>
Build the held-file release path (D-03/D-03a, RECONCILED): a NEW, narrowly-scoped `CronJob(release_awaiting_cloud, "*/5 * * * *")` on the controller worker that does a STATE-driven scan (`SELECT FileRecord WHERE state = AWAITING_CLOUD`) and, when a compute agent is online, enqueues each held file to the compute queue AND resets its state to DISCOVERED. Also add the D-04 regression test asserting AWAITING_CLOUD stays pending in recovery.

Purpose: This is the half of CLOUDROUTE-02's lifecycle that drains the held set once a compute agent comes online — automatically, within ~5 min, without re-introducing a general auto-advance cron (respecting the Phase-42 "automation only in recovery" principle). It is NOT an extension of the deleted */5 reenqueue cron and NOT a ledger replay (held files have no ledger row).
Output: A new `tasks/release_awaiting_cloud.py` producer, its controller cron registration, and the D-04 regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-duration-routing-backfill/49-CONTEXT.md
@.planning/phases/49-duration-routing-backfill/49-RESEARCH.md
@.planning/phases/49-duration-routing-backfill/49-01-SUMMARY.md

<critical_reconciliation>
DO NOT "extend the */5 reenqueue cron" — Phase 42 (PR #132) DELETED it; controller.py:204-224 carries an explicit "DO NOT re-add a recover cron" comment. Held AWAITING_CLOUD files have NO scheduling-ledger row (they were never enqueued, D-02), so `recover_orphaned_work`'s ledger replay structurally cannot release them. Release MUST be a state-driven scan, registered as a SEPARATE narrow cron, NOT a `recover_orphaned_work` extension.
</critical_reconciliation>

<interfaces>
<!-- Consumed from Plan 01: -->
select_active_agent(session, kind="compute") -> Agent  # raises NoActiveAgentError when no compute agent online
FileState.AWAITING_CLOUD = "awaiting_cloud"
get_files_by_state(session, state) -> list[FileRecord]   # services/pipeline.py (existing) — reuse for the AWAITING_CLOUD scan

<!-- Reused verbatim: -->
enqueue_process_file(queue, file, agent_id, models_path) -> Any   # services/analysis_enqueue.py:43 — writes ledger row via before_enqueue hook
ctx["async_session"], ctx["task_router"], ctx["queue"]   # controller worker ctx (see recover_orphaned_work)
task_router.queue_for(agent_id) -> Queue
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: State-driven release_awaiting_cloud producer + controller cron registration (D-03/D-03a)</name>
  <files>src/phaze/tasks/release_awaiting_cloud.py, src/phaze/tasks/controller.py, tests/test_tasks/test_release_awaiting_cloud.py</files>
  <read_first>
    - src/phaze/tasks/reenqueue.py (recover_orphaned_work L238-311 — the ctx shape, select_active_agent + task_router.queue_for replay pattern, NoActiveAgentError skip discipline; this is the structural model, NOT the code to extend)
    - src/phaze/tasks/controller.py (settings dict L198-230 — functions list + cron_jobs list + the "DO NOT re-add a recover cron" comment; add release_awaiting_cloud to functions AND a CronJob)
    - src/phaze/services/analysis_enqueue.py (enqueue_process_file L43-101 — models_path + agent_id args; ledger row written by the hook)
    - src/phaze/services/enqueue_router.py (select_active_agent kind param from Plan 01; NoActiveAgentError)
    - src/phaze/services/pipeline.py (get_files_by_state — reuse for the AWAITING_CLOUD scan)
    - src/phaze/config.py (get_settings / models_path for the payload)
    - tests/test_tasks/test_recovery.py (controller-task test harness: ctx fixture, fake queue/router, real PG session)
    - tests/_queue_fakes.py (kind-aware seed_active_agent; DedupFakeQueue/Router capture)
  </read_first>
  <behavior>
    - With files in AWAITING_CLOUD and a compute agent online: each file enqueues process_file onto the compute agent's queue (key process_file:<id>) AND is reset to FileState.DISCOVERED (committed)
    - With files in AWAITING_CLOUD and NO compute agent online: no enqueue captured, no state change, no exception (clean no-op via NoActiveAgentError skip)
    - With NO AWAITING_CLOUD files: clean no-op
    - A held file whose process_file key is already live dedups to a no-op (deterministic key); the state reset still applies so the count card stays honest (D-03a)
    - release_awaiting_cloud appears in controller settings["functions"] AND as a CronJob("*/5 * * * *")
  </behavior>
  <action>
    Create `src/phaze/tasks/release_awaiting_cloud.py` with `async def release_awaiting_cloud(ctx: dict[str, Any]) -> dict[str, int]`. Open `ctx["async_session"]()`; `held = await get_files_by_state(session, FileState.AWAITING_CLOUD)`; if empty return a zero tally. Resolve the compute agent via `select_active_agent(session, kind="compute")` inside `try/except NoActiveAgentError` → on miss log a structured no-op and return zero (gated on a compute agent being online, D-03). When online: `compute_q = ctx["task_router"].queue_for(agent.id)`; for each held file, `await enqueue_process_file(compute_q, file, agent.id, get_settings().models_path)` (the enqueue writes the ledger row via the before_enqueue hook; deterministic key dedups) AND set `file.state = FileState.DISCOVERED` (D-03a); `await session.commit()`. Return `{"released": N, "skipped": M}`. Keep this module control-only and FastAPI-free (mirrors recover_orphaned_work imports).
    Register it in `controller.py`: add `release_awaiting_cloud` to `settings["functions"]` AND add `CronJob(release_awaiting_cloud, cron="*/5 * * * *")` to `cron_jobs`, with a comment stating this is a NARROW recovery-only cron scoped ONLY to AWAITING_CLOUD→compute (NOT a general pipeline auto-advance — distinct from the deleted reenqueue cron, respects the Phase-42 principle).
    Add tests in `test_release_awaiting_cloud.py` covering: release+reset with compute online, no-op with no compute online (NoActiveAgentError), no-op with empty held set, and dedup of an already-live key.
  </action>
  <verify>
    <automated>uv run pytest tests/test_tasks/test_release_awaiting_cloud.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - release_awaiting_cloud.py exists; the producer scans state==AWAITING_CLOUD and, on a compute agent online, enqueues to the compute queue AND resets to DISCOVERED with an explicit commit
    - With no compute agent online the producer enqueues nothing and raises nothing (NoActiveAgentError skip)
    - controller.py registers release_awaiting_cloud in functions AND as CronJob("*/5 * * * *")
    - The module does NOT import fastapi / phaze.routers and does NOT touch recover_orphaned_work
    - `uv run pytest tests/test_tasks/test_release_awaiting_cloud.py -x` exits 0
  </acceptance_criteria>
  <done>Held files auto-release to compute within ~5 min once a compute agent is online, via a new narrow state-driven cron that respects the Phase-42 recovery-only principle.</done>
</task>

<task type="auto">
  <name>Task 2: D-04 regression test — AWAITING_CLOUD stays pending in recovery</name>
  <files>tests/test_tasks/test_recovery.py</files>
  <read_first>
    - src/phaze/tasks/reenqueue.py (_DOMAIN_COMPLETED_STAGES L107-121; _select_done_analyze_ids L162-167 — analyze done-set is {ANALYZED, ANALYSIS_FAILED} ONLY; is_domain_completed)
    - src/phaze/models/file.py (FileState.AWAITING_CLOUD added in Plan 01)
    - tests/test_tasks/test_recovery.py (existing domain-completed / done-set test patterns)
  </read_first>
  <behavior>
    - An AWAITING_CLOUD file id is NOT present in the analyze done-set produced by _select_done_analyze_ids / _build_done_sets
    - An AWAITING_CLOUD file is NOT treated as domain-completed for process_file (is_domain_completed returns False)
  </behavior>
  <action>
    Add a regression test asserting that a FileRecord in `FileState.AWAITING_CLOUD` is NOT classified as analyze-done: it must be absent from the analyze done-set (`{ANALYZED, ANALYSIS_FAILED}`) and must NOT be treated as domain-completed for `process_file` (D-04 is satisfied by OMISSION — no source change to `_DOMAIN_COMPLETED_STAGES` or the done-set; this test guards the omission). Use the existing recovery test harness/fixtures.
  </action>
  <verify>
    <automated>uv run pytest tests/test_tasks/test_recovery.py -x -q -k "awaiting or pending or domain"</automated>
  </verify>
  <acceptance_criteria>
    - test_recovery.py contains a test seeding an AWAITING_CLOUD file and asserting it is NOT in the analyze done-set and NOT domain-completed for process_file
    - No production change to _DOMAIN_COMPLETED_STAGES or the analyze done-set (D-04 by omission)
    - `uv run pytest tests/test_tasks/test_recovery.py -x -k "awaiting or pending or domain"` exits 0
  </acceptance_criteria>
  <done>A regression guard proves AWAITING_CLOUD stays pending so recovery/release keeps re-driving held files (D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller cron → compute queue | scheduled state-driven release; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-07 | Denial of Service | release cron over-enqueues held files | mitigate | Bounded state scan (only state==AWAITING_CLOUD) + `process_file:<id>` deterministic-key dedup + state reset to DISCOVERED so a released file leaves the scanned set; no whole-backlog sweep |
| T-49-08 | Elevation of Privilege | release routes a long file to a media-less compute agent only | accept | Intended — the kind="compute" filter is load-bearing; compute agents are the correct target for long files (Phase 48) |
</threat_model>

<verification>
- `uv run pytest tests/test_tasks/test_release_awaiting_cloud.py tests/test_tasks/test_recovery.py -x` passes
- `uv run ruff check src/phaze tests` and `uv run mypy src/phaze` clean
- Confirm controller import boundary intact: `uv run pytest tests/test_task_split.py -x` (agent worker must not import control-only modules)
</verification>

<success_criteria>
- Held files release automatically to compute within ~5 min once a compute agent is online (state reset to DISCOVERED), via a NEW narrow cron — not the deleted reenqueue cron, not a ledger replay; AWAITING_CLOUD provably stays pending (D-04).
</success_criteria>

<output>
Create `.planning/phases/49-duration-routing-backfill/49-04-SUMMARY.md` when done.
</output>
