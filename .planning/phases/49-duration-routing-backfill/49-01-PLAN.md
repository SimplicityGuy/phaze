---
phase: 49-duration-routing-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/config.py
  - src/phaze/models/file.py
  - src/phaze/services/enqueue_router.py
  - src/phaze/services/pipeline.py
  - tests/_queue_fakes.py
  - tests/test_services/test_enqueue_router.py
  - tests/test_services/test_pipeline.py
  - tests/test_config.py
autonomous: true
requirements: [CLOUDROUTE-01, CLOUDROUTE-02, CLOUDROUTE-03, CLOUDROUTE-04]
must_haves:
  truths:
    - "A new config knob cloud_route_threshold_sec exists (default 5400, alias PHAZE_CLOUD_ROUTE_THRESHOLD_SEC) and is bounded"
    - "FileState.AWAITING_CLOUD exists as a code-only StrEnum member with value 'awaiting_cloud'"
    - "select_active_agent(session, kind='compute') returns only a compute agent; kind='fileserver' excludes compute agents; no match raises NoActiveAgentError"
    - "A duration-join helper returns (FileRecord, duration|None) tuples for DISCOVERED files without triggering a lazy load"
    - "A degrade-safe get_awaiting_cloud_count returns the count of files in AWAITING_CLOUD"
    - "A backfill-candidate query/count returns exactly ANALYSIS_FAILED files whose joined duration >= threshold (not all ANALYSIS_FAILED)"
    - "seed_active_agent test helper accepts a kind param (default 'fileserver') so a compute agent can be seeded"
  artifacts:
    - path: "src/phaze/config.py"
      provides: "cloud_route_threshold_sec ControlSettings Field"
      contains: "cloud_route_threshold_sec"
    - path: "src/phaze/models/file.py"
      provides: "AWAITING_CLOUD state member"
      contains: "AWAITING_CLOUD"
    - path: "src/phaze/services/enqueue_router.py"
      provides: "kind-filtered select_active_agent"
      contains: "kind"
    - path: "src/phaze/services/pipeline.py"
      provides: "get_discovered_files_with_duration, get_awaiting_cloud_count, backfill-candidate helpers"
      contains: "get_discovered_files_with_duration"
  key_links:
    - from: "src/phaze/services/pipeline.py"
      to: "src/phaze/models/metadata.py"
      via: "outerjoin on FileMetadata.duration"
      pattern: "outerjoin\\(FileMetadata"
    - from: "src/phaze/services/enqueue_router.py"
      to: "src/phaze/models/agent.py"
      via: "Agent.kind filter in select_active_agent"
      pattern: "Agent\\.kind"
---

<objective>
Build the routing primitives every later Phase-49 plan composes against: the duration-threshold config knob (D-07), the AWAITING_CLOUD held-state member (D-01), the kind-filtered active-agent selector (D-13), and the read-only service helpers (duration join, awaiting-cloud count, backfill candidates). Also extend the `seed_active_agent` test helper with a `kind` param so downstream waves can seed a compute agent (RESEARCH A3 / Wave 0).

Purpose: These are the contracts the per-file router (Plan 02), backfill (Plan 03), and release cron (Plan 04) consume. Defining them first prevents downstream scavenger hunts and keeps all `services/pipeline.py` edits in one plan so later plans touch only routers/templates/tasks.
Output: New config Field, new FileState member, kind param on `select_active_agent`, three new `services/pipeline.py` helpers, extended `seed_active_agent`, and their unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-duration-routing-backfill/49-CONTEXT.md
@.planning/phases/49-duration-routing-backfill/49-RESEARCH.md
@.planning/phases/49-duration-routing-backfill/49-VALIDATION.md

<interfaces>
<!-- Contracts established by this plan, consumed by Plans 02/03/04. Use these directly. -->

select_active_agent (extended):
  async def select_active_agent(session: AsyncSession, kind: str | None = None) -> Agent
  # kind=None  -> existing behavior (most-recently-seen non-revoked, ANY kind) — existing callers unchanged
  # kind="compute" / kind="fileserver" -> adds Agent.kind == kind to the WHERE; raises NoActiveAgentError on no match

get_discovered_files_with_duration:
  async def get_discovered_files_with_duration(session: AsyncSession) -> list[tuple[FileRecord, float | None]]
  # LEFT OUTER JOIN FileMetadata.duration; duration captured into the in-memory list (FileRecord.file_metadata is lazy="noload")

get_awaiting_cloud_count:
  async def get_awaiting_cloud_count(session: AsyncSession) -> int   # _safe_count over state == AWAITING_CLOUD, degrades to 0

get_backfill_candidates / count_backfill_candidates:
  async def get_backfill_candidates(session: AsyncSession, threshold_sec: int) -> list[tuple[FileRecord, float | None]]
  async def count_backfill_candidates(session: AsyncSession, threshold_sec: int) -> int
  # state == ANALYSIS_FAILED AND FileMetadata.duration >= threshold_sec (JOIN metadata — NOT get_analysis_failed_count, which over-counts)

FileState.AWAITING_CLOUD = "awaiting_cloud"   # code-only StrEnum, String(30) column, no migration
ControlSettings.cloud_route_threshold_sec: int  # default 5400, alias PHAZE_CLOUD_ROUTE_THRESHOLD_SEC
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cloud_route_threshold_sec config, AWAITING_CLOUD state member, and seed_active_agent kind param</name>
  <files>src/phaze/config.py, src/phaze/models/file.py, tests/_queue_fakes.py, tests/test_config.py</files>
  <read_first>
    - src/phaze/config.py (read the `straggler_threshold_sec` Field ~L344-356 as the exact convention template)
    - src/phaze/models/file.py (FileState StrEnum L20-47, ANALYSIS_FAILED precedent L37-39; FileRecord.state String(30) L62)
    - tests/_queue_fakes.py (seed_active_agent ~L331-348 — the helper to extend with kind)
    - src/phaze/models/agent.py (Agent.kind String(16), server_default 'fileserver', CHECK kind IN ('fileserver','compute'))
    - tests/test_config.py (existing config Field default/alias test style)
  </read_first>
  <behavior>
    - cloud_route_threshold_sec defaults to 5400 when no env set
    - PHAZE_CLOUD_ROUTE_THRESHOLD_SEC env override is honored and parsed as int
    - cloud_route_threshold_sec rejects values <= 0 and >= 86400 (bounded Field)
    - FileState.AWAITING_CLOUD == "awaiting_cloud" and is a valid StrEnum member
    - seed_active_agent(session, kind="compute") inserts an Agent with kind == "compute"; default call still yields kind == "fileserver"
  </behavior>
  <action>
    Add `cloud_route_threshold_sec: int` to `ControlSettings` (config.py), mirroring `straggler_threshold_sec`: `Field(default=5400, gt=0, lt=86400, validation_alias=AliasChoices("PHAZE_CLOUD_ROUTE_THRESHOLD_SEC", "cloud_route_threshold_sec"), description=...)`. Place it adjacent to `straggler_threshold_sec` on `ControlSettings` (D-07).
    Add `AWAITING_CLOUD = "awaiting_cloud"` to the `FileState` StrEnum (file.py), with a comment noting it is a code-only state over `String(30)` (no migration; "awaiting_cloud" is 14 chars), following the ANALYSIS_FAILED precedent (D-01).
    Extend `seed_active_agent` (tests/_queue_fakes.py) with a `kind: str = "fileserver"` keyword param and pass it to the `Agent(...)` constructor so a `kind="compute"` agent can be seeded (RESEARCH A3 / Wave 0). Keep the default so existing callers are unchanged.
    Add a config unit test asserting the default, the alias override, and the bound rejection. Assert the state member value where a config/model test fits.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - config.py contains `cloud_route_threshold_sec` with `default=5400`, `gt=0`, `lt=86400`, and alias `PHAZE_CLOUD_ROUTE_THRESHOLD_SEC`
    - file.py contains `AWAITING_CLOUD = "awaiting_cloud"` inside the FileState StrEnum
    - tests/_queue_fakes.py `seed_active_agent` accepts `kind` (default "fileserver") and sets `Agent.kind`
    - `uv run pytest tests/test_config.py -x` exits 0
    - No Alembic migration file is added (state is code-only over String(30))
  </acceptance_criteria>
  <done>Config knob, AWAITING_CLOUD member, and the kind-aware test helper exist with passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add kind filter to select_active_agent (D-13)</name>
  <files>src/phaze/services/enqueue_router.py, tests/test_services/test_enqueue_router.py</files>
  <read_first>
    - src/phaze/services/enqueue_router.py (select_active_agent L93-117; resolve_queue_for_task L120-151 — confirm existing callers pass NO kind)
    - src/phaze/models/agent.py (Agent.kind column + CHECK)
    - tests/test_services/test_enqueue_router.py (existing select_active_agent test patterns)
    - tests/_queue_fakes.py (seed_active_agent — now kind-aware from Task 1)
  </read_first>
  <behavior>
    - select_active_agent(session, kind="compute") returns the compute agent and only the compute agent
    - select_active_agent(session, kind="fileserver") returns a fileserver agent and EXCLUDES compute agents
    - select_active_agent(session) (no kind) preserves existing behavior — most-recently-seen non-revoked of ANY kind
    - select_active_agent(session, kind="compute") with no compute agent online raises NoActiveAgentError
    - The deterministic most-recently-seen (last_seen_at DESC) + revoked_at IS NULL rule still holds within the kind scope
  </behavior>
  <action>
    Add an optional `kind: str | None = None` parameter to `select_active_agent`. When `kind` is not None, add `Agent.kind == kind` to the existing `.where(...)` predicate; otherwise leave the statement unchanged. Do NOT touch `resolve_queue_for_task` or its `select_active_agent(session)` call (D-13: keep existing callers unchanged). Update the docstring to note the kind scoping. This is the minimal-change route (a param, not a sibling helper) per RESEARCH Pattern 1.
    Add unit tests covering: compute-only match, fileserver excludes compute, no-kind back-compat, and NoActiveAgentError when the requested kind is absent. Seed agents via the kind-aware `seed_active_agent` from Task 1.
  </action>
  <verify>
    <automated>uv run pytest tests/test_services/test_enqueue_router.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - enqueue_router.py `select_active_agent` signature includes `kind: str | None = None`
    - When kind is set, the query filters `Agent.kind == kind`
    - resolve_queue_for_task is unchanged (still calls `select_active_agent(session)` with no kind)
    - Tests prove kind="compute" excludes fileserver agents and vice versa, and that absent-kind raises NoActiveAgentError
    - `uv run pytest tests/test_services/test_enqueue_router.py -x` exits 0
  </acceptance_criteria>
  <done>select_active_agent scopes by agent kind while preserving the default deterministic rule and all existing callers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add duration-join, awaiting-cloud-count, and backfill-candidate service helpers</name>
  <files>src/phaze/services/pipeline.py, tests/test_services/test_pipeline.py</files>
  <read_first>
    - src/phaze/services/pipeline.py (get_files_by_state ~L725; _safe_count ~L272; get_analysis_failed_count ~L761; count_active_agents ~L699 for the SAVEPOINT/degrade idiom)
    - src/phaze/models/file.py (FileRecord.state, file_metadata lazy="noload" L71)
    - src/phaze/models/metadata.py (FileMetadata.duration Float nullable, FileMetadata.file_id FK)
    - tests/test_services/test_pipeline.py (existing count-helper + fixture patterns, real PG session)
  </read_first>
  <behavior>
    - get_discovered_files_with_duration returns one (FileRecord, duration) tuple per DISCOVERED file; duration is the joined FileMetadata.duration or None when no metadata row exists
    - get_awaiting_cloud_count returns the count of files in state AWAITING_CLOUD and degrades to 0 on a DB error (via _safe_count)
    - count_backfill_candidates returns exactly the count of ANALYSIS_FAILED files whose joined duration >= threshold_sec — a short ANALYSIS_FAILED file and a null-duration ANALYSIS_FAILED file are EXCLUDED
    - get_backfill_candidates returns the matching (FileRecord, duration) tuples for the same predicate
  </behavior>
  <action>
    Add three helpers to `services/pipeline.py`:
    1. `get_discovered_files_with_duration(session) -> list[tuple[FileRecord, float | None]]` — `select(FileRecord, FileMetadata.duration).outerjoin(FileMetadata, FileMetadata.file_id == FileRecord.id).where(FileRecord.state == FileState.DISCOVERED)`; return the rows as `(FileRecord, duration)` tuples so duration is captured in-memory before any background task (RESEARCH Pattern 3 — file_metadata is lazy="noload").
    2. `get_awaiting_cloud_count(session) -> int` — clone `get_analysis_failed_count` exactly, swapping the predicate to `FileRecord.state == FileState.AWAITING_CLOUD`, node="awaiting_cloud" (D-05; degrade-safe via `_safe_count`).
    3. `count_backfill_candidates(session, threshold_sec: int) -> int` and `get_backfill_candidates(session, threshold_sec: int) -> list[tuple[FileRecord, float | None]]` — INNER JOIN `FileMetadata` filtering `FileRecord.state == FileState.ANALYSIS_FAILED AND FileMetadata.duration >= threshold_sec` (D-09/D-10; do NOT reuse `get_analysis_failed_count`, which over-counts per RESEARCH Pitfall 5). Use `_safe_count` discipline for the count.
    Add unit tests with FileRecord+FileMetadata fixtures at >=threshold, <threshold, and null duration (Wave 0 fixture requirement) proving each helper's filtering.
  </action>
  <verify>
    <automated>uv run pytest tests/test_services/test_pipeline.py -x -q -k "duration or awaiting or backfill"</automated>
  </verify>
  <acceptance_criteria>
    - pipeline.py contains `get_discovered_files_with_duration`, `get_awaiting_cloud_count`, `count_backfill_candidates`, `get_backfill_candidates`
    - get_discovered_files_with_duration uses an `outerjoin` on FileMetadata and returns tuples (no lazy load)
    - count_backfill_candidates JOINs metadata and excludes short/null-duration ANALYSIS_FAILED files (test asserts a short ANALYSIS_FAILED file is NOT counted)
    - get_awaiting_cloud_count returns 0 (not raise) on a forced DB error
    - `uv run pytest tests/test_services/test_pipeline.py -x -k "duration or awaiting or backfill"` exits 0
  </acceptance_criteria>
  <done>The three read helpers exist with the correct duration-join semantics and passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → ControlSettings | PHAZE_CLOUD_ROUTE_THRESHOLD_SEC parsed into a pydantic int Field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Tampering | cloud_route_threshold_sec config | mitigate | Bounded pydantic int Field (gt=0, lt=86400) mirroring straggler_threshold_sec — an out-of-range value fails validation at startup, never reaches the SQL compare |
| T-49-02 | Injection | duration/backfill queries | mitigate | All new queries are ORM with bound params; `duration >= threshold` is a bound int, never interpolated SQL |
</threat_model>

<verification>
- `uv run pytest tests/test_config.py tests/test_services/test_enqueue_router.py tests/test_services/test_pipeline.py -x` passes
- `uv run ruff check src/phaze tests` and `uv run mypy src/phaze` clean (mypy excludes tests/)
- No Alembic migration added (verify `ls src/phaze/migrations/versions` unchanged for state)
</verification>

<success_criteria>
- cloud_route_threshold_sec, FileState.AWAITING_CLOUD, kind-filtered select_active_agent, and the three service helpers all exist with passing unit tests, providing every primitive Plans 02/03/04 compose against.
</success_criteria>

<output>
Create `.planning/phases/49-duration-routing-backfill/49-01-SUMMARY.md` when done.
</output>
