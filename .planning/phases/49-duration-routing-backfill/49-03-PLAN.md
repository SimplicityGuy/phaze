---
phase: 49-duration-routing-backfill
plan: 03
type: execute
wave: 3
depends_on: ["49-01", "49-02"]
files_modified:
  - src/phaze/routers/pipeline.py
  - src/phaze/templates/pipeline/partials/dag_canvas.html
  - src/phaze/templates/pipeline/partials/backfill_response.html
  - tests/test_routers/test_pipeline.py
autonomous: true
requirements: [CLOUDROUTE-04]
must_haves:
  truths:
    - "A 'Backfill to cloud' button on the pipeline dashboard posts to /pipeline/backfill-cloud and returns a count-confirmed partial"
    - "Backfill selects exactly ANALYSIS_FAILED files whose duration >= threshold, resets them to DISCOVERED (committed), and routes each through the same duration router"
    - "Files routed to AWAITING_CLOUD during backfill get an explicit ledger row via insert_ledger_if_absent; the enqueued branch relies on the before_enqueue hook (no double-write)"
    - "A double-click backfill dedups to a no-op via the process_file:<id> deterministic key — never a whole-backlog over-enqueue"
    - "Never-failed and short ANALYSIS_FAILED files are untouched"
  artifacts:
    - path: "src/phaze/routers/pipeline.py"
      provides: "POST /pipeline/backfill-cloud endpoint"
      contains: "backfill-cloud"
    - path: "src/phaze/templates/pipeline/partials/backfill_response.html"
      provides: "count-confirmed backfill response partial"
      contains: "backfill"
    - path: "src/phaze/templates/pipeline/partials/dag_canvas.html"
      provides: "Backfill to cloud button"
      contains: "/pipeline/backfill-cloud"
  key_links:
    - from: "src/phaze/routers/pipeline.py"
      to: "src/phaze/services/scheduling_ledger.py"
      via: "insert_ledger_if_absent for AWAITING_CLOUD-routed backfill files"
      pattern: "insert_ledger_if_absent"
    - from: "src/phaze/routers/pipeline.py"
      to: "src/phaze/routers/pipeline.py"
      via: "reuses _route_discovered_by_duration from Plan 02"
      pattern: "_route_discovered_by_duration"
---

<objective>
Add the operator "Backfill to cloud" action (D-08): select the timed-out long files (`ANALYSIS_FAILED ∧ duration ≥ threshold`), reset them to DISCOVERED, and route each through the SAME per-file duration router built in Plan 02 — compute if online, else AWAITING_CLOUD (D-09). The explicit filter plus deterministic-key dedup close the over-enqueue class (D-10): a double-click is a no-op, and never-failed/short files are untouched.

Purpose: Delivers CLOUDROUTE-04 — backfilling the existing 144 timed-out long files scoped through the Phase-45 ledger, with no whole-backlog over-enqueue.
Output: A `POST /pipeline/backfill-cloud` endpoint, a "Backfill to cloud" button next to the Recover button, and a count-confirmed response partial.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-duration-routing-backfill/49-CONTEXT.md
@.planning/phases/49-duration-routing-backfill/49-RESEARCH.md
@.planning/phases/49-duration-routing-backfill/49-01-SUMMARY.md
@.planning/phases/49-duration-routing-backfill/49-02-SUMMARY.md

<interfaces>
<!-- Consumed from Plans 01/02: -->
get_backfill_candidates(session, threshold_sec) -> list[tuple[FileRecord, float | None]]
count_backfill_candidates(session, threshold_sec) -> int
_route_discovered_by_duration(app_state, session, files_with_duration, threshold_sec, models_path) -> dict[str,int]  # routers/pipeline.py
insert_ledger_if_absent(session, *, key, function, kwargs, timeout=None, retries=None) -> None  # services/scheduling_ledger.py
process_file_job_key(file_id) -> str  # services/analysis_enqueue.py — "process_file:<id>"
settings.cloud_route_threshold_sec: int
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backfill endpoint POST /pipeline/backfill-cloud</name>
  <files>src/phaze/routers/pipeline.py, tests/test_routers/test_pipeline.py</files>
  <read_first>
    - src/phaze/routers/pipeline.py (trigger_recover_ui L943-972 — the background-task + ctx pattern; _route_discovered_by_duration + trigger_analysis_ui from Plan 02; _background_tasks discipline)
    - src/phaze/services/scheduling_ledger.py (insert_ledger_if_absent L95-114; routing_for_function L45-57)
    - src/phaze/services/analysis_enqueue.py (process_file_job_key L32-40, enqueue_process_file payload fields for the ledger kwargs)
    - src/phaze/services/pipeline.py (get_backfill_candidates / count_backfill_candidates from Plan 01)
    - tests/test_routers/test_pipeline.py (router test patterns; dedup fakes; kind-seeded agents)
    - tests/_queue_fakes.py (DedupFakeQueue dedup-to-None behavior for the double-click no-op assertion)
  </read_first>
  <behavior>
    - Backfill selects exactly ANALYSIS_FAILED files with duration >= threshold; a short ANALYSIS_FAILED file and a never-failed DISCOVERED file are NOT touched
    - Selected files are reset to FileState.DISCOVERED and the reset is committed before enqueue
    - With a compute agent online, selected files enqueue process_file onto the compute queue (via the reused router); with none online they end AWAITING_CLOUD
    - A backfill file routed to AWAITING_CLOUD gets an insert_ledger_if_absent row keyed process_file:<id>; the enqueued branch does NOT get a second explicit ledger write (the before_enqueue hook owns it)
    - A second backfill click over the same set dedups to a no-op (deterministic key) — counts reflect zero new enqueues
    - The response reports the candidate count (and the local/cloud/awaiting split)
  </behavior>
  <action>
    Add `POST /pipeline/backfill-cloud` returning the `backfill_response.html` partial. Flow (D-09): `count = await count_backfill_candidates(session, settings.cloud_route_threshold_sec)`; if zero, return the empty-count fragment. Otherwise `candidates = await get_backfill_candidates(...)`, set each `file.state = FileState.DISCOVERED`, `await session.commit()` (RESEARCH Pitfall 3 — explicit commit; bounded UPDATE in the request). Then route the candidates through `_route_discovered_by_duration(...)` (reuse Plan 02's helper verbatim — same compute/local/held fork + dedup, D-10). For files the router placed in AWAITING_CLOUD (held, never enqueued, so no before_enqueue hook fires), call `insert_ledger_if_absent(session, key=process_file_job_key(file.id), function="process_file", kwargs=<process_file payload dict>, timeout=7200, retries=2)` and commit — per RESEARCH Open-Q3, ONLY for the held branch (the enqueued branch's hook already wrote the row; avoid a double-write). Run enqueues in a background task per the existing `_background_tasks` discipline. Surface the candidate count in the response context (optionally the split).
    Add router tests: exact-set selection (short/never-failed excluded), DISCOVERED reset committed, compute-online enqueue capture, no-compute held + ledger row, and a double-click no-op (second call enqueues nothing new).
  </action>
  <verify>
    <automated>uv run pytest tests/test_routers/test_pipeline.py -x -q -k "backfill"</automated>
  </verify>
  <acceptance_criteria>
    - routers/pipeline.py defines `POST /pipeline/backfill-cloud`
    - The endpoint resets candidates to DISCOVERED with an explicit `await session.commit()` and routes via `_route_discovered_by_duration`
    - insert_ledger_if_absent is called ONLY for AWAITING_CLOUD-routed backfill files (test asserts the enqueued branch is not double-written)
    - A test asserts a short ANALYSIS_FAILED file and a DISCOVERED file are untouched by backfill
    - A test asserts a second backfill click produces zero new enqueues (deterministic-key dedup)
    - `uv run pytest tests/test_routers/test_pipeline.py -x -k "backfill"` exits 0
  </acceptance_criteria>
  <done>Backfill re-drives exactly the long failed set through the shared router with ledger scoping and dedup — no whole-backlog over-enqueue.</done>
</task>

<task type="auto">
  <name>Task 2: "Backfill to cloud" button + response partial (D-08)</name>
  <files>src/phaze/templates/pipeline/partials/dag_canvas.html, src/phaze/templates/pipeline/partials/backfill_response.html, tests/test_routers/test_pipeline.py</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/dag_canvas.html (the Recover button block L260-282 — the EXACT button + hx-post + hx-indicator + Alpine error-flag pattern to mirror)
    - src/phaze/templates/pipeline/partials/recover_response.html (the response-fragment precedent)
    - src/phaze/templates/pipeline/partials/trigger_response.html (split-count copy style from Plan 02)
  </read_first>
  <action>
    Add a "Backfill to cloud" button next to the Recover button in `dag_canvas.html`, mirroring its markup: `hx-post="/pipeline/backfill-cloud"`, `hx-target` a sibling response slot, `hx-swap="innerHTML"`, `hx-indicator` a spinner, and the Alpine `@htmx:after-request` error flag. Give it a distinct id/aria-label ("Backfill timed-out long files to the cloud"). Keep it a neutral header action, NOT a per-stage DAG node (NODE_LAYOUT/EDGES counts unchanged).
    Create `backfill_response.html` modeled on `recover_response.html` / `trigger_response.html`: render the count-confirmed result (e.g. "Backfilled N long files: M cloud, K awaiting cloud" or a zero-candidates message), plain ints through autoescape.
    Add a template/router test asserting the button posts to /pipeline/backfill-cloud and the response partial renders the count copy.
  </action>
  <verify>
    <automated>uv run pytest tests/test_routers/test_pipeline.py -x -q -k "backfill_button or backfill_response or backfill"</automated>
  </verify>
  <acceptance_criteria>
    - dag_canvas.html contains a button with `hx-post="/pipeline/backfill-cloud"` and a distinct aria-label
    - backfill_response.html exists and renders the count-confirmed copy
    - The DAG NODE_LAYOUT/EDGES node count is unchanged (header action, not a node)
    - `uv run pytest tests/test_routers/test_pipeline.py -x -k "backfill"` exits 0
  </acceptance_criteria>
  <done>The operator can trigger backfill from the dashboard and sees a count-confirmed result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → POST /pipeline/backfill-cloud | no free-text; the candidate set is a server-side ANALYSIS_FAILED∧duration≥threshold query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-05 | Denial of Service | backfill double-click detonates the queue | mitigate | Explicit `ANALYSIS_FAILED ∧ duration≥threshold` filter (NOT a backlog sweep) + `process_file:<id>` deterministic-key dedup (D-10); test asserts a second click enqueues nothing new |
| T-49-06 | Injection | backfill candidate query | mitigate | ORM bound-param query; `duration >= threshold` is a bound int |
</threat_model>

<verification>
- `uv run pytest tests/test_routers/test_pipeline.py -x` passes
- `uv run ruff check src/phaze tests` and `uv run mypy src/phaze` clean
- Manual: before trusting the "144" figure, run `SELECT count(*) FROM files f JOIN metadata m ON m.file_id=f.id WHERE f.state='analysis_failed' AND m.duration >= 5400;` and confirm the button-label count matches (49-VALIDATION manual-only check / RESEARCH A1)
</verification>

<success_criteria>
- CLOUDROUTE-04 observable: backfill enqueues exactly the long failed set through the shared router, held files get a ledger row, a double-click is a no-op, and unrelated files are untouched.
</success_criteria>

<output>
Create `.planning/phases/49-duration-routing-backfill/49-03-SUMMARY.md` when done.
</output>
