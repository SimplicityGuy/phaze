---
phase: 49-duration-routing-backfill
plan: 02
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - src/phaze/routers/pipeline.py
  - src/phaze/templates/pipeline/partials/trigger_response.html
  - src/phaze/templates/pipeline/partials/awaiting_cloud_card.html
  - src/phaze/templates/pipeline/dashboard.html
  - src/phaze/templates/pipeline/partials/stats_bar.html
  - tests/test_routers/test_pipeline.py
autonomous: true
requirements: [CLOUDROUTE-01, CLOUDROUTE-02, CLOUDROUTE-03]
must_haves:
  truths:
    - "A >=threshold file with a compute agent online enqueues process_file onto the compute agent's queue, NOT the fileserver queue"
    - "A <threshold or null-duration file enqueues process_file onto the fileserver agent's queue with the same key/payload/policy as today"
    - "A >=threshold file with no compute agent online transitions to state AWAITING_CLOUD (committed) and produces NO process_file enqueue"
    - "The Run-analysis response reports split counts: N local, M cloud, K awaiting cloud"
    - "An 'Awaiting cloud' count card renders on the dashboard and re-pushes via OOB swap on the 5s poll"
  artifacts:
    - path: "src/phaze/routers/pipeline.py"
      provides: "per-file duration router fork + reusable routing helper + awaiting-cloud card wiring"
      contains: "_route_discovered_by_duration"
    - path: "src/phaze/templates/pipeline/partials/awaiting_cloud_card.html"
      provides: "Awaiting cloud count card"
      contains: "awaiting-cloud-card"
  key_links:
    - from: "src/phaze/routers/pipeline.py"
      to: "src/phaze/services/enqueue_router.py"
      via: "select_active_agent(session, kind=...) pre-selected once per kind"
      pattern: "select_active_agent\\(.*kind="
    - from: "src/phaze/routers/pipeline.py"
      to: "src/phaze/services/analysis_enqueue.py"
      via: "enqueue_process_file reused verbatim for local AND cloud"
      pattern: "enqueue_process_file"
    - from: "src/phaze/templates/pipeline/partials/stats_bar.html"
      to: "src/phaze/templates/pipeline/partials/awaiting_cloud_card.html"
      via: "OOB include on the 5s poll"
      pattern: "awaiting_cloud_card.html"
---

<objective>
Replace the all-files-to-one-queue analyze enqueue with a per-file duration router (D-06/D-11), report split counts in the trigger response (D-12), and surface held files as an "Awaiting cloud" count card (D-05). Factor the per-file routing decision into a reusable module-level helper so Plan 03 (backfill) routes through the SAME path.

Purpose: This delivers the core of CLOUDROUTE-01/02/03 — long files go to a compute agent, short/null files stay local unchanged, and ≥threshold files with no compute agent online are held in AWAITING_CLOUD and never silently analyzed locally (the load-bearing safety invariant), with the held count visible to the operator.
Output: A `_route_discovered_by_duration` helper, the forked `trigger_analysis` / `trigger_analysis_ui`, the split-count `trigger_response.html`, and the new `awaiting_cloud_card.html` wired into `dashboard()` + `pipeline_stats_partial()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-duration-routing-backfill/49-CONTEXT.md
@.planning/phases/49-duration-routing-backfill/49-RESEARCH.md
@.planning/phases/49-duration-routing-backfill/49-01-SUMMARY.md

<interfaces>
<!-- Consumed from Plan 01 (use directly): -->
select_active_agent(session, kind="compute"|"fileserver") -> Agent  # raises NoActiveAgentError on no match
get_discovered_files_with_duration(session) -> list[tuple[FileRecord, float | None]]
get_awaiting_cloud_count(session) -> int
FileState.AWAITING_CLOUD = "awaiting_cloud"
settings.cloud_route_threshold_sec: int  # default 5400

<!-- Reused verbatim (do NOT hand-roll a new key/payload): -->
enqueue_process_file(queue, file, agent_id, models_path) -> Any   # services/analysis_enqueue.py:43; key process_file:<id>, timeout=7200, retries=2
app.state.task_router.queue_for(agent_id) -> Queue                # per-agent queue (NEVER the default queue)

<!-- Contract this plan DEFINES for Plan 03 to reuse: -->
async def _route_discovered_by_duration(app_state, session, files_with_duration, threshold_sec, models_path) -> dict[str, int]
  # routers/pipeline.py module-level helper. Pre-selects fileserver_q (kind="fileserver") and
  # compute_q (kind="compute", None on NoActiveAgentError) ONCE. Per file:
  #   duration is None or < threshold -> fileserver_q + enqueue_process_file  [local]
  #   duration >= threshold AND compute online -> compute_q + enqueue_process_file  [cloud]
  #   duration >= threshold AND no compute -> UPDATE FileRecord.state = AWAITING_CLOUD  [held]
  # Commits the held-state UPDATEs (get_session does NOT auto-commit). Enqueues run after commit.
  # Returns {"local": N, "cloud": M, "awaiting": K}.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-file duration router fork + split-count response</name>
  <files>src/phaze/routers/pipeline.py, src/phaze/templates/pipeline/partials/trigger_response.html, tests/test_routers/test_pipeline.py</files>
  <read_first>
    - src/phaze/routers/pipeline.py (trigger_analysis L254-284; trigger_analysis_ui L434-461; _enqueue_analysis_jobs L225-245; deepen_analysis L464-490 for the NoActiveAgentError/cast pattern)
    - src/phaze/services/analysis_enqueue.py (enqueue_process_file L43-101 — the verbatim producer for both local and cloud)
    - src/phaze/services/enqueue_router.py (resolve_queue_for_task L120-151 — the "never default queue" invariant; select_active_agent now kind-aware)
    - src/phaze/templates/pipeline/partials/trigger_response.html (current shape — no_active_agent / count branches)
    - tests/_queue_fakes.py (DedupFakeQueue/Router capture of (queue_name, task, kwargs); seed_active_agent kind param; stub_app_state)
    - tests/test_routers/test_pipeline.py (existing trigger_analysis test patterns + FakeTaskRouter capture)
  </read_first>
  <behavior>
    - >=threshold file + compute agent online: a process_file job is captured on queue name phaze-agent-<compute-id>, NOT phaze-agent-<fileserver-id>
    - <threshold file: process_file captured on phaze-agent-<fileserver-id> with the existing deterministic key process_file:<id>
    - null-duration file: routes local (treated as short) — captured on the fileserver queue
    - >=threshold file + NO compute agent online: file ends in state AWAITING_CLOUD (row committed), and NO process_file job is captured for it
    - The HTMX /pipeline/analyze response renders the split counts "N local, M cloud, K awaiting cloud"
    - With no fileserver agent online at all, the endpoint surfaces the existing no-active-agent fragment (does NOT fall through to the default queue)
  </behavior>
  <action>
    Add a module-level `_route_discovered_by_duration(app_state, session, files_with_duration, threshold_sec, models_path) -> dict[str, int]` to routers/pipeline.py implementing the contract in the `<interfaces>` block: pre-select `fileserver_q` via `select_active_agent(session, kind="fileserver")` and `compute_q` via `select_active_agent(session, kind="compute")` wrapped in `try/except NoActiveAgentError` (None on miss) ONCE before the loop (RESEARCH anti-pattern: do NOT call resolve_queue_for_task per file); obtain each queue from `app_state.task_router.queue_for(agent.id)` (Phase-30 invariant — never the default queue). Loop the `(file, duration)` tuples and route per D-06: None/`< threshold` → local enqueue, `>= threshold` with compute → cloud enqueue, `>= threshold` without compute → set `file.state = FileState.AWAITING_CLOUD`. Commit the AWAITING_CLOUD UPDATEs with an explicit `await session.commit()` BEFORE backgrounding the enqueues (RESEARCH Pitfall 3 — get_session does not auto-commit; do the bounded state write in the request). Reuse `enqueue_process_file` verbatim for both local and cloud (identical key → cross-path dedup, D-10). Return the `{local, cloud, awaiting}` tally.
    Rewrite `trigger_analysis` (JSON API) and `trigger_analysis_ui` (HTMX) to call `get_discovered_files_with_duration` then `_route_discovered_by_duration`, returning the split counts. If `select_active_agent(session, kind="fileserver")` raises NoActiveAgentError (no local agent at all), preserve the existing no-active-agent response. Keep `_enqueue_analysis_jobs` available for the actual enqueue loop or inline the per-queue enqueue lists; ensure enqueues still run in a background task to avoid HTTP timeout.
    Update `trigger_response.html` to render the split counts (e.g. "Enqueued N local, M cloud, K awaiting cloud") via new context keys, preserving the no_active_agent and zero-files branches.
    Add router tests using the dedup fakes + kind-seeded agents covering all five behaviors above (compute capture, fileserver capture, null→local, held→AWAITING_CLOUD no-enqueue, split-count copy).
  </action>
  <verify>
    <automated>uv run pytest tests/test_routers/test_pipeline.py -x -q -k "analy or route or awaiting or duration"</automated>
  </verify>
  <acceptance_criteria>
    - routers/pipeline.py contains `_route_discovered_by_duration` with an explicit `await session.commit()` on the AWAITING_CLOUD branch
    - The compute path obtains its queue via `task_router.queue_for(agent.id)` (never an unnamed/default queue)
    - A test asserts a >=5400s file with a compute agent online is captured on the compute queue and NOT the fileserver queue
    - A test asserts a >=5400s file with no compute agent online ends state==AWAITING_CLOUD with NO process_file capture
    - A test asserts a <threshold and a null-duration file are captured on the fileserver queue with key process_file:<id>
    - trigger_response.html renders local/cloud/awaiting split counts
    - `uv run pytest tests/test_routers/test_pipeline.py -x -k "analy or route or awaiting or duration"` exits 0
  </acceptance_criteria>
  <done>Analyze routes per-file by duration with the held-not-local invariant enforced and split counts reported; backfill (Plan 03) can reuse `_route_discovered_by_duration`.</done>
</task>

<task type="auto">
  <name>Task 2: "Awaiting cloud" count card (D-05)</name>
  <files>src/phaze/templates/pipeline/partials/awaiting_cloud_card.html, src/phaze/templates/pipeline/dashboard.html, src/phaze/templates/pipeline/partials/stats_bar.html, src/phaze/routers/pipeline.py, tests/test_routers/test_pipeline.py</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/straggler_failed_card.html (the EXACT card + OOB contract to mirror: same id on both renders, `{% if oob %}hx-swap-oob="true"{% endif %}`)
    - src/phaze/templates/pipeline/dashboard.html (L21 — where straggler_failed_card is included inline)
    - src/phaze/templates/pipeline/partials/stats_bar.html (L71-73 — the `{% with oob = True %}` OOB include of straggler_failed_card on the 5s poll)
    - src/phaze/routers/pipeline.py (dashboard() L316-387 and pipeline_stats_partial() L390-431 — where straggler_count/analysis_failed_count are seeded into BOTH contexts)
    - .planning/phases/49-duration-routing-backfill/49-01-SUMMARY.md (get_awaiting_cloud_count signature)
  </read_first>
  <action>
    Create `awaiting_cloud_card.html` mirroring `straggler_failed_card.html`: a `<section id="awaiting-cloud-card" {% if oob %}hx-swap-oob="true"{% endif %} ...>` rendering `{{ awaiting_cloud_count }}` with a clear "Awaiting cloud" label and a short subtitle ("held — no compute agent online"). Render plain int through Jinja autoescape (no operator free-text).
    Wire `get_awaiting_cloud_count` (from Plan 01) into BOTH `dashboard()` and `pipeline_stats_partial()` contexts as `awaiting_cloud_count` (degrade-safe service read — no router try/except, mirroring the straggler wiring). Include the card inline in `dashboard.html` near the straggler card, and add the `{% with oob = True %}{% include "pipeline/partials/awaiting_cloud_card.html" %}{% endwith %}` OOB push in `stats_bar.html` next to the straggler OOB include (D-05).
    Add a router/template test asserting the card renders the AWAITING_CLOUD count on the dashboard and that the stats partial emits the OOB-swapped card.
  </action>
  <verify>
    <automated>uv run pytest tests/test_routers/test_pipeline.py -x -q -k "awaiting_card or dashboard or stats"</automated>
  </verify>
  <acceptance_criteria>
    - awaiting_cloud_card.html exists with id "awaiting-cloud-card" and the `{% if oob %}hx-swap-oob="true"{% endif %}` contract
    - dashboard() and pipeline_stats_partial() both put `awaiting_cloud_count` in context
    - dashboard.html includes the card inline; stats_bar.html includes it with oob=True
    - A test asserts the rendered dashboard contains the AWAITING_CLOUD count and the card id
    - `uv run pytest tests/test_routers/test_pipeline.py -x -k "awaiting_card or dashboard or stats"` exits 0
  </acceptance_criteria>
  <done>Operators see the held-file count on first load and live on the 5s poll, satisfying the "operator can see it waiting" half of CLOUDROUTE-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → POST /pipeline/analyze | triggers per-file routing; no operator free-text (file ids are server UUIDs) |
| control plane → compute/fileserver queues | per-agent named queues only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-03 | Denial of Service | held long files silently analyzed locally and time out | mitigate | Load-bearing CLOUDROUTE-02 invariant: >=threshold + no compute → AWAITING_CLOUD, NEVER the fileserver queue (D-02); test asserts no process_file capture for held files |
| T-49-04 | Tampering | enqueue targeting the consumer-less default queue | mitigate | Queues obtained only via `task_router.queue_for(agent.id)`; never an unnamed Queue (Phase-30 invariant) |
</threat_model>

<verification>
- `uv run pytest tests/test_routers/test_pipeline.py -x` passes
- `uv run ruff check src/phaze tests` and `uv run mypy src/phaze` clean
- Manual dashboard smoke optional; the OOB card contract is unit-asserted
</verification>

<success_criteria>
- CLOUDROUTE-01/02/03 observable: long→compute, short/null→local-unchanged, no-compute→held-not-local with the count visible; split counts reported; `_route_discovered_by_duration` is reusable by Plan 03.
</success_criteria>

<output>
Create `.planning/phases/49-duration-routing-backfill/49-02-SUMMARY.md` when done.
</output>
