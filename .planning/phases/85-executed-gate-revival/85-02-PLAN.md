---
phase: 85-executed-gate-revival
plan: 02
type: execute
wave: 2
depends_on: ["85-01"]
files_modified:
  - src/phaze/services/tag_writer.py
  - src/phaze/routers/tags.py
  - tests/review/services/test_tag_writer.py
  - tests/review/routers/test_tags.py
autonomous: true
requirements: [READ-05]

must_haves:
  truths:
    - "READ-05/D-01: the tag-write guard `tag_writer.py:185` gates on `await is_applied(session, file_record.id)`, not `file_record.state != FileState.EXECUTED`"
    - "READ-05/D-01: the tags.py stat-card count (:44), list_tags WHERE+count (:174/:179) and per-record write guard (:336) read `applied_clause()`/`is_applied()`; the previously-empty operator Tag list now populates for actually-applied files"
    - "D-02: the `completed_subq` (TagWriteLog.status==COMPLETED) anti-join is preserved at the bulk builder — idempotency intact, no state-based de-dupe reintroduced"
    - "D-03: `bulk_write_no_discrepancies` (:422) is bounded by a LIMIT cap so a large first-time-visible applied backlog cannot blow up the loop at 200K scale"
    - "READ-05/SC#2: a mutation-checked behavior test proves an actually-applied file (proposals.status='executed', file.state != 'executed') now PASSES the tag-write guard that previously always failed"
  artifacts:
    - path: "src/phaze/services/tag_writer.py"
      provides: "is_applied-gated execute_tag_write"
      contains: "is_applied"
    - path: "src/phaze/routers/tags.py"
      provides: "applied_clause readers + is_applied guard + bounded bulk"
      contains: "applied_clause"
    - path: "tests/review/services/test_tag_writer.py"
      provides: "SC#2 behavior-change + mutation-checked guard test"
      contains: "is_applied"
  key_links:
    - from: "src/phaze/services/tag_writer.py::execute_tag_write"
      to: "services.stage_status.is_applied"
      via: "await is_applied(session, file_record.id) guard"
      pattern: "is_applied\\(session"
    - from: "src/phaze/routers/tags.py"
      to: "services.stage_status.applied_clause"
      via: ".where(applied_clause())"
      pattern: "applied_clause\\(\\)"
---

<objective>
Cut the tag-write path (write guard + the five `tags.py` gate/count/list sites) over to the D-01
`applied()` predicate, bound the unbounded bulk builder (D-03), and land the load-bearing,
mutation-checked SC#2 behavior test that proves an actually-applied file now passes a
previously-always-failing guard. This is the milestone's one behavior-reviving, filesystem-mutating
seam — kept small and self-contained.

Purpose: Turn the permanently-dead tag-writer path live for READ-05.
Output: `is_applied`-gated `execute_tag_write`, `applied_clause()` readers in `tags.py`, a bounded
bulk loop, and migrated + mutation-checked tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-executed-gate-revival/85-RESEARCH.md
@.planning/phases/85-executed-gate-revival/85-PATTERNS.md
@.planning/phases/85-executed-gate-revival/85-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (services/stage_status.py) — consume directly, do not re-implement. -->
  def applied_clause() -> ColumnElement[bool]        # correlated exists over proposals, status=='executed'
  async def is_applied(session, file_id: uuid.UUID) -> bool

Import in the consuming modules:
  from phaze.services.stage_status import applied_clause, is_applied

Sites to swap (VERIFIED line numbers, branch SimplicityGuy/phase-85):
  tag_writer.py:185   if file_record.state != FileState.EXECUTED:  → if not await is_applied(session, file_record.id):
  tags.py:44          .where(FileRecord.state == FileState.EXECUTED)                      → .where(applied_clause())
  tags.py:174         .where(FileRecord.state == FileState.EXECUTED)  (list_tags — ALREADY paginated) → swap WHERE only
  tags.py:179         count_stmt ... .where(FileRecord.state == FileState.EXECUTED)        → .where(applied_clause())
  tags.py:336         if file_record.state != FileState.EXECUTED:  → if not await is_applied(session, file_id):
  tags.py:422         .where(FileRecord.state == FileState.EXECUTED, FileRecord.id.not_in(completed_subq)) → .where(applied_clause(), FileRecord.id.not_in(completed_subq)) + LIMIT

completed_subq (PRESERVE — D-02): select(TagWriteLog.file_id).where(TagWriteLog.status == TagWriteStatus.COMPLETED)
execute_tag_write signature: async def execute_tag_write(session, file_record, tags, source) (tag_writer.py:165)
Commit discipline (VERIFIED correct — memory landmine): write_file_tags commits at tags.py:369; bulk at :438; undo at :475. Re-verify after edits; add NO new commit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap tag_writer + tags.py gates to applied(); bound the bulk builder</name>
  <files>src/phaze/services/tag_writer.py, src/phaze/routers/tags.py</files>
  <read_first>
    - src/phaze/services/tag_writer.py (lines 160-200: execute_tag_write + the :185 state guard; :189 file_record.current_path — the field the writer mutates)
    - src/phaze/routers/tags.py (lines 40-46 stat card; 157-225 list_tags — the already-paginated reference idiom; 330-370 write_file_tags guard+commit; 404-450 bulk_write_no_discrepancies + completed_subq + commit)
    - src/phaze/services/stage_status.py (applied_clause / is_applied from Plan 01)
    - .planning/phases/85-executed-gate-revival/85-PATTERNS.md (sections "routers/tags.py" and "services/tag_writer.py")
  </read_first>
  <action>
    Import `applied_clause, is_applied` from `phaze.services.stage_status` in both files. tag_writer.py:
    swap the :185 guard to `if not await is_applied(session, file_record.id):` (execute_tag_write is
    already async with `session` in scope) — keep the existing `raise ValueError(...)` message
    containing "executed". tags.py: swap the WHERE at :44, :174, :179 to `applied_clause()`; swap the
    :336 per-record guard to `if not await is_applied(session, file_id):`; at :422 swap the WHERE to
    `.where(applied_clause(), FileRecord.id.not_in(completed_subq))` and append a `.limit(...)` cap using
    a new module constant (e.g. `_MAX_BULK_TAG_WRITE`, discretion on value — pick a batch cap in the
    low-thousands consistent with in-tree page bounds) so the operator-triggered loop is bounded (D-03).
    PRESERVE `completed_subq` (D-02 idempotency anti-join) — do NOT replace it with a state-based
    de-dupe. Do NOT re-paginate `list_tags` (:174 is already fully paginated — swap the WHERE only).
    Remove the now-dead `FileState` import from each file IF unused after the swap (ruff F401 — verify
    per file; `tags.py` may lose it entirely). Re-verify commit discipline (memory landmine): the swaps
    are read-only at the guards; the downstream `session.commit()` calls at tags.py:369/438/475 are
    unchanged — add NO new commit.
  </action>
  <verify>
    <automated>uv run ruff check src/phaze/services/tag_writer.py src/phaze/routers/tags.py && uv run mypy src/phaze/services/tag_writer.py src/phaze/routers/tags.py && ! grep -n "FileState.EXECUTED" src/phaze/services/tag_writer.py src/phaze/routers/tags.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FileState.EXECUTED" src/phaze/services/tag_writer.py src/phaze/routers/tags.py` == 0 (all swapped).
    - `tag_writer.py` contains `await is_applied(session, file_record.id)`; `tags.py` contains `applied_clause()` (readers) and `await is_applied(session, file_id)` (:336 guard).
    - `tags.py` bulk builder still contains `TagWriteStatus.COMPLETED` (completed_subq preserved) AND a `.limit(` cap.
    - `list_tags` still contains its existing `offset`/`.limit(page_size)`/`Pagination(` (not re-paginated).
    - `uv run mypy` and `uv run ruff check` on both files exit 0 (no F401 dead FileState import).
  </acceptance_criteria>
  <done>All five tags.py sites + the tag_writer guard read applied(); bulk bounded; completed_subq preserved; commit discipline unchanged; mypy/ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SC#2 mutation-checked behavior test + migrate EXECUTED-seeded tag fixtures</name>
  <files>tests/review/services/test_tag_writer.py, tests/review/routers/test_tags.py</files>
  <read_first>
    - tests/review/services/test_tag_writer.py (TestExecuteTagWrite, lines 183-243 — the MagicMock().state fixture that STOPS exercising the guard once it becomes a real DB EXISTS; must be reshaped)
    - tests/review/routers/test_tags.py (existing fixtures seeding state='executed' — migrate to proposals.status='executed')
    - .planning/phases/85-executed-gate-revival/85-PATTERNS.md (section "tests/review/services/test_tag_writer.py — behavior-change test")
    - .planning/phases/85-executed-gate-revival/85-VALIDATION.md (SC#2 row + Wave 0 mutation-test requirement)
  </read_first>
  <behavior>
    - Applied fixture: a real FileRecord with `state='moved'` (deliberately NOT 'executed') + a RenameProposal with `status='executed'` and no COMPLETED TagWriteLog → `execute_tag_write` PROCEEDS (no ValueError) and the write path runs.
    - Non-applied fixture: file with a proposal `status='failed'` (or 'approved') and no executed proposal → `execute_tag_write` RAISES ValueError matching "executed".
    - Mutation check: reverting the guard to `file_record.state != FileState.EXECUTED` makes the applied fixture (whose file.state='moved') FAIL the guard (RED); restore → GREEN.
    - Any persistence assertion reads from an INDEPENDENT session (conftest get_session override reads uncommitted rows).
  </behavior>
  <action>
    Reshape `TestExecuteTagWrite` off the `MagicMock().state` fixtures (an AsyncMock session no longer
    yields a truthful `is_applied` boolean once the guard is a real DB EXISTS). Migrate the guard cases
    to seed REAL rows against the test DB: an applied file (`FileRecord.state='moved'` + `RenameProposal.
    status='executed'`) that the guard must ADMIT, and a non-applied file (proposal `status='failed'`/
    `'approved'`) that must RAISE `ValueError` matching "executed". Assert any resulting persistence from
    an INDEPENDENT session (memory: conftest overrides get_session; uncommitted rows read true otherwise).
    Perform the mutation check manually: temporarily revert `tag_writer.py:185` to `file_record.state !=
    FileState.EXECUTED`, run the applied case, confirm it goes RED (the file.state='moved' fixture now
    fails the guard), restore the swap, confirm GREEN — record this RED→GREEN in the SUMMARY (memory
    feedback_mutation_test_guard_tests: a green guard proves nothing until you watch it go red). Migrate
    the `state='executed'`-seeded fixtures in `test_tags.py` to seed `proposals.status='executed'`
    (optionally `file.state='moved'`) so the list/count/guard tests exercise the real predicate.
  </action>
  <verify>
    <automated>just test-bucket review</automated>
  </verify>
  <acceptance_criteria>
    - `test_tag_writer.py` no longer sets `.state` on a MagicMock to drive the guard; the applied case seeds `RenameProposal.status='executed'` with `FileRecord.state` != 'executed' and asserts the write proceeds.
    - A non-applied case asserts `pytest.raises(ValueError, match="executed")`.
    - `grep -c "state=\"executed\"\|state='executed'" tests/review/routers/test_tags.py` == 0 after migration (fixtures moved to proposals.status).
    - The SUMMARY records the RED→GREEN mutation check for the reverted predicate.
    - `just test-bucket review` exits 0 in isolation.
  </acceptance_criteria>
  <done>SC#2 behavior change proven and mutation-checked; tag fixtures seed the real apply-outcome; review bucket green in isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator click → POST tag-write route → filesystem (mutagen write on file.current_path) | operator-triggered filesystem mutation, gated by applied() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-02 | Tampering/Integrity | tag write targeting a stale/deleted path | mitigate | Gate on `proposals.status=='executed'` (transactionally coupled to `current_path` in the apply commit) not `execution_log` (false-positive risk) — the applied() predicate is the mitigation (RESEARCH §Central Tension). |
| T-85-03 | Elevation/Path traversal | mutagen write on `file.current_path` | accept (untouched) | The path-containment guard `tasks/execution.py::_resolve_and_check_containment` (T-26-11-S1) gates path derivation in the apply path and is OUT of scope / unchanged. Reviving the gate adds no new write surface — the writer was already wired; only the dead predicate changed. |
| T-85-04 | Denial of Service | `bulk_write_no_discrepancies` unbounded loop over a large newly-visible applied backlog | mitigate | `.limit(_MAX_BULK_TAG_WRITE)` batch cap (D-03) bounds the operator-triggered one-shot loop at 200K scale. |
| T-85-SC | Tampering | package installs | N/A | Zero new packages. |
</threat_model>

<verification>
- `uv run ruff check .` / `uv run mypy .` clean for touched files (no dead FileState import).
- `just test-bucket review` green in isolation.
- SUMMARY records the mutation RED→GREEN observation.
</verification>

<success_criteria>
- All five `tags.py` EXECUTED sites + the `tag_writer.py:185` guard read `applied()`.
- `completed_subq` preserved (D-02); bulk builder bounded (D-03); commit discipline unchanged.
- SC#2 behavior test proves an applied file passes a previously-always-failing guard, mutation-checked.
</success_criteria>

<output>
Create `.planning/phases/85-executed-gate-revival/85-02-SUMMARY.md` when done.
</output>
