---
phase: 85-executed-gate-revival
plan: 04
type: execute
wave: 3
depends_on: ["85-01", "85-02", "85-03"]
files_modified:
  - src/phaze/services/review.py
  - tests/review/services/test_review_degrade.py
  - tests/integration/test_review_audit.py
autonomous: true
requirements: [READ-05]

must_haves:
  truths:
    - "READ-05/D-01: the two unbounded review builders read `applied_clause()` — `get_tagwrite_review_rows` (review.py:109) and `get_cue_review_cards` gated set (review.py:251) — instead of `FileRecord.state == FileState.EXECUTED`"
    - "D-03: both builders are bounded (LIMIT cap / page) so a large first-time-visible applied backlog cannot blow up the render at 200K scale"
    - "D-02: the `completed_subq` (TagWriteLog.status==COMPLETED) anti-join is preserved in get_tagwrite_review_rows — idempotency intact"
    - "the `session.begin_nested()` SAVEPOINT + `return []` degrade wrapper is preserved (no router try/except added)"
    - "no `FileRecord.state == FileState.EXECUTED` reader survives in review.py (import removed if dead)"
  artifacts:
    - path: "src/phaze/services/review.py"
      provides: "applied_clause readers + bounded list builders"
      contains: "applied_clause"
    - path: "tests/review/services/test_review_degrade.py"
      provides: "pagination-bound + applied-admit assertion"
      contains: "applied"
  key_links:
    - from: "src/phaze/services/review.py::get_tagwrite_review_rows"
      to: "services.stage_status.applied_clause"
      via: ".where(applied_clause(), FileRecord.id.not_in(completed_subq))"
      pattern: "applied_clause\\(\\)"
    - from: "src/phaze/services/review.py::get_cue_review_cards"
      to: "services.stage_status.applied_clause"
      via: ".where(... applied_clause() ...)"
      pattern: "applied_clause\\(\\)"
---

<objective>
Cut the two genuinely-unbounded operator list builders in `services/review.py` over to the D-01
`applied()` predicate and bound them (D-03) so the now-populating applied backlog cannot blow up the
review render at 200K scale. This is the last cutover seam and depends on Plans 02 (`tags.py`) and 03
(`cue.py`) landing first — `review.py` imports from both at module level and its eligible/tagwrite
halves flow through them.

Purpose: Populate the Tag-write / CUE review lists correctly and safely for READ-05.
Output: `applied_clause()` readers + a LIMIT/page bound in `get_tagwrite_review_rows` /
`get_cue_review_cards`, with the degrade wrapper + `completed_subq` preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-executed-gate-revival/85-RESEARCH.md
@.planning/phases/85-executed-gate-revival/85-PATTERNS.md
@.planning/phases/85-executed-gate-revival/85-02-SUMMARY.md
@.planning/phases/85-executed-gate-revival/85-03-SUMMARY.md

<interfaces>
<!-- From Plan 01 (services/stage_status.py) — consume directly. -->
  def applied_clause() -> ColumnElement[bool]
  from phaze.services.stage_status import applied_clause

<!-- Pagination primitive — DO NOT hand-roll (services/proposal_queries.py:23-58). -->
  @dataclass class Pagination:  page: int; page_size: int; total: int
      total_pages / has_prev / has_next / start / end  (computed properties)

Sites to swap (VERIFIED line numbers, branch SimplicityGuy/phase-85):
  review.py:109  .where(FileRecord.state == FileState.EXECUTED, FileRecord.id.not_in(completed_subq))  → .where(applied_clause(), FileRecord.id.not_in(completed_subq)) + bound
  review.py:251  FileRecord.state == FileState.EXECUTED,  (get_cue_review_cards gated_stmt)             → applied_clause() + bound

PRESERVE (do not touch): completed_subq (D-02); the `async with session.begin_nested():` SAVEPOINT +
`return []` degrade wrappers (review.py:60-76, 103-136, 219-269). Both builders currently take only
`session`. review.py:31 imports `_get_eligible_tracklist_query`/`_build_cue_tracks` from routers.cue
(fixed by Plan 03) and review.py:32 imports from routers.tags (fixed by Plan 02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap review.py:109/:251 to applied_clause() and bound both builders (D-03)</name>
  <files>src/phaze/services/review.py</files>
  <read_first>
    - src/phaze/services/review.py (lines 90-136 get_tagwrite_review_rows incl. completed_subq at :105 + WHERE :109 + begin_nested; lines 202-270 get_cue_review_cards incl. gated_stmt WHERE :251 + begin_nested)
    - src/phaze/routers/tags.py (list_tags:157-225 — the in-tree pagination/Query bound idiom, for reference)
    - src/phaze/services/proposal_queries.py (Pagination dataclass, lines 23-58)
    - src/phaze/services/stage_status.py (applied_clause from Plan 01)
    - .planning/phases/85-executed-gate-revival/85-PATTERNS.md (section "services/review.py")
  </read_first>
  <action>
    Import `applied_clause` from `phaze.services.stage_status`. Swap the WHERE at review.py:109 to
    `.where(applied_clause(), FileRecord.id.not_in(completed_subq))` — PRESERVE `completed_subq` (D-02);
    do NOT re-introduce a state-based de-dupe. Swap the `FileRecord.state == FileState.EXECUTED` conjunct
    in the `get_cue_review_cards` gated_stmt (:251) to `applied_clause()` (leave the sibling
    `Tracklist.status == "approved"`, `Tracklist.file_id.is_not(None)`, and timestamp-subquery conjuncts
    intact). Bound BOTH builders (D-03): the primary approach is a fixed `.limit(...)` cap via a module
    constant (e.g. `_MAX_REVIEW_ROWS`, discretion on value — low-thousands consistent with in-tree page
    bounds), which keeps the edit self-contained to review.py and is the acceptable idiom for these
    degrade-safe render helpers per D-03. Threading full `page`/`page_size` through the router callers to
    match `list_tags` is acceptable ONLY if it stays within review.py + its immediate caller; prefer the
    fixed cap to minimize blast radius. PRESERVE the `session.begin_nested()` SAVEPOINT + `return []`
    degrade wrappers exactly — add NO router try/except. Remove the now-dead `FileState` import (line 28)
    if unused after the swap (ruff F401 — verify).
  </action>
  <verify>
    <automated>uv run ruff check src/phaze/services/review.py && uv run mypy src/phaze/services/review.py && ! grep -n "FileState.EXECUTED" src/phaze/services/review.py && grep -q "applied_clause()" src/phaze/services/review.py && grep -q "TagWriteStatus.COMPLETED\|completed_subq" src/phaze/services/review.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FileState.EXECUTED" src/phaze/services/review.py` == 0.
    - `review.py` contains `applied_clause()` in both `get_tagwrite_review_rows` and `get_cue_review_cards`.
    - `get_tagwrite_review_rows` still contains the `completed_subq` anti-join (`TagWriteStatus.COMPLETED`).
    - Both builders contain a `.limit(` bound; the `async with session.begin_nested():` wrappers are unchanged.
    - `uv run mypy` / `uv run ruff check` exit 0 (no dead FileState import).
  </acceptance_criteria>
  <done>Both review builders read applied() and are bounded; completed_subq + degrade wrappers preserved; mypy/ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Pagination-bound + applied-admit tests; migrate review-audit fixtures</name>
  <files>tests/review/services/test_review_degrade.py, tests/integration/test_review_audit.py</files>
  <read_first>
    - tests/review/services/test_review_degrade.py (existing get_tagwrite_review_rows / get_cue_review_cards degrade tests — analog for the bound + admit cases)
    - tests/integration/test_review_audit.py (existing state='executed'-seeded fixtures to migrate)
    - src/phaze/services/review.py (the just-bounded builders)
    - .planning/phases/85-executed-gate-revival/85-VALIDATION.md (D-03 pagination row + Wave 0)
  </read_first>
  <behavior>
    - Seed more applied files (proposals.status='executed') than the LIMIT cap → the builder returns at most the cap (bounded page), proving D-03.
    - An applied file (proposals.status='executed', file.state != 'executed') with no COMPLETED TagWriteLog appears in get_tagwrite_review_rows; a file with a COMPLETED TagWriteLog does NOT (completed_subq / D-02).
  </behavior>
  <action>
    Add a bounded-list test in `test_review_degrade.py`: seed N applied files (`RenameProposal.status=
    'executed'`, `FileRecord.state='moved'`) exceeding `_MAX_REVIEW_ROWS` and assert the builder returns
    at most the cap. Add an idempotency assertion: an applied file with a COMPLETED `TagWriteLog` is NOT
    offered (completed_subq preserved) while one without IS. Keep the degrade-path tests intact. Migrate
    the `state='executed'`-seeded fixtures in `tests/integration/test_review_audit.py` to seed
    `proposals.status='executed'` (optionally `file.state='moved'`) so the audit exercises the real
    predicate; keep it in the `integration` bucket (partition guard).
  </action>
  <verify>
    <automated>just test-bucket review && just test-bucket integration</automated>
  </verify>
  <acceptance_criteria>
    - `test_review_degrade.py` asserts the builder returns at most `_MAX_REVIEW_ROWS` when more applied files are seeded (D-03 bound).
    - A test asserts a COMPLETED-TagWriteLog applied file is excluded and a non-completed applied file is included (D-02 idempotency).
    - `grep -c "state=\"executed\"\|state='executed'" tests/integration/test_review_audit.py` == 0 after migration.
    - `just test-bucket review` and `just test-bucket integration` exit 0 in isolation.
  </acceptance_criteria>
  <done>Pagination bound + applied-admit + idempotency proven; review-audit fixtures migrated; review + integration buckets green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator poll/render → review list builders over the newly-visible applied backlog | read-only render; the DoS surface is the unbounded list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-01 | Denial of Service | `get_tagwrite_review_rows` / `get_cue_review_cards` rendering a large first-time-visible applied backlog | mitigate | `.limit(_MAX_REVIEW_ROWS)` bound (D-03) caps the query; because there is no operator-supplied `page_size` param on these render helpers, the fixed cap is a stronger DoS control than a `Query(le=100)` bound (V5). |
| T-85-02 | Tampering/Integrity | predicate source | mitigate | `applied_clause()` reads `proposals.status=='executed'` (not execution_log). |
| T-85-SC | Tampering | package installs | N/A | Zero new packages. |
</threat_model>

<verification>
- `uv run ruff check .` / `uv run mypy .` clean for touched files.
- `just test-bucket review` and `just test-bucket integration` green in isolation.
- `grep -rn "FileState.EXECUTED" src/phaze/services/review.py` returns nothing.
- Phase-wide close-out: `grep -rn "FileState.EXECUTED\|proposal.file.state" src/phaze/services src/phaze/routers src/phaze/templates` shows only the OUT-OF-SCOPE sites (services/pipeline.py, services/proposal.py:39, services/shadow_compare.py:139, agent_metadata.py, agent_s3.py, pipeline.py) — none of the 15 revived READ-05 sites remain.
</verification>

<success_criteria>
- Both `review.py` builders read `applied()` and are bounded (D-03).
- `completed_subq` (D-02) and the `begin_nested()` degrade wrappers preserved.
- After this wave, no `FileState.EXECUTED` / `proposal.file.state` reader survives among the 15 READ-05 sites.
</success_criteria>

<output>
Create `.planning/phases/85-executed-gate-revival/85-04-SUMMARY.md` when done.
</output>
