---
phase: 85-executed-gate-revival
plan: 03
type: execute
wave: 2
depends_on: ["85-01"]
files_modified:
  - src/phaze/routers/cue.py
  - src/phaze/routers/tracklists.py
  - tests/review/routers/test_cue.py
  - tests/identify/routers/test_tracklists.py
autonomous: true
requirements: [READ-05]

must_haves:
  truths:
    - "READ-05/D-01: the CUE eligible set (cue.py:48) and missing-timestamp set (cue.py:89) read `applied_clause()`; the `generate_cue` per-record guard (cue.py:251) gates on `await is_applied(session, file_record.id)`"
    - "READ-05/SC#2: an actually-applied file now appears in the CUE eligible/gated list and `generate_cue` admits it (previously always rejected)"
    - "READ-05/D-01: the three tracklists cue-version guards (:138, :600, :897) gate on `await is_applied(...)` instead of `fr.state == FileState.EXECUTED`"
    - "D-04-adjacent: no `FileRecord.state == FileState.EXECUTED` reader survives in cue.py or the three tracklists guards"
  artifacts:
    - path: "src/phaze/routers/cue.py"
      provides: "applied_clause readers + is_applied generate_cue guard"
      contains: "applied_clause"
    - path: "src/phaze/routers/tracklists.py"
      provides: "is_applied cue-version guards"
      contains: "is_applied"
    - path: "tests/review/routers/test_cue.py"
      provides: "SC#2 CUE-admit behavior + migrated fixtures"
      contains: "executed"
  key_links:
    - from: "src/phaze/routers/cue.py::generate_cue"
      to: "services.stage_status.is_applied"
      via: "await is_applied(session, file_record.id) guard before write_cue_file"
      pattern: "is_applied\\(session"
    - from: "src/phaze/routers/cue.py::_get_eligible_tracklist_query"
      to: "services.stage_status.applied_clause"
      via: ".where(applied_clause())"
      pattern: "applied_clause\\(\\)"
---

<objective>
Cut the CUE-write path (eligible/gated readers + the `generate_cue` guard) and the three
`tracklists.py` cue-version per-record guards over to the D-01 `applied()` predicate. This revives
CUE generation for actually-applied files and removes the last `FileRecord.state == EXECUTED` readers
outside `review.py`.

Purpose: Turn the dead CUE-write guard live for READ-05.
Output: `applied_clause()` readers + `is_applied` guards in `cue.py`/`tracklists.py`, with migrated
fixtures and an SC#2 CUE-admit behavior assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-executed-gate-revival/85-RESEARCH.md
@.planning/phases/85-executed-gate-revival/85-PATTERNS.md
@.planning/phases/85-executed-gate-revival/85-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (services/stage_status.py) — consume directly. -->
  def applied_clause() -> ColumnElement[bool]
  async def is_applied(session, file_id: uuid.UUID) -> bool
  from phaze.services.stage_status import applied_clause, is_applied

Sites to swap (VERIFIED line numbers, branch SimplicityGuy/phase-85):
  cue.py:48    FileRecord.state == FileState.EXECUTED,  (eligible set, inside .where(Tracklist.status=="approved", ...)) → applied_clause()
  cue.py:89    FileRecord.state == FileState.EXECUTED,  (missing-timestamp/gated set)                                   → applied_clause()
  cue.py:251   if file_record is None or file_record.state != FileState.EXECUTED:  → if file_record is None or not await is_applied(session, file_record.id):
  tracklists.py:138   if fr and fr.state == FileState.EXECUTED:  → if fr and await is_applied(session, fr.id):
  tracklists.py:600   if fr and fr.state == FileState.EXECUTED:  → if fr and await is_applied(session, fr.id):
  tracklists.py:897   if fr and fr.state == FileState.EXECUTED:  → if fr and await is_applied(session, fr.id):

NOTE (commit discipline, VERIFIED): generate_cue writes a .cue to DISK ONLY, no DB mutation → NO session.commit() needed. Do not add one.
NOTE: services/review.py imports `_get_eligible_tracklist_query` from cue.py — fixing cue.py:48 transitively fixes review.py's eligible half (Plan 04 depends on this landing first).
NOTE: tracklists.py may still use other FileState members elsewhere — verify before removing the FileState import (it may need to stay).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap cue.py readers + generate_cue guard to applied(); assert CUE admit (SC#2)</name>
  <files>src/phaze/routers/cue.py, tests/review/routers/test_cue.py</files>
  <read_first>
    - src/phaze/routers/cue.py (lines 40-95: eligible + missing-timestamp query builders; lines 238-326: generate_cue guard at :251 + write_cue_file at :276; :273 Path(file_record.current_path))
    - src/phaze/services/stage_status.py (applied_clause / is_applied from Plan 01)
    - tests/review/routers/test_cue.py (existing state='executed'-seeded fixtures + generate_cue tests)
    - .planning/phases/85-executed-gate-revival/85-PATTERNS.md (section "routers/cue.py")
  </read_first>
  <behavior>
    - An applied file (RenameProposal.status='executed', file.state != 'executed') appears in the eligible/gated CUE set and generate_cue ADMITS it (proceeds to write_cue_file).
    - A non-applied file (no executed proposal) is excluded and generate_cue returns the "must be executed" error toast.
  </behavior>
  <action>
    Import `applied_clause, is_applied` from `phaze.services.stage_status`. Swap the
    `FileRecord.state == FileState.EXECUTED` line at cue.py:48 and cue.py:89 to `applied_clause()`
    (leave the sibling `Tracklist.status == "approved"`, `Tracklist.file_id.is_not(None)`, and
    timestamp-subquery conjuncts intact). Swap the :251 guard to `if file_record is None or not await
    is_applied(session, file_record.id):` keeping the existing error-toast message. Do NOT add a
    `session.commit()` (generate_cue is disk-only — VERIFIED). Remove the now-dead `FileState` import if
    unused (ruff F401). Migrate the `state='executed'`-seeded fixtures in `test_cue.py` to seed
    `RenameProposal.status='executed'` (optionally `file.state='moved'`) and add/adjust a case asserting
    generate_cue ADMITS an applied file and rejects a non-applied one (SC#2 for the CUE path).
  </action>
  <verify>
    <automated>uv run ruff check src/phaze/routers/cue.py && uv run mypy src/phaze/routers/cue.py && ! grep -n "FileState.EXECUTED" src/phaze/routers/cue.py && just test-bucket review</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FileState.EXECUTED" src/phaze/routers/cue.py` == 0.
    - `cue.py` contains `applied_clause()` (readers) and `await is_applied(session, file_record.id)` (:251 guard).
    - `test_cue.py` seeds `proposals.status='executed'` (not `state='executed'`) and asserts generate_cue admits an applied file + rejects a non-applied file.
    - No `session.commit()` added to generate_cue.
    - `just test-bucket review` exits 0 in isolation; mypy/ruff clean.
  </acceptance_criteria>
  <done>cue.py reads applied(); CUE generation admits applied files; fixtures migrated; review bucket green.</done>
</task>

<task type="auto">
  <name>Task 2: Swap the three tracklists cue-version guards to is_applied; migrate fixtures</name>
  <files>src/phaze/routers/tracklists.py, tests/identify/routers/test_tracklists.py</files>
  <read_first>
    - src/phaze/routers/tracklists.py (lines 130-145, 594-604, 890-901: the `fr = ...scalar_one_or_none(); if fr and fr.state == FileState.EXECUTED: ... _get_cue_version(fr.current_path)` shape at :138/:600/:897)
    - src/phaze/services/stage_status.py (is_applied from Plan 01)
    - tests/identify/routers/test_tracklists.py (existing state='executed'-seeded fixtures)
    - .planning/phases/85-executed-gate-revival/85-PATTERNS.md (section "routers/tracklists.py")
  </read_first>
  <action>
    Import `is_applied` from `phaze.services.stage_status`. At :138, :600, :897 swap `if fr and fr.state
    == FileState.EXECUTED:` to `if fr and await is_applied(session, fr.id):` (option (b) — `fr` is
    already loaded so `fr.current_path` remains available for `_get_cue_version`; the enclosing handlers
    are async with `session` in scope). Sites :138 and :897 are inside list loops (N per-record EXISTS
    queries — negligible on bounded lists). VERIFY whether `FileState` is still used elsewhere in
    `tracklists.py` before removing its import (it may still be needed — do NOT remove blindly; ruff will
    flag F401 only if fully dead). Migrate the `state='executed'`-seeded fixtures in `test_tracklists.py`
    to seed `RenameProposal.status='executed'` so the cue-version guards are exercised against the real
    predicate; keep the file in the `identify` bucket (partition guard).
  </action>
  <verify>
    <automated>uv run ruff check src/phaze/routers/tracklists.py && uv run mypy src/phaze/routers/tracklists.py && ! grep -n "state == FileState.EXECUTED" src/phaze/routers/tracklists.py && just test-bucket identify</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "state == FileState.EXECUTED" src/phaze/routers/tracklists.py` == 0 (all three guards swapped).
    - `tracklists.py` contains `await is_applied(session, fr.id)` at each of the three former guard sites.
    - `test_tracklists.py` seeds `proposals.status='executed'` (not `state='executed'`) for the cue-version cases.
    - `just test-bucket identify` exits 0 in isolation; mypy/ruff clean (FileState import kept only if still used).
  </acceptance_criteria>
  <done>All three tracklists cue-version guards read applied(); fixtures migrated; identify bucket green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator click → POST /cue/{id}/generate → filesystem (write_cue_file on file.current_path) | operator-triggered CUE write, gated by applied() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-02 | Tampering/Integrity | CUE write targeting a stale/deleted path | mitigate | Gate on `proposals.status=='executed'` (coupled to `current_path`) not `execution_log` — the applied() predicate is the mitigation (RESEARCH §Central Tension). |
| T-85-05 | Elevation/Path traversal | write_cue_file on `Path(file.current_path)` | accept (untouched) | The apply-path containment guard is unchanged; reviving the CUE gate adds no new write surface (writer already wired). |
| T-85-SC | Tampering | package installs | N/A | Zero new packages. |
</threat_model>

<verification>
- `uv run ruff check .` / `uv run mypy .` clean for touched files.
- `just test-bucket review` and `just test-bucket identify` green in isolation.
- `grep -rn "FileState.EXECUTED" src/phaze/routers/cue.py src/phaze/routers/tracklists.py` returns nothing (guard sites).
</verification>

<success_criteria>
- cue.py eligible/gated readers + generate_cue guard read `applied()`; CUE generation admits applied files.
- All three tracklists cue-version guards read `applied()`.
- Fixtures migrated to `proposals.status='executed'`; review + identify buckets green.
</success_criteria>

<output>
Create `.planning/phases/85-executed-gate-revival/85-03-SUMMARY.md` when done.
</output>
