---
phase: 10-ci-config-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/models/file.py
  - tests/test_models.py
autonomous: true
requirements:
  - INF-03
gap_closure: true
must_haves:
  truths:
    - "FileRecord.batch_id ORM column declares ForeignKey('scan_batches.id') matching the DB-level constraint from migration 002"
    - "pre-commit run --all-files passes with zero failures"
    - "All existing tests continue to pass"
  artifacts:
    - path: "src/phaze/models/file.py"
      provides: "FileRecord model with ForeignKey on batch_id"
      contains: "ForeignKey"
    - path: "tests/test_models.py"
      provides: "Test asserting batch_id has foreign key"
      contains: "foreign_keys"
  key_links:
    - from: "src/phaze/models/file.py"
      to: "src/phaze/models/scan_batch.py"
      via: "ForeignKey('scan_batches.id') on batch_id column"
      pattern: "ForeignKey.*scan_batches\\.id"
---

<objective>
Add the missing ForeignKey annotation to FileRecord.batch_id so the ORM model matches the database constraint created by migration 002.

Purpose: The DB-level FK from files.batch_id to scan_batches.id exists (migration 002 creates it via op.create_foreign_key), but the SQLAlchemy ORM model lacks the corresponding ForeignKey() declaration. This means SQLAlchemy relationship() lookups and autogenerated migrations won't see this constraint. Fixing this closes the last remaining tech debt item for Phase 10.

Output: Updated FileRecord model and test confirming the FK exists.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From src/phaze/models/file.py:
```python
class FileRecord(TimestampMixin, Base):
    __tablename__ = "files"
    id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    batch_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), nullable=True)
    # ... other columns
```

From src/phaze/models/scan_batch.py:
```python
class ScanBatch(TimestampMixin, Base):
    __tablename__ = "scan_batches"
```

From alembic/versions/002_add_scan_batches_and_unique_path.py:
```python
op.create_foreign_key("fk_files_batch_id_scan_batches", "files", "scan_batches", ["batch_id"], ["id"])
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ForeignKey to FileRecord.batch_id and update test</name>
  <files>src/phaze/models/file.py, tests/test_models.py</files>
  <read_first>
    - src/phaze/models/file.py
    - src/phaze/models/scan_batch.py
    - tests/test_models.py
    - alembic/versions/002_add_scan_batches_and_unique_path.py
  </read_first>
  <action>
1. In src/phaze/models/file.py:
   - Add `ForeignKey` to the import from sqlalchemy: `from sqlalchemy import BigInteger, ForeignKey, Index, String, Text`
   - Change line 40 from:
     `batch_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), nullable=True)`
     to:
     `batch_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("scan_batches.id"), nullable=True)`

2. In tests/test_models.py, update the `test_file_record_has_batch_id` test function to also assert the foreign key exists:
   - After the existing `assert col.nullable is True` line, add:
     `assert len(col.foreign_keys) == 1`
     `fk = next(iter(col.foreign_keys))`
     `assert fk.target_fullname == "scan_batches.id"`

3. Run `uv run pytest tests/test_models.py -x -q` to confirm the updated test passes.
4. Run `uv run pre-commit run --all-files` to confirm nothing breaks.
  </action>
  <verify>
    <automated>uv run pytest tests/test_models.py::test_file_record_has_batch_id -x -v && uv run pre-commit run ruff --all-files && uv run pre-commit run mypy --all-files</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'ForeignKey("scan_batches.id")' src/phaze/models/file.py returns 0
    - grep -q 'foreign_keys' tests/test_models.py returns 0
    - uv run pytest tests/test_models.py::test_file_record_has_batch_id passes
    - uv run pre-commit run --all-files exits 0
  </acceptance_criteria>
  <done>
    - FileRecord.batch_id declares ForeignKey("scan_batches.id") in the ORM model
    - Test asserts the foreign key target is scan_batches.id
    - All pre-commit hooks pass
    - All existing tests continue to pass
  </done>
</task>

</tasks>

<verification>
1. `grep -q 'ForeignKey("scan_batches.id")' src/phaze/models/file.py` -- FK annotation present
2. `uv run pytest tests/test_models.py -x -q` -- model tests pass including FK assertion
3. `uv run pre-commit run --all-files` -- all hooks green
4. `uv run pytest -x -q` -- full test suite passes
</verification>

<success_criteria>
- FileRecord.batch_id has ForeignKey("scan_batches.id") in ORM model matching migration 002
- pre-commit run --all-files passes with zero failures
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-ci-config-bug-fixes/10-01-SUMMARY.md`
</output>
