---
phase: 61-full-record-k-agents
plan: 02
type: execute
wave: 2
depends_on: [61-01]
files_modified:
  - src/phaze/routers/record.py
  - src/phaze/main.py
  - src/phaze/templates/shell/shell.html
  - src/phaze/templates/shell/partials/record_host.html
  - src/phaze/templates/record/record_body.html
  - src/phaze/templates/record/record_not_found.html
  - src/phaze/templates/pipeline/partials/_file_table.html
  - src/phaze/templates/pipeline/partials/analyze_workspace.html
autonomous: true
requirements: [RECORD-01]
must_haves:
  truths:
    - "D-01: opening a file (row or ⌘K) shows a wide right-anchored slide-in FRAGMENT over the shell — no extends base.html — served by GET /record/{file_id}"
    - "RECORD-01: the record shows header/facts, windowed multi-lane timeline, metadata diff, identity, this file's pending approvals (inline-approvable), and history — all read-only, all scoped by file_id"
    - "D-02: the record body is a SNAPSHOT (renders once, no self-refresh, no new poll); it never re-renders the operator's in-progress approval subtree"
    - "The record body's _diff_row.html Alpine islands re-init via Alpine.initTree after the HTMX body swap (RESEARCH Pitfall 3)"
    - "A missing/de-duplicated file_id returns a 404 friendly fragment (not 500), with the close/focus-return contract intact"
  artifacts:
    - path: "src/phaze/routers/record.py"
      provides: "GET /record/{file_id} read-only fragment route, typed UUID, file_id-scoped reads"
      exports: ["router"]
    - path: "src/phaze/templates/shell/partials/record_host.html"
      provides: "persistent chrome host with x-trap.inert.noscroll + record-body swap target"
      contains: "x-trap.inert.noscroll"
    - path: "src/phaze/templates/record/record_body.html"
      provides: "composed record body (timeline + diff + identity + approvals + history)"
      contains: "analysis_timeline.html"
  key_links:
    - from: "src/phaze/templates/pipeline/partials/_file_table.html"
      to: "/record/{file_id}"
      via: "hx-get + $dispatch('record:open')"
      pattern: "record:open"
    - from: "src/phaze/templates/shell/partials/record_host.html"
      to: "Alpine.initTree"
      via: "hx-on::after-swap on #record-body"
      pattern: "Alpine.initTree"
    - from: "src/phaze/routers/record.py"
      to: "AnalysisWindow.file_id == file_id"
      via: "file_id-scoped read (T-31-06-02 mitigation)"
      pattern: "file_id"
---

<objective>
Build the per-file full-record slide-in (RECORD-01): a wide, right-anchored HTMX fragment over the shell, composing existing read-only per-file reads (windowed timeline, metadata diff, identity, this file's pending approvals, history) into a persistent chrome host with an `x-trap` focus-trap.

Purpose: RECORD-01 + D-01/D-02. This is the phase's heaviest surface but is pure composition — the timeline partial, the diff row, and the Phase 60 approve/edit/undo routes already exist. The plan's job is the read-only assembly route, the persistent host that survives rail swaps, and the row/⌘K entry point.
Output: `GET /record/{file_id}` fragment route, `record_host.html` chrome host, composed `record_body.html`, a 404 fragment, and the row-open trigger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-full-record-k-agents/61-CONTEXT.md
@.planning/phases/61-full-record-k-agents/61-RESEARCH.md
@.planning/phases/61-full-record-k-agents/61-PATTERNS.md
@.planning/phases/61-full-record-k-agents/61-UI-SPEC.md

<interfaces>
<!-- Verified against the working tree 2026-07-01. Executor uses these directly. -->

Route analog — proposals.py:259 proposal_timeline (GET /{id}/timeline):
  proposal_id: uuid.UUID path param; resolves file_id; then:
  stmt = select(AnalysisWindow).where(AnalysisWindow.file_id == file_id).order_by(AnalysisWindow.tier, AnalysisWindow.window_index)
  fine = [w for w in windows if w.tier == "fine"]; coarse = [w for w in windows if w.tier == "coarse"]
  analysis = (await session.execute(select(AnalysisResult).where(AnalysisResult.file_id == file_id))).scalar_one_or_none()
  renders proposals/partials/analysis_timeline.html  (the ready-made multi-lane SVG + sampled badge + deepen button)

Fragment fork (shell.py:236-238, VERBATIM shape) — HX-Request → bare fragment (no extends base.html); oob_counts stays absent/False (Pitfall 5).

_diff_row.html context contract (pipeline/partials/_diff_row.html:7-19):
  row_id_prefix (id stem; row id = {row_id_prefix}-{pid}; every control hx-targets THIS id only, R-6),
  pid, file (display name), original_path (title), before/after,
  approve_url/skip_url/undo_url/edit_url, approve_method, edit_facet, row_state.
  Row carries x-data='{editing:false, val:{{ after|tojson }}}' → an Alpine island (needs initTree after swap).
  Every DB cell is autoescaped; JS-attribute contexts use |tojson NOT |e (T-60-XSS / SP-5).

Phase 60 approve/edit/undo routes (reuse verbatim):
  proposals.py:168/193/218 (PATCH approve/reject/undo), proposals.py /{id}/edit (PATCH),
  tags.py:309 (POST /tags/{id}/write), tags.py undo.
  Pending-proposal rows helper (Phase 60): get_pending_proposal_rows — filter to this file_id.

Identity: proposals.py:243 row_detail + get_proposal_with_file → proposals/partials/row_detail.html.
History (Discretion — pick cleanest read-only): ExecutionLog (models/execution.py) + TagWriteLog (models/tag_write_log.py) scoped by file_id, OR /audit/ (execution.py:350) scoped to the file.

record_host precedent — shell/partials/cmdk_modal.html: persistent {% include %} at shell.html:172,
  OUTSIDE #stage-workspace; x-data open/close + $nextTick focus + @keydown.escape returning focus to opener.

shell.html:236-247 htmx:afterSwap re-inits Alpine ONLY for #stage-workspace (id === 'stage-workspace') —
  the record host is NOT covered, hence the per-host hx-on::after-swap initTree (Pitfall 3).

Row entry point — _file_table.html:41 the <tr> already carries cursor-pointer + a stable id
  ({{ row_id_prefix }}-{{ loop.index }}) but NO click binding ("row→record is Phase 61", :98 of
  analyze_workspace.html). rows are list[list[cell dict]]; there is NO file_id passed per row yet.

Register routers in src/phaze/main.py near :199 (app.include_router(shell.router)).
</interfaces>

@src/phaze/routers/proposals.py
@src/phaze/templates/shell/partials/cmdk_modal.html
@src/phaze/templates/pipeline/partials/_diff_row.html
@src/phaze/templates/pipeline/partials/_file_table.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /record/{file_id} read-only fragment route + 404 fragment + registration</name>
  <files>src/phaze/routers/record.py, src/phaze/main.py, src/phaze/templates/record/record_not_found.html</files>
  <read_first>
    - src/phaze/routers/proposals.py:243-308 (`row_detail`, `proposal_timeline`, `get_proposal_with_file` — the file_id-scoping + timeline-assembly analogs, SP-2)
    - src/phaze/routers/shell.py:122-238 (the fragment-vs-full fork; `oob_counts=False`; template-response shape)
    - src/phaze/main.py:186-233 (router registration block)
    - 61-PATTERNS.md §`record.py` (imports pattern + compose-these-reads list) and SP-1/SP-2
    - 61-UI-SPEC.md §Surface 1 (sections list) + §Copywriting Contract (the 404 copy)
  </read_first>
  <behavior>
    - test_record_fragment_bare_and_scoped: GET /record/{file_id} with HX-Request returns 200, no `<html>`/`<head>`, and content assembled strictly from that file_id's reads.
    - test_record_missing_file_404_fragment: GET /record/{unknown-uuid} returns 404 with the friendly fragment copy, no stack trace.
    - test_record_pending_approvals_wired: the response carries `_diff_row.html` approval rows whose approve/edit/undo URLs are the Phase 60 routes.
  </behavior>
  <action>
    Create `src/phaze/routers/record.py` with `router = APIRouter(prefix="/record", tags=["record"])` and `GET /{file_id}` taking a typed `file_id: uuid.UUID` path param (FastAPI-validated — closes the template-path/BAC surface, T-61-03). Resolve the `FileRecord` by id; if absent/de-duplicated (None) raise/return 404 rendering `record/record_not_found.html` with the UI-SPEC copy ("That file no longer exists. …"). Otherwise compose these read-only, all scoped by `file_id`, mirroring `proposal_timeline` (SP-2): AnalysisWindow rows (ordered tier+window_index, split fine/coarse) + the 1:1 AnalysisResult (`scalar_one_or_none`); the file's pending RenameProposal + tag comparison built into `_diff_row.html` context via the Phase 60 `get_pending_proposal_rows` helper filtered to this file_id (row_id_prefix="record-row", approve/skip/undo/edit URLs = the verified Phase 60 routes, approve_method per facet); identity via `get_proposal_with_file`; history from `ExecutionLog` + `TagWriteLog` scoped to file_id (Discretion — whichever composes cleanest read-only). Render `record/record_body.html` for HX requests (bare fragment, `oob_counts=False` — Pitfall 5); no logic changes anywhere. Register `record.router` in `src/phaze/main.py` next to `shell.router`.
  </action>
  <verify>
    <automated>uv run pytest "tests/test_record_palette_agents.py::test_record_fragment_bare_and_scoped" "tests/test_record_palette_agents.py::test_record_missing_file_404_fragment" "tests/test_record_palette_agents.py::test_record_pending_approvals_wired" -x</automated>
  </verify>
  <acceptance_criteria>
    - `GET /record/{file_id}` exists with a typed `uuid.UUID` path param and is registered in `main.py`.
    - A valid file_id returns a bare fragment (`"<html" not in r.text` and `"<head" not in r.text`).
    - An unknown UUID returns HTTP 404 rendering `record_not_found.html` (friendly copy, no 500/stack trace).
    - Every AnalysisWindow/AnalysisResult/proposal/history read is filtered by `file_id` (no cross-file query).
    - Pending-approval rows carry the Phase 60 approve/edit/undo route URLs.
    - The three named tests pass.
  </acceptance_criteria>
  <done>The read-only record route composes the file's timeline/diff/identity/approvals/history into a bare fragment, 404s cleanly on a missing file, and is registered.</done>
</task>

<task type="auto">
  <name>Task 2: record_host.html persistent chrome + shell wiring + composed record_body.html</name>
  <files>src/phaze/templates/shell/partials/record_host.html, src/phaze/templates/shell/shell.html, src/phaze/templates/record/record_body.html, src/phaze/templates/record/record_not_found.html</files>
  <read_first>
    - src/phaze/templates/shell/partials/cmdk_modal.html (the persistent-host open/close/$nextTick-focus/Esc-return precedent)
    - src/phaze/templates/shell/shell.html:160-247 (the #stage-workspace region, the :172 cmdk include, and the htmx:afterSwap/historyRestore re-init handlers)
    - src/phaze/templates/proposals/partials/analysis_timeline.html + src/phaze/templates/pipeline/partials/_diff_row.html (the two partials the body reuses verbatim)
    - src/phaze/templates/proposals/partials/row_detail.html (identity section reuse)
    - 61-PATTERNS.md §record_host.html + §"Record body sections" table (geometry + reuse map)
    - 61-UI-SPEC.md §Surface 1 (sections, panel geometry w-[760px] inset-y-4 right-4, sticky header, facts grid, amber pending box) + §Interaction/Focus/ARIA Baseline (dialog role, aria-label via |tojson, x-trap, focus to h2, Esc→opener)
  </read_first>
  <action>
    Create `src/phaze/templates/shell/partials/record_host.html` — a NEW persistent `{% include %}` sibling of `cmdk_modal.html`, carrying `x-data` (open/opener/show/hide, focus-return to the opener with `#stage-workspace` fallback), `@record:open.window`, `@keydown.escape.window`, and on the panel `role="dialog" aria-modal="true"`, an `aria-label` bound via `|tojson`, `x-trap.inert.noscroll="open"`, backdrop `bg-black/60` (click closes), panel geometry `absolute inset-y-4 right-4 w-[760px] max-w-[94vw] bg-phaze border rounded-2xl shadow-2xl overflow-y-auto`, and an inner `#record-body` container with `hx-on::after-swap="if(window.Alpine) Alpine.initTree(this)"` (Pitfall 3 — the `_diff_row.html` islands). Include it in `shell.html` next to the cmdk include; move focus to the record `<h2>` (`tabindex="-1"`) on open. Do NOT use the empty `<aside>` at shell.html:164 (D-01; its removal is Phase 62).
    Create `src/phaze/templates/record/record_body.html` composing the sections top→bottom per UI-SPEC §Surface 1: sticky header (Jura `<h2>` title uppercase tracking-[0.15em] + mono path + `✕` `aria-label="Close record"`); facts grid (`grid-cols-4 gap-3` Format/Duration/sha256/Lane badge); windowed timeline card via `{% include "proposals/partials/analysis_timeline.html" %}`; a `grid-cols-2` metadata-diff (left, `_diff_row.html` rows) + identity (right, `row_detail.html`); the amber "Pending approvals for this file" box (inline `_diff_row.html` rows, or the "up to date" empty copy); and a History section (mono per-file event list). Also create `src/phaze/templates/record/record_not_found.html` (the 404 friendly fragment). Every DB-sourced cell autoescaped; JS-attribute contexts use `|tojson` NOT `|e` (SP-5 / T-61-01). NO `hx-trigger="every"`, `setInterval`, or `hx-swap-oob` on the approval subtree (D-02 / SP-6).
  </action>
  <verify>
    <automated>uv run pytest "tests/test_record_palette_agents.py::test_new_fragments_single_poll_clean" tests/test_base_html_sri.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `record_host.html` exists, is included in `shell.html` as a sibling of `cmdk_modal.html`, and carries `x-trap.inert.noscroll="open"` on the panel + `role="dialog"` + `aria-modal="true"`.
    - `#record-body` carries `hx-on::after-swap` calling `Alpine.initTree`.
    - `record_body.html` includes `proposals/partials/analysis_timeline.html` and renders the sticky header, facts grid, metadata diff + identity grid, amber pending-approvals box, and history.
    - No `hx-trigger="every"`/`setInterval`/`hx-swap-oob` appears on the record's approval-row subtree; `test_new_fragments_single_poll_clean` passes.
    - Alpine `aria-label`/`x-data` contexts use `|tojson` (grep confirms; no `| e` in a JS-attribute context).
  </acceptance_criteria>
  <done>The persistent record host traps focus and re-inits its Alpine islands after swap; the composed body renders all seven sections from reused partials with XSS-safe escaping.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the row → record open trigger (file rows + ⌘K contract)</name>
  <files>src/phaze/templates/pipeline/partials/_file_table.html, src/phaze/templates/pipeline/partials/analyze_workspace.html</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/_file_table.html:38-51 (the `<tr>` with the stable id + cursor-pointer, currently click-unbound — the D-06/R-1 handoff site)
    - src/phaze/templates/pipeline/partials/analyze_workspace.html:98-140 (where `ns.rows` + `row_id_prefix="analyze-row"` are built and `_file_table.html` is fed; the ":98 row→record is Phase 61" comment)
    - 61-PATTERNS.md §record_host.html (the `hx-get="/record/{file_id}"` opener snippet) + §"Record body sections"
    - 61-UI-SPEC.md §Copywriting Contract ("OPEN FULL RECORD →" / "FULL ↗")
  </read_first>
  <action>
    Extend `_file_table.html` to accept an OPTIONAL parallel `row_file_ids` list (one file_id per row). When present, add to each `<tr>` (or a dedicated per-row affordance): `hx-get="/record/{{ row_file_ids[loop.index0] }}"` `hx-target="#record-body"` `hx-swap="innerHTML"` and `@click="$dispatch('record:open', {el: $el})"` (the host's `x-data` records the opener for focus-return). Keep the click-unbound behavior intact when `row_file_ids` is absent (backward compatible for other workspaces). In `analyze_workspace.html`, pass `row_file_ids` (the file ids backing `ns.rows`) into the `_file_table.html` include so Analyze rows open the record. The ⌘K Files-group rows (Plan 03) reuse the SAME `hx-get="/record/{file_id}"` contract — do not duplicate logic. XSS: file_id is a UUID (safe); still autoescape.
  </action>
  <verify>
    <automated>uv run pytest tests/test_record_palette_agents.py tests/test_shell_routes.py -x -k "record or shell"</automated>
  </verify>
  <acceptance_criteria>
    - `_file_table.html` emits `hx-get="/record/{file_id}"` + `$dispatch('record:open', …)` on rows when `row_file_ids` is provided, and stays click-unbound otherwise.
    - `analyze_workspace.html` passes `row_file_ids` to the `_file_table.html` include.
    - The record open contract (`hx-target="#record-body"`, `hx-swap="innerHTML"`) matches the host's swap target.
    - `uv run pytest tests/test_shell_routes.py -x` stays green (no regression to existing workspace rows).
  </acceptance_criteria>
  <done>Analyze file rows open the record slide-in via the shared table partial; the ⌘K Files group reuses the same route contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /record/{file_id} | An arbitrary/guessed file_id crosses into a DB read |
| DB → rendered HTML | filenames, paths, tags, artist names flow DB→record body |
| HTMX-swapped fragment → Alpine | swapped body carries un-initialized `x-data` islands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-03 | Elevation of Privilege | `GET /record/{file_id}` | mitigate | typed `uuid.UUID` path param (FastAPI-validated) + every read scoped strictly by `file_id` (mirror proposals.py:257 T-31-06-02); missing file → 404 friendly fragment, never a template-path build |
| T-61-01 | Tampering | record header/facts/history + diff cells | mitigate | Jinja2 autoescape on every DB cell; `|tojson` (NOT `|e`) in Alpine JS-attribute contexts (Phase 60 apostrophe-filename XSS class); `_diff_row.html` already correct — keep it |
| T-61-05 | Information Disclosure | 404 path | mitigate | friendly fragment copy, no stack trace / no filesystem detail on a missing/de-duplicated file |
</threat_model>

<verification>
- `uv run pytest tests/test_record_palette_agents.py -k "record" -x` passes the three RECORD-01 tests + the fragment guard.
- `uv run pytest tests/test_base_html_sri.py tests/test_shell_routes.py -x` stays green.
- `uv run pytest --cov --cov-report=term-missing` meets the 85% floor; pre-commit + mypy strict pass (never `--no-verify`).
- Manual (per 61-VALIDATION §Manual-Only): open a record, Tab stays inside the panel; Esc/✕/backdrop returns focus to the opening row.
</verification>

<success_criteria>
Opening a file (row or ⌘K) shows a bare, focus-trapped, right-anchored slide-in composing the file's identity, metadata diff, windowed multi-lane timeline, inline-approvable pending approvals, and history — a snapshot that rides only the existing poll and 404s cleanly on a missing file.
</success_criteria>

<output>
Create `.planning/phases/61-full-record-k-agents/61-02-SUMMARY.md` when done.
</output>
