---
phase: 61-full-record-k-agents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/templates/shell/shell.html
  - src/phaze/templates/base.html
  - tests/test_base_html_sri.py
  - tests/conftest.py
  - tests/test_record_palette_agents.py
autonomous: true
requirements: [RECORD-01, RECORD-02, RECORD-03, RECORD-04]
must_haves:
  truths:
    - "@alpinejs/focus@3.15.12 loads <script defer> BEFORE Alpine core in BOTH shell.html and base.html (the x-trap dep for the D-01 slide-in AND the D-04 palette)"
    - "tests/test_base_html_sri.py guards shell.html too — a stale/missing focus-plugin hash fails the test where the shell actually runs the traps (RESEARCH Pitfall 1)"
    - "tests/test_record_palette_agents.py exists and collects, encoding the RECORD-01..04 + fragment-guard behavior contract (Wave 0 scaffold)"
    - "conftest factories seed AnalysisResult+AnalysisWindow, distinct-artist FileMetadata/Tracklist, CloudJob running/submitted+inadmissible/none, and empty-DB — the fixtures Plans 02-05 verify against"
  artifacts:
    - path: "tests/test_record_palette_agents.py"
      provides: "RECORD-01..04 route+template assertions (RED until surfaces land)"
      contains: "def test_record_fragment_bare_and_scoped"
    - path: "tests/test_base_html_sri.py"
      provides: "SRI gate extended to scan shell.html"
      contains: "shell.html"
  key_links:
    - from: "src/phaze/templates/shell/shell.html"
      to: "cdn.jsdelivr.net/npm/@alpinejs/focus@3.15.12"
      via: "<script defer> before Alpine core, SRI SHA-384 pinned"
      pattern: "@alpinejs/focus@3.15.12"
    - from: "tests/test_base_html_sri.py"
      to: "src/phaze/templates/shell/shell.html"
      via: "_extract_cdn_scripts parametrized over both templates"
      pattern: "shell.html"
---

<objective>
Lay the load-bearing foundation for Phase 61: add the one new CDN dep (`@alpinejs/focus@3.15.12` — the `x-trap` focus-trap plugin both overlays need), guard its integrity where the shell actually runs it, and stand up the Wave-0 test scaffold + fixtures that Plans 02-05 turn green.

Purpose: The focus-plugin dep GATES the record slide-in (D-01) and the ⌘K palette (D-04) focus-traps — both fail open if the plugin is missing or its SRI drifts. RESEARCH Pitfall 1: the current SRI test scans `base.html` only, but the v7.0 shell runs on `shell.html` (a standalone full page with its OWN `<head>` block). This plan closes that gap and creates the test file + fixtures the four surface plans depend on.
Output: focus plugin in both head blocks, an extended SRI test, `tests/test_record_palette_agents.py` (RED scaffold), and new conftest factories.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-full-record-k-agents/61-RESEARCH.md
@.planning/phases/61-full-record-k-agents/61-VALIDATION.md
@.planning/phases/61-full-record-k-agents/61-UI-SPEC.md

<interfaces>
<!-- Verified against the working tree 2026-07-01. -->

shell.html + base.html head block (identical, :32-39) currently loads, in order:
  htmx.org@2.0.10 → htmx-ext-sse@2.2.4 → alpinejs@3.15.12 (<script defer>, jsdelivr).
The focus plugin MUST be inserted as <script defer> IMMEDIATELY BEFORE the Alpine core line.

Computed SRI (RESEARCH §CDN Audit, verified this session, 26,051 bytes, Accept-Encoding: identity):
  integrity="sha384-ysJcnHb6oCzqAGKdoTm+IqKqmPKgxHT+ApZCawkyWOJfMq15WvzW3RRmHl7tWpEY"
  src="https://cdn.jsdelivr.net/npm/@alpinejs/focus@3.15.12/dist/cdn.min.js"
  crossorigin="anonymous"
Recompute recipe if it drifts: curl -fsSL -H "Accept-Encoding: identity" <url> | openssl dgst -sha384 -binary | openssl base64 -A

tests/test_base_html_sri.py:
  _BASE_HTML = parents[1] / "src/phaze/templates/base.html"  (:44 — reads ONLY base.html)
  _SCRIPT_TAG regex + _extract_cdn_scripts() (:57) → list[(src, integrity)]
  test_every_cdn_script_pins_a_specific_version() (:69 — rejects @<major>/@<major>.<minor>)
  network SRI check is @pytest.mark.integration (self-skips offline)

tests/conftest.py factories (async fixtures):
  make_file(*, state, original_filename, file_type, sha256) -> FileRecord  (:351)
  seed_pending_proposal(confidence, *, proposed_filename, proposed_path, original_filename) -> RenameProposal  (:381)
  seed_executed_file_with_metadata (:413 — already sets FileMetadata.artist)
  client / session async fixtures (:158-172)

Models for fixtures:
  AnalysisResult (models/analysis.py:13): file_id, bpm, musical_key, mood, style,
    fine_windows_analyzed/total, coarse_windows_analyzed/total, sampled, analysis_completed_at
  AnalysisWindow (models/analysis.py:41): file_id (indexed), tier ("fine"/"coarse"), window_index, bpm, musical_key
  FileMetadata.artist (models/metadata.py:19, Text nullable, UNINDEXED)
  Tracklist.artist (models/tracklist.py:35)
  CloudJob (models/cloud_job.py:65): file_id (unique FK), s3_key, status, inadmissible (bool)
  CloudJobStatus (StrEnum): RUNNING="running", SUBMITTED="submitted", SUCCEEDED="succeeded", ...
</interfaces>

@src/phaze/templates/shell/shell.html
@tests/test_base_html_sri.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @alpinejs/focus to both head blocks + extend the SRI test to shell.html</name>
  <files>src/phaze/templates/shell/shell.html, src/phaze/templates/base.html, tests/test_base_html_sri.py</files>
  <read_first>
    - src/phaze/templates/shell/shell.html:30-42 (the head script block being modified)
    - src/phaze/templates/base.html:30-42 (identical block — the second insertion site)
    - tests/test_base_html_sri.py:44-90 (`_BASE_HTML`, `_extract_cdn_scripts`, `test_every_cdn_script_pins_a_specific_version` — the analog being parametrized)
    - 61-RESEARCH.md §Package Legitimacy / CDN Audit (the SRI hash + Pitfall 1) and §Verified References "The one new dep + the SRI gate"
    - 61-UI-SPEC.md §Registry Safety + §Interaction/Focus/ARIA Baseline (load-order contract)
  </read_first>
  <action>
    Insert the `@alpinejs/focus@3.15.12` `<script defer>` tag IMMEDIATELY BEFORE the `alpinejs@3.15.12` core line in BOTH `src/phaze/templates/shell/shell.html` AND `src/phaze/templates/base.html`. Use the exact src `https://cdn.jsdelivr.net/npm/@alpinejs/focus@3.15.12/dist/cdn.min.js`, `integrity="sha384-ysJcnHb6oCzqAGKdoTm+IqKqmPKgxHT+ApZCawkyWOJfMq15WvzW3RRmHl7tWpEY"`, `crossorigin="anonymous"`. Version MUST equal Alpine core (3.15.12) — plugins register on core init, so load order is load-bearing.
    Then extend `tests/test_base_html_sri.py`: generalize `_extract_cdn_scripts` to read a passed template path (add a `_SHELL_HTML` Path constant alongside `_BASE_HTML`), and parametrize `test_every_cdn_script_pins_a_specific_version` (and the network SRI check) over BOTH templates via `@pytest.mark.parametrize`. Keep the existing base.html assertions green. This closes RESEARCH Pitfall 1 (a stale/missing focus hash in shell.html was previously invisible to the test). Mitigates T-61-02 (supply-chain).
  </action>
  <verify>
    <automated>uv run pytest tests/test_base_html_sri.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `@alpinejs/focus@3.15.12` `<script defer>` appears in BOTH shell.html and base.html, positioned before the `alpinejs@3.15.12` core `<script>` line.
    - The integrity attribute is exactly `sha384-ysJcnHb6oCzqAGKdoTm+IqKqmPKgxHT+ApZCawkyWOJfMq15WvzW3RRmHl7tWpEY`.
    - `tests/test_base_html_sri.py` references `shell.html` (grep `shell.html` in the file returns a match) and parametrizes its version-pin assertion over both templates.
    - `uv run pytest tests/test_base_html_sri.py -x` exits 0.
  </acceptance_criteria>
  <done>Both head blocks load the focus plugin (full-semver, before Alpine core, SRI-pinned); the SRI test guards shell.html where the traps run.</done>
</task>

<task type="auto">
  <name>Task 2: Add conftest fixtures for the record/palette/agents/empty-state surfaces</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py:350-470 (`make_file`, `seed_pending_proposal`, `seed_executed_file_with_metadata` — the async-factory pattern to extend; `make_file` is the base every higher factory builds on)
    - src/phaze/models/analysis.py:13,41 (AnalysisResult, AnalysisWindow column sets)
    - src/phaze/models/cloud_job.py:30-95 (CloudJob + CloudJobStatus members)
    - src/phaze/models/metadata.py:19 + src/phaze/models/tracklist.py:35 (artist columns)
    - 61-VALIDATION.md §Wave 0 Requirements (the exact fixture list) + 61-PATTERNS.md "Fixture extensions"
  </read_first>
  <action>
    Add async-factory fixtures to `tests/conftest.py` mirroring the `make_file`/`seed_pending_proposal` pattern (build on `make_file`; `session.add` → `commit` → `refresh`):
    (a) `seed_file_with_windows` — a FileRecord + one `AnalysisResult` (bpm/musical_key set, `sampled=True`, fine/coarse window counts) + several `AnalysisWindow` rows across `tier="fine"` and `tier="coarse"` with distinct `window_index`/`bpm`/`musical_key` (for the record timeline, RECORD-01).
    (b) `seed_distinct_artists` — several `FileMetadata` and `Tracklist` rows with distinct `artist` values (including at least one shared name across both tables, one None) for `distinct_artists` (RECORD-02, D-05).
    (c) `seed_cloud_jobs` — a factory that seeds `CloudJob` rows in a chosen mix of `status=running` / `status=submitted`+`inadmissible=True` / none, for `classify_compute_lanes` (RECORD-03, D-07). Each CloudJob needs a distinct `file_id` (unique FK) via `make_file`.
    Keep all fixtures read-model only — no logic changes. The empty-DB case (file_count==0, RECORD-04) needs no factory (it is the default clean `session`).
  </action>
  <verify>
    <automated>uv run pytest tests/conftest.py --collect-only -q 2>&1 | tail -5; uv run python -c "import tests.conftest"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` defines `seed_file_with_windows`, `seed_distinct_artists`, and `seed_cloud_jobs` async-factory fixtures following the existing `make_file` shape.
    - `seed_cloud_jobs` can produce running / submitted+inadmissible / none CloudJob states.
    - `uv run python -c "import tests.conftest"` exits 0 (no import/syntax error).
  </acceptance_criteria>
  <done>Fixtures for windows, distinct artists, and CloudJob liveness states exist and import cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Create the tests/test_record_palette_agents.py Wave-0 scaffold (RED)</name>
  <files>tests/test_record_palette_agents.py</files>
  <read_first>
    - tests/test_shell_routes.py:67-79 (the bare-fragment assertion analog: `"<html" not in r.text`)
    - 61-VALIDATION.md §Per-Task Verification Map (the 11 named tests + their exact behavior + Automated Command column)
    - 61-RESEARCH.md §Validation Architecture "Phase Requirements → Test Map" and the "Fragment-correctness test" + "Distinct-artist read" + "Compute-lane liveness" code examples
  </read_first>
  <action>
    Create `tests/test_record_palette_agents.py` with the full set of async tests named in 61-VALIDATION.md (all `@pytest.mark.asyncio`, using the async `client`/`session` fixtures + the Task-2 factories):
    `test_record_fragment_bare_and_scoped`, `test_record_missing_file_404_fragment`, `test_record_pending_approvals_wired`, `test_cmdk_grouped_results`, `test_distinct_artists_query`, `test_cmdk_commands_and_artist_nav`, `test_agents_two_sections_never_dead`, `test_compute_lane_liveness_states`, `test_empty_state_agent_roots_scan`, `test_empty_state_suppressed_when_files_exist`, `test_new_fragments_single_poll_clean`.
    Encode the real behavior contract as the assertions (bare fragment has no `<html>`/`<head>`; record reads scoped by `file_id`; missing file → 404 friendly fragment not 500; grouped results carry `role="option"` rows + `role="presentation"` group headers; `distinct_artists` returns DISTINCT non-None artists LIMIT-bounded; Scan command posts `/pipeline/scan-live-sets`; artist Enter navigates with `artist=`; Agents renders two sections and NEVER a DEAD/rose compute state; `classify_compute_lanes` → ACTIVE/WAITING/IDLE + IDLE-on-error; empty state posts `POST /pipeline/scans` with agent_id+scan_root and shows no free-text path input; count>0 suppresses it; new fragments contain no `hx-trigger="every"`/`setInterval`/`hx-swap-oob` on approval subtrees). These are RED now (routes/queries land in Plans 02-05); this task's gate is that the file COLLECTS.
    **`test_new_fragments_single_poll_clean` MUST be skip-on-404 resilient** — it is a cross-cutting test spanning all THREE Phase-61 surfaces (record body via `GET /record/{file_id}`, ⌘K grouped palette results, first-run empty-state), and it is referenced in the per-task `<automated>` verify of 61-02 T2, 61-03 T2, AND 61-05 T2. Because Wave-2 executes sequentially, 61-02/61-03 run BEFORE 61-05 lands the empty-state surface, so the test must NOT assert HTTP 200 across all three. Instead, for EACH of the three surfaces: fetch its route and, if it returns 404 (surface not yet implemented), SKIP/xfail (or treat-as-clean) that surface; assert the "single-poll clean" properties — bare fragment (no `<html>`/`<head>`), no `hx-trigger="every"`, no `setInterval`, and no `hx-swap-oob` on an approval-row subtree — ONLY against the surfaces currently returning 200. This makes each Wave-2 plan's per-task verify pass cleanly on its own just-built surface without spurious executor retries, while the full three-surface assertion still holds once 61-05 lands and in the post-wave full-suite run.
    Do NOT weaken assertions to pass early — Plans 02-05 must satisfy them as written (the skip-on-404 structure above is the ONLY permitted resilience; do not stub or `assert True`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_record_palette_agents.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_record_palette_agents.py` defines all 11 named test functions from 61-VALIDATION.md.
    - `uv run pytest tests/test_record_palette_agents.py --collect-only -q` lists 11 tests and exits 0 (import/collection clean).
    - Assertions encode bare-fragment, file_id-scoping, 404-fragment, role="option"/"presentation", DISTINCT-artist, scan-endpoint, and never-DEAD contracts (not placeholder `assert True`).
    - `test_new_fragments_single_poll_clean` skips/xfails (treats-as-clean) any of the three surfaces returning 404 and asserts the fragment-clean properties (no `<html>`/`<head>`, no `hx-trigger="every"`/`setInterval`, no `hx-swap-oob` on approval subtrees) ONLY on routes returning 200 — so 61-02/61-03/61-05 per-task verifies each pass without spurious retries.
  </acceptance_criteria>
  <done>The Wave-0 test file collects with all 11 behavior tests present (RED), ready for Plans 02-05 to turn green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jsdelivr CDN → browser | The focus plugin is third-party script crossing into the shell's execution context |
| Test template files → CI gate | A drifted/missing SRI must fail loudly, not silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-02 | Tampering | `@alpinejs/focus` CDN script in shell.html + base.html | mitigate | SRI SHA-384 pin + full-semver URL + first-party publisher (same as Alpine core); test EXTENDED to guard shell.html (Pitfall 1) |
| T-61-SC | Tampering | package installs | accept | No npm/pip/cargo install occurs this phase; the only external asset is a CDN `<script>` guarded by SRI (the analog gate). No Package Legitimacy table required. |
</threat_model>

<verification>
- `uv run pytest tests/test_base_html_sri.py -x` passes (both templates guarded).
- `uv run pytest tests/test_record_palette_agents.py --collect-only -q` lists 11 tests.
- `uv run python -c "import tests.conftest"` exits 0.
- Pre-commit hooks + mypy strict pass (never `--no-verify`).
</verification>

<success_criteria>
The focus plugin is loaded and SRI-guarded in BOTH head blocks where the shell runs; the Wave-0 test file + fixtures exist and collect; downstream plans (02-05) can attach their `<automated>` verifies to concrete, already-written tests.
</success_criteria>

<output>
Create `.planning/phases/61-full-record-k-agents/61-01-SUMMARY.md` when done.
</output>
