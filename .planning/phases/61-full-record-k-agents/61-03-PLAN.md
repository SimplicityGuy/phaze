---
phase: 61-full-record-k-agents
plan: 03
type: execute
wave: 2
depends_on: [61-01]
files_modified:
  - src/phaze/services/search_queries.py
  - src/phaze/routers/search.py
  - src/phaze/templates/search/partials/palette_results.html
  - src/phaze/templates/shell/partials/cmdk_modal.html
autonomous: true
requirements: [RECORD-02]
must_haves:
  truths:
    - "D-03: ⌘K searches Files / Tracklists / Artists over the existing search_queries.search() and offers four quick commands (Scan / Jump to a stage / Jump to a review queue / Open Agents)"
    - "D-05: the Artists group is populated by a NEW read-only distinct_artists() SELECT DISTINCT over FileMetadata.artist / Tracklist.artist; Enter filters files via the existing artist= param — the ONE sanctioned additive backend touch"
    - "D-04: labeled groups with roving ↑/↓ across ALL groups, Enter activates the active item, Esc closes and returns focus to #cmdk-trigger; focus-trap via x-trap.inert.noscroll"
    - "The ⌘K Scan command posts the parameterless /pipeline/scan-live-sets (correct for ⌘K); palette rows are static role=option markup (no Alpine islands, no initTree needed)"
  artifacts:
    - path: "src/phaze/services/search_queries.py"
      provides: "distinct_artists() read-only facet (LIMIT-bounded, debounce-gated)"
      contains: "async def distinct_artists"
    - path: "src/phaze/templates/search/partials/palette_results.html"
      provides: "grouped Files/Tracklists/Artists/Commands fragment with ARIA listbox roles"
      contains: "role=\"option\""
  key_links:
    - from: "src/phaze/templates/shell/partials/cmdk_modal.html"
      to: "grouped palette results endpoint"
      via: "debounced x-on:input hx-get swapping the results body only"
      pattern: "hx-get"
    - from: "src/phaze/routers/search.py"
      to: "distinct_artists"
      via: "grouped branch over search() + distinct_artists()"
      pattern: "distinct_artists"
    - from: "src/phaze/templates/shell/partials/cmdk_modal.html"
      to: "#cmdk-trigger"
      via: "Esc returns focus (existing contract) + x-trap.inert.noscroll on the panel"
      pattern: "x-trap"
---

<objective>
Make the Phase 57 ⌘K skeleton functional (RECORD-02): grouped Files/Tracklists/Artists results over the existing search service plus a read-only distinct-artist facet (the one sanctioned additive query), four quick commands, full roving arrow-nav, ARIA listbox semantics, and an `x-trap` focus-trap.

Purpose: RECORD-02 + D-03/D-04/D-05. The search service, the `artist=` filter, the scan trigger, and the `/s/<stage>` nav all exist; the only new backend is `distinct_artists()`. Everything else is client-side roving state + a grouped fragment.
Output: `distinct_artists()`, a grouped palette results endpoint + template, and the wired `cmdk_modal.html`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-full-record-k-agents/61-CONTEXT.md
@.planning/phases/61-full-record-k-agents/61-RESEARCH.md
@.planning/phases/61-full-record-k-agents/61-PATTERNS.md
@.planning/phases/61-full-record-k-agents/61-UI-SPEC.md

<interfaces>
<!-- Verified against the working tree 2026-07-01. -->

search_queries.search() (services/search_queries.py:37):
  returns (list[SearchResult], pagination); SearchResult.result_type ∈ {file, tracklist, discogs_release};
  fields title/artist; supports artist= ilike filter + FTS concat_ws(original_filename, artist, title, genre).
  NO artist result_type — artist is a field. get_summary_counts (:167) is the count-read analog to mirror.

distinct_artists (NEW, D-05 — the one sanctioned additive read; mirror get_summary_counts shape):
  async def distinct_artists(session, query, *, limit=20) -> list[str]
  SELECT DISTINCT over FileMetadata.artist (models/metadata.py:19, Text nullable, UNINDEXED) +
  Tracklist.artist (models/tracklist.py:35), both ilike f"%{query}%", is_not(None), union_all, .distinct().limit(limit).
  Pitfall 4: UNINDEXED — caller debounces (≥150-250ms), LIMITs, gates on len(query) >= 2; defer the index (schema change out of scope).

search.py:39-84 existing HX fork: non-HX GET → RedirectResponse("/?palette=1"); HX GET renders
  search/partials/results_content.html (FLAT). artist= Query param (:23) → search(artist=…).
  ADD a grouped branch/endpoint that renders palette_results.html.

Commands (D-03):
  Scan → POST /pipeline/scan-live-sets (pipeline.py:1187, parameterless, enqueue_router) — CORRECT for ⌘K.
  Jump to a stage / review queue → HTMX nav to /s/<stage> (shell.py:247 whitelist; e.g. /s/rename, /s/dedupe).
  Open Agents → /admin/agents.

cmdk_modal.html (shell/partials/cmdk_modal.html — Phase 57 skeleton, persistent {% include %} at shell.html:172):
  owns open/close, ?palette=1 auto-open, $nextTick input focus (x-ref="input"),
  @keydown.escape → #cmdk-trigger. Body is a static placeholder; NO fetch/groups/roving-nav yet.
  Panel: absolute left-1/2 top-24 -translate-x-1/2 w-[640px] max-w-[92vw]; header h-14; results max-h-[50vh].
  @alpinejs/focus is loaded by Plan 61-01 (x-trap available).

ARIA (Research Pattern 2): input role="combobox" aria-expanded aria-controls aria-activedescendant;
  results role="listbox"; selectable rows role="option" :aria-selected; group headers role="presentation".
  ↑/↓ roving over the FLAT ordered list (skip headers); Enter activates items[activeIndex].
  Palette rows are STATIC option markup (no Alpine islands) → no initTree needed.

Record open contract (from Plan 61-02): a Files-group row opens the slide-in via
  hx-get="/record/{file_id}" hx-target="#record-body" hx-swap="innerHTML" @click="$dispatch('record:open', {el:$el})".
</interfaces>

@src/phaze/routers/search.py
@src/phaze/services/search_queries.py
@src/phaze/templates/shell/partials/cmdk_modal.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: distinct_artists() + grouped palette results endpoint + template</name>
  <files>src/phaze/services/search_queries.py, src/phaze/routers/search.py, src/phaze/templates/search/partials/palette_results.html</files>
  <read_first>
    - src/phaze/services/search_queries.py:37,75,167 (`search`, the `artist=` filter, `get_summary_counts` — the count-read shape to mirror)
    - src/phaze/routers/search.py:19-90 (the existing HX fork + `artist` Query param)
    - src/phaze/templates/search/partials/results_content.html (the flat-results analog)
    - src/phaze/models/metadata.py:19 + src/phaze/models/tracklist.py:35 (artist columns)
    - 61-PATTERNS.md §search.py + services/search_queries.py (the `distinct_artists` body + commands wiring) + 61-RESEARCH.md §Code Examples "Distinct-artist read"
    - 61-UI-SPEC.md §Surface 2 (grouped results table + row shapes + copy) + §Copywriting Contract (group headers, command labels, no-results copy)
  </read_first>
  <behavior>
    - test_distinct_artists_query: distinct_artists(session, q) returns DISTINCT `FileMetadata.artist`/`Tracklist.artist` matching q (case-insensitive), LIMIT-bounded, with no None values.
    - test_cmdk_grouped_results: the grouped endpoint returns Files/Tracklists/Artists/Commands groups; selectable rows carry role="option"; group headers carry role="presentation".
  </behavior>
  <action>
    Add `async def distinct_artists(session, query, *, limit=20) -> list[str]` to `services/search_queries.py`, mirroring `get_summary_counts`: `SELECT DISTINCT` over `FileMetadata.artist` and `Tracklist.artist` (both `is_not(None)` + `ilike(f"%{query}%")`), `union_all`, `.distinct().limit(limit)`; return the non-None artist strings. Read-only, parameterized ILIKE (bound param — no interpolation, T-61-06). Note the UNINDEXED-columns tradeoff in the docstring (Pitfall 4: debounce + LIMIT + min-length live in the caller).
    In `search.py`, add a grouped-results branch (a dedicated HX route `GET /search/palette?q=` is cleanest — it leaves the existing flat `results_content.html` branch untouched) that runs `search()` for Files/Tracklists (Discogs = Discretion: surface as a group or omit) + `distinct_artists()` for the Artists group, and renders `search/partials/palette_results.html`. Gate the artist query on `len(q) >= 2`.
    Create `src/phaze/templates/search/partials/palette_results.html`: four labeled groups (Jura 11px uppercase headers, `role="presentation"`) inside a `role="listbox"`; each selectable row is `role="option"` with a stable id (for `aria-activedescendant`) and the row's action attributes — Files rows carry the record-open contract (`hx-get="/record/{file_id}"` `hx-target="#record-body"` `hx-swap="innerHTML"` `@click="$dispatch('record:open',{el:$el})"`); Tracklists rows nav to the tracklist workspace; Artists rows nav to the file list with `?artist={{ artist }}` (existing `artist=` param); the Commands group renders the four static commands (`⊕ Scan` → `POST /pipeline/scan-live-sets`, `→ Jump to a stage` → `/s/<stage>`, `✓ Jump to a review queue` → a Phase 60 gate `/s/<stage>`, `⎈ Open Agents` → `/admin/agents`). Every DB-sourced cell autoescaped; JS-attribute contexts use `|tojson` (SP-5 / T-61-01). No-results → the muted "No matches…" copy. Rows are static markup (no `x-data` islands).
  </action>
  <verify>
    <automated>uv run pytest "tests/test_record_palette_agents.py::test_distinct_artists_query" "tests/test_record_palette_agents.py::test_cmdk_grouped_results" -x</automated>
  </verify>
  <acceptance_criteria>
    - `distinct_artists` exists in `search_queries.py`, is read-only, returns DISTINCT non-None artists LIMIT-bounded, and uses a bound ILIKE param.
    - The grouped endpoint returns Files/Tracklists/Artists/Commands; `palette_results.html` emits `role="listbox"`, `role="option"` rows, and `role="presentation"` group headers.
    - Files-group rows carry the `/record/{file_id}` open contract; Artists rows carry `?artist=`; the Scan command targets `/pipeline/scan-live-sets`.
    - Both named tests pass.
  </acceptance_criteria>
  <done>The grouped palette fragment renders four ARIA-correct groups over the search service + the new read-only distinct-artist facet.</done>
</task>

<task type="auto">
  <name>Task 2: Wire cmdk_modal.html — x-trap, roving arrow-nav, debounced fetch, commands</name>
  <files>src/phaze/templates/shell/partials/cmdk_modal.html</files>
  <read_first>
    - src/phaze/templates/shell/partials/cmdk_modal.html (the full skeleton — open/close/$nextTick/Esc contract to preserve)
    - src/phaze/templates/shell/partials/header.html:27 (`#cmdk-trigger` — the Esc focus-return target)
    - 61-RESEARCH.md §Architecture Patterns Pattern 2 (roving index + ARIA listbox contract) + §Pitfall 1 (x-trap must be on the persistent host, which cmdk_modal already is)
    - 61-PATTERNS.md §cmdk_modal.html (MODIFY) (the exact ADD list)
    - 61-UI-SPEC.md §Surface 2 (Full keyboard nav + ARIA listbox + Focus-trap + empty/no-results states) + §Interaction/Focus/ARIA Baseline
  </read_first>
  <action>
    Modify `cmdk_modal.html` in place, keeping the existing open/close/`?palette=1`/`$nextTick`-focus/`@keydown.escape`→`#cmdk-trigger` contract. Add `x-trap.inert.noscroll="open"` to the panel (replacing the skeleton's basic focus management). Extend `x-data` with roving state `{ q, activeIndex, items: [] }`; add a debounced (`≥150-250ms`) `x-on:input` that fires an `hx-get` to the grouped results endpoint from Task 1, swapping ONLY the results body (not the header/input); on new results reset `activeIndex` to the first selectable row and rebuild `items` from the rendered `role="option"` rows. Bind `↑`/`↓` to move `activeIndex` across the FLAT ordered list (skip `role="presentation"` headers, wrap at ends, auto-scroll into view); `Enter` activates `items[activeIndex]` (dispatch its click / follow its `hx-get` — open record · run command · navigate). Set the input `role="combobox"` `aria-expanded` `aria-controls` `aria-activedescendant="{active row id}"`, and keep the dialog `role="dialog" aria-modal="true" aria-label="Command palette"`. Empty query → show the Commands group; non-matching → the muted no-results copy. Palette results are static option markup — do NOT call `Alpine.initTree` (only the record body needs it). No new poll loop (SP-6).
  </action>
  <verify>
    <automated>uv run pytest "tests/test_record_palette_agents.py::test_cmdk_commands_and_artist_nav" "tests/test_record_palette_agents.py::test_new_fragments_single_poll_clean" -x</automated>
  </verify>
  <acceptance_criteria>
    - The panel carries `x-trap.inert.noscroll="open"`; the input is `role="combobox"` with `aria-activedescendant`.
    - Typing debounce-fetches the grouped endpoint and swaps only the results body; `↑`/`↓` move a single active row across groups; `Enter` activates it; `Esc` returns focus to `#cmdk-trigger`.
    - The four commands wire to `/pipeline/scan-live-sets`, `/s/<stage>` (stage + review queue), and `/admin/agents`; artist Enter navigates with `artist=`.
    - No `hx-trigger="every"`/`setInterval` is added; `test_cmdk_commands_and_artist_nav` and `test_new_fragments_single_poll_clean` pass.
  </acceptance_criteria>
  <done>⌘K is a fully keyboard-navigable, focus-trapped, grouped command palette funneled through the existing search service and enqueue guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client query → search / distinct_artists | free-text ⌘K query crosses into a DB read |
| DB → rendered palette rows | filenames, artist names, tracklist titles flow DB→HTML |
| ⌘K Scan command → enqueue_router | a command triggers a scan enqueue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-06 | Tampering | `distinct_artists` ILIKE query + `search()` | mitigate | SQLAlchemy bound parameters (parameterized ILIKE — no string interpolation into SQL); read-only SELECT DISTINCT |
| T-61-01 | Tampering | palette result rows (file/artist/tracklist names) | mitigate | Jinja2 autoescape on every cell; `|tojson` (NOT `|e`) in Alpine JS-attribute contexts |
| T-61-07 | Denial of Service | per-keystroke distinct-artist scan on UNINDEXED columns | accept | debounce (≥150-250ms) + `LIMIT` + `len(q) >= 2` gate; single-user tool; a real trigram index is deferred (schema change, out of presentation scope — RESEARCH A2) |
</threat_model>

<verification>
- `uv run pytest tests/test_record_palette_agents.py -k "cmdk or distinct or artist or single_poll" -x` passes the RECORD-02 tests.
- `uv run pytest --cov --cov-report=term-missing` meets the 85% floor; pre-commit + mypy strict pass (never `--no-verify`).
- Manual (per 61-VALIDATION §Manual-Only): open ⌘K, Tab stays inside; Esc returns focus to the trigger.
</verification>

<success_criteria>
⌘K opens a grouped, arrow-navigable, focus-trapped palette searching files / tracklists / artists (the last via a read-only distinct-artist facet) and offering the four quick commands — all over the existing search service and `enqueue_router` guards, with no new poll loop.
</success_criteria>

<output>
Create `.planning/phases/61-full-record-k-agents/61-03-SUMMARY.md` when done.
</output>
