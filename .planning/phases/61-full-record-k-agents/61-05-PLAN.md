---
phase: 61-full-record-k-agents
plan: 05
type: execute
wave: 2
depends_on: [61-01]
files_modified:
  - src/phaze/routers/shell.py
  - src/phaze/templates/pipeline/partials/empty_state.html
autonomous: true
requirements: [RECORD-04]
must_haves:
  truths:
    - "RECORD-04: when file count == 0 the home/Analyze workspace renders a first-run empty-state guide instead of the dashboard"
    - "D-08: the guide lists each registered agent + its configured scan_roots with a 'Scan {agent}' button and a 'Configure roots →' link — ZERO new input surface (no free-text path field, no directory-browsing endpoint)"
    - "Correction 2 (RESEARCH Pitfall 2): 'Scan {agent}' posts the DISCOVERY scan POST /pipeline/scans (agent_id + scan_root form), NOT the parameterless scan-live-sets — with 0 files there is nothing to fingerprint"
    - "Live scan progress rides the EXISTING /pipeline/stats 5s poll — no new loop (D-08 / SP-6); file_count>0 suppresses the empty state"
  artifacts:
    - path: "src/phaze/templates/pipeline/partials/empty_state.html"
      provides: "agent-roots-only first-run guide (no free-text path input)"
      contains: "/pipeline/scans"
    - path: "src/phaze/routers/shell.py"
      provides: "file_count==0 branch swapping the analyze stage_partial to the empty state"
      contains: "empty_state"
  key_links:
    - from: "src/phaze/templates/pipeline/partials/empty_state.html"
      to: "POST /pipeline/scans"
      via: "per-agent Scan form (agent_id + scan_root)"
      pattern: "/pipeline/scans"
    - from: "src/phaze/routers/shell.py"
      to: "empty_state.html"
      via: "count==0 branch on the analyze render (oob_counts stays False)"
      pattern: "empty_state"
---

<objective>
Add the first-run empty state (RECORD-04): when no files exist, the home/Analyze workspace shows a centered guide that lists each agent's already-configured `scan_roots` and a per-agent "Scan {agent}" button — reusing the DISCOVERY scan endpoint, with zero new input surface — and shows live scan progress on the existing poll.

Purpose: RECORD-04 + D-08 + RESEARCH Correction 2. The critical landmine: the empty-state Scan must post the DISCOVERY scan `POST /pipeline/scans` (agent_id + scan_root), NOT the parameterless `scan-live-sets` the ⌘K "Scan" uses — with 0 files there is nothing to fingerprint. No free-text path field, no directory browser (deliberate — no filesystem-listing attack surface on the archive host).
Output: a `file_count==0` branch in the analyze render + the `empty_state.html` guide.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-full-record-k-agents/61-CONTEXT.md
@.planning/phases/61-full-record-k-agents/61-RESEARCH.md
@.planning/phases/61-full-record-k-agents/61-PATTERNS.md
@.planning/phases/61-full-record-k-agents/61-UI-SPEC.md

<interfaces>
<!-- Verified against the working tree 2026-07-01. -->

shell.py:122-160 _render_stage: builds context with stage/stage_partial/oob_counts=False; the analyze branch
  (stage == "analyze", :144) merges build_dashboard_context. The fragment fork (:236-238):
  HX-Request → shell/_stage_fragment.html (bare) ; else shell/shell.html (full chrome).
  To render the empty state, set context["stage_partial"] = "pipeline/partials/empty_state.html" when file_count==0
  (leaves the fragment fork + oob_counts=False discipline intact; NO edit to analyze_workspace.html).
  Read file_count with a lightweight degrade-safe count (select(func.count(FileRecord.id))).

DISCOVERY scan (empty-state target — CORRECTION, pipeline_scans.py:305 POST /pipeline/scans):
  Form fields agent_id + scan_root (+ optional subpath); validates scan_root against agent.scan_roots
  (NFC-normalize + reject "..", prefix check — pipeline_scans.py:319, T-27-03); creates a RUNNING ScanBatch,
  enqueues scan_directory, returns scan_progress_card.html. This — NOT scan-live-sets — is the correct 0-files trigger.
  agent-roots dropdown helper: GET /pipeline/scans/agent-roots (pipeline_scans.py:150).
  Reused form precedent: pipeline/partials/discover_workspace.html:39 (already posts /pipeline/scans).

Agent (models/agent.py:29): scan_roots: JSONB (list[str]); kind ∈ {fileserver, compute}.
  Non-revoked agent list: select(Agent).where(Agent.revoked_at.is_(None)).order_by(Agent.name)  (shell.py discover branch precedent).

Live progress: the existing #pipeline-stats 5s poll (shell.html:187) — scan progress rides it (SP-6), NO new loop.
</interfaces>

@src/phaze/routers/shell.py
@src/phaze/templates/pipeline/partials/discover_workspace.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: file_count==0 branch on the analyze render + agent list read</name>
  <files>src/phaze/routers/shell.py</files>
  <read_first>
    - src/phaze/routers/shell.py:122-160 (the `_render_stage` context build + the `analyze` branch merging `build_dashboard_context`) + :236-262 (the fragment fork + `shell_home`)
    - src/phaze/routers/shell.py discover branch (the non-revoked `select(Agent)` precedent)
    - 61-PATTERNS.md §"Empty-state + shell.py count==0 branch" (the analog + the scan-endpoint correction)
    - 61-UI-SPEC.md §Surface 4 (placement = Discretion: home/Analyze workspace)
  </read_first>
  <behavior>
    - test_empty_state_suppressed_when_files_exist: with ≥1 file, GET / renders the analyze dashboard (NOT the empty state).
  </behavior>
  <action>
    In `shell.py`, in the `analyze` branch of `_render_stage` (which `shell_home` GET `/` also drives), read a lightweight degrade-safe `file_count` (`select(func.count(FileRecord.id))`, `.scalar()` — fall back to a non-zero sentinel on error so a transient DB issue never falsely shows first-run). When `file_count == 0`: set `context["stage_partial"] = "pipeline/partials/empty_state.html"` and add `context["agents"] = ` the non-revoked agent list (`select(Agent).where(Agent.revoked_at.is_(None)).order_by(Agent.name)`, scalars) for the roots guide. Keep `oob_counts=False` and the existing fragment fork untouched (do NOT edit `analyze_workspace.html` — the swap is via `stage_partial`). No logic changes; all reads read-only.
  </action>
  <verify>
    <automated>uv run pytest "tests/test_record_palette_agents.py::test_empty_state_suppressed_when_files_exist" -x</automated>
  </verify>
  <acceptance_criteria>
    - The analyze render reads `file_count` degrade-safe and, when 0, sets `stage_partial` to `pipeline/partials/empty_state.html` and injects the non-revoked `agents` list.
    - `file_count > 0` leaves the existing analyze dashboard render unchanged (`analyze_workspace.html` is not edited).
    - `oob_counts` stays False; the fragment fork is unchanged.
    - `test_empty_state_suppressed_when_files_exist` passes.
  </acceptance_criteria>
  <done>The analyze render branches to the empty state only when file count is exactly 0, with the agent list available for the roots guide.</done>
</task>

<task type="auto">
  <name>Task 2: empty_state.html — agent-roots guide + per-agent DISCOVERY scan + live progress</name>
  <files>src/phaze/templates/pipeline/partials/empty_state.html</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/discover_workspace.html:39 (the reused Trigger Scan form that already posts `/pipeline/scans` with agent_id + scan_root)
    - src/phaze/routers/pipeline_scans.py:305-330 (the `POST /pipeline/scans` Form contract + `scan_roots` prefix/`..` validation, T-27-03) + :150 (agent-roots helper)
    - src/phaze/models/agent.py:29 (`scan_roots` JSONB)
    - 61-RESEARCH.md §Pitfall 2 (the scan-endpoint landmine — DISCOVERY not fingerprint) + §Security Domain (info-disclosure mitigation)
    - 61-UI-SPEC.md §Surface 4 (wave logo, Display heading, agent-roots cards, live progress card) + §Copywriting Contract (heading, body, "Configure roots →", live-progress copy) + §Typography (24px Display first renders here)
  </read_first>
  <behavior>
    - test_empty_state_agent_roots_scan: with 0 files, GET / renders the empty-state guide listing each agent + its scan_roots; each "Scan {agent}" control posts `POST /pipeline/scans` (agent_id + scan_root); NO free-text path `<input>` for a path is present.
  </behavior>
  <action>
    Create `src/phaze/templates/pipeline/partials/empty_state.html` per UI-SPEC §Surface 4: a centered `w-[560px]` column with the wave-logo SVG (64×64, brand blue), the Jura 24px Display heading "POINT PHAZE AT YOUR MUSIC" (`uppercase tracking-[0.2em]`), the muted body copy, and — for each agent in `agents` — a card listing the agent name + its configured `scan_roots` (mono) with a "Scan {agent}" button and a "Configure roots →" link. Each "Scan {agent}" is a form/button posting `POST /pipeline/scans` with hidden `agent_id` + `scan_root` (render one control per (agent, root), OR post `scan_roots[0]` per agent — planner-permitted). This REUSES the discover-scan form shape (`discover_workspace.html`) and the endpoint's existing `scan_roots` prefix + `..` validation — do NOT add a free-text path field or any directory-browsing endpoint (D-08 / T-61-04). Add a live scan-progress card (`bg-blue-400 h-1.5` bar + the UI-SPEC copy) that rides the EXISTING `/pipeline/stats` poll — NO new `hx-trigger="every"`/`setInterval`/second loop (SP-6). All values autoescaped; `scan_roots` cross the DB→HTML boundary — never `| safe`.
  </action>
  <verify>
    <automated>uv run pytest "tests/test_record_palette_agents.py::test_empty_state_agent_roots_scan" "tests/test_record_palette_agents.py::test_new_fragments_single_poll_clean" -x</automated>
  </verify>
  <acceptance_criteria>
    - `empty_state.html` lists each agent + its `scan_roots` and a "Scan {agent}" control that posts `POST /pipeline/scans` with `agent_id` + `scan_root`.
    - No free-text path `<input>` and no directory-browse endpoint is introduced; a "Configure roots →" link is present.
    - The scan target is `/pipeline/scans` (grep confirms) and NOT `/pipeline/scan-live-sets`.
    - No `hx-trigger="every"`/`setInterval`/second poll loop appears; `test_empty_state_agent_roots_scan` and `test_new_fragments_single_poll_clean` pass.
  </acceptance_criteria>
  <done>The first-run guide reuses each agent's configured roots + the discovery scan endpoint (no new input surface) and shows live progress on the existing poll.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| empty-state Scan form → POST /pipeline/scans | agent_id + scan_root cross to a scan-enqueue |
| DB (Agent.scan_roots) → rendered guide | configured roots cross into HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-04 | Information Disclosure | first-run scan surface | mitigate | NO directory-browse endpoint, NO free-text path field (D-08); the scan reuses `POST /pipeline/scans`'s existing `scan_roots` prefix + `..`-traversal validation (pipeline_scans.py:319, T-27-03) |
| T-61-01 | Tampering | rendered `scan_roots` values | mitigate | Jinja2 autoescape on all `scan_roots`/agent values; never `| safe` |
</threat_model>

<verification>
- `uv run pytest tests/test_record_palette_agents.py -k "empty_state or single_poll" -x` passes the RECORD-04 tests + the fragment guard.
- `uv run pytest --cov --cov-report=term-missing` meets the 85% floor; pre-commit + mypy strict pass (never `--no-verify`).
- `grep -n "scan-live-sets" src/phaze/templates/pipeline/partials/empty_state.html` returns nothing (correct endpoint).
- Manual (per 61-VALIDATION §Manual-Only): with 0 files, click "Scan {agent}", observe progress advancing via the existing poll (no second request loop in the Network tab).
</verification>

<success_criteria>
When no files exist, a first-run empty state guides the operator to point phaze at an already-configured agent root (no new input surface), triggers the correct discovery scan, and shows live scan progress on the existing single poll.
</success_criteria>

<output>
Create `.planning/phases/61-full-record-k-agents/61-05-SUMMARY.md` when done.
</output>
