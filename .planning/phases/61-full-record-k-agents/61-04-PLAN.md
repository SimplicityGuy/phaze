---
phase: 61-full-record-k-agents
plan: 04
type: execute
wave: 2
depends_on: [61-01]
files_modified:
  - src/phaze/services/agent_liveness.py
  - src/phaze/routers/admin_agents.py
  - src/phaze/templates/admin/agents.html
  - src/phaze/templates/admin/partials/agents_table.html
autonomous: true
requirements: [RECORD-03]
must_haves:
  truths:
    - "D-06: the Agents page has TWO sections — Section 1 heartbeating Agents (local/A1 via _load_agents + agent_liveness.classify/sort_key, reused verbatim) and Section 2 a distinct 'Compute / burst lanes · ephemeral' k8s section driven by CloudJob counts, not Agent rows"
    - "D-07: the compute lane liveness is ACTIVE (≥1 running) / WAITING (submitted+inadmissible) / IDLE (none) and NEVER DEAD — degrade-safe to IDLE on DB error (KDEPLOY-04)"
    - "RECORD-03: local and A1 render as heartbeating agents; k8s renders as an ephemeral Job-based identity — no perpetually-DEAD agent, no rose/red DEAD state anywhere in Section 2"
    - "The second section rides the EXISTING /admin/agents/_table 5s self-poll — no new loop (RESEARCH OQ-1: restyle the standalone page in place)"
  artifacts:
    - path: "src/phaze/services/agent_liveness.py"
      provides: "classify_compute_lanes() read-only CloudJob aggregation"
      contains: "async def classify_compute_lanes"
    - path: "src/phaze/templates/admin/agents.html"
      provides: "two-section layout (heartbeating + ephemeral compute lanes)"
      contains: "Compute / burst lanes"
  key_links:
    - from: "src/phaze/routers/admin_agents.py"
      to: "classify_compute_lanes"
      via: "_load_agents/page/table_partial context injection"
      pattern: "classify_compute_lanes"
    - from: "src/phaze/services/agent_liveness.py"
      to: "CloudJob.status / CloudJob.inadmissible"
      via: "degrade-safe count aggregation (mirror pipeline.py:1117/1162)"
      pattern: "CloudJobStatus"
---

<objective>
Turn the Agents page into the two-section surface RECORD-03 requires: Section 1 the existing heartbeating agents (local/A1) reused verbatim, and Section 2 a distinct "Compute / burst lanes · ephemeral" section that models the k8s burst lane as a Job-based identity — Active/Waiting/Idle from live CloudJob counts, never a perpetually-DEAD agent.

Purpose: RECORD-03 + D-06/D-07 + KDEPLOY-04 intent. The heartbeating section is reuse-verbatim; the only new code is a read-only `classify_compute_lanes()` aggregation with two direct in-repo precedents. Per RESEARCH OQ-1, restyle the standalone `/admin/agents` page in place (lowest risk, keeps its existing single `_table` 5s self-poll).
Output: `classify_compute_lanes()`, the two-section `agents.html`/`agents_table.html`, and the router context wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-full-record-k-agents/61-CONTEXT.md
@.planning/phases/61-full-record-k-agents/61-RESEARCH.md
@.planning/phases/61-full-record-k-agents/61-PATTERNS.md
@.planning/phases/61-full-record-k-agents/61-UI-SPEC.md

<interfaces>
<!-- Verified against the working tree 2026-07-01. -->

Section 1 (reuse VERBATIM):
  admin_agents.py:59 _load_agents(session) -> (agents, now)  — injects a._status = classify(a, now), sorts by sort_key.
  admin_agents.py:80 page (full page OR partial on HX) ; :108 table_partial (the /_table 5s self-poll target).
  agent_liveness.py:68 classify(agent, now) -> alive/stale/dead/revoked/never ; :89 sort_key.
  templates: admin/agents.html (full page, extends base.html; already carries a STATIC k8s note, Phase 56) +
             admin/partials/agents_table.html (the polled partial; re-emits its own hx-trigger="every 5s").

Section 2 (NEW read-only aggregation, D-07 — mirror the degrade-safe count precedents):
  services/pipeline.py:1116 get_inadmissible_count + :1159 get_cloud_phase_counts — both try/except → default 0.
  CloudJob (models/cloud_job.py:65): status (String), inadmissible (bool).
  CloudJobStatus (StrEnum): RUNNING="running", SUBMITTED="submitted", SUCCEEDED="succeeded", UPLOADING, UPLOADED, FAILED.
  Predicate (RESEARCH §Code Examples): ACTIVE = ≥1 status==RUNNING ; WAITING = ≥1 status==SUBMITTED AND inadmissible is True ; IDLE = none.
  classify_compute_lanes(session) -> tuple[str, int]  returns ("ACTIVE"|"WAITING"|"IDLE", in_flight_count);
    except SQLAlchemyError → ("IDLE", 0)   # degrade-safe: NEVER a DEAD/red state.

UI-SPEC §Surface 3 + §Color compute-lane table:
  ACTIVE emerald-400 dot / "ACTIVE · {n} workloads" ; WAITING amber-400 / "WAITING · quota" (role="alert") ;
  IDLE gray dot / "IDLE" ; DEAD never rendered. Section 2 = amber-tinted ephemeral framing + the verbatim NOTE copy.
  oob_counts stays False on the render (Pitfall 5); live refresh rides the existing _table poll (no new loop).
</interfaces>

@src/phaze/routers/admin_agents.py
@src/phaze/services/agent_liveness.py
@src/phaze/templates/admin/agents.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: classify_compute_lanes() read-only CloudJob aggregation</name>
  <files>src/phaze/services/agent_liveness.py</files>
  <read_first>
    - src/phaze/services/pipeline.py:1116-1136 (`get_inadmissible_count`) + :1159-1184 (`get_cloud_phase_counts`) — the degrade-safe `try/except → default` count precedents (SP-3)
    - src/phaze/models/cloud_job.py:30-95 (CloudJob + CloudJobStatus members)
    - src/phaze/services/agent_liveness.py:68-114 (`classify`/`sort_key` — the "classify server-side, inject on the row" shape, SP-4)
    - 61-RESEARCH.md §Code Examples "Compute-lane liveness" + §Pattern 3
    - 61-UI-SPEC.md §Color (compute-lane liveness table) — the never-DEAD contract
  </read_first>
  <behavior>
    - test_compute_lane_liveness_states: classify_compute_lanes → ("ACTIVE", n) when ≥1 CloudJob.status==running; ("WAITING", n) when ≥1 submitted+inadmissible and none running; ("IDLE", 0) when none in-flight; ("IDLE", 0) on a DB error (never DEAD).
  </behavior>
  <action>
    Add `async def classify_compute_lanes(session) -> tuple[str, int]` to `services/agent_liveness.py`, mirroring the degrade-safe `try/except → default` count shape at `pipeline.py:1117/1162`. Count `CloudJob` rows with `status == CloudJobStatus.RUNNING.value` (running) and with `status == CloudJobStatus.SUBMITTED.value` AND `inadmissible.is_(True)` (waiting). Return `("ACTIVE", running)` if running ≥ 1, else `("WAITING", waiting)` if waiting ≥ 1, else `("IDLE", 0)`; on `SQLAlchemyError` return `("IDLE", 0)`. Read-only, no logic change, NEVER a DEAD/red state (KDEPLOY-04 / D-07). Keep it a pure aggregation the router injects on the render (SP-4).
  </action>
  <verify>
    <automated>uv run pytest "tests/test_record_palette_agents.py::test_compute_lane_liveness_states" -x</automated>
  </verify>
  <acceptance_criteria>
    - `classify_compute_lanes` exists in `agent_liveness.py`, is read-only, and returns `("ACTIVE"|"WAITING"|"IDLE", int)`.
    - ACTIVE when ≥1 running; WAITING when ≥1 submitted+inadmissible and no running; IDLE when none; IDLE (never DEAD) on `SQLAlchemyError`.
    - `test_compute_lane_liveness_states` passes.
  </acceptance_criteria>
  <done>A degrade-safe read-only CloudJob aggregation yields Active/Waiting/Idle and never a DEAD state.</done>
</task>

<task type="auto">
  <name>Task 2: Two-section Agents page (heartbeating + ephemeral compute lanes) + router wiring</name>
  <files>src/phaze/routers/admin_agents.py, src/phaze/templates/admin/agents.html, src/phaze/templates/admin/partials/agents_table.html</files>
  <read_first>
    - src/phaze/routers/admin_agents.py:59-135 (`_load_agents`, `page`, `table_partial` — where the Section-2 context is injected)
    - src/phaze/templates/admin/agents.html (the full page with the STATIC Phase-56 k8s note being replaced by a LIVE section) + src/phaze/templates/admin/partials/agents_table.html (the polled partial)
    - 61-PATTERNS.md §admin_agents.py + agent_liveness.py (MODIFY) (the Section-1-verbatim + Section-2-new split + OQ-1 placement)
    - 61-UI-SPEC.md §Surface 3 (both section layouts, the compute-lane card, the ephemeral NOTE) + §Color (state colors) + §Copywriting Contract (section headings, state labels, NOTE copy)
  </read_first>
  <action>
    Inject the compute-lane liveness into the Agents render: call `classify_compute_lanes(session)` in `admin_agents.py` (in `_load_agents` or directly in `page`/`table_partial`) and pass `(state, in_flight_count)` into the template context for BOTH the full page and the `/_table` partial (so the existing 5s self-poll refreshes Section 2 too — no new loop, OQ-1). Restyle `admin/agents.html` into TWO sections: Section 1 "Agents · heartbeating" — the existing `_load_agents` grid reused verbatim (do NOT change `classify`/`sort_key`); Section 2 "Compute / burst lanes · ephemeral" — a visually distinct amber-tinted section rendering ONE lane card from `classify_compute_lanes`, with the dot+status per the UI-SPEC color table (ACTIVE emerald "ACTIVE · {n} workloads", WAITING amber "WAITING · quota" `role="alert"`, IDLE gray "IDLE" — DEAD never emitted), the in-flight breakdown when Active/Waiting, and the verbatim ephemeral NOTE copy in an amber info box. Move the reusable Section-2 markup into `agents_table.html` (the polled partial) if that is where the live counts land, so a poll tick updates both sections. Keep `oob_counts` semantics off (Pitfall 5); every DB cell autoescaped. The ⌘K "Open Agents" command and the header status strip already link here (no change needed).
  </action>
  <verify>
    <automated>uv run pytest "tests/test_record_palette_agents.py::test_agents_two_sections_never_dead" -x</automated>
  </verify>
  <acceptance_criteria>
    - `admin/agents.html` renders two clearly-labeled sections: "Agents · heartbeating" and "Compute / burst lanes · ephemeral".
    - Section 1 uses `_load_agents` + `classify`/`sort_key` unchanged; Section 2 renders from `classify_compute_lanes`.
    - Section 2 shows ACTIVE/WAITING/IDLE with the correct dot colors + labels and the ephemeral NOTE; NO rose/red DEAD state string appears anywhere in Section 2.
    - The compute-lane counts are present in the `/_table` partial context so they refresh on the existing 5s poll (no new `hx-trigger`/loop added).
    - `test_agents_two_sections_never_dead` passes.
  </acceptance_criteria>
  <done>The Agents page shows heartbeating local/A1 and an ephemeral k8s burst lane (Active/Waiting/Idle, never DEAD), refreshing on the existing self-poll.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB (CloudJob) → rendered lane card | in-flight counts + state cross into HTML |
| CloudJob read → operator trust | a DB hiccup must not paint the lane DEAD/red (false alarm) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-08 | Denial of Service (false-alarm) | `classify_compute_lanes` on DB error | mitigate | degrade-safe `try/except → ("IDLE", 0)`; a DB error yields IDLE, never DEAD (KDEPLOY-04) |
| T-61-01 | Tampering | agent/lane card cells | mitigate | Jinja2 autoescape on all DB-sourced values; status cells carry a word not hue-only (WCAG 1.4.1) |
</threat_model>

<verification>
- `uv run pytest tests/test_record_palette_agents.py -k "compute or agents" -x` passes the two RECORD-03 tests.
- `uv run pytest --cov --cov-report=term-missing` meets the 85% floor; pre-commit + mypy strict pass (never `--no-verify`).
- `grep -ni "dead" src/phaze/templates/admin/` shows no DEAD state emitted in the compute-lanes section.
</verification>

<success_criteria>
The Agents page shows local and A1 as heartbeating agents and the k8s burst lane as an ephemeral, Job-based identity (Active/Waiting/Idle derived from in-flight CloudJob counts) — never a perpetually-DEAD agent — refreshing on the existing single self-poll.
</success_criteria>

<output>
Create `.planning/phases/61-full-record-k-agents/61-04-SUMMARY.md` when done.
</output>
