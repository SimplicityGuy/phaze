---
phase: 81-per-stage-failure-persistence-retry-paths
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/enums/stage.py
  - src/phaze/services/stage_status.py
  - tests/integration/test_stage_status_equivalence.py
autonomous: true
requirements: [FAIL-01, FAIL-04]
must_haves:
  truths:
    - "D-14: FAILURE_IS_TERMINAL is created in the DB-free src/phaze/enums/stage.py (it exists in no .py today; Phase 78 inlined analyze terminality)."
    - "D-15: two explicit tables encode different axes — FAILURE_IS_TERMINAL={ANALYZE:True, METADATA:True, FINGERPRINT:False} (recovery) and ELIGIBLE_AFTER_FAILURE={ANALYZE:False, METADATA:True, FINGERPRINT:True} (eligibility)."
    - "D-16: the eligible() refactor is semantics-preserving — Phase 78's ELIG-01..ELIG-04 tests pass unchanged; only the per-stage constants move out of the dispatch body."
    - "D-17: the pure domain_completed(status_map, stage) ships in enums/stage.py AND its SQL twin domain_completed_clause() ships in services/stage_status.py this same phase, drift-locked by extending the Phase 78 equivalence test now."
    - "D-04: no file's derived status changes; the eligible()/domain_completed refactor is pure and the Phase 79 shadow gate stays green."
    - "enums/stage.py stays DB-free: it imports no sqlalchemy and no phaze.models (a Postgres-free agent imports it)."
    - "FAIL-01 (analyze terminal) and FAIL-04 (fingerprint auto-retryable) are encoded by the two tables so the 44.5K over-enqueue guard and fingerprint retry stop being coincidences two readers must independently remember."
  artifacts:
    - path: "src/phaze/enums/stage.py"
      provides: "FAILURE_IS_TERMINAL, ELIGIBLE_AFTER_FAILURE, domain_completed(); refactored eligible()"
      contains: "FAILURE_IS_TERMINAL"
    - path: "src/phaze/services/stage_status.py"
      provides: "domain_completed_clause() SQL twin"
      contains: "def domain_completed_clause"
    - path: "tests/integration/test_stage_status_equivalence.py"
      provides: "domain_completed Python<->SQL equivalence cells"
      contains: "domain_completed"
  key_links:
    - from: "src/phaze/services/stage_status.py"
      to: "src/phaze/enums/stage.py"
      via: "import FAILURE_IS_TERMINAL"
      pattern: "from phaze.enums.stage import"
    - from: "tests/integration/test_stage_status_equivalence.py"
      to: "domain_completed / domain_completed_clause"
      via: "parametrized equivalence assertion"
      pattern: "domain_completed"
---

<objective>
Create the DB-free terminality/eligibility tables and the `domain_completed` predicate (Python
helper + SQL twin), and refactor `eligible()` to consume them without changing behavior.

Purpose: `FAILURE_IS_TERMINAL` is referenced by 80-CONTEXT D-01, ROADMAP Phase-80 SC#3, and
`.planning/research/` but exists in no `.py` today (D-14). Phase 80 consumes this; the operator
chose to create it here rather than leave it an unowned dependency. Shipping the Python helper and
its SQL twin in the same phase closes the exact drift window Phase 78 D-04 established.

Output: two module-level tables + `domain_completed()` in `enums/stage.py`, a semantics-preserving
`eligible()` refactor, `domain_completed_clause()` in `services/stage_status.py`, and extended
equivalence-test cells locking the two definitions together.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-per-stage-failure-persistence-retry-paths/81-CONTEXT.md
@.planning/phases/81-per-stage-failure-persistence-retry-paths/81-RESEARCH.md
@.planning/phases/81-per-stage-failure-persistence-retry-paths/81-PATTERNS.md
@.planning/milestones/PARALLEL-ENRICH-DAG-DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the two tables + domain_completed() and refactor eligible() in enums/stage.py</name>
  <read_first>
    - src/phaze/enums/stage.py (the whole module — DB-free docstring lines 9-11; ELIGIBILITY_DAG table lines 61-69; `_analyze_status` done≻failed lines 75-84; the `eligible()` dispatch lines 167-193)
    - tests/shared/test_stage_eligibility_dag.py (ELIG-01..04 — must pass UNCHANGED)
    - tests/shared/test_stage_resolver.py (the no-sqlalchemy/no-phaze.models import guard this module must not break)
    - .planning/phases/81-per-stage-failure-persistence-retry-paths/81-RESEARCH.md (§ Terminality / Eligibility Tables — the exact dict literals and the collapse formula)
    - .planning/phases/81-per-stage-failure-persistence-retry-paths/81-PATTERNS.md (the enums/stage.py assignment block)
  </read_first>
  <action>
    Add two module-level dicts next to `ELIGIBILITY_DAG`: `FAILURE_IS_TERMINAL: dict[Stage, bool]`
    = `{Stage.ANALYZE: True, Stage.METADATA: True, Stage.FINGERPRINT: False}` and
    `ELIGIBLE_AFTER_FAILURE: dict[Stage, bool]` = `{Stage.ANALYZE: False, Stage.METADATA: True,
    Stage.FINGERPRINT: True}` (D-15 — two axes, do not conflate). Add pure helper
    `domain_completed(status_map, stage) -> bool` returning
    `st is Status.DONE or (st is Status.FAILED and FAILURE_IS_TERMINAL[stage])` where
    `st = status_map.get(stage, Status.NOT_STARTED)` (D-17). Refactor `eligible()`'s three enrich
    branches (the metadata/fingerprint "not in (DONE, IN_FLIGHT)" branch is at `enums/stage.py:186-187`
    and the ANALYZE carve-out `== NOT_STARTED` is at `:188-189` — RESEARCH-corrected; `:190` is the
    APPLY branch, DO NOT TOUCH it) into the single expression
    `status not in (Status.DONE, Status.IN_FLIGHT) and (status != Status.FAILED or
    ELIGIBLE_AFTER_FAILURE[stage])` (D-16 — this yields identical truth: ANALYZE eligible iff
    NOT_STARTED; METADATA/FINGERPRINT eligible iff NOT_STARTED or FAILED). Keep the module stdlib-only —
    no sqlalchemy, no phaze.models import. Use `Final` typing consistent with existing module tables.
  </action>
  <acceptance_criteria>
    - Source: `src/phaze/enums/stage.py` defines `FAILURE_IS_TERMINAL` and `ELIGIBLE_AFTER_FAILURE` as module-level dicts and a `def domain_completed(` function.
    - Source: `rg -c 'import sqlalchemy|from sqlalchemy|from phaze.models|import phaze.models' src/phaze/enums/stage.py` returns 0.
    - Test: `uv run just test-bucket shared` exits 0 (ELIG-01..04 in test_stage_eligibility_dag.py pass UNCHANGED; the DB-free import guard in test_stage_resolver.py passes).
    - Behavior: `eligible(Stage.METADATA, {Stage.METADATA: Status.FAILED})` is True; `eligible(Stage.ANALYZE, {Stage.ANALYZE: Status.FAILED})` is False; `eligible(Stage.FINGERPRINT, {Stage.FINGERPRINT: Status.FAILED})` is True.
  </acceptance_criteria>
  <verify>
    <automated>uv run just test-bucket shared</automated>
  </verify>
  <done>The two tables + domain_completed() exist in the DB-free module, eligible() is refactored, and ELIG-01..04 pass unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add domain_completed_clause() SQL twin in services/stage_status.py</name>
  <read_first>
    - src/phaze/services/stage_status.py (`done_clause` lines 89-117, `failed_clause` lines 120-147, existing `from phaze.enums.stage import ...` dependency direction line ~65)
    - src/phaze/enums/stage.py (the FAILURE_IS_TERMINAL table added in Task 1)
    - .planning/phases/81-per-stage-failure-persistence-retry-paths/81-RESEARCH.md (§ SQL twin)
  </read_first>
  <action>
    Add `domain_completed_clause(stage: Stage) -> ColumnElement[bool]` importing `FAILURE_IS_TERMINAL`
    from `phaze.enums.stage`. When `FAILURE_IS_TERMINAL[stage]` is True return
    `or_(done_clause(stage), failed_clause(stage))`; otherwise return `done_clause(stage)` (the second
    disjunct collapses to `false()` for fingerprint). Reuse the existing `done_clause`/`failed_clause`
    predicates verbatim — do NOT author a new CASE (D-17; those predicates are locked by the
    equivalence test). No f-string SQL.
  </action>
  <acceptance_criteria>
    - Source: `src/phaze/services/stage_status.py` contains `def domain_completed_clause(stage: Stage)` and imports `FAILURE_IS_TERMINAL` from `phaze.enums.stage`.
    - Source: `rg -n "def domain_completed_clause" src/phaze/services/stage_status.py` returns one match.
    - Behavior: the function reuses `done_clause`/`failed_clause` (no new literal SQL string is introduced for the predicate).
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from phaze.services.stage_status import domain_completed_clause; from phaze.enums.stage import Stage; print(bool(domain_completed_clause(Stage.FINGERPRINT) is not None))"</automated>
  </verify>
  <done>domain_completed_clause() exists as the SQL twin of domain_completed, built from the locked done_clause/failed_clause predicates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend the Phase 78 equivalence test with domain_completed cells</name>
  <read_first>
    - tests/integration/test_stage_status_equivalence.py (the `CASES` list lines 306-337 — tuple shape `(Stage, seed_fn, expected_status)`; driver `test_sql_equals_python` lines 400-412; `load_scalars` line 351; existing failed/done seed fns `seed_analysis_failed`, `seed_metadata_failed_only`, `seed_fp_failed_only`, `seed_fp_success_and_failed`)
    - src/phaze/enums/stage.py (domain_completed from Task 1)
    - src/phaze/services/stage_status.py (domain_completed_clause from Task 2)
  </read_first>
  <behavior>
    - For every existing seed fn, `domain_completed(load_scalars(...), stage)` (Python) equals `bool(<domain_completed_clause(stage) SQL result>)` (SQL) for the same seeded row.
    - `(ANALYZE, seed_analysis_failed)` -> domain_completed True (analyze terminal).
    - `(METADATA, seed_metadata_failed_only)` -> domain_completed True (metadata terminal for recovery).
    - `(FINGERPRINT, seed_fp_failed_only)` -> domain_completed False (fingerprint NOT terminal).
    - `(FINGERPRINT, seed_fp_success_and_failed)` -> domain_completed True (done wins).
  </behavior>
  <action>
    Add a parallel parametrized cell set (reuse the SAME seed fns as the existing `CASES`) asserting
    `domain_completed(load_scalars(session, file_id), stage)` equals the boolean of the
    `select(domain_completed_clause(stage)).where(...)` SQL result per cell, so the Python table and
    SQL twin can never drift (the exact drift Phase 78 D-04 closed). Do not modify the existing
    `CASES` rows or the existing `test_sql_equals_python` assertions.
  </action>
  <acceptance_criteria>
    - Source: `rg -c "domain_completed" tests/integration/test_stage_status_equivalence.py` returns >= 2.
    - Test: `uv run just test-bucket integration` exits 0 (new domain_completed cells pass; existing equivalence cells unchanged and green).
    - Test: the new cells pass in isolation — `uv run just test-bucket integration` run standalone (not only inside the full suite).
  </acceptance_criteria>
  <verify>
    <automated>uv run just test-bucket integration</automated>
  </verify>
  <done>domain_completed Python<->SQL equivalence is asserted per seeded cell; both twins are drift-locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | This plan is pure derivation logic (DB-free tables + SQL predicate). No new untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-01-01 | Tampering | `enums/stage.py` DB-free invariant | mitigate | test_stage_resolver.py import guard keeps sqlalchemy/models out; a DB import would let an agent's process pull the ORM into a Postgres-free context |
| T-81-01-02 | Information Disclosure | derived-status semantics | accept | no PII; refactor is semantics-preserving (D-04), shadow gate proves no derived status changes |
| T-81-01-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan (zero new deps for the phase) |
</threat_model>

<verification>
- `uv run just test-bucket shared` green (ELIG-01..04 unchanged; DB-free guard).
- `uv run just test-bucket integration` green (domain_completed equivalence cells).
- `uv run mypy src/phaze/enums/stage.py src/phaze/services/stage_status.py` clean.
- Phase 79 shadow gate stays green (no derived-status change — verified at wave merge).
</verification>

<success_criteria>
- FAILURE_IS_TERMINAL, ELIGIBLE_AFTER_FAILURE, domain_completed() exist in DB-free enums/stage.py.
- domain_completed_clause() exists in services/stage_status.py.
- eligible() refactored, ELIG-01..04 pass unchanged.
- Python and SQL twins are equivalence-tested and cannot drift.
</success_criteria>

<output>
Create `.planning/phases/81-per-stage-failure-persistence-retry-paths/81-01-SUMMARY.md` when done.
</output>
