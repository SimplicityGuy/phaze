---
phase: 81-per-stage-failure-persistence-retry-paths
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/routers/agent_fingerprint.py
  - tests/fingerprint/routers/test_agent_fingerprint_failure.py
autonomous: true
requirements: [FAIL-04]
must_haves:
  truths:
    - "D-18: no new fingerprint writer — report_fingerprint_failed keeps persisting NO fingerprint_results row and only clears the ledger; the durable marker is the per-engine fingerprint_results.status='failed' row from put_fingerprint; a synthetic engine='_task' row would poison the two aliased per-engine joins at services/pipeline.py:939-940 and _trackid_engine_badge at :864. FAIL-04's deliverable is regression tests + docstrings that assert this asymmetry and that a failed fingerprint stays auto-retryable (eligible)."
  artifacts:
    - path: "tests/fingerprint/routers/test_agent_fingerprint_failure.py"
      provides: "no-row regression + per-engine-eligible + join-unpoisoned assertions"
      contains: "report_fingerprint_failed"
    - path: "src/phaze/routers/agent_fingerprint.py"
      provides: "docstrings documenting the no-row asymmetry"
      contains: "report_fingerprint_failed"
  key_links:
    - from: "tests/fingerprint/routers/test_agent_fingerprint_failure.py"
      to: "fingerprint_results row count"
      via: "assert unchanged after report_fingerprint_failed"
      pattern: "report_fingerprint_failed"
---

<objective>
Prove FAIL-04 without writing a new writer: `report_fingerprint_failed` persists nothing (only clears
the ledger), the durable failure marker is the existing per-engine
`fingerprint_results.status='failed'` row, and a failed fingerprint stays auto-retryable.

Purpose: FAIL-04's phrase "reused, not re-invented" is load-bearing (D-18). A synthetic
`fingerprint_results(engine='_task', status='failed')` sentinel would poison the two aliased
per-engine joins (`services/pipeline.py:939-940`) that feed `_trackid_engine_badge`
(`services/pipeline.py:864`). So the deliverable is regression tests + docstrings that lock the
asymmetry in place.

Output: a fingerprint-bucket regression test file and clarifying docstrings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-per-stage-failure-persistence-retry-paths/81-CONTEXT.md
@.planning/phases/81-per-stage-failure-persistence-retry-paths/81-RESEARCH.md
@.planning/phases/81-per-stage-failure-persistence-retry-paths/81-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fingerprint no-row + eligible + unpoisoned-join regression tests</name>
  <read_first>
    - src/phaze/routers/agent_fingerprint.py (`put_fingerprint` lines 21-56 upserts status+error_message from FingerprintWriteRequest; `report_fingerprint_failed` lines 59-89 clears ledger only, persists no row)
    - src/phaze/services/fingerprint.py (lines ~103,105 — the IngestResult -> status='failed' row producer; spot-read to confirm A1)
    - src/phaze/services/pipeline.py (`_trackid_engine_badge` line 864; the two aliased per-engine outer-joins `audfprint.engine == _TRACKID_ENGINE_AUDFPRINT` / `panako.engine == _TRACKID_ENGINE_PANAKO` lines 939-940)
    - src/phaze/enums/stage.py (`eligible(fingerprint)` — a FAILED fingerprint stays eligible)
    - existing tests/fingerprint/routers/ tests (the authed-agent client pattern)
  </read_first>
  <behavior>
    - `report_fingerprint_failed` persists NO `fingerprint_results` row: the row count for the file is unchanged before/after the call, and only the ledger entry is cleared.
    - A per-engine `fingerprint_results` row with `status='failed'` persists via put_fingerprint and keeps the file `eligible(Stage.FINGERPRINT)` (auto-retryable).
    - The two aliased per-engine joins at `services/pipeline.py:939-940` never observe an `engine='_task'` sentinel row (no synthetic engine name is ever written).
  </behavior>
  <action>
    Create `tests/fingerprint/routers/test_agent_fingerprint_failure.py`. Assert: (a) `fingerprint_results`
    row count for a file is unchanged after `report_fingerprint_failed` and the ledger is cleared;
    (b) a `status='failed'` per-engine row written by put_fingerprint keeps the file
    `eligible(Stage.FINGERPRINT)` True; (c) no `engine='_task'` (or any synthetic sentinel) row exists
    after `report_fingerprint_failed`, so the aliased per-engine joins stay unpoisoned. Use the
    authed-agent client pattern. Must pass in bucket isolation.
  </action>
  <acceptance_criteria>
    - Test: `uv run just test-bucket fingerprint` exits 0, in isolation.
    - Behavior: a test asserts `fingerprint_results` count is identical before and after `report_fingerprint_failed`.
    - Behavior: a test asserts no row with a synthetic engine sentinel (`_task`) exists after the call.
    - Behavior: a test asserts `eligible(Stage.FINGERPRINT, {Stage.FINGERPRINT: Status.FAILED})` is True.
  </acceptance_criteria>
  <verify>
    <automated>uv run just test-bucket fingerprint</automated>
  </verify>
  <done>The no-row asymmetry, per-engine eligibility, and unpoisoned joins are asserted in the fingerprint bucket in isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Document the fingerprint failure asymmetry in docstrings</name>
  <read_first>
    - src/phaze/routers/agent_fingerprint.py (`report_fingerprint_failed` lines 59-89; `put_fingerprint` lines 21-56)
    - .planning/phases/81-per-stage-failure-persistence-retry-paths/81-CONTEXT.md (D-18)
  </read_first>
  <action>
    Update the `report_fingerprint_failed` docstring to state explicitly that it persists NO
    `fingerprint_results` row by design (a synthetic `engine='_task'` row would poison the aliased
    per-engine joins at `services/pipeline.py:939-940` and `_trackid_engine_badge` at `:864`), and that
    the durable per-engine failure marker is the `status='failed'` row written by `put_fingerprint`.
    Note in `put_fingerprint`'s docstring that its `status='failed'` row is the auto-retryable
    fingerprint failure marker (FAILURE_IS_TERMINAL[fingerprint] = False). Docstrings only — no
    behavior change.
  </action>
  <acceptance_criteria>
    - Source: `rg -n "no .*fingerprint_results row|persists no row|_task" src/phaze/routers/agent_fingerprint.py` returns a match in the report_fingerprint_failed docstring.
    - Source: no code line (only docstrings/comments) changed in agent_fingerprint.py — `git diff --stat` shows no new imports or logic.
    - Test: `uv run just test-bucket fingerprint` still exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run just test-bucket fingerprint</automated>
  </verify>
  <done>The docstrings document why report_fingerprint_failed writes no row and where the durable marker lives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent -> control-plane API | `report_fingerprint_failed` is agent-authenticated; this plan asserts it writes no DB row (only clears the ledger). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-04-01 | Tampering | synthetic `engine='_task'` sentinel poisoning per-engine joins | mitigate | regression test asserts no synthetic-engine row is ever written by report_fingerprint_failed (D-18) |
| T-81-04-02 | Denial of Service | poison-file infinite fingerprint auto-retry | accept | FAILURE_IS_TERMINAL[fingerprint]=False means auto-retry is intentional; MAX_FINGERPRINT_ATTEMPTS bound is a Phase-82 deferred idea, no requirement asks for it here |
| T-81-04-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan (zero new deps for the phase) |
</threat_model>

<verification>
- `uv run just test-bucket fingerprint` green in isolation.
- `git diff src/phaze/routers/agent_fingerprint.py` shows docstring-only changes.
</verification>

<success_criteria>
- report_fingerprint_failed proven to persist no row and only clear the ledger.
- A failed per-engine fingerprint stays eligible (auto-retryable); joins unpoisoned.
- The asymmetry is documented.
</success_criteria>

<output>
Create `.planning/phases/81-per-stage-failure-persistence-retry-paths/81-04-SUMMARY.md` when done.
</output>
