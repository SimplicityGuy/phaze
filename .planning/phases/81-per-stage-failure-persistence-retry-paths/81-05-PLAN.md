---
phase: 81-per-stage-failure-persistence-retry-paths
plan: 05
type: execute
wave: 2
depends_on: [81-02]
files_modified:
  - src/phaze/routers/agent_analysis.py
  - tests/analyze/routers/test_agent_analysis_failure.py
autonomous: true
requirements: [FAIL-01]
must_haves:
  truths:
    - "D-05: report_analysis_failed dual-writes — it stamps analysis.failed_at + error_message AND keeps writing state = FileState.ANALYSIS_FAILED in the same transaction (three live files.state readers stay working until Phase 80/82; the state write dies in Phase 90)."
    - "D-06: report_analysis_failed clears analysis_completed_at when stamping failed_at (mutual exclusion), satisfying the CHECK; the upsert is INSERT..ON CONFLICT (file_id) DO UPDATE because a pure analyze failure has no prior analysis row."
    - "D-07: analysis.error_message = f'{reason}: {error}' from the existing AnalysisFailurePayload (Literal reason + bounded error), truncated to the column bound — no schema change."
    - "D-13: put_analysis unconditionally clears failed_at + error_message on success (not exclude_unset-driven) — this is also what makes the completion branch satisfy the D-06 CHECK."
    - "D-02: FAIL-01 is one of the two writers (with FAIL-02) that unblock Phase 80's recovery derivation from durable markers."
    - "D-04: the dual-write changes no file's derived status vs the Phase 78 predicates; the Phase 79 shadow gate stays green (state is still written; the marker is additive)."
  artifacts:
    - path: "src/phaze/routers/agent_analysis.py"
      provides: "report_analysis_failed dual-write + put_analysis clear-on-success"
      contains: "failed_at"
    - path: "tests/analyze/routers/test_agent_analysis_failure.py"
      provides: "no-prior-row failure, clears completed_at + CHECK holds, success-after-failure clears"
      contains: "report_analysis_failed"
  key_links:
    - from: "src/phaze/routers/agent_analysis.py"
      to: "analysis table"
      via: "pg_insert(...).on_conflict_do_update stamping failed_at + clearing analysis_completed_at"
      pattern: "on_conflict_do_update"
    - from: "src/phaze/routers/agent_analysis.py"
      to: "files.state readers"
      via: "state = FileState.ANALYSIS_FAILED still written (dual-write)"
      pattern: "ANALYSIS_FAILED"
---

<objective>
Make `report_analysis_failed` persist a durable analyze failure marker (`analysis.failed_at` +
`error_message`) while keeping the `state = ANALYSIS_FAILED` write, and make `put_analysis`
unconditionally clear that marker on success.

Purpose: today the analyze failure path writes only `FileState.ANALYSIS_FAILED` and no `failed_at`
(`agent_analysis.py:329`). Phase 80's recovery derives `failed(analyze)` from the marker (D-02), and
D-06's CHECK requires `analysis_completed_at` and `failed_at` to be mutually exclusive. The dual-write
(D-05) keeps three live `files.state` readers alive until Phases 80/82 cut over; the state write dies
in Phase 90. The backfill is already shipped in `032` (D-03) — this is the go-forward writer only.

Output: the dual-write + clear-on-success in `agent_analysis.py` and analyze-bucket tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-per-stage-failure-persistence-retry-paths/81-CONTEXT.md
@.planning/phases/81-per-stage-failure-persistence-retry-paths/81-RESEARCH.md
@.planning/phases/81-per-stage-failure-persistence-retry-paths/81-PATTERNS.md
@.planning/phases/81-per-stage-failure-persistence-retry-paths/81-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: report_analysis_failed dual-write (upsert failed_at + clear completed_at, keep state)</name>
  <read_first>
    - src/phaze/routers/agent_analysis.py (`report_analysis_failed` lines 310-337 — currently writes only `FileRecord.state = ANALYSIS_FAILED`, `clear_ledger_entry(process_file:...)` line 333, `_delete_staged_object_if_cloud` line 336, `commit` line 337; `put_analysis` upsert lines 197-210 + completion branch lines 234-241)
    - alembic/versions/032_add_derived_status_schema.py (`_BACKFILL_ANALYZE_FAILED` lines 73-82 — the INSERT..ON CONFLICT (file_id) DO UPDATE shape to reuse; the `'backfilled from ANALYSIS_FAILED'` placeholder style)
    - src/phaze/schemas/agent_analysis.py (`AnalysisFailurePayload` lines 114-127 — reason Literal + bounded error)
    - .planning/phases/81-per-stage-failure-persistence-retry-paths/81-RESEARCH.md (§ Discretion Items Resolved #1 txn order; § Open Questions #2 no-prior-row)
    - .planning/phases/81-per-stage-failure-persistence-retry-paths/81-02-PLAN.md (the D-06 CHECK this writer must satisfy)
  </read_first>
  <action>
    In `report_analysis_failed`, add an `analysis` upsert INTO THE SAME transaction, ordered (RESEARCH
    Discretion #1): (a) `pg_insert(Analysis).values([{"file_id": file_id, "id": uuid.uuid4(),
    "failed_at": <now>, "error_message": <msg>, "analysis_completed_at": None}]).on_conflict_do_update(
    index_elements=["file_id"], set_={"failed_at": <now>, "error_message": <msg>,
    "analysis_completed_at": None})` — use ON CONFLICT DO UPDATE because a pure analyze failure never
    wrote an `analysis` row (a bare UPDATE would silently no-op), and clear `analysis_completed_at` to
    satisfy the D-06 CHECK; (b) keep `FileRecord.state = FileState.ANALYSIS_FAILED` (D-05 dual-write);
    (c) `clear_ledger_entry`; (d) `_delete_staged_object_if_cloud`; (e) single `commit`. `<msg>` =
    `f"{body.reason}: {body.error}"` truncated to the `error_message` column bound (D-07). Use the
    real `analysis`/`Analysis` model name and `failed_at` timestamp expression that match the module's
    existing imports.
  </action>
  <acceptance_criteria>
    - Source: `report_analysis_failed` contains an `on_conflict_do_update` on `index_elements=["file_id"]` that sets `failed_at` and `error_message` and `analysis_completed_at` to None.
    - Source: `rg -n "ANALYSIS_FAILED" src/phaze/routers/agent_analysis.py` still shows the `state = FileState.ANALYSIS_FAILED` write (dual-write preserved).
    - Source: exactly one `session.commit()` remains in `report_analysis_failed` (same-transaction).
    - Test: `uv run mypy src/phaze/routers/agent_analysis.py` clean.
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy src/phaze/routers/agent_analysis.py</automated>
  </verify>
  <done>report_analysis_failed stamps failed_at + error_message, clears completed_at, keeps the state write, all in one transaction, with an upsert that handles the no-prior-row case.</done>
</task>

<task type="auto">
  <name>Task 2: put_analysis clear-on-success (unconditional failed_at/error_message clear)</name>
  <read_first>
    - src/phaze/routers/agent_analysis.py (`put_analysis` completion branch lines 234-241 — flips ANALYZED + stamps `analysis_completed_at=func.now()`; the upsert set_ built from `dumped` lines 199-210)
    - .planning/phases/81-per-stage-failure-persistence-retry-paths/81-RESEARCH.md (§ Common Pitfalls 1)
  </read_first>
  <action>
    In `put_analysis`'s success/completion path, add `failed_at=None, error_message=None`
    UNCONDITIONALLY to the upsert `set_` clause (outside `exclude_unset` — `failed_at` is never in the
    agent body). This makes a successful (re)analysis after a failure clear the stale marker and is
    also what makes the completion branch satisfy the D-06 CHECK (both columns cannot be non-NULL).
  </action>
  <acceptance_criteria>
    - Source: `put_analysis`'s success-path `set_` includes `failed_at` set to None and `error_message` set to None, not driven by `exclude_unset`.
    - Test: `uv run mypy src/phaze/routers/agent_analysis.py` clean.
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy src/phaze/routers/agent_analysis.py</automated>
  </verify>
  <done>put_analysis unconditionally clears failed_at + error_message on success.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests — failure with no prior row, CHECK holds, success-after-failure clears</name>
  <read_first>
    - existing tests/analyze/routers/ tests (the authed-agent client pattern; how an analysis row / file is seeded)
    - src/phaze/routers/agent_analysis.py (report_analysis_failed + put_analysis after Tasks 1-2)
    - .planning/phases/81-per-stage-failure-persistence-retry-paths/81-RESEARCH.md (§ FAIL-01 Test Map; § Open Questions #2)
  </read_first>
  <behavior>
    - `POST /analysis/{id}/failed` on a file with NO prior analysis row inserts one with `failed_at` NOT NULL, `error_message == "{reason}: {error}"`, `analysis_completed_at` NULL, `state == ANALYSIS_FAILED`, ledger cleared.
    - The same call on a previously-analyzed file clears `analysis_completed_at` and stamps `failed_at` (the D-06 CHECK holds — no mixed row is ever written).
    - A successful `put_analysis` after a failure clears `failed_at` and `error_message` and stamps `analysis_completed_at` (CHECK still holds).
  </behavior>
  <action>
    Create `tests/analyze/routers/test_agent_analysis_failure.py` covering the three behaviors. Include
    the "failed with no prior row" cell (RESEARCH OQ2) and an assertion that no row ever has both
    `analysis_completed_at` and `failed_at` set. Use the authed-agent client. Must pass in bucket
    isolation.
  </action>
  <acceptance_criteria>
    - Test: `uv run just test-bucket analyze` exits 0, in isolation.
    - Behavior: a test asserts the failure insert works with NO prior analysis row.
    - Behavior: a test asserts a success-after-failure clears `failed_at` and stamps `analysis_completed_at`.
    - Migration: `uv run just test-bucket integration` (the migration 033 CHECK from 81-02) still exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run just test-bucket analyze</automated>
  </verify>
  <done>The dual-write, no-prior-row upsert, CHECK invariant, and clear-on-success are proven in the analyze bucket in isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent -> control-plane API | `POST /analysis/{file_id}/failed` is agent-authenticated; the body carries agent-supplied `reason`/`error`. |
| free text -> Postgres -> render | `analysis.error_message` persists agent free text and is later rendered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-05-01 | Spoofing | forged agent/file identity via body | mitigate | `agent` from `Depends(get_authenticated_agent)`; ledger key from PATH `file_id` only, never the body (AUTH-01/T-45-05) |
| T-81-05-02 | Tampering | mixed row (both completed_at + failed_at) violating the invariant | mitigate | the writer clears `analysis_completed_at` when stamping failed_at; put_analysis clears failed_at on success; the D-06 DB CHECK (81-02) is the backstop |
| T-81-05-03 | Tampering | PG-invalid / oversized error free text | mitigate | `error` bounded via `AnalysisFailurePayload` (max_length=2000); `error_message` truncated to the column bound (D-07) |
| T-81-05-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan (zero new deps for the phase) |
</threat_model>

<verification>
- `uv run just test-bucket analyze` green in isolation.
- `uv run just test-bucket integration` green (migration 033 CHECK holds).
- `uv run mypy src/phaze/routers/agent_analysis.py` clean.
- Phase 79 shadow gate stays green (state still written; marker additive, D-04).
</verification>

<success_criteria>
- An analyze failure persists a durable marker (failed_at + error_message) with the state write kept.
- No mixed row is ever written; the D-06 CHECK holds.
- put_analysis clears the marker on success.
</success_criteria>

<output>
Create `.planning/phases/81-per-stage-failure-persistence-retry-paths/81-05-SUMMARY.md` when done.
</output>
