---
phase: 81-per-stage-failure-persistence-retry-paths
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/033_add_analysis_completed_xor_failed.py
  - src/phaze/models/analysis.py
  - tests/integration/test_migrations/test_migration_033_additive_check.py
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/milestones/PARALLEL-ENRICH-DAG-DESIGN.md
autonomous: true
requirements: [FAIL-01]
must_haves:
  truths:
    - "D-06: a DB CHECK (NOT (analysis_completed_at IS NOT NULL AND failed_at IS NOT NULL)) is added to analysis and mirrored into the ORM __table_args__ so alembic autogenerate still yields an empty diff."
    - "D-08: Phase 81 ships migration 033 (revision='033', down_revision='032'); the destructive Phase-90 migration is renumbered 033->034 in every planning doc that names it."
    - "D-09: the mixed-row cleanup UPDATE (clears failed_at, keeps analysis_completed_at — done wins) runs BEFORE create_check_constraint, or the migration aborts on the live-corpus mixed rows that 032's unguarded backfill produced."
    - "D-04: the D-09 cleanup keeps done on every mixed row so no file's derived status changes and the Phase 79 shadow gate stays green."
  artifacts:
    - path: "alembic/versions/033_add_analysis_completed_xor_failed.py"
      provides: "cleanup UPDATE then CHECK constraint; DDL-only downgrade"
      contains: "create_check_constraint"
    - path: "src/phaze/models/analysis.py"
      provides: "CheckConstraint mirrored into __table_args__"
      contains: "analysis_completed_xor_failed"
    - path: "tests/integration/test_migrations/test_migration_033_additive_check.py"
      provides: "autogenerate-empty + mixed-row-cleaned-before-CHECK + round-trip assertions"
      contains: "analysis_completed_xor_failed"
  key_links:
    - from: "alembic/versions/033_add_analysis_completed_xor_failed.py"
      to: "analysis table"
      via: "op.execute cleanup UPDATE then op.create_check_constraint"
      pattern: "create_check_constraint"
    - from: "src/phaze/models/analysis.py"
      to: "alembic autogenerate"
      via: "CheckConstraint in __table_args__ (empty-diff contract)"
      pattern: "CheckConstraint"
---

<objective>
Ship migration `033`: clean the live-corpus mixed `analysis` rows, then add the
`analysis_completed_at` XOR `failed_at` CHECK, mirror it into the ORM, and renumber the *destructive*
(Phase 90) migration references `033 -> 034` across the planning docs.

Purpose: FAIL-01's analyze failure marker (`analysis.failed_at`) needs mutual exclusion with
`analysis_completed_at` enforced at the DB (D-06). `032`'s unguarded backfill already created mixed
rows in the live corpus (D-09), so the cleanup MUST precede the CHECK or the migration aborts.
Alembic numbering is sequential — `033` is genuinely next; Phase 90's destructive migration becomes
`034` (D-08), and its doc references move with it.

Output: `alembic/versions/033_*.py`, a CHECK mirror in `models/analysis.py`, a new migration test
copying the `032` empty-diff template, and the renumbered planning docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-per-stage-failure-persistence-retry-paths/81-CONTEXT.md
@.planning/phases/81-per-stage-failure-persistence-retry-paths/81-RESEARCH.md
@.planning/phases/81-per-stage-failure-persistence-retry-paths/81-PATTERNS.md
@alembic/versions/032_add_derived_status_schema.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 033 (cleanup UPDATE before CHECK) + mirror CHECK into models/analysis.py</name>
  <read_first>
    - alembic/versions/032_add_derived_status_schema.py (the full template — static-SQL constants, `op.execute(sa.text(...))`, `_BACKFILL_ANALYZE_FAILED` `ON CONFLICT DO UPDATE` with NO completed_at guard lines 73-82 that IS the mixed-row source, the `status_enum` bare-name comment lines 66-67, DDL-only downgrade lines 167-186)
    - src/phaze/models/analysis.py (existing `__table_args__` with two partial indexes lines 49-52)
    - src/phaze/models/pipeline_stage_control.py (named CHECK precedent: `CheckConstraint("priority BETWEEN 0 AND 100", name="priority_range")`)
    - .planning/phases/81-per-stage-failure-persistence-retry-paths/81-RESEARCH.md (§ Migration 033 Mechanics)
  </read_first>
  <action>
    Create `alembic/versions/033_add_analysis_completed_xor_failed.py` with `revision = "033"`,
    `down_revision = "032"`. In `upgrade()`, ORDER IS MANDATORY (D-09): FIRST
    `op.execute(sa.text("UPDATE analysis SET failed_at = NULL WHERE analysis_completed_at IS NOT NULL
    AND failed_at IS NOT NULL"))` (clears failed_at, keeps analysis_completed_at so done wins —
    matches `_analyze_status` done≻failed precedence, D-04); THEN `op.create_check_constraint(
    "analysis_completed_xor_failed", "analysis", "NOT (analysis_completed_at IS NOT NULL AND
    failed_at IS NOT NULL)")` passing the BARE name (the `ck_%(table_name)s_%(constraint_name)s`
    naming convention re-prefixes it to `ck_analysis_analysis_completed_xor_failed` — passing an
    already-prefixed name double-prefixes). In `downgrade()`,
    `op.drop_constraint("analysis_completed_xor_failed", "analysis", type_="check")`; do NOT reverse
    the D-09 UPDATE (016/032 best-effort-DDL precedent). Mirror the CHECK into `models/analysis.py`
    `__table_args__` as a third element: `CheckConstraint("NOT (analysis_completed_at IS NOT NULL AND
    failed_at IS NOT NULL)", name="analysis_completed_xor_failed")` (import `CheckConstraint` from
    sqlalchemy) so `alembic revision --autogenerate` stays empty (D-06 / 77-precedent). Do NOT touch
    this phase's own new `033` file when renumbering (that is Task 3).
  </action>
  <acceptance_criteria>
    - Source: `alembic/versions/033_add_analysis_completed_xor_failed.py` has `revision = "033"` and `down_revision = "032"`.
    - Source: in `upgrade()`, the `UPDATE analysis SET failed_at = NULL` string appears at a line number LESS THAN the `create_check_constraint(` line (cleanup precedes CHECK).
    - Source: `src/phaze/models/analysis.py` contains `CheckConstraint(` with `name="analysis_completed_xor_failed"`.
    - Migration: `uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head</automated>
  </verify>
  <done>Migration 033 applies the cleanup then the CHECK, round-trips down/up, and the ORM mirror keeps autogenerate empty.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test_migration_033 (autogenerate-empty + mixed-row-cleaned-before-CHECK + round-trip)</name>
  <read_first>
    - tests/integration/test_migrations/test_migration_032_additive_schema.py (the FULL template — `test_revision_identifiers_are_bare_numbers` lines 91-96, `test_migration_never_references_saq_jobs` lines 99-103, the autogenerate-emptiness assertion via `compare_metadata(ctx, Base.metadata)` filtered to the migration's objects lines 139-162 / 245-248)
    - tests/integration/test_migrations/conftest.py (`_build_alembic_config`, `upgrade_to`, `downgrade_to`; migrations DB is `phaze_migrations_test` / `MIGRATIONS_TEST_DATABASE_URL`)
    - alembic/versions/033_add_analysis_completed_xor_failed.py (from Task 1)
  </read_first>
  <behavior>
    - `revision == "033"` and `down_revision == "032"` (bare numbers).
    - After upgrade, `compare_metadata(ctx, Base.metadata)` is EMPTY for the CHECK object (empty-diff contract).
    - A pre-seeded mixed row (both `analysis_completed_at` and `failed_at` set) is cleaned BEFORE the CHECK: after upgrade its `failed_at IS NULL` and `analysis_completed_at` is retained.
    - The rendered constraint name is `ck_analysis_analysis_completed_xor_failed`.
    - down/up round-trips without error.
  </behavior>
  <action>
    Create `tests/integration/test_migrations/test_migration_033_additive_check.py` by copying the
    `032` test structure. Scope the autogenerate-object filter to the new CHECK constraint. Seed a
    mixed `analysis` row BEFORE the migration head under test is applied, upgrade, and assert
    `failed_at IS NULL` with `analysis_completed_at` retained (proves the D-09 ordering). Assert the
    rendered constraint name equals `ck_analysis_analysis_completed_xor_failed`. Runs in the
    `integration` bucket against `phaze_migrations_test`.
  </action>
  <acceptance_criteria>
    - Source: `rg -c "analysis_completed_xor_failed" tests/integration/test_migrations/test_migration_033_additive_check.py` returns >= 1.
    - Test: `uv run just test-bucket integration` exits 0, in isolation.
    - Behavior: the test asserts a pre-seeded mixed row ends with `failed_at IS NULL` and `analysis_completed_at` non-NULL after upgrade.
  </acceptance_criteria>
  <verify>
    <automated>uv run just test-bucket integration</automated>
  </verify>
  <done>The migration is proven: empty autogenerate diff, mixed row cleaned before the CHECK, correct constraint name, round-trip clean.</done>
</task>

<task type="auto">
  <name>Task 3: Renumber the destructive migration 033 -> 034 across planning docs</name>
  <read_first>
    - .planning/phases/81-per-stage-failure-persistence-retry-paths/81-RESEARCH.md (§ D-08 doc renumber — the CORRECTED enumeration: ROADMAP lines 21/25/36/281/485 AND the MISSED 492/494; REQUIREMENTS MIG-04 line 98 carries "033", MIG-02 line 96 does NOT)
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/milestones/PARALLEL-ENRICH-DAG-DESIGN.md
  </read_first>
  <action>
    Run `rg -n '\b033\b' .planning/ROADMAP.md .planning/REQUIREMENTS.md
    .planning/milestones/PARALLEL-ENRICH-DAG-DESIGN.md` and renumber ONLY the references to the
    *destructive* (Phase 90) migration from `033` to `034` (D-08). Do NOT trust the CONTEXT line list
    (it missed ROADMAP 492/494 and over-listed REQUIREMENTS MIG-02) — grep and fix what is actually
    found. Do NOT rename this phase's own new `033` migration file or its test. After the edit, the
    only remaining literal `033` references in those docs must point at Phase 81's additive migration
    (Phase 79's "before 033" ordering line at ROADMAP:25 changes to "before 034" since it refers to
    the destructive migration). Confirm no destructive-migration `033` string remains.
  </action>
  <acceptance_criteria>
    - Behavior: `rg -n '\b033\b' .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/milestones/PARALLEL-ENRICH-DAG-DESIGN.md` returns no line that describes the DESTRUCTIVE/Phase-90 migration (every remaining `033`, if any, refers to Phase 81's additive migration).
    - Behavior: `rg -n 'Destructive Migration `?034`?|Migration `?034`?' .planning/ROADMAP.md .planning/REQUIREMENTS.md` returns the renumbered Phase 90 references.
    - Guard: `uv run just docs-drift` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run just docs-drift</automated>
  </verify>
  <done>Every destructive-migration reference reads 034; the additive 033 file/test are untouched; docs-drift passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| alembic -> live corpus | The migration runs `ACCESS EXCLUSIVE`-locking DDL and a data-mutating UPDATE against production `analysis` rows on deploy. No untrusted request input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-02-01 | Denial of Service | `create_check_constraint` on a pre-existing mixed row | mitigate | D-09 cleanup UPDATE runs FIRST; without it the CHECK validation aborts the whole migration on the live corpus |
| T-81-02-02 | Tampering | data-loss on the cleanup UPDATE | mitigate | cleanup clears only `failed_at` on rows where `analysis_completed_at` is already set (done wins, D-04); no done marker is lost; downgrade intentionally does not reverse (best-effort DDL precedent) |
| T-81-02-03 | Repudiation | schema drift (ORM vs migration) | mitigate | CHECK mirrored into `__table_args__`; the migration test asserts an EMPTY autogenerate diff so the two cannot diverge silently |
| T-81-02-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan (zero new deps for the phase) |
</threat_model>

<verification>
- `uv run alembic upgrade head && uv run alembic downgrade -1 && uv run alembic upgrade head` clean.
- `uv run just test-bucket integration` green (migration 033 test).
- `uv run just docs-drift` green after the renumber.
- `uv run mypy src/phaze/models/analysis.py` clean.
</verification>

<success_criteria>
- Migration 033 exists, cleanup precedes CHECK, round-trips.
- CHECK mirrored into the ORM; autogenerate diff empty.
- Destructive migration references renumbered 033->034; docs-drift green.
</success_criteria>

<output>
Create `.planning/phases/81-per-stage-failure-persistence-retry-paths/81-02-SUMMARY.md` when done.
</output>
