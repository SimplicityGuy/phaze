---
phase: 64-per-module-coverage-uplift-gate-raise
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/review/services/test_review_degrade.py
  - tests/agents/services/test_agent_liveness.py
autonomous: true
requirements: [COV-01]

must_haves:
  truths:
    - "services/review.py combined coverage is >= 85% (was 83.16%, the only sub-floor module)"
    - "Each review.py degrade branch returns [] AND emits its named warning when its DB call raises"
    - "The review.py formatters return the documented strings for None/PB/no-bitrate inputs"
    - "agent_liveness.classify_compute_lanes degrade branch returns (IDLE, 0) on SQLAlchemyError"
  artifacts:
    - path: "tests/review/services/test_review_degrade.py"
      provides: "Behavior-asserting degrade + formatter tests raising review.py above 85% (D-07)"
    - path: "tests/agents/services/test_agent_liveness.py"
      provides: "Extended with the classify_compute_lanes SQLAlchemyError degrade test (margin, D-07)"
  key_links:
    - from: "tests/review/services/test_review_degrade.py"
      to: "phaze.services.review"
      via: "raising-stub session → assert [] + caplog degrade key"
      pattern: "_degraded"
---

<objective>
Raise `services/review.py` above the 85% per-module floor with BEHAVIOR-ASSERTING tests
(D-07), and add optional margin to `services/agent_liveness.py` (85.42%, sitting exactly on
the floor). Per the authoritative combined-coverage map (RESEARCH §Re-Baselining),
`services/review.py` at 83.16% is the ONLY module below the floor — the stale "worst offender"
list (shell/pipeline/tracklists/agent_liveness at 12–70%) is a no-DB measurement artifact;
those modules are all ≥90% combined. Do NOT write tests for them (RESEARCH Pitfall 2).

D-06: the 85% floor IS the scope — review.py clears it and the phase's uplift is done.
D-08 (behavior-preserving seams): none needed — every gap is an already-testable degrade
or pure-formatter branch (RESEARCH §D-08 seams likely unnecessary).

Purpose: COV-01 — close the single real floor gap with observable-outcome tests.
Output: one new review-bucket test file + one extended agents-bucket test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-per-module-coverage-uplift-gate-raise/64-RESEARCH.md
@.planning/phases/64-per-module-coverage-uplift-gate-raise/64-PATTERNS.md

<interfaces>
<!-- review.py degrade branches + log keys (VERIFIED this session in src/phaze/services/review.py) -->
get_pending_proposal_rows(session)  → except → logger.warning("pending_proposal_rows_degraded", exc_info=True) → return []
get_tagwrite_review_rows(session)   → except → logger.warning("tagwrite_review_rows_degraded", exc_info=True) → return []
get_dedupe_groups(session)          → except → logger.warning("dedupe_groups_degraded", exc_info=True) → return []
get_cue_review_cards(session)       → except → logger.warning("cue_review_cards_degraded", exc_info=True) → return []
  (each opens `async with session.begin_nested():` as the first thing inside the try)
_format_size(num_bytes: int | None) -> str   # None/0 → "unknown size"; loop over B..TB then "… PB"
_format_quality(file_dict) -> str            # "320 kbps · <size>" when bitrate present, else just <size>
<!-- agent_liveness.py margin target (VERIFIED) -->
classify_compute_lanes(session)     → except SQLAlchemyError → rollback → return ("IDLE", 0)
  (rollback-failure log key: compute_lane_liveness_rollback_failed)
<!-- caplog works because autouse _route_structlog_through_stdlib (tests/conftest.py L96-97) routes structlog→stdlib -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Behavior-asserting review.py degrade + formatter tests</name>
  <files>tests/review/services/test_review_degrade.py</files>
  <read_first>
    - src/phaze/services/review.py (the module under test — confirm each `async with session.begin_nested():` entry point at ~L61/104/173/220 and the exact `_format_size`/`_format_quality` bodies at L139-158)
    - tests/review/services/test_proposal_queries.py (co-located review-bucket service-test conventions: `from __future__ import annotations`, TYPE_CHECKING guard on AsyncSession, `@pytest.mark.asyncio`)
    - tests/shared/services/test_pipeline.py (service-fn-via-session structural analog)
    - .planning/phases/64-per-module-coverage-uplift-gate-raise/64-PATTERNS.md (§test_review_degrade.py — exact lines + log-key table + the raising-stub note)
    - .planning/phases/64-per-module-coverage-uplift-gate-raise/64-RESEARCH.md (lines 217-220 degrade-path template; lines 340-356 formatter template)
    - tests/conftest.py (the autouse `_route_structlog_through_stdlib` fixture so caplog captures structlog)
  </read_first>
  <action>
    Create `tests/review/services/test_review_degrade.py` (bucket: review — RESEARCH Pitfall 6). Cover the
    six sub-floor branches from the 64-PATTERNS.md table, each with an OBSERVABLE outcome (D-07 — never
    "call it and assert no exception", which RESEARCH Pitfall 4 flags as a defect):
    - FOUR degrade tests, one each for `get_pending_proposal_rows`, `get_tagwrite_review_rows`,
      `get_dedupe_groups`, `get_cue_review_cards`. Inject a tiny stub session whose `begin_nested` raises
      (verify against the real `async with session.begin_nested():` call site — the stub's `begin_nested`
      raising synchronously enters the `except`). Assert BOTH `result == []` AND that the named warning key
      (`pending_proposal_rows_degraded` / `tagwrite_review_rows_degraded` / `dedupe_groups_degraded` /
      `cue_review_cards_degraded`) appears in `caplog.records` via `r.getMessage()` (wrap the call in
      `with caplog.at_level(logging.WARNING):`).
    - TWO formatter tests (pure return-value assertions, no seam): `_format_size(None) == "unknown size"`,
      `_format_size(0) == "unknown size"` (L142 falsy guard), `_format_size(22_400_000).endswith(" MB")`,
      `_format_size(2**60).endswith(" PB")` (L148 loop-exhaustion); `_format_quality({"file_size":22_400_000,
      "bitrate":320}).startswith("320 kbps · ")` and `"kbps" not in _format_quality({"file_size":22_400_000})`
      (L156 no-bitrate).
    Use `from __future__ import annotations` + `@pytest.mark.asyncio` on async tests (asyncio_mode=auto).
    Do NOT hand-roll app bootstrapping (RESEARCH Don't-Hand-Roll) — the degrade tests use raising stubs, not
    the real `session` fixture. Tests must pass in ISOLATION via `just test-bucket review` (RESEARCH Pitfall 5).
  </action>
  <verify>
    <automated>uv run pytest tests/review/services/test_review_degrade.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/review/services/test_review_degrade.py -q` exits 0.
    - Each of the four degrade tests asserts `result == []` AND the specific `*_degraded` warning key in caplog.
    - Formatter tests assert exact/`endswith`/`startswith` return values for None, 0, MB, PB, and no-bitrate inputs.
    - After this file lands, `services/review.py` combined coverage ≥ 85% (verify via the coverage command below).
  </acceptance_criteria>
  <done>services/review.py clears the 85% floor via behavior-asserting degrade + formatter tests in the review bucket.</done>
</task>

<task type="auto">
  <name>Task 2: Margin test for agent_liveness.classify_compute_lanes degrade branch</name>
  <files>tests/agents/services/test_agent_liveness.py</files>
  <read_first>
    - src/phaze/services/agent_liveness.py (lines 174-180 — the `classify_compute_lanes` SQLAlchemyError branch: rollback → `return ("IDLE", 0)`; rollback-failure log key `compute_lane_liveness_rollback_failed` at ~L179)
    - tests/agents/services/test_agent_liveness.py (the file to EXTEND — today covers only pure `classify`/`sort_key`; has no session-fixture test yet)
    - .planning/phases/64-per-module-coverage-uplift-gate-raise/64-PATTERNS.md (§test_agent_liveness.py — optional margin, degrade-path pattern reuse)
  </read_first>
  <action>
    Append ONE `@pytest.mark.asyncio` degrade test to the existing `tests/agents/services/test_agent_liveness.py`
    (agents bucket). Inject a session whose `.execute` raises `sqlalchemy.exc.SQLAlchemyError` and assert
    `await classify_compute_lanes(<stub>)` returns the tuple `("IDLE", 0)` (observable return, D-07). This
    covers lines 174-180 and buys margin for the module that sits exactly on the 85.42% floor. Keep the existing
    pure-function tests untouched. This is OPTIONAL margin (D-05 headroom), NOT floor-clearing — agent_liveness
    already passes 85%; it is included because it is a real degrade branch with an observable outcome (not padding).
    Follow the same raising-stub + assert-return pattern as the review degrade tests. Must pass in isolation via
    `just test-bucket agents`.
  </action>
  <verify>
    <automated>uv run pytest tests/agents/services/test_agent_liveness.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/agents/services/test_agent_liveness.py -q` exits 0 including the new degrade test.
    - The new test asserts the `("IDLE", 0)` return tuple on an injected `SQLAlchemyError` (observable outcome).
    - Pre-existing `classify`/`sort_key` tests remain green and unmodified.
  </acceptance_criteria>
  <done>agent_liveness.classify_compute_lanes SQLAlchemyError degrade path is behavior-asserted, buying margin above the 85% floor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → service under test | Tests inject raising stubs; no real trust boundary crossed (test-only, no runtime behavior change per milestone constraint). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-04 | Tampering (test quality) | new tests could coverage-pad (D-07 violation) | mitigate | Every test asserts an observable outcome — `[]`/`("IDLE",0)` return AND the named `caplog` warning key, or exact formatter return values. No "assert no exception" tests. Reviewer flags padding as a defect (D-07). |
| T-64-05 | Tampering (behavior change) | src/phaze under a "no backend behavior change" milestone | accept | This plan is TEST-ONLY (no `src/phaze/**` edits); no D-08 seam needed (RESEARCH: gaps are already-testable branches). Zero behavior delta by construction. |
</threat_model>

<verification>
- `uv run pytest tests/review/services/test_review_degrade.py tests/agents/services/test_agent_liveness.py -q` green.
- Re-measure per-module coverage the RESEARCH way (fastest local path): `just test-db`, export the TEST_DATABASE_URL/DATABASE_URL/PHAZE_REDIS_URL vars (64-RESEARCH.md lines 99-108), `uv run pytest tests/ -q --cov=phaze --cov-report= --cov-fail-under=0`, `uv run coverage report --sort=cover | grep review.py`, `just test-db-down` — confirm `services/review.py` ≥ 85%.
- Isolation: `just test-bucket review` and `just test-bucket agents` both green (RESEARCH Pitfall 5).
</verification>

<success_criteria>
- `services/review.py` combined coverage ≥ 85% (D-04/D-06 — the only sub-floor module cleared) via behavior-asserting tests (D-07).
- `services/agent_liveness.py` has margin above the floor via a real degrade-branch test.
- No `src/phaze/**` changes (no D-08 seam needed); milestone "no backend behavior change" preserved.
</success_criteria>

<output>
Create `.planning/phases/64-per-module-coverage-uplift-gate-raise/64-02-SUMMARY.md` when done.
</output>
