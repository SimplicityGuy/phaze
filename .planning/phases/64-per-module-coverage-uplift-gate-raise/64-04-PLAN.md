---
phase: 64-per-module-coverage-uplift-gate-raise
plan: 04
type: execute
wave: 3
depends_on: ["64-03"]
files_modified: []
autonomous: false
requirements: [COV-02]

must_haves:
  truths:
    - "The main-branch required status check gates on the combine job (or aggregate-results), so a red coverage-combine blocks merge"
    - "The raised gate + per-module floor are enforcing (fail-closed), not advisory"
  artifacts: []
  key_links:
    - from: "GitHub branch protection (main)"
      to: "the combine / aggregate-results CI job"
      via: "required_status_checks contexts"
      pattern: "combine|aggregate-results"
---

<objective>
Confirm the raised gate and per-module floor are ENFORCING, not advisory (COV-02 "wired into
CI so future coverage regressions fail the build"). RESEARCH Open-Q1 / Assumption A3 flag an
open risk: Phase 63 intended the branch-protection required check to be a stable aggregate/
combine job, but "set required check = `aggregate-results`" is a DEFERRED post-Phase-63 chore
(MEMORY: Phase 63 UAT #11). If the required check does not gate on the combine result, a red
`just coverage-combine` (from either the global gate or the floor script) would NOT block merge
— making this whole phase's machinery advisory.

Purpose: COV-02 — verify (and, if needed, direct the operator to set) that a failing combine
job is a merge-blocking required check.
Output: a verified fail-closed CI gate; no repo files change (GitHub settings + confirmation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-per-module-coverage-uplift-gate-raise/64-RESEARCH.md
@.github/workflows/tests.yml
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read current main-branch required status checks via gh api</name>
  <files>(none — read-only inspection)</files>
  <read_first>
    - .github/workflows/tests.yml (the `combine` job name/id — the job that runs `just coverage-combine`)
    - .github/workflows/ci.yml (the `aggregate-results` skip-with-success contract from Phase 63)
    - .planning/phases/64-per-module-coverage-uplift-gate-raise/64-RESEARCH.md (lines 244-246 gate-wiring; Open-Q1 / A3)
  </read_first>
  <action>
    Query the live main-branch protection to see which status checks are REQUIRED:
    `gh api repos/{owner}/{repo}/branches/main/protection/required_status_checks` (resolve owner/repo via
    `gh repo view --json nameWithOwner`). List the `contexts` / `checks[].context` entries. Determine whether
    the combine job (the one running `just coverage-combine` in tests.yml) OR the Phase 63 `aggregate-results`
    job is among the required contexts. The endpoint LEGITIMATELY 404s ("Branch not protected") when no
    branch protection is configured — that is a definite answer (a GAP), NOT a tooling failure. Distinguish it
    from a `gh` auth/CLI failure (401/no token/network), which is an INCONCLUSIVE read. Capture the raw response
    verbatim. Record in the SUMMARY: the current required contexts (or the explicit 404 "not protected"), and a
    clear PASS (combine/aggregate-results IS required → gate is enforcing) or GAP (it is NOT, or unprotected →
    gate is advisory, operator action needed) determination. This is the automatable half of the verification
    (automation-first).
  </action>
  <verify>
    <automated>REPO=$(gh repo view --json nameWithOwner -q .nameWithOwner) &amp;&amp; OUT=$(gh api "repos/$REPO/branches/main/protection/required_status_checks" 2>&amp;1); echo "$OUT" | grep -qiE '"contexts"|"checks"|"strict"|Not Found|not protected' &amp;&amp; { echo "definite answer: $OUT"; exit 0; } || { echo "INCONCLUSIVE gh auth/CLI failure: $OUT"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the exact current required status-check contexts for main (or the explicit 404 "not protected" GAP).
    - A PASS/GAP determination is stated: is the combine (or aggregate-results) job a required check?
    - The verify command distinguishes a definite answer (HTTP 200 contexts OR HTTP 404 "not protected") from an inconclusive `gh` auth/CLI failure.
  </acceptance_criteria>
  <done>Current main-branch required checks are captured (definite 200-or-404 answer, not a CLI failure) and a PASS/GAP verdict on combine-gating is recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm the combine job is a merge-blocking required check</name>
  <action>
    Human verifies (and sets, if needed) that the main-branch required status check gates on the combine /
    aggregate-results job so a red `just coverage-combine` (global gate OR per-module floor breach) blocks merge.
    See what-built / how-to-verify. If the Task 1 read showed a GAP, the operator sets the required check in
    GitHub branch-protection settings (repo-admin — not automatable from here) or explicitly defers it.

    SUMMARY-recording contract (both outcomes must be recorded unambiguously):
    - On "approved": record that the combine/`aggregate-results` context is now a merge-blocking required check
      → COV-02 fully delivered (machinery + CI enforcement).
    - On "defer": COV-02 is only HALF delivered. The gate/floor machinery ships and runs, but its CI-ENFORCEMENT
      half remains ADVISORY-ONLY (a red `coverage-combine` does not yet block merge). The SUMMARY MUST state this
      explicitly and MUST NOT imply full COV-02 completion. Mirror the Phase 63 UAT #11 tracked-chore language:
      add a "set GitHub branch-protection required-check = combine/aggregate-results" item to STATE.md Deferred
      Items AND surface it in MEMORY, exactly as Phase 63 carried its branch-protection chore forward. The phase
      may complete, but ROADMAP Success Criterion #3 (unconditional merge-blocking gate) is recorded as
      NOT-YET-MET pending that tracked follow-up.
  </action>
  <what-built>
    The two-guardrail coverage gate is wired into `just coverage-combine` (raised global `fail_under` > 90.38
    + `scripts/coverage_floor.py` per-module 85% floor), and Task 1 read the current main-branch required
    status checks via `gh api`.
  </what-built>
  <how-to-verify>
    1. Review the Task 1 SUMMARY finding: are the required status checks on `main` gating on the combine job
       (or `aggregate-results`)?
    2. If PASS (it is required): confirm a red `combine` job would block merge — optionally eyeball a recent PR's
       checks, or trust the recorded `required_status_checks` contexts. Reply "approved".
    3. If GAP (it is NOT required, or protection was unreadable): set the branch-protection required check to the
       combine/`aggregate-results` context in GitHub → Settings → Branches → main (the deferred Phase 63 UAT #11
       chore), then confirm. This requires repo-admin rights (a genuine human/GitHub-settings action, not
       automatable from here). Reply "approved" once the required check gates on the combine result — or reply
       "defer" to explicitly accept it as advisory-for-now and carry the GAP forward as a tracked chore (the
       executor will then record COV-02's enforcement half as advisory-only per the SUMMARY-recording contract).
  </how-to-verify>
  <resume-signal>Type "approved" (gate enforcing / now set → COV-02 fully delivered) or "defer" (accept advisory, executor records COV-02 enforcement half as a tracked chore)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR merge → required status checks | Branch protection is the enforcement point; if the combine job is not a required context, the coverage gate is advisory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-07 | Repudiation/Elevation | branch protection not gating on the combine result → coverage regressions merge unblocked (fail-open at the policy layer) | mitigate | Task 1 reads `required_status_checks` via `gh api` (distinguishing a definite 200/404 answer from a CLI failure); the human checkpoint confirms/sets the required check so a red `coverage-combine` blocks merge (closes RESEARCH A3 / Open-Q1). A "defer" outcome is an explicit, tracked acceptance recorded in STATE/MEMORY as an unmet Success Criterion #3 — not a silent gap. |
</threat_model>

<verification>
- `gh api .../branches/main/protection/required_status_checks` lists the combine (or aggregate-results) context as required, OR the operator sets it during the checkpoint.
- A failing `just coverage-combine` (global gate or floor breach) surfaces as a red REQUIRED check that blocks merge.
</verification>

<success_criteria>
- EITHER: the raised gate + per-module floor are confirmed fail-closed at the CI/branch-protection layer (a red combine blocks merge) → COV-02 fully delivered.
- OR: the residual gap is explicitly deferred with operator sign-off, AND the executor has recorded — mirroring Phase 63's UAT #11 tracked-chore language — that COV-02's CI-enforcement half is advisory-only (machinery ships, merge-blocking not yet set), as a tracked follow-up in STATE.md Deferred Items + MEMORY. Phase completion under "defer" must NOT be read as full COV-02 / Success Criterion #3 delivery.
</success_criteria>

<output>
Create `.planning/phases/64-per-module-coverage-uplift-gate-raise/64-04-SUMMARY.md` when done.
</output>
