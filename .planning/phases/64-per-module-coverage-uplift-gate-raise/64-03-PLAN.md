---
phase: 64-per-module-coverage-uplift-gate-raise
plan: 03
type: execute
wave: 2
depends_on: ["64-01", "64-02"]
files_modified:
  - justfile
  - pyproject.toml
  - tests/shared/test_coverage_gate.py
autonomous: true
requirements: [COV-01, COV-02]

must_haves:
  truths:
    - "just coverage-combine emits coverage.json, enforces the raised global gate, AND runs the per-module floor script"
    - "The global fail_under is an integer strictly > 90.38 (pinned from the measured post-uplift overall minus ~1)"
    - "pyproject.toml fail_under equals the justfile coverage-combine --fail-under value"
    - "A guard test fails if the two gate numbers drift or drop to/below 90.38"
  artifacts:
    - path: "justfile"
      provides: "coverage-combine wired: coverage json + raised --fail-under + coverage_floor.py invocation (D-02)"
      contains: "coverage_floor.py"
    - path: "pyproject.toml"
      provides: "[tool.coverage.report] fail_under raised above 90.38 (D-05)"
    - path: "tests/shared/test_coverage_gate.py"
      provides: "Guard asserting the two gate sites agree and are > 90.38 (RESEARCH Pitfall 3)"
  key_links:
    - from: "justfile coverage-combine"
      to: "scripts/coverage_floor.py"
      via: "uv run python scripts/coverage_floor.py after coverage json"
      pattern: "coverage_floor"
    - from: "tests/shared/test_coverage_gate.py"
      to: "pyproject.toml + justfile"
      via: "tomllib + _extract_recipe → assert equal AND > 90.38"
      pattern: "fail.under"
---

<objective>
Raise the enforced global coverage gate above the 90.38% baseline (D-05) and wire the
per-module floor script (built in 64-01) into the single combine seam so both guardrails
(D-01) run on the authoritative COMBINED coverage in CI. Add a guard test so the two gate
edit sites cannot silently drift (RESEARCH Pitfall 3).

D-05: the global gate = measured post-uplift overall minus ~1 point, pinned at execute time,
strictly > 90.38, low-90s-or-higher. Combined overall measured 96.89% pre-uplift → after the
64-02 review.py uplift it is ≥ that → recommend integer 95 (~1.5–2pt headroom). Re-measure
CI-faithfully at execute time and pin the exact digit.

Purpose: COV-02 (raised gate wired into CI so regressions fail the build) + COV-01 (the
per-module floor invocation lands here in the combine recipe, D-02).
Output: edited `justfile` + `pyproject.toml` + new `tests/shared/test_coverage_gate.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-per-module-coverage-uplift-gate-raise/64-RESEARCH.md
@.planning/phases/64-per-module-coverage-uplift-gate-raise/64-PATTERNS.md
@.planning/phases/64-per-module-coverage-uplift-gate-raise/64-01-SUMMARY.md
@.planning/phases/64-per-module-coverage-uplift-gate-raise/64-02-SUMMARY.md

<interfaces>
<!-- current justfile coverage-combine recipe (VERIFIED L107-110) -->
coverage-combine:
    uv run coverage combine
    uv run coverage xml
    uv run coverage report --fail-under=85
<!-- current pyproject (VERIFIED): [tool.coverage.report] fail_under = 85 (L68) -->
<!-- DO NOT touch test-bucket (L102-103, --cov-fail-under=0 is deliberate: a shard is partial) -->
<!-- guard-test analog helper: tests/shared/test_ci_workflow_wiring.py::_extract_recipe (L46-55), _REPO_ROOT parents[2] (L40) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure post-uplift overall, pin the gate, wire the combine recipe</name>
  <files>justfile, pyproject.toml</files>
  <read_first>
    - justfile (the `coverage-combine` recipe L107-110 AND the `test-bucket` recipe L95-103 + its comment — confirm you must NOT touch test-bucket's --cov-fail-under=0)
    - pyproject.toml (`[tool.coverage.report] fail_under = 85` at L68)
    - scripts/coverage_floor.py (must exist from 64-01 — this is what the recipe invokes)
    - .planning/phases/64-per-module-coverage-uplift-gate-raise/64-RESEARCH.md (lines 110-119 CI-faithful reproduce commands; lines 182-191 recipe edit; lines 238-249 the two edit sites + D-05 number)
    - .planning/phases/64-per-module-coverage-uplift-gate-raise/64-PATTERNS.md (§Config edit sites — exact instructions, keep-consistent rule)
  </read_first>
  <action>
    First MEASURE the authoritative combined overall AFTER the 64-02 uplift, the CI-faithful way (64-RESEARCH.md
    lines 110-118): `just test-db` then run each bucket into `.coverage.<bucket>` for buckets discovery metadata
    fingerprint analyze identify review agents integration shared, then `uv run coverage combine`, `uv run coverage
    json -o coverage.json`, `uv run coverage report --sort=cover`; read `totals.percent_covered`. Pin NEW_GLOBAL =
    the integer floor of (measured overall − ~1), which MUST be strictly > 90.38 and land in the low-90s-or-higher
    (D-05); recommend 95 if measured overall ≥ ~96 (RESEARCH Open-Q2). Use an INTEGER to avoid precision-vs-fail_under
    float edges. Record the measured overall AND the chosen NEW_GLOBAL in the SUMMARY.
    Then edit BOTH gate sites to the SAME NEW_GLOBAL (RESEARCH Pitfall 3 — CLI --fail-under silently overrides the
    config value, so they must move together):
    - `pyproject.toml [tool.coverage.report] fail_under = 85` → `fail_under = NEW_GLOBAL`.
    - `justfile` `coverage-combine` step `coverage report --fail-under=85` → `--fail-under=NEW_GLOBAL`.
    And WIRE the floor check into `coverage-combine` (D-01/D-02): insert `uv run coverage json` after
    `uv run coverage xml` (the recipe currently emits only xml; the floor script needs coverage.json), keep the
    raised `uv run coverage report --fail-under=NEW_GLOBAL` (global gate), then append `uv run python
    scripts/coverage_floor.py` as the LAST step (per-module floor). Both guardrails now run inside the one recipe
    on the combined `.coverage`. Do NOT edit `.github/workflows/tests.yml` — its `combine` job already runs
    `just coverage-combine`, so the floor rides in automatically (RESEARCH lines 244-246, VERIFIED). Do NOT touch
    `test-bucket`'s `--cov-fail-under=0` (RESEARCH Pitfall 1). Run `just test-db-down` when done measuring.
  </action>
  <verify>
    <automated>uv run python -c "import tomllib, pathlib, re; p = tomllib.loads(pathlib.Path('pyproject.toml').read_text())['tool']['coverage']['report']['fail_under']; j = int(re.search(r'--fail-under=(\\d+)', pathlib.Path('justfile').read_text()).group(1)); assert p == j and p > 90.38, (p, j); print('gate =', p)"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml [tool.coverage.report] fail_under` and the `justfile coverage-combine --fail-under=` value are equal integers, strictly > 90.38.
    - The `coverage-combine` recipe contains, in order: `coverage combine`, `coverage xml`, `coverage json`, `coverage report --fail-under=<NEW_GLOBAL>`, `python scripts/coverage_floor.py`.
    - `test-bucket`'s `--cov-fail-under=0` is unchanged; `.github/workflows/tests.yml` is unedited.
    - The measured combined overall and chosen NEW_GLOBAL are recorded in the SUMMARY.
  </acceptance_criteria>
  <done>Both gate sites raised to the same pinned integer > 90.38 and the floor script is invoked inside coverage-combine after coverage json.</done>
</task>

<task type="auto">
  <name>Task 2: Gate-consistency guard test</name>
  <files>tests/shared/test_coverage_gate.py</files>
  <read_first>
    - tests/shared/test_ci_workflow_wiring.py (EXACT analog — `_extract_recipe(justfile_text, name)` at L46-55 anchoring on `re.MULTILINE ^`; `_REPO_ROOT = Path(__file__).resolve().parents[2]`, `_JUSTFILE`, `_PYPROJECT` at L39-43)
    - justfile (the edited `coverage-combine` recipe, from Task 1)
    - pyproject.toml (the raised `[tool.coverage.report] fail_under`, from Task 1)
    - .planning/phases/64-per-module-coverage-uplift-gate-raise/64-PATTERNS.md (§test_coverage_gate.py — the invariant to assert)
  </read_first>
  <action>
    Create `tests/shared/test_coverage_gate.py` (bucket: shared — DB-free, subprocess-free, mirrors
    test_ci_workflow_wiring.py). Reuse the repo-root idiom (`_REPO_ROOT = Path(__file__).resolve().parents[2]`)
    and the `_extract_recipe` helper pattern (copy or import) to pull the `coverage-combine` recipe body and
    regex the `--fail-under=(\d+)` value. Parse `pyproject.toml` with stdlib `tomllib` for
    `[tool.coverage.report] fail_under`. Assert the two numbers are EQUAL and BOTH strictly > 90.38 (D-05,
    RESEARCH Pitfall 3 tripwire). Add a second assertion that the `coverage-combine` recipe body contains
    `scripts/coverage_floor.py` and `coverage json` (the floor wiring cannot be silently removed). Use
    `from __future__ import annotations`. Must pass in isolation via `just test-bucket shared`.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/test_coverage_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/shared/test_coverage_gate.py -q` exits 0.
    - The test fails (proven by a scratch edit, then reverted) if the two `fail_under` numbers differ or either ≤ 90.38.
    - The test asserts the `coverage-combine` recipe still invokes `scripts/coverage_floor.py` and emits `coverage json`.
    - File lives under `tests/shared/` (passes the partition guard).
  </acceptance_criteria>
  <done>A shared-bucket guard test locks the two gate sites to the same >90.38 value and asserts the floor wiring stays in coverage-combine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI required check → combine recipe | The branch-protection gate depends on `just coverage-combine` exiting non-zero on gate/floor failure (fail-closed). Verified end-to-end in plan 64-04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-02 | Repudiation/Tampering | gate misconfigured — CLI `--fail-under` drifts from config, or floor invocation dropped, yielding a false-green gate | mitigate | Guard test asserts the two `fail_under` sites are equal and > 90.38 AND that `coverage-combine` still invokes `scripts/coverage_floor.py` + `coverage json`. Prevents silent gate weakening (RESEARCH Pitfall 3). |
| T-64-06 | Denial of Service (brittle gate) | gate set too high blocks unrelated future PRs | accept | D-05 achieved-minus-~1 leaves ~1.5–2pt headroom; integer pin avoids float edges. Deliberate margin, not an over-tight gate. |
| T-64-SC | Tampering (supply chain) | dependency changes | mitigate | No package installs; only config edits + a stdlib-only guard test. Nothing to vet under the 7-day cooldown. |
</threat_model>

<verification>
- Gate-consistency: the Task 1 automated one-liner + `uv run pytest tests/shared/test_coverage_gate.py -q` green.
- End-to-end: `just coverage-combine` (after a full combined run) exits 0 with all modules ≥ 85% and overall ≥ NEW_GLOBAL; runs the global gate AND the floor script.
- Negative smoke (optional): temporarily lower NEW_GLOBAL check or feed a sub-floor coverage.json → `just coverage-combine` exits non-zero; revert.
</verification>

<success_criteria>
- Global `fail_under` raised to a pinned integer strictly > 90.38 at BOTH edit sites (D-05), consistent and guard-tested.
- `just coverage-combine` runs both guardrails (global gate + per-module floor over combined coverage.json) — the D-01 two-guardrail model wired into the one CI combine seam (D-02).
- No workflow YAML edit; `test-bucket` untouched.
</success_criteria>

<output>
Create `.planning/phases/64-per-module-coverage-uplift-gate-raise/64-03-SUMMARY.md` when done.
</output>
