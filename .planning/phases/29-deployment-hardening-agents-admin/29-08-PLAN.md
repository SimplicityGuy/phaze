---
phase: 29-deployment-hardening-agents-admin
plan: 08
type: execute
wave: 4
depends_on: [29-04, 29-07]
files_modified:
  - justfile
  - docs/deployment.md
  - .planning/PROJECT.md
  - scripts/update-project.sh
autonomous: false
requirements: [OPS-02, OPS-03, OPS-04]
tags: [phase-29, docs, justfile, project, d-23, v4.0]

must_haves:
  truths:
    - "`just up-agent` runs `docker compose -f docker-compose.agent.yml up -d` (D-18)"
    - "`just up-all` runs `docker compose -f docker-compose.yml -f docker-compose.agent.yml up -d` (D-18; single-host dev)"
    - "existing `just up` recipe is unchanged (`docker compose up -d` for app-server stack)"
    - "docs/deployment.md exists and documents the 6-step two-host setup walkthrough (D-23): app-server up → copy CA → register agent + token → populate .env.agent → just up-agent → verify on /admin/agents"
    - "docs/deployment.md includes the filesystem-isolation smoke (D-20): exec into api container, confirm /data/music is absent or empty"
    - "PROJECT.md gains a 'Deployment' subsection under Constraints noting v4.0 production runs as two compose files (one per host), HTTPS + Redis hardening as locked invariants (D-23)"
    - "scripts/update-project.sh references the new modules (admin_agents router + cert_bootstrap + heartbeat + model_bootstrap) if the script lists services/modules (D-23)"
    - "Operator can run `just up-agent` from a freshly-cloned file-server checkout and reach the admin page within 60s (manual smoke)"
  artifacts:
    - path: "justfile"
      provides: "Two new recipes: up-agent + up-all (D-18)"
      contains: "up-agent"
    - path: "docs/deployment.md"
      provides: "6-step two-host setup walkthrough + filesystem-isolation smoke + CA distribution + production checklist"
      min_lines: 90
    - path: ".planning/PROJECT.md"
      provides: "Deployment subsection under Constraints — two compose files, HTTPS internal CA, Redis password-bound LAN"
      contains: "docker-compose.agent.yml"
    - path: "scripts/update-project.sh"
      provides: "Updated module/service list if it enumerates services (touch-only per memory rule)"
      min_lines: 0
  key_links:
    - from: "justfile::up-agent"
      to: "docker-compose.agent.yml"
      via: "docker compose -f flag"
      pattern: "docker compose -f docker-compose.agent.yml"
    - from: "docs/deployment.md"
      to: "all 6 acceptance criteria in ROADMAP Phase 29"
      via: "step-by-step operator walkthrough mapping decisions D-01..D-23"
      pattern: "phaze-ca.crt"
---

<objective>
Land the doc + operator-workflow closeout for Phase 29 (D-23). Three new justfile recipes (`up-agent`, `up-all`; existing `up` unchanged); a new `docs/deployment.md` with the 6-step two-host walkthrough including the D-20 filesystem-isolation smoke; a Deployment subsection in `PROJECT.md`; and a touch-up of `scripts/update-project.sh` to keep the module-list current per the user memory rule.

A blocking human-verify checkpoint at the end of this plan confirms the operator can perform the end-to-end walkthrough on a real (or simulated) two-host setup — this is the only checkpoint in the entire phase because Phase 29 is the operational-hardening phase and the operator needs to physically verify that nothing in the documented sequence is missing.

Purpose: Closes D-23 (the doc + .env.example sweep — most of which lands in Plans 03 + 04 with .env.example + .env.example.agent); finalizes OPS-02/OPS-03/OPS-04 by giving the operator the one place to read about the new deployment shape; satisfies the user memory rules around "Justfile as command runner", "README per service", "Update script current", "Docs up to date".

Output: 2 new justfile recipes; new `docs/deployment.md`; new PROJECT.md subsection; updated `scripts/update-project.sh`; one human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-deployment-hardening-agents-admin/29-CONTEXT.md
@.planning/phases/29-deployment-hardening-agents-admin/29-RESEARCH.md
@.planning/phases/29-deployment-hardening-agents-admin/29-PATTERNS.md
@.planning/phases/29-deployment-hardening-agents-admin/29-04-SUMMARY.md
@.planning/phases/29-deployment-hardening-agents-admin/29-07-SUMMARY.md

<interfaces>
<!-- Existing justfile recipes (lines 1-12 in current justfile): -->

```just
[doc('Start all services in Docker')]
[group('dev')]
up:
    docker compose up -d
```

<!-- D-18 adds 2 new recipes; the existing `up` is unchanged. -->

<!-- PROJECT.md format unknown until inspection — read first. -->

<!-- scripts/update-project.sh — user memory rule requires it to stay updated when services/modules are added; check if it enumerates services. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add up-agent + up-all justfile recipes; write docs/deployment.md; update PROJECT.md + update-project.sh</name>
  <files>justfile, docs/deployment.md, .planning/PROJECT.md, scripts/update-project.sh</files>
  <acceptance_criteria>
    - **WARNING-5:** `bash -c "just --list 2>&1 | grep -v '^#' | grep -cE '(up-agent|up-all)'"` returns >= 2 (substring count of the two recipe names anywhere in non-comment output of `just --list`; no line-anchor regex since just's `[doc(...)]`-annotated list format places the recipe name with leading whitespace + group prefixes that the original `^\s+` regex did not reliably match).
    - `just up-agent` (dry-run via just's `--dry-run` if supported, else `just -n up-agent`) produces a command line containing `docker compose -f docker-compose.agent.yml up -d`.
    - `just up-all` produces `docker compose -f docker-compose.yml -f docker-compose.agent.yml up -d`.
    - Existing `just up` recipe is unchanged.
    - `docs/deployment.md` exists at the path; >=80 lines; contains the strings: "phaze-ca.crt", "just up-agent", "REDIS_PASSWORD", "/admin/agents", "PHAZE_AGENT_TOKEN".
    - `docs/deployment.md` has 6 distinct numbered/headed sections corresponding to the operator's setup order: (1) Application-server up, (2) Copy CA to file servers, (3) Register agent + mint token via psql, (4) Populate .env on file-server host, (5) `just up-agent`, (6) Verify on `/admin/agents`.
    - `docs/deployment.md` includes the D-20 filesystem-isolation smoke paragraph: exec into api container, confirm `/data/music` is absent (or use the YAML-parse test as primary verification with the manual smoke as secondary).
    - `docs/deployment.md` includes a "CA Rotation" note documenting the destructive `rm -rf ./certs/` operation per D-02 (loud banner + all-agents-fail-until-re-distributed).
    - `.planning/PROJECT.md` has a new `### Deployment` (or `## Deployment`) subsection somewhere appropriate (under Constraints OR at the end), >=10 lines, documenting v4.0's two-compose-file invariant + HTTPS internal CA + Redis password-bound LAN.
    - `scripts/update-project.sh` — if it lists services or modules (grep first), update the list to include new Phase 29 modules: `admin_agents` router, `cert_bootstrap`, `heartbeat`, `_shared/model_bootstrap`. If the script doesn't list modules (it's pure orchestration), leave it untouched and document in 29-08-SUMMARY.md.
    - `just --list` exits 0 (the justfile parses).
    - `uv run pre-commit run --files justfile docs/deployment.md .planning/PROJECT.md scripts/update-project.sh` passes.
  </acceptance_criteria>
  <read_first>
    - justfile (full current file — verify recipe formatting style + group tags + doc strings; the existing `up` recipe at lines 9-12 is the analog)
    - .planning/PROJECT.md (full current file — find an appropriate section to insert "Deployment" subsection; preserve existing structure)
    - scripts/update-project.sh (full file — determine if it enumerates services/modules; if so, where)
    - .planning/phases/29-deployment-hardening-agents-admin/29-CONTEXT.md §D-18 (3 new recipes; `up` unchanged) + §D-23 (full doc + env sweep spec)
    - .planning/phases/29-deployment-hardening-agents-admin/29-RESEARCH.md §"Code Examples → Justfile Recipes" lines 1107-1125 (literal recipe target)
    - .planning/phases/29-deployment-hardening-agents-admin/29-PATTERNS.md §"justfile" lines 870-890 (recipe placement after existing `up`)
    - .planning/phases/29-deployment-hardening-agents-admin/29-CONTEXT.md §D-20 (filesystem-isolation smoke for ops doc)
    - .planning/phases/29-deployment-hardening-agents-admin/29-RESEARCH.md §"Runtime State Inventory" lines 947-958 (CA distribution is the only out-of-band runtime state — must be documented in deployment.md)
    - .planning/phases/29-deployment-hardening-agents-admin/29-RESEARCH.md §"Open Questions #2" lines 1159-1162 (CA distribution mechanism: manual scp/rsync; document the literal command)
    - .planning/REQUIREMENTS.md §"Topology & Boundary" + §"Deployment & Operations" (verify D-23 doc covers all 6 ROADMAP success criteria for Phase 29)
  </read_first>
  <action>
**Step 1 — justfile.** Read the current `justfile`. Locate the `up` recipe at lines 9-12. Insert the following two recipes IMMEDIATELY AFTER the `up` recipe (BEFORE the `down` recipe at line 14-17):

```just
[doc('Start file-server agent stack (standalone docker-compose.agent.yml)')]
[group('dev')]
up-agent:
    docker compose -f docker-compose.agent.yml up -d

[doc('Start both stacks on one host (developer convenience)')]
[group('dev')]
up-all:
    docker compose -f docker-compose.yml -f docker-compose.agent.yml up -d
```

Preserve all other recipes byte-for-byte. Use the exact `[doc(...)]` + `[group(...)]` annotation style matching the existing recipes (the project uses just's `[attribute]` syntax per the current file).

Verify: `just --list` exits 0 and shows both new recipes under the `dev` group. `just -n up-agent` prints the docker compose command. **WARNING-5:** the verification uses substring-count grep (`grep -v '^#' | grep -cE '(up-agent|up-all)'`) instead of a line-anchored regex because just's `--list` output format includes `[doc()]`-annotated header lines and indented recipe lines that the original `^\s+` regex did not reliably match.

**Step 2 — `docs/deployment.md`.** Create the file with the following structure (target ~120 lines):

```markdown
# Phaze v4.0 Deployment Guide

Production deployment of Phaze v4.0 (Distributed Agents) runs as **two compose files on two hosts**:

- **Application server** (`docker-compose.yml`): API, UI, Postgres, Redis, fileless controller worker. No file mounts. HTTPS via internal CA. Redis `requirepass` + LAN binding.
- **File servers** (`docker-compose.agent.yml`, one per host): agent worker, watcher, audfprint, panako. Holds music/video files locally; reaches the app-server over HTTPS for every state change.

This guide walks through bringing up a fresh two-host deployment from clean checkout.

## Prerequisites

- Docker Engine 20.10+ and `docker compose` v2.x on both hosts
- `just` installed on both hosts (or run `docker compose` directly)
- Both hosts on the same private LAN; no firewall between them
- Postgres + Redis are NOT directly exposed to the public internet
- On the app-server host: `./certs/`, `./models/` (optional), `.env`
- On each file-server host: `./certs/` (CA only), `./models/` (auto-downloads on first start), `.env`

## Step 1 — Bring up the application server

On the **app-server host**:

```bash
git clone https://github.com/simplicityguy/phaze.git
cd phaze
cp .env.example .env
# Edit .env: set REDIS_PASSWORD to a strong unique value
# Edit .env: set REDIS_BIND_IP to the app-server's private LAN IP (e.g., 192.168.1.10)
just up
```

On first start, the api container generates an internal CA + leaf cert into `./certs/`. Watch the logs:

```bash
docker compose logs -f api
```

You will see a multi-line banner:

```
==============================================================
GENERATED NEW PHAZE INTERNAL CA at /certs/phaze-ca.crt
COPY THIS FILE TO EVERY FILE SERVER and point each agent's
PHAZE_AGENT_CA_FILE env var at it. EXISTING AGENTS WILL FAIL
TO CONNECT UNTIL THEY HAVE THIS NEW CA.
==============================================================
```

After the banner, uvicorn binds port 8000 with TLS.

**Verify**: `curl --cacert ./certs/phaze-ca.crt https://localhost:8000/docs` returns the OpenAPI UI.

## Step 2 — Copy the CA cert to each file server

The CA private key (`./certs/phaze-ca.key`) **stays on the app-server host**. Only the public CA cert (`./certs/phaze-ca.crt`) is distributed.

From the app-server host, for each file server:

```bash
scp ./certs/phaze-ca.crt operator@fileserver-east:/home/operator/phaze/certs/phaze-ca.crt
```

Or use rsync, ansible, etc. — any one-time file transfer mechanism. The operator-distributed CA is a `0644` public cert; non-secret.

## Step 3 — Register an agent and mint a token

On the **app-server host**, in a psql session (or via your preferred SQL client):

```sql
INSERT INTO agents (id, name, token_hash, scan_roots, created_at)
VALUES (
    'fileserver-east',
    'File Server East',
    -- token_hash is sha256() of the chosen plaintext token
    sha256('phaze_agent_REPLACE_WITH_RANDOM_32_URLSAFE'::bytea)::text,
    '["/data/music", "/data/concerts"]'::jsonb,
    now()
);
```

The plaintext token (the part you sha256) is what you'll put in `.env` on the file-server side. **Save it now — it's not recoverable from the database.**

Generate a strong token: `python -c "import secrets; print('phaze_agent_' + secrets.token_urlsafe(32))"`.

A sentinel `LIVE` ScanBatch row will be auto-created the first time the agent posts a file (Phase 24 + Phase 27 invariant).

## Step 4 — Populate the file-server `.env`

On the **file-server host**:

```bash
git clone https://github.com/simplicityguy/phaze.git
cd phaze
cp .env.example.agent .env
```

Edit `.env` to set:

- `PHAZE_AGENT_API_URL=https://<app-server-lan-ip>:8000`
- `PHAZE_REDIS_URL=redis://default:<REDIS_PASSWORD>@<app-server-lan-ip>:6379/0`
- `PHAZE_AGENT_ID=fileserver-east`
- `PHAZE_AGENT_TOKEN=<the plaintext token from Step 3>`
- `PHAZE_AGENT_QUEUE=phaze-agent-fileserver-east`
- `PHAZE_AGENT_CA_FILE=/certs/phaze-ca.crt`
- `PHAZE_AGENT_ENV=production`
- `SCAN_PATH=/path/to/your/music/library`
- `MODELS_PATH=./models`
- `CA_PATH=./certs`
- `PHAZE_AGENT_SCAN_ROOTS=/data/music,/data/concerts`
- `PHAZE_IMAGE_TAG=v4.0.0` (or `latest` for first-time setup)

## Step 5 — Bring up the agent stack

On the **file-server host**:

```bash
just up-agent
```

On first start, the agent worker boots, calls `/whoami` to verify its token, then downloads ~150MB of essentia weights to `./models/` (2-5 minutes; logs an INFO line). The watcher comes up in parallel.

Watch the logs:

```bash
docker compose -f docker-compose.agent.yml logs -f worker
```

You should see:

- `phaze.tasks.agent_worker startup role=agent api=https://... auth_id_prefix=phaze_agent_a1b2... queue=phaze-agent-fileserver-east`
- `/models is empty; downloading essentia weights (~150MB, takes 2-5min on first start)...`
- `Models downloaded successfully to /models`
- `phaze.tasks.agent_worker startup complete agent_id=fileserver-east queue=phaze-agent-fileserver-east`

After ~5 minutes, the heartbeat cron starts firing every 30s.

## Step 6 — Verify on the admin page

From any browser on the LAN (or via SSH tunnel from your laptop):

```bash
# Trust the CA in your local browser, or use curl:
curl --cacert ./certs/phaze-ca.crt https://<app-server-lan-ip>:8000/admin/agents
```

You should see the agent row with status **ALIVE** (green pill) within 60s of `just up-agent`.

If the row shows **NEVER**: the agent worker hasn't completed startup yet. Check the worker logs.

If the row shows **DEAD** after 5 minutes: the worker is up but heartbeats aren't reaching the app-server. Check the agent worker logs for `heartbeat failed: ...` WARNING lines.

## Filesystem-Isolation Smoke (D-20)

To verify DIST-01 (the app-server has no way to read or write music files), exec into the api container and try to read a file:

```bash
docker compose exec api ls -la /data/music
# Expected: ls: cannot access '/data/music': No such file or directory
```

Or trust the structural test that runs in CI:

```bash
uv run pytest tests/test_deployment/test_api_filesystem_isolation.py -v
```

## CA Rotation (caution)

The CA + leaf cert generated in Step 1 is valid for 10 years (CA) / 2 years (leaf). If you ever need to rotate:

```bash
# On the app-server host:
rm -rf ./certs/                       # destructive — all current cert state is lost
docker compose restart api            # cert_bootstrap regenerates + prints the loud banner again
# Then repeat Step 2 (copy ./certs/phaze-ca.crt to every file server) and restart each agent.
```

Every file-server agent will fail to connect until you re-distribute the new `phaze-ca.crt`. The loud banner is the only safeguard — do not delete the certs directory casually.

## Pinning the agent image for production

For first-time setup, `PHAZE_IMAGE_TAG=latest` pulls the most recent tagged release from GHCR. For production, pin to a specific version:

```bash
# On the file-server host's .env:
PHAZE_IMAGE_TAG=v4.0.0
```

Then `just up-agent` pulls exactly that version. The docker-publish.yml workflow tags both `:latest` and `:v<version>` on tagged releases.

## Pre-warming models (skip the first-start wait)

To avoid the 2-5 minute model download on first agent boot:

```bash
# On the file-server host BEFORE just up-agent:
just download-models
```

This populates `./models/` directly; the agent's auto-download check then no-ops.

## See also

- `.env.example` — app-server environment template
- `.env.example.agent` — file-server agent environment template
- `docker-compose.yml` — app-server compose
- `docker-compose.agent.yml` — file-server agent compose
- `PROJECT.md` — v4.0 architecture overview
- `ROADMAP.md` — phase history
```

**Step 3 — PROJECT.md Deployment subsection.** Read the current `.planning/PROJECT.md`. Find the `Constraints` section (or equivalent). Append a new subsection (use the appropriate heading level — `###` if Constraints is `##`, etc.):

```markdown
### Deployment (v4.0 — Distributed Agents)

Phaze v4.0 production runs as **two Docker Compose files on two private-LAN hosts**:

- **Application server** (`docker-compose.yml`): `api` (uvicorn-direct TLS via internal CA), `worker` (fileless controller-role SAQ worker), `postgres`, `redis` (password-auth + LAN-bound port). **No file mounts** — the app-server has no way to read or write music/video file content.
- **File servers** (`docker-compose.agent.yml`, one stack per file-server host): `worker` (agent-role SAQ worker), `watcher` (watchdog-based file event poster), `audfprint` + `panako` (local fingerprint sidecars). Holds the music/video library locally; reaches the app-server over HTTPS for every state change.

Locked invariants:

- All agent → app-server traffic uses **HTTPS** terminated by uvicorn against a self-signed internal CA generated in the app-server's `api` container's first start. Operators distribute the public CA cert (`phaze-ca.crt`) to each file server via scp/rsync; the CA private key never leaves the app-server.
- **Redis** on the app-server requires `requirepass` and is bound to the private LAN IP (or loopback in dev). Agents connect with `redis://default:<password>@<host>:6379`.
- **0 new pip dependencies** beyond `cryptography` (added Phase 29 for cert generation).
- Operator workflow: `just up` (app-server), `just up-agent` (each file-server), `just up-all` (single-host dev). Full walkthrough in `docs/deployment.md`.

Deferred to a future ops phase: mTLS for the agent boundary, agent self-registration UI, Prometheus metrics scrape endpoint, automated CA rotation. See REQUIREMENTS.md §"Future Requirements → Operational Polish" (OPS-05..OPS-07).
```

If PROJECT.md has a different structure that makes "Constraints" inappropriate, find the section that documents architecture or infrastructure and append there. Use judgment based on the actual file structure.

**Step 4 — scripts/update-project.sh.** Read the current `scripts/update-project.sh`. Check if it enumerates services or modules (grep for `services/`, `routers/`, `tasks/`, `phaze.routers`, `admin_agents` etc.). If YES, update the list to include the new Phase 29 modules: `admin_agents` (router), `cert_bootstrap` (module), `heartbeat` (task), `_shared/model_bootstrap` (shared bootstrap). If NO (the script is purely orchestration/CI/etc with no module list), leave it untouched and note in 29-08-SUMMARY.md ("scripts/update-project.sh does not enumerate modules; no edit required per content audit").

This step satisfies the user memory rule "Update script current" but only if the script actually has a list to keep current.

Run `uv run pre-commit run --files justfile docs/deployment.md .planning/PROJECT.md scripts/update-project.sh` to check formatting (markdownlint, yamllint, shellcheck if applicable).
  </action>
  <verify>
    <automated>bash -c "just --list 2>&1 | grep -v '^#' | grep -cE '(up-agent|up-all)'" && test -f docs/deployment.md && grep -c 'phaze-ca.crt' docs/deployment.md; uv run pre-commit run --files justfile docs/deployment.md .planning/PROJECT.md scripts/update-project.sh</automated>
  </verify>
  <done>
- `justfile` has `up-agent` and `up-all` recipes; `just up` unchanged
- `docs/deployment.md` exists with all 6 numbered steps and the D-20 filesystem-isolation smoke
- `.planning/PROJECT.md` has the new Deployment subsection
- `scripts/update-project.sh` either updated with new modules OR audited and left unchanged with note
- `just --list` exits 0
- WARNING-5: substring-count grep on `just --list` returns >= 2 for `(up-agent|up-all)` (no line-anchor regex)
- Pre-commit hooks pass on the 4 modified files
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator walkthrough verification — execute docs/deployment.md end-to-end on real or simulated two-host setup</name>
  <what-built>
Plan 08 Task 1 landed:
- `just up-agent` recipe (runs `docker compose -f docker-compose.agent.yml up -d`)
- `just up-all` recipe (single-host dev — both compose files)
- `docs/deployment.md` 6-step walkthrough
- PROJECT.md Deployment subsection

This checkpoint runs the documented walkthrough end-to-end to confirm:
1. The documentation is sufficient (no missing steps, no missing variables, no missing commands)
2. The compose files actually bring up the stack on a fresh checkout
3. The agent registers, downloads models (or pre-warms them), starts heartbeating
4. The `/admin/agents` page renders with the agent showing ALIVE status within 60s

This is the ONLY checkpoint in the entire phase. Phase 29 is operational-hardening; the operator must physically verify nothing is missing before the phase ships.
  </what-built>
  <how-to-verify>
**Option A — Real two-host setup (preferred if hardware available):**

1. On a fresh app-server host, follow `docs/deployment.md` Step 1 verbatim:
   ```bash
   git clone https://github.com/simplicityguy/phaze.git
   cd phaze
   cp .env.example .env
   # Set REDIS_PASSWORD and REDIS_BIND_IP per the doc
   just up
   ```
   Confirm: api container logs show the GENERATED NEW PHAZE INTERNAL CA banner; `curl --cacert ./certs/phaze-ca.crt https://localhost:8000/docs` returns the OpenAPI page.

2. On a fresh file-server host, follow Steps 2-5 verbatim:
   - scp the CA cert from app-server
   - psql to app-server, register an agent, save the plaintext token
   - cp .env.example.agent .env, populate per the doc
   - `just up-agent`

3. Wait up to 5 minutes (model download + first heartbeat). Then on the app-server, `curl --cacert ./certs/phaze-ca.crt https://<app-server>:8000/admin/agents | grep -c "ALIVE"` returns >=1.

4. Verify D-20: `docker compose exec api ls /data/music 2>&1 | grep -c "No such file"` returns 1.

**Option B — Simulated single-host setup (acceptable for CI / no hardware):**

1. On a single dev host with two terminals or compose projects:
   ```bash
   # Terminal 1 — app-server simulation
   cd /tmp/phaze-app && git clone ...
   cp .env.example .env  # set REDIS_PASSWORD=test; REDIS_BIND_IP=127.0.0.1
   just up
   ```

2. Terminal 2 — file-server simulation (use docker compose project name to isolate):
   ```bash
   cd /tmp/phaze-fileserver && git clone ...
   # cp ./certs/phaze-ca.crt from the app-server's volume
   cp .env.example.agent .env
   # Edit to point at PHAZE_AGENT_API_URL=https://host.docker.internal:8000 (or use compose external network)
   COMPOSE_PROJECT_NAME=phaze-agent just up-agent
   ```

3. Verify `https://localhost:8000/admin/agents` shows the agent.

**Option C — Documentation review only (minimum):**

If neither A nor B is feasible, do a thorough doc review:

1. Read `docs/deployment.md` start-to-end, simulating the operator.
2. Check that every command is complete (no `...` ellipses, no missing flags).
3. Check that every env var mentioned in the doc appears in `.env.example` or `.env.example.agent`.
4. Check that the URLs match the actual deployed routes (`/admin/agents`, `/api/internal/agent/heartbeat`).
5. Verify the SQL snippet (Step 3) is valid Postgres — paste into a `psql` smoke session.
6. Verify the file paths referenced (`./certs/phaze-ca.crt`, `./models/`, `.env`) match the compose mount paths.

**Resume signal options:**
- `verified-real` — Option A executed; admin page shows ALIVE within 60s; phase ready to ship
- `verified-simulated` — Option B executed in single-host mode; same evidence as A
- `verified-docs-only` — Option C executed; docs are complete; recommend a follow-up real-deployment smoke when hardware available
- `gap-found: <details>` — verification revealed a missing step / var / command; planner cycles back to fix

Phase 29 cannot ship without one of the first three resume signals.
  </how-to-verify>
  <resume-signal>Type `verified-real`, `verified-simulated`, `verified-docs-only`, or `gap-found: <details>`</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → docs/deployment.md | docs describe the locked threat model (no new boundaries) |
| docs → operator action | misleading docs lead to misconfigured deployments — operational risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-08-01 | Operational | docs missing a step → operator's deployment fails | mitigate | Blocking human-verify checkpoint (Task 2) walks the operator through the actual docs; gaps surface as `gap-found` resume signal |
| T-29-08-02 | Information Disclosure | docs leak production-style secrets in examples | mitigate | All example tokens use placeholders (`<REDIS_PASSWORD>`, `phaze_agent_REPLACE_WITH_RANDOM_32_URLSAFE`); the SQL snippet explicitly tells the operator to generate a strong token via `secrets.token_urlsafe` |
| T-29-08-03 | Operational | `rm -rf ./certs/` mistakenly executed by an operator following the docs | mitigate | The "CA Rotation (caution)" section explicitly warns about the destructive nature; the loud banner (Phase 29 D-02) is the runtime safeguard |
| T-29-08-04 | Operational | docs misalign with actual code paths (e.g., wrong endpoint URL, wrong env var name) | mitigate | Human-verify checkpoint enforces an end-to-end test; any mismatch surfaces as `gap-found` |
| T-29-08-05 | Operational | `just up-agent` recipe broken (typo, wrong flag) | mitigate | `just --list` smoke check + the manual `just -n up-agent` dry-run verification; tested via the human-verify checkpoint |
</threat_model>

<verification>
- `just --list` exits 0; shows `up-agent` and `up-all` under `dev` group
- `bash -c "just --list 2>&1 | grep -v '^#' | grep -cE '(up-agent|up-all)'"` returns >= 2 (WARNING-5: substring count, no line-anchor regex)
- `just -n up-agent` prints `docker compose -f docker-compose.agent.yml up -d`
- `just -n up-all` prints `docker compose -f docker-compose.yml -f docker-compose.agent.yml up -d`
- `docs/deployment.md` exists, has 6 steps + D-20 smoke + CA rotation section
- `.planning/PROJECT.md` has the Deployment subsection
- `uv run pre-commit run --files justfile docs/deployment.md .planning/PROJECT.md scripts/update-project.sh` passes
- Human-verify checkpoint resume signal in {`verified-real`, `verified-simulated`, `verified-docs-only`}
</verification>

<success_criteria>
- D-18 fully implemented (3 just recipes — existing `up` + new `up-agent` + new `up-all`)
- D-20 documented in deployment.md
- D-23 doc + .env + project sweep complete (in conjunction with .env.example from Plan 03 + .env.example.agent from Plan 04)
- Phase 29 ships with a verified-walkthrough resume signal
- All 6 ROADMAP success criteria for Phase 29 are addressable from `docs/deployment.md`
</success_criteria>

<output>
Create `.planning/phases/29-deployment-hardening-agents-admin/29-08-SUMMARY.md` when both tasks complete. Summary must list: new files (docs/deployment.md), modified files (justfile, PROJECT.md, scripts/update-project.sh if touched), decision IDs implemented (D-18, D-20, D-23 final closure), the human-verify resume signal, and a phase-level rollup summary linking back to each plan's SUMMARY.
</output>
