---
phase: 63-parallel-ci-code-change-gating
plan: 04
type: execute
wave: 3
depends_on: [63-01, 63-02]
files_modified:
  - .github/workflows/ci.yml
  - scripts/classify-changed-files.sh
  - justfile
  - tests/shared/test_change_gate.py
autonomous: true
requirements: [CI-04]
user_setup: []

must_haves:
  truths:
    - "D-08: the doc-only skip set is broadened from *.md-only to also cover .planning/**, LICENSE, docs/, and .txt — a change touching only those flips code-changed=false"
    - "D-08: the classifier is CONSERVATIVE — any path not clearly a doc path keeps code-changed=true (a code change can never skip security scans)"
    - "D-09: the changed-files gate job is kept (not paths-ignore); aggregate-results stays the single stable required check with if: always() + code-changed==false → exit 0"
    - "D-10: the classifier is a versioned tested script invoked via a just recipe, not inline workflow shell"
    - "CI-04: docs/.planning/markdown-only PRs skip heavy jobs while required checks still report SUCCESS (skip-with-success, never skip-absent)"
  artifacts:
    - path: "scripts/classify-changed-files.sh"
      provides: "Unit-testable doc-vs-code classifier"
      contains: "code-changed"
    - path: "tests/shared/test_change_gate.py"
      provides: "Regression tests over the classifier (incl. the conservative positive case)"
      contains: "classify"
    - path: ".github/workflows/ci.yml"
      provides: "detect-changes delegates classification to the script; aggregate-results unchanged"
      contains: "classify-changed-files"
  key_links:
    - from: ".github/workflows/ci.yml detect-changes"
      to: "scripts/classify-changed-files.sh"
      via: "piping CHANGED_FILES into the script"
      pattern: "classify-changed-files"
    - from: "tests/shared/test_change_gate.py"
      to: "scripts/classify-changed-files.sh"
      via: "subprocess with crafted file lists"
      pattern: "subprocess"
---

<objective>
Finish CI-04: broaden the existing doc-only skip so `.planning/**`, `LICENSE`, `docs/`, and
`.txt` (not just `*.md`) also skip the heavy jobs, extract the classifier into a versioned,
unit-testable script (invoked via a `just` recipe), and add regression tests — while keeping the
`detect-changes` gate job and the `aggregate-results` skip-with-success contract exactly as-is so
a doc-only PR stays mergeable under branch protection.

Purpose: CI-04 is ~80% built; this makes the classifier broader, conservative, delegated, and
tested without touching the required-check topology.
Output: `scripts/classify-changed-files.sh`, a `just` recipe, a broadened `ci.yml` detect-changes
step, and `tests/shared/test_change_gate.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-parallel-ci-code-change-gating/63-CONTEXT.md
@.planning/phases/63-parallel-ci-code-change-gating/63-PATTERNS.md
@.planning/phases/63-parallel-ci-code-change-gating/63-RESEARCH.md
@.github/workflows/ci.yml

<interfaces>
Line to broaden (ci.yml:72): `NON_MD_FILES=$(echo "${CHANGED_FILES}" | grep -v '\.md$' || true)`.
Broadened classifier (PATTERNS §ci.yml / RESEARCH §Code Examples 4):
  grep -vE '(\.md$|^\.planning/|^LICENSE$|^docs/|\.txt$)'  → remainder empty ⇒ code-changed=false.

Skip-with-success contract to PRESERVE VERBATIM (ci.yml:119,144-148):
  aggregate-results: needs:[detect-changes,quality,test,security,docker]; if: always()
  … if [[ "${CODE_CHANGED}" == "false" ]]; then echo "…"; exit 0; fi   # required check stays green

UNTOUCHED: the SHA edge-case block (ci.yml:52-67 — schedule/tag, zero-SHA new branch, force-push
gone before-SHA). Only the classification of CHANGED_FILES changes.

Meta-test analog: tests/test_no_default_queue_producers.py:127-138 (feed crafted sample → assert).
Anti-pattern (RESEARCH): never convert to trigger-level `paths-ignore` (→ skip-absent → unmergeable).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the classifier to a script + just recipe; broaden the skippable set</name>
  <read_first>
    - .github/workflows/ci.yml:43-80 (detect-changes: SHA edge-cases + the grep classifier) and :111-158 (aggregate-results)
    - .planning/phases/63-parallel-ci-code-change-gating/63-PATTERNS.md §".github/workflows/ci.yml"
    - justfile §[group('test')] (attribute stanza to mirror for a new recipe)
  </read_first>
  <files>scripts/classify-changed-files.sh, justfile, .github/workflows/ci.yml</files>
  <action>
    Create `scripts/classify-changed-files.sh`: a POSIX `bash` script (`set -euo pipefail`,
    shellcheck-clean at --severity=warning) that reads a newline-delimited list of changed file
    paths on stdin and prints exactly `code-changed=true` or `code-changed=false`. Classification:
    keep only NON-doc paths via `grep -vE '(\.md$|^\.planning/|^LICENSE$|^docs/|\.txt$)'`; if the
    remainder is empty ⇒ `code-changed=false`, else `code-changed=true`. Conservative by
    construction: anything not matching a doc pattern counts as code (security mitigation). Add a
    `just` recipe (e.g. `detect-code-changes`) in an appropriate group that pipes its stdin to the
    script, so CI delegates to `just` per D-10. In `ci.yml` detect-changes: KEEP the SHA edge-case
    block that computes `CHANGED_FILES` (52-67) unchanged; replace ONLY the inline
    `NON_MD_FILES=… grep -v '\.md$' …` + the following `if` with a call that pipes `${CHANGED_FILES}`
    into the classifier (via `just detect-code-changes` or the script directly) and writes the
    resulting `code-changed=…` to `${GITHUB_OUTPUT}`. Add a `setup-just` step to the detect-changes
    job if invoking via `just` (frozen SHA extractions/setup-just@53165ef7…  # v4.0.0). Do NOT touch
    the `aggregate-results` job, the `if: always()`, or the `code-changed==false → exit 0` branch —
    those are THE required-check contract. Do NOT switch to `paths-ignore`.
  </action>
  <acceptance_criteria>
    - `printf '.planning/x.md\nLICENSE\ndocs/y.rst\nz.txt\n' | bash scripts/classify-changed-files.sh` prints `code-changed=false`.
    - `printf '.planning/x.md\nsrc/phaze/main.py\n' | bash scripts/classify-changed-files.sh` prints `code-changed=true`.
    - `printf 'README.md\n' | bash scripts/classify-changed-files.sh` prints `code-changed=false` (md still skips).
    - ci.yml detect-changes writes code-changed from the script; the SHA edge-case block (52-67) is unchanged.
    - aggregate-results job (if: always(), exit 0 on code-changed==false) is byte-for-byte unchanged.
    - shellcheck + actionlint + check-jsonschema pre-commit hooks pass.
  </acceptance_criteria>
  <verify>
    <automated>printf '.planning/x.md\nLICENSE\ndocs/y.rst\nz.txt\n' | bash scripts/classify-changed-files.sh | grep -qx 'code-changed=false' && printf '.planning/x.md\nsrc/phaze/main.py\n' | bash scripts/classify-changed-files.sh | grep -qx 'code-changed=true' && printf 'README.md\n' | bash scripts/classify-changed-files.sh | grep -qx 'code-changed=false' && pre-commit run shellcheck --files scripts/classify-changed-files.sh && pre-commit run actionlint --files .github/workflows/ci.yml</automated>
  </verify>
  <done>Classifier is a delegated, shellcheck-clean script; ci.yml broadens the skip set conservatively without touching the required-check contract.</done>
</task>

<task type="auto">
  <name>Task 2: Regression tests over the classifier (D-09)</name>
  <read_first>
    - scripts/classify-changed-files.sh (from Task 1)
    - tests/test_no_default_queue_producers.py:127-138 (feed-sample-assert-output meta-test pattern)
    - .planning/phases/63-parallel-ci-code-change-gating/63-PATTERNS.md §"tests/<bucket>/test_change_gate.py"
  </read_first>
  <files>tests/shared/test_change_gate.py</files>
  <action>
    Create `tests/shared/test_change_gate.py` (in the `shared` bucket so it is collected + covered).
    Invoke `scripts/classify-changed-files.sh` via `subprocess.run` feeding crafted changed-file
    lists on stdin and assert the printed `code-changed` value. Required cases: all-`.planning/**`
    ⇒ false; `LICENSE`-only ⇒ false; `docs/…` + `.txt`-only ⇒ false; markdown-only ⇒ false; a bare
    `.py` ⇒ true; MIXED doc + `.py` ⇒ true (the conservative security property — a code change is
    never skipped); empty input ⇒ false. Locate the script via a repo-root anchor
    (`Path(__file__).resolve().parents[2]` from tests/shared/). Keep it a plain sync test (no DB
    fixture) so the `shared` bucket stays a candidate for `-n auto`.
  </action>
  <acceptance_criteria>
    - Test covers: all-.planning, LICENSE-only, docs/+txt, md-only, bare .py, mixed doc+.py, empty.
    - The mixed doc+code case asserts `code-changed=true` (conservative property has a positive test).
    - Test uses subprocess against the real script; no DB fixture used.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/shared/test_change_gate.py -x -q</automated>
  </verify>
  <done>Classifier regression suite green, including the conservative mixed-change positive case.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR contents → CI gate | The classifier decides whether security/test/docker jobs run at all |
| workflow trigger → secrets | fork PRs must not receive secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-04-01 | Tampering / EoP | doc-only skip lets a code change bypass security scans | mitigate | Classifier is conservative (keep-only-non-doc; any non-doc path ⇒ code-changed=true) with a dedicated positive test on a mixed doc+code list (Task 2) |
| T-63-04-02 | DoS (unmergeable) | required-check contract | mitigate | Keep the `detect-changes` gate + `aggregate-results` `if: always()` + `exit 0` on code-changed==false verbatim; never switch to `paths-ignore` (skip-absent trap) |
| T-63-04-03 | Tampering | classifier logic drift | mitigate | Classifier is a versioned shellcheck-clean script covered by regression tests (D-09), not opaque inline YAML shell |
| T-63-04-04 | EoP | fork-PR secret exposure | mitigate | ci.yml keeps `pull_request` (not `pull_request_target`); this plan does not change triggers or permissions |
| T-63-04-SC | Tampering | package installs | accept | No new packages; pytest-xdist gated in Plan 01 |
</threat_model>

<verification>
- Script classifies the four doc-only shapes → false and any code path → true.
- ci.yml SHA edge-case block + aggregate-results unchanged; no paths-ignore.
- `tests/shared/test_change_gate.py` green; shellcheck/actionlint clean.
</verification>

<success_criteria>
Docs/.planning/markdown-only PRs skip heavy jobs while `aggregate-results` still reports SUCCESS
(skip-with-success), the classifier is conservative + tested, and the required-check topology is
untouched (CI-04).
</success_criteria>

<output>
Create `.planning/phases/63-parallel-ci-code-change-gating/63-04-SUMMARY.md` when done.
</output>
