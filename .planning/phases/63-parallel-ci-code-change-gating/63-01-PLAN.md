---
phase: 63-parallel-ci-code-change-gating
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - justfile
  - tests/buckets.json
autonomous: false          # blocking package-legitimacy checkpoint before `uv add`
requirements: [CI-01, CI-02, CI-03]
user_setup: []

must_haves:
  truths:
    - "D-02: `coverage combine` unions per-bucket shard data by relative path (relative_files=true) and `coverage report --fail-under=85` enforces the gate on the combined number"
    - "D-01: `just test-bucket <name>` runs one bucket serially by default (DB-safe) and accepts an opt-in xdist flag for verified DB-free buckets"
    - "D-10: new CI logic lands as `just` recipes (test-bucket, coverage-combine), not inline workflow shell"
    - "CI-02: pytest-xdist is available in the dev group so DB-free buckets can run -n auto"
    - "CI-03: the canonical 9-bucket list has one source of truth (tests/buckets.json) consumed by matrix + guard + recipe"
  artifacts:
    - path: "tests/buckets.json"
      provides: "Canonical bucket-name list (single source of truth)"
      contains: "discovery"
    - path: "justfile"
      provides: "test-bucket + coverage-combine recipes in [group('test')]"
      contains: "coverage-combine"
    - path: "pyproject.toml"
      provides: "coverage relative_files + pytest-xdist dev dep"
      contains: "relative_files = true"
  key_links:
    - from: "justfile coverage-combine"
      to: "coverage report --fail-under=85"
      via: "recipe body"
      pattern: "fail-under=85"
    - from: "pyproject.toml [tool.coverage.run]"
      to: "coverage combine cross-shard union"
      via: "relative_files"
      pattern: "relative_files = true"
---

<objective>
Lay the Wave-0 foundation the reorg and matrix build on: install `pytest-xdist`, enable
cross-shard coverage combine (`relative_files = true`, concurrency unchanged), add the two
`just` recipes every CI shard/combine step delegates to, and publish the canonical bucket
list as a single source of truth.

Purpose: CI-02/CI-03 need the xdist dep, the combine plumbing, and the delegated recipes to
exist before `tests.yml` can call them (Plan 03) and before the reorg (Plan 02) creates the
bucket directories those recipes point at.
Output: `pytest-xdist` in the dev group, `relative_files` coverage config, `just test-bucket`
+ `just coverage-combine`, and `tests/buckets.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-parallel-ci-code-change-gating/63-CONTEXT.md
@.planning/phases/63-parallel-ci-code-change-gating/63-PATTERNS.md
@.planning/phases/63-parallel-ci-code-change-gating/63-RESEARCH.md

<interfaces>
Existing coverage config (pyproject.toml:72-75) — ADD `relative_files = true`, KEEP concurrency:
  [tool.coverage.run]
  concurrency = ["greenlet", "thread"]   # do NOT add "multiprocessing" (research A2)
  omit = ["tests/*"]
  source = ["phaze"]

Dev-group insert point (pyproject.toml:223) — pytest-xdist sorts alphabetically AFTER
`pytest-cov>=7.1.0` and BEFORE `respx>=0.23.1`.

Existing recipe shape to mirror (justfile:85-93): `[doc(...)]` + `[group('test')]` +
`uv run pytest ...` body. `test-file FILE:` shows the parameter-with-default idiom.

The 9 buckets (D-04): discovery, metadata, fingerprint, analyze, identify, review, agents,
integration, shared.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — pytest-xdist</name>
  <read_first>
    - .planning/phases/63-parallel-ci-code-change-gating/63-RESEARCH.md §"Package Legitimacy Audit" (pytest-xdist tagged [ASSUMED]; slopcheck not run this session)
  </read_first>
  <what-built>Nothing yet — this gate precedes the `uv add`. pytest-xdist is the ONLY new package this phase adds; research tagged it `[ASSUMED]` because slopcheck could not run.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/pytest-xdist/ and confirm it is the pytest-dev official package (repo github.com/pytest-dev/pytest-xdist), mature version history through 3.8.0, very high download count.
    2. Confirm the latest release publish date is > 7 days old (clears `[tool.uv] exclude-newer = "7 days"`, pyproject.toml:197). If 3.8.0 is < 7 days old, approve pinning an older floor (3.7.0 / 3.6.1) instead.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms pytest-xdist is the legitimate pytest-dev package.
    - Human states the floor to pin (`>=3.8.0` if it clears cooldown, else an older release that does).
  </acceptance_criteria>
  <resume-signal>Type "approved" (and the floor to use) or describe concerns</resume-signal>
  <done>Human has verified package legitimacy and approved the install with a specific version floor.</done>
</task>

<task type="auto">
  <name>Task 2: Add pytest-xdist + enable cross-shard coverage combine</name>
  <read_first>
    - pyproject.toml §[tool.coverage.run] (72-75) and §[dependency-groups] dev (216-226)
    - .planning/phases/63-parallel-ci-code-change-gating/63-PATTERNS.md §"pyproject.toml"
  </read_first>
  <files>pyproject.toml, uv.lock</files>
  <action>
    Run `uv add --dev "pytest-xdist>=3.8.0"` (or the older floor the checkpoint approved if 3.8.0 is inside the cooldown window). If `uv` resolution fails on the cooldown, retry with the approved older floor; if it still fails, STOP and surface the blocker (do not disable exclude-newer). Confirm the entry lands alphabetically between `pytest-cov>=7.1.0` and `respx>=0.23.1` in `[dependency-groups] dev` — re-sort if `uv add` appended it out of order (CLAUDE.md alphabetical-deps convention). In `[tool.coverage.run]` add `relative_files = true`; KEEP `concurrency = ["greenlet", "thread"]` exactly as-is — do NOT add `"multiprocessing"` (research A2: xdist workers are execnet processes merged by pytest-cov, not multiprocessing children of the code under test). Leave `[tool.coverage.report] fail_under = 85` unchanged (Phase 64 raises it, not this phase).
  </action>
  <acceptance_criteria>
    - `uv run python -c "import xdist"` exits 0.
    - `grep -c "relative_files = true" pyproject.toml` returns 1.
    - `grep 'concurrency' pyproject.toml` still shows `["greenlet", "thread"]` with no `multiprocessing`.
    - `grep 'fail_under' pyproject.toml` still shows `85`.
    - pytest-xdist appears in `[dependency-groups] dev` immediately after `pytest-cov`.
  </acceptance_criteria>
  <verify>
    <automated>uv sync && uv run python -c "import xdist; print('xdist ok')" && uv run coverage debug config 2>/dev/null | grep -i relative_files</automated>
  </verify>
  <done>pytest-xdist importable under `uv run`; coverage config unions shards by relative path; concurrency + fail_under unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Add test-bucket + coverage-combine recipes and buckets.json</name>
  <read_first>
    - justfile §[group('test')] (75-178) — recipe attribute stanza, `test-ci` and `test-file` bodies
    - .planning/phases/63-parallel-ci-code-change-gating/63-PATTERNS.md §"justfile" (recipe shapes, --fail-under=85 mirror)
    - scripts/update-project.sh:20 + verify sweep (1046-1060) — confirm no edit needed
  </read_first>
  <files>justfile, tests/buckets.json</files>
  <action>
    Add two recipes to `[group('test')]`, mirroring the existing `[doc(...)]` + `[group('test')]` stanza. Recipe `test-bucket NAME XDIST="":` runs `COVERAGE_FILE=.coverage.{{NAME}} uv run pytest tests/{{NAME}} {{XDIST}} --cov=phaze --cov-report= -q` — the empty `XDIST` default keeps DB buckets serial (D-01 revised, Q-B); DB-free buckets pass `XDIST="-n auto"`. Recipe `coverage-combine:` runs three lines: `uv run coverage combine`, then `uv run coverage xml`, then `uv run coverage report --fail-under=85` (mirror `[tool.coverage.report] fail_under`; do NOT raise here). Write `tests/buckets.json` as a JSON array of the 9 canonical bucket names in matrix order: discovery, metadata, fingerprint, analyze, identify, review, agents, integration, shared — this is the single source of truth the Plan 03 matrix (fromJSON) and the Plan 02 partition guard both consume. Confirm `scripts/update-project.sh` needs no edit (the new recipes are not part of its lint/typecheck/test verify sweep per PATTERNS); note this in the summary. The bucket directories do not exist yet — that is expected; these recipes are declarative and are exercised after Plan 02's reorg.
  </action>
  <acceptance_criteria>
    - `just --list` shows `test-bucket` and `coverage-combine` under the test group.
    - `just --show coverage-combine` output contains `coverage combine`, `coverage xml`, and `--fail-under=85`.
    - `tests/buckets.json` parses as a JSON array of exactly the 9 bucket names above.
    - `just --evaluate` (or `just --list`) exits 0 — recipes are syntactically valid.
  </acceptance_criteria>
  <verify>
    <automated>just --list >/dev/null && just --show test-bucket | grep -q 'COVERAGE_FILE=.coverage' && just --show coverage-combine | grep -q 'fail-under=85' && uv run python -c "import json,pathlib; b=json.loads(pathlib.Path('tests/buckets.json').read_text()); assert b==['discovery','metadata','fingerprint','analyze','identify','review','agents','integration','shared'], b; print('buckets ok')"</automated>
  </verify>
  <done>Both recipes valid and delegated-ready; buckets.json is the canonical 9-name list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → dev toolchain | New third-party package (pytest-xdist) enters the build via `uv add` |
| pyproject/uv.lock → CI | Coverage config + lockfile drive the enforced gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01-SC | Tampering | `uv add pytest-xdist` | mitigate | Blocking `checkpoint:human-verify` (Task 1) verifies pytest-dev provenance on pypi.org before install; `[ASSUMED]` gate is never auto-approvable; `exclude-newer = "7 days"` cooldown enforced (no override) |
| T-63-01-02 | Tampering | `[tool.coverage.run] concurrency` | mitigate | Keep `["greenlet","thread"]`; adding `multiprocessing` would silently mis-merge shard data and corrupt the CI-03 baseline Phase 64 raises the gate on — explicitly prohibited |
| T-63-01-03 | Repudiation | coverage gate | accept | `fail_under=85` unchanged; combined number stays the auditable source of truth (no product-runtime surface) |
</threat_model>

<verification>
- `uv sync` clean; `uv run python -c "import xdist"` ok.
- `just --list` shows both new recipes; `tests/buckets.json` valid.
- No change to fail_under (still 85) and no `multiprocessing` in concurrency.
</verification>

<success_criteria>
pytest-xdist installed within the cooldown policy; coverage combine plumbed via
`relative_files`; `just test-bucket` / `just coverage-combine` exist and delegate correctly;
`tests/buckets.json` is the single canonical bucket list.
</success_criteria>

<output>
Create `.planning/phases/63-parallel-ci-code-change-gating/63-01-SUMMARY.md` when done.
</output>
