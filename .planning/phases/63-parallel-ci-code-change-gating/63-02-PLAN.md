---
phase: 63-parallel-ci-code-change-gating
plan: 02
type: execute
wave: 2
depends_on: [63-01]
files_modified:
  - tests/**                                  # mass git mv of test_*.py into tests/<bucket>/<layer>/
  - tests/BUCKETS.md                          # NEW — the file→bucket mapping artifact
  - tests/shared/test_partition_guard.py      # NEW — D-06 structural guard
autonomous: true
requirements: [CI-01, CI-03]
user_setup: []

must_haves:
  truths:
    - "D-03: every collected test_*.py lives under exactly one known tests/<bucket>/ directory — structurally exclusive partition"
    - "D-04: the 9 bucket dirs (discovery, metadata, fingerprint, analyze, identify, review, agents, integration, shared) each exist as packages"
    - "D-05: ONLY tests/conftest.py, tests/_queue_fakes.py, tests/_route_introspection.py, tests/kube_fakes.py stay at tests/ root; every collected test file (test_*.py AND *_test.py, incl. tests/_queue_fakes_test.py) relocates into a bucket"
    - "D-06: a partition-guard test (collecting BOTH test_*.py and *_test.py) fails CI if any collected test escapes a known bucket directory"
    - "CI-01: the suite is partitioned into independently-runnable per-workflow-step buckets"
    - "CI-03: the reorg preserves collection — post-reorg full-suite pass count equals the pre-reorg baseline (no test lost, no double-count)"
  artifacts:
    - path: "tests/BUCKETS.md"
      provides: "Explicit file→bucket mapping for all 212 test files"
      contains: "| bucket |"
    - path: "tests/shared/test_partition_guard.py"
      provides: "Collection-completeness guard + meta-test"
      contains: "KNOWN_BUCKETS"
    - path: "tests/buckets.json"
      provides: "Consumed by the guard as the authoritative bucket set"
      contains: "discovery"
  key_links:
    - from: "tests/shared/test_partition_guard.py"
      to: "tests/buckets.json"
      via: "load bucket set as single source of truth"
      pattern: "buckets.json"
    - from: "reorg moves"
      to: "tests/conftest.py auto-marker"
      via: "test_migrations + integration remain in path_parts"
      pattern: "integration"
---

<objective>
Physically partition the ~212-file suite into the 9 domain buckets (D-03/D-04) with a
behavior-preserving `git mv` reorg, resolve the pre-existing basename collisions and the
`tests.test_migrations.conftest` imports the flatten would break, and add the partition-guard
test (D-06) that keeps the partition trustworthy going forward.

Purpose: CI-01 needs each workflow-step selectable in isolation by path; CI-03's combined
coverage number is only trustworthy if every test lands in exactly one bucket. This is the one
invasive move of the phase — keep it mechanical and prove the full suite is green (same pass
count) BEFORE any CI wiring (Plans 03/04) so a regression cannot hide behind a CI change.
Output: reorganized `tests/<bucket>/`, `tests/BUCKETS.md`, and the partition guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-parallel-ci-code-change-gating/63-CONTEXT.md
@.planning/phases/63-parallel-ci-code-change-gating/63-PATTERNS.md
@.planning/phases/63-parallel-ci-code-change-gating/63-RESEARCH.md

<interfaces>
Auto-marker (tests/conftest.py:116-135) — STAYS at root. It marks a test `integration` when a
DB fixture is used OR `"test_migrations" in path_parts` OR `"integration" in path_parts`. Both
path triggers must survive the reorg (keep those literal segments in the destination paths).

Collision-hook analog (tests/conftest.py:132-135): `for item in items: path_parts = item.path.parts`
— the guard reuses `item.path.parts` to assert the top segment under `tests/` is a known bucket.

Guard skeleton analog (tests/test_dead_template_guard.py:43-95): `_REPO_ROOT = Path(__file__).resolve().parents[1]`,
one set-difference invariant, one assertion whose message enumerates offenders.

Meta-test analog (tests/test_no_default_queue_producers.py:127-138): feed a crafted sample →
assert the guard flags it (proves the guard is not vacuously green).

Root files that STAY (D-05): tests/__init__.py, tests/conftest.py, tests/_queue_fakes.py,
tests/_route_introspection.py, tests/kube_fakes.py. These four helpers are the ENTIRE stay-at-root
allowlist. CRITICAL: pytest's default `python_files` glob is `test_*.py *_test.py`, so
`tests/_queue_fakes_test.py` (4 real tests) IS collected today via the `*_test.py` half — it is a
TEST file, NOT a helper, and MUST be bucketed like every other test (suggested destination
`tests/shared/core/queue_fakes_test.py`). Everywhere this plan enumerates or guards "collected
tests" it means BOTH globs: `test_*.py` AND `*_test.py`. (Verified: `_queue_fakes_test.py` is the
only `*_test.py` file today, but keep the enumeration/guard globs broad so a reintroduction cannot
silently escape.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture baseline + write the file→bucket mapping (tests/BUCKETS.md)</name>
  <read_first>
    - .planning/phases/63-parallel-ci-code-change-gating/63-RESEARCH.md §"Open Questions Q-C" (mapping is manual/semantic; guard enforces completeness) and §"Recommended Project Structure"
    - tests/buckets.json (from Plan 01 — the 9 canonical names)
  </read_first>
  <files>tests/BUCKETS.md</files>
  <action>
    First capture the PRE-reorg baseline so the reorg can be proven behavior-preserving: run the
    full suite via `just integration-test` and record the exact passed/failed/skipped counts, and
    run a coverage total (`uv run coverage report` after a covered run, or reuse `just test-ci`)
    and record the combined percentage (~90.38% expected). Write both numbers into BUCKETS.md
    header — they are the acceptance target for Task 2.

    Then assign every collected test file to exactly one bucket and record it in `tests/BUCKETS.md`
    as a table (columns: current path | bucket | destination path). Enumerate with BOTH pytest
    globs — `find tests \( -name 'test_*.py' -o -name '*_test.py' \)` — so the root file
    `tests/_queue_fakes_test.py` (collected via the `*_test.py` glob; 4 real tests) is NOT dropped.
    Add it to the table with destination `tests/shared/core/queue_fakes_test.py` (the `core` layer
    convention for root-originating files). Assignment rubric (keyword →
    bucket; a file matches the first rule that applies, else `shared`):
      - discovery: scan*, ingestion, companion, collision, dedup, agent_watcher/*, scan_reaper, scan_deletion, agent_files*, scan_batches
      - metadata: metadata*, agent_metadata
      - fingerprint: fingerprint*, agent_fingerprint, pipeline_fingerprint
      - analyze: analysis*, process_file*, pool, stage_* (pause/priority/progress/control/concurrency/resume/endpoints), pipeline_counters, reenqueue, recovery, reconcile_cloud, submit_cloud, s3_upload, staging_cron, push_pipeline, cloud_staging, kube_staging, s3_staging, job_runner, ledger*, scheduling_ledger, controller_reenqueue, queue_defaults, queue_factory, queue_fakes*, deterministic_key
      - identify: identify*, tracklist*, discogs*, search*, scrape, match, agent_tracklists
      - review: review*, proposal*, tags, tag_*, cue*, duplicates, execution*, execute_approved*, agent_proposals, agent_exec*, summarize_dict
      - agents: agent_* (auth/identity/heartbeat/liveness/client/bootstrap/upsert/task_router/startup/presign/push/s3), admin_agents, deployment/*, cli/agents_add, parity/*, model_bootstrap, cert_bootstrap, shared_agent_bootstrap
      - integration: everything currently under tests/integration/ AND tests/test_migrations/ (real-Postgres) — keep both `integration` and `test_migrations` literal segments in the destination (auto-marker depends on them)
      - shared (catch-all): config*, models/*, schemas/* (unless clearly a workflow-step schema), health, logging*, main_lifespan, entrypoint, constants, database, redirect*, shell_routes, a11y, base_html_sri, docs_ia, humanize, rail_narrow_width, record_palette_agents, enrich_analyze_workspaces, review_apply_workspaces, identify_workspaces (workspace UI tests → shared unless obviously a single step), template_helpers/*, web/*, task_split, routing_seam, no_default_queue_producers, no_auto_metadata_enqueue, phase0*_gaps, migration_019_dedupe, proposals_upsert, pipeline_dag_context
    IMPORTANT: exact bucket choice for ambiguous files is LOW-risk — a test runs identically
    regardless of which bucket dir holds it; the bucket only determines its coverage shard, and
    the guard only checks it lands in SOME known bucket. So resolve ambiguity by the rubric and
    move on; do not agonize. Destination path convention (collision-safe, see Task 2):
    `tests/<bucket>/<layer>/<basename>` where `<layer>` = the file's current immediate parent dir
    name (services, routers, schemas, tasks, models, config, deployment, cli, agent_watcher,
    parity, template_helpers, utils, web, scripts, test_migrations, integration) or `core` for
    files currently at `tests/` root.
  </action>
  <acceptance_criteria>
    - BUCKETS.md header records the pre-reorg passed count and coverage %.
    - Every file from `find tests \( -name 'test_*.py' -o -name '*_test.py' \)` appears in the BUCKETS.md table exactly once (incl. tests/_queue_fakes_test.py).
    - Every assigned bucket is one of the 9 names in tests/buckets.json.
    - No destination path collides (two rows never share the same destination path).
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import pathlib,subprocess; txt=pathlib.Path('tests/BUCKETS.md').read_text(); rows=[l for l in txt.splitlines() if l.strip().startswith('| tests/')]; files=subprocess.check_output(['bash','-lc',\"find tests \\( -name 'test_*.py' -o -name '*_test.py' \\) | sort\"]).decode().split(); assert len(rows)>=200, f'rows={len(rows)}'; assert 'tests/_queue_fakes_test.py' in txt, '_queue_fakes_test.py missing from mapping'; print('mapping rows', len(rows), 'files', len(files))"</automated>
  </verify>
  <done>Baseline captured; complete, collision-free, one-bucket-per-file mapping written.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the reorg (git mv + __init__.py + fix migrations imports) and prove green</name>
  <read_first>
    - .planning/phases/63-parallel-ci-code-change-gating/63-RESEARCH.md §"Common Pitfalls" (Pitfalls 1-3: basename collisions, tests.test_migrations.conftest imports, path auto-marker)
    - tests/BUCKETS.md (from Task 1)
    - tests/test_migrations/conftest.py + the ~13 importer sites (`grep -rln 'from tests.test_migrations.conftest' tests/`)
  </read_first>
  <files>tests/** (mass move)</files>
  <action>
    Move every mapped test file per the BUCKETS.md destination column using `git mv` (drive it from a
    shell loop reading the table so it is mechanical and reproducible). This includes
    `tests/_queue_fakes_test.py` → `tests/shared/core/queue_fakes_test.py` (a `*_test.py` file — do
    not skip it; a `test_*.py`-only loop would orphan it at root and silently drop its 4 tests from
    every matrix leg). Create every new directory
    (`tests/<bucket>/` and each `<layer>/` subdir) as a package by adding an empty `__init__.py`
    — `import-mode=prepend` requires packages, and layer sub-nesting is what keeps the ~18
    colliding basenames (test_fingerprint.py, test_pipeline.py, test_proposal.py, test_execution.py,
    test_agent_*.py, test_companion.py, test_scan.py, test_tracklist.py, test_scheduling_ledger.py,
    test_pipeline_scans.py) unique as distinct dotted modules. Keep the migrations tests AND their
    conftest at `tests/integration/test_migrations/` so both `"integration"` and `"test_migrations"`
    survive in `path_parts` (auto-marker Pitfall 3). Rewrite every `from tests.test_migrations.conftest`
    import to `from tests.integration.test_migrations.conftest` (mechanical `grep -rl … | xargs sed`;
    ~13 sites). Do NOT move the four root helpers or the root conftest (D-05). Do NOT switch
    `import-mode` to importlib. After moving: confirm zero same-directory basename collisions with
    `find tests -name 'test_*.py' -printf '%h %f\n' | sort | uniq -d` (must be empty), then run the
    FULL suite via `just integration-test` and confirm the passed count EQUALS the Task-1 baseline
    (no test dropped, none newly failing). If the count differs, diagnose (most likely a stray
    collision, an unmoved file, or an unrewritten migrations import) before proceeding.
  </action>
  <acceptance_criteria>
    - `find tests -maxdepth 1 \( -name 'test_*.py' -o -name '*_test.py' \)` returns nothing (all root test files, incl. _queue_fakes_test.py, relocated).
    - No same-directory duplicate basenames (uniq -d over `%h %f` empty).
    - `grep -rl 'from tests.test_migrations.conftest' tests/` returns nothing (all rewritten).
    - Every new bucket/layer dir has an `__init__.py`.
    - Root helpers unchanged: tests/conftest.py, tests/_queue_fakes.py, tests/_route_introspection.py, tests/kube_fakes.py still at tests/ root.
    - `just integration-test` passed count == Task-1 baseline passed count.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(find tests -maxdepth 1 \( -name 'test_*.py' -o -name '*_test.py' \))" && test -z "$(find tests \( -name 'test_*.py' -o -name '*_test.py' \) -exec sh -c 'echo "$(dirname "$1") $(basename "$1")"' _ {} \; | sort | uniq -d)" && ! grep -rq 'from tests.test_migrations.conftest' tests/ && just integration-test</automated>
  </verify>
  <done>All test files partitioned into buckets, collision-free, migrations imports fixed, full suite green at baseline count.</done>
</task>

<task type="auto">
  <name>Task 3: Partition-guard test (D-06) + meta-test</name>
  <read_first>
    - tests/conftest.py:132-135 (item.path.parts walk — the exact surface to reuse)
    - tests/test_dead_template_guard.py:43-95 (guard skeleton: _REPO_ROOT, set-difference invariant, one enumerating assertion)
    - tests/test_no_default_queue_producers.py:127-138 (meta-test pattern)
    - tests/buckets.json (authoritative bucket set)
  </read_first>
  <files>tests/shared/test_partition_guard.py</files>
  <action>
    Create `tests/shared/test_partition_guard.py`. Load `KNOWN_BUCKETS` from `tests/buckets.json`
    (single source of truth — do NOT hardcode the list; that is what prevents matrix/guard drift).
    Implement the guard as a `pytest_collection_modifyitems`-style check OR a test that walks the
    collected session. It MUST cover BOTH pytest collection globs (`test_*.py` AND `*_test.py`) —
    walking collected `item`s already covers both since pytest collects both by default; if instead
    you enumerate files on disk, glob both patterns so a `*_test.py` reintroduction at root is
    caught (this is the exact blind spot that would have dropped tests/_queue_fakes_test.py). For
    every collected test item, compute the path segment immediately under
    `tests/` (`item.path.parts` relative to the tests root) and assert it is in `KNOWN_BUCKETS`.
    The single assertion message must enumerate each offending file plus its (unknown) top segment
    so a future unbucketed test fails loud. Add a meta-test that feeds a synthetic path
    `tests/not_a_bucket/test_x.py` (or a crafted item/path) through the same segment-extraction +
    membership logic and asserts it is flagged — proving the guard is not vacuously green. Place
    the guard's own file under `tests/shared/` so it lives inside a known bucket (it must pass its
    own check).
  </action>
  <acceptance_criteria>
    - `KNOWN_BUCKETS` is read from tests/buckets.json, not hardcoded.
    - Guard covers both `test_*.py` and `*_test.py` collection globs (a root `*_test.py` would fail it).
    - Guard passes over the real (post-reorg) tree.
    - Meta-test proves a `tests/not_a_bucket/...` path is flagged.
    - The guard file itself resides under `tests/shared/` (a known bucket).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/shared/test_partition_guard.py -x -q</automated>
  </verify>
  <done>Partition guard green over the reorganized tree, backed by buckets.json, with a passing meta-test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test layout → coverage combine | Which files land in which bucket determines coverage-shard completeness (CI-03) |
| reorg → runtime behavior | A mishandled move could silently drop tests or change collection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-02-01 | Tampering | reorg drops/duplicates a test | mitigate | Pre/post baseline pass-count equality (Task 2) + partition guard (Task 3) fail CI on any test escaping a bucket; `uniq -d` collision check |
| T-63-02-02 | DoS (flaky) | shared phaze_test DB race | accept | DB buckets stay serial (no `-n auto`); reorg does not touch fixtures — no shared-DB rework (out of scope) |
| T-63-02-03 | Repudiation | coverage total drifts silently | mitigate | Combined `coverage report` total compared to the recorded pre-reorg baseline (~90.38%) as an acceptance check; guard prevents zero-bucket drops |
| T-63-02-SC | Tampering | no new packages here | accept | This plan installs nothing; the only new dep (pytest-xdist) is gated in Plan 01 |
</threat_model>

<verification>
- All test_*.py under a known `tests/<bucket>/`; root helpers + conftest unmoved.
- No basename collisions within any directory; migrations imports rewritten.
- `just integration-test` passes at the recorded baseline count.
- `tests/shared/test_partition_guard.py` green + meta-test green.
</verification>

<success_criteria>
The suite is a structurally-exclusive directory partition (CI-01), behavior-preserving
(baseline pass count held), with a guard (D-06) that keeps it trustworthy for CI-03.
</success_criteria>

<output>
Create `.planning/phases/63-parallel-ci-code-change-gating/63-02-SUMMARY.md` when done.
</output>
