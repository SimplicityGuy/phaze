---
phase: 59-identify-workspaces
plan: 02
type: execute
wave: 2
depends_on: [59-01]
files_modified:
  - src/phaze/templates/pipeline/partials/trackid_workspace.html
  - src/phaze/routers/shell.py
  - tests/test_identify_workspaces.py
autonomous: true
requirements: [IDENT-01]
must_haves:
  truths:
    - "D-03: ONE combined per-file table (not two sub-sections) with columns File · audfprint · Panako · Tracklist · Confidence"
    - "D-01: audfprint and Panako cells render status words (done/failed/pending) colored per UI-SPEC, never hue-only"
    - "D-02: no numeric fingerprint score column — Confidence is the tracklist match_confidence int, not a fabricated fingerprint score"
    - "D-04: Tracklist cell shows matched/candidate/no-match; Confidence shows linked tracklist's match_confidence or best candidate, '—' when none"
    - "GET /s/trackid returns a bare fragment (no <html>/<head>); shell.py STAGE_PARTIALS['trackid'] is a static string literal (T-57-01)"
    - "Track-ID rows are inert-but-present (row_id_prefix='trackid-row', click UNBOUND); no second poll loop in the fragment"
  artifacts:
    - path: "src/phaze/templates/pipeline/partials/trackid_workspace.html"
      provides: "Track-ID combined per-file identity table (Pattern A)"
      min_lines: 25
    - path: "src/phaze/routers/shell.py"
      provides: "trackid STAGE_PARTIALS entry + _render_stage trackid branch"
      contains: "trackid_workspace.html"
  key_links:
    - from: "src/phaze/routers/shell.py"
      to: "get_trackid_stage_files"
      via: "_render_stage trackid branch context['trackid_files']"
      pattern: "get_trackid_stage_files"
    - from: "src/phaze/templates/pipeline/partials/trackid_workspace.html"
      to: "_file_table.html"
      via: "include with columns + ns.rows feed"
      pattern: "_file_table.html"
---

<objective>
Build the Track-ID workspace: one combined per-file identity table surfacing existing audfprint + Panako fingerprint state and tracklist match/confidence, superseding the `trackid` placeholder in `STAGE_PARTIALS`.

Purpose: First UI surface of `FingerprintResult.status` per engine + the linked/best-candidate tracklist confidence (IDENT-01). A read-only consolidated view — no trigger button (the prototype's AcoustID IDENTIFY flow is deferred IDENT-03).

Output: `trackid_workspace.html` fragment + the `shell.py` wiring (STAGE_PARTIALS entry, `_render_stage` branch, helper import) + the converted Track-ID behavior tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-identify-workspaces/59-CONTEXT.md
@.planning/phases/59-identify-workspaces/59-UI-SPEC.md
@.planning/phases/59-identify-workspaces/59-PATTERNS.md
@.planning/phases/59-identify-workspaces/59-01-SUMMARY.md

<interfaces>
<!-- Reuse these directly — no codebase exploration needed. -->

shell.py STAGE_PARTIALS + _render_stage pattern (src/phaze/routers/shell.py):
  STAGE_PARTIALS["trackid"] = _STAGE_PLACEHOLDER  ← replace with "pipeline/partials/trackid_workspace.html" (static literal; T-57-01)
  _render_stage has elif branches for "metadata"/"fingerprint" (:116-129) injecting read-only context;
  add `elif stage == "trackid": context["trackid_files"] = await get_trackid_stage_files(session)`
  import line :33 — extend: from phaze.services.pipeline import get_fingerprint_pending_files, get_metadata_pending_files, get_trackid_stage_files
  oob_counts stays False on the stage render; fragment-vs-full fork (:131-133) is UNCHANGED.

get_trackid_stage_files(session) -> list[dict] (from Plan 59-01) per-row keys:
  filename, path, audfprint_status, panako_status, tracklist_state, confidence

_workspace_scaffold.html macro (import as ws):
  {% call ws.workspace(title, subcount='', actions='', cloud_cards=false) %} ... {% endcall %}
  emits exactly one <h1 tabindex="-1">; auto-includes _workspace_poll_seeds.html; OMIT actions= for Track-ID (empty slot)

_file_table.html context contract:
  columns: list[str]; rows: list of cell-dict lists {text, mono?, title?, color?} (text ALWAYS autoescaped, NEVER | safe);
  empty_heading, empty_body, row_id_prefix (inert rows: cursor-pointer + stable id, click UNBOUND), optional table_id

UI-SPEC colors (Track-ID):
  fingerprint done "done" text-emerald-600 dark:text-emerald-300 | failed "failed" text-rose-600 dark:text-rose-400 | pending "pending" text-gray-500 dark:text-gray-400
  tracklist state "matched" emerald-300 | "candidate" amber-300 | "no match" gray-500
  confidence {n}% : ≥90 emerald-300 / 70-89 amber-300 / <70 gray-500 ; none "—" gray-500
  subcount JS string: `${$store.pipeline.fingerprintDone} fingerprinted · ${$store.pipeline.tracklistDone} with a tracklist match`
  empty: heading "Nothing to identify" / body "No discovered files carry a fingerprint or tracklist signal yet. Run Fingerprint or Tracklist Search first."

Analog to copy: src/phaze/templates/pipeline/partials/fingerprint_workspace.html (scaffold + single _file_table feed) — but with EMPTY actions slot.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create trackid_workspace.html (Pattern A combined table)</name>
  <files>src/phaze/templates/pipeline/partials/trackid_workspace.html</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/fingerprint_workspace.html (the scaffold + single-table analog; copy the ns.rows feed idiom, drop the actions/trigger block)
    - src/phaze/templates/pipeline/partials/_workspace_scaffold.html (macro signature + empty-actions behavior)
    - src/phaze/templates/pipeline/partials/_file_table.html (cell-dict contract, autoescape boundary, inert-row + empty-state)
    - .planning/phases/59-identify-workspaces/59-UI-SPEC.md (NEW Pattern A; Color → Track-ID engine/match/confidence; Copywriting → empty states)
    - .planning/phases/59-identify-workspaces/59-PATTERNS.md (trackid_workspace.html section)
  </read_first>
  <action>
    Create src/phaze/templates/pipeline/partials/trackid_workspace.html. Import `_workspace_scaffold.html` as `ws`. Set `subcount` to the JS string `` `${$store.pipeline.fingerprintDone} fingerprinted · ${$store.pipeline.tracklistDone} with a tracklist match` ``. Call `{% call ws.workspace(title="TRACK-ID", subcount=subcount) %}` with NO `actions=` arg (read-only view — the dropped AcoustID IDENTIFY button, IDENT-03). Inside a `<div class="p-6">`, set `columns = ["File", "audfprint", "Panako", "Tracklist", "Confidence"]`, build `ns = namespace(rows=[])`, loop `for f in trackid_files` appending one cell-dict list per row: File cell `{'text': f.filename, 'mono': True, 'title': f.path}`; audfprint cell `{'text': af_word, 'color': af_color}`; Panako cell `{'text': pk_word, 'color': pk_color}`; Tracklist cell `{'text': match_word, 'color': match_color}`; Confidence cell `{'text': conf_text, 'mono': True, 'color': conf_color}`. Derive af_word/af_color and pk_word/pk_color from f.audfprint_status / f.panako_status using the UI-SPEC colors (done→emerald, failed→rose, pending→gray). Derive match_word/match_color from f.tracklist_state (matched→emerald, candidate→amber, no match→gray). Derive conf_text as `{n}%` (or "—" when f.confidence is none) tiered emerald/amber/gray per UI-SPEC. Set `empty_heading="Nothing to identify"`, `empty_body="No discovered files carry a fingerprint or tracklist signal yet. Run Fingerprint or Tracklist Search first."`, `row_id_prefix="trackid-row"`, `table_id="trackid-file-table"`, then `{% include "pipeline/partials/_file_table.html" %}`. Do NOT add any hx-get on rows, any hx-trigger="every"/setInterval, any `| safe`, any trigger button, or the legacy confidence_badge/status_badge pills. Run `just tailwind` if any new utility class is introduced (app.css is a build artifact, never committed).
  </action>
  <verify>
    <automated>uv run pytest tests/test_identify_workspaces.py::test_identify_fragments_are_bare tests/test_identify_workspaces.py::test_identify_single_poll_discipline -x</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `grep -c "tabindex" src/phaze/templates/pipeline/partials/trackid_workspace.html` shows the single scaffold h1 is the only focus target (via the imported macro — the fragment itself adds none)
    - Fragment contains NO `<html`/`<head`/`<header`/`{% extends`, NO `hx-trigger="every`, NO `setInterval`, NO `| safe`, NO `confidence_badge`/`status_badge` include, NO trigger `<button` (read-only view)
    - Columns are exactly File · audfprint · Panako · Tracklist · Confidence (D-03 one combined table)
    - `grep -v '^[[:space:]]*{#' src/phaze/templates/pipeline/partials/trackid_workspace.html | grep -c 'hx-get'` is 0 (inert rows)
  </acceptance_criteria>
  <done>trackid_workspace.html renders the one combined identity table via the reused scaffold + file table, read-only, inert rows, no second poll.</done>
</task>

<task type="auto">
  <name>Task 2: Wire shell.py trackid stage + convert Track-ID behavior tests</name>
  <files>src/phaze/routers/shell.py, tests/test_identify_workspaces.py</files>
  <read_first>
    - src/phaze/routers/shell.py (STAGE_PARTIALS dict :55-77, _render_stage branches :96-133, import line :33)
    - src/phaze/templates/pipeline/partials/trackid_workspace.html (the fragment from Task 1 — the context keys it consumes)
    - tests/test_identify_workspaces.py (the IDENT-01 stubs to convert: test_trackid_table_signals, test_trackid_success_renders_done)
    - .planning/phases/59-identify-workspaces/59-RESEARCH.md (Pitfall 1 + §4 shell wiring; Test Map IDENT-01 rows)
  </read_first>
  <action>
    In src/phaze/routers/shell.py: replace `"trackid": _STAGE_PLACEHOLDER,` with `"trackid": "pipeline/partials/trackid_workspace.html",` (a STATIC string literal — never an f-string/format; T-57-01) and add the Phase-59 comment convention (cite phase + IDENT-01 + T-57-01 + supersede-in-place) mirroring the Phase-58 metadata/fingerprint comments. Extend the existing `from phaze.services.pipeline import ...` line (:33) to also import `get_trackid_stage_files` (combine-as-imports, force-sort-within-sections). In `_render_stage`, append `elif stage == "trackid": context["trackid_files"] = await get_trackid_stage_files(session)` after the fingerprint branch; keep `oob_counts` False (no router try/except — the helper is degrade-safe). Do NOT delete any legacy template (supersede-in-place; CUT-02 is Phase 62). Then in tests/test_identify_workspaces.py convert the two IDENT-01 xfail stubs to real assertions: test_trackid_table_signals seeds files with FingerprintResult rows (audfprint success, panako failed) + a linked Tracklist(match_confidence) and a candidate-only file, then GETs /s/trackid (HX) and asserts within the `id="trackid-file-table"` scoped substring that the status words ("done"/"failed"/"pending") and match-state words appear, exactly one combined table, and rows carry no hx-get; test_trackid_success_renders_done (Pitfall-1 guard) seeds FingerprintResult(status="success") and asserts the row renders "done" NOT "pending". Use the scoped-substring idiom `tbl = body[body.index('id="trackid-file-table"'):]`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_identify_workspaces.py tests/test_dead_template_guard.py tests/test_shell_routes.py -x && uv run mypy src/phaze/routers/shell.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'trackid' src/phaze/routers/shell.py` shows STAGE_PARTIALS['trackid'] is the static literal "pipeline/partials/trackid_workspace.html" (no f-string/format/concatenation)
    - `_render_stage` has an `elif stage == "trackid":` branch calling `get_trackid_stage_files`; oob_counts unchanged (False)
    - test_trackid_table_signals and test_trackid_success_renders_done are real assertions (xfail removed) and pass
    - GET /s/trackid (HX) returns a fragment containing the audfprint/panako status words from seeded FingerprintResult rows
    - `uv run pytest tests/test_dead_template_guard.py -x` green (new fragment reachable; no legacy template deleted) and `uv run pytest tests/test_shell_routes.py -x` green
    - `uv run mypy src/phaze/routers/shell.py` clean
  </acceptance_criteria>
  <done>/s/trackid serves the real Track-ID workspace; IDENT-01 behavior tests pass; dead-template + shell-route guards stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /s/{stage} | `stage` path segment is whitelisted against STAGE_PARTIALS; must never reach a template path |
| DB → template cell | filename/path/artist values cross into rendered HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | shell.py STAGE_PARTIALS['trackid'] | mitigate | value is a STATIC string literal; `stage` is matched against the dict keys and NEVER spliced into a template path (unknown stage 404s at shell.py:149-151) |
| T-59-XSS | Info-disclosure/Tampering | trackid_workspace.html table cells | mitigate | all DB-sourced `text` rendered via _file_table.html autoescape; NO `| safe`; color is a caller-controlled class string, not user data |
| T-59-DOS | Denial of Service | _render_stage trackid branch | mitigate | get_trackid_stage_files is degrade-safe ([] on DB error); no router try/except needed; oob_counts stays False (Pitfall 5) |
| T-59-SC | Tampering | package installs | accept | Phase 59 installs nothing; no supply-chain surface |
</threat_model>

<verification>
- `uv run pytest tests/test_identify_workspaces.py tests/test_dead_template_guard.py tests/test_shell_routes.py -x` green
- `uv run mypy src/phaze/routers/shell.py` clean
- `uv run ruff check src/phaze/routers/shell.py tests/test_identify_workspaces.py` clean
- pre-commit hooks pass (never --no-verify)
</verification>

<success_criteria>
- /s/trackid renders one combined per-file identity table (D-03) with audfprint/Panako status words (D-01) and tracklist match/confidence (D-04), no fabricated score (D-02)
- STAGE_PARTIALS['trackid'] is a static literal (T-57-01); rows inert; no second poll loop
- IDENT-01 behavior tests pass incl. the Pitfall-1 success⟶done guard; dead-template + shell-route guards green
</success_criteria>

<output>
Create `.planning/phases/59-identify-workspaces/59-02-SUMMARY.md` when done
</output>
</content>
