---
phase: 59-identify-workspaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_identify_workspaces.py
  - src/phaze/services/pipeline.py
autonomous: true
requirements: [IDENT-01, IDENT-02]
must_haves:
  truths:
    - "D-01: get_trackid_stage_files returns per-file audfprint + panako status where done==FingerprintResult.status=='success' (tolerating 'completed'), failed=='failed', pending==no row for (file_id, engine)"
    - "D-02: no numeric fingerprint score is fabricated — the assembled row carries only engine status words + tracklist confidence (the existing match_confidence int)"
    - "D-04: tracklist match-state is 'matched' + Tracklist.match_confidence when file_id IS NOT NULL, else best-candidate via match_confidence desc nulls_last, else 'no match'"
    - "D-07: get_tracklist_set_rows returns per-set N/M track coverage from TracklistTrack.confidence over the linked tracklist's tracks"
    - "Both new helpers are read-only (no enqueue, no session.commit, no schema change) and degrade-safe — return [] inside a SAVEPOINT on any DB error"
    - "tests/test_identify_workspaces.py collects cleanly with foundation tests filled (bare-fragment + single-poll) and IDENT-01/IDENT-02 behavior stubs present"
  artifacts:
    - path: "src/phaze/services/pipeline.py"
      provides: "get_trackid_stage_files + get_tracklist_set_rows read-only assembly helpers"
      contains: "async def get_trackid_stage_files"
    - path: "tests/test_identify_workspaces.py"
      provides: "Phase-59 test surface mirroring tests/test_enrich_analyze_workspaces.py"
      contains: "_WORKSPACE_STAGES"
  key_links:
    - from: "src/phaze/services/pipeline.py"
      to: "fingerprint_results / tracklists / tracklist_versions / tracklist_tracks"
      via: "read-only SELECT with LEFT JOIN"
      pattern: "get_trackid_stage_files|get_tracklist_set_rows"
---

<objective>
Lay the Phase-59 foundation: the Wave-0 test surface plus the two read-only row-assembly helpers both Identify workspaces consume. No template or shell wiring here — this plan establishes the data contracts and the test scaffold that Plans 02/03 fill.

Purpose: The two new row shapes (Track-ID per-file: audfprint/panako status + tracklist match/confidence; Tracklist per-set: N/M track coverage + match state) are the ONLY genuinely new code in the phase (RESEARCH "Don't Hand-Roll" key insight). Building them first, with unit tests, gives Plans 02/03 a tested data layer to render against.

Output: `tests/test_identify_workspaces.py` (foundation tests filled, IDENT-01/02 behavior stubs) and two degrade-safe read-only helpers in `services/pipeline.py` with unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-identify-workspaces/59-CONTEXT.md
@.planning/phases/59-identify-workspaces/59-RESEARCH.md
@.planning/phases/59-identify-workspaces/59-PATTERNS.md
@.planning/phases/59-identify-workspaces/59-VALIDATION.md

<interfaces>
<!-- Extracted from the live tree — executor should use these directly, no codebase exploration needed. -->

FingerprintResult (src/phaze/models/fingerprint.py) — NO score column:
  file_id: uuid (FK files.id), engine: str(30), status: str(20), error_message: str|None
  unique index ix_fprint_file_engine on (file_id, engine)
  PERSISTED VOCAB (Pitfall 1): engine ∈ {"audfprint","panako"} (lowercase); status ∈ {"success","failed"} — "completed" is NEVER written by the agent path.

Tracklist (src/phaze/models/tracklist.py):
  file_id: uuid|None (FK), match_confidence: int|None, auto_linked: bool, latest_version_id: uuid|None,
  artist/event: Text|None, status: str (default "approved"), source: str (default "1001tracklists")
TracklistVersion: id, tracklist_id (FK tracklists.id), version_number
TracklistTrack: id, version_id (FK tracklist_versions.id), position, confidence: float|None

Phase-58 precedent helper to MIRROR VERBATIM (src/phaze/services/pipeline.py:768-835):
  async def get_analyze_stage_files(session) -> list[dict[str, Any]]
    try:
        async with session.begin_nested():     # SAVEPOINT degrade-safe wrapper
            stmt = select(...).select_from(FileRecord).outerjoin(...).order_by(...)
            rows = (await session.execute(stmt)).all()
    except Exception:
        logger.warning("analyze_stage_files_degraded", exc_info=True); return []
    # then loop rows -> list of plain dicts (NOT ORM objects)

Existing tracklist pending helpers (reuse, do NOT reimplement):
  get_untracked_files(session) -> list[FileRecord]          # services/pipeline.py:1183 — search pending
  get_scrape_pending_tracklists(session) -> list[Tracklist] # :668
  get_match_pending_tracklists(session) -> list[Tracklist]  # :682
  get_stage_progress(session) -> dict[node, {"done": int, "total": int|None}]  # :294 — scan_search/scrape/match counts
  Best-candidate ordering (D-04): Tracklist.match_confidence.desc().nulls_last()  (tracklists.py:101)

Test analog to copy whole shape (tests/test_enrich_analyze_workspaces.py):
  async fixtures `client: AsyncClient` + `session: AsyncSession` (in conftest.py)
  module-level async _seed_* helpers = ORM inserts only (session.add + await session.commit() + await session.refresh())
  _seed_file(session, *, state, original_filename, file_type, file_size) -> FileRecord  (copy verbatim, :56-82)
  foundation tests test_stage_fragment_is_bare (:139), test_single_poll_discipline (:154)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the Phase-59 test scaffold (foundation tests filled + IDENT stubs)</name>
  <files>tests/test_identify_workspaces.py</files>
  <read_first>
    - tests/test_enrich_analyze_workspaces.py (the exact analog — copy its module structure, fixtures usage, _seed_file helper, and the two foundation tests verbatim, retargeted to trackid/tracklist)
    - .planning/phases/59-identify-workspaces/59-RESEARCH.md (§ Validation Architecture → Test Map; § "Existing test patterns to mirror")
    - .planning/phases/59-identify-workspaces/59-PATTERNS.md (the test table: which Phase-58 test each new test mirrors)
    - src/phaze/models/fingerprint.py and src/phaze/models/tracklist.py (seed-helper field sets)
  </read_first>
  <behavior>
    - test_identify_fragments_are_bare: GET /s/trackid and /s/tracklist with HX-Request:true return 200 and contain neither "<html" nor "<head" (passes against placeholder today; guards R-5)
    - test_identify_single_poll_discipline: GET / fires exactly one hx-get="/pipeline/stats"; neither /s/trackid nor /s/tracklist fragment contains 'hx-trigger="every' or "setInterval" (WORK-05/R-2)
    - IDENT-01/IDENT-02 behavior tests present as xfail stubs that COLLECT cleanly now (filled by Plans 02/03): test_trackid_table_signals, test_trackid_success_renders_done, test_tracklist_step_cards_and_triggers, test_tracklist_per_set_coverage
  </behavior>
  <action>
    Create tests/test_identify_workspaces.py mirroring tests/test_enrich_analyze_workspaces.py. Add `from __future__ import annotations`; import FingerprintResult, Tracklist, TracklistVersion, TracklistTrack, FileRecord, FileState. Define `_WORKSPACE_STAGES = ["trackid", "tracklist"]`. Copy `_seed_file` verbatim, then add module-level async seed helpers (ORM inserts only — session.add + await session.commit() + await session.refresh(); NO backend change): `_seed_fingerprint_result(session, file_id, engine, status)`, `_seed_tracklist(session, *, file_id=None, match_confidence=None, external_id=...)`, `_seed_tracklist_version(session, tracklist_id)`, `_seed_tracklist_track(session, version_id, confidence)`. FILL the two foundation tests (test_identify_fragments_are_bare, test_identify_single_poll_discipline) against /s/trackid and /s/tracklist exactly as the analog does for the Phase-58 stages. Add the four IDENT behavior tests as `@pytest.mark.xfail(reason="filled by Plan 59-02/59-03")` stubs that import-and-collect cleanly (a trivial assertion or `pytest.skip`-free xfail body) so the file is green now and Plans 02/03 convert them. Run via `uv run pytest` (NEVER bare pytest). Keep ruff line-length 150; tests may use T201/PLC/S105 per per-file ignores.
  </action>
  <verify>
    <automated>uv run pytest tests/test_identify_workspaces.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_identify_workspaces.py -x` exits 0 (foundation tests pass; IDENT stubs xfail/collect cleanly)
    - File defines `_WORKSPACE_STAGES = ["trackid", "tracklist"]` and the four `_seed_*` helpers plus copied `_seed_file`
    - test_identify_single_poll_discipline asserts `body.count('hx-get="/pipeline/stats"') == 1` and absence of `hx-trigger="every` / `setInterval` for both new fragments
    - No `session.commit` of any production-table mutation outside the test seed helpers; no enqueue calls
  </acceptance_criteria>
  <done>The Phase-59 test file exists, collects + runs green, with foundation tests filled and IDENT-01/02 behavior stubs ready for Plans 02/03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the two read-only row-assembly helpers to services/pipeline.py</name>
  <files>src/phaze/services/pipeline.py, tests/test_identify_workspaces.py</files>
  <read_first>
    - src/phaze/services/pipeline.py:759-835 (get_analyze_stage_files + _ANALYZE_STAGE_STATES — the degrade-safe SAVEPOINT + dict-shaping pattern to mirror VERBATIM)
    - src/phaze/services/pipeline.py:668-699 (get_scrape_pending_tracklists / get_match_pending_tracklists — ORM ~exists/.not_in idioms)
    - src/phaze/services/pipeline.py:1183-1197 (get_untracked_files — MUSIC_VIDEO_TYPES filter)
    - src/phaze/models/fingerprint.py + src/phaze/models/tracklist.py (column shapes; Pitfall 1 vocab)
    - .planning/phases/59-identify-workspaces/59-RESEARCH.md (§1/§2 derivation rules; Pitfall 1, 2)
  </read_first>
  <behavior>
    - get_trackid_stage_files: returns a list of plain dicts, one per in-stage file (music/video file having ≥1 FingerprintResult row OR any linked/candidate Tracklist — RESEARCH Open-Q2 signal-bearing set). Each dict carries: filename, path, audfprint_status ("done"/"failed"/"pending"), panako_status, tracklist_state ("matched"/"candidate"/"no match"), confidence (int|None). done⟺status=="success" (tolerate "completed"); pending⟺no (file_id,engine) row.
    - get_tracklist_set_rows: returns a list of plain dicts, one per set/file carrying a tracklist — set name, path, tracklist_state, tracks_confident (int), tracks_total (int) from TracklistTrack.confidence over the linked tracklist, matched_to_file (bool from Tracklist.file_id).
    - Both return [] inside a SAVEPOINT on any DB error (degrade-safe; never raise into the hot render/poll).
  </behavior>
  <action>
    Add `async def get_trackid_stage_files(session) -> list[dict[str, Any]]` and `async def get_tracklist_set_rows(session) -> list[dict[str, Any]]` to src/phaze/services/pipeline.py, mirroring get_analyze_stage_files VERBATIM for the `async with session.begin_nested(): ... except Exception: logger.warning("trackid_stage_files_degraded"/"tracklist_set_rows_degraded", exc_info=True); return []` wrapper. Build read-only SELECTs (no enqueue, no session.commit, no schema change). For Track-ID: LEFT-join FingerprintResult per engine via dict-lookup keyed on lowercase "audfprint"/"panako"; map done⟺status=="success" (tolerate "completed"), failed⟺"failed", pending⟺absent row (Pitfall 1/2). Derive tracklist match-state per D-04: linked (Tracklist.file_id IS NOT NULL) → "matched" + its match_confidence; else best candidate using `Tracklist.match_confidence.desc().nulls_last()` → "candidate" + highest match_confidence; else "no match". For Tracklist per-set (D-07): count TracklistTrack.confidence rows over the set's linked tracklist version as N (confident) of M (total). Return plain dicts (NOT ORM objects). If an explicit state/engine set is needed declare it as a module constant with a comment (mirror _ANALYZE_STAGE_STATES). Keep imports combine-as-imports + force-sort-within-sections (CLAUDE.md). Add unit tests to tests/test_identify_workspaces.py: shape assertions for both helpers (seed via the Task-1 helpers) AND a degrade-safety test (a closed/poisoned session returns []), mirroring the existing services degrade-safe test style.
  </action>
  <verify>
    <automated>uv run pytest tests/test_identify_workspaces.py -x && uv run mypy src/phaze/services/pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "async def get_trackid_stage_files\|async def get_tracklist_set_rows" src/phaze/services/pipeline.py` returns both
    - Neither helper contains `enqueue`, `session.commit`, `session.add`, or any DDL — verify: `uv run python -c "import ast,sys; src=open('src/phaze/services/pipeline.py').read(); print('OK')"` plus reviewer-grep showing both function bodies are read-only
    - Both helpers wrap their SELECT in `async with session.begin_nested():` and return `[]` on `except Exception` (degrade-safe)
    - Track-ID done badge keys on `status == "success"` (NOT "completed"); a unit test seeds FingerprintResult(status="success") and asserts the row's audfprint_status == "done"
    - `uv run pytest tests/test_identify_workspaces.py -x` exits 0; `uv run mypy src/phaze/services/pipeline.py` clean
  </acceptance_criteria>
  <done>Two degrade-safe read-only helpers exist, return the documented dict shapes, and have passing unit tests including the Pitfall-1 "success⟶done" guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → service helper | rows read from fingerprint_results / tracklists / tracklist_tracks; values flow to dicts then to autoescaped templates downstream |
| hot render/poll → helper | helpers run inside the request render path; an unhandled exception would 500 the page/poll |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-DOS | Denial of Service | get_trackid_stage_files / get_tracklist_set_rows | mitigate | SAVEPOINT (`session.begin_nested`) + `except Exception: return []` so a DB hiccup degrades to empty, never raises into the render/poll (mirrors get_analyze_stage_files) |
| T-59-INJ | Tampering | ORM read queries | mitigate | pure ORM `~exists`/`.not_in`/`.outerjoin` with no interpolated operator input (T-41-01 precedent); no raw SQL string-building |
| T-59-SCOPE | Elevation/Tampering | helper write surface | accept→mitigate | helpers are read-only by construction (no enqueue, no commit, no DDL) — asserted by acceptance criteria; preserves the no-backend-change boundary |
| T-59-SC | Tampering | package installs | accept | Phase 59 installs nothing (RESEARCH: no Package Legitimacy Audit applies); no supply-chain surface |
</threat_model>

<verification>
- `uv run pytest tests/test_identify_workspaces.py -x` green
- `uv run mypy src/phaze/services/pipeline.py` clean
- `uv run ruff check src/phaze/services/pipeline.py tests/test_identify_workspaces.py` clean
- Manual grep confirms both helpers are read-only (no enqueue/commit/DDL)
</verification>

<success_criteria>
- Test scaffold exists, runs green, with foundation tests filled and IDENT-01/02 stubs collecting cleanly
- get_trackid_stage_files + get_tracklist_set_rows return the documented read-only dict shapes, degrade-safe to []
- Pitfall-1 guard in place: done⟺status=="success"
- No backend behavior change (no enqueue, no commit of production tables, no schema change)
</success_criteria>

<output>
Create `.planning/phases/59-identify-workspaces/59-01-SUMMARY.md` when done
</output>

---

## Multi-Source Coverage Audit (phase-wide, recorded once here in Plan 01)

| Source | Item | Covered by |
|--------|------|-----------|
| GOAL (ROADMAP §59) | Track-ID surfaces each file's existing fingerprint + tracklist match state/confidence | Plan 01 (helper) + Plan 02 (template/wire) |
| GOAL (ROADMAP §59) | Tracklist visible 3-step Search→Scrape→Match + per-set match progress, one surface | Plan 01 (helper) + Plan 03 (template/wire) |
| REQ | IDENT-01 (Track-ID identity signals) | Plan 01, Plan 02 |
| REQ | IDENT-02 (Tracklist 3-step + per-set progress) | Plan 01, Plan 03 |
| RESEARCH | read-only assembly helpers (get_analyze_stage_files precedent) | Plan 01 |
| RESEARCH | Pitfall 1 fingerprint status vocab ("success" not "completed") | Plan 01 (helper) + Plan 02 (test guard) |
| RESEARCH | reuse _workspace_scaffold/_file_table/_lane_card; no new OOB id / store key | Plan 02, Plan 03 |
| RESEARCH | bulk triggers wired verbatim (search/scrape/match) + Pitfall 4 partial asymmetry | Plan 03 |
| CONTEXT | D-01 per-engine status badges, no invented score | Plan 01, Plan 02 |
| CONTEXT | D-02 requirement-wording reconciliation (state, not score) | Plan 01, Plan 02 |
| CONTEXT | D-03 one combined per-file table | Plan 02 |
| CONTEXT | D-04 confidence = linked, fallback best candidate | Plan 01, Plan 02 |
| CONTEXT | D-05 three sequential step cards (lane-card pattern) | Plan 03 |
| CONTEXT | D-06 per-step ALL triggers, no chain orchestrator | Plan 03 |
| CONTEXT | D-07 per-set N/M track coverage | Plan 01, Plan 03 |
| CONTEXT | D-08 per-set table below step cards | Plan 03 |

Excluded (not gaps): IDENT-03 (AcoustID/MusicBrainz — deferred), single "run chain" trigger (deferred), row-click→record (Phase 61), numeric fingerprint score (deferred), dead-template removal (Phase 62 / CUT-02). No MISSING items.
</content>
</invoke>
