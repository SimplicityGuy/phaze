---
phase: 59-identify-workspaces
plan: 03
type: execute
wave: 3
depends_on: [59-01, 59-02]
files_modified:
  - src/phaze/templates/pipeline/partials/tracklist_workspace.html
  - src/phaze/routers/shell.py
  - tests/test_identify_workspaces.py
autonomous: true
requirements: [IDENT-02]
must_haves:
  truths:
    - "D-05: THREE sequential step cards (Search · Scrape · Match) following the lane-card visual pattern, NOT a horizontal stepper"
    - "D-06: each card carries its own ALL trigger (SEARCH ALL / SCRAPE ALL / MATCH ALL) wired verbatim to the existing per-step endpoints; NO single run-chain button"
    - "D-07: per-set table renders N/M track coverage from TracklistTrack.confidence"
    - "D-08: per-set table sits below the three step cards (aggregate on top, per-set detail below)"
    - "GET /s/tracklist returns a bare fragment; shell.py STAGE_PARTIALS['tracklist'] is a static string literal (T-57-01)"
    - "each ALL trigger carries R-4 guard (hx-confirm + :disabled on its $store.pipeline.{searchBusy|scrapeBusy|matchBusy}); step counts server-rendered; busy pills bind to existing store keys (no new key, no second poll)"
  artifacts:
    - path: "src/phaze/templates/pipeline/partials/tracklist_workspace.html"
      provides: "Tracklist 3 step cards (Pattern B) + per-set table (Pattern C)"
      min_lines: 50
    - path: "src/phaze/routers/shell.py"
      provides: "tracklist STAGE_PARTIALS entry + _render_stage tracklist branch"
      contains: "tracklist_workspace.html"
  key_links:
    - from: "src/phaze/templates/pipeline/partials/tracklist_workspace.html"
      to: "/pipeline/search-tracklists, /pipeline/scrape-tracklists, /pipeline/match-tracklists"
      via: "hx-post on the three step-card ALL triggers"
      pattern: "pipeline/(search|scrape|match)-tracklists"
    - from: "src/phaze/routers/shell.py"
      to: "get_stage_progress + get_tracklist_set_rows + pending helpers"
      via: "_render_stage tracklist branch context"
      pattern: "get_tracklist_set_rows"
---

<objective>
Build the Tracklist workspace: three sequential step cards (Search · Scrape · Match) with per-step ALL triggers over the existing bulk endpoints, plus a per-set table showing N/M track coverage — superseding the `tracklist` placeholder in `STAGE_PARTIALS`.

Purpose: Make the Search→Scrape→Match sub-chain a visible 3-step triggerable from one surface, with per-set match progress (IDENT-02). All triggers wire verbatim to existing endpoints; there is NO chain-orchestration button (no backend change).

Output: `tracklist_workspace.html` fragment (Patterns B + C) + the `shell.py` wiring + the converted Tracklist behavior tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-identify-workspaces/59-CONTEXT.md
@.planning/phases/59-identify-workspaces/59-UI-SPEC.md
@.planning/phases/59-identify-workspaces/59-PATTERNS.md
@.planning/phases/59-identify-workspaces/59-01-SUMMARY.md
@.planning/phases/59-identify-workspaces/59-02-SUMMARY.md

<interfaces>
<!-- Reuse these directly — no codebase exploration needed. -->

Trigger endpoints (wired VERBATIM — Pitfall 4 partial asymmetry):
  POST /pipeline/search-tracklists  busy gate searchBusy  → returns trigger_response.html (HAS no_active_agent branch)  sink #search-trigger-response
  POST /pipeline/scrape-tracklists  busy gate scrapeBusy  → returns trigger_tracklist_response.html (NO agent branch)   sink #scrape-trigger-response
  POST /pipeline/match-tracklists   busy gate matchBusy   → returns trigger_tracklist_response.html                      sink #match-trigger-response

Server-rendered step counts (from _render_stage context):
  get_stage_progress(session)["scan_search"|"scrape"|"match"] = {"done": int, "total": int|None}  (scan_search total is None → render "done / —")
  pending counts: len(get_untracked_files(session)) (search), len(get_scrape_pending_tracklists(session)) (scrape), len(get_match_pending_tracklists(session)) (match)

get_tracklist_set_rows(session) -> list[dict] (from Plan 59-01) per-row keys:
  name (set/file), path, tracklist_state, tracks_confident (int), tracks_total (int), matched_to_file (bool)

shell.py wiring:
  STAGE_PARTIALS["tracklist"] = _STAGE_PLACEHOLDER ← replace with "pipeline/partials/tracklist_workspace.html" (static literal; T-57-01)
  add `elif stage == "tracklist":` branch injecting: context["tracklist_steps"] = await get_stage_progress(session) (+ the three pending counts) and context["tracklist_sets"] = await get_tracklist_set_rows(session); oob_counts stays False
  extend the services.pipeline import with get_stage_progress, get_tracklist_set_rows, get_untracked_files, get_scrape_pending_tracklists, get_match_pending_tracklists as needed (combine-as-imports)

_lane_card.html shape (Pattern B base): rounded-xl border phaze-border bg phaze-panel p-4, title row + mono font-medium numeral. Extend with the ALL button.
Secondary button class (_btn) — copy verbatim from fingerprint_workspace.html:22.
R-4 button idiom (fingerprint_workspace.html:25-31): hx-post + hx-target=#<sink> + hx-confirm + :disabled="$store.pipeline.<busy> > 0".
Busy store keys searchBusy/scrapeBusy/matchBusy already seeded in shell.html (:113,:119) and have OOB targets in _workspace_poll_seeds.html — NO new store key (Pitfall 3).

UI-SPEC copy:
  subcount: `${$store.pipeline.tracklistDone} sets matched · 1001Tracklists`
  per-set empty: heading "No tracklists yet" / body "Run SEARCH ALL to discover 1001Tracklists matches for your live sets."
  step-card busy pill: <span x-show="$store.pipeline.searchBusy > 0" class="text-blue-300">· running…</span>
  per-set columns: Set · Tracklist · Tracks · Matched to file ; coverage {n}/{m}: full emerald-300 / partial blue-300 / none "—" gray-500

Analog to copy: src/phaze/templates/pipeline/partials/analyze_workspace.html (grid grid-cols-3 gap-4 p-6 card row + border-t p-6 file table below).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tracklist_workspace.html (Pattern B step cards + Pattern C per-set table)</name>
  <files>src/phaze/templates/pipeline/partials/tracklist_workspace.html</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/analyze_workspace.html (the grid-of-cards-on-top + table-below structure to copy; the _btn class + scaffold call)
    - src/phaze/templates/pipeline/partials/fingerprint_workspace.html:22-32 (the verbatim R-4 ALL-trigger button + _btn class)
    - src/phaze/templates/pipeline/partials/_lane_card.html (the card visual shape Pattern B extends)
    - src/phaze/templates/pipeline/partials/_file_table.html (per-set table contract)
    - src/phaze/templates/pipeline/partials/trigger_tracklist_response.html (scrape/match response sink contract — Pitfall 4 asymmetry)
    - .planning/phases/59-identify-workspaces/59-UI-SPEC.md (NEW Pattern B + Pattern C; Color; Copywriting; the "do NOT build the breadcrumb stepper" note)
    - .planning/phases/59-identify-workspaces/59-PATTERNS.md (tracklist_workspace.html section — the trigger→endpoint→partial map)
  </read_first>
  <action>
    Create src/phaze/templates/pipeline/partials/tracklist_workspace.html. Import `_workspace_scaffold.html` as `ws`; copy the `_btn` secondary-button class string verbatim from fingerprint_workspace.html. Set `subcount` to `` `${$store.pipeline.tracklistDone} sets matched · 1001Tracklists` ``. Call `{% call ws.workspace(title="TRACKLIST", subcount=subcount) %}`. Inside, render a `<div class="grid grid-cols-3 gap-4 p-6">` of THREE step cards following the _lane_card.html visual shape (rounded-xl border phaze-border bg p-4): card 1 title `1 · 🔎 SEARCH`, card 2 `2 · 📄 SCRAPE`, card 3 `3 · 🔗 MATCH`. Each card shows a server-rendered count numeral (`font-mono font-medium text-gray-300`) — Search renders `{{ tracklist_steps.scan_search.done }} / —` (total is None), Scrape/Match render `done / total` from tracklist_steps; a sub-label line `{pending} pending` from the pending counts; a live busy pill `<span x-show="$store.pipeline.{search|scrape|match}Busy > 0" class="text-blue-300">· running…</span>`; and a per-step ALL trigger button copied VERBATIM from the fingerprint R-4 idiom, swapping endpoint/target/copy per the map: SEARCH ALL → hx-post="/pipeline/search-tracklists" hx-target="#search-trigger-response" :disabled="$store.pipeline.searchBusy > 0"; SCRAPE ALL → "/pipeline/scrape-tracklists" #scrape-trigger-response scrapeBusy; MATCH ALL → "/pipeline/match-tracklists" #match-trigger-response matchBusy. Each button carries hx-confirm (e.g. "Enqueue scraping for all pending tracklists?") and hx-swap="innerHTML". Place a `<div id="search-trigger-response" class="px-6 ... empty:hidden"></div>` (and scrape/match equivalents) as the per-card sinks. Do NOT build a single run-chain button and do NOT build the prototype breadcrumb stepper (UI-SPEC). Below the cards, in a `<div class="border-t border-gray-200 p-6 dark:border-phaze-border">`, render the per-set table (Pattern C): `columns = ["Set", "Tracklist", "Tracks", "Matched to file"]`, feed `ns.rows` from `tracklist_sets` — Set cell `{'text': s.name, 'mono': True, 'title': s.path}`; Tracklist cell match-state word+color; Tracks cell `{'text': f"{s.tracks_confident}/{s.tracks_total}", 'mono': True, 'color': coverage_color}` (full emerald / partial blue / none gray per UI-SPEC); Matched-to-file cell "matched" emerald / "—" gray from s.matched_to_file. Set `empty_heading="No tracklists yet"`, `empty_body="Run SEARCH ALL to discover 1001Tracklists matches for your live sets."`, `row_id_prefix="tracklist-row"`, `table_id="tracklist-set-table"`, then `{% include "pipeline/partials/_file_table.html" %}`. No hx-get on rows; no hx-trigger="every"/setInterval; no `| safe`; no confidence_badge/status_badge pills. Run `just tailwind` if any new utility class is introduced.
  </action>
  <verify>
    <automated>uv run pytest tests/test_identify_workspaces.py::test_identify_single_poll_discipline -x</automated>
  </verify>
  <acceptance_criteria>
    - File exists with three step cards in a `grid grid-cols-3` and a per-set table inside a `border-t` section below them (D-05/D-08)
    - The three ALL buttons hx-post to /pipeline/search-tracklists, /pipeline/scrape-tracklists, /pipeline/match-tracklists respectively (D-06); each has hx-confirm and :disabled on its matching *Busy key (R-4)
    - Fragment contains NO single run-chain button, NO breadcrumb stepper, NO `<html`/`<head`/`<header`/`{% extends`, NO `hx-trigger="every`, NO `setInterval`, NO `| safe`, NO confidence_badge/status_badge include
    - `grep -v '^[[:space:]]*{#' src/phaze/templates/pipeline/partials/tracklist_workspace.html | grep -c 'hx-get'` is 0 (inert rows; the chrome poll lives in shell, not here)
  </acceptance_criteria>
  <done>tracklist_workspace.html renders three step cards with verbatim-wired ALL triggers + a per-set coverage table, single-poll-clean, no chain orchestrator.</done>
</task>

<task type="auto">
  <name>Task 2: Wire shell.py tracklist stage + convert Tracklist behavior tests</name>
  <files>src/phaze/routers/shell.py, tests/test_identify_workspaces.py</files>
  <read_first>
    - src/phaze/routers/shell.py (STAGE_PARTIALS dict, _render_stage branches, import line — now also imports get_trackid_stage_files from Plan 02)
    - src/phaze/templates/pipeline/partials/tracklist_workspace.html (Task 1 — the context keys it consumes: tracklist_steps + pending counts + tracklist_sets)
    - tests/test_identify_workspaces.py (the IDENT-02 stubs to convert: test_tracklist_step_cards_and_triggers, test_tracklist_per_set_coverage)
    - src/phaze/services/pipeline.py (get_stage_progress, get_untracked_files, get_scrape_pending_tracklists, get_match_pending_tracklists, get_tracklist_set_rows signatures)
    - .planning/phases/59-identify-workspaces/59-RESEARCH.md (§3 + §4; Test Map IDENT-02 rows; Pitfall 4)
  </read_first>
  <action>
    In src/phaze/routers/shell.py: replace `"tracklist": _STAGE_PLACEHOLDER,` with `"tracklist": "pipeline/partials/tracklist_workspace.html",` (STATIC string literal; T-57-01) plus the Phase-59 comment convention (phase + IDENT-02 + T-57-01 + supersede-in-place). Extend the `from phaze.services.pipeline import ...` line to also import get_stage_progress, get_tracklist_set_rows, get_untracked_files, get_scrape_pending_tracklists, get_match_pending_tracklists (combine-as-imports, force-sort-within-sections). Add `elif stage == "tracklist":` to _render_stage injecting context["tracklist_steps"] = await get_stage_progress(session), context["tracklist_search_pending"] = len(await get_untracked_files(session)), context["tracklist_scrape_pending"] = len(await get_scrape_pending_tracklists(session)), context["tracklist_match_pending"] = len(await get_match_pending_tracklists(session)), and context["tracklist_sets"] = await get_tracklist_set_rows(session); keep oob_counts False. Match the exact context key names the Task-1 template reads. Do NOT delete any legacy template (supersede-in-place). Then convert the two IDENT-02 xfail stubs in tests/test_identify_workspaces.py: test_tracklist_step_cards_and_triggers GETs /s/tracklist (HX) and asserts three step cards exist, that hx-post to all three endpoints appears, that each ALL button has hx-confirm and :disabled on its *Busy key (R-4), and that NO single run-chain button exists; test_tracklist_per_set_coverage seeds a Tracklist + TracklistVersion + TracklistTrack rows (mix of confident/non-confident) and asserts the per-set table (scoped via `body[body.index('id="tracklist-set-table"'):]`) renders the N/M coverage numeral and inert rows (no hx-get).
  </action>
  <verify>
    <automated>uv run pytest tests/test_identify_workspaces.py tests/test_dead_template_guard.py tests/test_shell_routes.py -x && uv run mypy src/phaze/routers/shell.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'tracklist' src/phaze/routers/shell.py` shows STAGE_PARTIALS['tracklist'] is the static literal "pipeline/partials/tracklist_workspace.html" (no f-string/format/concatenation)
    - `_render_stage` has an `elif stage == "tracklist":` branch populating tracklist_steps + the three pending counts + tracklist_sets; oob_counts unchanged (False)
    - GET /s/tracklist (HX) returns a fragment whose three ALL buttons hx-post to the search/scrape/match endpoints
    - test_tracklist_step_cards_and_triggers and test_tracklist_per_set_coverage are real assertions (xfail removed) and pass
    - `uv run pytest tests/test_dead_template_guard.py tests/test_shell_routes.py -x` green; `uv run mypy src/phaze/routers/shell.py` clean
    - Full suite + coverage: `uv run pytest --cov --cov-report=term-missing` passes the ≥85% gate
  </acceptance_criteria>
  <done>/s/tracklist serves the real Tracklist workspace; IDENT-02 behavior tests pass; dead-template + shell-route guards green; coverage ≥85%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /s/{stage} | `stage` whitelisted against STAGE_PARTIALS; never reaches a template path |
| client → bulk trigger endpoints | SEARCH/SCRAPE/MATCH ALL re-use existing endpoints; existing CSRF/auth/routing posture preserved, not weakened |
| DB → template cell | set name/path/artist cross into rendered HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | shell.py STAGE_PARTIALS['tracklist'] | mitigate | value is a STATIC string literal; `stage` matched against dict keys, never spliced into a template path; unknown stage 404s |
| T-59-XSS | Info-disclosure/Tampering | tracklist_workspace.html cells | mitigate | DB-sourced `text` via _file_table.html autoescape; NO `| safe`; status words are caller class strings |
| T-59-OVERENQ | Denial of Service | SEARCH/SCRAPE/MATCH ALL triggers | mitigate | R-4 guard (hx-confirm + :disabled on *Busy) on each; endpoints are deterministic-keyed + idempotent (Phase-34/35) — preserves existing posture, adds no new sink |
| T-59-DOS | Denial of Service | _render_stage tracklist branch | mitigate | get_stage_progress + get_tracklist_set_rows are degrade-safe; oob_counts stays False (Pitfall 5) |
| T-59-SC | Tampering | package installs | accept | Phase 59 installs nothing; no supply-chain surface |
</threat_model>

<verification>
- `uv run pytest tests/test_identify_workspaces.py tests/test_dead_template_guard.py tests/test_shell_routes.py -x` green
- `uv run pytest --cov --cov-report=term-missing` ≥85%
- `uv run mypy src/phaze/routers/shell.py` clean
- `uv run ruff check src/phaze/routers/shell.py tests/test_identify_workspaces.py` clean
- pre-commit hooks pass (never --no-verify)
</verification>

<success_criteria>
- /s/tracklist renders three step cards (D-05) with verbatim-wired per-step ALL triggers (D-06, R-4 guarded) and a per-set N/M coverage table below (D-07/D-08)
- STAGE_PARTIALS['tracklist'] is a static literal (T-57-01); no new store key / no second poll; rows inert
- No single run-chain button (no backend change); IDENT-02 behavior tests pass; dead-template + shell-route guards green; coverage ≥85%
</success_criteria>

<output>
Create `.planning/phases/59-identify-workspaces/59-03-SUMMARY.md` when done
</output>
</content>
