---
phase: 87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri
plan: 05
type: execute
wave: 4
depends_on: [87-04]
files_modified:
  - src/phaze/templates/pipeline/partials/files_table_view.html
  - src/phaze/templates/pipeline/partials/_status_filter_bar.html
  - src/phaze/templates/pipeline/partials/metadata_workspace.html
  - src/phaze/templates/pipeline/partials/analyze_workspace.html
  - tests/shared/test_no_raw_state_render.py
  - tests/integration/test_files_filter.py
autonomous: true
requirements: [UI-01, UI-02]
must_haves:
  truths:
    - "The operator can filter the files table by 'Show files where {stage} = {bucket}', including failed (D-03, UI-02 failure visibility)"
    - "Filter state is carried in the URL query and survives the record slide-in + back/forward (D-03)"
    - "The raw-enum 'State' string is removed from metadata_workspace.html + analyze_workspace.html and replaced by the pill matrix (UI-01 cutover, anti-feature table)"
    - "A mutation-tested grep guard fails if any template re-introduces a raw f.state status render (UI-01)"
  artifacts:
    - path: "src/phaze/templates/pipeline/partials/_status_filter_bar.html"
      provides: "Per-stage bucket filter bar (one canonical surface, no separate failures page)"
    - path: "tests/shared/test_no_raw_state_render.py"
      provides: "Grep guard asserting no raw-enum State render sites remain"
  key_links:
    - from: "src/phaze/templates/pipeline/partials/_status_filter_bar.html"
      to: "GET /pipeline/files?stage=&bucket="
      via: "hx-get with URL-carried filter state (params plumbed in Plan 04)"
      pattern: "stage="
---

<objective>
Add the failure/status filter lens over the same paginated files table (UI-02 failure visibility as a filter,
not a separate page), and complete the UI-01 cutover by retiring the raw-enum "State" string everywhere it
renders — guarded so it can never silently return.

Purpose: D-03 makes "show files where {stage} = failed" just another lens on the one canonical list, not a
third place to maintain. The anti-feature table forbids rendering raw internal status strings — the cutover
plus a mutation-tested grep guard enforces that permanently.
Output: `_status_filter_bar.html`, retired State cells in the two workspaces, the grep guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-UI-SPEC.md
@.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-PATTERNS.md
@src/phaze/templates/pipeline/partials/metadata_workspace.html
@src/phaze/templates/pipeline/partials/analyze_workspace.html

The `GET /pipeline/files` route already accepts `stage`+`bucket` query params (plumbed in Plan 04); this plan
is templates + tests only (no router/service edit). Empty-state copy is fixed in the UI-SPEC Copywriting
Contract (unfiltered vs failed-filter headings/bodies).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-stage status filter bar (URL-carried, failure-inclusive)</name>
  <files>src/phaze/templates/pipeline/partials/_status_filter_bar.html, src/phaze/templates/pipeline/partials/files_table_view.html, tests/integration/test_files_filter.py</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/files_table_view.html (from Plan 04 — the table + pagination host)
    - src/phaze/templates/tracklists/partials/pagination.html (the query/filter idiom to mirror)
    - .planning/phases/87-.../87-UI-SPEC.md (§ Status filter bar + Copywriting Contract empty states + Interaction table)
  </read_first>
  <action>
    Create `_status_filter_bar.html` above the table: a per-stage bucket selector expressing "Show files where
    `{stage}` = `{bucket}`" (buckets include `failed`, plus done/in_flight/not_started/skipped). Wire it with
    `hx-get="/pipeline/files"` carrying `stage`+`bucket` in the URL query (`hx-push-url` so it survives the
    record slide-in + browser back/forward). Include it in `files_table_view.html`. Empty-state copy per the
    UI-SPEC: unfiltered heading "No files yet" / body "Discovered files will appear here with their per-stage
    status."; failed-filter heading "No failed files in {stage}" / body "Nothing is stuck in {stage} right now."
    Add `tests/integration/test_files_filter.py`: seed files in mixed buckets, request `?stage=metadata&bucket=failed`
    and assert only failed-metadata rows return; assert the empty-filter branch renders the failed-filter copy.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_files_filter.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Filtering `?stage={S}&bucket=failed` returns only files whose `stage_status_case(S)` == failed (UI-02).
    - Filter state is in the URL query (`hx-push-url`); the failed-filter empty state renders the UI-SPEC copy.
    - It is one canonical surface — no separate failures page/route added.
  </acceptance_criteria>
  <done>Failure/status filtering works as a URL-carried lens on the single files table.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the raw-enum "State" render + mutation-tested grep guard</name>
  <files>src/phaze/templates/pipeline/partials/metadata_workspace.html, src/phaze/templates/pipeline/partials/analyze_workspace.html, tests/shared/test_no_raw_state_render.py</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/metadata_workspace.html:40-51 (the `{'text': f.state, ...}` State cell)
    - src/phaze/templates/pipeline/partials/analyze_workspace.html:81-86 (the raw-enum State cell)
    - .planning/phases/87-.../87-UI-SPEC.md (§ Retirement Note) + REQUIREMENTS.md anti-feature table
    - .planning/phases/87-.../87-PATTERNS.md (§ Retire raw-enum "State")
  </read_first>
  <action>
    In `metadata_workspace.html:40-51` and `analyze_workspace.html:81-86`, replace the raw-enum State
    column/cell (`{'text': f.state, 'color': state_color}`) with a `_stage_matrix` (or the relevant per-stage
    `_stage_pill`) cell reading the derived status. Remove any remaining `f.state`-as-status render in these
    partials. Add `tests/shared/test_no_raw_state_render.py`: scan `src/phaze/templates/pipeline/partials/`
    (and any per-file view templates) for `f.state` status-render sites and assert NONE remain — filtering out
    non-status references so the guard is specific. Make the guard mutation-proof: the test must go RED if a
    `{{ f.state }}`-style status render is re-added (document the break-and-restore observation in the SUMMARY).
  </action>
  <verify>
    <automated>uv run pytest tests/shared/test_no_raw_state_render.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - No raw-enum "State" status string renders in `metadata_workspace.html` / `analyze_workspace.html` (or any per-file view); replaced by the pill matrix (UI-01).
    - The grep guard is mutation-tested: re-adding a raw `f.state` status render turns it RED (recorded in SUMMARY).
  </acceptance_criteria>
  <done>The raw-enum State render is gone everywhere and cannot silently return.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filter query params → files query | stage/bucket cross into the paginated query (validated in Plan 04's route) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-15 | Information Disclosure | rendering raw internal status strings (anti-feature) | mitigate | Cutover to derived pill matrix + mutation-tested grep guard forbids raw `f.state` renders |
| T-87-16 | Tampering | filter param injection | mitigate | Route validates stage/bucket against enum allowlists (Plan 04); template only reads validated context |
| T-87-17 | Denial of Service | filter widening the poll to a whole-corpus scan | accept | Filter rides the same paginated `get_files_page` (Plan 04) — bounded LIMIT preserved |

No package installs this phase (zero new deps) — no supply-chain checkpoint required.
</threat_model>

<verification>
- `just test-bucket integration` + `just test-bucket shared` green in isolation.
- Grep guard mutation observation recorded in the SUMMARY.
</verification>

<success_criteria>
- Failure/status filter works as a URL-carried lens; raw-enum State render retired and guarded.
</success_criteria>

<output>
Create `.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-05-SUMMARY.md` when done.
</output>
