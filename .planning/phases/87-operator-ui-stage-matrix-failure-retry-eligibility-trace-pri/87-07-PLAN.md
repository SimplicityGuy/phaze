---
phase: 87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri
plan: 07
type: execute
wave: 5
depends_on: [87-04, 87-05, 87-06]
files_modified:
  - src/phaze/routers/pipeline.py
  - src/phaze/templates/pipeline/partials/files_table_view.html
  - src/phaze/templates/pipeline/partials/_stage_matrix.html
  - tests/analyze/test_retry_affordances.py
  - tests/metadata/test_retry_affordances.py
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - "The operator can retry a failed enrich file per-file (scoped variant) AND bulk-per-stage (D-04, UI-02)"
    - "Analyze retry routes through the MANUAL path (ELIGIBLE_AFTER_FAILURE[ANALYZE]=False) — no auto-retry loop (D-00b, behavior 8, guards the 44.5K over-enqueue class)"
    - "Retry acks are count/bool-only (no operator free-text through Jinja); a per-row Retry appears only on a failed cell (D-04, UI-SPEC)"
    - "Failed fingerprints are surfaced (they auto-retry via the pending set) — no new fingerprint retry backend (RESEARCH)"
  artifacts:
    - path: "src/phaze/routers/pipeline.py"
      provides: "Per-file scoped variants of analysis-failed/retry + metadata-failed/retry"
      contains: "retry"
  key_links:
    - from: "src/phaze/templates/pipeline/partials/files_table_view.html"
      to: "POST /pipeline/analysis-failed/retry + /pipeline/metadata-failed/retry (+ per-file scope)"
      via: "per-row Retry on failed cells + bulk 'Retry all failed · {stage}' on the failed filter view"
      pattern: "retry"
---

<objective>
Give failed enrich files a retry affordance from the console (UI-02): a per-file Retry on each failed cell and
a bulk "Retry all failed · {stage}" on the failed-filter view, both re-wiring the already-live, Phase-30-hardened
bulk retry endpoints — with the analyze terminal-guard strictly preserved (manual retry, never an auto-loop).

Purpose: The failed bucket is only useful if the operator can act on it. The retry endpoints already exist and
are guarded; this plan surfaces them and adds a per-file scoped variant. The analyze path MUST remain manual —
auto-retry caused the 44.5K-job over-enqueue incident (ELIG-03).
Output: per-file retry variants + per-row/bulk retry affordances wired to the live funnel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-UI-SPEC.md
@.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-PATTERNS.md
@.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-VALIDATION.md
@src/phaze/templates/pipeline/partials/metadata_retry_response.html

<interfaces>
- Live bulk endpoints (Phase-30 hardened, dedup-safe): `routers/pipeline.py:934 retry_analysis_failed`,
  `:1017 retry_metadata_failed`. Guarded funnel: `enqueue_router.resolve_queue_for_task` → `NoActiveAgentError`
  guard → enqueue.
- Analyze terminal guard (D-00b): the analyze retry flips `ANALYSIS_FAILED → FINGERPRINTED` and clears
  `analysis.failed_at` in the SAME transaction, COMMITs BEFORE enqueue (`pipeline.py:997-1002`); no auto-loop.
- Response partials (3-branch int/bool acks — no operator free-text): `metadata_retry_response.html`,
  `retry_failed_response.html`. Branches: no_active_agent / count==0 / success.
- Fingerprint failures self-retry via `eligible_clause(FINGERPRINT)` pending set — NO manual fingerprint retry backend.
</interfaces>

UI-SPEC copy: bulk "Retry all failed · {stage}"; per-row "Retry" (`aria-label="Retry {stage} for this file"`);
acks — success "Re-queued {n} failed file(s) for {stage}."; no-agent "No active agent — nothing re-queued.";
none "No failed files to retry."
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-file scoped retry variants (analyze + metadata), manual-analyze preserved</name>
  <files>src/phaze/routers/pipeline.py, tests/analyze/test_retry_affordances.py, tests/metadata/test_retry_affordances.py</files>
  <read_first>
    - src/phaze/routers/pipeline.py:934-1014 (retry_analysis_failed — the manual flip + commit-before-enqueue) and :1017+ (retry_metadata_failed)
    - src/phaze/templates/pipeline/partials/metadata_retry_response.html + retry_failed_response.html (the 3-branch ack shape)
    - .planning/phases/87-.../87-VALIDATION.md (behavior 8: terminal-analyze retry is manual-only)
  </read_first>
  <action>
    Add per-file scoped retry variants (one `file_id`) for analyze and metadata that reuse the SAME guarded
    funnel as the bulk endpoints (`enqueue_router.resolve_queue_for_task` → `NoActiveAgentError` guard →
    enqueue), filtered to one file instead of the whole failed set. The analyze per-file retry keeps the manual
    path: flip `ANALYSIS_FAILED → FINGERPRINTED` + clear `analysis.failed_at` in the same transaction, commit
    BEFORE enqueue — it must NOT create an auto-retry loop (`ELIGIBLE_AFTER_FAILURE[ANALYZE]=False` stays
    respected). Reuse the existing response partials verbatim (adjust stage label only). Add
    `tests/analyze/test_retry_affordances.py` + `tests/metadata/test_retry_affordances.py`: assert per-file and
    bulk both re-queue through the guarded funnel; assert the analyze retry does not re-enqueue on the automatic
    path afterwards (behavior 8 — manual-only, no auto-loop); assert the no-agent branch returns the amber ack
    without enqueue.
  </action>
  <verify>
    <automated>uv run pytest tests/analyze/test_retry_affordances.py tests/metadata/test_retry_affordances.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Behavior 8: analyze retry is manual-only — after a manual retry, no automatic path re-enqueues the analyze (over-enqueue guard intact).
    - Per-file and bulk both route through the guarded funnel (NoActiveAgentError → amber ack, no enqueue).
    - Acks are count/bool-only (no operator free-text through Jinja).
  </acceptance_criteria>
  <done>Per-file + bulk retry work through the live guarded funnel; analyze stays manual.</done>
</task>

<task type="auto">
  <name>Task 2: Wire per-row Retry (failed cells) + bulk "Retry all failed · {stage}"</name>
  <files>src/phaze/templates/pipeline/partials/files_table_view.html, src/phaze/templates/pipeline/partials/_stage_matrix.html</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/files_table_view.html + _stage_matrix.html (from Plans 04/05)
    - src/phaze/templates/pipeline/partials/metadata_retry_response.html (ack render target)
    - .planning/phases/87-.../87-UI-SPEC.md (§ Per-row retry / Bulk retry + Copywriting + Interaction table)
  </read_first>
  <action>
    Render a per-row "Retry" affordance inside a stage cell ONLY when that cell's bucket == `failed` (HTMX POST
    to the per-file scoped variant from Task 1; ack rendered via `metadata_retry_response.html`-shaped partial;
    `aria-label="Retry {stage} for this file"`). On the failed-filter view, render one primary (accent) "Retry
    all failed · {stage}" button posting to the bulk endpoint. The row's failed pill clears on the NEXT poll
    tick (not optimistic). For fingerprint, surface the failed files (they auto-retry via the pending set) — do
    NOT add a manual fingerprint retry control. Use the UI-SPEC copy for all acks and buttons.
  </action>
  <verify>
    <automated>uv run pytest tests/analyze/test_retry_affordances.py -x -q -k render || uv run pytest tests/analyze/test_retry_affordances.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - A per-row Retry renders only on a failed cell; the bulk button renders on the failed-filter view (UI-02).
    - No manual fingerprint retry control is added (fingerprint failures self-retry); acks use UI-SPEC copy.
  </acceptance_criteria>
  <done>Failed enrich files are retryable per-file and in bulk from the console.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → retry endpoints | untrusted file_id/stage cross into an enqueue path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-24 | Denial of Service | mis-scoped analyze bulk retry re-enables the 44.5K over-enqueue | mitigate | Analyze retry stays MANUAL (ELIGIBLE_AFTER_FAILURE[ANALYZE]=False); behavior-8 test asserts no auto-loop |
| T-87-25 | Denial of Service | retry with no active agent | mitigate | Reuses the guarded funnel — `NoActiveAgentError` → amber ack, nothing enqueued |
| T-87-26 | Tampering | duplicate enqueue on rapid retry | mitigate | Reuses Phase-30 deterministic dedup key of the live endpoints |
| T-87-27 | Input Validation | invalid file_id/stage on per-file retry | mitigate | UUID path param + enrich allowlist reuse; ORM bound params |

No package installs this phase (zero new deps) — no supply-chain checkpoint required.
</threat_model>

<verification>
- `just test-bucket analyze` + `just test-bucket metadata` green in isolation.
- `uv run ruff check .` clean.
</verification>

<success_criteria>
- Per-file + bulk retry for failed enrich files; analyze manual-only (no auto-loop); fingerprint surfaced not re-wired.
</success_criteria>

<output>
Create `.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-07-SUMMARY.md` when done.
</output>
