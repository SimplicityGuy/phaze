---
phase: 87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri
plan: 03
type: execute
wave: 3
depends_on: [87-02]
files_modified:
  - tests/integration/test_stage_status_equivalence.py
  - tests/metadata/test_skipped_leaves_pending.py
  - tests/fingerprint/test_skipped_leaves_pending.py
  - tests/analyze/test_skipped_leaves_pending.py
  - tests/analyze/tasks/test_recovery.py
  - tests/integration/test_shadow_compare_skipped.py
autonomous: true
requirements: [UI-04]
must_haves:
  truths:
    - "DERIV-04 SQL⇔Python equivalence harness exercises `skipped` on CASES/ELIGIBLE_CASES/DOMAIN_COMPLETED_CASES and they agree (DERIV-04, D-13c)"
    - "A skipped file is ABSENT from get_metadata_pending_files / get_fingerprint_pending_files / get_discovered_files_with_duration (behavior 1)"
    - "A force-skipped file is NOT re-enqueued by tasks/reenqueue.py (behavior 5, guards over-enqueue class)"
    - "The additive skip marker keeps shadow_compare green with NO new allowlist entry (behavior 6, D-13d)"
    - "skipped ≻ failed precedence proven: a terminally-failed analyze + skip marker reads `skipped` (Pitfall 2)"
  artifacts:
    - path: "tests/integration/test_stage_status_equivalence.py"
      provides: "DERIV-04 harness extended with skipped seed fns + cells on all 3 axes + load_scalars skipped read"
      contains: "skipped"
    - path: "tests/integration/test_shadow_compare_skipped.py"
      provides: "Shadow-compare-green-post-skip proof (additive writer)"
  key_links:
    - from: "tests/*/test_skipped_leaves_pending.py"
      to: "eligible_clause ~skipped conjunct"
      via: "mutation-tested — drop conjunct → RED"
      pattern: "skipped"
---

<objective>
Lock the `skipped` marker against SQL⇔Python drift and prove its five load-bearing behaviors: distinct
bucket (skipped ≻ failed), leaves all three enrich pending sets, downstream-unblocks within enrich, not
re-enqueued by recovery, and shadow-compare stays green (additive writer). Tests only — no source change.

Purpose: The DERIV-04 equivalence harness is the drift-lock this milestone exists to preserve; bypassing it
recreates the exact drift class. Every behavioral guard is mutation-tested (a green guard proves nothing —
break source, watch RED, restore) per the Phase-84 standing rule.
Output: extended DERIV-04 harness + pending-set-drop + recovery + shadow-green tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-RESEARCH.md
@.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-PATTERNS.md
@.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-VALIDATION.md
@tests/integration/test_stage_status_equivalence.py

DB tests need `TEST_DATABASE_URL` + `PHAZE_QUEUE_URL` at `:5433`. Buckets: `integration`, `metadata`,
`fingerprint`, `analyze`. Each must pass in isolation (`just test-bucket <bucket>` — non-hermetic hazard).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the DERIV-04 equivalence harness with skipped cells on all 3 axes</name>
  <files>tests/integration/test_stage_status_equivalence.py</files>
  <read_first>
    - tests/integration/test_stage_status_equivalence.py (CASES ~306-337; ELIGIBLE_CASES ~502-520; DOMAIN_COMPLETED_CASES ~445-460; load_scalars ~351-389; seed idiom ~284-300; parametrized tests test_sql_equals_python ~400 / test_domain_completed_sql_equals_python ~471 / test_eligible_sql_equals_python ~531)
    - .planning/phases/87-.../87-PATTERNS.md (§ DERIV-04 harness — the exact seed fns + extension points)
    - .planning/phases/87-.../87-RESEARCH.md (Code Example: seed_analysis_skipped_over_failed)
  </read_first>
  <action>
    Add seed fns mirroring the existing idiom: `seed_analysis_skipped_over_failed` (insert an AnalysisResult
    with `failed_at` set, flush, then insert a `StageSkip(file_id, stage="analyze", reason=...)`, flush) plus
    `seed_metadata_skipped` and `seed_fp_skipped`. Add cells: `CASES` gets `(Stage.ANALYZE,
    seed_analysis_skipped_over_failed, "skipped")` (the skipped≻failed precedence cell) plus metadata/fingerprint
    skipped cells expecting `"skipped"`; `ELIGIBLE_CASES` gets `(Stage.<enrich>, seed_<stage>_skipped, False)`
    (skipped leaves the pending set); `DOMAIN_COMPLETED_CASES` gets `(Stage.<enrich>, seed_<stage>_skipped,
    True)` (recovery treats skipped as complete). Extend `load_scalars` to read a `skipped` bool from
    `stage_skip` for each enrich stage and add it to the returned dict so the Python-twin side sees it. The
    three parametrized tests then cover the new cells with zero test-body edits.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_stage_status_equivalence.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `skipped` is exercised on CASES, ELIGIBLE_CASES, and DOMAIN_COMPLETED_CASES; SQL == Python for all cells (behaviors 2,3,4).
    - The skipped≻failed cell (`seed_analysis_skipped_over_failed`) asserts status `"skipped"`, not `"failed"`.
    - Mutation (documented in SUMMARY): dropping the `skipped_clause` branch from `stage_status_case` turns this suite RED.
  </acceptance_criteria>
  <done>DERIV-04 harness drift-locks the skipped marker on all three derivation axes.</done>
</task>

<task type="auto">
  <name>Task 2: Pending-set-drop (per enrich stage) + recovery-not-re-enqueued guards</name>
  <files>tests/metadata/test_skipped_leaves_pending.py, tests/fingerprint/test_skipped_leaves_pending.py, tests/analyze/test_skipped_leaves_pending.py, tests/analyze/tasks/test_recovery.py</files>
  <read_first>
    - src/phaze/services/pipeline.py (get_metadata_pending_files ~1441, get_fingerprint_pending_files ~1480, get_discovered_files_with_duration ~1151 — all read eligible_clause)
    - src/phaze/tasks/reenqueue.py (_build_done_sets / _select_done_analyze_ids — read only domain_completed_clause)
    - .planning/phases/87-.../87-VALIDATION.md (behaviors 1 & 5)
  </read_first>
  <action>
    For each enrich stage, seed a file that WOULD be in that stage's pending set, add a `StageSkip` marker for
    that stage, and assert the file is ABSENT from the corresponding pending helper
    (`get_metadata_pending_files` / `get_fingerprint_pending_files` / `get_discovered_files_with_duration`) —
    proving the `~skipped` conjunct in `eligible_clause` propagates zero-edit. Add a recovery test: seed a
    force-skipped file and assert `tasks/reenqueue.py` treats it as domain-complete (present in the done set /
    absent from the re-enqueue candidate set) on both the automatic and manual paths. Mutation-test each guard:
    temporarily drop the `~skipped` conjunct (from Plan 02) so the pending-set tests go RED; restore. Record the
    RED observation in the SUMMARY.
  </action>
  <verify>
    <automated>uv run pytest tests/metadata/test_skipped_leaves_pending.py tests/fingerprint/test_skipped_leaves_pending.py tests/analyze/test_skipped_leaves_pending.py tests/analyze/tasks/test_recovery.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - One pending-set-drop test per enrich stage passes (behavior 1); the recovery test passes (behavior 5).
    - Mutation discipline recorded: dropping the `~skipped` conjunct turns the pending-set tests RED.
    - Each file passes under `just test-bucket <metadata|fingerprint|analyze>` in isolation.
  </acceptance_criteria>
  <done>Skipped files leave all 3 pending sets and are never re-enqueued by recovery.</done>
</task>

<task type="auto">
  <name>Task 3: Shadow-compare stays green post-skip (additive-writer proof)</name>
  <files>tests/integration/test_shadow_compare_skipped.py</files>
  <read_first>
    - src/phaze/services/shadow_compare.py (INVARIANTS ~123-151; the `analysis_failed` invariant ~128; soft allowlist ~145-151 — must never grow past {fingerprinted, local_analyzing})
    - existing shadow_compare tests (run_shadow_compare Report shape)
    - .planning/phases/87-.../87-RESEARCH.md (§4 shadow-compare stays green WITHOUT allowlisting)
  </read_first>
  <action>
    Seed a corpus containing a terminally-failed analyze file (`state='analysis_failed'`, `analysis.failed_at`
    set) and add a `StageSkip(stage="analyze")` marker to it WITHOUT clearing `failed_at` (additive-only). Run
    `run_shadow_compare` and assert zero hard divergences and that the allowlist is unchanged
    (`{fingerprinted, local_analyzing}`). Assert directly that `failed_clause(ANALYZE)` still evaluates True for
    the skipped file, so the `state='analysis_failed' ⇒ failed_clause(ANALYZE)` implication still holds (no
    false flag, no allowlist growth). This encodes the "writer is purely additive" property as a hard test
    (behavior 6) so a later writer that "tidies" `failed_at` regresses the gate loudly.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_shadow_compare_skipped.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `run_shadow_compare` reports zero hard divergences with the skipped-file corpus; allowlist unchanged.
    - `failed_clause(ANALYZE)` is still True for the skipped-over-failed file (implication preserved).
    - Passes under `just test-bucket integration` in isolation.
  </acceptance_criteria>
  <done>The additive skip marker keeps the Phase-79 shadow gate green with no allowlist growth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| derived representation ↔ legacy files.state | Shadow-compare implication gate over the live corpus |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-08 | Integrity | SQL⇔Python twin drift | mitigate | DERIV-04 harness extended with skipped cells on all 3 axes (Task 1) — SQL==Python enforced |
| T-87-09 | Integrity | toothless (always-green) guard | mitigate | Every behavioral guard mutation-tested: break source → RED → restore (Phase-84 rule) |
| T-87-10 | Integrity | shadow-compare regression on skip | mitigate | Additive-writer property asserted directly (Task 3); allowlist assertion prevents silent growth |

No package installs this phase (zero new deps) — no supply-chain checkpoint required.
</threat_model>

<verification>
- `just test-bucket integration`, `just test-bucket metadata`, `just test-bucket fingerprint`,
  `just test-bucket analyze` all green in isolation.
- Mutation observations (RED-on-break) recorded in the SUMMARY for the harness + pending-set guards.
</verification>

<success_criteria>
- DERIV-04 harness covers skipped on all three axes; SQL == Python.
- Behaviors 1, 5, 6 proven; skipped ≻ failed precedence proven.
</success_criteria>

<output>
Create `.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-03-SUMMARY.md` when done.
</output>
