---
phase: 87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri
plan: 06
type: execute
wave: 4
depends_on: [87-02, 87-04]
files_modified:
  - src/phaze/routers/pipeline.py
  - src/phaze/templates/pipeline/partials/_eligibility_trace.html
  - src/phaze/templates/pipeline/partials/_force_skip_dialog.html
  - src/phaze/templates/record/record_body.html
  - tests/analyze/test_force_skip_writer.py
  - tests/shared/test_eligibility_trace.py
autonomous: true
requirements: [UI-03, UI-04]
must_haves:
  truths:
    - "POST /pipeline/files/{file_id}/skip/{stage} writes a StageSkip row (committed) with a sanitized required reason, enrich-only (D-09/D-10, UI-04)"
    - "The force-skip writer is additive-only — it never clears analysis.failed_at or any failure marker (behavior 6, shadow-compare stays green)"
    - "A non-enrich stage force-skip returns 422; an empty reason is rejected before any write (D-09/D-10, V4/V5)"
    - "The reason is run through sanitize_pg_text before persist (NUL-abort footgun) and survives round-trip (behavior 9)"
    - "The right-pane eligibility trace evaluates eligible() conjuncts for ONE file and names the unmet blocker (D-06/D-07, UI-03, behavior 10)"
    - "The expanded 6-pill matrix + trace + force-skip live in the per-file right pane — the right-pane half of the D-02 dual home (D-02, UI-03)"
  artifacts:
    - path: "src/phaze/routers/pipeline.py"
      provides: "force_skip_stage writer + single-row eligibility trace endpoint"
      contains: "skip"
    - path: "src/phaze/templates/pipeline/partials/_eligibility_trace.html"
      provides: "Named-conjunct pass/fail trace with highlighted blocker"
    - path: "src/phaze/templates/record/record_body.html"
      provides: "Expanded 6-pill matrix (trace triggers) + enrich-only force-skip control"
  key_links:
    - from: "src/phaze/templates/record/record_body.html"
      to: "POST /pipeline/files/{id}/skip/{stage}"
      via: "Alpine x-trap confirm dialog with required reason textarea (enrich pills only)"
      pattern: "skip"
---

<objective>
Deliver the per-file right pane: the single-row eligibility trace (UI-03 — the diagnostic whose absence hid
the deadlock) and the force-skip escape hatch (UI-04 — writer + confirm dialog + required reason), plus the
expanded pill matrix whose pills are trace triggers. The force-skip writer is the correctness-sensitive
mutating endpoint of this phase.

Purpose: UI-03 gives the operator "why is this file not eligible?" as named conjuncts + the specific blocker.
UI-04 lets the `failed` bucket converge for genuinely-unprocessable files by writing a distinct, additive
`skipped` marker (enrich-only, honest — never counterfeit `done`).
Output: force_skip endpoint + trace endpoint + `_eligibility_trace.html` + `_force_skip_dialog.html` +
record_body right-pane wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-UI-SPEC.md
@.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-RESEARCH.md
@.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-PATTERNS.md
@src/phaze/routers/pipeline_stages.py
@src/phaze/templates/record/record_body.html

<interfaces>
- Writer analog: `routers/pipeline_stages.py:53-56` (`_validate_stage` allowlist), `:82-99` (`Form()` field +
  `await session.commit()`). Retry analog: `routers/pipeline.py:934` (HTMX fragment response).
- Enrich allowlist: `tasks/_shared/stage_control.py:51 STAGE_TO_FUNCTION` (metadata/analyze/fingerprint).
- `services/pg_text.py:sanitize_pg_text` — mandatory for persisted free text (NUL passes pydantic then aborts PG txn).
- `get_session` NEVER auto-commits — the writer commits itself; tests assert from an INDEPENDENT session.
- Trace: `enums/stage.py:eligible` (~237) + `ELIGIBILITY_DAG` (~61-69); `load_scalars` shape
  (`test_stage_status_equivalence.py:351-389`) to fetch one file's per-stage scalars; single-row read, NOT a scan.
- Trace subtlety: a skipped upstream counts as met (`stage_satisfied = done OR skipped`).
</interfaces>

Copy strings verbatim from the UI-SPEC Copywriting Contract (force-skip confirm heading/body/reason label/
validation/success toast; trace verdict + conjunct lines).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Force-skip writer endpoint (enrich-only, additive, sanitized, committed)</name>
  <files>src/phaze/routers/pipeline.py, tests/analyze/test_force_skip_writer.py</files>
  <read_first>
    - src/phaze/routers/pipeline_stages.py:53-56 (_validate_stage allowlist), :82-99 (Form + await session.commit)
    - src/phaze/routers/pipeline.py:934 (retry endpoint — HTMX fragment response idiom)
    - src/phaze/services/pg_text.py (sanitize_pg_text), src/phaze/tasks/_shared/stage_control.py:51 (STAGE_TO_FUNCTION)
    - .planning/phases/87-.../87-RESEARCH.md (Force-skip writer Code Example) + 87-VALIDATION behaviors 6 & 9
  </read_first>
  <behavior>
    - POST /pipeline/files/{id}/skip/{stage} with stage='propose' → HTTP 422 (enrich-only), no row written.
    - POST with empty/whitespace reason → inline "A reason is required." fragment, no row written.
    - POST with valid reason → a StageSkip(file_id, stage, sanitized reason) row is committed and readable from an INDEPENDENT session.
    - A reason containing a NUL byte is sanitized (no PG txn abort) and the sanitized text round-trips.
    - The endpoint never clears analysis.failed_at (additive-only): a terminally-failed analyze keeps failed_at after a skip.
  </behavior>
  <action>
    Add `force_skip_stage(file_id: uuid.UUID, stage: str, reason: Annotated[str, Form()], session=Depends(...))`
    at `POST /pipeline/files/{file_id}/skip/{stage}` (HTMLResponse). Guard `if stage not in STAGE_TO_FUNCTION:
    raise HTTPException(422, "stage not force-skippable")` (D-10 enrich-only — mirror `_validate_stage`). If
    `not reason.strip()`: return the inline "A reason is required." fragment (no write). `reason =
    sanitize_pg_text(reason)`. `session.add(StageSkip(file_id=file_id, stage=stage, reason=reason))`;
    `await session.commit()` (get_session does NOT auto-commit). Return an HTMX ack fragment (the pill flips to
    `⊘ skipped` on the NEXT poll tick — not optimistic). Do NOT touch `analysis.failed_at` or any failure marker
    (additive-only — this keeps the shadow gate green). Add `tests/analyze/test_force_skip_writer.py` covering
    the five behaviors, reading back from an INDEPENDENT session.
  </action>
  <verify>
    <automated>uv run pytest tests/analyze/test_force_skip_writer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Behaviors 6 (additive: failed_at unchanged) and 9 (sanitize + required reason + round-trip) assert green.
    - Non-enrich stage → 422; empty reason → no write; valid write is committed (independent-session read).
  </acceptance_criteria>
  <done>The force-skip marker is written safely: enrich-only, additive, sanitized, committed.</done>
</task>

<task type="auto">
  <name>Task 2: Single-row eligibility trace endpoint + _eligibility_trace.html</name>
  <files>src/phaze/routers/pipeline.py, src/phaze/templates/pipeline/partials/_eligibility_trace.html, tests/shared/test_eligibility_trace.py</files>
  <read_first>
    - src/phaze/routers/record.py:41-131 (file_record — the file_id-scoped single-row read pattern)
    - src/phaze/enums/stage.py (eligible ~237, ELIGIBILITY_DAG ~61-69, resolve_status ~154-183)
    - tests/integration/test_stage_status_equivalence.py:351-389 (load_scalars shape)
    - .planning/phases/87-.../87-UI-SPEC.md (§ Copywriting: Trace verdict + conjunct lines) + 87-VALIDATION behavior 10
  </read_first>
  <action>
    Add a trace endpoint (e.g. `GET /pipeline/files/{file_id}/trace/{stage}`) that loads ONE file's per-stage
    scalars (load_scalars shape) and evaluates `resolve_status` + the `eligible()` conjuncts in Python — a
    single-row read, NOT a corpus query. Render `_eligibility_trace.html`: a verdict line ("{Stage} — NOT
    eligible" / "{Stage} — eligible (in the pending set)") and the four conjunct lines `done?` · `in-flight?` ·
    `upstream met?` · `terminal fail?`, each with `✓`/`✗` + a short phrase; the unmet blocker gets `← blocker`
    (bold), naming the specific upstream from `ELIGIBILITY_DAG[stage]`. For enrich stages `upstream met?` is
    vacuously true (empty upstream). A skipped upstream counts as met (`stage_satisfied = done OR skipped`).
    Degrade to "Trace unavailable this tick." on error. Add `tests/shared/test_eligibility_trace.py`: seed a
    file where analyze is blocked on metadata (downstream case) and assert the trace names "metadata not done"
    as the blocker; assert the trace is a single-row evaluation (no corpus query).
  </action>
  <verify>
    <automated>uv run pytest tests/shared/test_eligibility_trace.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Behavior 10: the trace evaluates `eligible()` conjuncts for ONE file and names the correct unmet blocker.
    - Enrich upstream renders vacuously-met; a skipped upstream renders as satisfied.
    - The trace endpoint is degrade-safe ("Trace unavailable this tick.").
  </acceptance_criteria>
  <done>The right-pane eligibility trace explains non-eligibility per stage, cheaply and accurately.</done>
</task>

<task type="auto">
  <name>Task 3: Right-pane — expanded matrix (trace triggers) + enrich-only force-skip dialog</name>
  <files>src/phaze/templates/record/record_body.html, src/phaze/templates/pipeline/partials/_force_skip_dialog.html</files>
  <read_first>
    - src/phaze/templates/record/record_body.html (the #record-body slide-in host)
    - src/phaze/templates/pipeline/partials/_stage_matrix.html (from Plan 04 — the pill matrix)
    - .planning/phases/87-.../87-UI-SPEC.md (§ Component Inventory: Expanded matrix / Eligibility trace block / Force-done-skip control + Copywriting Contract + Accessibility baseline)
    - .planning/phases/87-.../87-PATTERNS.md (§ Force-skip control + confirm dialog — Alpine x-trap idiom)
  </read_first>
  <action>
    In `record_body.html` add the expanded 6-pill `_stage_matrix` where each pill is a trace trigger:
    `hx-get` the Task-2 trace endpoint (with `role="button"`, `hx-trigger="click, keyup[key=='Enter']"`,
    focus ring) revealing `_eligibility_trace.html` beneath the clicked pill. Add the Force complete/skip control
    for enrich stages ONLY (metadata/fingerprint/analyze) → `_force_skip_dialog.html`: an Alpine `x-trap`
    focus-trap dialog (record-slide-in/⌘K pattern) with a REQUIRED `<textarea>` reason (placeholder per UI-SPEC),
    an accent (Phaze cyan) confirm button, Cancel as default focus; submit `POST`s to the Task-1 endpoint. There
    must be NO force-skip affordance on the propose/review/apply pills (approval-bypass hazard, D-10). Use the
    UI-SPEC copy verbatim (confirm heading/body, reason label, validation, success toast); `aria-live="polite"`
    toast. Every stepper/trigger carries an explicit `aria-label`.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/test_eligibility_trace.py -x -q && uv run ruff check src/phaze/routers/pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - The expanded matrix renders 6 pills as keyboard-accessible trace triggers (Enter/Space, focus ring).
    - The force-skip dialog appears ONLY on enrich pills; propose/review/apply have no skip affordance (verified by a render assertion).
    - The dialog's reason textarea is required; confirm posts to the Task-1 endpoint; copy matches the UI-SPEC.
  </acceptance_criteria>
  <done>The right pane composes the expanded matrix, per-pill trace, and enrich-only force-skip dialog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → force-skip mutating endpoint | untrusted stage + free-text reason cross into a DB write |
| operator reason free-text → PostgreSQL | NUL/control chars can abort the PG txn |
| stage pill → single-row trace read | file_id-scoped read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-18 | Elevation of Privilege | approval bypass via force-skip of propose/review/apply | mitigate | Enrich-only: `STAGE_TO_FUNCTION` allowlist → 422 (D-10), backstopped by the Plan-01 DB CHECK; no skip affordance rendered on approval pills |
| T-87-19 | Denial of Service | NUL byte in reason aborts PG txn | mitigate | `sanitize_pg_text(reason)` before persist (project memory: unbounded recovery loop otherwise) |
| T-87-20 | Integrity | writer "tidies" failed_at → shadow-compare regression | mitigate | Additive-only writer (never clears failure markers); behavior-6 test asserts failed_at unchanged |
| T-87-21 | Tampering | XSS via reason echoed in template | mitigate | Jinja2 autoescape; reason rendered as escaped text, never `\| safe`; acks are int/bool only where possible |
| T-87-22 | Input Validation | missing/blank required reason | mitigate | `reason.strip()` required → inline validation error, no write (D-09) |
| T-87-23 | Denial of Service | trace becomes a corpus scan | mitigate | Single-row `resolve_status`/`eligible()` eval (behavior 10); degrade-safe |

No package installs this phase (zero new deps) — no supply-chain checkpoint required.
</threat_model>

<verification>
- `just test-bucket analyze` + `just test-bucket shared` green in isolation.
- `uv run ruff check .` clean; independent-session read confirmed in the writer test.
</verification>

<success_criteria>
- Force-skip writer is enrich-only, additive, sanitized, committed; approval stages cannot be skipped.
- Per-pill eligibility trace names the blocker; right-pane composes matrix + trace + enrich-only dialog.
</success_criteria>

<output>
Create `.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-06-SUMMARY.md` when done.
</output>
