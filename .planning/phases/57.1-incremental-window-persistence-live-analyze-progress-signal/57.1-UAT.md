---
status: partial
phase: 57.1-incremental-window-persistence-live-analyze-progress-signal
source: [57.1-01-SUMMARY.md, 57.1-02-SUMMARY.md, 57.1-03-SUMMARY.md, 57.1-04-SUMMARY.md]
started: 2026-06-30
updated: 2026-06-30
---

## Current Test

[testing complete — operator-run by Claude as tester (backend phase, no UI); 2 items deployment-gated]

## Tests

### 1. Cold Start Smoke — fresh migrate + boot
expected: From an empty DB, `alembic upgrade head` runs the full chain (incl. 027→028 `analysis_completed_at`) cleanly with a single head; the app imports/boots; migration 028 file present.
result: pass
evidence: "Running upgrade 027 -> 028, Add the analysis_completed_at completion discriminator (Phase 57.1, D-03 KEY RISK)" · heads=1 · current rev=028 · `import phaze.main` OK.

### 2. Progress endpoint — counter-only upsert + auth
expected: `POST /api/internal/agent/analysis/{file_id}/progress` upserts ONLY fine_windows_analyzed/total; rejects no-bearer (401) and forged/negative body (422, extra="forbid"/ge=0); no FileState flip / window write / ledger clear / S3 delete / completed_at stamp.
result: pass
evidence: tests/test_routers/test_agent_analysis.py progress + no-side-effects + auth tests green in the full suite (2505 passed).

### 3. In-flight progress signal advances
expected: analyze_file's progress_cb fires START(0, N) then per-window bumps; fine_windows_analyzed advances mid-flight; denominator == completion fine_windows_total. This is the read-only signal Phase 58 displays.
result: pass
evidence: tests/test_services/test_analysis.py progress tests green.

### 4. KEY RISK — partial row excluded from proposals
expected: A partial in-progress analysis row (analysis_completed_at NULL) does NOT appear in get_proposal_pending_batches; a completed row (completed_at set) DOES. bpm-filtered search excludes a NULL-bpm partial row.
result: pass
evidence: tests/test_services/test_pipeline.py partial/completed regression + test_search_queries.py bpm invariant green.

### 5. Crash-mid-run idempotency
expected: A file killed mid-analysis leaves only a partial row; a re-run via put_analysis produces final aggregates + window set byte-identical to an uninterrupted control; FileState reaches ANALYZED exactly once; no duplicate/orphan windows.
result: pass
evidence: tests/test_analysis_progress_spike.py idempotent tests green (real Postgres).

### 6. Completion stamp is server-set, unforgeable
expected: analysis_completed_at is set by put_analysis via func.now() at completion; it is absent from every payload (AnalysisWritePayload/AnalysisProgressPayload) → a forged completed_at body is rejected (422).
result: pass
evidence: tests/test_routers/test_agent_analysis.py completed_at tests green.

### 7. Both lane bridges post mid-flight, best-effort, kill-safe
expected: The pebble (local/A1) Queue-drainer and the k8s to_thread/run_coroutine_threadsafe bridge both post throttled mid-flight counts + a final flush; a dropped/failed POST never crashes the job; SIGKILL of the child tears the drainer down (no hung slot); EXIT_ANALYSIS/EXIT_CALLBACK contract preserved.
result: pass
evidence: tests/test_tasks/test_functions.py + tests/test_job_runner.py progress tests green.

### 8. Real multi-hour kill-9 on the local/A1 lane (live)
expected: On a real multi-hour concert file: ≥2 distinct mid-flight counter values logged before completion; kill-9 mid-fine-pass → drainer terminates promptly (no hung slot); Phase 32 re-enqueue → one clean re-run identical to an uninterrupted run, ANALYZED once.
result: blocked
blocked_by: prior-phase
reason: "Deployment-gated — operator-approved deferred-to-live (this host did not drive a real multi-hour file). Proven in isolation via automated proxies (tests 5 + 7). Re-run at the homelab rollout; see 57.1-HUMAN-UAT.md UAT-57.1-01."

### 9. Live Kueue k8s-lane mid-flight progress
expected: A real per-file Kueue Job posts ≥2 mid-flight counts to the progress endpoint via the to_thread/run_coroutine_threadsafe bridge; exit-code contract preserved.
result: blocked
blocked_by: prior-phase
reason: "Deployment-gated — needs a live Kueue cluster. Operator-approved deferred-to-live; proxied by test 7. See 57.1-HUMAN-UAT.md UAT-57.1-02."

## Summary

total: 9
passed: 7
issues: 0
pending: 0
skipped: 0
blocked: 2

## Gaps

[none — 0 code issues. The 2 blocked items are deployment-gated prerequisite gates, not code gaps; tracked in 57.1-HUMAN-UAT.md.]

## Notes

- Backend phase (no UI) — Claude ran the UAT as the tester by executing each observable behavior. Tests 2–7 verified via the full pytest suite (**2505 passed / 0 failed / 0 errors** on a pristine empty DB with the conftest's `create_all`/`drop_all` schema lifecycle — the suite is NOT run via Alembic).
- Harness note for future runs: do NOT pre-run `alembic upgrade head` against the ephemeral test DB; the `async_engine` conftest fixture (tests/conftest.py:131) owns schema via `create_all`/`drop_all`. Pre-migrating collides with that lifecycle and produces spurious `pk_agents` UniqueViolation errors. Migration 028 is verified separately (test 1).
