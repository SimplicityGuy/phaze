---
phase: 57.1-incremental-window-persistence-live-analyze-progress-signal
verified: 2026-06-30T00:00:00Z
status: human_needed
score: 7/7 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Real multi-hour kill-9 on local/A1 lane and k8s lane live progress"
    expected: "Kill -9 mid-analysis terminates parent drainer promptly (no hang), Phase 32 re-enqueue produces a final row identical to an uninterrupted run, FileState ANALYZED exactly once, >= 2 distinct mid-flight counters logged; k8s lane shows >= 2 run_coroutine_threadsafe progress POSTs on a live cluster run"
    why_human: "Operator-approved deferral (recorded in 57.1-01-SPIKE-FINDINGS.md §3 and deferred-items.md). The automated proxies (test_analysis_progress_spike.py Tasks 1+2) prove the pebble mechanics and idempotency separately. The composed real multi-hour run on essentia audio + the k8s live cluster check are deployment-gated UAT items per the v6.0-style deferral pattern."
---

# Phase 57.1: Incremental Window Persistence & Live Analyze Progress Signal Verification Report

**Phase Goal:** Bump a progress COUNT (`analysis.fine_windows_analyzed`/`fine_windows_total`) incrementally as each window completes during `analyze_file` — exposing a read-only per-file mid-flight progress signal the Phase 58 Analyze workspace can display for in-flight files. COUNTER-ONLY (57.1-CONTEXT D-01): `analysis_window` detail rows still land atomically at completion via `put_analysis`; NOT written incrementally. Idempotent + safe under Phase 32 reboot/re-enqueue. A partial in-progress row must NOT be treated as a completed analysis by proposals/search/sort — gated on a new `analysis_completed_at` completion discriminator (the KEY RISK).
**Verified:** 2026-06-30
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `analyze_file` has `progress_cb` wired into its per-window fine loop (START + per-window bump; denominator = `fine_windows_total`); does NO I/O / no `agent_client` import | VERIFIED | `analysis.py:448-498`: `_analyze_fine_windows(..., progress_cb=None)` fires `progress_cb(0, len(natural))` before the loop and `progress_cb(len(fine_windows), len(natural))` after each append. `analyze_file:554` accepts `progress_cb` and threads it through at line 604. No `httpx` or `agent_client` import in `analysis.py` header (confirmed by grep — empty result). |
| 2 | Counter-only `POST .../analysis/{file_id}/progress` upserts ONLY `fine_windows_analyzed`/`fine_windows_total`; does NOT flip `FileState.ANALYZED`, write `AnalysisWindow` rows, clear the ledger, delete staged S3, or set `analysis_completed_at`; Bearer auth; `AnalysisProgressPayload` `extra="forbid"`, `ge=0` | VERIFIED | `agent_analysis.py:245-294`: handler upserts exactly two count columns in the `on_conflict_do_update` SET clause (lines 282-288). Comment at lines 290-293 explicitly confirms no side effects. `AnalysisProgressPayload` at `schemas/agent_analysis.py:82-100`: `model_config = ConfigDict(extra="forbid")`, both counts `int = Field(ge=0)`, REQUIRED (no default). Bearer auth via `Depends(get_authenticated_agent)`. |
| 3 | `agent_client.post_analysis_progress` is best-effort (returns `None` on failure) | VERIFIED | `agent_client.py:543-562`: `async def post_analysis_progress(...) -> None` — issues POST to `/api/internal/agent/analysis/{file_id}/progress`, routes through `self._request` (inherits tenacity retries), returns `None`. Docstring states "caller swallows `AgentApiError` after retries (D-16)". |
| 4 | Migration 028 adds `analysis_completed_at`; `put_analysis` stamps it at completion; `get_proposal_pending_batches` is gated on `analysis_completed_at IS NOT NULL` (KEY RISK closed) | VERIFIED | Migration `028_add_analysis_completed_at.py`: `revision="028"`, `down_revision="027"`, `op.add_column("analysis", sa.Column("analysis_completed_at", sa.DateTime(timezone=True), nullable=True))`. `AnalysisResult.analysis_completed_at: Mapped[datetime | None]` at `models/analysis.py:38`. `put_analysis` at `agent_analysis.py:228` stamps via `func.now()` inside `if dumped:` branch (same txn as `ANALYZED` flip). `pipeline.py:1132`: `AnalysisResult.analysis_completed_at.isnot(None)` in convergence gate. `analysis_completed_at` absent from `AnalysisWritePayload` and `_ANALYSIS_COLUMN_FIELDS` — cannot be wire-set. |
| 5 | Pebble bridge (`functions.py`) + k8s bridge (`job_runner.py`) both post progress via the client, best-effort, with `analysis_progress_interval_sec` throttle | VERIFIED | `functions.py:111-227`: `_QueueProgressSink` (module-level, picklable, no closure/httpx), `_drain_progress` (parent-side, `asyncio.to_thread(queue.get)`, throttled by `interval_sec`, final flush), future-done sentinel teardown (`add_done_callback` → `queue.put(_PROGRESS_SENTINEL)`) with `asyncio.wait_for` + cancel backstop. `job_runner.py:133-158`: `_make_progress_cb` builds a sync callback using `asyncio.run_coroutine_threadsafe(..., loop)` (fire-and-forget, NOT `.result()`); `loop` captured before `asyncio.to_thread` (line 266); `analyze_file` invoked via `asyncio.to_thread` (line 269); progress errors swallowed. `config.py:830-834`: `analysis_progress_interval_sec: float = Field(default=5.0, ge=0.0, ...)`. |
| 6 | D-01 counter-only honored: NO incremental `analysis_window` detail-row writes anywhere | VERIFIED | `analysis.py`, `tasks/functions.py`, and `job_runner.py` contain no `AnalysisWindow` INSERT/upsert outside of `put_analysis`. The `AnalysisWindow` delete/insert in `put_analysis` at `agent_analysis.py:206-213` is guarded by `if body.windows is not None` — only fires on completion. `post_analysis_progress` handler body has zero `AnalysisWindow` reference. |
| 7 | A partial in-progress row is NOT treated as a completed analysis by proposals/search/sort — KEY RISK regression tested | VERIFIED | `tests/test_services/test_pipeline.py:959`: `test_get_proposal_pending_batches_excludes_partial_analysis_row` seeds a METADATA_EXTRACTED file + FileMetadata + partial analysis row (analyzed=3<40, bpm=None, completed_at=None) → asserts 0 batches; then stamps `completed_at` → asserts file appears (positive control). `tests/test_services/test_search_queries.py:213`: `test_search_bpm_filter_excludes_partial_analysis_row` asserts NULL-bpm partial row is excluded from bpm-filtered search. |

**Score:** 7/7 truths verified

### Deferred Items

Items not yet met but explicitly addressed at deployment — not actionable gaps.

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Real multi-hour kill-9 on local/A1 lane + k8s live progress check | Deployment / live run (operator-approved) | `57.1-01-SPIKE-FINDINGS.md §3`: "deferred to live"; `deferred-items.md`: pre-existing env test failures pre-exist this phase; plan 57.1-01 Task 3 fallback clause explicitly authorizes deferral when local lane is environment-blocked. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `alembic/versions/028_add_analysis_completed_at.py` | Additive nullable `analysis_completed_at` column on `analysis` (down_revision 027) | VERIFIED | Exists; correct revision/down_revision; upgrade adds nullable `DateTime(timezone=True)` column; downgrade drops it; touches ONLY the `analysis` table. |
| `src/phaze/models/analysis.py` | `AnalysisResult.analysis_completed_at` mapped column | VERIFIED | Line 38: `analysis_completed_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)` |
| `src/phaze/schemas/agent_analysis.py` | `AnalysisProgressPayload` (`extra="forbid"`, `ge=0` required counts) + `AnalysisProgressResponse` | VERIFIED | Lines 82-111: both classes present; `extra="forbid"`, both int counts required `Field(ge=0)`, fine-only (no coarse), response has `agent_id: str` + `file_id: uuid.UUID`. |
| `src/phaze/routers/agent_analysis.py` | `post_analysis_progress` counter-only sibling handler | VERIFIED | Lines 245-294: `@router.post("/{file_id}/progress", ...)` — counter-only upsert, all completion side effects absent. |
| `src/phaze/services/agent_client.py` | `post_analysis_progress` best-effort POST method | VERIFIED | Lines 543-562: async method, routes through `self._request`, returns None, best-effort semantics documented. |
| `src/phaze/services/analysis.py` | `progress_cb` param on `analyze_file` + `_analyze_fine_windows` (START + per-window emit) | VERIFIED | Lines 448-498 and 545-624: both functions have `progress_cb: Callable[[int, int], None] | None = None`; correct START + bump emission; no I/O; fine-only. |
| `src/phaze/tasks/functions.py` | Pebble Queue-drainer bridge | VERIFIED | Lines 95-227: `_QueueProgressSink`, `_drain_progress`, `_run_analysis_with_progress`, `_post_progress_count` — full Queue-drainer implementation per SPIKE-FINDINGS. |
| `src/phaze/job_runner.py` | k8s `to_thread` + `run_coroutine_threadsafe` progress bridge | VERIFIED | Lines 119-158, 260-276: `_safe_post_progress`, `_make_progress_cb` with `run_coroutine_threadsafe` (fire-and-forget); `analyze_file` moved to `asyncio.to_thread`; loop captured before offload. |
| `src/phaze/config.py` | `analysis_progress_interval_sec` throttle knob (default 5) | VERIFIED | Lines 830-834: `analysis_progress_interval_sec: float = Field(default=5.0, ge=0.0, ...)` with dual alias. |
| `src/phaze/services/pipeline.py` | Convergence gate requires `analysis_completed_at IS NOT NULL` | VERIFIED | Lines 1122-1134: `exists(select(AnalysisResult.id).where(AnalysisResult.file_id == FileRecord.id, AnalysisResult.analysis_completed_at.isnot(None)))` |
| `tests/test_analysis_progress_spike.py` | Transport kill-safety proof + crash-mid-run idempotency regression | VERIFIED | File exists; contains transport tests (`-k transport`) and idempotency tests (`-k idempotent`) per spike findings. |
| `tests/test_services/test_pipeline.py` | KEY RISK regression (partial → 0 batches; completed → batched) | VERIFIED | `test_get_proposal_pending_batches_excludes_partial_analysis_row` at line 959 — both negative case and positive control. (Note: plan 57.1-02 specified `test_proposal_queries.py` as target; test landed in `test_pipeline.py` — correct module for `get_proposal_pending_batches` testing.) |
| `tests/test_services/test_search_queries.py` | Search BPM invariant: partial-row excluded from bpm-filtered search | VERIFIED | `test_search_bpm_filter_excludes_partial_analysis_row` at line 213. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `analyze_file progress_cb` | `tasks/functions.py` parent drainer | picklable `_QueueProgressSink` → Manager queue proxy, drained by `_drain_progress` | VERIFIED | `functions.py:194-211`: `manager.Queue()` proxy + `_QueueProgressSink(progress_queue)` passed as `progress_cb=sink` into `run_in_process_pool`; done-callback pushes sentinel; `_drain_progress` task created. |
| `tasks/functions.py` drainer | `agent_client.post_analysis_progress` | throttled best-effort POST, errors swallowed | VERIFIED | `functions.py:129-141`: `_post_progress_count` calls `api.post_analysis_progress(...)`, swallows all exceptions. |
| `job_runner.py` cb | `agent_client.post_analysis_progress` | `run_coroutine_threadsafe` fire-and-forget, throttle-checked, errors swallowed | VERIFIED | `job_runner.py:154-155`: `asyncio.run_coroutine_threadsafe(_safe_post_progress(...), loop)` — NOT `.result()`. |
| `src/phaze/routers/agent_analysis.py post_analysis_progress` | `AnalysisResult.file_id` UQ | `pg_insert(...).on_conflict_do_update(index_elements=['file_id'], set_=fine counts)` | VERIFIED | Lines 281-288 exactly match the plan's key link pattern. |
| `src/phaze/services/agent_client.py post_analysis_progress` | `/api/internal/agent/analysis/{file_id}/progress` | `self._request POST` through the tenacity funnel | VERIFIED | Line 558: `f"/api/internal/agent/analysis/{file_id}/progress"`. |
| `put_analysis` completion branch | `AnalysisResult.analysis_completed_at` | `func.now()` stamp in same `if dumped:` txn as `ANALYZED` flip | VERIFIED | `agent_analysis.py:228`: `update(AnalysisResult).where(...).values(analysis_completed_at=func.now())` inside `if dumped:` block. |
| `get_proposal_pending_batches` | `AnalysisResult.analysis_completed_at` | `exists()` subquery predicate `.isnot(None)` | VERIFIED | `pipeline.py:1132`: `AnalysisResult.analysis_completed_at.isnot(None)` inside the convergence exists subquery. |

### Data-Flow Trace (Level 4)

The counter-only phase does not produce a new rendering artifact; it writes two integer columns to an existing DB row. Phase 58 (downstream) reads the counter — tracing the forward path is deferred to Phase 58 verification. The convergence gate (KEY RISK) has been directly tested via `test_get_proposal_pending_batches_excludes_partial_analysis_row`.

### Behavioral Spot-Checks

Behavioral spot-checks require a running server/database. Skipped — no runnable entry point available without starting services. Automated test coverage in the touched modules verified above is the substitute.

### Probe Execution

No probe scripts declared in PLAN frontmatter or found at `scripts/*/tests/probe-*.sh` for this phase.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| PROG-01 | Plans 57.1-01, 57.1-04 | `analyze_file` bumps `fine_windows_analyzed/total` incrementally per window (counter-only, not detail rows) | VERIFIED | `analysis.py:448-498`, `functions.py` bridge, `job_runner.py` bridge all confirmed. |
| PROG-02 | Plans 57.1-01, 57.1-04 | Incremental counter idempotent + safe under Phase 32 reboot/re-enqueue; killed file re-runs cleanly, no dup/orphaned state | VERIFIED | Spike `test_analysis_progress_spike.py` (automated proxy proves `put_analysis` file_id-UQ replace overwrites partial rows with no dup windows); counter-only on_conflict_do_update in `post_analysis_progress` is overwrite-safe. |
| PROG-03 | Plans 57.1-02, 57.1-03 | Incremental progress readable per-file via `fine_windows_analyzed/total` on the in-progress `analysis` row; partial row NOT treated as completed (gated on `analysis_completed_at IS NOT NULL`) | VERIFIED | `post_analysis_progress` endpoint confirmed; KEY RISK gate confirmed; negative + positive control regression tests confirmed. |

All three requirements for this phase satisfied.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | — | No TBD/FIXME/XXX markers found in any phase-touched file | — | — |

No debt markers, no stubs, no empty placeholder implementations found across all 9 modified source files and the migration.

The `return {}` at `analysis.py:540` is inside `_representative_features` and is the legitimate no-coarse-windows guard — not a stub.

### Human Verification Required

#### 1. Real Multi-Hour kill-9 (local/A1 lane) + k8s Live Progress

**Test:** Pick a real multi-hour concert audio file. Drive a local/A1 analyze with the Queue-drainer wired. Confirm >= 2 distinct mid-flight counter values are logged before completion. `kill -9` the analysis worker/child mid-fine-pass; confirm the parent drainer terminates promptly (no hung slot). Let Phase 32 re-enqueue re-run the file; confirm exactly one clean re-run, final analysis row + windows match an uninterrupted run, and `FileState` ends `ANALYZED` exactly once. For the k8s lane: run a live Kueue cluster job and confirm >= 2 `run_coroutine_threadsafe` progress POSTs are scheduled mid-flight.

**Expected:** >= 2 mid-flight counter values logged before completion; drainer terminates promptly after kill -9; re-run is clean with no duplicate/orphaned state; FileState ANALYZED exactly once; k8s lane shows live progress POSTs.

**Why human:** Requires a real multi-hour audio file, a running pebble process pool, actual SIGKILL delivery, Phase 32 re-enqueue infrastructure, and a live Kueue cluster. Cannot be reproduced without the homelab environment. Automated proxies in `test_analysis_progress_spike.py` prove the mechanics in isolation. This is a v6.0-style operator-approved deferral recorded in `57.1-01-SPIKE-FINDINGS.md §3`.

### Gaps Summary

No gaps found. All 7 observable truths are VERIFIED in the actual codebase. The single human verification item is an operator-approved deployment-deferred live run, not a code gap.

---

_Verified: 2026-06-30_
_Verifier: Claude (gsd-verifier)_
