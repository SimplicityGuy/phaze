---
phase: 57.1-incremental-window-persistence-live-analyze-progress-signal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_analysis_progress_spike.py
autonomous: false
requirements: [PROG-01, PROG-02]
user_setup: []

must_haves:
  truths:
    - "A SIGKILL of the analysis child process terminates the parent progress drainer with no hang (kill-safe transport proven, Assumption A2)"
    - "A file killed mid-analysis re-runs cleanly via Phase 32 re-enqueue: the final analysis row aggregates AND analysis_window set are identical to an uninterrupted control run"
    - "FileState reaches ANALYZED exactly once after the re-run; no duplicate or orphaned analysis_window rows remain"
    - "At least 2 distinct mid-flight counter values are observable during a single long-file run (not only the final count)"
  artifacts:
    - path: "tests/test_analysis_progress_spike.py"
      provides: "Automated crash-mid-run idempotency regression (PROG-02) + pebble transport kill-safety proof"
      min_lines: 60
    - path: ".planning/phases/57.1-incremental-window-persistence-live-analyze-progress-signal/57.1-01-SPIKE-FINDINGS.md"
      provides: "Recorded transport-fork decision (pebble Queue-drainer vs child-side sync httpx) consumed by plan 04"
  key_links:
    - from: "parent progress drainer"
      to: "pebble child future completion"
      via: "sentinel teardown on future done/ProcessExpired"
      pattern: "wrap_future|add_done_callback|ProcessExpired"
    - from: "re-run put_analysis"
      to: "AnalysisResult.file_id UQ"
      via: "on_conflict_do_update full replace overwrites the partial row"
      pattern: "on_conflict_do_update"
---

<objective>
Before any production write path is locked, this SPIKE resolves the transport fork and empirically proves the two load-bearing safety properties of the whole phase on a real long file:

1. **Transport fork (RESEARCH Q1 / Assumption A2):** decide how a per-window `progress_cb` reaches the control plane from inside a pebble child (local + A1 lanes have no event loop / no `agent_client` in the child). Prototype the recommended **Queue-drainer** (a picklable `multiprocessing.Manager().Queue()` proxy drained parent-side) and prove it is **kill-safe** when the child is SIGKILLed; fall back to **child-side sync httpx** only if the drainer can hang. Record the decision in `57.1-01-SPIKE-FINDINGS.md`, which plan 04 reads.
2. **Crash-mid-run idempotency (PROG-02 / RESEARCH Q3):** prove that a file killed mid-analysis, re-enqueued by Phase 32, re-runs cleanly with a final `analysis` row + `analysis_window` set byte-identical to an uninterrupted run, `FileState` ANALYZED exactly once, and no duplicate/orphaned state.

This is non-invasive: it touches NO production module. It uses a synthetic picklable worker to prove the drainer mechanics, the existing `put_analysis` file_id-replace path to prove idempotency, and the real `analyze_file` only in the human-verify long-file run.

Purpose: De-risk the two MEDIUM-confidence forks (A2 pebble kill-safety, A3 k8s thread-safety) so plan 04 implements a settled design.
Output: `tests/test_analysis_progress_spike.py` (becomes the PROG-02 regression) + `57.1-01-SPIKE-FINDINGS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57.1-incremental-window-persistence-live-analyze-progress-signal/57.1-CONTEXT.md
@.planning/phases/57.1-incremental-window-persistence-live-analyze-progress-signal/57.1-RESEARCH.md
@.planning/phases/57.1-incremental-window-persistence-live-analyze-progress-signal/57.1-PATTERNS.md

<interfaces>
<!-- Verified seams the spike exercises (read-only — the spike does NOT modify these). -->

run_in_process_pool (src/phaze/tasks/pool.py:29) — schedules func on a pebble ProcessPool;
  pool.schedule(func, args=list(args), kwargs=kwargs, timeout=...) PICKLES args/kwargs to the child;
  a SIGKILLed child raises builtins.TimeoutError (timeout) or pebble.ProcessExpired (crash);
  asyncio.wrap_future bridges the ProcessFuture onto the loop.
create_process_pool (pool.py:19) — ProcessPool(max_workers=..., max_tasks=1) (worker recycled per task).
put_analysis (src/phaze/routers/agent_analysis.py:125) — pg_insert(...).on_conflict_do_update on
  AnalysisResult.file_id UQ; REPLACES analysis_window rows (delete-then-insert) in the same txn;
  flips FileState.ANALYZED only when dumped is non-empty.
reenqueue done-predicate (src/phaze/tasks/reenqueue.py): process_file is "done" iff
  FileRecord.state IN {ANALYZED, ANALYSIS_FAILED} — pure STATE, independent of the analysis row.
Spike DB/queue scaffolding: reuse tests/integration/conftest.py `stage_env` (real build_pipeline_queue
  + SQLAlchemy session on the same DB) — do NOT install a new framework.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prototype the pebble callback transport and prove SIGKILL kill-safety</name>
  <files>tests/test_analysis_progress_spike.py, .planning/phases/57.1-incremental-window-persistence-live-analyze-progress-signal/57.1-01-SPIKE-FINDINGS.md</files>
  <read_first>
    - src/phaze/tasks/pool.py (the pickling boundary + ProcessExpired/TimeoutError semantics)
    - src/phaze/tasks/functions.py:89-241 (process_file — where ctx["api_client"] and ctx["process_pool"] live)
    - src/phaze/services/agent_client.py:520-549 (post_exec_batch_progress / heartbeat — the best-effort POST shape)
    - .planning/phases/57.1-incremental-window-persistence-live-analyze-progress-signal/57.1-PATTERNS.md (§ functions.py bridge, Pitfall 4)
  </read_first>
  <action>
    Build a throwaway spike harness (inside tests/test_analysis_progress_spike.py, clearly marked SPIKE) that exercises the pebble callback transport WITHOUT touching production modules. Define a module-level picklable worker function that takes a picklable progress sink and a window count, emits N synthetic (analyzed, total) tuples with a short sleep between them, and is schedulable via pool.schedule. Prototype option A (recommended): a multiprocessing.Manager().Queue() proxy passed to the child; the child calls put_nowait((analyzed, total)); a parent-side async drainer (asyncio.to_thread(q.get, timeout=...) loop) reads tuples and would call a stand-in async progress POST. Prove kill-safety: schedule the worker with a tiny pebble timeout so the child is SIGKILLed mid-emit (raising builtins.TimeoutError), and assert the parent drainer TERMINATES (joins within a bounded deadline, no hang) once the child future completes/raises — wire the teardown via a sentinel pushed on the future's done-callback. Also prototype option B (child-side sync httpx.Client POST from picklable primitives) far enough to compare. Pick Queue-drainer if its SIGKILL teardown is clean; otherwise pick child-side httpx. Do NOT add progress_cb to the real analyze_file here — that is plan 04. Record the chosen option, the teardown mechanism, and the residual risk in a new file 57.1-01-SPIKE-FINDINGS.md in the phase directory.
  </action>
  <verify>
    <automated>uv run pytest tests/test_analysis_progress_spike.py -k transport -x -q</automated>
  </verify>
  <done>
    A transport test schedules a picklable worker on a real pebble ProcessPool, observes ≥2 drained (analyzed,total) tuples, forces a SIGKILL (tiny timeout → TimeoutError), and proves the drainer tears down within a bounded deadline; 57.1-01-SPIKE-FINDINGS.md records the chosen transport + teardown mechanism; no src/phaze/ file is modified.
  </done>
  <acceptance_criteria>
    - tests/test_analysis_progress_spike.py contains a transport test that schedules a picklable worker on a real pebble ProcessPool and observes ≥2 emitted (analyzed,total) tuples drained parent-side.
    - The test forces a SIGKILL of the child (tiny pebble timeout → builtins.TimeoutError) and asserts the parent drainer task completes within a bounded deadline (no hang) — i.e. the future-done sentinel tears the drainer down.
    - 57.1-01-SPIKE-FINDINGS.md exists and states the chosen pebble transport (Queue-drainer or child-side httpx) with a one-paragraph justification and the teardown mechanism plan 04 must reuse.
    - No file under src/phaze/ is modified by this task (the spike is non-invasive).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Prove crash-mid-run idempotency on the existing put_analysis replace path</name>
  <files>tests/test_analysis_progress_spike.py</files>
  <read_first>
    - src/phaze/routers/agent_analysis.py:125-234 (put_analysis — aggregate upsert + window delete/insert + ANALYZED flip)
    - src/phaze/tasks/reenqueue.py:100-190 (state-only done-predicate for process_file)
    - tests/integration/conftest.py (the `stage_env` real-queue + session fixture)
    - tests/test_reenqueue.py and tests/test_tasks/test_controller_reenqueue.py (existing re-enqueue test patterns)
  </read_first>
  <action>
    Add an automated integration test (tests/test_analysis_progress_spike.py) using the integration `stage_env` fixture that proves PROG-02 idempotency against the EXISTING write path (no production change needed — analyze_file is unmodified at this wave). Simulate the crash by performing a first put_analysis that lands a partial/in-flight analysis row plus a leftover set of analysis_window rows (mimicking a previous completed-then-killed state), THEN perform a second full put_analysis for the same file_id representing the clean re-run with a known-good aggregate + window set. Assert: (a) the final analysis row aggregates equal the uninterrupted-control values (the re-run's file_id-keyed on_conflict_do_update overwrote the partial row); (b) the analysis_window set for the file exactly equals the control window set with NO duplicate or orphaned rows (the delete-then-insert replace ran); (c) FileState transitions to ANALYZED exactly once; (d) a query for the re-enqueue done-set treats the file as done only after the terminal state. Keep the control vs interrupted comparison explicit (assert row-for-row equality of windows by (tier, window_index)).
  </action>
  <verify>
    <automated>uv run pytest tests/test_analysis_progress_spike.py -k idempotent -x -q</automated>
  </verify>
  <done>
    The integration test asserts the post-re-run analysis row + analysis_window set equal the control run (no dup/orphan), FileState ANALYZED exactly once, done-predicate classifies done only post-completion, with no src/phaze/ modification.
  </done>
  <acceptance_criteria>
    - The test asserts the post-re-run analysis row aggregate columns equal the control-run values.
    - The test asserts the analysis_window rows for the file equal the control set with zero duplicates and zero orphans (counted by file_id and matched by (tier, window_index)).
    - The test asserts FileState is ANALYZED and that the reenqueue done-predicate (state IN {ANALYZED, ANALYSIS_FAILED}) classifies the file as done only post-completion.
    - The test passes without modifying any src/phaze/ module.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the real multi-hour kill-9 on the local/A1 lane</name>
  <files>.planning/phases/57.1-incremental-window-persistence-live-analyze-progress-signal/57.1-01-SPIKE-FINDINGS.md</files>
  <action>
    Operator-driven empirical verification of the crash/transport behavior on a REAL multi-hour file (the one behavior the automated Tasks 1+2 proxy but cannot fully reproduce). Follow the how-to-verify steps below; if the local lane is unavailable on this host, approve on the automated proxies and record the long-file/k8s live check as deferred (v6.0-style) in 57.1-01-SPIKE-FINDINGS.md.
  </action>
  <what-built>
    The automated transport kill-safety proof (Task 1) and the automated crash-idempotency regression (Task 2). What remains is the empirical run on a REAL multi-hour concert file through the local/A1 (pebble) lane — the one behavior that cannot be fully unit-mocked (true crash-mid-essentia under the process pool). The k8s-lane live proof is DEFERRED to a live cluster run, mirroring the v6.0 deployment-deferred E2E pattern.
  </what-built>
  <how-to-verify>
    1. Pick a real multi-hour audio file (a full concert set, the worst case for window count).
    2. Drive a local/A1 analyze of that file with the prototype Queue-drainer wired to log each (analyzed, total) it would POST.
    3. Confirm at least 2 distinct mid-flight counter values are logged BEFORE completion (not just the final).
    4. `kill -9` the analysis worker/child mid-fine-pass; confirm the parent drainer terminates promptly (no hung slot).
    5. Let Phase 32 re-enqueue re-run the file; confirm exactly one clean re-run, the final analysis row + windows match an uninterrupted run, and FileState ends ANALYZED exactly once.
    6. If the local lane is unavailable on this host, approve based on the automated proxies (Tasks 1+2) and record the k8s/long-file live check as deferred in 57.1-01-SPIKE-FINDINGS.md (v6.0-style deferral).
  </how-to-verify>
  <verify>
    <human-check>≥2 mid-flight counter values logged, drainer terminates promptly after kill -9, exactly one clean re-run with final row+windows matching an uninterrupted run, FileState ANALYZED once — OR a recorded v6.0-style deferral.</human-check>
  </verify>
  <done>
    Operator approves the real-file crash/transport behavior (or approves with a recorded deferral in 57.1-01-SPIKE-FINDINGS.md).
  </done>
  <resume-signal>Type "approved" (or "approved — deferred to live" if the real long-file run is environment-blocked), or describe the failure to feed back into the transport decision.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pebble child ↔ parent | SIGKILL boundary: a child that exceeds its timeout or crashes is killed; only picklable primitives cross. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57.1-30 | Denial of Service | parent progress drainer | mitigate | Prove the drainer tears down via a future-done sentinel when the child is SIGKILLed — a hung drainer would stall a worker slot (Assumption A2). This is the spike's primary purpose. |
| T-57.1-31 | Tampering/Integrity | analysis row + analysis_window on re-run | mitigate | Prove put_analysis file_id-UQ on_conflict_do_update + window delete-then-insert overwrite the partial row with no duplicate/orphan; re-enqueue is state-only so the partial row cannot mis-drive recovery. |
| T-57.1-SC | Tampering | npm/pip/cargo installs | accept | This phase installs ZERO external packages (RESEARCH § Package Legitimacy Audit). No supply-chain checkpoint applies. |
</threat_model>

<verification>
- `uv run pytest tests/test_analysis_progress_spike.py -q` green (transport + idempotent).
- `57.1-01-SPIKE-FINDINGS.md` records the transport decision plan 04 consumes.
- Human-verify checkpoint approved (or explicitly deferred-to-live, v6.0 style).
</verification>

<success_criteria>
- Pebble Queue-drainer kill-safety proven (or child-side httpx selected with recorded rationale).
- Crash-mid-run idempotency proven: final row + windows identical to control, ANALYZED once, no dup/orphan.
- Transport decision recorded; no production module modified.
</success_criteria>

<output>
Create `.planning/phases/57.1-incremental-window-persistence-live-analyze-progress-signal/57.1-01-SUMMARY.md` when done.
Also create `.planning/phases/57.1-incremental-window-persistence-live-analyze-progress-signal/57.1-01-SPIKE-FINDINGS.md` (transport decision).
</output>
