---
status: partial
phase: 57.1-incremental-window-persistence-live-analyze-progress-signal
source: [57.1-VERIFICATION.md, 57.1-01-SPIKE-FINDINGS.md]
started: 2026-06-30
updated: 2026-06-30
---

## Current Test

[awaiting deployment-gated operator verification — pre-authorized deferral]

## Tests

### UAT-57.1-01 — Real multi-hour kill-9 idempotency on the local/A1 lane
**Requirement:** PROG-01 / PROG-02 (the spike's Task 3)
**Status:** deferred-to-live (operator-approved 2026-06-30; v6.0-style deployment-gated UAT)
**Why deferred:** the automated proxies (transport kill-safety + `put_analysis`-replace idempotency on real Postgres) are GREEN and the code is verified 7/7; the one behavior they cannot fully reproduce is a true `kill -9` mid-essentia on a real multi-hour concert file through the local/A1 pebble lane. This host (arm64 colima) runs essentia + Postgres but a multi-hour real file was not driven.
**Steps (run at the homelab rollout):**
1. Pick a real multi-hour concert set (worst case for window count).
2. Drive a local/A1 analyze with progress logging; confirm **≥2 distinct mid-flight `(analyzed, total)` counter values** before completion.
3. `kill -9` the analysis child mid-fine-pass; confirm the parent Queue-drainer terminates promptly (no hung worker slot).
4. Let Phase 32 re-enqueue re-run the file; confirm exactly one clean re-run, final `analysis` row + `analysis_window` set identical to an uninterrupted run, `FileState` ends `ANALYZED` exactly once.

### UAT-57.1-02 — k8s-lane live mid-flight progress (Assumption A3)
**Requirement:** PROG-01 (k8s lane)
**Status:** deferred-to-live (operator-approved; deployment-gated, needs a live Kueue cluster)
**Steps (run at the cluster rollout):** submit a real per-file Kueue Job; confirm the `job_runner` `to_thread` + `run_coroutine_threadsafe` bridge posts **≥2 mid-flight counts** to the progress endpoint and the `EXIT_ANALYSIS`/`EXIT_CALLBACK` exit-code contract is preserved.

---

*All code-level must-haves (7/7) are verified in `57.1-VERIFICATION.md`. These two items are deployment-gated live confirmations, mirroring the v5.0/v6.0 deferred-E2E pattern — re-run at the homelab/cluster rollout, then flip `57.1-VERIFICATION.md` to `passed`.*
