---
phase: 57.1
slug: incremental-window-persistence-live-analyze-progress-signal
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-29
---

# Phase 57.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `57.1-RESEARCH.md` § Validation Architecture. The planner fills the
> Per-Task Verification Map row-by-row as plans are written.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.x + pytest-asyncio (async SQLAlchemy/asyncpg) |
| **Config file** | `pyproject.toml` (`[tool.pytest.ini_options]`) |
| **Quick run command** | `uv run pytest tests/test_agent_analysis.py tests/test_analysis.py -q` |
| **Full suite command** | `uv run pytest` |
| **Estimated runtime** | ~quick <30s · full several min (essentia/process-pool tests are slowest) |

---

## Sampling Rate

- **After every task commit:** Run the quick command for the touched area.
- **After every plan wave:** Run `uv run pytest` (full suite) + `uv run mypy .`.
- **Before `/gsd:verify-work`:** Full suite green + 85% coverage gate.
- **Max feedback latency:** ~30 seconds (quick), full suite per wave.

---

## Per-Task Verification Map

> Planner MUST add one row per task. Anchor rows (from RESEARCH § Validation Architecture):

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 57.1-01-xx | 01 (SPIKE) | 0/1 | PROG-01/02 | — | counter advances mid-flight; kill-mid-run → re-enqueue → final row identical to uninterrupted | integration / manual-spike | `uv run pytest tests/test_analysis_progress_spike.py -q` | ❌ W0 | ⬜ pending |
| 57.1-xx-xx | — | — | PROG-01 | — | `fine_windows_analyzed` increases during a run (progress callback fires + upserts) | unit/integration | `uv run pytest tests/test_agent_analysis.py -q` | ❌ W0 | ⬜ pending |
| 57.1-xx-xx | — | — | PROG-02 | — | counter overwrite is idempotent under `put_analysis` file_id replace on re-run | unit | `uv run pytest tests/test_agent_analysis.py -q` | ❌ W0 | ⬜ pending |
| 57.1-xx-xx | — | — | PROG-03 | — | per-file count readable off in-progress analysis row | unit | `uv run pytest tests/test_agent_analysis.py -q` | ❌ W0 | ⬜ pending |
| 57.1-xx-xx | — | — | PROG-03 (KEY RISK) | — | **partial analysis row does NOT leak into `get_proposal_pending_batches`** (gated on completion discriminator, not bare `exists(AnalysisResult)`) | regression | `uv run pytest tests/test_pipeline_convergence.py -q` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Test stubs for PROG-01/02/03 + the convergence-leak regression (planner names exact files; likely `tests/test_agent_analysis.py`, `tests/test_analysis.py`, `tests/test_pipeline_convergence.py` or the existing proposal-batch test).
- [ ] Spike harness fixture for kill-mid-analysis + re-enqueue on a real long file (the SPIKE task).

*Existing pytest + async fixtures cover most infrastructure; no new framework.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| SIGKILL kill-safety of the pebble Queue-drainer / k8s `to_thread` transport on a real multi-hour file | PROG-01/02 (SPIKE) | True crash-mid-essentia behavior under the process-pool / event-loop fork can't be fully unit-mocked | Spike: start a real long-file analysis, `kill -9` mid-run, re-enqueue, assert final analysis row + windows identical to an uninterrupted run and no leaked partial state |

*Most behaviors have automated verification; only the empirical crash/transport spike is manual.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s (quick)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
