---
phase: 57.1
slug: incremental-window-persistence-live-analyze-progress-signal
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-06-29
updated: 2026-06-30
---

# Phase 57.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `57.1-RESEARCH.md` § Validation Architecture. Per-Task Verification
> Map filled by the planner (4 plans, waves 1→4); audited post-execution 2026-06-30 (all green).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.x + pytest-asyncio (async SQLAlchemy/asyncpg) + httpx AsyncClient |
| **Config file** | `pyproject.toml` (`[tool.pytest.ini_options]`) |
| **Quick run command** | `uv run pytest tests/test_routers/test_agent_analysis.py tests/test_services/test_analysis.py -q` |
| **Full suite command** | `uv run pytest` (DB-backed: `TEST_DATABASE_URL=postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_test` + `just test-db`) |
| **Estimated runtime** | ~quick <30s · full ~5.5 min (essentia/process-pool tests are slowest) |

---

## Sampling Rate

- **After every task commit:** Run the quick command for the touched area.
- **After every plan wave:** Run `uv run pytest` (full suite) + `uv run mypy .` + `uv run ruff check .`.
- **Before `/gsd:verify-work`:** Full suite green + 85% coverage gate + the long-file spike result recorded.
- **Max feedback latency:** ~30 seconds (quick), full suite per wave.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 57.1-01-T1 | 01 (SPIKE) | 1 | PROG-01/02 | T-57.1-30 | pebble Queue-drainer is kill-safe: SIGKILL of child tears down the drainer (no hang) | spike/integration | `uv run pytest tests/test_analysis_progress_spike.py -k transport -x -q` | ✅ | ✅ green (4) |
| 57.1-01-T2 | 01 (SPIKE) | 1 | PROG-02 | T-57.1-31 | kill-mid-run → re-run final analysis row + windows identical to control; ANALYZED once; no dup/orphan | integration | `uv run pytest tests/test_analysis_progress_spike.py -k idempotent -x -q` | ✅ | ✅ green (2) |
| 57.1-01-T3 | 01 (SPIKE) | 1 | PROG-01/02 | T-57.1-30/31 | real multi-hour kill-9 on local/A1 lane; ≥2 mid-flight counts; clean re-run | human-verify | (manual; proxied by T1+T2; see 57.1-HUMAN-UAT.md) | n/a | ⏳ deferred-to-live (operator-approved) |
| 57.1-02-T1 | 02 | 2 | PROG-03 | T-57.1-12 | completion stamps analysis_completed_at (server-set func.now()); empty PUT leaves it NULL | router unit + migration | `uv run alembic upgrade head && uv run pytest tests/test_routers/test_agent_analysis.py -k completed_at -x -q` | ✅ | ✅ green (3) |
| 57.1-02-T2 | 02 | 2 | PROG-03 (KEY RISK) | T-57.1-10 | **partial analysis row does NOT leak into get_proposal_pending_batches** (gated on analysis_completed_at IS NOT NULL); completed row DOES batch | regression | `uv run pytest tests/test_services/test_pipeline.py -k "partial or completed" -x -q` *(landed in test_pipeline.py, not test_proposal_queries.py — where get_proposal_pending_batches is tested; verifier-noted deviation)* | ✅ | ✅ green (3) |
| 57.1-02-T2b | 02 | 2 | PROG-03 (KEY RISK) | T-57.1-11 | bpm-filtered search excludes a partial (NULL-bpm) row (invariant locked) | regression | `uv run pytest -k "search and bpm" -x -q` (`tests/test_services/test_search_queries.py`) | ✅ | ✅ green (3) |
| 57.1-03-T1 | 03 | 3 | PROG-01/03 | T-57.1-02 | AnalysisProgressPayload extra=forbid, required ge=0 counts (forged body/negative → ValidationError) | schema unit | `uv run pytest tests/test_routers/test_agent_analysis.py -k "progress and schema" -x -q` | ✅ | ✅ green (4) |
| 57.1-03-T2 | 03 | 3 | PROG-01/03 | T-57.1-01/03 | progress endpoint upserts counter-only; NO FileState flip / window write / ledger clear / S3 delete / completed_at; 401 no-bearer, 422 forged body | router unit | `uv run pytest tests/test_routers/test_agent_analysis.py -k progress -x -q` | ✅ | ✅ green (8) |
| 57.1-03-T3 | 03 | 3 | PROG-01 | T-57.1-05 | agent_client.post_analysis_progress POSTs the right path+verb+body, best-effort return None | client unit | `uv run pytest tests/test_services/test_agent_client.py -k analysis_progress -x -q` | ✅ | ✅ green (2) |
| 57.1-04-T1 | 04 | 4 | PROG-01 | — | analyze_file progress_cb fires START(0,len(natural)) + per-window bump; denominator == completion fine_windows_total | service unit | `uv run pytest tests/test_services/test_analysis.py -k progress -x -q` | ✅ | ✅ green (3) |
| 57.1-04-T2 | 04 | 4 | PROG-01/02 | T-57.1-20/21/22 | pebble bridge posts advancing throttled counts + final flush; drainer kill-safe; POST failure swallowed | task unit | `uv run pytest tests/test_tasks/test_functions.py -k progress -x -q` | ✅ | ✅ green (7) |
| 57.1-04-T3 | 04 | 4 | PROG-01 | T-57.1-23 | k8s to_thread+run_coroutine_threadsafe posts ≥2 mid-flight counts; EXIT_ANALYSIS/EXIT_CALLBACK contract unchanged | runner unit | `uv run pytest tests/test_job_runner.py -k progress -x -q` | ✅ | ✅ green (3) |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky · ⏳ deferred-to-live*

---

## Requirement Coverage Summary

| Requirement | Status | Covering tasks |
|-------------|--------|----------------|
| PROG-01 (incremental counter, all lanes) | COVERED | 01-T1, 03-T1/T2/T3, 04-T1/T2/T3 |
| PROG-02 (idempotent under re-enqueue) | COVERED | 01-T1/T2, 04-T2/T3 |
| PROG-03 (read-only signal + completion discriminator / KEY RISK) | COVERED | 02-T1/T2/T2b, 03-T1/T2 |

All three requirements have green automated verification. No MISSING gaps.

---

## Wave 0 Requirements

- [x] `tests/test_analysis_progress_spike.py` — pebble transport kill-safety + crash-mid-run idempotency (Plan 01).
- [x] `tests/test_routers/test_agent_analysis.py` — `analysis_completed_at` completion stamp (Plan 02) + counter-only progress endpoint, no side effects, auth (Plan 03).
- [x] `tests/test_services/test_pipeline.py` — KEY-RISK regression: partial row not batched; completed row batched (Plan 02). *(planned for test_proposal_queries.py; landed in test_pipeline.py where the query is tested.)*
- [x] `tests/test_services/test_search_queries.py` — bpm-search NULL-row invariant (Plan 02).
- [x] `tests/test_services/test_agent_client.py` — `post_analysis_progress` client method (Plan 03).
- [x] `tests/test_services/test_analysis.py` — `progress_cb` START + per-window emit (Plan 04).
- [x] `tests/test_tasks/test_functions.py` — pebble Queue-drainer bridge + throttle + final flush + kill-safety (Plan 04).
- [x] `tests/test_job_runner.py` — k8s `to_thread`+cb mid-flight posts; exit-code contract preserved (Plan 04).

*Existing pytest + async fixtures (`tests/conftest.py`, `tests/integration/conftest.py`) cover infrastructure; no new framework.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| SIGKILL kill-safety + ≥2 mid-flight counts on a real multi-hour file (local/A1 lane) | PROG-01/02 (SPIKE, Plan 01 Task 3) | True crash-mid-essentia under the process pool can't be fully unit-mocked | Plan 01 Task 3 checkpoint; proxied by automated T1+T2; deferred-to-live per `57.1-HUMAN-UAT.md` (v6.0 pattern) |
| Live Kueue k8s-lane mid-flight progress (≥2 counts) | PROG-01 (k8s lane, Assumption A3) | Needs a live cluster | Deferred-to-live per `57.1-HUMAN-UAT.md` |

*Most behaviors have automated verification; only the empirical crash/transport long-file run + the live k8s lane are manual/deployment-gated.*

---

## Validation Audit 2026-06-30

| Metric | Count |
|--------|-------|
| Requirements | 3 (PROG-01/02/03) |
| COVERED | 3 |
| PARTIAL | 0 |
| MISSING | 0 |
| Gaps found | 0 (1 test-location doc reconciliation: KEY-RISK regression test_proposal_queries.py → test_pipeline.py) |
| Resolved | n/a |
| Escalated | 0 |
| Manual/deferred-to-live | 2 (Plan 01 T3 long-file kill-9; k8s live progress) |
| Run By | orchestrator audit (all per-task commands re-run green; nyquist-auditor not required — no MISSING gaps) |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify (or a checkpoint `<human-check>` proxied by automated tasks)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all references (all files exist + green)
- [x] No watch-mode flags
- [x] Feedback latency < 30s (quick)
- [x] `nyquist_compliant: true` confirmed (all 3 requirements COVERED)

**Approval:** validated 2026-06-30
