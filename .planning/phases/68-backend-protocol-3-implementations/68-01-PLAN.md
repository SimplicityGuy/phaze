---
phase: 68-backend-protocol-3-implementations
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/analyze/core/test_dispatch_snapshot.py
  - tests/analyze/services/test_backends.py
  - tests/integration/test_migrations/test_migration_029_backend_id.py
autonomous: true
requirements: [BACK-01, BACK-02, BACK-03, BACK-04]

must_haves:
  truths:
    - "The D-01 golden side-effect snapshot for {compute,kueue,local}×{agent up,down} passes on CURRENT post-67 code (baseline captured before any refactor)"
    - "The snapshot preserves the GATE-1 asymmetry: compute+down holds ({staged:0}); kueue+down proceeds (GATE-1 skipped)"
    - "test_backends.py and test_migration_029_backend_id.py collect cleanly (guarded skip) while their production targets do not yet exist"
  artifacts:
    - path: "tests/analyze/core/test_dispatch_snapshot.py"
      provides: "D-01 golden characterization matrix (BACK-04 acceptance gate)"
      min_lines: 80
    - path: "tests/analyze/services/test_backends.py"
      provides: "Layer 3 protocol unit scaffold + Layer 2 D-02 invariant"
      min_lines: 60
    - path: "tests/integration/test_migrations/test_migration_029_backend_id.py"
      provides: "Layer 4 migration test scaffold"
      min_lines: 40
  key_links:
    - from: "tests/analyze/core/test_dispatch_snapshot.py"
      to: "src/phaze/tasks/release_awaiting_cloud.py::stage_cloud_window"
      via: "AsyncMock on select_active_agent/_stage_file_to_s3/_enqueue_push_file + ordered call log"
      pattern: "stage_cloud_window"
---

<objective>
Author the three Wave 0 test files named in 68-VALIDATION.md and CAPTURE the D-01 golden
side-effect snapshot on the CURRENT post-67 code — before any refactor touches production.

Decisions implemented: D-01: (golden snapshot vs current code, not a prod trace); D-01a:
(compute-requires-agent vs kueue-skips-GATE-1 asymmetry is a first-class snapshot assertion);
D-02: (equivalence invariant scaffold); D-10: (in-flight status set {UPLOADING,UPLOADED,SUBMITTED,RUNNING}).

Purpose: The characterization snapshot is the phase's acceptance gate (BACK-04). It must be
green on today's code so that the later refactor can assert it is UNCHANGED. The other two files
are guarded scaffolds that light up when their targets land (migration 029 in Wave 1, backends.py
in Wave 2).
Output: three new test files under tests/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-backend-protocol-3-implementations/68-CONTEXT.md
@.planning/phases/68-backend-protocol-3-implementations/68-VALIDATION.md
@.planning/phases/68-backend-protocol-3-implementations/68-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-01 golden snapshot matrix, captured on current code (BACK-04 baseline)</name>
  <files>tests/analyze/core/test_dispatch_snapshot.py</files>
  <read_first>
    - tests/analyze/core/test_staging_cron.py (THE harness + the individual side-effect assertions this snapshot aggregates: push branch ~L149-160, kueue branch ~L290-297, compute-down no-op ~L180-182, kueue-skips-GATE-1 ~L273-297; _StubCfg ~L44-71; _make_ctx/_make_file/_states_for ~L74-103)
    - tests/_queue_fakes.py (DedupFakeQueue/DedupFakeTaskRouter ~L217-280 with .captured (task,payload) pairs + .captured_policy; seed_active_agent ~L331-353)
    - tests/kube_fakes.py (fake_job/fake_workload/fake_local_queue ~L21-73)
    - src/phaze/tasks/release_awaiting_cloud.py (stage_cloud_window L110-193: cloud_enabled gate L127, advisory lock L138, GATE-1 compute fork L145-150, get_cloud_window_count L153, GATE-2 fileserver L166, per-file if/elif fork L179-192, single commit L193)
    - .planning/phases/68-backend-protocol-3-implementations/68-PATTERNS.md (section "tests/analyze/core/test_dispatch_snapshot.py")
    - .planning/phases/68-backend-protocol-3-implementations/68-CONTEXT.md (D-01, D-01a, §specifics L250-258)
  </read_first>
  <action>
    Create the D-01 golden characterization test driving the UNMODIFIED stage_cloud_window over the
    matrix {compute, kueue, local} × {agent up, agent down}. Per cell, record the ordered observable
    side-effect log and compare to an INLINE expected-dict (fixture shape is Claude's Discretion —
    use inline expected-dict per cell). Capture per cell: (a) whether the compute agent gate is checked
    vs skipped — AsyncMock select_active_agent and assert it IS called with kind="compute" for the
    compute cell and is NOT called for the kueue cell (D-01a asymmetry); (b) the staging call made
    (_stage_file_to_s3 for kueue vs _enqueue_push_file for compute); (c) the FileState transition
    (AWAITING_CLOUD → PUSHING); (d) the cloud_job upsert (present for kueue today; absent for compute
    on current code); (e) the enqueue task name (s3_upload vs push_file; dedup no-op counted as
    skipped); (f) the returned tally {"staged":N,"skipped":M}. Configure the per-cell registry via the
    _StubCfg pattern (cloud_enabled + a single-non-local backend of the cell's kind; local cell =
    cloud_enabled False). Matrix truths that MUST hold on current code: compute+down → {"staged":0}
    no-op (GATE-1 holds); kueue+down → proceeds (GATE-1 skipped, D-01a:); local → drain no-ops before
    the window logic (cloud_enabled gate). This test must PASS as written against today's code (baseline
    capture) and is written so that it remains green after the refactor (it asserts stage_cloud_window
    side effects, not internal branch structure). FORWARD-COMPATIBLE MOCKING: also stub the Kueue
    LocalQueue probe boundary (services/kube_staging.get_local_queue) to resolve "available" for the
    kueue cells — this boundary is UNCALLED on current code (harmless) but the post-refactor drain calls
    KueueBackend.is_available which probes it; stubbing it now keeps the snapshot green across the
    refactor without adding a tracked side-effect (the tracked GATE observation is select_active_agent
    calls only, NOT the kube probe). NOTE the ONE tracked expected-value that legitimately changes: the
    compute-cell cloud_job upsert becomes PRESENT after D-03/D-08 land (a deliberate new artifact per
    D-02/D-08, not a regression) — leave a clearly-commented TODO on that single expected field marking
    that Wave 3 (68-04) flips it; every OTHER asserted field must stay byte-identical (that is the
    BACK-04 proof).
  </action>
  <verify>
    <automated>uv run pytest tests/analyze/core/test_dispatch_snapshot.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/analyze/core/test_dispatch_snapshot.py -x` passes on current post-67 code
    - The compute cell asserts select_active_agent(kind="compute") was called; the kueue cell asserts it was NOT called (D-01a asymmetry present as an explicit assertion)
    - compute+down cell asserts {"staged":0}; kueue+down cell asserts staging proceeds
    - Each matrix cell compares against an inline expected-dict (6 cells total)
    - The kueue LocalQueue probe boundary is stubbed (forward-compatible) and the compute cloud_job expected field carries a commented Wave-3-flips-this TODO
  </acceptance_criteria>
  <done>Snapshot matrix green on current code; GATE-1 asymmetry asserted per D-01a; baseline captured; forward-compatible probe stub in place.</done>
</task>

<task type="auto">
  <name>Task 2: Protocol unit + D-02 invariant scaffold (guarded)</name>
  <files>tests/analyze/services/test_backends.py</files>
  <read_first>
    - tests/analyze/core/test_staging_cron.py (harness: _make_ctx/_make_file/_states_for/_StubCfg)
    - tests/_queue_fakes.py (DedupFakeQueue/DedupFakeTaskRouter/seed_active_agent)
    - tests/kube_fakes.py (fake_local_queue for KueueBackend.is_available)
    - src/phaze/services/pipeline.py (get_cloud_window_count L1243-1254 — RHS of the D-02 invariant; COUNT idiom)
    - src/phaze/models/cloud_job.py (CloudJobStatus 6 members L38-46; in-flight set {UPLOADING,UPLOADED,SUBMITTED,RUNNING}, terminal {FAILED,SUCCEEDED})
    - .planning/phases/68-backend-protocol-3-implementations/68-PATTERNS.md (section "tests/analyze/services/test_backends.py")
    - .planning/phases/68-backend-protocol-3-implementations/68-CONTEXT.md (D-02, D-10)
  </read_first>
  <action>
    Create the Layer 3 protocol unit tests (≥12 cells: 3 impls × 4 methods) plus the Layer 2 D-02
    equivalence invariant, targeting the not-yet-existing src/phaze/services/backends.py. Guard the whole
    module with `pytest.importorskip("phaze.services.backends")` at module top so the file COLLECTS
    cleanly in Wave 0 (skips) and lights up automatically in Wave 2 when backends.py appears — do NOT
    hand-manage skip markers. Author these behavioral cells so they are correct-by-construction against
    the design §4.2 contract: LocalBackend.is_available → always True and in_flight_count → 0;
    ComputeAgentBackend.is_available → True only when a compute agent is online via
    select_active_agent(kind="compute") (GATE-1), False (never raises) when absent;
    KueueBackend.is_available → kube/LocalQueue probe with NO compute-agent dependency (D-01a:), returns
    bool never raises; dispatch D-03: atomicity — assert no committed in-flight FileState without a live
    non-terminal cloud_job row (rollback between flip and row-write leaves no limbo row); in_flight_count
    → COUNT(cloud_job WHERE backend_id == self.id AND status IN {UPLOADING,UPLOADED,SUBMITTED,RUNNING})
    per D-10:. Add test_in_flight_equivalence asserting D-02: sum(await b.in_flight_count(session) for b
    in backends) == await get_cloud_window_count(session) over constructed FileState/cloud_job states for
    the single-backend case. Use the resolved-backends stub approach (monkeypatch
    phaze.services.backends.get_settings / resolve_backends) rather than the old _StubCfg selectors.
  </action>
  <verify>
    <automated>uv run pytest tests/analyze/services/test_backends.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/analyze/services/test_backends.py --collect-only -q` succeeds (module skips via importorskip while backends.py is absent)
    - File defines ≥12 protocol cells (3 impls × 4 methods) plus test_in_flight_equivalence
    - The in-flight status set used is exactly {UPLOADING,UPLOADED,SUBMITTED,RUNNING} (D-10)
  </acceptance_criteria>
  <done>Protocol unit + invariant scaffold collects green under importorskip; ready to light up in Wave 2.</done>
</task>

<task type="auto">
  <name>Task 3: Migration 029 test scaffold (guarded)</name>
  <files>tests/integration/test_migrations/test_migration_029_backend_id.py</files>
  <read_first>
    - tests/integration/test_migrations/test_migration_026_kube_columns.py (VERBATIM structure: static revision-id assertions no-DB ~L37-59; grep-assert-no-saq_jobs ~L62-66; integration body ~L82-188; helpers _build_alembic_config/downgrade_to/upgrade_to/MIGRATIONS_TEST_DATABASE_URL/_seed_file ~L26-79)
    - src/phaze/models/cloud_job.py (s3_key L74 nullable=False today → becomes nullable in 029)
    - .planning/phases/68-backend-protocol-3-implementations/68-PATTERNS.md (section "tests/integration/test_migrations/test_migration_029_backend_id.py")
    - .planning/phases/68-backend-protocol-3-implementations/68-CONTEXT.md (D-06, D-08)
  </read_first>
  <action>
    Mirror test_migration_026_kube_columns.py to create the Layer 4 migration test for revision 029.
    Three shapes: (1) static revision-id assertions loaded via importlib.util.spec_from_file_location
    (module name starts with a digit) — assert revision == "029", down_revision == "028",
    branch_labels is None; (2) grep-assert the migration file has no non-comment line containing
    "saq_jobs" (026 banner discipline — migration touches ONLY cloud_job); (3) integration body:
    downgrade_to(cfg,"base") → upgrade_to(cfg,"028") → assert backend_id absent + s3_key NOT NULL →
    upgrade_to(cfg,"029") → assert backend_id column exists + nullable + no backfill AND s3_key now
    nullable (insert a compute-shaped cloud_job row with s3_key = NULL to prove nullability, per D-08:)
    → downgrade_to(cfg,"028") → assert backend_id gone + s3_key NOT NULL again. Guard the static/grep
    tests so they SKIP when alembic/versions/029_add_cloud_job_backend_id.py does not yet exist
    (skipif on the file path) so the file collects green in Wave 0 and lights up in Wave 1; the
    integration body carries the existing DB-required marker from the 026 analog.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_migrations/test_migration_029_backend_id.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/integration/test_migrations/test_migration_029_backend_id.py --collect-only -q` succeeds
    - Static tests skip via a file-exists guard while 029 is absent
    - Test asserts backend_id nullable + no-backfill AND s3_key becomes nullable; grep-asserts no saq_jobs reference
  </acceptance_criteria>
  <done>Migration test scaffold collects green under a file-exists guard; ready to light up in Wave 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only plan — no runtime input surface. Fixtures drive existing control-plane code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Information Disclosure | test fixtures logging secrets | accept | Tests never construct real SecretStr/`*_file` values; snapshot serializes only {id,kind,rank,cap}-level projections and call-arg names, never secret material |
| T-68-SC | Tampering | npm/pip/cargo installs | accept (N/A) | Zero package installs this phase (zero-new-deps lock, 67-CONTEXT); no legitimacy gate required |
</threat_model>

<verification>
- All three files collect under `uv run pytest tests/analyze/core/test_dispatch_snapshot.py tests/analyze/services/test_backends.py tests/integration/test_migrations/test_migration_029_backend_id.py --collect-only -q`
- The snapshot test (Task 1) PASSES on current code
</verification>

<success_criteria>
- Snapshot baseline captured + green on current post-67 code (BACK-04 gate armed)
- Protocol-unit and migration scaffolds collect green under guards, ready to light up in later waves
</success_criteria>

<output>
Create `.planning/phases/68-backend-protocol-3-implementations/68-01-SUMMARY.md` when done
</output>
