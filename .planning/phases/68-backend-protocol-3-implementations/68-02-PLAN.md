---
phase: 68-backend-protocol-3-implementations
plan: 02
type: execute
wave: 1
depends_on: ["68-01"]
files_modified:
  - src/phaze/models/cloud_job.py
  - alembic/versions/029_add_cloud_job_backend_id.py
autonomous: true
requirements: [BACK-02]

must_haves:
  truths:
    - "cloud_job gains a nullable backend_id column via an additive migration 029 that revises 028"
    - "cloud_job.s3_key becomes nullable (compute rows carry no S3 object)"
    - "Migration 029 performs NO backfill and references ONLY cloud_job, never saq_jobs (D-06: nullable, no backfill — no live rows exist)"
  artifacts:
    - path: "alembic/versions/029_add_cloud_job_backend_id.py"
      provides: "additive nullable backend_id + s3_key nullability"
      contains: "backend_id"
    - path: "src/phaze/models/cloud_job.py"
      provides: "backend_id mapped column + s3_key nullable"
      contains: "backend_id"
  key_links:
    - from: "alembic/versions/029_add_cloud_job_backend_id.py"
      to: "cloud_job.backend_id"
      via: "op.add_column nullable=True"
      pattern: "add_column.*backend_id"
---

<objective>
Add the schema substrate for per-backend accounting: a nullable, config-derived cloud_job.backend_id
column and s3_key nullability so a compute row (no S3 object) is valid.

Decisions implemented: D-06: (backend_id nullable, additive migration, NO backfill — no live rows to
migrate; new rows stamp backend_id at dispatch); D-08: (migration 029 also makes s3_key nullable
because compute has no S3 object). Migration head verified: 028 → 029.

Purpose: BACK-02 substrate. This unblocks in_flight_count (Wave 2) and the compute cloud_job row (Wave 3).
Output: model change + migration 029; lights up the Wave 0 migration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-backend-protocol-3-implementations/68-CONTEXT.md
@.planning/phases/68-backend-protocol-3-implementations/68-PATTERNS.md

<interfaces>
From src/phaze/models/cloud_job.py (verified this session):
- CloudJobStatus StrEnum members: UPLOADING, UPLOADED, FAILED, SUBMITTED, RUNNING, SUCCEEDED (L38-46)
- s3_key: Mapped[str] = mapped_column(String(255), nullable=False)  # L74 — becomes nullable
- Optional-column pattern to mirror lives at L79-91 (Phase-54 kube columns)
Migration head: alembic/versions/028_add_analysis_completed_at.py (029 revises 028)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add nullable backend_id + make s3_key nullable on the CloudJob model</name>
  <files>src/phaze/models/cloud_job.py</files>
  <read_first>
    - src/phaze/models/cloud_job.py (s3_key L74 nullable=False; optional-column mirror L79-91; CloudJobStatus L38-46; status CheckConstraint L93-102 — UNCHANGED this phase)
    - .planning/phases/68-backend-protocol-3-implementations/68-PATTERNS.md (section "src/phaze/models/cloud_job.py")
    - .planning/phases/68-backend-protocol-3-implementations/68-CONTEXT.md (D-06, D-08, D-10)
  </read_first>
  <action>
    Add `backend_id: Mapped[str | None] = mapped_column(String(255), nullable=True)` mirroring the
    existing optional kube columns (L79-91). Comment it as config-derived (D-06:) — no default, no
    server_default, no backfill. Change s3_key (L74) from nullable=False to nullable=True (D-08:) —
    compute rows carry no S3 object; update its docstring/comment to note compute rows leave it NULL.
    Do NOT touch the ck_cloud_job_status_enum CheckConstraint (L93-102) — the status set is unchanged
    this phase (backend_id is plain free-text, no enum/CHECK migration needed). Keep the unique FK on
    file_id intact.
  </action>
  <verify>
    <automated>uv run python -c "from phaze.models.cloud_job import CloudJob; c=CloudJob.__table__.c; assert c.backend_id.nullable and c.s3_key.nullable, 'backend_id and s3_key must be nullable'"</automated>
  </verify>
  <acceptance_criteria>
    - CloudJob.__table__.c.backend_id exists and is nullable (String(255))
    - CloudJob.__table__.c.s3_key.nullable is True
    - No change to the ck_cloud_job_status_enum CheckConstraint
    - `uv run mypy src/phaze/models/cloud_job.py` clean
  </acceptance_criteria>
  <done>Model carries nullable backend_id; s3_key nullable; status CHECK untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Additive migration 029 (backend_id nullable + s3_key nullability, no backfill)</name>
  <files>alembic/versions/029_add_cloud_job_backend_id.py</files>
  <read_first>
    - alembic/versions/026_add_cloud_job_kube_columns.py (VERBATIM recipe: header L31-40, upgrade/downgrade L42-66, saq_jobs banner L22-25 — do NOT copy 026's status-CHECK swap dance L54-57)
    - alembic/versions/028_add_analysis_completed_at.py (current head — 029 revises it)
    - tests/integration/test_migrations/test_migration_029_backend_id.py (the Wave 0 test this satisfies)
    - .planning/phases/68-backend-protocol-3-implementations/68-PATTERNS.md (section "alembic/versions/029_add_cloud_job_backend_id.py")
  </read_first>
  <action>
    Create migration 029 mirroring 026's structure. Header: revision = "029", down_revision = "028",
    branch_labels = None, depends_on = None. upgrade(): op.add_column("cloud_job", sa.Column(
    "backend_id", sa.String(255), nullable=True)) then op.alter_column("cloud_job", "s3_key",
    nullable=True) (D-08:). NO CHECK/enum change (backend_id is free-text/config-derived). NO backfill
    (D-06: — no live rows exist). downgrade(): reverse in reverse order — op.alter_column("cloud_job",
    "s3_key", nullable=False) then op.drop_column("cloud_job", "backend_id"). Include the 026-style
    saq_jobs banner comment and touch ONLY the cloud_job table (never saq_jobs — 020 banner). Keep the
    module docstring describing it as additive/nullable/no-backfill.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_migrations/test_migration_029_backend_id.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/integration/test_migrations/test_migration_029_backend_id.py -x` passes (static + grep; integration body via `just integration-test` where DB available)
    - Static assertions pass: revision "029", down_revision "028", branch_labels None
    - Grep-assert: no non-comment line references saq_jobs
    - Migration adds backend_id nullable and alters s3_key to nullable; downgrade reverses both
  </acceptance_criteria>
  <done>Migration 029 exists, additive/nullable/no-backfill, cloud_job-only; Wave 0 migration test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic DDL → cloud_job table | Additive schema change on control-plane DB only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-02 | Tampering | migration accidentally touching saq_jobs (SAQ-owned) | mitigate | Grep-assert test forbids any saq_jobs reference (020 banner); migration references only cloud_job |
| T-68-03 | Denial of Service | s3_key nullability weakening an integrity invariant | accept | s3_key was NOT NULL only for the S3 lifecycle; compute rows legitimately have no object; application code stamps s3_key for kueue rows unchanged |
| T-68-SC | Tampering | npm/pip/cargo installs | accept (N/A) | Zero package installs this phase |
</threat_model>

<verification>
- `uv run pytest tests/integration/test_migrations/test_migration_029_backend_id.py -x` green (static+grep)
- `just integration-test` runs the 028→029→028 round-trip where the test DB is available
- `uv run pytest tests/analyze/ tests/shared/ -x` green (no regression)
</verification>

<success_criteria>
- Nullable backend_id + nullable s3_key present in model and migration 029 (revises 028), no backfill, cloud_job-only
</success_criteria>

<output>
Create `.planning/phases/68-backend-protocol-3-implementations/68-02-SUMMARY.md` when done
</output>
