---
phase: 68-backend-protocol-3-implementations
plan: 05
type: execute
wave: 4
depends_on: ["68-04"]
files_modified:
  - src/phaze/config.py
autonomous: true
requirements: [BACK-01, BACK-04]

must_haves:
  truths:
    - "config.active_cloud_kind and config.active_cap are removed (both dispatch selectors, now unreferenced)"
    - "config.cloud_enabled STAYS (registry on/off gate; BEUI-02 structural foundation)"
    - "config._single_non_local and the three config-VALUE accessors (active_kube/active_bucket/active_compute_scratch_dir) STAY, with docstrings re-tagged from Phase 68 to Phase 70 (MKUE-01)"
    - "Full suite green + migration 029 integration test green (phase acceptance gate)"
  artifacts:
    - path: "src/phaze/config.py"
      provides: "selectors removed; value accessors retained + re-tagged"
      contains: "cloud_enabled"
  key_links:
    - from: "src/phaze/config.py"
      to: "src/phaze/services/backends.py::resolve_backends"
      via: "the >1-non-local raise now lives in resolve_backends (boot); config keeps _single_non_local for value accessors"
      pattern: "_single_non_local"
---

<objective>
Remove the two now-unreferenced Phase-67 transitional dispatch selectors and re-tag the retained
value accessors, then run the full phase acceptance gate.

Decisions implemented: D-07: (remove active_cloud_kind/active_cap; keep cloud_enabled); D-09: (KEEP the
three config-value accessors active_kube/active_bucket/active_compute_scratch_dir + _single_non_local —
the single-cluster verbatim bodies read them until Phase 70; re-point their stale "removed in Phase 68"
docstrings to "Phase 70 (MKUE-01)").

Purpose: BACK-01 completion (the transitional shim is fully gone) + BACK-04 final gate. This is the LAST
wave: the selectors are deleted only now, AFTER 68-04 rewired every reader, so there is never a
mid-wave import break.
Output: cleaned config.py + a green full suite + migration integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-backend-protocol-3-implementations/68-CONTEXT.md
@.planning/phases/68-backend-protocol-3-implementations/68-PATTERNS.md

<interfaces>
From src/phaze/config.py (verified this session):
- cloud_enabled: L454 (KEEP)
- _single_non_local: L463-478 (KEEP — the retained value accessors call it; its raise is defense-in-depth alongside the resolve_backends boot guard)
- active_cloud_kind: L481 (REMOVE — dispatch selector)
- active_cap: L489 (REMOVE — dispatch selector)
- active_compute_scratch_dir: L495 (KEEP + re-tag to Phase 70)
- active_kube: L501 (KEEP + re-tag to Phase 70)
- active_bucket: L507 (KEEP + re-tag to Phase 70)
- log_effective_registry: L525 (KEEP — logs only {id,kind,rank,cap})
- stale docstrings tagging all five accessors "removed in Phase 68" also at comment blocks ~L577/606/646/822
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove active_cloud_kind/active_cap; keep + re-tag the value accessors</name>
  <files>src/phaze/config.py</files>
  <read_first>
    - src/phaze/config.py (cloud_enabled L454; _single_non_local L463-478; active_cloud_kind L481; active_cap L489; active_compute_scratch_dir L495; active_kube L501; active_bucket L507; log_effective_registry L525; stale "removed in Phase 68" comment blocks ~L577/606/646/822)
    - src/phaze/services/backends.py (resolve_backends boot guard — the relocated >1-non-local raise)
    - .planning/phases/68-backend-protocol-3-implementations/68-PATTERNS.md (section "src/phaze/config.py (MOD — accessor removal + retag, D-07/D-09)")
    - .planning/phases/68-backend-protocol-3-implementations/68-CONTEXT.md (D-07, D-09)
  </read_first>
  <action>
    Delete the active_cloud_kind property (L481-486) and active_cap property (L488-492) — the two
    dispatch selectors, now unreferenced after 68-04 (D-07:). KEEP cloud_enabled (L454), _single_non_local
    (L463-478 — the three retained value accessors call it; leave its raise-on->1 intact as defense-in-depth
    alongside the resolve_backends boot guard), active_compute_scratch_dir (L495), active_kube (L501),
    active_bucket (L507), and log_effective_registry (L525). Re-tag the docstrings of the three RETAINED
    value accessors from "removed in Phase 68 (BACK-01)" to "retained through Phase 70 (MKUE-01) — feeds
    the single-cluster kube_staging/s3_staging/agent_push reads" (D-09:). Also fix the stale
    "removed in Phase 68" mentions in the surrounding comment blocks (~L577/606/646/822) so they point to
    Phase 70 for the three retained accessors and note the two selectors were removed in Phase 68. Do NOT
    add the boot guard here (it lives in backends.py); do NOT change log_effective_registry's projection.
  </action>
  <verify>
    <automated>uv run bash -c "grep -rn 'active_cloud_kind\|active_cap' src/phaze/ && echo FOUND && exit 1 || echo CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'active_cloud_kind\|active_cap' src/phaze/` returns no matches (both selectors fully removed + unreferenced)
    - cloud_enabled, _single_non_local, active_kube, active_bucket, active_compute_scratch_dir, log_effective_registry all still present
    - The three retained accessors' docstrings reference Phase 70 (MKUE-01), not Phase 68
    - `uv run mypy src/phaze/config.py` clean
  </acceptance_criteria>
  <done>Selectors removed; value accessors retained + re-tagged to Phase 70; cloud_enabled kept.</done>
</task>

<task type="auto">
  <name>Task 2: Full phase acceptance gate (suite + migration integration + grep audit)</name>
  <files>src/phaze/config.py</files>
  <read_first>
    - .planning/phases/68-backend-protocol-3-implementations/68-VALIDATION.md (sampling: full suite + just integration-test before verify)
    - justfile (test / test-cov / integration-test / test-db recipes)
  </read_first>
  <action>
    Run the phase acceptance gate. Full suite: `uv run pytest` (on a colima DB flake, re-run only the
    failed subset in isolation — do NOT set PHAZE_QUEUE_URL=redis). Migration integration: `just test-db`
    then `just integration-test` to exercise the 028→029→028 round-trip (backend_id nullable + s3_key
    nullability). Confirm the BACK-04 gate: `uv run pytest tests/analyze/core/test_dispatch_snapshot.py -x`
    green (snapshot unchanged modulo the one deliberate compute field). Confirm the D-02 invariant:
    `uv run pytest tests/analyze/services/test_backends.py::test_in_flight_equivalence -x`. Final audit
    grep: no active_cloud_kind/active_cap anywhere in src/phaze/. If any full-suite failure is a genuine
    regression (not colima flake), fix it within this plan's file scope or flag it. No production code
    change beyond Task 1 is expected — this task is the green-gate confirmation.
  </action>
  <verify>
    <automated>uv run pytest && uv run pytest tests/analyze/core/test_dispatch_snapshot.py tests/analyze/services/test_backends.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest` full suite green (colima flakes re-run in isolation and pass)
    - `just integration-test` migration round-trip green (migration 029)
    - BACK-04 snapshot + D-02 invariant tests green
    - Final grep audit: zero active_cloud_kind/active_cap references in src/phaze/
    - pre-commit hooks pass (ruff/mypy/bandit etc.) — no --no-verify
  </acceptance_criteria>
  <done>Full suite + migration integration green; BACK-01..04 proven; phase acceptance gate passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config resolution → startup logging | log_effective_registry projects the registry to logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-12 | Information Disclosure | log_effective_registry leaking SecretStr / *_file paths | mitigate | Projection unchanged — logs only {id,kind,rank,cap} + cloud_enabled; never secret material (config.py L525-533) |
| T-68-13 | Denial of Service | removing the >1-non-local raise leaving no fail-fast guard | mitigate | Guard relocated to resolve_backends (boot, 68-03); _single_non_local's raise kept in config as defense-in-depth |
| T-68-SC | Tampering | npm/pip/cargo installs | accept (N/A) | Zero package installs this phase |
</threat_model>

<verification>
- `uv run pytest` full suite green
- `just integration-test` migration 029 round-trip green
- BACK-04 snapshot + D-02 invariant green
- grep audit clean (no active_cloud_kind/active_cap in src/phaze/)
</verification>

<success_criteria>
- Transitional selectors fully removed; value accessors retained + re-tagged; full suite + migration integration green; BACK-01..04 satisfied and proven behavior-preserving
</success_criteria>

<output>
Create `.planning/phases/68-backend-protocol-3-implementations/68-05-SUMMARY.md` when done
</output>
