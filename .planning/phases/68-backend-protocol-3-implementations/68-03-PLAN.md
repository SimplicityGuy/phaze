---
phase: 68-backend-protocol-3-implementations
plan: 03
type: execute
wave: 2
depends_on: ["68-01", "68-02"]
files_modified:
  - src/phaze/services/backends.py
  - tests/analyze/services/test_backends.py
autonomous: true
requirements: [BACK-01, BACK-03]

must_haves:
  truths:
    - "A single Backend Protocol (is_available/in_flight_count/dispatch/reconcile) exists with LocalBackend/ComputeAgentBackend/KueueBackend implementations re-homing existing bodies (not rewritten)"
    - "in_flight_count(session) returns COUNT(cloud_job WHERE backend_id AND status IN {UPLOADING,UPLOADED,SUBMITTED,RUNNING})"
    - "ComputeAgentBackend.is_available checks a live compute agent (GATE-1); KueueBackend.is_available probes the cluster with NO compute-agent dependency; both return bool and never raise"
    - "resolve_backends(settings) builds one impl per registry entry and raises fast at boot if >1 non-local backend resolves"
    - "sum(in_flight_count(b)) == get_cloud_window_count() holds for the single-backend case (D-02 invariant)"
  artifacts:
    - path: "src/phaze/services/backends.py"
      provides: "Backend Protocol + 3 impls + resolve_backends() boot guard + resolved_non_local_kind() helper"
      min_lines: 120
  key_links:
    - from: "src/phaze/services/backends.py::in_flight_count"
      to: "cloud_job.backend_id + status"
      via: "select(func.count) WHERE backend_id AND status IN in-flight"
      pattern: "func.count.*CloudJob"
    - from: "src/phaze/services/backends.py::ComputeAgentBackend.dispatch"
      to: "cloud_job upsert + FileState.PUSHING"
      via: "pg_insert(CloudJob).on_conflict_do_update in caller session, before/with the flip"
      pattern: "on_conflict_do_update"
---

<objective>
Create the phase's center of gravity: src/phaze/services/backends.py housing the Backend Protocol,
the three implementations (re-homing existing dispatch bodies verbatim), the uniform per-backend
in_flight_count, the resolve_backends() boot guard, and a resolved_non_local_kind() helper for the
Wave 3 reader rewires.

Decisions implemented: D-01a: (GATE-1 asymmetry lives in per-kind is_available); D-02: (in_flight_count
substrate + equivalence invariant proven); D-03: (dispatch writes cloud_job in-txn before/with the
PUSHING flip, never a separate commit); D-05: (KueueBackend calls single-cluster kube_staging/s3_staging
VERBATIM — token hack stays, no per-cluster parameterization); D-07: (raise-on->1-non-local relocated
to resolve_backends boot guard; cloud_enabled stays in config); D-10: (in-flight set
{UPLOADING,UPLOADED,SUBMITTED,RUNNING}).

Purpose: BACK-01 (protocol replaces the if/elif) + BACK-03 (uniform in_flight_count). This is a
RE-HOME, not a rewrite — every body already exists as an isolated async function.
Output: services/backends.py; lights up the Wave 0 protocol/invariant tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-backend-protocol-3-implementations/68-CONTEXT.md
@.planning/phases/68-backend-protocol-3-implementations/68-RESEARCH.md
@.planning/phases/68-backend-protocol-3-implementations/68-PATTERNS.md

<interfaces>
Design §4.2 protocol shape (planner-finalized signatures):
- class Backend(Protocol): id: str; rank: int; cap: int
  - async def is_available(self, session, ...) -> bool
  - async def in_flight_count(self, session) -> int
  - async def dispatch(self, file, session, ...) -> None   # writes cloud_job IN-TXN before/with the flip (D-03)
  - async def reconcile(self, ctx, ...) -> None

Bodies to re-home (verified this session):
- Compute enqueue leg: release_awaiting_cloud.py _enqueue_push_file L82-107 + else-branch L188-192
- Compute GATE-1: release_awaiting_cloud.py L145-150 (select_active_agent kind="compute", NoActiveAgentError)
- Kueue dispatch: services/cloud_staging.py _stage_file_to_s3 L71-148 (no-commit core; upserts cloud_job UPLOADING + enqueues s3_upload)
- Kueue probe: services/kube_staging.py get_local_queue ~L250-266 (active_kube L84; token hack — leave as-is, D-05)
- Kueue reconcile: tasks/reconcile_cloud_jobs.py reconcile_cloud_jobs body L282-322 (iterate {SUBMITTED,RUNNING})
- cloud_job upsert idiom: services/cloud_staging.py L104-125 (PK stamped OUT of set_)
- COUNT idiom: services/pipeline.py get_cloud_window_count L1243-1254
- Config submodels: config_backends.py L67-133 (Local/Compute/Kueue discriminated union; scratch_dir L89; kube/buckets L116-117)
- Boot guard source: config.py _single_non_local L463-478; cloud_enabled L454 (STAYS)
CloudJobStatus: UPLOADING/UPLOADED/FAILED/SUBMITTED/RUNNING/SUCCEEDED. In-flight={UPLOADING,UPLOADED,SUBMITTED,RUNNING}.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Protocol + shared in_flight_count + LocalBackend + resolve_backends + kind helper</name>
  <files>src/phaze/services/backends.py</files>
  <behavior>
    - Backend is a typing.Protocol with id/rank/cap and the four async methods (design §4.2)
    - in_flight_count(session) returns COUNT(cloud_job WHERE backend_id==self.id AND status IN {UPLOADING,UPLOADED,SUBMITTED,RUNNING}); 0 when none
    - LocalBackend.is_available → always True; in_flight_count → 0; reconcile → no-op
    - resolve_backends(settings) → list[Backend], one impl per registry entry; raises fast at boot naming offending ids if >1 non-local resolves
    - resolved_non_local_kind(settings) → "local" when not cloud_enabled, else the single non-local kind ("compute"|"kueue")
    - D-02 invariant: sum(in_flight_count(b)) == get_cloud_window_count() for the single-backend case
  </behavior>
  <read_first>
    - src/phaze/services/cloud_staging.py (imports pattern L22-49; upsert idiom L104-125; no-commit core rationale L71-77 Landmine L1)
    - src/phaze/services/pipeline.py (get_cloud_window_count L1243-1254 — COUNT idiom)
    - src/phaze/config.py (_single_non_local L463-478; cloud_enabled L454; retained accessors active_kube L501/active_bucket L507/active_compute_scratch_dir L495)
    - src/phaze/config_backends.py (Local/Compute/Kueue submodels L67-133)
    - src/phaze/models/cloud_job.py (CloudJobStatus L38-46)
    - tests/analyze/services/test_backends.py (the Wave 0 contract this satisfies)
    - .planning/phases/68-backend-protocol-3-implementations/68-PATTERNS.md (section "src/phaze/services/backends.py"; "in_flight_count"; "resolve_backends")
  </read_first>
  <action>
    Create src/phaze/services/backends.py. Copy the stateless-service imports pattern from
    cloud_staging.py L22-49 (from __future__ import annotations; uuid; TYPE_CHECKING; pg_insert;
    structlog; get_settings; CloudJob/CloudJobStatus; select_active_agent; TYPE_CHECKING-guarded
    AsyncSession/FileRecord/AgentTaskRouter). Define the Backend Protocol (typing.Protocol, structural)
    with id/rank/cap attributes and the four async method signatures from the interfaces block. Define a
    module-level IN_FLIGHT tuple = (CloudJobStatus.UPLOADING, CloudJobStatus.UPLOADED,
    CloudJobStatus.SUBMITTED, CloudJobStatus.RUNNING) (D-10:) and a shared async in_flight_count(self,
    session) using the pipeline.py COUNT idiom filtered by backend_id==self.id AND status IN
    [s.value for s in IN_FLIGHT], returning scalar or 0. Implement LocalBackend (bound to the local
    submodel): is_available → always True; in_flight_count → 0; dispatch → re-home the existing
    process_file local enqueue path (used by the Phase-69 scheduler, unit-tested here — NOT wired into
    the single-path drain this phase); reconcile → no-op. Implement resolve_backends(settings) ->
    list[Backend] building one impl per registry entry (Research Pattern 1: per-registry-entry, each
    holding its bound submodel); relocate the raise-on->1-non-local guard here (D-07:) preserving the
    exact message style that names the offending backend ids (mirror config.py L463-478). Add
    resolved_non_local_kind(settings) returning "local" when not settings.cloud_enabled else the single
    non-local backend kind — this is the registry-derived replacement the Wave 3 readers import. Never
    let is_available/dispatch/reconcile raise out to a cron caller (cron no-op discipline).
  </action>
  <verify>
    <automated>uv run pytest "tests/analyze/services/test_backends.py::test_in_flight_equivalence" -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/analyze/services/test_backends.py::test_in_flight_equivalence -x` passes (D-02 invariant)
    - LocalBackend.is_available cell + LocalBackend.in_flight_count==0 cell pass
    - resolve_backends raises at boot when >1 non-local backend resolves (test asserts the message names offending ids)
    - `uv run mypy src/phaze/services/backends.py` clean
  </acceptance_criteria>
  <done>Protocol, shared in_flight_count, LocalBackend, resolve_backends boot guard, resolved_non_local_kind helper exist; D-02 invariant green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ComputeAgentBackend + KueueBackend (re-home dispatch/is_available/reconcile)</name>
  <files>src/phaze/services/backends.py</files>
  <behavior>
    - ComputeAgentBackend.is_available → True iff select_active_agent(kind="compute") succeeds (GATE-1); False on NoActiveAgentError, never raises
    - ComputeAgentBackend.dispatch → flips FileState.PUSHING AND upserts cloud_job(backend_id, s3_key=None, status=SUBMITTED) in the SAME caller session BEFORE/WITH the flip, then re-homes the _enqueue_push_file leg; NEVER commits
    - KueueBackend.dispatch → calls _stage_file_to_s3 verbatim (single-cluster) in the caller session
    - KueueBackend.is_available → kube/LocalQueue probe, NO compute-agent dependency, returns bool never raises
    - KueueBackend.reconcile → re-homes reconcile_cloud_jobs body, backend_id-aware (row filter includes backend_id), NO advisory lock
    - D-03 atomicity: rollback between flip and row-write leaves no limbo row
  </behavior>
  <read_first>
    - src/phaze/tasks/release_awaiting_cloud.py (_enqueue_push_file L82-107; GATE-1 L145-150; else-branch enqueue L188-192; FileState.PUSHING flip L179)
    - src/phaze/services/cloud_staging.py (_stage_file_to_s3 L71-148 no-commit core; upsert idiom L104-125)
    - src/phaze/services/kube_staging.py (get_local_queue ~L250-266; active_kube L84; token hack L92-111 — leave verbatim, D-05)
    - src/phaze/tasks/reconcile_cloud_jobs.py (reconcile body L282-322; per-row rollback guard L315-319; {SUBMITTED,RUNNING} iteration)
    - src/phaze/tasks/controller.py (non-fatal kube catch pattern L178-185)
    - src/phaze/services/enqueue_router.py (select_active_agent, NoActiveAgentError)
    - .planning/phases/68-backend-protocol-3-implementations/68-PATTERNS.md (ComputeAgentBackend.dispatch / KueueBackend.dispatch / is_available / reconcile sections)
    - .planning/phases/68-backend-protocol-3-implementations/68-CONTEXT.md (D-01a, D-03, D-05, D-10)
  </read_first>
  <action>
    Add ComputeAgentBackend and KueueBackend to backends.py, each bound to its Phase-67 submodel.
    ComputeAgentBackend.is_available: re-home GATE-1 from release_awaiting_cloud.py L145-150 — try
    select_active_agent(session, kind="compute"); return True; except NoActiveAgentError return False
    (degrade to hold, never raise). ComputeAgentBackend.dispatch(file, session, ...): per D-03:, set
    file.state = FileState.PUSHING then pg_insert(CloudJob).values(id=uuid.uuid4(), file_id=file.id,
    backend_id=self.id, s3_key=None, status=CloudJobStatus.SUBMITTED.value).on_conflict_do_update(
    index_elements=["file_id"], set_={backend_id, status}) executed on the SAME caller session (id OUT
    of set_ — PK immutable), then re-home the _enqueue_push_file leg (move the helper from
    release_awaiting_cloud.py into backends.py as a module-level helper; Wave 3 deletes the now-dead
    original). NEVER commit inside dispatch (drain owns the single post-loop commit; a mid-body commit
    releases pg_advisory_xact_lock — Landmine L1). KueueBackend.dispatch: call the no-commit core
    _stage_file_to_s3 verbatim (cloud_staging.py L71-148) exactly as the current kueue branch does at
    release_awaiting_cloud.py L180-186 — it already flips state + upserts cloud_job(UPLOADING) + enqueues
    s3_upload in the caller session; D-05: keep single-cluster (reads active_kube/active_bucket). Do NOT
    write a second cloud_job row for kueue. KueueBackend.is_available: re-home the get_local_queue probe
    (kube_staging.py) wrapped so kr8s NotFoundError/ServerError returns False rather than raising (mirror
    controller.py L178-185); NO compute-agent dependency (D-01a:). KueueBackend.reconcile: re-home
    reconcile_cloud_jobs.py L282-322, adding backend_id to the {SUBMITTED,RUNNING} row filter so
    reconcile is backend_id-aware; keep the per-row session.rollback() guard; do NOT add an advisory
    lock (Pitfall 2 — deferred to Phase 69). GATE-2 (fileserver online) stays a scheduler-level
    precondition in the drain — do NOT fold it into is_available.
  </action>
  <verify>
    <automated>uv run pytest tests/analyze/services/test_backends.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/analyze/services/test_backends.py -x` passes (all ≥12 protocol cells + invariant)
    - ComputeAgentBackend.is_available True with compute agent seeded, False (no raise) without
    - KueueBackend.is_available does NOT call select_active_agent(kind="compute") and returns bool on a kube probe failure (never raises) — D-01a asserted
    - ComputeAgentBackend.dispatch writes a cloud_job row (backend_id set, s3_key NULL, status SUBMITTED) and flips PUSHING with NO commit inside dispatch; rollback leaves no limbo row (D-03)
    - KueueBackend.reconcile filters cloud_job rows by backend_id and takes no advisory lock
    - `uv run mypy src/phaze/services/backends.py` clean
  </acceptance_criteria>
  <done>All three impls present; protocol unit suite green; GATE-1 asymmetry + D-03 atomicity proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| control plane → compute agent | select_active_agent heartbeat gate (GATE-1); read-only |
| control plane → Kueue cluster | kube/LocalQueue probe (is_available) + kr8s submit/reconcile; creds control-side only (DIST-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-04 | Information Disclosure | backend structlog logs leaking kube SA token / S3 creds | mitigate | backends.py logs only {id,kind,rank,cap}-level fields; never SecretStr/`*_file` values; token hack read but never logged (D-05) |
| T-68-05 | Denial of Service | is_available/dispatch/reconcile raising out of the cron | mitigate | Per-kind bodies degrade to a hold (return False / clean no-op), never raise — preserves cron no-op discipline |
| T-68-06 | Elevation of Privilege | DIST-01 media-plane boundary | mitigate | Unchanged — control plane stays sole S3 importer/presigner; pods/agents credential-free; only presigned file_id-scoped URLs |
| T-68-07 | Tampering | dispatch committing mid-body, releasing the advisory lock | mitigate | dispatch never commits; drain owns the single post-loop commit (Landmine L1); unit test asserts no in-body commit |
| T-68-SC | Tampering | npm/pip/cargo installs | accept (N/A) | Zero package installs this phase |
</threat_model>

<verification>
- `uv run pytest tests/analyze/services/test_backends.py -x` green (Layer 2 + Layer 3)
- `uv run pytest tests/analyze/ tests/shared/ -x` green (no regression)
- `uv run mypy src/phaze/services/backends.py` clean
</verification>

<success_criteria>
- Backend Protocol + 3 impls re-homed (not rewritten); uniform in_flight_count; boot guard; kind helper; GATE-1 asymmetry + D-03 atomicity proven
</success_criteria>

<output>
Create `.planning/phases/68-backend-protocol-3-implementations/68-03-SUMMARY.md` when done
</output>
