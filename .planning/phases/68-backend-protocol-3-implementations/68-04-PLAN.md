---
phase: 68-backend-protocol-3-implementations
plan: 04
type: execute
wave: 3
depends_on: ["68-03"]
files_modified:
  - src/phaze/tasks/release_awaiting_cloud.py
  - src/phaze/routers/agent_push.py
  - src/phaze/routers/pipeline.py
  - src/phaze/routers/agent_s3.py
  - src/phaze/tasks/controller.py
  - tests/analyze/core/test_dispatch_snapshot.py
autonomous: true
requirements: [BACK-01, BACK-03, BACK-04]

must_haves:
  truths:
    - "stage_cloud_window's per-kind if/elif fork is replaced by backend.dispatch(); GATE-1 is backend.is_available(); active_cap is backend.cap"
    - "The drain keeps its skeleton verbatim: advisory lock, cloud_enabled gate, get_cloud_window_count for slot math (D-02a), GATE-2 fileserver precondition, single post-loop commit"
    - "report_pushed terminalizes compute's cloud_job row (→SUCCEEDED) in the SAME txn as PUSHING→PUSHED, rowcount-guarded, keeping the D-02 invariant true live (D-08)"
    - "Every active_cloud_kind reader (pipeline.py:575/810, agent_s3.py:113, controller.py:179) resolves through resolved_non_local_kind() with no behavior change (Layer 5)"
    - "The D-01 golden snapshot is UNCHANGED post-refactor except the single deliberate compute cloud_job field (BACK-04 gate)"
  artifacts:
    - path: "src/phaze/tasks/release_awaiting_cloud.py"
      provides: "drain calling backend.dispatch()/is_available()/cap"
      contains: "backend.dispatch"
    - path: "src/phaze/routers/agent_push.py"
      provides: "compute cloud_job terminalization in report_pushed"
      contains: "CloudJobStatus.SUCCEEDED"
  key_links:
    - from: "src/phaze/tasks/release_awaiting_cloud.py"
      to: "src/phaze/services/backends.py::dispatch"
      via: "backend.dispatch(file, session, ...) in the drain loop, caller-owned session + single post-loop commit"
      pattern: "backend\\.dispatch"
    - from: "src/phaze/routers/agent_push.py::report_pushed"
      to: "cloud_job terminal write"
      via: "update(CloudJob).where(file_id).values(status=SUCCEEDED) inside the PUSHING→PUSHED txn"
      pattern: "CloudJob"
---

<objective>
Rewire the live seams onto the Backend protocol: replace the drain's if/elif fork with
backend.dispatch(), terminalize compute's new cloud_job row in report_pushed, and rewire the
remaining active_cloud_kind readers to the registry-derived helper — then prove the D-01 golden
snapshot is unchanged (BACK-04 acceptance gate).

Decisions implemented: D-01a: (GATE-1 asymmetry preserved via per-kind is_available); D-02a: (drain
KEEPS get_cloud_window_count for slot math; nothing consults per-backend count for cap yet); D-03:
(compute cloud_job written in-txn before/with the flip — already in ComputeAgentBackend.dispatch);
D-07: (fork removed; single-non-local invariant + cron no-op discipline preserved); D-08: (report_pushed
terminalizes the compute cloud_job row so the D-02 invariant holds LIVE); D-09: (rewire the 8
active_cloud_kind readers across 6 modules to registry-derived kind — NOT a bare property deletion).

Purpose: BACK-01 (fork gone), BACK-03 (uniform in_flight live via terminalization), BACK-04 (snapshot
proof). This is the rewire wave — config.py accessor DELETION is deferred to 68-05 so readers change
before the accessor disappears (no mid-wave import break).
Output: rewired drain + report_pushed + 4 readers; snapshot green post-refactor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-backend-protocol-3-implementations/68-CONTEXT.md
@.planning/phases/68-backend-protocol-3-implementations/68-RESEARCH.md
@.planning/phases/68-backend-protocol-3-implementations/68-PATTERNS.md

<interfaces>
From src/phaze/services/backends.py (Wave 2):
- resolve_backends(settings) -> list[Backend]  (boot guard: raises on >1 non-local)
- resolved_non_local_kind(settings) -> "local" | "compute" | "kueue"
- Backend.is_available(session) -> bool ; Backend.cap: int ; Backend.dispatch(file, session, ...) -> None
Drain skeleton to KEEP verbatim (release_awaiting_cloud.py):
- cloud_enabled gate L127; advisory lock L138 (pg_advisory_xact_lock 5_000_504); get_cloud_window_count L153 (D-02a KEEP);
  GATE-2 fileserver select_active_agent(kind="fileserver") L166; FileState.PUSHING flip L179; single commit L193
Readers to rewire (D-09):
- routers/pipeline.py:575 (dashboard cloud_lane_kind) ; :810 (ledger-seed fork == "kueue")
- routers/agent_s3.py:113 (defensive guard) ; tasks/controller.py:179 (LocalQueue-probe gate; keep try/except L178-185)
report_pushed (routers/agent_push.py): PUSHING→PUSHED UPDATE L103-108 (rowcount guard L109); commit L131; active_compute_scratch_dir read L122 (STAYS — D-09)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire stage_cloud_window fork to backend.dispatch()/is_available()/cap</name>
  <files>src/phaze/tasks/release_awaiting_cloud.py, tests/analyze/core/test_dispatch_snapshot.py</files>
  <behavior>
    - The drain resolves the single non-local backend via resolve_backends() and dispatches through backend.dispatch()
    - GATE-1 becomes `if not await backend.is_available(session): return {"staged":0}` (compute checks agent; kueue probes cluster, no compute dep; local always True)
    - active_cap → backend.cap; per-file if/elif fork (L179-192) → await backend.dispatch(file, session, ...)
    - Skeleton unchanged: advisory lock, cloud_enabled gate, get_cloud_window_count for slot math (D-02a), GATE-2 fileserver, single post-loop commit
    - The dead _enqueue_push_file helper (re-homed into backends.py in Wave 2) is removed from this module
    - The D-01 snapshot passes with ONLY the compute-cell cloud_job expected field flipped to present
  </behavior>
  <read_first>
    - src/phaze/tasks/release_awaiting_cloud.py (WHOLE stage_cloud_window L110-193; _enqueue_push_file L82-107 — now dead, delete; GATE-1 L145-150; if/elif L179-192; active_cap L131; active_cloud_kind L145,180)
    - src/phaze/services/backends.py (resolve_backends, resolved_non_local_kind, Backend.dispatch/is_available/cap)
    - tests/analyze/core/test_dispatch_snapshot.py (the BACK-04 gate + its commented Wave-3 TODO on the compute cloud_job field)
    - .planning/phases/68-backend-protocol-3-implementations/68-PATTERNS.md (section "src/phaze/tasks/release_awaiting_cloud.py (MOD)")
    - .planning/phases/68-backend-protocol-3-implementations/68-CONTEXT.md (D-02a, D-03, D-07)
  </read_first>
  <action>
    In stage_cloud_window: keep the skeleton exactly (cloud_enabled gate L127; advisory lock L138;
    get_cloud_window_count L153 for slot math per D-02a: — DO NOT switch slot math to per-backend
    in_flight_count, that flip is Phase 69/SCHED-02; GATE-2 fileserver L166; FileState.PUSHING flip
    L179; the SINGLE post-loop session.commit() L193). Resolve the active backend via
    resolve_backends(cfg) (single non-local backend + local; the boot guard already enforces ≤1
    non-local). Replace active_cap (L131) with backend.cap. Replace the compute-only GATE-1 fork
    (L145-150) with a uniform pre-loop gate `if not await backend.is_available(session): return the
    clean no-op tally` — ComputeAgentBackend.is_available preserves GATE-1 (compute agent), while
    KueueBackend.is_available probes the cluster with NO compute dependency (D-01a:), so the asymmetry
    is preserved. Replace the per-file `if active_cloud_kind == "kueue" … else …` fork (L179-192) with
    `await backend.dispatch(file, session, task_router/push_queue as the body needs)` — dispatch owns
    the flip + row write in the caller session (D-03:), and this drain's single post-loop commit stays
    the atomic boundary (never commit inside the loop — Landmine L1). Delete the now-dead
    _enqueue_push_file helper (L82-107) from this module (it was re-homed into backends.py in Wave 2).
    Preserve cron no-op discipline: is_available/dispatch failures degrade to a hold, never raise.
    Then update tests/analyze/core/test_dispatch_snapshot.py: flip ONLY the compute-cell cloud_job
    expected field from absent→present (the deliberate D-03/D-08 new artifact) and remove that field's
    TODO comment; every OTHER asserted snapshot field must remain byte-identical — if any other field
    diverges, that is a regression to fix, not a snapshot to edit.
  </action>
  <verify>
    <automated>uv run pytest tests/analyze/core/test_dispatch_snapshot.py tests/analyze/core/test_staging_cron.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/analyze/core/test_dispatch_snapshot.py tests/analyze/core/test_staging_cron.py -x` passes
    - stage_cloud_window contains `backend.dispatch(` and `backend.cap` and no `active_cloud_kind`/`active_cap` reference (grep-clean in this file)
    - get_cloud_window_count is still the slot-math source (D-02a preserved); the advisory lock + single post-loop commit are unchanged
    - Only the compute-cell cloud_job snapshot field changed; all other asserted fields byte-identical
    - _enqueue_push_file no longer defined in release_awaiting_cloud.py
    - `uv run mypy src/phaze/tasks/release_awaiting_cloud.py` clean
  </acceptance_criteria>
  <done>Drain dispatches through the protocol; skeleton + slot math preserved; BACK-04 snapshot green with the one deliberate compute-row change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Terminalize compute's cloud_job row in report_pushed (D-08)</name>
  <files>src/phaze/routers/agent_push.py</files>
  <behavior>
    - After the guarded PUSHING→PUSHED UPDATE succeeds (rowcount != 0), report_pushed writes the compute cloud_job row to SUCCEEDED in the SAME transaction, before commit
    - When the PUSHING→PUSHED UPDATE is a no-op (rowcount == 0, idempotent late callback), NO cloud_job write occurs
    - active_compute_scratch_dir read at L122 is unchanged (D-09 retained accessor); put_analysis result return untouched (§5)
    - sum(in_flight_count) == get_cloud_window_count holds LIVE after a compute push completes
  </behavior>
  <read_first>
    - src/phaze/routers/agent_push.py (report_pushed L62-139: PUSHING→PUSHED UPDATE L103-108, rowcount guard L109-111, commit L131, active_compute_scratch_dir L122; the WR-02 idempotency guard)
    - src/phaze/services/cloud_staging.py (upsert/update idiom L104-125)
    - src/phaze/models/cloud_job.py (CloudJobStatus.SUCCEEDED; backend_id/file_id)
    - .planning/phases/68-backend-protocol-3-implementations/68-PATTERNS.md (section "src/phaze/routers/agent_push.py (MOD — report_pushed, D-08)")
    - .planning/phases/68-backend-protocol-3-implementations/68-CONTEXT.md (D-08)
  </read_first>
  <action>
    In report_pushed, after the guarded PUSHING→PUSHED UPDATE (L103-108) and ONLY when res.rowcount != 0
    (reuse the existing WR-02 guard so a duplicate/late callback is a clean idempotent no-op with no
    cloud_job write), add a terminal write of the compute cloud_job row in the SAME transaction, before
    session.commit() (L131): update(CloudJob).where(CloudJob.file_id == file_id).values(
    status=CloudJobStatus.SUCCEEDED.value). This is D-08: — compute's reconcile per §4.2 is the existing
    /pushed callback path; the row is now terminalized so in_flight_count(compute) drains and the D-02
    invariant holds live. Do NOT touch put_analysis or the analysis result return (§5 boundary); the
    active_compute_scratch_dir read at L122 STAYS (D-09 retained accessor — only its docstring tag
    re-points to Phase 70, handled in 68-05). Add the CloudJob/CloudJobStatus imports if absent.
  </action>
  <verify>
    <automated>uv run pytest tests/analyze/ tests/shared/ -x -k "push or report_pushed or agent_push"</automated>
  </verify>
  <acceptance_criteria>
    - report_pushed writes CloudJobStatus.SUCCEEDED for the file's cloud_job only when the PUSHING→PUSHED UPDATE affected a row (rowcount != 0), inside the same committed txn
    - The idempotent-no-op path (rowcount == 0) performs NO cloud_job write
    - put_analysis / analysis result return unchanged
    - `uv run pytest tests/analyze/ tests/shared/ -x -k "push or report_pushed or agent_push"` passes
    - `uv run mypy src/phaze/routers/agent_push.py` clean
  </acceptance_criteria>
  <done>Compute cloud_job terminalized in the /pushed callback; D-02 invariant holds live; §5 boundary intact.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire active_cloud_kind readers to resolved_non_local_kind() (D-09, Layer 5)</name>
  <files>src/phaze/routers/pipeline.py, src/phaze/routers/agent_s3.py, src/phaze/tasks/controller.py</files>
  <read_first>
    - src/phaze/routers/pipeline.py (cloud_lane_kind at :575; ledger-seed fork `== "kueue"` at :810)
    - src/phaze/routers/agent_s3.py (defensive guard at :113)
    - src/phaze/tasks/controller.py (LocalQueue-probe gate at :179; keep the non-fatal try/except L178-185)
    - src/phaze/services/backends.py (resolved_non_local_kind)
    - .planning/phases/68-backend-protocol-3-implementations/68-PATTERNS.md (section "src/phaze/config.py (MOD)" reader-rewire table)
    - .planning/phases/68-backend-protocol-3-implementations/68-CONTEXT.md (D-09); 68-VALIDATION.md (Layer 5)
  </read_first>
  <action>
    Rewire each active_cloud_kind reader to call resolved_non_local_kind(settings) (Wave 2 helper)
    instead of the config accessor — behavior-preserving (D-09:). pipeline.py:575 dashboard
    cloud_lane_kind → resolved_non_local_kind (returns "local" when not cloud_enabled, else the single
    non-local kind). pipeline.py:810 ledger-seed fork (the `== "kueue"` check) → compare
    resolved_non_local_kind(settings) == "kueue". agent_s3.py:113 defensive guard → resolved kind.
    controller.py:179 LocalQueue-probe gate → resolved kind, KEEPING the surrounding non-fatal
    try/except (L178-185) exactly. Do NOT change any observable output (dashboard label text, ledger
    seed branch taken, guard behavior, probe gate). Leave config.active_cloud_kind in place for now —
    68-05 deletes it once these readers no longer reference it. Layer 5 regression guards that each
    reader's behavior is unchanged.
  </action>
  <verify>
    <automated>uv run pytest tests/analyze/ tests/shared/ -x -k "pipeline or agent_s3 or controller or cloud_lane or ledger"</automated>
  </verify>
  <acceptance_criteria>
    - None of pipeline.py:575/810, agent_s3.py:113, controller.py:179 reference active_cloud_kind after this task (grep-clean)
    - Each rewired reader resolves through resolved_non_local_kind() and produces identical output (Layer 5 regression green)
    - controller.py non-fatal try/except around the probe gate is unchanged
    - `uv run pytest tests/analyze/ tests/shared/ -x -k "pipeline or agent_s3 or controller or cloud_lane or ledger"` passes
    - `uv run mypy src/phaze/routers/pipeline.py src/phaze/routers/agent_s3.py src/phaze/tasks/controller.py` clean
  </acceptance_criteria>
  <done>All active_cloud_kind readers resolve through the registry-derived helper with no behavior change; accessor now unreferenced (deletion in 68-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compute agent → control plane (/pushed callback) | report_pushed receives a push-completion callback; already authenticated (§5, unchanged) |
| control plane drain → backends | dispatch orchestration in the advisory-locked drain txn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-08 | Tampering | duplicate/late /pushed callback clobbering an advanced file or double-terminalizing | mitigate | cloud_job SUCCEEDED write is gated behind the existing WR-02 rowcount!=0 guard — idempotent no-op path writes nothing |
| T-68-09 | Tampering | dispatch committing mid-loop, releasing the advisory lock | mitigate | Drain keeps the single post-loop commit; dispatch never commits (Landmine L1); snapshot + staging_cron tests assert the tick shape |
| T-68-10 | Denial of Service | is_available/dispatch raising out of the cron | mitigate | Cron no-op discipline preserved — failures degrade to {"staged":0} holds, never raise |
| T-68-11 | Information Disclosure | reader rewire leaking backend identity/creds to the dashboard | accept | resolved_non_local_kind returns only a kind string ("local"/"compute"/"kueue"); no secret/creds surfaced |
| T-68-SC | Tampering | npm/pip/cargo installs | accept (N/A) | Zero package installs this phase |
</threat_model>

<verification>
- `uv run pytest tests/analyze/core/test_dispatch_snapshot.py -x` green — BACK-04 acceptance gate (snapshot unchanged except the one deliberate compute field)
- `uv run pytest tests/analyze/services/test_backends.py -x` green (invariant now holds with live terminalization)
- `uv run pytest tests/analyze/ tests/shared/ -x` green (no regression across the rewired readers)
- Grep: no active_cloud_kind reader remains in the four rewired files
</verification>

<success_criteria>
- Drain dispatches through the Backend protocol; compute row terminalized (D-08); readers rewired (D-09); BACK-04 snapshot proven unchanged modulo the deliberate compute cloud_job artifact
</success_criteria>

<output>
Create `.planning/phases/68-backend-protocol-3-implementations/68-04-SUMMARY.md` when done
</output>
