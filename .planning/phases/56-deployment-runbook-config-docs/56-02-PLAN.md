---
phase: 56-deployment-runbook-config-docs
plan: 02
type: execute
wave: 2
depends_on: [56-01]
files_modified:
  - src/phaze/routers/pipeline.py
  - src/phaze/templates/pipeline/partials/localqueue_card.html
  - src/phaze/templates/pipeline/dashboard.html
  - src/phaze/templates/pipeline/partials/stats_bar.html
autonomous: true
requirements: [KDEPLOY-04]
must_haves:
  truths:
    - "D-05: the pipeline dashboard surfaces a 'K8s LocalQueue unreachable' amber alert when the controller flagged it, and stays silent (empty) when reachable"
    - "The alert is seeded into BOTH render paths (first-load SSR and the 5s OOB re-push) identically to inadmissible_count"
    - "The alert lives OUTSIDE #pipeline-stats and is re-pushed as hx-swap-oob on the existing 5s /pipeline/stats poll, with a stable section id on both renders"
    - "All alert copy is static through Jinja autoescape — no operator free-text is interpolated"
  artifacts:
    - path: "src/phaze/templates/pipeline/partials/localqueue_card.html"
      provides: "amber OOB alert partial, empty when reachable"
      contains: "K8s LocalQueue unreachable"
    - path: "src/phaze/routers/pipeline.py"
      provides: "localqueue_unreachable seeded into both dashboard contexts"
      contains: "get_localqueue_unreachable"
  key_links:
    - from: "src/phaze/routers/pipeline.py"
      to: "phaze.services.pipeline.get_localqueue_unreachable"
      via: "read app.state.redis in both render paths"
      pattern: "get_localqueue_unreachable"
    - from: "src/phaze/templates/pipeline/partials/stats_bar.html"
      to: "localqueue_card.html"
      via: "OOB include with oob=True on the 5s poll"
      pattern: "localqueue_card.html"
---

<objective>
Surface the LocalQueue-unreachable flag (written by the 56-01 probe, read by get_localqueue_unreachable) on the pipeline dashboard as an amber OOB alert (D-05), reusing the inadmissible_card.html pattern verbatim. Makes the 56-00 render/OOB tests green.

Purpose: Operator sees a clear, non-blocking "K8s LocalQueue unreachable" warning on the dashboard whenever the k8s lane can't reach its LocalQueue — loud on failure, silent when healthy.
Output: localqueue_card.html partial; flag seeded into both router render paths; inline include in dashboard.html + OOB re-push in stats_bar.html.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/56-deployment-runbook-config-docs/56-CONTEXT.md
@.planning/phases/56-deployment-runbook-config-docs/56-PATTERNS.md
@.planning/phases/56-deployment-runbook-config-docs/56-UI-SPEC.md

<interfaces>
<!-- Existing wiring to mirror exactly (inadmissible_count is the template). -->

src/phaze/routers/pipeline.py:
  from phaze.services.pipeline import (... get_inadmissible_count ...)  (:25-35) — add get_localqueue_unreachable here
  first-load render: inadmissible_count = await get_inadmissible_count(session) (:499); added to context dict (:526)
  5s OOB re-push:    inadmissible_count = await get_inadmissible_count(session) (:575); added to context dict (:598)
  the redis handle is read like _read_pipeline_counters: getattr(request.app.state, "redis", None)

Templates:
  src/phaze/templates/pipeline/dashboard.html:35  -> {% include "pipeline/partials/inadmissible_card.html" %}
  src/phaze/templates/pipeline/partials/stats_bar.html:90 -> {% with oob = True %}{% include "pipeline/partials/inadmissible_card.html" %}{% endwith %}
  src/phaze/templates/pipeline/partials/inadmissible_card.html (whole file) — the structure to clone

Service read service (from 56-01):
  phaze.services.pipeline.get_localqueue_unreachable(redis) -> bool   (key: phaze:k8s:localqueue_unreachable)

Tests this plan must turn green:
  tests/test_routers/test_pipeline_localqueue.py (alert empty-when-reachable / banner-when-flagged / stable id on first-load + OOB)

Locked copy (56-UI-SPEC §Copywriting — do NOT alter):
  heading: ⚠ K8s LocalQueue unreachable
  body:    K8s LocalQueue unreachable — verify PHAZE_KUBE_LOCAL_QUEUE / cluster connectivity.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: localqueue_card.html amber OOB alert partial</name>
  <read_first>
    - src/phaze/templates/pipeline/partials/inadmissible_card.html (whole file — clone verbatim, swap flag + copy)
    - .planning/phases/56-deployment-runbook-config-docs/56-UI-SPEC.md (§Copywriting, §Interaction Contract, §Color — amber not red)
    - .planning/phases/56-deployment-runbook-config-docs/56-PATTERNS.md (localqueue_card.html section)
  </read_first>
  <action>Create src/phaze/templates/pipeline/partials/localqueue_card.html by cloning inadmissible_card.html: a single top-level <section> with a stable id (use "localqueue-card"), {% if oob %}hx-swap-oob="true"{% endif %}, gated by {% if localqueue_unreachable %}. Inside the gate render the amber box (border-amber-300 dark:border-amber-700 bg-amber-50 dark:bg-amber-950 rounded-lg p-4 space-y-1) with role="alert" + aria-labelledby, an <h2 id="localqueue-heading"> reading the locked heading "⚠ K8s LocalQueue unreachable", and a <p> with the locked body "K8s LocalQueue unreachable — verify PHAZE_KUBE_LOCAL_QUEUE / cluster connectivity." Use amber text classes matching the analog. Do NOT use red (D-05 is warn+surface, app stays up). When localqueue_unreachable is falsy the <section> renders empty (silent). All strings static — no variable interpolation of operator input.</action>
  <verify>
    <automated>uv run pytest tests/test_routers/test_pipeline_localqueue.py -k "flagged or empty" -x</automated>
  </verify>
  <acceptance_criteria>
    - src/phaze/templates/pipeline/partials/localqueue_card.html exists with a stable id "localqueue-card"
    - Contains the exact locked heading and body strings; uses amber (not red) classes; role="alert" + aria-labelledby
    - Gated by {% if localqueue_unreachable %}; supports {% if oob %}hx-swap-oob="true"{% endif %}
    - Renders empty when localqueue_unreachable is falsy
  </acceptance_criteria>
  <done>The amber OOB alert partial exists, mirrors inadmissible_card.html, and carries the locked copy.</done>
</task>

<task type="auto">
  <name>Task 2: seed localqueue_unreachable into both render paths + wire includes</name>
  <read_first>
    - src/phaze/routers/pipeline.py:25-35,93-111,490-530,570-600 (import block, _read_pipeline_counters redis-handle read, both render paths where inadmissible_count is seeded)
    - src/phaze/templates/pipeline/dashboard.html:30-40 (the inadmissible_card include site)
    - src/phaze/templates/pipeline/partials/stats_bar.html:85-92 (the inadmissible_card OOB re-push site)
    - tests/test_routers/test_pipeline_localqueue.py (the OOB + first-load id contract)
    - .planning/phases/56-deployment-runbook-config-docs/56-PATTERNS.md (routers/pipeline.py section)
  </read_first>
  <action>In src/phaze/routers/pipeline.py: add get_localqueue_unreachable to the existing `from phaze.services.pipeline import (...)` block alongside get_inadmissible_count. In BOTH render paths (the first-load dashboard handler near :499/:526 and the 5s OOB pipeline_stats handler near :575/:598), compute localqueue_unreachable = await get_localqueue_unreachable(getattr(request.app.state, "redis", None)) (use the same app-state accessor the surrounding handler uses; mirror how the redis handle is obtained for counters) and add "localqueue_unreachable": localqueue_unreachable to the template context dict, immediately beside "inadmissible_count". In src/phaze/templates/pipeline/dashboard.html add {% include "pipeline/partials/localqueue_card.html" %} next to the inadmissible_card include (OUTSIDE #pipeline-stats). In src/phaze/templates/pipeline/partials/stats_bar.html add {% with oob = True %}{% include "pipeline/partials/localqueue_card.html" %}{% endwith %} next to the inadmissible_card OOB re-push so it rides the same 5s poll. Keep the same stable section id on both render paths (the OOB contract).</action>
  <verify>
    <automated>uv run pytest tests/test_routers/test_pipeline_localqueue.py -x</automated>
  </verify>
  <acceptance_criteria>
    - src/phaze/routers/pipeline.py imports get_localqueue_unreachable and seeds "localqueue_unreachable" in both the first-load and OOB context dicts
    - dashboard.html includes localqueue_card.html outside #pipeline-stats; stats_bar.html re-pushes it with oob=True
    - `uv run pytest tests/test_routers/test_pipeline_localqueue.py -x` exits 0 (empty-when-reachable, banner-when-flagged, stable id on both renders)
  </acceptance_criteria>
  <done>The flag is seeded identically in both render paths and the alert is OOB-pushed on the 5s poll with a stable id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redis cache → SSR render | The dashboard reads the boot flag; a read failure must degrade silently |
| static template strings → browser | Jinja autoescape; no operator free-text in the alert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-POLL | Denial of Service | dashboard render | mitigate | localqueue_unreachable read is degrade-safe (56-01 get_localqueue_unreachable returns False on error); the 5s poll never 500s (T-54-10) |
| T-56-XSS | Tampering | localqueue_card.html | mitigate | all copy is static and passes through Jinja autoescape; no operator string interpolated (T-54-11) |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | zero new external packages |
</threat_model>

<verification>
- `uv run pytest tests/test_routers/test_pipeline_localqueue.py -x` green (full file).
- Manual (no live cluster needed): with the Redis key set, GET /pipeline shows the amber alert; absent, the section is empty; the alert re-renders on /pipeline/stats with the same id.
- `pre-commit run --all-files` passes.
</verification>

<success_criteria>
- Alert renders amber when flagged, empty when reachable.
- Seeded in both render paths; OOB-pushed on the 5s poll; stable id.
</success_criteria>

<output>
Create `.planning/phases/56-deployment-runbook-config-docs/56-02-SUMMARY.md` when done.
</output>
