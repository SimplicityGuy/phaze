---
phase: 56-deployment-runbook-config-docs
plan: 01
type: execute
wave: 1
depends_on: [56-00]
files_modified:
  - src/phaze/services/kube_staging.py
  - src/phaze/tasks/controller.py
  - src/phaze/services/pipeline.py
autonomous: true
requirements: [KDEPLOY-04]
must_haves:
  truths:
    - "D-06: the LocalQueue-reachability probe runs once at the controller SAQ worker's startup hook, gated on cloud_target=='k8s', reusing the existing kr8s client"
    - "D-05: the probe is non-fatal — on any failure it logs a WARNING and raises the cross-process flag, but the control plane still boots (Postgres/Redis/UI/local-analysis unaffected)"
    - "get_local_queue() GETs the configured LocalQueue by name and lives in kube_staging.py (the single home of every kr8s call)"
    - "The unreachable state crosses the controller->api process boundary via the Redis key phaze:k8s:localqueue_unreachable, read degrade-safe by get_localqueue_unreachable()"
    - "The probe WARNING names the env var (PHAZE_KUBE_LOCAL_QUEUE), never the bearer token or kube DSN"
  artifacts:
    - path: "src/phaze/services/kube_staging.py"
      provides: "async get_local_queue() via new_class + refresh()"
      contains: "async def get_local_queue"
    - path: "src/phaze/tasks/controller.py"
      provides: "non-fatal LocalQueue probe in startup gated on cloud_target=='k8s'"
      contains: "phaze:k8s:localqueue_unreachable"
    - path: "src/phaze/services/pipeline.py"
      provides: "degrade-safe get_localqueue_unreachable(redis) -> bool"
      contains: "async def get_localqueue_unreachable"
  key_links:
    - from: "src/phaze/tasks/controller.py"
      to: "phaze.services.kube_staging.get_local_queue"
      via: "await inside startup, broad try/except"
      pattern: "get_local_queue"
    - from: "src/phaze/tasks/controller.py"
      to: "Redis key phaze:k8s:localqueue_unreachable"
      via: "ctx['redis'].set / .delete"
      pattern: "localqueue_unreachable"
    - from: "src/phaze/services/pipeline.py"
      to: "Redis key phaze:k8s:localqueue_unreachable"
      via: "redis.exists"
      pattern: "phaze:k8s:localqueue_unreachable"
---

<objective>
Implement the net-new backend for KDEPLOY-04: a live LocalQueue-reachability probe at controller startup (D-05/D-06), the kr8s GET that backs it, and the degrade-safe cross-process flag read the dashboard consumes. This makes the 56-00 kube_staging + controller-startup RED tests green and provides the read service 56-02 will render.

Purpose: Give the operator a clear, non-fatal signal when cloud_target=="k8s" but the configured Kueue LocalQueue is unreachable — without ever crash-looping the control plane on a transient mesh blip.
Output: get_local_queue() in kube_staging.py; a gated non-fatal probe block in controller.startup; get_localqueue_unreachable() in services/pipeline.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/56-deployment-runbook-config-docs/56-CONTEXT.md
@.planning/phases/56-deployment-runbook-config-docs/56-PATTERNS.md
@.planning/phases/56-deployment-runbook-config-docs/56-RESEARCH.md

<interfaces>
<!-- Existing in-repo contracts the executor implements against. -->

kube_staging.py (clone these):
  _kube_config() -> ControlSettings        (:72-84, the kube_api_url/namespace/local_queue gate)
  async _api(cfg) -> Any                    (:87-106, builds the kr8s client; token rebuild solved here)
  async get_job(name) -> Any               (:197-203, construct-by-name + refresh() idiom)
  get_workload_for(...)                     (:218-235, uses new_class(kind="Workload", version=cfg.kube_workload_api_version, namespaced=True) at :229)
  imports already present: import kr8s; from kr8s.asyncio.objects import Job, new_class (:25-27)

controller.startup (clone the boot-resilience block):
  async def startup(ctx) (:51)
  cfg = get_settings() (:57)
  ctx["redis"] = redis_async.Redis.from_url(cfg.redis_url) (:104)
  ledger backfill + recover_orphaned_work each wrapped in their own broad try/except (:145-157) — copy this discipline

services/pipeline.py degrade-safe analogs:
  get_inadmissible_count(session) (:821-841, the never-500 degrade discipline + docstring tone)
  routers/pipeline.py:_read_pipeline_counters (:93-111, the redis-handle / missing-handle -> {} degrade — model the bool read on this, returning False not {})

Tests this plan must turn green (read them — they are the contract):
  tests/test_services/test_kube_staging.py -k local_queue
  tests/test_tasks/test_controller_startup_localqueue.py
  tests/test_routers/test_pipeline_localqueue.py::test_get_localqueue_unreachable_degrades_to_false
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: get_local_queue() in kube_staging.py</name>
  <behavior>
    - get_local_queue() success: GET returns 200 -> returns the LocalQueue object
    - 404 -> propagates kr8s.NotFoundError (caller treats as unreachable)
    - transient 500 / connection error -> propagates the exception (caller treats as unreachable)
  </behavior>
  <read_first>
    - src/phaze/services/kube_staging.py:25-27,72-106,197-235 (imports, _kube_config, _api, get_job, get_workload_for/new_class)
    - tests/test_services/test_kube_staging.py (the -k local_queue cases from 56-00 — the exact contract)
    - .planning/phases/56-deployment-runbook-config-docs/56-PATTERNS.md (kube_staging get_local_queue section)
    - .planning/phases/56-deployment-runbook-config-docs/56-RESEARCH.md (§Net-New Code Design item 1)
  </read_first>
  <action>Add async def get_local_queue() -> Any to src/phaze/services/kube_staging.py, immediately after get_job. Mirror get_job exactly: call _kube_config() to get cfg, await _api(cfg), build the class via new_class(kind="LocalQueue", version=cfg.kube_workload_api_version, namespaced=True) (LocalQueue is in the same kueue.x-k8s.io group as Workload — no new import), construct the object by name {"metadata": {"name": cfg.kube_local_queue, "namespace": cfg.kube_namespace}} with api=api, await its refresh(), and return it. Do NOT add any try/except, validator, or fail-fast here — this service raises; the controller-startup caller owns the non-fatal catch (anti-pattern per 56-RESEARCH). Add a docstring noting refresh() raises kr8s.NotFoundError on 404 and that the caller treats both 404 and transient errors as "unreachable".</action>
  <verify>
    <automated>uv run pytest tests/test_services/test_kube_staging.py -k local_queue -x</automated>
  </verify>
  <acceptance_criteria>
    - src/phaze/services/kube_staging.py contains "async def get_local_queue"
    - The function uses new_class(kind="LocalQueue", version=cfg.kube_workload_api_version, namespaced=True) and refresh()
    - No try/except / no validator added in this function
    - `uv run pytest tests/test_services/test_kube_staging.py -k local_queue -x` exits 0 (the three 56-00 cases pass)
  </acceptance_criteria>
  <done>get_local_queue exists, mirrors get_job, and the three RED cases from 56-00 are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: degrade-safe get_localqueue_unreachable() reader</name>
  <behavior>
    - redis is None -> returns False (test client skips lifespan)
    - redis.exists("phaze:k8s:localqueue_unreachable") truthy -> True
    - any Redis exception -> returns False (silent; logs a warning)
  </behavior>
  <read_first>
    - src/phaze/services/pipeline.py:821-841 (get_inadmissible_count — degrade discipline + docstring tone) and the module logger import (:10)
    - src/phaze/routers/pipeline.py:93-111 (_read_pipeline_counters — the redis-handle degrade to model the bool on)
    - tests/test_routers/test_pipeline_localqueue.py::test_get_localqueue_unreachable_degrades_to_false (the contract)
    - .planning/phases/56-deployment-runbook-config-docs/56-PATTERNS.md (services/pipeline get_localqueue_unreachable section)
  </read_first>
  <action>Add async def get_localqueue_unreachable(redis: Any) -> bool to src/phaze/services/pipeline.py near get_inadmissible_count. Return False when redis is None; otherwise return bool(await redis.exists("phaze:k8s:localqueue_unreachable")); wrap the whole body in try/except that logs logger.warning("localqueue_unreachable_read_degraded", exc_info=True) and returns False on any error. Mirror the get_inadmissible_count docstring discipline: name the dashboard surface, state the silent-degrade contract, and the "never 500s the hot 5s poll (T-54-10)" rationale, and note the controller.startup probe is the writer of the key. Use the existing module logger; do not add new imports beyond Any (already imported).</action>
  <verify>
    <automated>uv run pytest tests/test_routers/test_pipeline_localqueue.py -k degrades_to_false -x</automated>
  </verify>
  <acceptance_criteria>
    - src/phaze/services/pipeline.py contains "async def get_localqueue_unreachable"
    - Reads the key "phaze:k8s:localqueue_unreachable" and returns False on None / any exception
    - `uv run pytest tests/test_routers/test_pipeline_localqueue.py -k degrades_to_false -x` exits 0
  </acceptance_criteria>
  <done>The degrade-safe reader exists and returns False on missing/erroring redis; 56-02 will wire it into the render paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: non-fatal LocalQueue probe in controller.startup (D-05/D-06)</name>
  <behavior>
    - cloud_target != "k8s" -> get_local_queue never called (probe skipped)
    - cloud_target == "k8s" + get_local_queue raises -> logger.warning naming PHAZE_KUBE_LOCAL_QUEUE, ctx["redis"].set("phaze:k8s:localqueue_unreachable", "1"), startup returns (no re-raise)
    - cloud_target == "k8s" + get_local_queue ok -> ctx["redis"].delete("phaze:k8s:localqueue_unreachable")
  </behavior>
  <read_first>
    - src/phaze/tasks/controller.py:51-157 (startup, cfg at :57, ctx["redis"] at :104, boot-resilience try/except at :145-157)
    - tests/test_tasks/test_controller_startup_localqueue.py (the three probe cases — the contract)
    - .planning/phases/56-deployment-runbook-config-docs/56-CONTEXT.md (D-05, D-06)
    - .planning/phases/56-deployment-runbook-config-docs/56-PATTERNS.md (controller.py probe-wiring section, including the exact block)
  </read_first>
  <action>In src/phaze/tasks/controller.py, after the recovery block (around :157) inside startup, add the LocalQueue probe gated on cfg.cloud_target == "k8s" (cfg already bound at :57). Inside the gate, wrap in its OWN broad try/except mirroring the recovery block: on success await kube_staging.get_local_queue() then await ctx["redis"].delete("phaze:k8s:localqueue_unreachable"); on Exception call logger.warning with a static message that names PHAZE_KUBE_LOCAL_QUEUE / cluster connectivity (NEVER interpolate the token or kube DSN) and await ctx["redis"].set("phaze:k8s:localqueue_unreachable", "1"). The except must NEVER re-raise (boot-resilience — a mesh blip cannot abort controller boot). Add the import `from phaze.services import kube_staging` at the top of the module if not already present. Add an inline comment citing KDEPLOY-04, D-05/D-06 and that this is a live runtime probe distinct from the three fail-fast config validators.</action>
  <verify>
    <automated>uv run pytest tests/test_tasks/test_controller_startup_localqueue.py -x</automated>
  </verify>
  <acceptance_criteria>
    - src/phaze/tasks/controller.py gates the probe on cfg.cloud_target == "k8s"
    - The probe block is wrapped in a broad try/except that does not re-raise; the warning message contains "PHAZE_KUBE_LOCAL_QUEUE" and no token/DSN interpolation
    - Sets/clears the Redis key "phaze:k8s:localqueue_unreachable" via ctx["redis"]
    - `uv run pytest tests/test_tasks/test_controller_startup_localqueue.py -x` exits 0 (all three cases pass, including startup-returns-on-failure)
  </acceptance_criteria>
  <done>The probe runs only when cloud_target=="k8s", is non-fatal, and writes/clears the cross-process flag.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| control-plane → kube API | The probe GETs a LocalQueue over the operator-provided mesh; failures must degrade, not crash |
| controller process → api process | The unreachable signal must cross via shared Redis, not in-memory state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-DOS | Denial of Service | controller.startup probe | mitigate | gated on cloud_target=="k8s", broad try/except that never re-raises (clone of :145-157); a mesh/kube blip cannot abort boot |
| T-56-LOG | Information Disclosure | probe WARNING | mitigate | static log message names the env var only; never interpolates agent_token / kube DSN (T-54-07) |
| T-56-POLL | Denial of Service | get_localqueue_unreachable | mitigate | degrade-safe read returns False on any error / missing redis so the 5s /pipeline/stats poll never 500s (T-54-10) |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | zero new external packages (56-RESEARCH audit: N/A) |
</threat_model>

<verification>
- `uv run pytest tests/test_services/test_kube_staging.py tests/test_tasks/test_controller_startup_localqueue.py -x` green.
- `uv run pytest tests/test_routers/test_pipeline_localqueue.py -k degrades_to_false -x` green (the render/OOB cases stay RED until 56-02).
- `uv run mypy src/phaze/services/kube_staging.py src/phaze/tasks/controller.py src/phaze/services/pipeline.py` clean.
- `pre-commit run --all-files` passes.
</verification>

<success_criteria>
- get_local_queue mirrors get_job and raises (no swallow) on 404/transient.
- The probe is gated, non-fatal, and flags via Redis.
- The dashboard read service degrades to False.
</success_criteria>

<output>
Create `.planning/phases/56-deployment-runbook-config-docs/56-01-SUMMARY.md` when done.
</output>
