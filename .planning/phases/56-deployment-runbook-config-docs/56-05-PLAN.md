---
phase: 56-deployment-runbook-config-docs
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/configuration.md
  - docs/deployment.md
  - docs/README.md
autonomous: true
requirements: [KDEPLOY-02, KDEPLOY-05]
must_haves:
  truths:
    - "D-04: docs/configuration.md documents every K8s/S3 knob (cloud_target, kube_api_url, kube_namespace, kube_local_queue, kube_workload_api_version, s3_* fields, presign/lifecycle/part-size/max-attempts), sourced from the config.py Field(...) descriptions, flagging the _FILE-secret fields"
    - "KDEPLOY-02: the documented knobs note the fail-fast model validators (required-when-k8s) already shipped in config.py, distinct from the non-fatal LocalQueue probe"
    - "KDEPLOY-05 / D-04: docs/deployment.md documents the single-cloud_target revert to all-local (or A1) with no other change, and points to docs/k8s-burst.md for the full cluster/bucket/secret setup"
    - "D-04: docs/README.md indexes docs/k8s-burst.md under Operations"
  artifacts:
    - path: "docs/configuration.md"
      provides: "complete K8s/S3 knob table with _FILE flags"
      contains: "kube_workload_api_version"
    - path: "docs/deployment.md"
      provides: "single-cloud_target revert section + k8s-burst.md pointer"
      contains: "k8s-burst.md"
    - path: "docs/README.md"
      provides: "k8s-burst.md index row under Operations"
      contains: "k8s-burst.md"
  key_links:
    - from: "docs/deployment.md"
      to: "docs/k8s-burst.md"
      via: "pointer for full cluster/bucket/secret setup"
      pattern: "k8s-burst.md"
    - from: "docs/configuration.md"
      to: "src/phaze/config.py Field descriptions"
      via: "knob table sourced verbatim from Field(...) descriptions"
      pattern: "PHAZE_KUBE"
---

<objective>
Document the already-shipped K8s/S3 config surface (KDEPLOY-02) and the single-toggle revert (KDEPLOY-05). The config knobs and the cloud_target toggle already exist in code (Phases 53/54/55); this plan is documentation only — no new validators, no new code.

Purpose: Give the operator a canonical knob reference (with _FILE-secret flags) and a deployment.md revert procedure that literally satisfies KDEPLOY-05's "documents the revert + the full cluster/bucket/secret setup" criterion.
Output: extended K8s/S3 knob table in configuration.md; a revert section + k8s-burst.md pointer in deployment.md; a k8s-burst.md index row in README.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/56-deployment-runbook-config-docs/56-CONTEXT.md
@.planning/phases/56-deployment-runbook-config-docs/56-RESEARCH.md
@.planning/phases/56-deployment-runbook-config-docs/56-PATTERNS.md

<interfaces>
<!-- Source the knob descriptions VERBATIM from these config.py Field(...) entries. -->

src/phaze/config.py (ControlSettings):
  cloud_target (:406, Literal["local","a1","k8s"], default "local", PHAZE_CLOUD_TARGET) — master selector
  cloud_route_threshold_sec (:389), cloud_max_in_flight (:417)
  s3_endpoint_url (:466), s3_bucket (:471), s3_region (:476), s3_addressing_style (:481)
  s3_presign_put_ttl_sec (:498), s3_presign_get_ttl_sec (:505), s3 part-size (:524)
  kube_api_url (:534), kube_namespace (:539), kube_local_queue (:544), kube_workload_api_version (:564)
  kube_submit_max_attempts (search the file), kube credentials (file-mounted SecretStr)
  SECRET_FILE_FIELDS (:348 — extends base with kube/agent/s3 credential fields) — flag these as _FILE
  validators (already shipped, fail-fast): _enforce_s3_config_when_k8s (:601), _enforce_kube_config_when_k8s, _enforce_compute_scratch_dir_when_a1

Existing doc anchors (extend, do not duplicate):
  docs/configuration.md:81 "## Cloud-burst settings" — a partial table already lists cloud_target/s3_*/some kube knobs; EXTEND it to full coverage
  docs/configuration.md:21 "## Secrets via files (`_FILE` convention)" — the _FILE table to keep consistent
  docs/deployment.md:44 "## Cloud-burst compute agent" — the insertion neighborhood for the revert section
  docs/README.md:24 "## 🚀 Operations" with rows for Deployment Guide (:28) + Cloud Burst (:29)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: complete the K8s/S3 knob table in configuration.md (KDEPLOY-02)</name>
  <read_first>
    - src/phaze/config.py:348,389-417,461-566,582-660 (the Field descriptions, SECRET_FILE_FIELDS, and the three validators — source the table verbatim)
    - docs/configuration.md:21-51,81-101 (the _FILE convention section + the existing partial Cloud-burst settings table to extend)
    - .planning/phases/56-deployment-runbook-config-docs/56-CONTEXT.md (D-04)
    - .planning/phases/56-deployment-runbook-config-docs/56-RESEARCH.md (KDEPLOY-02 row — documentation only, no new validators)
  </read_first>
  <action>In docs/configuration.md, extend the "## Cloud-burst settings" table (or add a clearly-headed K8s/S3 subsection) so EVERY shipped K8s/S3 knob is documented, each row sourced from the matching config.py Field(...) description: cloud_target, cloud_route_threshold_sec, cloud_max_in_flight, s3_endpoint_url, s3_bucket, s3_region, s3_addressing_style, s3_presign_put_ttl_sec, s3_presign_get_ttl_sec, the S3 part-size knob, kube_api_url, kube_namespace, kube_local_queue, kube_workload_api_version, kube_submit_max_attempts, and the file-mounted kube/S3/agent credentials. For each, give the env-var alias (PHAZE_*), role (Control/Agent), default, and a _FILE column flagging the credential fields present in SECRET_FILE_FIELDS. Add a short note that when cloud_target=k8s the already-shipped fail-fast model validators (_enforce_s3_config_when_k8s / _enforce_kube_config_when_k8s) require the K8s/S3 config at startup — and that this is DISTINCT from the non-fatal runtime LocalQueue probe (warn+surface). Reference docs/k8s-burst.md for the cluster-side setup. Do NOT add or change any validator; this is documentation only.</action>
  <verify>
    <automated>grep -q PHAZE_KUBE_WORKLOAD_API_VERSION docs/configuration.md && grep -q PHAZE_S3_ENDPOINT_URL docs/configuration.md && grep -q PHAZE_KUBE_LOCAL_QUEUE docs/configuration.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/configuration.md documents PHAZE_CLOUD_TARGET, PHAZE_KUBE_API_URL, PHAZE_KUBE_NAMESPACE, PHAZE_KUBE_LOCAL_QUEUE, PHAZE_KUBE_WORKLOAD_API_VERSION, and the S3 knobs (PHAZE_S3_ENDPOINT_URL / PHAZE_S3_BUCKET / region / addressing / presign TTLs / part-size)
    - The credential fields are flagged with their _FILE siblings; the fail-fast required-when-k8s validators are mentioned and distinguished from the non-fatal LocalQueue probe
    - The grep verify command prints OK
  </acceptance_criteria>
  <done>configuration.md is the complete K8s/S3 knob reference with _FILE flags, sourced from config.py.</done>
</task>

<task type="auto">
  <name>Task 2: deployment.md revert section + pointer; README.md index row (KDEPLOY-05)</name>
  <read_first>
    - docs/deployment.md:44-60 ("## Cloud-burst compute agent" — insertion neighborhood; the cloud_target rename note)
    - docs/README.md:24-29 ("## 🚀 Operations" index rows to mirror)
    - .planning/phases/56-deployment-runbook-config-docs/56-CONTEXT.md (D-04, KDEPLOY-05 names deployment.md literally)
    - .planning/phases/56-deployment-runbook-config-docs/56-PATTERNS.md (configuration/deployment/README edits section)
  </read_first>
  <action>In docs/deployment.md, add a clearly-headed "Revert / single-toggle" subsection (near the Cloud-burst compute agent section) documenting that setting PHAZE_CLOUD_TARGET=local (or a1) and restarting the controller worker + api reverts the entire K8s offload to all-local (or the v5.0 A1 path) with NO other change — long files route off the k8s path; the cluster/bucket/secret objects can be left in place. Add an explicit pointer to docs/k8s-burst.md for the full cluster + bucket + secret setup (so deployment.md literally documents both the revert AND a reachable path to the full setup — satisfying KDEPLOY-05 criterion 5). In docs/README.md, add a k8s-burst.md row under "## 🚀 Operations" mirroring the existing Cloud Burst row format (link + emoji + one-line description: Kubernetes/Kueue burst runbook, RBAC, Secret, master toggle).</action>
  <verify>
    <automated>grep -q "k8s-burst.md" docs/deployment.md && grep -q "k8s-burst.md" docs/README.md && grep -qi "PHAZE_CLOUD_TARGET=local" docs/deployment.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/deployment.md contains a revert subsection naming PHAZE_CLOUD_TARGET=local (and a1) with no-other-change semantics, plus a pointer to docs/k8s-burst.md for the full cluster/bucket/secret setup
    - docs/README.md contains a k8s-burst.md index row under Operations
    - The grep verify command prints OK
  </acceptance_criteria>
  <done>deployment.md documents the single-toggle revert + full-setup pointer; README indexes the new doc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → config (env/_FILE) | Documented knobs; secrets must be flagged for _FILE mounting, never inline |
| docs → operator action | The revert procedure must be exact so a toggle flip fully disables the k8s path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-SECRETS | Information Disclosure | documented credentials | mitigate | the knob table flags every credential field with its _FILE sibling (mount, never inline); SecretStr fields are masked in logs |
| T-56-SSRF | Tampering/SSRF | s3_endpoint_url doc | accept | already validated http(s)+netloc at construction (config.py:582); doc only restates the constraint |
| T-56-REVERT | Availability | revert procedure | mitigate | deployment.md states the single-toggle revert with no other change, so an operator can fully disable k8s without partial-config drift |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | zero new external packages |
</threat_model>

<verification>
- `grep -q PHAZE_KUBE_WORKLOAD_API_VERSION docs/configuration.md` and the per-task grep verifies pass.
- Manual (KDEPLOY-05 end-to-end is manual-only — no CI cluster): set PHAZE_CLOUD_TARGET=local, redeploy, confirm long files route off the k8s path.
- `pre-commit run --all-files` passes (markdownlint as configured).
</verification>

<success_criteria>
- configuration.md fully documents the K8s/S3 knobs with _FILE flags.
- deployment.md documents the revert + points to k8s-burst.md; README indexes it.
</success_criteria>

<output>
Create `.planning/phases/56-deployment-runbook-config-docs/56-05-SUMMARY.md` when done.
</output>
