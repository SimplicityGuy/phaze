---
phase: 54-kube-submit-watch-reconcile-cron
plan: 04
type: execute
wave: 2
depends_on: ["54-02"]
files_modified:
  - src/phaze/services/pipeline.py
  - src/phaze/routers/pipeline.py
  - src/phaze/templates/pipeline/partials/inadmissible_card.html
  - src/phaze/templates/pipeline/dashboard.html
  - src/phaze/templates/pipeline/partials/stats_bar.html
  - tests/test_services/test_pipeline_counts.py
  - tests/test_routers/test_pipeline_inadmissible.py
autonomous: true
requirements: [KSUBMIT-04]
user_setup: []

must_haves:
  truths:
    - "D-06: when any cloud_job row has `inadmissible = true`, the pipeline DAG dashboard shows a warning alert ('K8s Jobs not admitting — check LocalQueue config'); healthy Pending stays invisible (no card when the count is 0)"
    - "D-06: the alert is driven off a degrade-safe COUNT(cloud_job WHERE inadmissible) reader (a DB hiccup surfaces 0, never 500s the 5s poll)"
    - "The card lives OUTSIDE #pipeline-stats and is re-pushed OOB by stats_bar.html on each 5s poll (mirrors awaiting_cloud_card)"
  artifacts:
    - path: "src/phaze/services/pipeline.py"
      provides: "get_inadmissible_count(session) degrade-safe reader"
      contains: "def get_inadmissible_count"
    - path: "src/phaze/templates/pipeline/partials/inadmissible_card.html"
      provides: "the Inadmissible warning card (shown only when count > 0)"
      contains: "inadmissible-card"
  key_links:
    - from: "src/phaze/routers/pipeline.py dashboard() + /pipeline/stats"
      to: "get_inadmissible_count"
      via: "inadmissible_count context key seeded into both render paths"
      pattern: "inadmissible_count"
---

<objective>
Surface the Inadmissible operator alert (D-06) on the existing Phase 34/44 pipeline DAG dashboard,
driven off the `cloud_job.inadmissible` flag — an additive warning card, no new frontend surface.

Purpose: KSUBMIT-04 distinguishes a misconfigured LocalQueue/ClusterQueue (loud) from a healthy
quota wait (silent). The reconcile cron (Plan 06) sets the flag; this plan makes it visible so the
operator sees "K8s Jobs not admitting — check LocalQueue config" without tailing logs.
Output: a degrade-safe count reader + the OOB warning card wired into the dashboard + stats poll.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-kube-submit-watch-reconcile-cron/54-CONTEXT.md
@.planning/phases/54-kube-submit-watch-reconcile-cron/54-PATTERNS.md
@src/phaze/templates/pipeline/partials/awaiting_cloud_card.html
@CLAUDE.md

<interfaces>
<!-- Exact analog to mirror: the "Awaiting cloud" count card pattern. -->
async def get_awaiting_cloud_count(session) -> int:        # services/pipeline.py:805 — _safe_count degrade-safe reader
    return await _safe_count(session, select(func.count(FileRecord.id)).where(FileRecord.state == FileState.AWAITING_CLOUD), node="awaiting_cloud")
# routers/pipeline.py: awaiting_cloud_count seeded into dashboard() context (~484) AND the /pipeline/stats poll context (~547),
#   stats_bar.html re-pushes the card OOB on the 5s poll (oob=True).
# dashboard.html: {% include "pipeline/partials/awaiting_cloud_card.html" %} sits OUTSIDE #pipeline-stats.
# awaiting_cloud_card.html: <section id="awaiting-cloud-card" {% if oob %}hx-swap-oob="true"{% endif %} ...>  (same id on both renders = OOB contract)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: get_inadmissible_count reader + router context wiring</name>
  <files>src/phaze/services/pipeline.py, src/phaze/routers/pipeline.py, tests/test_services/test_pipeline_counts.py</files>
  <behavior>
    - get_inadmissible_count(session) returns COUNT(cloud_job WHERE inadmissible = true).
    - A DB error degrades to 0 (uses _safe_count, mirrors get_awaiting_cloud_count) — never raises.
    - dashboard() and /pipeline/stats both put `inadmissible_count` into their template context.
  </behavior>
  <read_first>
    - src/phaze/services/pipeline.py (get_awaiting_cloud_count at 805-816, the _safe_count helper it calls)
    - src/phaze/routers/pipeline.py (the dashboard() context build ~477-518 and the /pipeline/stats context ~542-567 where awaiting_cloud_count is seeded)
    - src/phaze/models/cloud_job.py (the inadmissible column from Plan 02)
    - .planning/phases/54-kube-submit-watch-reconcile-cron/54-PATTERNS.md (Inadmissible alert section)
  </read_first>
  <action>
    Add `get_inadmissible_count(session)` to services/pipeline.py mirroring `get_awaiting_cloud_count`: `_safe_count(session, select(func.count(CloudJob.id)).where(CloudJob.inadmissible.is_(True)), node="inadmissible")`. Import CloudJob from phaze.models.cloud_job. In routers/pipeline.py call it in BOTH the `dashboard()` context build and the `/pipeline/stats` poll context, seeding `inadmissible_count` alongside `awaiting_cloud_count` (same degrade-safe, no-extra-try/except idiom). Add tests to tests/test_services/test_pipeline_counts.py (or create it) asserting the reader returns the true count with N inadmissible rows seeded and degrades to 0 on a forced DB error.
  </action>
  <verify>
    <automated>uv run pytest tests/test_services/test_pipeline_counts.py -x -k inadmissible</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def get_inadmissible_count" src/phaze/services/pipeline.py` returns 1.
    - `grep -c "inadmissible_count" src/phaze/routers/pipeline.py` is ≥ 2 (dashboard + stats contexts).
    - The reader test proves count==N for N seeded inadmissible cloud_job rows and ==0 on a DB error.
    - `uv run mypy src/phaze/services/pipeline.py` passes.
  </acceptance_criteria>
  <done>A degrade-safe inadmissible count reader is seeded into both dashboard render paths.</done>
</task>

<task type="auto">
  <name>Task 2: Inadmissible card partial + dashboard include + OOB stats push</name>
  <files>src/phaze/templates/pipeline/partials/inadmissible_card.html, src/phaze/templates/pipeline/dashboard.html, src/phaze/templates/pipeline/partials/stats_bar.html, tests/test_routers/test_pipeline_inadmissible.py</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/awaiting_cloud_card.html (the OOB section structure + the `{% if oob %}hx-swap-oob="true"{% endif %}` contract)
    - src/phaze/templates/pipeline/dashboard.html (the cloud-card include block at 19-30 — where to add the include OUTSIDE #pipeline-stats)
    - src/phaze/templates/pipeline/partials/stats_bar.html (the oob_counts OOB re-push block ~46-58)
    - tests/test_routers (an existing pipeline render test to mirror for the dashboard assertion)
  </read_first>
  <action>
    Create `inadmissible_card.html` mirroring `awaiting_cloud_card.html`: a `<section id="inadmissible-card" {% if oob %}hx-swap-oob="true"{% endif %} ...>` that renders the WARNING banner "K8s Jobs not admitting — check LocalQueue config" ONLY when `inadmissible_count > 0` (a `{% if inadmissible_count %}` guard around the visible content so a healthy Pending state shows nothing — D-06). Use a warning color vocabulary (e.g. amber/yellow) consistent with the dashboard. Add `{% include "pipeline/partials/inadmissible_card.html" %}` to dashboard.html alongside the existing cloud cards (OUTSIDE #pipeline-stats). Add the OOB re-push for `#inadmissible-card` in stats_bar.html's oob_counts block so the card stays live on the 5s poll (render the partial with oob=True). Write tests/test_routers/test_pipeline_inadmissible.py asserting: with an inadmissible row the dashboard HTML contains the warning copy + `id="inadmissible-card"`; with zero inadmissible rows the warning copy is ABSENT (the card renders empty/hidden, healthy Pending invisible).
  </action>
  <verify>
    <automated>uv run pytest tests/test_routers/test_pipeline_inadmissible.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "inadmissible-card" src/phaze/templates/pipeline/partials/inadmissible_card.html src/phaze/templates/pipeline/dashboard.html src/phaze/templates/pipeline/partials/stats_bar.html` shows the id in all three.
    - The card body is gated behind `{% if inadmissible_count %}` (no banner when 0).
    - The router test proves the warning copy appears with an inadmissible row and is absent with none.
    - The dashboard still renders (no template error) on the all-zero path.
  </acceptance_criteria>
  <done>An additive OOB warning card surfaces Inadmissible only when present; healthy Pending stays silent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database → dashboard render | the dashboard reads an aggregate count and renders operator-facing HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-10 | Denial of service | the hot 5s /pipeline/stats poll | mitigate | get_inadmissible_count is degrade-safe (_safe_count → 0 on error), so a DB hiccup never 500s the poll |
| T-54-11 | Information disclosure | dashboard alert content | accept | the alert renders only an integer count + a static string through Jinja autoescape; no operator free-text or PII is interpolated |
</threat_model>

<verification>
- `uv run pytest tests/test_services/test_pipeline_counts.py tests/test_routers/test_pipeline_inadmissible.py -x` green.
- `uv run ruff check . && uv run mypy src/phaze/services/pipeline.py src/phaze/routers/pipeline.py` clean.
</verification>

<success_criteria>
The pipeline DAG dashboard surfaces an Inadmissible warning alert driven off `cloud_job.inadmissible`
(D-06, KSUBMIT-04) via a degrade-safe count reader and an OOB card, shown only when present so healthy
Pending stays invisible — no new frontend surface, mirroring the awaiting_cloud card pattern.
</success_criteria>

<output>
Create `.planning/phases/54-kube-submit-watch-reconcile-cron/54-04-SUMMARY.md` when done.
</output>
