---
phase: 66-docs-drift-gate-dead-code-sweep
plan: 03
type: execute
wave: 2
depends_on: [66-01]
files_modified:
  - pyproject.toml
  - uv.lock
  - vulture_whitelist.py
  - justfile
  - src/phaze/**
autonomous: false
requirements: [CLEAN-02]
user_setup: []
must_haves:
  truths:
    - "vulture>=2.16 is a dev-only dependency, alphabetically placed in [dependency-groups] dev, resolving under the exclude-newer=7-days cooldown (D-13)"
    - "A hand-audited vulture_whitelist.py at repo root suppresses framework false-positives (FastAPI handlers, Pydantic validators, transient ORM attrs) so genuinely-live code is never flagged (D-13)"
    - "Only confirmed-dead code is removed — each deletion verified against dynamic references (grep) AND a green full suite before removal (D-12)"
    - "No runtime/backend behavior changes; the full test suite is green after the sweep"
    - "build_dashboard_context / get_stage_progress / get_queue_activity are NOT deleted (still feed the Analyze workspace + /pipeline/stats)"
  artifacts:
    - path: "pyproject.toml"
      provides: "vulture>=2.16 dev dependency"
      contains: "vulture"
    - path: "vulture_whitelist.py"
      provides: "hand-audited false-positive suppression list for the sweep"
      contains: "phaze"
    - path: "justfile"
      provides: "non-blocking `vulture` recipe documenting the sweep flags"
      contains: "vulture"
  key_links:
    - from: "justfile vulture recipe"
      to: "src/phaze + vulture_whitelist.py"
      via: "uv run vulture with --ignore-decorators + --min-confidence"
      pattern: "vulture"
---

<objective>
Add the `vulture` dead-code analyzer as a dev tool and perform a one-shot, manual-verify
full-repo dead-code sweep over `src/phaze/`, deleting only confirmed-dead code (CLEAN-02,
D-12/D-13).

Purpose: Remove vestigial dead code left after the v7.0 cutover, thoroughly and safely.
The sweep is a manual-verify one-shot (NOT a blocking CI gate) because framework-invoked
code produces false-positives that require human reachability judgment per candidate.

Output: `vulture>=2.16` in the dev group, a hand-audited `vulture_whitelist.py`, a
non-blocking `just vulture` recipe, and confirmed-dead deletions across `src/phaze/`. No
runtime/backend behavior change — the full suite stays green.

NOTE: This plan is NON-AUTONOMOUS. It contains a blocking package-legitimacy checkpoint
(slopcheck was not run at research time — vulture is treated as [ASSUMED]) and a
blocking human-verify checkpoint for the deletions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-docs-drift-gate-dead-code-sweep/66-CONTEXT.md
@.planning/phases/66-docs-drift-gate-dead-code-sweep/66-RESEARCH.md
@.planning/phases/66-docs-drift-gate-dead-code-sweep/66-PATTERNS.md

<interfaces>
<!-- Extracted from research; executor should use these directly. -->

vulture version: 2.16 (released 2026-03-25 — ~3 months old, clears exclude-newer="7 days").
  Requires Python >=3.9, supports 3.14. Canonical source repo: github.com/jendrikseipp/vulture.

pyproject.toml [dependency-groups] dev — existing alpha-sorted list ending with "ruff>=0.15.18".
  vulture sorts LAST: append `"vulture>=2.16",` after ruff. Then `uv lock && uv sync` (via `just install`).

Sweep commands (RESEARCH Deep-Dive 3):
  uv run vulture src/phaze --make-whitelist > vulture_whitelist.py    # generate, then hand-audit
  uv run vulture src/phaze vulture_whitelist.py \
    --min-confidence 80 \
    --ignore-decorators "@router.*,@app.*,@field_validator,@model_validator,@validator,@pytest.fixture"

Known this-repo false-positive sources (whitelist / ignore, do NOT delete):
  - FastAPI route handlers (@router.get/post) — --ignore-decorators
  - Pydantic validators (@field_validator/@model_validator/@validator) — --ignore-decorators
  - Transient ORM attrs: admin_agents._load_agents sets a._status; Phase 27 _agent_name, _elapsed_seconds — whitelist
  - SAQ hooks / lifespan callbacks in main.py + task settings — whitelist as needed
  - CLI entry points: phaze.entrypoint, agents-add CLI — whitelist / --ignore-names

DO-NOT-DELETE (view-adjacent but LIVE): build_dashboard_context, get_stage_progress,
  get_queue_activity — feed the Analyze workspace + /pipeline/stats poll.

Guardrail (D-12): delete a candidate only after (1) `grep -rn` for dynamic references
  (string-dispatch, getattr, template names, SAQ task names) confirms it is unreferenced AND
  (2) the full suite is green. Nothing may alter runtime behavior.

Wave note: this plan is wave 2 (depends_on 66-01) — it runs on a stable, fully-green wave-1
  tree, which the D-12 "green suite before each removal" guardrail requires. It also shares
  `justfile` ownership with plan 66-01, so it must land after it.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for vulture (blocking)</name>
  <what-built>
    Nothing installed yet. This checkpoint precedes the `uv sync` that would install vulture.
    slopcheck was not run at research time (offline sandbox), so per the package-legitimacy
    protocol vulture is treated as [ASSUMED] and must be human-verified before install.
    Threat ref: T-66-SC (supply-chain tampering via a new dependency).
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/vulture/ and confirm version 2.16 exists and lists
       "Homepage"/"Source" pointing at github.com/jendrikseipp/vulture (author: Jendrik Seipp).
    2. Confirm 2.16 was published on or around 2026-03-25 (clears the exclude-newer="7 days"
       cooldown; not a freshly-published typosquat).
    3. Confirm the package name is exactly `vulture` (not a look-alike) and it is dev-only.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the vulture install, or describe any concern (e.g. name mismatch, unexpected publish date) to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add vulture dev dependency, whitelist, and `just vulture` recipe</name>
  <read_first>
    - pyproject.toml [dependency-groups] dev (the file being modified — existing alpha-sorted list ending with ruff)
    - justfile (the group('test') / group idiom for a new non-blocking `vulture` recipe)
    - .planning/phases/66-docs-drift-gate-dead-code-sweep/66-PATTERNS.md sections "pyproject.toml" and "vulture_whitelist.py" (insert position + generate/audit mechanism)
    - .planning/phases/66-docs-drift-gate-dead-code-sweep/66-RESEARCH.md Deep-Dive 3 (flags, --min-confidence, known false-positive sources)
  </read_first>
  <action>
    Append `"vulture>=2.16",` to pyproject.toml `[dependency-groups] dev` in alphabetical
    position (after `"ruff>=0.15.18",`), then run `uv lock && uv sync` (via `just install`) to
    regenerate uv.lock and the dev venv. Generate a starter whitelist with
    `uv run vulture src/phaze --make-whitelist > vulture_whitelist.py`, then HAND-AUDIT it: remove
    any entry that is genuinely dead (those become sweep candidates in Task 3) and keep only the
    framework false-positives named in the interfaces block (transient ORM attrs `_status`/`_agent_name`/`_elapsed_seconds`,
    SAQ hooks/lifespan callbacks, CLI entry points). Add a non-blocking `just vulture` recipe (test
    group, `[doc(...)]` + `[group(...)]` idiom) whose body runs
    `uv run vulture src/phaze vulture_whitelist.py --min-confidence 80 --ignore-decorators "@router.*,@app.*,@field_validator,@model_validator,@validator,@pytest.fixture"`.
    Do NOT wire vulture into pre-commit or CI as a blocking gate (framework false-positives make it
    too noisy — RESEARCH). Keep the whitelist greppable in one file rather than scattering noqa
    comments through runtime source.
  </action>
  <verify>
    <automated>uv run vulture --version && uv run vulture src/phaze vulture_whitelist.py --min-confidence 80 --ignore-decorators "@router.*,@app.*,@field_validator,@model_validator,@validator,@pytest.fixture"; echo "vulture ran (nonzero exit lists remaining candidates for Task 3)"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run vulture --version` reports 2.16 or newer.
    - `grep -c 'vulture' pyproject.toml` >= 1 and the entry sits after `ruff` (alphabetical); `uv.lock` contains a vulture entry.
    - `vulture_whitelist.py` exists at repo root and contains only hand-audited framework false-positives (references `phaze` symbols).
    - `just vulture` runs (its nonzero exit merely lists remaining candidates — that output feeds Task 3).
    - `pre-commit run --all-files` passes (deps alphabetically sorted, TOML valid).
  </acceptance_criteria>
  <done>vulture is installed dev-only and cooldown-clean, a hand-audited whitelist exists, and a repeatable non-blocking `just vulture` recipe is committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirmed-dead sweep and deletion (manual verify per candidate)</name>
  <what-built>
    Ran `just vulture` (Task 2) to produce the candidate list. For EACH candidate, verified
    reachability before deleting: `grep -rn` across src/phaze + templates + task-name/string-dispatch
    sites for dynamic references, then confirmed the full suite is green. Deleted only candidates
    that are unreferenced by any static OR dynamic path. Explicitly did NOT delete
    build_dashboard_context / get_stage_progress / get_queue_activity (LIVE — feed the Analyze
    workspace + /pipeline/stats). Any candidate that could not be proven dead was left in place and
    (if a persistent vulture false-positive) added to vulture_whitelist.py with an inline justification.
  </what-built>
  <how-to-verify>
    1. Review the git diff of deletions: every removed symbol/file should be one the operator agrees
       is dead (no live UI, route, task, CLI, or migration path).
    2. Confirm the DO-NOT-DELETE trio (build_dashboard_context, get_stage_progress, get_queue_activity)
       is still present.
    3. Confirm the full suite is green: `just integration-test` (or `just test-bucket <affected buckets>`)
       passes and `uv run mypy .` + `uv run ruff check .` are clean.
    4. Confirm no runtime/backend behavior change — deletions are removal-only, no logic edits.
  </how-to-verify>
  <resume-signal>Type "approved" to accept the deletions, or list any symbol/file to keep (it will be restored and, if a vulture false-positive, whitelisted).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI -> dev venv | A new external package (vulture) is resolved and installed into the dev environment. |
| vulture candidate list -> source deletions | An automated tool proposes deletions; a human gates which are actually removed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-SC | Tampering | vulture install (npm/pip-class supply chain) | mitigate | slopcheck not run (offline) => vulture is [ASSUMED] => blocking `checkpoint:human-verify` (Task 1) verifies canonical source github.com/jendrikseipp/vulture + publish date before `uv sync`. Dev-only; the exclude-newer="7 days" cooldown already excludes fresh typosquats. Legitimacy checkpoint is NOT auto-approvable. |
| T-66-08 | Tampering | deleting live code as "dead" | mitigate | D-12 guardrail: grep dynamic refs + green full suite before each removal; DO-NOT-DELETE list enumerated; blocking human-verify checkpoint (Task 3) reviews the deletion diff; hand-audited whitelist prevents framework false-positive deletions. |
| T-66-09 | Denial of Service | vulture wired as a blocking gate producing false failures | accept (avoided) | vulture is explicitly NOT a blocking CI/pre-commit gate — a non-blocking `just vulture` recipe only, per D-12/D-13. |
</threat_model>

<verification>
- `uv run vulture --version` >= 2.16; `uv.lock` regenerated.
- `just vulture` runs and its candidate output was worked through in Task 3.
- After deletions: `just integration-test` (or affected buckets) green; `uv run mypy .` + `uv run ruff check .` clean; `pre-commit run --all-files` passes.
- DO-NOT-DELETE trio still present; deletions are removal-only (no logic edits).
</verification>

<success_criteria>
- vulture>=2.16 added dev-only, alphabetically, cooldown-clean, behind a passed legitimacy checkpoint (D-13, T-66-SC).
- A hand-audited vulture_whitelist.py + non-blocking `just vulture` recipe exist (D-13).
- Only confirmed-dead code removed, each verified against dynamic refs + a green suite (D-12); no runtime/backend behavior change.
</success_criteria>

<output>
Create `.planning/phases/66-docs-drift-gate-dead-code-sweep/66-03-SUMMARY.md` when done
</output>
