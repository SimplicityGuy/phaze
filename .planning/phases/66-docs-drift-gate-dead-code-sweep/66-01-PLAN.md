---
phase: 66-docs-drift-gate-dead-code-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/shared/core/test_requirements_traceability.py
  - tests/shared/core/test_dead_template_guard.py
  - justfile
  - .github/workflows/code-quality.yml
autonomous: true
requirements: [DOCS-01, CLEAN-02]
must_haves:
  truths:
    - "A passed phase (ROADMAP [x] AND {NN}-VERIFICATION.md status: passed) whose mapped requirement is left unmarked FAILS the guard with a message naming the phase+requirement (D-01, D-02, D-08)"
    - "A requirement whose checkbox state disagrees with its Traceability-table Status FAILS the guard (D-03)"
    - "A requirement marked Complete whose mapped phase is not passed FAILS the guard (D-02)"
    - "An in-flight phase (Phase 66 itself: [ ] with reqs [ ] + Status Pending) PASSES the guard (D-05)"
    - "Archived milestones (milestones/vN.M-*) are validated for internal checkbox<->table<->Complete/Deferred consistency only, never gated on missing VERIFICATION files (D-04)"
    - "Every router-captured \"...html\" literal under templates/ resolves to an on-disk file, so a dead entry-root literal fails loudly (D-14)"
    - "The guard runs on doc-only PRs via a `just docs-drift` step in the always-run code-quality job, without re-enabling CI-04-skipped heavy jobs (D-06, D-07)"
  artifacts:
    - path: "tests/shared/core/test_requirements_traceability.py"
      provides: "DOCS-01 hermetic traceability drift guard (5 drift-class assertions)"
      contains: "def test_"
    - path: "tests/shared/core/test_dead_template_guard.py"
      provides: "D-14 entry-literal-resolves assertion (added, existing test_no_orphan_templates untouched)"
      contains: "test_entry_literals_resolve_to_templates"
    - path: "justfile"
      provides: "docs-drift recipe invoking the traceability guard"
      contains: "docs-drift"
    - path: ".github/workflows/code-quality.yml"
      provides: "always-run step invoking `just docs-drift`"
      contains: "just docs-drift"
  key_links:
    - from: ".github/workflows/code-quality.yml"
      to: "tests/shared/core/test_requirements_traceability.py"
      via: "just docs-drift recipe"
      pattern: "just docs-drift"
    - from: "tests/shared/core/test_requirements_traceability.py"
      to: ".planning/REQUIREMENTS.md + .planning/ROADMAP.md + .planning/phases/*/*VERIFICATION*.md"
      via: "read_text parse-then-assert"
      pattern: "_PLANNING"
---

<objective>
Deliver the DOCS-01 docs-drift traceability guard and close the dead-template guard's
own entry-root-literal blind spot (D-14), then wire the traceability guard to run on
doc-only PRs.

Purpose: Close the manual REQUIREMENTS/ROADMAP sync gap called out across retrospectives —
a passed phase with an unmarked requirement, a checkbox that disagrees with the traceability
table, or a requirement marked Complete without a passed phase must all fail CI loudly and
precisely. Simultaneously make a dead `_STAGE_PLACEHOLDER`-shape router literal fail the
existing dead-template guard instead of silently masking an orphan.

Output: A new hermetic filesystem guard `test_requirements_traceability.py`, one new
assertion in `test_dead_template_guard.py`, a `just docs-drift` recipe, and one
`code-quality.yml` step. No runtime/backend behavior change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-docs-drift-gate-dead-code-sweep/66-CONTEXT.md
@.planning/phases/66-docs-drift-gate-dead-code-sweep/66-RESEARCH.md
@.planning/phases/66-docs-drift-gate-dead-code-sweep/66-PATTERNS.md

<interfaces>
<!-- Extracted from repo; executor should use these directly. -->

Existing guard idiom — tests/shared/core/test_docs_ia_current.py (closest analog):
  _REPO_ROOT = Path(__file__).resolve().parents[3]   # tests/shared/core/X.py -> repo root
  # pure read_text() parse; one assertion function per behavior; every assert carries a
  # precise offender message; NO phaze.* import -> hermetic (immune to get_settings lru_cache /
  # saq_jobs stub cross-test poison; passes in isolation via `just test-bucket shared`).

Existing dead-template guard — tests/shared/core/test_dead_template_guard.py:
  _REPO_ROOT = Path(__file__).resolve().parents[3]
  _TEMPLATES = _REPO_ROOT / "src" / "phaze" / "templates"
  _HTML_LITERAL = re.compile(r'''["']([^"']+\.html)["']''')
  def _entry_templates() -> set[str]: ...   # all quoted "...html" literals across routers/*.py
  def test_no_orphan_templates() -> None: ...   # DO NOT relax this

Parser regexes (verbatim, RESEARCH Deep-Dive 1 lines 132-169):
  requirement checkbox:  ^- \[([ x])\] \*\*([A-Z]+-\d+)\*\*
  traceability row:      ^\|\s*([A-Z]+-\d+)\s*\|\s*([^|]+?)\s*\|\s*([^|]+?)\s*\|
  phase-number in cell:  Phase (\d+(?:\.\d+)?)     # no match => deferred/unmapped row
  roadmap phase-pass:    ^- \[([ x])\] \*\*Phase (\d+(?:\.\d+)?)[:\s]
  verification status:   ^status:\s*passed   (fallback \*\*Status:\*\*\s*passed)

Status-vocabulary normalization: {complete, done} -> COMPLETE ; {deferred} -> DEFERRED ;
  {pending, in progress, ...} -> PENDING (case-insensitive). Active uses Complete/Pending;
  v7.0 uses Done/Deferred.

On-disk facts (VERIFIED this session):
  - Active: .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/{63,64,65}-*/{NN}-VERIFICATION.md
    (status: passed). Phase 66 has NO VERIFICATION file (in-flight).
  - Archived: .planning/milestones/vN.M-REQUIREMENTS.md / vN.M-ROADMAP.md. v7.0 has NO
    v7.0-phases/ dir and zero VERIFICATION files; v4.0 phases 39-42 have none; phases 31/46
    use bare VERIFICATION.md. => archived MUST be internal-consistency-only.
  - All 67 current router .html literals already resolve on disk (D-14 assertion green today).

CI wiring facts (VERIFIED in .github/workflows/ci.yml):
  - `quality` job (code-quality.yml) has NO `if:` gate -> runs on EVERY PR incl. doc-only.
  - `aggregate-results` requires QUALITY_RESULT == 'success' unconditionally (repo merge gate).
  - code-quality.yml already runs `just install` then `just pre-commit`; add the new step after it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the DOCS-01 traceability drift guard</name>
  <read_first>
    - tests/shared/core/test_docs_ia_current.py (analog: repo-root pathing, one-assertion-per-behavior, precise offender messages, hermetic)
    - tests/shared/core/test_dead_template_guard.py (secondary analog: parents[3] constant, small pure helpers)
    - .planning/phases/66-docs-drift-gate-dead-code-sweep/66-RESEARCH.md Deep-Dive 1 (verbatim regexes, active-vs-archived degradation rule, the 5 drift classes, Pitfalls 1-5)
    - .planning/REQUIREMENTS.md (dual encoding: per-req checkbox + Traceability Status column)
    - .planning/ROADMAP.md (phase-pass `- [x] **Phase NN...**` lines, decimal 57.1, <details>-wrapped archived sections)
    - .planning/milestones/v7.0-REQUIREMENTS.md and .planning/milestones/v4.0-REQUIREMENTS.md (archived vocab: Done/Deferred; appended phase names; deferred "Future (...)" rows)
  </read_first>
  <action>
    Create tests/shared/core/test_requirements_traceability.py as a hermetic filesystem guard
    mirroring test_docs_ia_current.py exactly: `from __future__ import annotations`, module
    globals `_REPO_ROOT = Path(__file__).resolve().parents[3]` and `_PLANNING = _REPO_ROOT / ".planning"`,
    zero `phaze.*` imports, only `re` + `pathlib`. Build small pure helpers named `_parse_requirements`
    (returns per-req {req_id: (checkbox_state, table_status_normalized, phase_number_or_None)}),
    `_parse_roadmap_phases` (returns {phase_number: checkbox_state}), and `_verification_passed`
    (globs `.planning/phases/{dir starting with NN-}/*VERIFICATION*.md`, matches `^status:\s*passed`
    in frontmatter with `\*\*Status:\*\*\s*passed` fallback; missing file OR non-passed => not passed).
    Use the verbatim regexes from the interfaces block; normalize status vocab {complete,done}->COMPLETE,
    {deferred}->DEFERRED before comparing (Pitfall 1). Rows whose phase cell yields no `Phase N` match are
    deferred/unmapped and participate ONLY in the deferred-consistency check, never phase-pass (Pitfall 2).
    Use `Phase (\d+(?:\.\d+)?)` for the 57.1 decimal insert and parse `<details>`-wrapped archived
    sections as plain markdown (Pitfall 4). Implement exactly five assertion functions, each with a
    precise D-08 offender message naming the exact phase/requirement:
      - test_active_passed_phases_have_all_requirements_marked (D-01/D-02: phase passed => every mapped
        req checkbox [x] AND table Status COMPLETE; else fail e.g. "Phase 65 passed but VER-03 checkbox [ ] unmarked")
      - test_active_marked_requirements_have_passed_phases (D-02: req marked Complete => mapped phase passed;
        else "DOCS-01 marked Complete but Phase 66 not passed")
      - test_active_checkbox_and_table_status_agree (D-03: per-req checkbox [x]<->table COMPLETE and
        [ ]<->non-COMPLETE must agree; else "table Status 'Complete' != checkbox [ ] for VER-03")
      - test_archived_milestones_internally_consistent (D-04: for each milestones/vN.M-REQUIREMENTS.md,
        assert only checkbox<->table<->Complete/Deferred internal consistency; NEVER consult VERIFICATION files)
      - test_inflight_phase_with_unmarked_requirements_passes (D-05 regression: Phase 66 [ ] with
        DOCS-01/CLEAN-01/CLEAN-02 [ ] + Pending must PASS — this guard MUST be green throughout Phase 66)
    "Active" phase-pass = ROADMAP `[x]` AND `_verification_passed(NN)` per D-01. Do NOT apply
    VERIFICATION gating to archived milestones (they lack the files — it would fail every archived req).
    No fenced code inside the test beyond the implementation itself; no markdown-lib dependency.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/core/test_requirements_traceability.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/shared/core/test_requirements_traceability.py -q` exits 0 against the CURRENT
      repo state (Phase 66 in-flight => green, proving D-05 tolerance).
    - The file contains exactly the five `def test_` functions named above.
    - The file has zero `import phaze` / `from phaze` lines (hermetic — `grep -c 'phaze' tests/shared/core/test_requirements_traceability.py` counts only doc/path strings, no import statements).
    - `just test-bucket shared` passes with the new file present (isolation-safe).
  </acceptance_criteria>
  <done>The traceability guard exists, is hermetic, passes on the current in-flight repo state, and each assertion emits a precise offender message.</done>
</task>

<task type="auto">
  <name>Task 2: Close the dead-template guard entry-literal blind spot (D-14)</name>
  <read_first>
    - tests/shared/core/test_dead_template_guard.py (the file being modified — `_entry_templates()`, `_HTML_LITERAL`, `_TEMPLATES`, `_ALLOWLIST`, and the module docstring warning against relaxing the closure)
    - .planning/phases/66-docs-drift-gate-dead-code-sweep/66-PATTERNS.md section "test_dead_template_guard.py" (exact new function body, de-risk note)
    - .planning/phases/66-docs-drift-gate-dead-code-sweep/66-RESEARCH.md Deep-Dive 3 D-14 (blind-spot description, all 67 literals resolve today)
  </read_first>
  <action>
    Add ONE new sibling assertion `test_entry_literals_resolve_to_templates` to
    test_dead_template_guard.py that consumes the existing `_entry_templates()` set as-is and
    asserts every captured `"...html"` literal resolves to an on-disk file under `_TEMPLATES`
    (i.e. `(_TEMPLATES / lit).is_file()`), collecting non-resolving literals into a sorted
    `missing` list and asserting it is empty with a message naming the dead literals and telling
    the maintainer to delete the literal or restore the template. Do NOT modify or relax
    `test_no_orphan_templates`, `_referenced_from`, `_entry_templates`, or `_ALLOWLIST`. If a
    legitimate non-template `.html` href literal is ever needed later, the documented path is a
    tiny explicit `_NON_TEMPLATE_HTML: frozenset[str]` with inline justification — do NOT add one
    now (all 67 literals resolve today, so the assertion is green with no allowlist).
  </action>
  <verify>
    <automated>uv run pytest tests/shared/core/test_dead_template_guard.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/shared/core/test_dead_template_guard.py -q` exits 0 (both the existing orphan test and the new literal-resolves test pass).
    - `grep -c 'def test_' tests/shared/core/test_dead_template_guard.py` returns 2 (existing + new).
    - `test_no_orphan_templates` body is byte-for-byte unchanged (`git diff` shows only additions, no edits to the existing function or `_ALLOWLIST`).
  </acceptance_criteria>
  <done>A dead entry-root `.html` literal now fails the guard loudly; the existing orphan-file check is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Wire `just docs-drift` into the always-run code-quality job (D-06/D-07)</name>
  <read_first>
    - justfile (the `[group('test')]` recipes ~lines 85-103: `[doc(...)]` + `[group('test')]` attribute idiom, `uv run pytest ...` body)
    - .github/workflows/code-quality.yml (the step list; emoji-prefixed names; `run: just install` / `run: just pre-commit` — workflows delegate to just, never inline shell)
    - .github/workflows/ci.yml (confirm `quality` job has no `if:` gate and `aggregate-results` requires QUALITY_RESULT==success — do NOT edit ci.yml)
    - .planning/phases/66-docs-drift-gate-dead-code-sweep/66-RESEARCH.md Deep-Dive 2 (why code-quality.yml is the clean path; do NOT add `.planning/**` trigger paths)
  </read_first>
  <action>
    Add a `docs-drift` recipe to justfile in the `test` group (mirror the existing `[doc('...')]`
    + `[group('test')]` attribute idiom): body runs `uv run pytest tests/shared/core/test_requirements_traceability.py -q`.
    Then add ONE step to .github/workflows/code-quality.yml AFTER the "Run pre-commit hooks" step
    (last step in the job), named with an emoji prefix (e.g. "🧭 Docs-drift traceability gate"),
    body `run: just docs-drift`. Do NOT add any `if:` gate to the step, do NOT edit ci.yml, and do
    NOT add `.planning/**` to any trigger path (that would break CI-04's skip-with-success for the
    heavy test/security/docker jobs — RESEARCH rejected alternative). The guard also already lives in
    the `shared` test bucket, so code PRs exercise it a second time in the `test` job (redundant, harmless).
  </action>
  <verify>
    <automated>just docs-drift</automated>
  </verify>
  <acceptance_criteria>
    - `just docs-drift` exits 0 locally.
    - `grep -c 'just docs-drift' .github/workflows/code-quality.yml` returns 1 (step present).
    - `.github/workflows/ci.yml` is unchanged (`git diff --stat` shows no ci.yml modification).
    - The new code-quality.yml step carries no `if:` condition and appears after the pre-commit step.
    - `uv run yamllint .github/workflows/code-quality.yml` passes (strict mode, per project config) — or the pre-commit yamllint hook passes.
  </acceptance_criteria>
  <done>The docs-drift guard runs on every PR (including doc-only) via the always-run quality job; CI-04's heavy-job skip-with-success is preserved untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo files -> guard parser | The guard reads only trusted in-repo `.planning/` markdown; no external/untrusted input crosses. |
| PR contents -> CI quality job | A PR author controls the docs the guard parses, but the guard only asserts internal consistency; it cannot be coerced into executing attacker input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | Tampering | traceability guard parser over `.planning/` markdown | accept | Input is trusted repo content; regex parse over `read_text` cannot execute code. No mitigation needed beyond keeping the guard dependency-free (no markdown-lib). |
| T-66-02 | Denial of Service | guard runs in the always-run quality job | accept | Pure filesystem parse of a handful of small markdown files (~sub-second); no network, no DB, bounded runtime. |
| T-66-03 | Repudiation | drift silently merged | mitigate | The whole point of the gate: `aggregate-results` requires QUALITY_RESULT==success, so a drift failure blocks merge and names the exact offender (D-08). |
| T-66-04 | Tampering | code-quality.yml wiring drifts out (step removed) | accept | Low risk; optional structural wiring-guard exists as a follow-up (RESEARCH Deep-Dive 2) but not required this phase. |
</threat_model>

<verification>
- `uv run pytest tests/shared/core/test_requirements_traceability.py tests/shared/core/test_dead_template_guard.py -q` green.
- `just docs-drift` exits 0.
- `just test-bucket shared` passes in isolation.
- `uv run ruff check tests/shared/core/test_requirements_traceability.py` and `pre-commit run --all-files` clean.
- `.github/workflows/ci.yml` unmodified; `code-quality.yml` has exactly one new `just docs-drift` step with no `if:` gate.
</verification>

<success_criteria>
- The traceability guard fails on passed-but-unmarked, marked-but-unpassed, and checkbox<->table drift, and PASSES the Phase-66 in-flight state (D-01..D-05, D-08).
- Archived milestones validated for internal consistency only, never on missing VERIFICATION files (D-04).
- A dead router `.html` literal now fails the dead-template guard (D-14); the existing orphan check is untouched.
- The guard runs on doc-only PRs via the always-run quality job without breaking CI-04 skip-with-success (D-06, D-07).
</success_criteria>

<output>
Create `.planning/phases/66-docs-drift-gate-dead-code-sweep/66-01-SUMMARY.md` when done
</output>
