---
phase: 66-docs-drift-gate-dead-code-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/routers/admin_agents.py
  - src/phaze/templates/admin/agents.html
  - tests/agents/routers/test_admin_agents.py
autonomous: true
requirements: [CLEAN-01]
must_haves:
  truths:
    - "The Agents page (/admin/agents full-page render) shows a discreet muted footer link to /saq when settings.enable_saq_ui is true (D-09, D-10)"
    - "The /saq link is absent from the rendered page when enable_saq_ui is false, so it never renders a dead 404 (D-09)"
    - "The link opens in a new tab with target=_blank and rel=noopener (reverse-tabnabbing guard) (D-11)"
    - "The polled /_table partial is unchanged — the link lives only in the full page shell"
  artifacts:
    - path: "src/phaze/routers/admin_agents.py"
      provides: "enable_saq_ui injected into the page() context via get_settings() call-site idiom"
      contains: "enable_saq_ui"
    - path: "src/phaze/templates/admin/agents.html"
      provides: "flag-gated discreet /saq footer link"
      contains: "/saq"
    - path: "tests/agents/routers/test_admin_agents.py"
      provides: "render test asserting link present/absent per flag + href/target/rel"
      contains: "enable_saq_ui"
  key_links:
    - from: "src/phaze/templates/admin/agents.html"
      to: "/saq"
      via: "flag-gated anchor in {% block content %}"
      pattern: "href=\"/saq\""
    - from: "src/phaze/routers/admin_agents.py::page"
      to: "settings.enable_saq_ui"
      via: "get_settings() call-site"
      pattern: "get_settings\\(\\)\\.enable_saq_ui"
---

<objective>
Restore a discreet in-UI link to the still-mounted `/saq` SAQ monitor on the shell's
Agents/Compute page (CLEAN-01). Presentation-only.

Purpose: Phase 62's cutover left `/saq` mounted and URL-reachable but with no shell link
(a surfaced supersession gap, v7.0 D-05). Operators currently must type the raw URL. Add a
low-weight footer link gated on the exact settings flag that gates the `/saq` mount, so the
link never dangles as a 404.

Output: One context-key addition in `admin_agents.py::page`, one flag-gated footer anchor in
`admin/agents.html`, and a render test. No backend behavior change — this is a template-context
addition and a template edit only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-docs-drift-gate-dead-code-sweep/66-CONTEXT.md
@.planning/phases/66-docs-drift-gate-dead-code-sweep/66-RESEARCH.md
@.planning/phases/66-docs-drift-gate-dead-code-sweep/66-PATTERNS.md

<interfaces>
<!-- Extracted from repo; executor should use these directly. -->

src/phaze/routers/admin_agents.py::page (lines 80-111) — the full-page handler.
  Renders `admin/agents.html` for non-HTMX loads (the shell Agents page), and
  `admin/partials/agents_table.html` for HTMX polls. Current context dict keys:
    request, agents, now, current_page, refreshed_at_iso, compute_lane_state, compute_lane_count
  The `/_table` handler (table_partial, lines 114-138) is the poll target and must NOT change.

get_settings() call-site idiom — src/phaze/routers/pipeline_scans.py:39,146:
  from phaze.config import get_settings
  ... get_settings().scan_stall_seconds ...
  (Prefer the call-site form over a module-level snapshot so the test lru_cache-clear
   fixture is respected.)

settings.enable_saq_ui — defined on BaseSettings (config.py:292), default True, alias
  PHAZE_ENABLE_SAQ_UI. It gates the `/saq` mount in main.py:160. Available on both role settings.

src/phaze/templates/admin/agents.html — {% block content %} wraps the page in
  <div class="space-y-6">...</div> (closes line 19), followed by a <script> block (lines 27-50)
  then {% endblock %} (line 51). Existing muted-text utility on line 9:
  class="text-sm text-gray-500 dark:text-gray-400" — reuse this muted palette.

Existing render-test analog — tests/agents/routers/test_admin_agents.py:
  _make_smoke_app(session) mounts admin_agents.router on a bare FastAPI app + overrides
  get_session; `smoke` fixture seeds agent rows and yields an httpx AsyncClient. Follow this
  to GET /admin/agents and assert on response text. Monkeypatch enable_saq_ui via the
  get_settings lru_cache-clear pattern (see tests/shared/core/test_main_lifespan.py).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject enable_saq_ui and add the flag-gated /saq footer link</name>
  <read_first>
    - src/phaze/routers/admin_agents.py (the file being modified — page() context dict lines 99-111; note table_partial must stay untouched)
    - src/phaze/routers/pipeline_scans.py (analog for the `from phaze.config import get_settings` call-site idiom)
    - src/phaze/templates/admin/agents.html (the file being modified — {% block content %} structure, muted-text palette on line 9)
    - .planning/phases/66-docs-drift-gate-dead-code-sweep/66-PATTERNS.md sections "admin_agents.py" and "admin/agents.html" (exact context-key placement + exact Jinja snippet)
    - src/phaze/main.py lines 134-169 (confirm enable_saq_ui gates the /saq mount — the link must mirror the same condition)
  </read_first>
  <action>
    In src/phaze/routers/admin_agents.py add `from phaze.config import get_settings` (call-site
    idiom, mirroring pipeline_scans.py — NOT a module-level settings snapshot) and add exactly one
    key to the `page()` handler's TemplateResponse context dict: `"enable_saq_ui": get_settings().enable_saq_ui`
    per D-09. Do NOT add it to the `table_partial()` (`/_table`) handler — the link lives in the full
    page shell, not the polled partial. In src/phaze/templates/admin/agents.html, inside
    `{% block content %}` after the main `<div class="space-y-6">...</div>` closes (line 19) and
    before the `<script>` block (line 27), add a discreet muted footer paragraph gated by
    `{% if enable_saq_ui %}...{% endif %}` containing an anchor `href="/saq"` with
    `target="_blank" rel="noopener"` (D-11), low visual weight reusing the existing muted
    `text-gray-400 dark:text-gray-500` palette (D-10), link text along the lines of "SAQ monitor".
    This is presentation-only: no service, router logic, or backend behavior changes.
  </action>
  <verify>
    <automated>uv run pytest tests/agents/routers/test_admin_agents.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'get_settings().enable_saq_ui' src/phaze/routers/admin_agents.py` returns 1 (in page(), not table_partial).
    - `admin/agents.html` contains `href="/saq"`, `target="_blank"`, `rel="noopener"`, and a `{% if enable_saq_ui %}` guard.
    - `table_partial` / `/_table` handler and `admin/partials/agents_table.html` are unchanged (`git diff` shows no edits there).
    - `uv run pytest tests/agents/routers/test_admin_agents.py -q` still passes (no regression in existing render tests).
    - `uv run mypy src/phaze/routers/admin_agents.py` clean.
  </acceptance_criteria>
  <done>The Agents full page renders a discreet flag-gated /saq link in a new tab; the polled partial is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Render test for the /saq link in both flag states</name>
  <read_first>
    - tests/agents/routers/test_admin_agents.py (the file being modified — _make_smoke_app + smoke fixture pattern)
    - tests/shared/core/test_main_lifespan.py (analog for monkeypatching enable_saq_ui via the get_settings lru_cache-clear pattern)
    - .planning/phases/66-docs-drift-gate-dead-code-sweep/66-RESEARCH.md CLEAN-01 section (test belongs in the agents bucket; asserts present-when-true / absent-when-false + href/target/rel)
  </read_first>
  <action>
    Add a render test to tests/agents/routers/test_admin_agents.py that GETs the full page
    `/admin/agents` (non-HTMX) twice: once with `enable_saq_ui=True` asserting the response HTML
    contains `href="/saq"` with `target="_blank"` and `rel="noopener"`, and once with
    `enable_saq_ui=False` asserting `/saq` (or `href="/saq"`) is ABSENT. Toggle the flag using the
    repo's get_settings lru_cache-clear monkeypatch idiom (test_main_lifespan.py) so the call-site
    `get_settings().enable_saq_ui` in the handler observes the override. Reuse the existing
    `_make_smoke_app` / `smoke` fixture style. The test uses a TestClient/DB fixture, so conftest
    auto-marks it `integration` and it runs in the `agents` bucket.
  </action>
  <verify>
    <automated>uv run pytest tests/agents/routers/test_admin_agents.py -k saq -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/agents/routers/test_admin_agents.py -k saq -q` exits 0 with at least one test selected.
    - The test asserts BOTH: link present (with href/target/rel) when enable_saq_ui true, AND link absent when false.
    - `just test-bucket agents` passes with the new test present.
  </acceptance_criteria>
  <done>An automated test proves the /saq link's conditional visibility and its href/target/rel attributes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser -> Agents page | Operator clicks the new footer link; opens `/saq` in a new tab. |
| shell page -> /saq embedded sub-app | `/saq` is a separate mounted SAQ sub-app with no link back to the console. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-05 | Tampering | new `target="_blank"` anchor to /saq | mitigate | `rel="noopener"` on the anchor prevents reverse-tabnabbing (the opened `/saq` tab cannot access `window.opener`) — specified in D-11 and asserted by the render test. |
| T-66-06 | Information Disclosure | surfacing /saq more broadly | accept | `/saq` is already mounted and URL-reachable (Phase 33); the link only adds discoverability, gated by `enable_saq_ui`. Access control is unchanged — reverse-proxy internal-realm auth (Phase 33 LOCKED). No new exposure. |
| T-66-07 | (UX, not security) | dead 404 link when /saq unmounted | mitigate | `{% if enable_saq_ui %}` gate (D-09) mirrors the `/saq` mount condition, so the link never renders when the sub-app is absent. |
</threat_model>

<verification>
- `uv run pytest tests/agents/routers/test_admin_agents.py -q` green (existing + new render tests).
- `just test-bucket agents` passes in isolation.
- `uv run ruff check src/phaze/routers/admin_agents.py` + `uv run mypy src/phaze/routers/admin_agents.py` clean.
- `git diff` shows no changes to `table_partial` / `admin/partials/agents_table.html`.
</verification>

<success_criteria>
- The full Agents page renders a discreet muted /saq footer link only when `enable_saq_ui` is true (D-09, D-10).
- The link opens in a new tab with `rel="noopener"` (D-11).
- The polled partial is unchanged; no backend/runtime behavior change.
</success_criteria>

<output>
Create `.planning/phases/66-docs-drift-gate-dead-code-sweep/66-02-SUMMARY.md` when done
</output>
