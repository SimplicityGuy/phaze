---
phase: 43-analyze-throughput-fix
plan: 04
type: execute
wave: 3
depends_on: ["43-01", "43-02", "43-03"]
files_modified:
  - src/phaze/services/analysis_enqueue.py
  - src/phaze/tasks/functions.py
  - tests/test_services/test_analysis_enqueue.py
  - tests/test_tasks/test_functions.py
autonomous: true
requirements: [ANALYZE-RETRY-POLICY, ANALYZE-TIMEOUT-TERMINAL, ANALYZE-WORKER-WIRING]

must_haves:
  truths:
    - "enqueue_process_file emits timeout=7200 (lowered from 14400) and keeps retries=2 (one real retry; survives apply_project_job_defaults unchanged)."
    - "An inner-timeout TimeoutError in process_file is terminal: report_analysis_failed(reason='timeout') is called and the task returns normally (SAQ COMPLETE → NO retry) — no blind re-run of a too-long file."
    - "A pebble ProcessExpired (essentia OOM/crash) is terminal the same way (reason='crashed')."
    - "On a non-timeout error, the worker reports failure only when ctx['job'] is not retryable, then re-raises (transient errors still retry once)."
    - "A successful analysis forwards the five coverage fields to put_analysis; the worker passes the inner timeout + the 60/30 caps from settings."
  artifacts:
    - path: "src/phaze/services/analysis_enqueue.py"
      provides: "timeout=7200, retries=2 enqueue policy"
      contains: "7200"
    - path: "src/phaze/tasks/functions.py"
      provides: "timeout-terminal classification + coverage forwarding + cap/inner-timeout threading"
      contains: "report_analysis_failed"
  key_links:
    - from: "src/phaze/tasks/functions.py::process_file"
      to: "run_in_process_pool(timeout, fine_cap, coarse_cap)"
      via: "kwargs passthrough to pebble schedule"
      pattern: "analysis_inner_timeout_sec"
    - from: "src/phaze/tasks/functions.py::process_file"
      to: "api.report_analysis_failed"
      via: "except TimeoutError / ProcessExpired / non-retryable Exception"
      pattern: "report_analysis_failed"
---

<objective>
Wire the worker side together: lower the SAQ `process_file` timeout to 7200s (outer safety net), keep `retries=2` (the faithful "one real retry"), and make `process_file` classify the inner pebble `TimeoutError` (and `ProcessExpired`) as terminal — reporting `ANALYSIS_FAILED` via the Plan 03 endpoint and returning normally so SAQ marks the job COMPLETE (no wasteful re-run of a deterministically-too-long file). Thread the inner timeout + 60/30 caps from settings into the pool call, and forward the five coverage fields to `put_analysis`.

Purpose: Stop the 4h-then-retry churn (72 timeouts vs 60 completions over 57h) and make outcomes terminal + visible, depending on the killable pool (01), the bounded coverage-emitting analyze_file (02), and the control-plane state/coverage/failure API (03).
Output: enqueue policy change; `process_file` terminal-classification + coverage-forwarding rewrite; tests for the timeout/crash/retry branches and the enqueue policy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-analyze-throughput-fix-bound-per-file-analysis-cost-kill-on-/43-CONTEXT.md
@.planning/phases/43-analyze-throughput-fix-bound-per-file-analysis-cost-kill-on-/43-RESEARCH.md

<interfaces>
<!-- src/phaze/services/analysis_enqueue.py:70-85 enqueue_process_file: timeout=14400 (:78) → 7200; retries=2 (:83) KEEP (do NOT set 1 — RESEARCH Pitfall 1: SAQ default 1 is clobbered to 4, and retries=1 = 0 real retries) -->

<!-- src/phaze/tasks/functions.py:114-155 process_file body. Today:
       analysis = await run_in_process_pool(ctx, _load_analyze_file(), payload.original_path, payload.models_path)
       ... builds AnalysisWritePayload(bpm, musical_key, mood, style, danceability, energy, windows) and api.put_analysis(...) -->

<!-- Plan 01: run_in_process_pool(ctx, func, *args, timeout=None, **kwargs) → pebble schedule; raises builtins.TimeoutError on kill, pebble.ProcessExpired on child crash -->
<!-- Plan 01 config: settings.analysis_inner_timeout_sec (6600), settings.analysis_fine_cap (60), settings.analysis_coarse_cap (30) -->
<!-- Plan 02 analyze_file: accepts keyword fine_cap/coarse_cap; returns + fine_windows_analyzed/total, coarse_windows_analyzed/total, sampled -->
<!-- Plan 03: api.report_analysis_failed(file_id, AnalysisFailurePayload(reason=..., error=...)); AnalysisWritePayload now accepts the 5 coverage fields -->

<!-- SAQ terminal semantics (RESEARCH §Q5): ctx["job"] available; job.attempts incremented at dequeue; retryable = retries > attempts.
     TimeoutError/ProcessExpired → return normally (COMPLETE, no retry). Other Exception → report only if not job.retryable, then re-raise. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lower SAQ process_file timeout to 7200, keep retries=2</name>
  <files>src/phaze/services/analysis_enqueue.py, tests/test_services/test_analysis_enqueue.py</files>
  <read_first>
    - src/phaze/services/analysis_enqueue.py:70-85 (the only process_file producer; timeout=14400 at :78, retries=2 at :83 + the inline comment explaining the clobber)
    - 43-RESEARCH.md §Pitfall 1 (retries=1 = 0 retries AND clobbered to 4 by apply_project_job_defaults; keep retries=2)
    - tests/test_services/test_analysis_enqueue.py (existing enqueue-policy assertions)
  </read_first>
  <behavior>
    - `enqueue_process_file` enqueues with `timeout=7200` (was 14400) and `retries=2` (unchanged).
    - The deterministic key `process_file:<file_id>` and the full payload are unchanged.
    - After `apply_project_job_defaults`, the job still carries `retries=2` (not clobbered to 4) — 2 != the SAQ default 1.
  </behavior>
  <action>In analysis_enqueue.py change `timeout=14400` to `timeout=7200` (update the inline comment: outer SAQ safety net; the inner pebble `analysis_inner_timeout_sec` does the real killing — RESEARCH §Q5). Keep `retries=2` and its comment (do NOT change to 1 — Pitfall 1). Update/extend test_analysis_enqueue.py to assert the emitted job has `timeout == 7200` and `retries == 2`, and that it survives `apply_project_job_defaults` unchanged.</action>
  <verify>
    <automated>uv run pytest tests/test_services/test_analysis_enqueue.py -q exits 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "7200" src/phaze/services/analysis_enqueue.py` shows the new timeout; `grep -c "14400" src/phaze/services/analysis_enqueue.py` == 0.
    - `grep -n "retries=2" src/phaze/services/analysis_enqueue.py` still present.
    - `uv run pytest tests/test_services/test_analysis_enqueue.py -q` exits 0.
  </acceptance_criteria>
  <done>Enqueue policy is timeout=7200 + retries=2, proven to survive the defaults hook.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Terminal classification + coverage forwarding in process_file</name>
  <files>src/phaze/tasks/functions.py, tests/test_tasks/test_functions.py</files>
  <read_first>
    - src/phaze/tasks/functions.py:114-155 (full process_file body — the run_in_process_pool call + AnalysisWritePayload build)
    - tests/test_tasks/test_functions.py (established mocks: analyze_file + pool patched via MagicMock; api as AsyncMock)
    - 43-RESEARCH.md §Q5 + §Code Examples (the inner-timeout-aware skeleton: TimeoutError/ProcessExpired terminal → return; non-retryable Exception → report+re-raise)
  </read_first>
  <behavior>
    - Pool raises `TimeoutError` → `api.report_analysis_failed(file_id, AnalysisFailurePayload(reason="timeout"))` called exactly once; `api.put_analysis` NOT called; returns `{"file_id": ..., "status": "analysis_failed"}` (normal return → SAQ COMPLETE → no retry).
    - Pool raises `pebble.ProcessExpired` → same terminal handling with `reason="crashed"`.
    - Pool raises a generic `Exception` with `ctx["job"].retryable == False` → `report_analysis_failed(reason="error")` then re-raise; with `retryable == True` → re-raise WITHOUT reporting.
    - Success path forwards the five coverage fields (`fine_windows_analyzed`/`fine_windows_total`/`coarse_windows_analyzed`/`coarse_windows_total`/`sampled`) into `AnalysisWritePayload` and calls `run_in_process_pool` with `timeout=settings.analysis_inner_timeout_sec`, `fine_cap=settings.analysis_fine_cap`, `coarse_cap=settings.analysis_coarse_cap`.
  </behavior>
  <action>Rewrite the analysis section of `process_file`: wrap the `run_in_process_pool(ctx, _load_analyze_file(), payload.original_path, payload.models_path, timeout=settings.analysis_inner_timeout_sec, fine_cap=settings.analysis_fine_cap, coarse_cap=settings.analysis_coarse_cap)` call in try/except. `except TimeoutError:` → `await api.report_analysis_failed(payload.file_id, AnalysisFailurePayload(reason="timeout"))`; `return {"file_id": str(payload.file_id), "status": "analysis_failed"}`. `except ProcessExpired:` (import from pebble) → same with `reason="crashed"`. `except Exception:` → `job = ctx.get("job")`; `if job is not None and not job.retryable: await api.report_analysis_failed(payload.file_id, AnalysisFailurePayload(reason="error", error=...truncated str(exc)))`; `raise`. On success, extend the `AnalysisWritePayload(...)` build with the five coverage fields read from the `analysis` dict (`analysis.get("fine_windows_analyzed")` etc.). Import `AnalysisFailurePayload` and `settings`. Add tests in test_functions.py for each branch (timeout terminal, ProcessExpired terminal, retryable vs non-retryable generic error, coverage forwarded on success) using the existing AsyncMock/patch patterns — NO real essentia, NO real pebble.</action>
  <verify>
    <automated>uv run pytest tests/test_tasks/test_functions.py -q exits 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "report_analysis_failed|ProcessExpired|analysis_inner_timeout_sec|fine_cap" src/phaze/tasks/functions.py` shows the terminal handling + cap/timeout threading.
    - `grep -n "fine_windows_total" src/phaze/tasks/functions.py` shows coverage forwarded to the payload.
    - `uv run pytest tests/test_tasks/test_functions.py -q` exits 0 (timeout / crashed / retryable / non-retryable / coverage cases).
    - `uv run mypy src/phaze/tasks/functions.py` clean.
  </acceptance_criteria>
  <done>process_file treats TimeoutError/ProcessExpired as terminal (report + COMPLETE, no retry), reports non-retryable generic errors, forwards coverage, and threads the inner timeout + caps from settings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SAQ job → worker task body | job payload + retry state (ctx["job"]) drive terminal classification |
| worker task → control API | failure/coverage reported over HTTP (mitigations in Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-08 | Denial of Service | blind-retry of a deterministically-too-long file (up to 12h slot waste/file) | mitigate | TimeoutError/ProcessExpired terminal → normal return (COMPLETE, no retry); transient errors retry only once (retries=2) |
| T-43-09 | Tampering | unbounded `error` string forwarded from worker exception text | mitigate | truncate `str(exc)` before sending; control-side `AnalysisFailurePayload.error max_length=2000` (Plan 03) is the authoritative bound |
| T-43-10 | Denial of Service | unbounded outer job timeout pinning a slot | mitigate | SAQ `process_file` timeout lowered 14400→7200; inner pebble timeout (6600) kills first |
</threat_model>

<verification>
- `uv run pytest tests/test_tasks/test_functions.py tests/test_services/test_analysis_enqueue.py -q` green (boundary-mocked; no essentia/pebble children).
- Full suite + coverage gate at wave merge: `uv run pytest --cov --cov-report=term-missing` (>=85%).
- `uv run mypy .` / `uv run ruff check .` clean; `pre-commit run --all-files` passes (NEVER --no-verify).
- Manual (homelab, post-redeploy — 43-VALIDATION manual table): a >4h set now completes in minutes; a wedged job hits the inner timeout and frees its slot (CPU/slot drops).
</verification>

<success_criteria>
- Enqueue policy bounded to 7200s with one real retry.
- Inner-timeout and crash outcomes are terminal and reported (no wasteful re-runs); transient errors retry once.
- Successful analyses forward coverage and run under the 60/30 caps + inner timeout.
</success_criteria>

<output>
Create `.planning/phases/43-analyze-throughput-fix-bound-per-file-analysis-cost-kill-on-/43-04-SUMMARY.md` when done.
</output>
