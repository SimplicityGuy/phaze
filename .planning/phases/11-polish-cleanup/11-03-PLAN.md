---
phase: 11-polish-cleanup
plan: 03
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified:
  - .planning/phases/10-ci-config-bug-fixes/10-VALIDATION.md
autonomous: true
requirements: [APR-02, ING-05, EXE-01]
gap_closure: true

must_haves:
  truths:
    - "pre-commit run --all-files passes with zero failures"
    - "Full test suite passes"
    - "Phase 10 VALIDATION.md exists and is Nyquist compliant"
  artifacts:
    - path: ".planning/phases/10-ci-config-bug-fixes/10-VALIDATION.md"
      provides: "Phase 10 Nyquist validation"
      contains: "nyquist_compliant: true"
  key_links: []
---

<objective>
Create Phase 10 Nyquist VALIDATION.md, run final pre-commit and test suite validation to confirm all Phase 11 changes are clean.

Purpose: Ensure all code and documentation changes pass quality gates and Nyquist validation is complete for all phases.
Output: Phase 10 VALIDATION.md, confirmed green pre-commit and test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/11-polish-cleanup/11-01-SUMMARY.md
@.planning/phases/11-polish-cleanup/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 10 Nyquist VALIDATION.md and run final validation</name>
  <files>
    .planning/phases/10-ci-config-bug-fixes/10-VALIDATION.md
  </files>
  <read_first>
    .planning/phases/10-ci-config-bug-fixes/10-01-PLAN.md,
    .planning/phases/10-ci-config-bug-fixes/10-01-SUMMARY.md,
    .planning/phases/10-ci-config-bug-fixes/10-VERIFICATION.md,
    .planning/phases/09-pipeline-orchestration/09-VALIDATION.md
  </read_first>
  <action>
    **Create Phase 10 VALIDATION.md (audit item 10):**

    Create `.planning/phases/10-ci-config-bug-fixes/10-VALIDATION.md` following the same structure as the Phase 9 VALIDATION.md. The file should contain:

    Frontmatter:
    ```yaml
    ---
    phase: 10
    slug: ci-config-bug-fixes
    status: approved
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-03-30
    ---
    ```

    Content sections:
    - **Test Infrastructure**: Same as Phase 9 (pytest 8.x with pytest-asyncio, pyproject.toml config, same commands)
    - **Sampling Rate**: Same as Phase 9
    - **Per-Task Verification Map**: Based on Phase 10 plan tasks — reference the actual test files that were created/used. Phase 10 had 3 tasks in 1 plan covering INF-03. The test commands are `uv run pytest tests/ -x -q` (full suite). Mark all statuses as `green`.
    - **Wave 0 Requirements**: Mark as complete — all test files already exist
    - **Manual-Only Verifications**: `pre-commit run --all-files` passes (verified by Phase 10 execution)
    - **Validation Sign-Off**: All boxes checked, Approval: approved

    **Run final validation:**

    After creating the file, run:
    1. `uv run pytest tests/ -x -q` — full test suite must pass
    2. `uv run ruff check .` — no linting errors
    3. `uv run ruff format --check .` — formatting clean
    4. `uv run mypy .` — type checking passes
    5. `pre-commit run --all-files` — all hooks green

    If any failures, fix the issues in the relevant files before completing.
  </action>
  <verify>
    <automated>cd /Users/Robert/Code/public/phaze && uv run pytest tests/ -x -q && pre-commit run --all-files</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/10-ci-config-bug-fixes/10-VALIDATION.md exists
    - grep "nyquist_compliant: true" .planning/phases/10-ci-config-bug-fixes/10-VALIDATION.md returns match
    - uv run pytest tests/ -x -q exits 0
    - pre-commit run --all-files exits 0
  </acceptance_criteria>
  <done>
    Phase 10 VALIDATION.md exists with nyquist_compliant: true.
    Full test suite passes.
    pre-commit run --all-files passes with zero failures.
    All Phase 11 success criteria met.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -x -q` — all tests pass
- `pre-commit run --all-files` — zero failures
- Phase 10 VALIDATION.md exists with nyquist_compliant: true
</verification>

<success_criteria>
1. Phase 10 VALIDATION.md exists and is Nyquist compliant
2. Full test suite passes
3. pre-commit run --all-files passes with zero failures
4. All 10 Phase 11 success criteria from ROADMAP are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/11-polish-cleanup/11-03-SUMMARY.md`
</output>
