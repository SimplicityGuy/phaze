---
phase: 75-engineering-hygiene-guard-hardening-tech-debt-stale-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - docker-compose.yml
autonomous: true
requirements: [HYG-01, HYG-02, HYG-03, HYG-05]
must_haves:
  truths:
    - "HYG-01 is recorded as already-satisfied by PR #207 (ec80a53a) in REQUIREMENTS.md + ROADMAP text — NO new code, NO new test (D-01/D-02)"
    - "HYG-03 is recorded as SUPERSEDED by Phase 72 D-03 in REQUIREMENTS.md + ROADMAP + STATE deferred row — NO src change (D-05/D-06/D-07)"
    - "The two stale cloud_target/Phase-67 breadcrumb comment sentences are removed from docker-compose.yml; the backends.toml mount explainer stays intact (D-03/D-04, HYG-02)"
    - "ALL THREE open 2026.7.1 deferred-items rows are reconciled: HYG-02 comment-cleanup row (~L245) → resolved citing this phase's comment deletion; HYG-03 >1-compute row (~L246) → superseded (Phase 72 D-03); HYG-04 force-local-test row (~L247) → resolved citing the regression test in 75-02. The 70-UAT deployment-gated row stays deferred"
    - "STATE deferred-items 2026.7.0 rows are reconciled: 63-UAT complete + quick-tasks 260628-wzq / 260629-eev complete (D-11, HYG-05)"
    - "`just docs-drift` exits 0 after every reconciliation edit"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "HYG-01 disposition = satisfied (PR #207); HYG-03 disposition = superseded (Phase 72 D-03)"
      contains: "ec80a53a"
    - path: "docker-compose.yml"
      provides: "cloud_target/Phase-67 breadcrumbs deleted, backends.toml explainer kept"
    - path: ".planning/STATE.md"
      provides: "reconciled 2026.7.0 deferred-items table (63-UAT, quick-tasks) AND all three open 2026.7.1 rows (HYG-02, HYG-03, HYG-04)"
  key_links:
    - from: "docker-compose.yml"
      to: "git grep guard"
      via: "no cloud_target / Phase 67 reference remains"
      pattern: "cloud_target|Phase 67"
    - from: ".planning/REQUIREMENTS.md"
      to: "just docs-drift"
      via: "traceability guard stays green"
      pattern: "docs-drift"
---

<objective>
Reconcile the four no-code hygiene items that close milestone 2026.7.2: mark HYG-01 already-satisfied,
HYG-03 superseded, delete the two stale docker-compose breadcrumb comments (HYG-02), and reconcile the
stale 2026.7.0 tracking rows (HYG-05). Three of these (HYG-01, HYG-03, HYG-05) are documentation/tracking
edits only; HYG-02 deletes two inert comment lines from docker-compose.yml. As part of the reconciliation
this plan ALSO clears all three still-`open` rows in the STATE.md 2026.7.1 deferred-items table (HYG-02,
HYG-03, and HYG-04 — the last citing the regression test that plan 75-02 lands) so the phase's stated
purpose (reconcile stale tracking) is actually met and no row is left dangling. There is NO `src/` behavior
change and NO new test in this plan.

Purpose: Close-out hygiene — record where shipped code already overtook the requirement premises, so the
milestone-close `git rm REQUIREMENTS.md` path stays green and the tracking reflects reality.
Output: Reconciled REQUIREMENTS.md / ROADMAP.md / STATE.md + a cleaned docker-compose.yml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/75-engineering-hygiene-guard-hardening-tech-debt-stale-tracking/75-CONTEXT.md
@.planning/phases/75-engineering-hygiene-guard-hardening-tech-debt-stale-tracking/75-RESEARCH.md

<critical_reconciliation_facts>
These are LOCKED user decisions (CONTEXT.md D-01..D-11). Do NOT relitigate; do NOT implement the stale
requirement text literally.

- HYG-01 — already satisfied by PR #207 (`ec80a53a`, 2026-07-05). `tests/shared/core/test_requirements_traceability.py`
  defines `_NO_ACTIVE_MILESTONE = not _REQUIREMENTS.exists()` (~L64) and skipif-gates the active-milestone
  tests (~L256/L265/L275, plus a 4th at ~L327). The `git rm REQUIREMENTS.md` close path already stays green.
  NO new code, NO new test. (D-01/D-02)
- HYG-03 — SUPERSEDED. The `>1`-compute fail-fast this req asks to "promote to boot-time" was DELETED by
  Phase 72 (D-03) to enable N-compute — the entire milestone deliverable. Re-adding it would break Phases
  72-74's shipped/verified behavior. The correct boot guard already exists: `config.py:_validate_registry`
  rejects a duplicate `agent_ref`. Reconcile to SUPERSEDED citing Phase 72 D-03. NO code change; any `src/`
  diff here is a red flag. (D-05/D-06/D-07)
- HYG-02 — delete ONLY the "Replaces the removed cloud_target selector + flat …fields (Phase 67 …)"
  breadcrumb sentence in both the `api` and `worker` services. There is NO `PHAZE_CLOUD_TARGET` env line
  anywhere (git grep clean) — the deferred-item "env + comment lines" wording is inaccurate; only the two
  comments exist. Keep the backends.toml mount explainer. (D-03/D-04)
- HYG-05 — the quick-task SUMMARY files already carry `status: complete` in frontmatter; the reconciliation
  target is the STATE.md 2026.7.0 deferred-items table rows (63-UAT `partial`, quick-tasks `missing`). (D-11)
- 2026.7.1 deferred table — the phase's whole purpose is reconciling stale tracking, so the three still-`open`
  tech-debt/test-coverage rows in the STATE.md 2026.7.1 table MUST all be cleared: HYG-02 (`docker-compose
  PHAZE_CLOUD_TARGET comments`) → resolved by this phase's comment deletion; HYG-03 (`>1`-compute lazy
  fail-fast) → superseded; HYG-04 (`force-local gate regression test`) → resolved by the committed test in
  plan 75-02. Only the 70-UAT deployment-gated row stays deferred.
</critical_reconciliation_facts>

<line_anchors>
Confirmed against the live worktree this session — re-grep before editing (line numbers may drift; anchor on
row CONTENT, not the line number):
- docker-compose.yml `api` breadcrumb: L24-25 ("Replaces the removed cloud_target selector + flat s3_*/kube_*/compute_* fields (Phase 67, no back-compat). Mount a backends.toml to enable cloud backends.") — KEEP explainer L22-23.
- docker-compose.yml `worker` breadcrumb: L52 ("Replaces the removed cloud_target selector + flat fields (Phase 67, no back-compat).") — KEEP explainer L50-51.
- REQUIREMENTS.md: HYG-01 L24, HYG-02 L25, HYG-03 L26, HYG-05 L28; Traceability rows L64/L65/L66/L68.
- ROADMAP.md: bullet L25; Phase-75 success criteria L1100 (HYG-01) / L1102 (HYG-03); Notes L1106; backlog entry L1118 (HYG-01).
- STATE.md: 2026.7.0 deferred-items table rows L234 (63-UAT partial) / L235 (260628-wzq missing) / L236 (260629-eev missing); 2026.7.1 deferred rows L244 (70-UAT deployment-gated — LEAVE) / L245 (HYG-02 tech_debt, `docker-compose PHAZE_CLOUD_TARGET comments`) / L246 (HYG-03 tech_debt, `>1-compute-backend fail-fast is lazy`) / L247 (HYG-04 test_coverage, `force-local gate regression test`).
</line_anchors>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile HYG-01 (satisfied) + HYG-03 (superseded) requirement dispositions AND clear all three open 2026.7.1 STATE deferred rows (HYG-02/03/04)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (HYG-01/HYG-03 descriptions L24/L26; Traceability table L55-74)
    - .planning/ROADMAP.md (bullet L25; Phase-75 detail L1094-1106; backlog L1118)
    - .planning/STATE.md (2026.7.1 deferred-items table L242-249 — 70-UAT row L244, HYG-02 row L245, HYG-03 row L246, HYG-04 row L247)
    - .planning/phases/75-.../75-CONTEXT.md (D-01, D-02, D-05, D-06, D-07, D-09 — authoritative dispositions)
  </read_first>
  <action>
    Record the two stale-premise requirement dispositions as DOCUMENTATION reconciliation, and clear the three
    open 2026.7.1 STATE deferred rows. NO code, NO test, NO `src/` edit.

    HYG-01 → already-satisfied: In REQUIREMENTS.md, annotate the HYG-01 description (L24) to state the
    premise ("reads REQUIREMENTS.md with no existence check") was overtaken and the guard already fails-clean
    via `_NO_ACTIVE_MILESTONE` skipif landed in PR #207 (`ec80a53a`); no additional regression test is needed
    (the module-level skip + `test_archived_milestones_internally_consistent` covers the no-active-milestone
    case). Mirror the same "satisfied by PR #207" correction on ROADMAP.md success-criterion #1 (L1100) and on
    the backlog entry (L1118) so neither still reads as an open follow-up. Confirm the wording keeps the
    milestone-close `git rm REQUIREMENTS.md` path green.

    HYG-03 → SUPERSEDED: In REQUIREMENTS.md, annotate the HYG-03 description (L26) to state the `>1`-compute
    fail-fast it asks to "promote to boot-time" was DELETED by Phase 72 (D-03) to enable N-compute (MCOMP-01),
    that re-adding it would break Phases 72-74, and that the correct boot guard (duplicate-`agent_ref` reject
    in `config.py:_validate_registry`) already exists — disposition SUPERSEDED, no code change. Correct the
    matching stale premise on ROADMAP.md success-criterion #3 (L1102) and the Notes line (L1106).

    STATE.md 2026.7.1 deferred-items table reconciliation (block ~L242-247, re-grep — anchor on row CONTENT):
    close all three still-`open` rows in that table in one pass.
      - HYG-02 row (~L245, `docker-compose PHAZE_CLOUD_TARGET comments (Phase 67)`): flip `open` → resolved/
        complete, noting the two breadcrumb comments are DELETED by this phase (Task 2 of this plan); git grep
        for `cloud_target`/`Phase 67` in docker-compose.yml is clean.
      - HYG-03 row (~L246, `>1-compute-backend fail-fast is lazy, not boot-time`): flip `open` → superseded,
        citing Phase 72 D-03 and the shipped N-compute capability (the correct duplicate-`agent_ref` boot guard
        already lives in `_validate_registry`).
      - HYG-04 row (~L247, `force-local gate regression test (Phase 71, W2)`): flip `open` → resolved/complete,
        citing the committed regression test added in plan 75-02 (`tests/shared/routers/test_pipeline.py`,
        3 gate sites L396/L718/L793 + a force-local-False control).
      - Do NOT touch the 70-UAT deployment-gated row (~L244) — it stays deferred.

    Exact status-column wording in the REQUIREMENTS.md Traceability table (L64/L66) is your discretion per
    CONTEXT — record the satisfied/superseded disposition. HARD CONSTRAINT: `just docs-drift` must exit 0 after
    the edits. If flipping the Traceability status or the `- [ ]` checkbox to `[x]` trips the guard's
    "flagged-`[x]`-req-not-in-a-passed-phase" check, keep the checkbox/status as-is and carry the disposition
    in the description prose + a Coverage note — the standard phase-completion flow flips checkboxes later.
    Do NOT introduce duplicate Traceability rows (PR #199 guard fails loud on duplicates).
  </action>
  <verify>
    <automated>uv run pytest tests/shared/core/test_requirements_traceability.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "ec80a53a" .planning/REQUIREMENTS.md` returns at least one hit (HYG-01 satisfied disposition present).
    - `git grep -n -i "supersed" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md` shows the HYG-03 disposition recorded in all three files, each citing Phase 72 (D-03).
    - `git grep -n "Phase 72" .planning/STATE.md` shows the reconciled HYG-03 deferred row.
    - The STATE.md 2026.7.1 deferred table no longer shows the HYG-02 row (`docker-compose PHAZE_CLOUD_TARGET comments`) or the HYG-04 row (`force-local gate regression test`) as `open` — HYG-02 reads resolved (comment deletion) and HYG-04 reads resolved citing 75-02's regression test.
    - `git grep -n "75-02" .planning/STATE.md` (or the plan/test path) appears in the reconciled HYG-04 row.
    - The 70-UAT deployment-gated row in the 2026.7.1 table is UNCHANGED (`git grep -n "70-UAT" .planning/STATE.md` still reads deployment-gated).
    - `git diff --stat -- src/` is EMPTY (no source change for HYG-01/HYG-03).
    - `uv run pytest tests/shared/core/test_requirements_traceability.py -q` exits 0 (docs-drift green).
    - No duplicate rows added to the REQUIREMENTS.md Traceability table.
  </acceptance_criteria>
  <done>HYG-01 recorded satisfied (PR #207) and HYG-03 recorded superseded (Phase 72 D-03) across REQUIREMENTS.md, ROADMAP.md, STATE.md; all three open 2026.7.1 STATE rows (HYG-02 resolved, HYG-03 superseded, HYG-04 resolved-via-75-02) cleared; 70-UAT row untouched; docs-drift green; zero src diff.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the two stale cloud_target/Phase-67 breadcrumb comments from docker-compose.yml (HYG-02)</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (api service block L18-27; worker service block L48-53)
    - .planning/phases/75-.../75-CONTEXT.md (D-03, D-04; RESEARCH Pitfall 1)
  </read_first>
  <action>
    Remove ONLY the stale breadcrumb sentence "Replaces the removed cloud_target selector + flat
    s3_*/kube_*/compute_* fields (Phase 67, no back-compat)." from the `api` service (currently L24 and its
    continuation onto L25) and the equivalent one-line breadcrumb "Replaces the removed cloud_target selector
    + flat fields (Phase 67, no back-compat)." in the `worker` service (currently L52). KEEP the surrounding
    backends.toml mount explainer intact ("backends.toml mounted at PHAZE_BACKENDS_CONFIG_FILE …", api L22-23 /
    worker L50-51). The trailing operator-guidance sentence "Mount a backends.toml to enable cloud backends."
    (api, currently tail of L25) is operator-useful explainer — keep it; delete only the Phase-67/cloud_target
    reference. There is NO `PHAZE_CLOUD_TARGET` env key to delete (git grep clean) — comments only. No service,
    env, volume, or command semantics change. (The STATE.md 2026.7.1 HYG-02 row that tracks this deletion is
    flipped to resolved in Task 1 of this plan — no STATE edit is needed here.)
  </action>
  <verify>
    <automated>bash -c 'git grep -n -E "cloud_target|Phase 67" -- docker-compose.yml; test -z "$(git grep -n -E "cloud_target|Phase 67" -- docker-compose.yml)"'</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n -E "cloud_target|Phase 67" -- docker-compose.yml` returns CLEAN (zero lines).
    - `git grep -n "PHAZE_BACKENDS_CONFIG_FILE" -- docker-compose.yml` still returns the api + worker explainer lines (explainer preserved).
    - `git diff -- docker-compose.yml` shows ONLY comment-line deletions — no change to any `image:`, `environment:`, `volumes:`, `command:`, or service key.
    - `docker compose config -q` (or `docker compose config >/dev/null`) parses without error if the docker CLI is available; otherwise YAML remains valid (no structural edit made).
  </acceptance_criteria>
  <done>Both cloud_target/Phase-67 breadcrumb comments deleted; backends.toml explainer intact; git grep clean; no non-comment diff.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile stale 2026.7.0 tracking rows in STATE.md (HYG-05)</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (2026.7.0 deferred-items table L228-236: 63-UAT L234, 260628-wzq L235, 260629-eev L236)
    - .planning/quick/260628-wzq-fix-job-env-contract-inject-pod-runtime-/SUMMARY.md (frontmatter already `status: complete`)
    - .planning/quick/260629-eev-convert-the-two-ascii-architecture-at-a/SUMMARY.md (frontmatter already `status: complete`)
    - .planning/phases/75-.../75-CONTEXT.md (D-11)
  </read_first>
  <action>
    Reconcile the STATE.md 2026.7.0 deferred-items table (the block introduced at ~L228). Flip the `63-UAT`
    row (L234) status `partial` → complete, noting 0 pending scenarios / parallel-CI shipped in PR #193. Flip
    the `260628-wzq` (L235) and `260629-eev` (L236) rows status `missing` → complete, noting both are already
    committed (`5f43aa7` and `267109b` respectively) and both quick-task SUMMARY.md files already carry
    `status: complete`. This is status reconciliation only — do NOT touch the quick-task SUMMARY files (already
    complete), do NOT re-run or re-commit the underlying work. The 2026.7.1 deferred rows (HYG-02/03/04) are
    reconciled in Task 1; do NOT edit them here. Keep `just docs-drift` green.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/core/test_requirements_traceability.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The STATE.md 2026.7.0 deferred table no longer shows `63-UAT` as `partial`, `260628-wzq` as `missing`, or `260629-eev` as `missing` — all three read as reconciled/complete with a cited commit or PR.
    - `git grep -n "260628-wzq\|260629-eev" .planning/STATE.md` shows the committed SHAs (`5f43aa7`, `267109b`) in the reconciled rows.
    - The two quick-task SUMMARY.md files are UNCHANGED (`git diff --stat -- .planning/quick/` shows no SUMMARY edits).
    - `uv run pytest tests/shared/core/test_requirements_traceability.py -q` exits 0.
  </acceptance_criteria>
  <done>63-UAT flipped complete and both quick-tasks flipped complete in STATE.md; quick SUMMARY files untouched; docs-drift green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced. This plan edits only `.planning/` documentation, tracking, and two
inert comment lines in `docker-compose.yml`. No new endpoint, input, parser, auth, session, or crypto path
is added; there is no `src/` behavior change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-01-NA | — | docs/comment/tracking edits | accept | No new attack surface — no endpoints/inputs/auth/crypto, no runtime behavior change (RESEARCH Security Domain: N/A). HYG-02 deletes comments only; there was never a live `PHAZE_CLOUD_TARGET` env consumed (`git grep` clean, `extra=ignore` drops nothing). |
| T-75-SC | Tampering | package installs | accept | This plan installs ZERO packages (no npm/pip/cargo). No legitimacy gate applies. |
</threat_model>

<verification>
- `uv run pytest tests/shared/core/test_requirements_traceability.py -q` exits 0 (docs-drift guard).
- `git grep -n -E "cloud_target|Phase 67" -- docker-compose.yml` is CLEAN.
- `git diff --stat -- src/` is EMPTY (no source change anywhere in this plan).
- REQUIREMENTS.md / ROADMAP.md / STATE.md carry the satisfied (HYG-01) and superseded (HYG-03) dispositions.
- STATE.md 2026.7.1 deferred table shows HYG-02/HYG-03/HYG-04 rows reconciled and the 70-UAT row still deferred.
</verification>

<success_criteria>
- HYG-01 recorded satisfied by PR #207; HYG-03 recorded superseded by Phase 72 D-03 (both no-code).
- HYG-02 breadcrumbs deleted, backends.toml explainer preserved, git grep clean.
- HYG-05 STATE.md 2026.7.0 rows reconciled (63-UAT complete; both quick-tasks complete).
- All three open 2026.7.1 STATE rows cleared (HYG-02 resolved, HYG-03 superseded, HYG-04 resolved-via-75-02); 70-UAT stays deferred.
- `just docs-drift` green; zero `src/` diff.
</success_criteria>

<output>
Create `.planning/phases/75-engineering-hygiene-guard-hardening-tech-debt-stale-tracking/75-01-SUMMARY.md` when done
</output>
</content>
</invoke>
