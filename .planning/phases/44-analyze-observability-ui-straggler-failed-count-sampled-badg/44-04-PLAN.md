---
phase: 44-analyze-observability-ui-straggler-failed-count-sampled-badg
plan: 04
type: execute
wave: 3
depends_on:
  - "44-02"
  - "44-03"
files_modified:
  - src/phaze/routers/pipeline.py
  - src/phaze/routers/proposals.py
  - src/phaze/templates/pipeline/dashboard.html
  - src/phaze/templates/pipeline/partials/straggler_failed_card.html
  - src/phaze/templates/proposals/partials/sampled_badge.html
  - src/phaze/templates/proposals/partials/analysis_timeline.html
  - tests/test_routers/test_pipeline.py
  - tests/test_routers/test_proposals.py
autonomous: true
requirements:
  - "sampled badge driven by the coverage fields"
  - "dashboard straggler/ANALYSIS_FAILED count + list — dashboard surface (card + context wiring)"
  - "regression tests for the new reads + re-trigger — render/context tests"
must_haves:
  truths:
    - "The pipeline dashboard shows a straggler count and an ANALYSIS_FAILED count, refreshed by the existing 5s /pipeline/stats poll"
    - "A file whose analysis.sampled is true renders a 'sampled — more data available' badge with the four coverage counts in the tooltip; NULL/false sampled renders nothing (never an error)"
    - "The sampled-file timeline view exposes a 'Deepen analysis' button that POSTs to the deepen endpoint"
    - "All new dashboard context values come from the Plan-02 degrade-safe service reads (the router adds no try/except — the service owns the never-500 degrade)"
  artifacts:
    - path: "src/phaze/templates/pipeline/partials/straggler_failed_card.html"
      provides: "dashboard straggler + ANALYSIS_FAILED count card"
      contains: "straggler"
    - path: "src/phaze/templates/proposals/partials/sampled_badge.html"
      provides: "render-if-sampled badge with coverage tooltip"
      contains: "sampled"
    - path: "src/phaze/routers/pipeline.py"
      provides: "dashboard() + pipeline_stats_partial() seed straggler/failed counts into context"
      contains: "get_straggler_count"
  key_links:
    - from: "src/phaze/routers/pipeline.py (dashboard + stats partial)"
      to: "src/phaze/services/pipeline.py (Plan 02 reads)"
      via: "get_straggler_count / get_analysis_failed_count seeded into the template context"
      pattern: "get_straggler_count"
    - from: "src/phaze/templates/proposals/partials/analysis_timeline.html"
      to: "src/phaze/templates/proposals/partials/sampled_badge.html"
      via: "{% include %} gated on analysis.sampled + deepen button POST"
      pattern: "sampled_badge"
---

<objective>
Surface the Plan-02 service reads and the Plan-03 deepen action in the UI: (1) a dashboard
card showing the straggler + ANALYSIS_FAILED counts (wired through the existing 5s poll
context); (2) a render-if-present "sampled" badge on the per-file analysis view, driven by
analysis.sampled with the four coverage counts in the tooltip; (3) a "Deepen analysis" button
on the sampled-file timeline that POSTs to the Plan-03 endpoint.

Purpose: Make Phase 43's recorded truth (failed / straggling / sampled) visible and actionable
to the operator. No new analysis math, no new service reads.
Output: dashboard card partial, sampled badge partial, timeline integration, router context
wiring, render/context tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-analyze-observability-ui-straggler-failed-count-sampled-badg/44-CONTEXT.md
@.planning/phases/44-analyze-observability-ui-straggler-failed-count-sampled-badg/44-PATTERNS.md
@.planning/phases/44-analyze-observability-ui-straggler-failed-count-sampled-badg/44-02-SUMMARY.md
@.planning/phases/44-analyze-observability-ui-straggler-failed-count-sampled-badg/44-03-SUMMARY.md

<interfaces>
From src/phaze/services/pipeline.py (Plan 02):
  async get_straggler_count(session, threshold_sec) -> int
  async get_analysis_failed_count(session) -> int
  async get_analysis_failed_files(session) -> list[FileRecord]
  (all degrade-safe — the router adds NO try/except, per the established wiring comment idiom at routers/pipeline.py:175-178)
  settings.straggler_threshold_sec is the threshold

From src/phaze/routers/pipeline.py:
  dashboard() (~312-364) and pipeline_stats_partial() (~367-401) both build a context dict and spread **activity / **dag_ctx; both share the 5s poll — add the new counts to BOTH
  The service-owns-degrade wiring comment idiom (175-178): no try/except wraps the call

From src/phaze/templates/pipeline/dashboard.html:
  {% include %} card slots (lines 13-23); the #pipeline-stats block hx-get="/pipeline/stats" hx-trigger="every 5s"

From src/phaze/routers/proposals.py::proposal_timeline (252-292):
  already resolves proposal -> file_id and renders proposals/partials/analysis_timeline.html with context (has_windows, bpm_points, ...). This route must ALSO fetch the AnalysisResult for that file_id (select(AnalysisResult).where(AnalysisResult.file_id == file_id), scalar_one_or_none) and pass analysis (its sampled + four coverage counts) + file_id into the context for the badge + deepen button.

From src/phaze/models/analysis.py (AnalysisResult):
  sampled: bool | None; fine_windows_analyzed/total: int | None; coarse_windows_analyzed/total: int | None

Badge analog: src/phaze/templates/tracklists/partials/confidence_badge.html (the render-if-present, else-nothing pill). Card analog: the {% include %} slots already on dashboard.html.

Deepen endpoint (Plan 03): POST /pipeline/files/{file_id}/deepen
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire straggler/failed counts into dashboard + stats-partial context; add the dashboard card partial</name>
  <files>src/phaze/routers/pipeline.py, src/phaze/templates/pipeline/dashboard.html, src/phaze/templates/pipeline/partials/straggler_failed_card.html, tests/test_routers/test_pipeline.py</files>
  <read_first>
    - src/phaze/routers/pipeline.py (dashboard() ~312-364 and pipeline_stats_partial() ~367-401 — the context-dict build + **activity/**dag_ctx spread; the service-owns-degrade wiring comment at 175-178; the existing get_*-import block at lines 24-43)
    - src/phaze/templates/pipeline/dashboard.html (the {% include %} card slots at lines 13-23 and the #pipeline-stats poll block)
    - src/phaze/services/pipeline.py (the Plan-02 get_straggler_count / get_analysis_failed_count signatures)
    - .planning/phases/44-analyze-observability-ui-straggler-failed-count-sampled-badg/44-PATTERNS.md (dashboard-wiring + straggler/failed-card assignment sections)
  </read_first>
  <action>
    Import get_straggler_count and get_analysis_failed_count into the existing from phaze.services.pipeline import (...)
    block. In BOTH dashboard() and pipeline_stats_partial(), call them and add straggler_count and analysis_failed_count
    to the context dict, mirroring how **activity / **dag_ctx are spread — add NO try/except (the Plan-02 services own
    the never-500 degrade; the wiring comment at 175-178 documents why). Pass settings.straggler_threshold_sec as the
    threshold arg. Create templates/pipeline/partials/straggler_failed_card.html showing the two counts (straggler =
    "still grinding", analysis_failed = "gave up" — two distinct buckets per D-02), following the existing dashboard
    card markup style (Tailwind utility classes already used by trigger_scan_card.html). Add a
    {% include "pipeline/partials/straggler_failed_card.html" %} slot to dashboard.html. The counts come from context;
    if you add an HTMX drill-down for the failed LIST (Claude's discretion, D-01/D-02), use an hx-get to a new
    /pipeline/... partial route following the #pipeline-stats hx-get idiom — otherwise inline counts are sufficient for
    this plan. Add a router test asserting both counts land in the dashboard + stats context and the card renders the
    values.
  </action>
  <verify>
    <automated>uv run pytest tests/test_routers/test_pipeline.py -q -k "straggler or failed or dashboard or stats"</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "get_straggler_count\|get_analysis_failed_count" src/phaze/routers/pipeline.py shows both wired into context
    - grep -n "straggler_failed_card" src/phaze/templates/pipeline/dashboard.html shows the include
    - the dashboard()/stats handlers add NO new try/except around the straggler/failed reads (service owns degrade)
    - uv run pytest tests/test_routers/test_pipeline.py -q -k "straggler or failed or dashboard or stats" passes
    - uv run mypy src/phaze/routers/pipeline.py clean
  </acceptance_criteria>
  <done>straggler_count + analysis_failed_count ride the 5s poll context in both dashboard() and stats partial; the card partial renders them; no router-level try/except added; tests assert both counts + render.</done>
</task>

<task type="auto">
  <name>Task 2: Sampled badge partial + deepen button on the analysis timeline; pass AnalysisResult into the timeline route</name>
  <files>src/phaze/routers/proposals.py, src/phaze/templates/proposals/partials/sampled_badge.html, src/phaze/templates/proposals/partials/analysis_timeline.html, tests/test_routers/test_proposals.py</files>
  <read_first>
    - src/phaze/templates/tracklists/partials/confidence_badge.html (the exact render-if-present-else-nothing pill structure to copy)
    - src/phaze/routers/proposals.py (proposal_timeline 252-292 — it resolves file_id and renders analysis_timeline.html; extend its query + context)
    - src/phaze/templates/proposals/partials/analysis_timeline.html (the per-file analysis surface where the badge + deepen button attach)
    - src/phaze/models/analysis.py (AnalysisResult.sampled + the four coverage columns)
    - .planning/phases/44-analyze-observability-ui-straggler-failed-count-sampled-badg/44-CONTEXT.md (D-03, D-04)
    - .planning/phases/44-analyze-observability-ui-straggler-failed-count-sampled-badg/44-PATTERNS.md (sampled_badge assignment section)
  </read_first>
  <action>
    Create templates/proposals/partials/sampled_badge.html copying confidence_badge.html's structure exactly: gate on
    analysis being present AND analysis.sampled truthy — NULL/false sampled renders NOTHING (pre-Phase-43 rows carry
    NULL coverage; treat NULL as "not sampled", never an error, D-03). When sampled, render an amber/yellow pill
    (like confidence_badge's mid-tier) reading "Sampled — more data available", with the four coverage counts in the
    title= tooltip, e.g. title="fine {{ analysis.fine_windows_analyzed }}/{{ analysis.fine_windows_total }}, coarse
    {{ analysis.coarse_windows_analyzed }}/{{ analysis.coarse_windows_total }} windows — sampled". In
    proposals.py::proposal_timeline, after resolving file_id, also fetch the AnalysisResult
    (select(AnalysisResult).where(AnalysisResult.file_id == file_id), scalar_one_or_none) and add analysis and file_id
    to the context. In analysis_timeline.html, {% include "proposals/partials/sampled_badge.html" %} and, gated on
    analysis.sampled, render a "Deepen analysis" button as an hx-post to /pipeline/files/{{ file_id }}/deepen (the
    Plan-03 endpoint) with an hx-target/hx-swap returning the endpoint's fragment. Add proposals router tests: timeline
    renders the badge + deepen button when the file's AnalysisResult.sampled is True; renders neither when sampled is
    None/False; tooltip carries the coverage counts.
  </action>
  <verify>
    <automated>uv run pytest tests/test_routers/test_proposals.py -q -k "timeline or sampled or badge or deepen"</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "sampled" src/phaze/templates/proposals/partials/sampled_badge.html shows the sampled gate
    - grep -n "files/.*deepen\|/deepen" src/phaze/templates/proposals/partials/analysis_timeline.html shows the deepen hx-post
    - grep -n "AnalysisResult" src/phaze/routers/proposals.py shows the timeline route fetching the analysis row
    - a test asserts no badge/button when sampled is None or False (never errors on NULL coverage)
    - uv run pytest tests/test_routers/test_proposals.py -q -k "timeline or sampled or badge or deepen" passes
    - uv run mypy src/phaze/routers/proposals.py clean
  </acceptance_criteria>
  <done>Sampled badge renders only when analysis.sampled is true (with coverage tooltip), the deepen button POSTs to the Plan-03 endpoint, the timeline route loads the AnalysisResult, and NULL coverage renders nothing without error; tests cover sampled/not-sampled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator HTTP -> dashboard / timeline render | server-rendered Jinja fragments; degrade-safe context |
| operator click -> deepen hx-post | crosses into the Plan-03 queue-producing endpoint (mitigations there) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-11 | Denial of Service | dashboard 500 on a degraded straggler/failed read | mitigate | Counts come from the Plan-02 degrade-safe services (zero on error); router adds no try/except; card renders the (possibly zero) context value, never raises |
| T-44-12 | Information Disclosure / Error | badge erroring on NULL coverage (pre-43 rows) | mitigate | Badge gates on analysis present AND sampled truthy; NULL/false renders nothing (D-03); test asserts the not-sampled path renders no markup |
| T-44-13 | Cross-Site Scripting | coverage counts / file_id in the rendered badge + hx-post URL | mitigate | Values are ints / a typed uuid rendered via Jinja autoescape; the hx-post URL uses the path-param uuid only — no operator free-text interpolated |
| T-44-14 | Cross-Site Request Forgery | deepen hx-post triggered cross-site | accept | Single-user admin tool behind a reverse-proxy internal realm (consistent with the existing /pipeline/* POST endpoints — no app-layer auth per Phase 37 decision) |
| T-44-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed (Tailwind/HTMX/Alpine via existing self-hosted/CDN assets); nothing to audit. |
</threat_model>

<verification>
- uv run pytest tests/test_routers/test_pipeline.py tests/test_routers/test_proposals.py -q passes
- uv run mypy src/phaze/routers/pipeline.py src/phaze/routers/proposals.py clean
- uv run ruff check src/phaze/routers/pipeline.py src/phaze/routers/proposals.py clean
- pre-commit run --all-files passes (no --no-verify)
</verification>

<success_criteria>
The pipeline dashboard shows straggler + ANALYSIS_FAILED counts via the existing 5s poll
(counts sourced from the Plan-02 degrade-safe services, no router try/except). A file with
analysis.sampled=true shows a "Sampled — more data available" badge with the four coverage
counts in its tooltip and a "Deepen analysis" button POSTing to the Plan-03 endpoint; NULL/false
sampled renders nothing without error. Render/context tests cover both paths. 85% coverage held.
</success_criteria>

<output>
Create `.planning/phases/44-analyze-observability-ui-straggler-failed-count-sampled-badg/44-04-SUMMARY.md` when done
</output>
