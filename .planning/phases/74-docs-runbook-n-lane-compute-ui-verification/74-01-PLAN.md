---
phase: 74-docs-runbook-n-lane-compute-ui-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/multi-compute.md
  - docs/cloud-burst.md
  - docs/runbook.md
  - docs/configuration.md
  - docs/README.md
autonomous: true
requirements: [MCOMP-07]
must_haves:
  truths:
    - "An operator can read a single doc that walks through adding a 2nd+ compute agent, cost-tiered (MCOMP-07, D-01)"
    - "The doc shows a worked mixed arm64/x86 backends.toml with a cost-tier rationale table (MCOMP-07, D-02, D-03)"
    - "The doc shows the rank-tiered drain: free arm64 rank 10 fills first, spills to paid x86 rank 20, then local rank 99 (MCOMP-07, D-02)"
    - "The new doc is reachable from cloud-burst.md, runbook.md, configuration.md, and the docs README index (D-01)"
    - "cloud-burst.md no longer asserts a ≤1-compute-agent limit that would block the 2nd-agent recipe (D-01)"
  artifacts:
    - path: "docs/multi-compute.md"
      provides: "The add-a-2nd+-compute-agent cost-tiered operator guide"
      contains: "generated-by: gsd-doc-writer"
    - path: "docs/README.md"
      provides: "Docs index row linking multi-compute.md"
      contains: "multi-compute.md"
  key_links:
    - from: "docs/multi-compute.md"
      to: "docs/configuration.md#backend-registry-backendstoml"
      via: "cross-link to canonical [[backends]] field table (D-03, do NOT restate)"
      pattern: "configuration.md#backend-registry-backendstoml"
    - from: "docs/cloud-burst.md"
      to: "docs/multi-compute.md"
      via: "cross-link into the new doc"
      pattern: "multi-compute.md"
---

<objective>
Write the new dedicated operator doc `docs/multi-compute.md` (D-01/D-02/D-03) — the "now add a 2nd+ compute
agent, cost-tiered" guide over the Phase 72/73 machinery — and cross-link it into the existing multi-cloud
nav, generalizing any single-compute-agent framing in `cloud-burst.md` that would block the 2nd-agent recipe.

Purpose: MCOMP-07 requires the operator runbook + config docs to cover adding a 2nd+ compute agent and the
mixed arm64/x86 rank/cap cost-tiering. This is pure documentation over already-shipped 72/73 behavior; no
routing/dispatch code changes.

Output: `docs/multi-compute.md` (new) + cross-link edits in `cloud-burst.md`, `runbook.md`, `configuration.md`,
`docs/README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-CONTEXT.md
@.planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-RESEARCH.md
@.planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-PATTERNS.md

# The verified worked backends.toml shape is in 74-RESEARCH.md:217-249 (fields validated against config_backends.py).
# The mermaid render-path diagram is in 74-RESEARCH.md:83-96.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/multi-compute.md — the cost-tiered N-compute operator guide</name>
  <files>docs/multi-compute.md</files>
  <read_first>
    - docs/cloud-burst.md (line-1 marker + H1 + mermaid architecture-at-a-glance pattern lines 38-67; the single-A1 walkthrough this doc builds on; the PHAZE_AGENT_ID → PHAZE_AGENT_QUEUE=phaze-agent-<id> convention at :263,:307)
    - docs/deployment.md (Step 4, ~:318-326 — the canonical per-agent env block: PHAZE_AGENT_ID mnemonic feeding PHAZE_AGENT_QUEUE=phaze-agent-<agent_id>; there is NO bare AGENT_ID compose var)
    - docs/runbook.md (lines 1-21 cross-link vocabulary; the "Reading the N lanes" RANK/in_flight/cap read-out vocabulary to reuse verbatim)
    - docs/configuration.md (lines 103-152 § Backend registry — the canonical [[backends]] field table this doc must LINK to, not restate; anchor #backend-registry-backendstoml; PHAZE_AGENT_QUEUE convention note at :346)
    - docker-compose.cloud-agent.yml (lines 34-64 — the per-agent env/scratch/tag/command surface the doc documents running once per compute agent; secret hygiene *_FILE at lines 39-41)
    - .planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-RESEARCH.md (lines 217-249 verified worked backends.toml; lines 83-96 mermaid render-path; Pitfall 3 co-located-agent scratch/project isolation)
    - src/phaze/config_backends.py (lines 79-115 ComputeBackend required fields: agent_ref/push_host/scratch_dir required, ssh_user optional, rank ge=0 lt=1000, cap gt=0 lt=1000)
  </read_first>
  <action>
    Create docs/multi-compute.md. Line 1 MUST be the exact HTML comment `<!-- generated-by: gsd-doc-writer -->`;
    line 2 an H1 (e.g. `# Multi-Compute Agents — mixed arm64/x86 cost-tiered lanes`). Sections:
    (1) Intro — this is the N-compute cost-tiered guide; cloud-burst.md remains the single-A1 provisioning
    walkthrough (link to it); link OUT to configuration.md#backend-registry-backendstoml for the canonical
    [[backends]] field schema and DO NOT restate that field table (D-03).
    (2) A mermaid architecture/drain diagram (mermaid, not ASCII) showing the rank-tiered drain: free arm64 A1
    rank 10 (preferred, fills first) → paid x86 rank 20 (spill when arm64 at cap or offline) → local rank 99
    (final catch). Adapt the shape from cloud-burst.md:38-67 and the render-path diagram in 74-RESEARCH.md:83-96.
    (3) A worked mixed arm64/x86 `backends.toml` fenced block matching the schema-verified shape in
    74-RESEARCH.md:217-249 — two `kind="compute"` entries (id `a1-arm64` rank 10 cap 2 agent_ref `a1-arm64`;
    id `x86-spill` rank 20 cap 4 agent_ref `x86-spill`; each with distinct push_host + scratch_dir) plus a
    `kind="local"` rank 99 cap 1 catch. agent_ref values MUST be distinct (duplicate agent_ref fails at boot,
    config.py:437-450).
    (4) A short cost-tier rationale table: columns tier / rank / cap / cost posture (e.g. free arm64 A1 preferred
    at low rank; paid/trial x86 spill at higher rank; local final catch) — wording is your discretion (D-03).
    (5) Per-agent compose instructions (D-05): run the SINGLE `docker-compose.cloud-agent.yml` once per compute
    agent. Follow the ESTABLISHED convention from docs/cloud-burst.md + docs/deployment.md Step 4 — do NOT
    introduce a standalone `AGENT_ID` compose variable (no code reads a bare `AGENT_ID`; `grep -rn PHAZE_AGENT_ID
    src/` returns nothing either — it is a documentation mnemonic). Use `PHAZE_AGENT_ID` only as the
    documentation-mnemonic placeholder `<id>` that feeds `PHAZE_AGENT_QUEUE=phaze-agent-<id>` (the SAQ queue the
    worker consumes) and the backend's `agent_ref = <id>`. Give each agent distinct `PHAZE_AGENT_QUEUE=phaze-agent-<id>`,
    `PHAZE_CLOUD_SCRATCH_DIR`/scratch volume, SSH host, and `-p <project>` compose project-name. Document the
    arm64→x86 image/command swap: the arm64 agent uses the default `-arm64` tag + `python3 -m saq …`; a real x86
    compute agent overrides `PHAZE_CLOUD_AGENT_IMAGE` (standard x86 tag, NO `-arm64` suffix) and
    `PHAZE_CLOUD_AGENT_CMD` to `uv run saq phaze.tasks.agent_worker.settings` (x86 image is 3.14 + .venv). Call
    out the co-located-agent scratch/project-name collision (Pitfall 3) but note the worked example uses different
    hosts (A1 vs x86 box).
    (6) A "Reading the lanes" cross-link to runbook.md reusing its RANK {n} / {in_flight}/{cap} / offline
    word+glyph vocabulary so the doc and the `_lane_card.html` UI read-out speak the same language.
    Secret hygiene: reference `*_FILE` env pointers only; NEVER inline a token/SSH key/DATABASE_URL in any
    example. No SecretStr values anywhere.
    Do NOT place a restated [[backends]] field-reference table in this doc — link to configuration.md instead.
  </action>
  <verify>
    <automated>test "$(sed -n '1p' docs/multi-compute.md)" = '<!-- generated-by: gsd-doc-writer -->' && grep -q 'configuration.md#backend-registry-backendstoml' docs/multi-compute.md && grep -q 'kind = "compute"' docs/multi-compute.md && grep -q 'PHAZE_AGENT_QUEUE=phaze-agent-' docs/multi-compute.md && grep -q 'mermaid' docs/multi-compute.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/multi-compute.md` line 1 == `<!-- generated-by: gsd-doc-writer -->`
    - File contains a fenced ```mermaid block (drain diagram)
    - File contains a worked backends.toml with TWO `kind = "compute"` entries at rank 10 and rank 20 plus a `kind = "local"` rank 99
    - File contains a cost-tier rationale table (markdown table with a "rank" or "cost" column)
    - File cross-links `configuration.md#backend-registry-backendstoml` and does NOT contain a restated full [[backends]] field-reference table
    - Per-agent compose recipe uses `PHAZE_AGENT_ID` only as the documentation-mnemonic `<id>` feeding `PHAZE_AGENT_QUEUE=phaze-agent-<id>` / `agent_ref` — it does NOT introduce a standalone `AGENT_ID` compose variable
    - File documents `PHAZE_CLOUD_AGENT_IMAGE` and `PHAZE_CLOUD_AGENT_CMD` overrides and `PHAZE_AGENT_QUEUE=phaze-agent-<id>`
    - No inline secrets: `grep -iE '(ssh-rsa|BEGIN .*PRIVATE KEY|postgres://[^$])' docs/multi-compute.md` returns nothing
  </acceptance_criteria>
  <done>docs/multi-compute.md exists with the marker, mermaid drain diagram, worked mixed arm64/x86 backends.toml, cost-tier table, per-agent compose recipe incl. tag/command swap + the PHAZE_AGENT_ID→PHAZE_AGENT_QUEUE mnemonic, and a link out to the canonical field table.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-link the new doc into the multi-cloud nav + generalize single-agent framing</name>
  <files>docs/README.md, docs/runbook.md, docs/configuration.md, docs/cloud-burst.md</files>
  <read_first>
    - docs/README.md (lines 29-31 — the index row style for runbook/cloud-burst/k8s-burst; add a multi-compute row alongside)
    - docs/runbook.md (lines 16-21 — the cross-link block; add a link to multi-compute.md)
    - docs/cloud-burst.md (full — add a link into multi-compute.md AND scan for any language asserting a single/≤1 compute-agent limit that would block the 2nd-agent recipe; generalize it per D-01 while keeping cloud-burst.md as the single-A1 walkthrough)
    - docs/configuration.md (§ Backend registry, lines 103-152 — add a "for a worked multi-compute cost-tiered scenario see multi-compute.md" pointer near the compute backend fields)
  </read_first>
  <action>
    Add a docs/README.md index row for the new doc mirroring the existing row style at README.md:29-31 (e.g.
    `| **[Multi-Compute Agents](multi-compute.md)** | ⚙️ Add a 2nd+ compute agent, mixed arm64/x86 rank/cap
    cost-tiering, per-agent compose, N-lane read-out |`). Add a cross-link to multi-compute.md in runbook.md's
    cross-link block (~lines 16-21) and in configuration.md § Backend registry (near the compute backend field
    rows) pointing to the worked scenario. In cloud-burst.md, add a link into multi-compute.md ("to run more than
    one compute agent, cost-tiered, see multi-compute.md") AND generalize any assertion of a single/only-one
    compute-agent limit so nothing there blocks the 2nd-agent recipe — cloud-burst.md STAYS the single-A1
    provisioning walkthrough (D-01), just remove any ≤1-compute wording. Do not restate the backends.toml field
    table in any of these edits; only add navigational links + de-block the single-agent framing.
  </action>
  <verify>
    <automated>grep -q 'multi-compute.md' docs/README.md && grep -q 'multi-compute.md' docs/runbook.md && grep -q 'multi-compute.md' docs/configuration.md && grep -q 'multi-compute.md' docs/cloud-burst.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/README.md` contains a table row linking `multi-compute.md`
    - `docs/runbook.md`, `docs/configuration.md`, and `docs/cloud-burst.md` each contain a link to `multi-compute.md`
    - `cloud-burst.md` contains no remaining assertion of a single/≤1 compute-agent hard limit (grep for phrases like "only one compute" / "single compute agent" as a hard cap returns nothing that blocks a 2nd agent)
    - No backends.toml field-reference table was copied into any of these four files (links only)
  </acceptance_criteria>
  <done>The new doc is reachable from README index, runbook, configuration § Backend registry, and cloud-burst.md; cloud-burst.md no longer blocks the 2nd-compute-agent recipe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → docs | Docs are read by a human operator; no runtime input path, no network/auth boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | Information Disclosure | worked compose / backends.toml examples in docs/multi-compute.md | mitigate | Show `*_FILE` secret-pointer pattern only; acceptance grep forbids inline SSH keys / postgres URLs / private keys (V7/V9, per docker-compose.cloud-agent.yml:39-41 convention). |
| T-74-02 | Tampering | new compose override vars documented as shell `command:` | accept | Operator-controlled at their own host; no external input path; doc documents only the two accepted override values. No new exposed port or credential introduced by documentation. |
</threat_model>

<verification>
- `docs/multi-compute.md` opens with the gsd-doc-writer marker, carries a mermaid drain diagram, a worked
  mixed arm64/x86 backends.toml, a cost-tier table, and links OUT to configuration.md for the field schema.
- The new doc is cross-linked from README index, runbook, configuration § Backend registry, and cloud-burst.md.
- No inline secrets in any example.
- `uv run pytest tests/shared/core/test_requirements_traceability.py -x` still passes (in-flight [ ]+Pending tolerated).
</verification>

<success_criteria>
- MCOMP-07's documentation deliverable is met: a single operator doc covers adding a 2nd+ compute agent and
  mixed arm64/x86 cost-tiering, cross-linked into the existing multi-cloud nav, with the canonical field table
  kept in configuration.md (D-01/D-02/D-03).
</success_criteria>

<output>
Create `.planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-01-SUMMARY.md` when done.
</output>
