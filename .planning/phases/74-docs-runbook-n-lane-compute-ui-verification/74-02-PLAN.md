---
phase: 74-docs-runbook-n-lane-compute-ui-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.cloud-agent.yml
  - tests/agents/deployment/test_cloud_agent_compose.py
autonomous: true
requirements: [MCOMP-07]
must_haves:
  truths:
    - "The single docker-compose.cloud-agent.yml can serve both an arm64 A1 agent (default) and a real x86 spill agent (override), no new compose file (MCOMP-07, D-05, R-1)"
    - "With no overrides set, the compose still resolves to the arm64 image tag and the python3 -m saq command (D-05)"
    - "The compose guard test asserts the arm64 default is preserved while tolerating the ${VAR:-default} override form (D-05, Pitfall 2)"
  artifacts:
    - path: "docker-compose.cloud-agent.yml"
      provides: "Parametrized image + command with arm64 defaults preserved"
      contains: "PHAZE_CLOUD_AGENT_CMD"
    - path: "tests/agents/deployment/test_cloud_agent_compose.py"
      provides: "Guard test relaxed to the ${VAR:-default} form asserting arm64 default"
      contains: "arm64"
  key_links:
    - from: "docker-compose.cloud-agent.yml"
      to: "PHAZE_CLOUD_AGENT_IMAGE / PHAZE_CLOUD_AGENT_CMD"
      via: "docker-compose ${VAR:-default} substitution, arm64 defaults"
      pattern: "PHAZE_CLOUD_AGENT_(IMAGE|CMD)"
    - from: "tests/agents/deployment/test_cloud_agent_compose.py"
      to: "raw-YAML default assertion"
      via: "assert default renders -arm64 / python3 -m saq"
      pattern: "arm64"
---

<objective>
Parametrize the two arm64-hardcoded lines in `docker-compose.cloud-agent.yml` (image tag + command) so the
single compose file serves both the arm64 A1 agent (default, unchanged) and a real x86 spill compute agent
(override), then relax the two guard-test assertions that parse the raw un-interpolated YAML so they assert the
arm64 DEFAULT is preserved while tolerating the override form.

Purpose: D-05 mandates documenting running the existing compose once per compute agent including the arm64→x86
tag/command swap (R-1). This is a real compose + guard-test edit, not docs-only. No new compose file.

Output: parametrized `docker-compose.cloud-agent.yml` + updated `tests/agents/deployment/test_cloud_agent_compose.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-RESEARCH.md
@.planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-PATTERNS.md

<interfaces>
<!-- Verified current lines the executor edits — no exploration needed. -->

docker-compose.cloud-agent.yml:45 (current image line):
  image: ghcr.io/simplicityguy/phaze:${PHAZE_IMAGE_TAG:-latest}-arm64

docker-compose.cloud-agent.yml:51 (current command line):
  command: python3 -m saq phaze.tasks.agent_worker.settings

tests/agents/deployment/test_cloud_agent_compose.py:
  - test_worker_image_is_arm64_ghcr_pinned (~:106-118): parses raw YAML (yaml.safe_load, NO interpolation),
    asserts image.startswith("ghcr.io/simplicityguy/phaze:"), "PHAZE_IMAGE_TAG" in image, image.endswith("-arm64")
  - test_worker_command_invokes_system_python_not_uv (~:120-147): if command is None early-return; else
    tokens = command.split(); assert "uv" not in tokens; assert tokens[:3] == ["python3","-m","saq"];
    assert "phaze.tasks.agent_worker.settings" in tokens
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parametrize the compose image + command with arm64 defaults preserved</name>
  <files>docker-compose.cloud-agent.yml</files>
  <read_first>
    - docker-compose.cloud-agent.yml (lines 34-64: the block header comment documenting the arm64/x86 wrinkle, the image line :45, the command line :51 with its "NEVER uv run on arm64" comment, the environment/volumes block)
    - docs/arm64-agent-image.md (the arm64=py3.13/--system/python3 vs x86=uv/3.14/.venv distinction that dictates why the command must swap)
    - .planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-RESEARCH.md (Pattern 2 lines 129-137; Assumption A2 — verify with `docker compose config` if colima available)
  </read_first>
  <action>
    Edit docker-compose.cloud-agent.yml. Change the image line (:45) to a `${VAR:-default}` form whose default
    still renders the arm64 tag, e.g. `image: ${PHAZE_CLOUD_AGENT_IMAGE:-ghcr.io/simplicityguy/phaze:${PHAZE_IMAGE_TAG:-latest}-arm64}`
    — the raw default MUST still end in `-arm64` and preserve the `ghcr.io/simplicityguy/phaze:` prefix and the
    `PHAZE_IMAGE_TAG` pin. Change the command line (:51) to
    `command: ${PHAZE_CLOUD_AGENT_CMD:-python3 -m saq phaze.tasks.agent_worker.settings}` — the raw default MUST
    still begin `python3 -m saq` and reference `phaze.tasks.agent_worker.settings`. Extend the block header
    comment (:34-41) to document the two new override vars: `PHAZE_CLOUD_AGENT_IMAGE` (x86 operator sets the
    standard tag, NO `-arm64` suffix) and `PHAZE_CLOUD_AGENT_CMD` (x86 operator sets `uv run saq phaze.tasks.agent_worker.settings`
    because the x86 image is 3.14 + .venv). Do NOT change the `PHAZE_AGENT_KIND=compute`, network_mode, volumes,
    or env_file lines. If docker/colima is available locally, run `docker compose -f docker-compose.cloud-agent.yml
    config` to confirm the parametrized file validates (Assumption A2); if unavailable, rely on the guard test.
    Do not inline any secret.
  </action>
  <verify>
    <automated>uv run python -c "import yaml; d=yaml.safe_load(open('docker-compose.cloud-agent.yml')); w=d['services']['worker']; img=w['image']; cmd=w['command']; assert 'PHAZE_CLOUD_AGENT_IMAGE' in img and img.rstrip().endswith('-arm64}') and 'ghcr.io/simplicityguy/phaze:' in img, img; assert 'PHAZE_CLOUD_AGENT_CMD' in cmd and 'python3 -m saq' in cmd and 'phaze.tasks.agent_worker.settings' in cmd, cmd; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Raw (un-interpolated) `worker.image` contains `PHAZE_CLOUD_AGENT_IMAGE`, keeps the `ghcr.io/simplicityguy/phaze:` prefix + `PHAZE_IMAGE_TAG` pin, and its default still ends `-arm64}`
    - Raw `worker.command` contains `PHAZE_CLOUD_AGENT_CMD` and its default still begins `python3 -m saq` referencing `phaze.tasks.agent_worker.settings`
    - Block header comment documents both `PHAZE_CLOUD_AGENT_IMAGE` and `PHAZE_CLOUD_AGENT_CMD` overrides
    - No new compose file created; `PHAZE_AGENT_KIND=compute` and secret `*_FILE` machinery unchanged
  </acceptance_criteria>
  <done>docker-compose.cloud-agent.yml image + command are parametrized with arm64 defaults preserved and the two override vars documented in the header comment.</done>
</task>

<task type="auto">
  <name>Task 2: Relax the two compose guard-test assertions to the ${VAR:-default} form</name>
  <files>tests/agents/deployment/test_cloud_agent_compose.py</files>
  <read_first>
    - tests/agents/deployment/test_cloud_agent_compose.py (the two target tests: test_worker_image_is_arm64_ghcr_pinned ~:106-118 asserting image.endswith("-arm64"); test_worker_command_invokes_system_python_not_uv ~:120-147 asserting tokens[:3]==["python3","-m","saq"] and the command-is-None early return)
    - .planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-PATTERNS.md (the "Assertion to relax #1/#2" section lines 100-115)
  </read_first>
  <action>
    Update test_worker_image_is_arm64_ghcr_pinned: keep the `ghcr.io/simplicityguy/phaze:` prefix and
    `PHAZE_IMAGE_TAG in image` checks; replace the `image.endswith("-arm64")` assertion with one that recognizes
    the `${VAR:-…-arm64}` form and asserts the DEFAULT still renders arm64 (e.g. assert `"-arm64}" in image` or
    `"latest-arm64" in image`). Update test_worker_command_invokes_system_python_not_uv: keep the `command is None`
    early-return branch; keep the "uv not the default launcher" intent; replace the raw `tokens[:3] == ["python3","-m","saq"]`
    assertion with one that recognizes the `${PHAZE_CLOUD_AGENT_CMD:-python3 -m saq …}` form and asserts the
    DEFAULT is `python3 -m saq phaze.tasks.agent_worker.settings` (arm64 default preserved, `uv` still forbidden
    as the default). Do not weaken the intent — the DEFAULT must still guarantee arm64 image + system-python
    command; only tolerate the override wrapper. Keep the other assertions in the file (scratch volume, env,
    secrets) untouched.
  </action>
  <verify>
    <automated>uv run pytest tests/agents/deployment/test_cloud_agent_compose.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/agents/deployment/test_cloud_agent_compose.py -x` exits 0
    - test_worker_image_is_arm64_ghcr_pinned asserts the DEFAULT renders arm64 (not a bare `endswith("-arm64")` on the now-`}`-terminated raw string)
    - test_worker_command_invokes_system_python_not_uv asserts the DEFAULT is `python3 -m saq …` and still forbids `uv` as the default; the `command is None` early-return branch retained
    - `uv run ruff check tests/agents/deployment/test_cloud_agent_compose.py` clean
  </acceptance_criteria>
  <done>Both guard-test assertions accept the ${VAR:-default} compose form and assert the arm64 default is preserved; the deployment test file is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → compose | Compose variable overrides are set by the operator at their own host; no external/untrusted input path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-03 | Tampering | parametrized compose `image`/`command` defaults | mitigate | The guard test asserts the DEFAULT still renders the arm64 image + system-python command, so a mis-parametrization that silently drops the arm64 pin fails CI. |
| T-74-04 | Information Disclosure | compose env surface | accept | No secret value is added; parametrization introduces no new exposed port or credential — only two operator-set image/command vars with arm64 defaults. Existing `*_FILE` secret machinery unchanged. |
</threat_model>

<verification>
- `uv run pytest tests/agents/deployment/test_cloud_agent_compose.py -x` passes.
- Raw compose image default still ends `-arm64`; raw command default still starts `python3 -m saq`.
- `just test-bucket agents` green.
</verification>

<success_criteria>
- The single compose file serves both arm64 (default) and x86 (override) compute agents (D-05, R-1), with the
  guard test protecting the arm64 default. No new compose file.
</success_criteria>

<output>
Create `.planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-02-SUMMARY.md` when done.
</output>
