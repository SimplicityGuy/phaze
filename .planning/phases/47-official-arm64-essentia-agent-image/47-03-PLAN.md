---
phase: 47-official-arm64-essentia-agent-image
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/parity/compare_analysis.py
  - scripts/parity/dump_analysis.py
  - scripts/parity/generate_reference.py
  - scripts/parity/reference.wav
  - tests/test_parity/__init__.py
  - tests/test_parity/test_compare_analysis.py
  - justfile
autonomous: true
requirements: [CLOUDIMG-03]
must_haves:
  truths:
    - "A pure-Python comparator asserts bpm and musical_key EXACTLY and model scores within a configurable epsilon, over the analyze_file output schema"
    - "A shared dump tool runs analyze_file over a file + models dir and emits the comparable JSON (used by both the x86 golden job and the arm64 parity job)"
    - "A deterministic, committed, non-degenerate reference clip exists and regenerates byte-identically"
    - "The comparator is unit-tested (pass + fail cases) with no models / no essentia required"
  artifacts:
    - path: "scripts/parity/compare_analysis.py"
      provides: "bpm/key exact + model-score epsilon comparator over the analyze_file dict"
      exports: ["compare"]
    - path: "scripts/parity/dump_analysis.py"
      provides: "CLI: analyze_file(file, models_dir) -> comparable JSON on stdout"
    - path: "scripts/parity/reference.wav"
      provides: "deterministic non-degenerate parity reference audio"
    - path: "tests/test_parity/test_compare_analysis.py"
      provides: "unit tests for the comparator"
  key_links:
    - from: "scripts/parity/dump_analysis.py"
      to: "phaze.services.analysis.analyze_file"
      via: "import + call with models_dir"
      pattern: "analyze_file"
    - from: "scripts/parity/compare_analysis.py"
      to: "analyze_file output schema"
      via: "bpm/musical_key exact, features model-scores epsilon"
      pattern: "isclose"
---

<objective>
Build the numeric-parity toolkit that CLOUDIMG-03 hinges on: a pure-Python comparator (`bpm`/`musical_key` exact, model scores within epsilon), a shared `dump_analysis.py` CLI that runs the real `phaze.services.analysis.analyze_file` and emits the comparable JSON, and a deterministic, committed, non-degenerate reference clip. The CI parity jobs (plan 47-04) reuse this toolkit to compare the arm64 image against an x86 golden.

Purpose: CLOUDIMG-03 — without a comparator and a shared analysis-dump contract, there is nothing to gate the arm64 build against.
Output: `scripts/parity/{compare_analysis.py, dump_analysis.py, generate_reference.py, reference.wav}`, unit tests, and a `just parity-golden-regen` recipe.

This plan is fully autonomous: the comparator is unit-tested without models/essentia; the reference is Claude-generated and deterministic. The heavy real-audio runs (x86 golden + arm64 parity) happen in CI in plan 47-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-official-arm64-essentia-agent-image/47-RESEARCH.md
@.planning/phases/47-official-arm64-essentia-agent-image/47-PATTERNS.md

<interfaces>
<!-- The analyze_file output dict IS the JSON schema the golden + actual share. -->

src/phaze/services/analysis.py:
  def analyze_file(file_path: str, models_dir: str, *, fine_window_sec=..., coarse_window_sec=..., ...) -> dict[str, Any]
  return dict (lines 575-588):
    {
      "bpm": float|None,            # rounded 0.1 — EXACT match
      "musical_key": str|None,      # EXACT string match
      "mood": str|None,             # dominant label (exact)
      "style": str|None,            # dominant label (exact)
      "danceability": float|None,   # epsilon
      "features": dict,             # _representative_features(coarse) — model scores, epsilon (atol)
      "windows": [...], "fine_windows_analyzed": int, ... (counts — IGNORE for parity)
    }
  Classifier families the parity surface MUST cover (analysis.py lines 56-92): musicnn (msd + mtt variants),
  vggish (every _make_standard_set, line 54), effnet_discogs (GENRE_MODEL). BPM = RhythmExtractor2013;
  key = KeyExtractor. The spike only ran musicnn+effnet — VGGish/BPM/key are net-new coverage (Pitfall 3).

Module-shape analog (header provenance, from __future__, typed pure functions, unit-tested):
  src/phaze/scripts/download_models.py
Unit-test analog (docstring-rich, import pure fns, no I/O):
  tests/test_scripts/test_download_models.py
Comparator shape (RESEARCH Code Examples):
  compare(golden: dict, actual: dict, *, atol: float = ...) -> list[str]   # [] == parity OK
Operator recipe analog (delegate to a script):
  justfile download-models recipe (lines ~310-313)

Style: ruff target py313, line-length 150, double quotes, from __future__ import annotations, type hints on
all functions (mypy strict). T201 (print) allowed in CLI entry points — add scripts/parity/*.py to
[tool.ruff.lint.per-file-ignores] if they print, mirroring the existing CLI/entry-point ignores.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Comparator helper + unit tests (bpm/key exact, model scores epsilon)</name>
  <read_first>
    - src/phaze/services/analysis.py lines 507-588 (_representative_features + analyze_file return dict — the schema)
    - src/phaze/scripts/download_models.py (module-shape analog)
    - tests/test_scripts/test_download_models.py (unit-test analog)
    - .planning/phases/47-official-arm64-essentia-agent-image/47-PATTERNS.md (compare_analysis.py section + comparator shape)
    - pyproject.toml [tool.ruff.lint.per-file-ignores] (where to add scripts/parity if it prints)
  </read_first>
  <behavior>
    - compare(g, g) == [] (identical inputs → parity OK).
    - bpm mismatch (120.0 vs 120.1) → returns a non-empty list naming "bpm".
    - musical_key string mismatch ("C major" vs "A minor") → returns a failure naming "key".
    - model score within atol (0.80 vs 0.80005, atol=1e-3) → no failure for that score.
    - model score outside atol (0.80 vs 0.83, atol=1e-3) → failure naming the score.
    - None on one side where the other is a number → failure (no silent pass).
    - mood/style dominant-label mismatch → failure (exact label parity).
  </behavior>
  <action>
    Create `scripts/parity/compare_analysis.py` exporting `compare(golden: dict, actual: dict, *, atol: float = 1e-4) -> list[str]` that returns a list of human-readable failure strings ([] == parity). Compare `bpm` and `musical_key` for EXACT equality, `mood`/`style` dominant labels for exact equality, and `danceability` + every model score under `features` with `math.isclose(..., abs_tol=atol)`. Treat a None-vs-number as a failure. Make `atol` a CLI/keyword argument (default a conservative `1e-4` placeholder — plan 47-04 tunes it empirically from observed x86↔arm64 deltas). Add a `__main__` CLI: `compare_analysis.py <golden.json> <actual.json> [--atol X]` that prints failures and exits non-zero on any failure (this is what the CI parity-guard shells out to). Ignore the `windows`/`*_analyzed`/`*_total` count keys (run-to-run variable, not parity-relevant).
    Create `tests/test_parity/__init__.py` and `tests/test_parity/test_compare_analysis.py` covering every case in <behavior> as table tests. No essentia, no models — pure dict inputs.
  </action>
  <verify>
    <automated>uv run pytest tests/test_parity/test_compare_analysis.py -x -q && uv run ruff check scripts/parity/compare_analysis.py tests/test_parity/ && uv run mypy scripts/parity/compare_analysis.py</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/parity/compare_analysis.py` exports `compare(...) -> list[str]` and has a non-zero-exit `__main__` CLI taking `<golden> <actual> [--atol]`
    - bpm + musical_key compared EXACTLY; model scores via `math.isclose(abs_tol=atol)`
    - All <behavior> cases pass in `tests/test_parity/test_compare_analysis.py`
    - `uv run pytest tests/test_parity/`, `ruff check`, and `mypy` on the new files are clean
  </acceptance_criteria>
  <done>A tested comparator encodes the CLOUDIMG-03 tolerance contract (bpm/key exact, scores epsilon).</done>
</task>

<task type="auto">
  <name>Task 2: Shared analysis-dump CLI + deterministic reference clip + parity-golden-regen recipe</name>
  <read_first>
    - src/phaze/services/analysis.py lines 520-588 (analyze_file signature + return)
    - git show spike/arm64-essentia-analysis:spike/arm64-essentia/run_test.py (faulthandler + model-run shape)
    - .planning/phases/47-official-arm64-essentia-agent-image/47-PATTERNS.md (dump tool + reference fixture + golden generation sections)
    - justfile download-models recipe (~lines 310-313) + scripts/download-models.sh (models provisioning)
    - .planning/phases/47-official-arm64-essentia-agent-image/47-RESEARCH.md (Open Q2 reference-audio guidance; Pattern 2/3)
  </read_first>
  <action>
    Create `scripts/parity/dump_analysis.py` — a CLI `dump_analysis.py <audio_path> <models_dir> [--out PATH]` that enables `faulthandler`, calls `phaze.services.analysis.analyze_file(audio_path, models_dir)`, projects the result down to the parity-relevant keys (`bpm`, `musical_key`, `mood`, `style`, `danceability`, and a flattened `features`/model-scores map — DROP the variable `windows`/count keys), and writes that JSON to `--out` (default stdout). This SAME tool runs inside both the x86 image (golden) and the arm64 image (actual) in plan 47-04, guaranteeing an identical schema on both sides.
    Create `scripts/parity/generate_reference.py` — a deterministic numpy generator (fixed seed / no RNG; build the signal arithmetically) that writes `scripts/parity/reference.wav`: a short (~30-45s) NON-degenerate mono clip with harmonic content + a clear rhythmic pulse (NOT a pure 440Hz sine — that is too degenerate for meaningful BPM/key/model outputs per RESEARCH Open Q2). It must be byte-reproducible (regenerating yields an identical sha256) and license-clean (fully synthetic). Commit both the generator and the generated `reference.wav`. Note: for arm64-vs-x86 PARITY the clip only needs to be identical on both sides and drive finite, non-NaN model outputs — musical realism is not required.
    Add a `[group('docker')]` `parity-golden-regen TAG="latest"` recipe to `justfile` (mirror the download-models/image-push bash style): provision models (scripts/download-models.sh), then `docker run` the x86 api image over `scripts/parity/reference.wav` via `dump_analysis.py` to (re)generate `scripts/parity/golden-x86.json` for offline inspection. Document that CI (plan 47-04) is the authoritative golden producer; this recipe is the operator regen path.
  </action>
  <verify>
    <automated>uv run python scripts/parity/dump_analysis.py --help >/dev/null && uv run python scripts/parity/generate_reference.py && S1=$(shasum -a 256 scripts/parity/reference.wav | cut -d' ' -f1) && uv run python scripts/parity/generate_reference.py && S2=$(shasum -a 256 scripts/parity/reference.wav | cut -d' ' -f1) && [ "$S1" = "$S2" ] && uv run python -c "import wave; w=wave.open('scripts/parity/reference.wav'); assert w.getnframes()>0" && just --list 2>/dev/null | grep -q parity-golden-regen && uv run ruff check scripts/parity/dump_analysis.py scripts/parity/generate_reference.py</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/parity/dump_analysis.py --help` exits 0; it imports `analyze_file` and projects to the parity key set
    - `scripts/parity/generate_reference.py` regenerates `reference.wav` byte-identically (matching sha256 twice)
    - `reference.wav` is a non-empty, non-silent mono clip (>0 frames)
    - `just --list` shows `parity-golden-regen`
    - `ruff check` clean on both scripts
  </acceptance_criteria>
  <done>A shared dump contract + a deterministic reference clip + an operator golden-regen recipe exist; CI in 47-04 produces the authoritative golden and the arm64 actual from the same tool.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| golden/actual JSON → comparator | the comparator parses two JSON files it must not trust blindly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-07 | Tampering | comparator silently passing on missing data | mitigate | A None-vs-number or a missing model-score key is a FAILURE, never a silent pass (covered by unit tests) — a parity gate that passes on absent data is worse than none |
| T-47-05 | Information Disclosure | reference fixture | accept | `reference.wav` is fully synthetic (no copyrighted audio, no PII); generator is committed and deterministic |
| T-47-SC | Tampering | new files only, no new deps | accept | No new package dependencies; comparator uses stdlib `math`/`json` + existing numpy |
</threat_model>

<verification>
- `uv run pytest tests/test_parity/` green; comparator fails on bpm/key mismatch and out-of-epsilon scores, passes within epsilon.
- `reference.wav` regenerates byte-identically; is non-silent.
- `dump_analysis.py --help` and `parity-golden-regen` recipe present.
- ruff + mypy clean on new scripts.
</verification>

<success_criteria>
A tested comparator, a shared analysis-dump CLI, and a deterministic committed reference clip exist — the toolkit plan 47-04's CI jobs use to prove arm64↔x86 numeric parity.
</success_criteria>

<output>
Create `.planning/phases/47-official-arm64-essentia-agent-image/47-03-SUMMARY.md` when done. Record the reference clip's sha256, its duration/construction, and the exact parity key set `dump_analysis.py` projects.
</output>
