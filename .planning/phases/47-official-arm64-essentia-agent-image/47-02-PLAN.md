---
phase: 47-official-arm64-essentia-agent-image
plan: 02
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - .github/workflows/docker-publish.yml
  - .github/workflows/docker-validate.yml
  - justfile
  - tests/test_deployment/test_agent_compose.py
autonomous: true
requirements: [CLOUDIMG-01, CLOUDIMG-02]
must_haves:
  truths:
    - "CI builds and pushes the arm64 image on a native arm64 runner (ubuntu-24.04-arm, no QEMU) on the same release triggers as the x86 image"
    - "Every release produces a matching -arm64-suffixed tag (latest-arm64 + <version>-arm64) alongside the x86 tags"
    - "The built arm64 image is import-smoke-checked (import phaze.tasks.agent_worker + import essentia.standard) before publish"
    - "Dockerfile.agent-arm64 is in the hadolint validation matrix"
  artifacts:
    - path: ".github/workflows/docker-publish.yml"
      provides: "build-arm64 job on a native arm64 runner with -arm64 tags + import-smoke"
      contains: "ubuntu-24.04-arm"
    - path: ".github/workflows/docker-validate.yml"
      provides: "hadolint gate for Dockerfile.agent-arm64"
      contains: "Dockerfile.agent-arm64"
    - path: "justfile"
      provides: "operator image-build-arm64 / image-push-arm64 recipes"
      contains: "image-build-arm64"
  key_links:
    - from: ".github/workflows/docker-publish.yml build-arm64"
      to: "Dockerfile.agent-arm64"
      via: "file: Dockerfile.agent-arm64, platforms: linux/arm64"
      pattern: "Dockerfile.agent-arm64"
    - from: "tests/test_deployment/test_agent_compose.py"
      to: "build-arm64 tag block"
      via: "YAML-parse assertion of -arm64 latest + version tags"
      pattern: "arm64"
---

<objective>
Wire CI to build and publish `Dockerfile.agent-arm64` on a native arm64 runner (`ubuntu-24.04-arm`, free for public repos, no QEMU) on the same release triggers as the x86 image, emitting `-arm64`-suffixed tags, gated by an import-smoke step and a hadolint matrix entry. Add operator `just` recipes and a tag-strategy regression test.

Purpose: CLOUDIMG-02 — a matching arm64 tag must appear on every release so the Phase 51 cloud-agent compose can pin it. CLOUDIMG-01 — the import-smoke proves the image actually boots (catches the v4.1.0-class regression).
Output: a new `build-arm64` job, an extended hadolint matrix, two `just` recipes, and a `-arm64` tag-strategy test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-official-arm64-essentia-agent-image/47-RESEARCH.md
@.planning/phases/47-official-arm64-essentia-agent-image/47-PATTERNS.md

<interfaces>
<!-- All analogs are in-file; mirror them, changing only runner/dockerfile/platform/tag-suffix. -->

Existing x86 build job to mirror:
  .github/workflows/docker-publish.yml lines 19-145 (build-and-push job)
    - lines 52-57: "Set lowercase image name" step (replicate — IMAGE_NAME lowercasing)
    - lines 80-86: GHCR login (frozen-SHA docker/login-action, if: github.event_name != 'pull_request')
    - lines 96-115: docker/metadata-action meta step (tag strategy)
    - lines 125-145: build-push-action (provenance: true, sbom: true)

Existing hadolint matrix to extend:
  .github/workflows/docker-validate.yml lines 22-46 (matrix include + hadolint step)
    - lines 48-56: "Test Docker build" runs on ubuntu-latest x86 (gate OFF for agent-arm64)
    - lines 64-72: api import-smoke pattern (adapt for the agent entrypoint)

Existing operator recipe to mirror:
  justfile image-push recipe lines 248-268 (bash shebang, set -e, derive OWNER/REPO from git remote, lowercase)

Existing tag-strategy test to extend:
  tests/test_deployment/test_agent_compose.py
    - test_docker_publish_workflow_tags_both_latest_and_version (lines ~165-198)
    - reusable helpers _extract_api_metadata_action_step, _metadata_action_tag_lines (lines ~160-162)

Frozen-SHA actions to reuse verbatim (project mandate):
  actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
  docker/login-action@650006c6eb7dba73a995cc03b0b2d7f5ca915bee  # v4.2.0
  docker/metadata-action@80c7e94dd9b9319bd5eb7a0e0fe9291e23a2a2e9  # v6.1.0
  docker/setup-buildx-action@d7f5e7f509e45cec5c76c4d5afdd7de93d0b3df5  # v4.1.0
  docker/build-push-action@f9f3042f7e2789586610d6e8b85c8f03e5195baf  # v7.2.0
  hadolint/hadolint-action@2332a7b74a6de0dda2e2221d575162eba76ba5e5  # v3.3.0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the build-arm64 job to docker-publish.yml (native arm64 runner, -arm64 tags, import-smoke)</name>
  <read_first>
    - .github/workflows/docker-publish.yml (the full build-and-push job — the analog to mirror)
    - .planning/phases/47-official-arm64-essentia-agent-image/47-PATTERNS.md (docker-publish.yml section — runner/login/tag/build-push blocks)
    - .planning/phases/47-official-arm64-essentia-agent-image/47-RESEARCH.md (Code Examples → build-arm64 sketch; Security §V14)
    - Dockerfile.agent-arm64 (from plan 47-01 — the file this job builds)
  </read_first>
  <action>
    Add a new `build-arm64` job to `.github/workflows/docker-publish.yml` with `runs-on: ubuntu-24.04-arm` (native aarch64, free for public repos, NO QEMU — CLOUDIMG-02), `timeout-minutes: 60` (the essentia C++ compile is ~324s cold vs the x86 job's 30), and `permissions: { contents: read, packages: write }` (least-priv, matches existing).
    Replicate the "Set lowercase image name" step (lines 52-57) so IMAGE_NAME is lowercased. Use the frozen-SHA actions from the interfaces block. GHCR login `if: github.event_name != 'pull_request'`.
    For tags, use `docker/metadata-action` with `flavor: suffix=-arm64,onlatest=true` over `images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}` and the tag set `type=raw,value=latest,enable={{is_default_branch}}` + `type=semver,pattern={{version}}` + `type=ref,event=tag` → producing `latest-arm64` and `<version>-arm64`. VERIFY the `flavor: suffix` semantics against the docker/metadata-action docs (RESEARCH Assumption A5); if `suffix`+`onlatest` does not produce both cleanly, fall back to explicit `type=raw,value=latest-arm64` + `type=semver,pattern={{version}}-arm64` tags.
    Build with `docker/build-push-action`: `file: Dockerfile.agent-arm64`, `platforms: linux/arm64`, `push: ${{ github.event_name != 'pull_request' }}`, `provenance: true`, `sbom: true` (parity with x86 — RESEARCH §V14), `cache-from: type=gha,scope=arm64` + `cache-to: type=gha,scope=arm64,mode=max` (NOT the local-cache action — that is x86 runner.temp), `build-args: TF_VERSION=2.20.0`.
    Add an import-smoke step AFTER the build (the v4.1.0 regression guard, adapted to the agent entrypoint): `docker run --rm <built-image> uv run python -c "import phaze.tasks.agent_worker; import essentia.standard"` — proves the image boots and essentia loads on arm64 (CLOUDIMG-01). To run the smoke on the built image, build with `load: true` (or a fixed local tag) before push so the image is available to `docker run` on the runner.
  </action>
  <verify>
    <automated>uv run python -c "import yaml,sys; w=yaml.safe_load(open('.github/workflows/docker-publish.yml')); j=w['jobs']['build-arm64']; assert j['runs-on']=='ubuntu-24.04-arm', j['runs-on']; blob=open('.github/workflows/docker-publish.yml').read(); assert 'Dockerfile.agent-arm64' in blob; assert 'linux/arm64' in blob; assert 'agent_worker' in blob; assert 'provenance: true' in blob; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `docker-publish.yml` has a `build-arm64` job with `runs-on: ubuntu-24.04-arm`
    - The job builds `file: Dockerfile.agent-arm64` at `platforms: linux/arm64` with `provenance: true` and `sbom: true`
    - Tags resolve to `latest-arm64` + `<version>-arm64` (suffix flavor or explicit fallback)
    - An import-smoke step runs `import phaze.tasks.agent_worker; import essentia.standard` on the built image
    - All `uses:` carry frozen SHAs; cache uses `type=gha,scope=arm64`
  </acceptance_criteria>
  <done>A native-arm64 publish job emits matching -arm64 tags on every release trigger and proves the image boots before push.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the hadolint matrix + add operator image-build/push-arm64 just recipes</name>
  <read_first>
    - .github/workflows/docker-validate.yml (matrix lines 22-33; hadolint step 42-46; x86-only build step 48-56)
    - justfile (image-push recipe lines 248-268; docker-validate recipe lines 237-246)
    - .planning/phases/47-official-arm64-essentia-agent-image/47-PATTERNS.md (docker-validate.yml + justfile sections)
  </read_first>
  <action>
    In `.github/workflows/docker-validate.yml`, add an `agent-arm64` matrix entry (`name: agent-arm64`, `dockerfile: Dockerfile.agent-arm64`, `context: .`) so hadolint lints the new Dockerfile. The "Test Docker build" (lines 48-56) and api import-smoke (64-72) steps run on `ubuntu-latest` (x86) — a full QEMU C++ compile there is forbidden/slow, so guard those steps with `if: matrix.name != 'agent-arm64'` (lint-only for arm64; the real build runs in the native build-arm64 publish job from Task 1).
    In `justfile`, add `[group('docker')]` recipes mirroring `image-push` (bash shebang, `set -e`, derive OWNER/REPO from `git remote get-url origin`, lowercase): `image-build-arm64 TAG="latest"` (runs `docker build --build-arg TF_VERSION=2.20.0 -f Dockerfile.agent-arm64 -t ${REGISTRY}/${OWNER}/${REPO}:{{TAG}}-arm64 .`) and `image-push-arm64 TAG="latest"` (build + `docker push` of the `-arm64` tag). Add `[doc(...)]` lines. These are the operator fallback to the CI path (MEMORY: workflows delegate to just).
  </action>
  <verify>
    <automated>uv run python -c "import yaml; w=yaml.safe_load(open('.github/workflows/docker-validate.yml')); names=[m['name'] for m in w['jobs']['validate-dockerfiles']['strategy']['matrix']['include']]; assert 'agent-arm64' in names, names; print('OK')" && just --list 2>/dev/null | grep -q image-build-arm64 && just --list 2>/dev/null | grep -q image-push-arm64</automated>
  </verify>
  <acceptance_criteria>
    - `docker-validate.yml` matrix includes a `agent-arm64` entry with `dockerfile: Dockerfile.agent-arm64`
    - The x86 "Test Docker build" + import-smoke steps are guarded `if: matrix.name != 'agent-arm64'`
    - `just --list` shows `image-build-arm64` and `image-push-arm64`
    - Both recipes pass `--build-arg TF_VERSION=2.20.0` and tag with the `-arm64` suffix
  </acceptance_criteria>
  <done>hadolint gates the new Dockerfile in CI; operators have a native build/push fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tag-strategy regression test for the -arm64 build job</name>
  <read_first>
    - tests/test_deployment/test_agent_compose.py (test_docker_publish_workflow_tags_both_latest_and_version + the _extract_*/_metadata_action_tag_lines helpers)
    - .planning/phases/47-official-arm64-essentia-agent-image/47-PATTERNS.md (CI-config-assertion analog)
    - .github/workflows/docker-publish.yml (the build-arm64 job from Task 1 — the test target)
  </read_first>
  <behavior>
    - Test passes: the build-arm64 job's metadata-action step emits BOTH a latest-suffixed and a version-suffixed `-arm64` tag (via `flavor: suffix=-arm64` or explicit `-arm64` raw/semver tags).
    - Test fails (guard): if the build-arm64 job is missing, or its tag block produces no `-arm64`-suffixed latest+version pair (e.g. a refactor drops the suffix), the assertion fails with a clear message.
  </behavior>
  <action>
    Add a test to `tests/test_deployment/test_agent_compose.py` (e.g. `test_docker_publish_arm64_job_tags_latest_and_version`) that YAML-parses `docker-publish.yml`, locates the `build-arm64` job, extracts its `docker/metadata-action` step (reuse/adapt `_metadata_action_tag_lines`), and asserts the resolved tags yield a `-arm64`-suffixed `latest` AND a `-arm64`-suffixed version tag — accepting either the `flavor: suffix=-arm64` form (assert `flavor` contains `suffix=-arm64` plus base latest+version patterns) or explicit `latest-arm64` / `{{version}}-arm64` raw/semver patterns. Follow the existing test's docstring-rich style; type hints on all functions (ruff py313, line-length 150, double quotes, `from __future__ import annotations`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_deployment/test_agent_compose.py -k arm64 -x -q</automated>
  </verify>
  <acceptance_criteria>
    - A new test in `tests/test_deployment/test_agent_compose.py` asserts the build-arm64 job emits `-arm64` latest + version tags
    - The test fails if the build-arm64 job or its `-arm64` tag block is absent (proven by running it against the Task-1 workflow → green)
    - `uv run pytest tests/test_deployment/test_agent_compose.py -k arm64` passes; ruff/mypy clean
  </acceptance_criteria>
  <done>A CI-parsed regression test guards the -arm64 tag strategy across future metadata-action edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → GHCR | the publish job authenticates and pushes image layers |
| third-party GitHub Actions → CI workflow | each `uses:` executes in the privileged build job |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-02 | Elevation of Privilege | GHCR push token | mitigate | Job `permissions: { contents: read, packages: write }` only (no broader scope); login uses `secrets.GITHUB_TOKEN`, `if: github.event_name != 'pull_request'` |
| T-47-03 | Tampering | third-party GitHub Actions | mitigate | Every `uses:` pinned to a frozen commit SHA with a `# vX.Y.Z` comment (repo mandate); no floating tags |
| T-47-04 | Repudiation | image provenance gap | mitigate | `provenance: true` + `sbom: true` on the arm64 build-push (parity with the x86 job) |
| T-47-06 | Denial of Service | image ships but does not boot | mitigate | Import-smoke step (`import phaze.tasks.agent_worker; import essentia.standard`) blocks publish of a broken image |
</threat_model>

<verification>
- `docker-publish.yml` parses; `build-arm64` job runs on `ubuntu-24.04-arm`, builds `Dockerfile.agent-arm64`, emits `-arm64` tags, provenance+sbom on, import-smoke present.
- `docker-validate.yml` hadolint matrix includes `agent-arm64`.
- `just --list` shows `image-build-arm64` + `image-push-arm64`.
- `uv run pytest tests/test_deployment/test_agent_compose.py -k arm64` is green.
</verification>

<success_criteria>
CI builds + pushes the arm64 image natively (no QEMU) on the same release triggers as x86, with matching `-arm64` tags, an import-smoke gate, a hadolint gate, operator recipes, and a tag-strategy regression test.
</success_criteria>

<output>
Create `.planning/phases/47-official-arm64-essentia-agent-image/47-02-SUMMARY.md` when done. Record which tag mechanism was used (`flavor: suffix` vs explicit) and confirm provenance/sbom parity.
</output>
