---
phase: 47-official-arm64-essentia-agent-image
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile.agent-arm64
autonomous: true
requirements: [CLOUDIMG-01]
must_haves:
  truths:
    - "A Dockerfile.agent-arm64 exists that builds essentia from source on Python 3.13 with TF 2.20.0 and all 4 spike fixes baked in"
    - "The image installs the phaze package on Python 3.13 WITHOUT relaxing the repo's requires-python = '>=3.14,<3.15' (the 3.13 exception is scoped to this one image)"
    - "The final image carries the runtime apt set (libatomic1 ffmpeg libsndfile1 libchromaprint-tools libpq5) so import essentia / import phaze does not crash-loop"
    - "essentia is pinned to a specific commit SHA and TensorFlow to ==2.20.0 (no moving branch tip)"
  artifacts:
    - path: "Dockerfile.agent-arm64"
      provides: "arm64 essentia-from-source agent build recipe"
      contains: "FROM python:3.13-slim-bookworm"
  key_links:
    - from: "Dockerfile.agent-arm64"
      to: "phaze.tasks.agent_worker"
      via: "CMD uv run saq phaze.tasks.agent_worker.settings"
      pattern: "agent_worker.settings"
    - from: "Dockerfile.agent-arm64"
      to: "essentia native lib"
      via: "setup_from_python.sh symlink repoint + LD_LIBRARY_PATH"
      pattern: "libtensorflow_cc.so.2"
---

<objective>
Create `Dockerfile.agent-arm64` — the production arm64 essentia analysis agent image — by transcribing the PROVEN `spike/arm64-essentia-analysis` Dockerfile into a hardened, reproducible build. Build essentia from source (the essentia-tensorflow wheel is x86-only) on `python:3.13-slim-bookworm` against TensorFlow `2.20.0`, bake all four spike fixes, keep the runtime libs that prevent the v4.0.9/v4.1.1 crash-loop class, and reconcile the repo's `requires-python = ">=3.14,<3.15"` against the 3.13 agent WITHOUT dragging the whole repo to 3.13.

Purpose: CLOUDIMG-01 — an arm64 image that boots and imports essentia is the foundation of the v5.0 Cloud Burst path (unlocks OCI Always-Free A1).
Output: `Dockerfile.agent-arm64` (passes hadolint; the actual native arm64 build + real-audio validation run in CI — plans 02 and 04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-official-arm64-essentia-agent-image/47-RESEARCH.md
@.planning/phases/47-official-arm64-essentia-agent-image/47-PATTERNS.md

<interfaces>
<!-- The spike Dockerfile is the source-of-truth pattern. It is NOT a file on this branch. -->
<!-- Read it with: git show spike/arm64-essentia-analysis:spike/arm64-essentia/Dockerfile -->
<!-- Read its README (the 4 fixes + working combo) with: git show spike/arm64-essentia-analysis:spike/arm64-essentia/README.md -->

Agent entrypoint (the image CMD must launch this):
  src/phaze/tasks/agent_worker.py — `saq phaze.tasks.agent_worker.settings`
  CRITICAL invariant: agent_worker MUST NOT transitively import phaze.database (import-boundary test D-25).

Root x86 Dockerfile (runtime-libs + uv + non-root uid-1000 analogs to COPY FROM):
  /Users/Robert/Code/public/phaze/Dockerfile lines 5-21 (runtime apt set + the .so rationale comment),
  lines 23-39 (uv install layer), lines 41-45 (uid/gid 1000 non-root user).

pyproject arch-gate precedent (mirror this style for the 3.13 reconciliation comment):
  pyproject.toml:10  requires-python = ">=3.14,<3.15"   (DO NOT relax this)
  pyproject.toml:16  essentia-tensorflow ... ; sys_platform != 'linux' or platform_machine == 'x86_64'
  pyproject.toml:175-183  [tool.uv] environments  (already enumerates linux aarch64)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transcribe the spike build recipe (3.13 + TF 2.20.0 + essentia from source + fixes #1-#3)</name>
  <read_first>
    - git show spike/arm64-essentia-analysis:spike/arm64-essentia/Dockerfile (the proven recipe — transcribe, do NOT merge the branch)
    - git show spike/arm64-essentia-analysis:spike/arm64-essentia/README.md (the 4 fixes + working combo table)
    - .planning/phases/47-official-arm64-essentia-agent-image/47-PATTERNS.md (Dockerfile.agent-arm64 section — exact quoted blocks)
    - .planning/phases/47-official-arm64-essentia-agent-image/47-RESEARCH.md (The Four Spike Fixes; Build order)
  </read_first>
  <action>
    Create `Dockerfile.agent-arm64` starting from `# syntax=docker/dockerfile:1` and `FROM python:3.13-slim-bookworm` with `ARG TF_VERSION=2.20.0`. Install the source-build apt deps from the spike (build-essential ca-certificates git wget pkg-config libeigen3-dev libyaml-dev libfftw3-dev libavcodec-dev libavformat-dev libavutil-dev libswresample-dev libsamplerate0-dev libtag1-dev libchromaprint-dev) with `--no-install-recommends` and `rm -rf /var/lib/apt/lists/*`, justified `# hadolint ignore=DL3008`.
    Install the single toolchain with `python3 -m pip install --no-cache-dir numpy pyyaml "tensorflow==${TF_VERSION}"` (justified `# hadolint ignore=DL3013`) — this is the agent-image bare-pip exception RESEARCH grants (matches libstdc++ CXX11 ABI to avoid essentia #977).
    Pin essentia to a SPECIFIC commit SHA (NOT moving master — supply-chain/reproducibility, RESEARCH §V6, Open Q4): resolve the current master HEAD via `git ls-remote https://github.com/MTG/essentia.git HEAD`, record the 40-char SHA in an `ARG ESSENTIA_SHA=<sha>` with an inline comment giving the resolution date, and build with `git clone https://github.com/MTG/essentia.git && git -C essentia checkout ${ESSENTIA_SHA}`.
    Bake FIX #1 (repoint the dangling setup_from_python.sh symlinks: `ln -sf $TF/libtensorflow_cc.so.2` → `/usr/local/lib/libpywrap_tensorflow_internal.so` and `$TF/libtensorflow_framework.so.2` → `/usr/local/lib/libtensorflow_framework.so`, then `ldconfig`), FIX #2 (`ENV LIBRARY_PATH=/usr/local/lib`), and FIX #3-partial (`ENV LD_LIBRARY_PATH=/usr/local/lib:/usr/local/lib/python3.13/site-packages/tensorflow`) exactly as quoted in PATTERNS. Run `python3 waf configure --build-static --with-python --with-tensorflow && python3 waf -j"$(nproc)" && python3 waf install && ldconfig`. AFTER the compile layer, set the FIX #3-final `ENV LD_LIBRARY_PATH=...:/usr/local/lib/python3.13/site-packages/tensorflow.libs` (placed after so it does not bust the ~324s compile cache).
    Add `RUN mkdir -p /models` (models are mounted at runtime, never baked — Pattern 3).
  </action>
  <verify>
    <automated>docker run --rm -i hadolint/hadolint < Dockerfile.agent-arm64 && grep -q 'FROM python:3.13-slim-bookworm' Dockerfile.agent-arm64 && grep -q 'tensorflow==' Dockerfile.agent-arm64 && grep -Eq 'ESSENTIA_SHA=[0-9a-f]{7,40}' Dockerfile.agent-arm64 && grep -q 'libtensorflow_cc.so.2' Dockerfile.agent-arm64 && grep -q 'tensorflow.libs' Dockerfile.agent-arm64</automated>
  </verify>
  <acceptance_criteria>
    - `Dockerfile.agent-arm64` contains `FROM python:3.13-slim-bookworm`
    - Contains `ARG TF_VERSION=2.20.0` and `tensorflow==${TF_VERSION}` (exact pin, no range)
    - Contains an `ARG ESSENTIA_SHA=` with a concrete >=7-char hex SHA and a `git checkout ${ESSENTIA_SHA}` (no bare `master` build)
    - Fix #1 symlink line maps the pywrap slot to `libtensorflow_cc.so.2`
    - Final `LD_LIBRARY_PATH` includes the `tensorflow.libs` dir and is set AFTER the `waf install` RUN layer
    - `hadolint` exits 0 (failure-threshold error)
  </acceptance_criteria>
  <done>Dockerfile.agent-arm64 reproduces the spike's proven 3-fix build with reproducible pins; hadolint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Harden for production — runtime libs, uv phaze install with the 3.13/requires-python reconciliation, non-root user, agent CMD</name>
  <read_first>
    - /Users/Robert/Code/public/phaze/Dockerfile (lines 5-21 runtime apt set + comment; 23-39 uv layer; 41-45 uid-1000 user)
    - .planning/phases/47-official-arm64-essentia-agent-image/47-PATTERNS.md (Runtime-libs-or-crash-loop; uv install pattern; Non-root uid-1000; Shared Patterns → "Python-3.13 vs requires-python reconciliation")
    - .planning/phases/47-official-arm64-essentia-agent-image/47-RESEARCH.md (Pitfall 1; Pitfall 5)
    - pyproject.toml lines 10, 16, 175-183 (requires-python + arch-gate precedent — DO NOT edit this file)
    - Dockerfile.agent-arm64 (the file being extended)
  </read_first>
  <action>
    Install the runtime apt set in the FINAL stage exactly per the root Dockerfile (`libatomic1 ffmpeg libsndfile1 libchromaprint-tools libpq5`, `--no-install-recommends`, list cleanup, justified `# hadolint ignore=DL3008`) with the same `.so` rationale comment — these MUST survive even if a multi-stage build drops the `-dev` packages, or the agent crash-loops on `import essentia`/`import phaze` (the v4.0.9/v4.1.1 incident class).
    Implement decision D-47-PY (the #1 reconciliation): KEEP `pyproject.toml` `requires-python = ">=3.14,<3.15"` UNTOUCHED — the 3.13 exception is scoped to THIS image only. Concrete mechanism: install the third-party dependency closure with uv against the already-declared aarch64 `[tool.uv] environments` entry — `COPY pyproject.toml uv.lock ./` then `uv export --frozen --no-dev --no-emit-project --format requirements-txt > /tmp/agent-reqs.txt` and `uv pip install --system --python /usr/local/bin/python3 -r /tmp/agent-reqs.txt`; then install the pure-Python phaze package itself with the requires-python gate bypassed for this one image via `pip install --no-deps --ignore-requires-python .` (uv has no `--ignore-requires-python`; this single justified bare-pip line is the documented agent-image exception, paired with an inline comment mirroring the pyproject.toml:16 essentia arch-gate style explaining WHY 3.13 is scoped here and the main contract stays 3.14). `COPY src/ src/`, `COPY alembic/ alembic/`, `COPY alembic.ini ./` before the project install.
    Add the non-root user `groupadd -g 1000 phaze && useradd -m -u 1000 -g 1000 phaze` then `USER phaze` (uid-1000 media-read parity — the scan-uid incident fix), AFTER all root-requiring layers.
    Set the agent CMD: `CMD ["uv", "run", "saq", "phaze.tasks.agent_worker.settings"]` (NOT the api uvicorn CMD — this is the agent role; `agent_worker` must not import phaze.database). Justify every `# hadolint ignore=` inline.
  </action>
  <verify>
    <automated>docker run --rm -i hadolint/hadolint < Dockerfile.agent-arm64 && grep -q 'libatomic1 ffmpeg libsndfile1 libchromaprint-tools libpq5' Dockerfile.agent-arm64 && grep -q 'ignore-requires-python' Dockerfile.agent-arm64 && grep -q 'agent_worker.settings' Dockerfile.agent-arm64 && grep -q 'useradd -m -u 1000' Dockerfile.agent-arm64 && ! grep -q '3.13,<3.15' pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - Final stage installs the exact runtime set `libatomic1 ffmpeg libsndfile1 libchromaprint-tools libpq5`
    - `pyproject.toml` `requires-python` is unchanged (`>=3.14,<3.15`) — verified by `grep` showing it was NOT relaxed to 3.13
    - The phaze package install uses `--ignore-requires-python` (the scoped 3.13 mechanism), with an inline justification comment
    - CMD launches `saq phaze.tasks.agent_worker.settings`
    - `USER phaze` (uid 1000) is set after root layers; hadolint exits 0
  </acceptance_criteria>
  <done>The image installs phaze on 3.13 without touching the main app's 3.14 contract, keeps the runtime libs, and runs as the agent role under uid 1000.</done>
</task>

<task type="auto">
  <name>Task 3: Bake the dual-OpenMP fix (#4) env — the open production blocker</name>
  <read_first>
    - git show spike/arm64-essentia-analysis:spike/arm64-essentia/README.md (fix #4 description + "Remaining work" #1)
    - git show spike/arm64-essentia-analysis:spike/arm64-essentia/run_test.py (how the spike SIDESTEPPED it with np.sin — production must not)
    - .planning/phases/47-official-arm64-essentia-agent-image/47-RESEARCH.md (Pitfall 2; Assumption A1; The Four Spike Fixes #4)
    - Dockerfile.agent-arm64 (the file being extended)
  </read_first>
  <action>
    Bake the dual-OpenMP runtime fix (spike fix #4 — the one OPEN production blocker; NOT in the spike Dockerfile because the smoke test dodged it with `np.sin`). Set `ENV OMP_NUM_THREADS=1` as the primary mitigation, placed in an env layer near the final `LD_LIBRARY_PATH` (so it persists to runtime, does not bust the compile cache). Add an inline comment documenting the conflict (TF's LLVM libomp vs numpy's OpenBLAS libgomp colliding in one process when numpy C-extensions load after essentia/TF) and the documented fallback (`LD_PRELOAD` a single OpenMP runtime from `/usr/local/lib/python3.13/site-packages/tensorflow.libs/libomp-*.so` if `OMP_NUM_THREADS=1` alone is insufficient).
    CRITICAL: this fix CANNOT be proven by a hadolint lint or a synthetic `np.sin` signal. Its real validation is the real-audio analyze run through `phaze.services.analysis.analyze_file` in the parity-guard CI job (plan 47-04) on a native arm64 runner. Record in the SUMMARY that fix #4's runtime validation is owned by plan 47-04 (Assumption A1 — do NOT assume benign).
  </action>
  <verify>
    <automated>docker run --rm -i hadolint/hadolint < Dockerfile.agent-arm64 && grep -q 'OMP_NUM_THREADS=1' Dockerfile.agent-arm64 && grep -qi 'libomp' Dockerfile.agent-arm64</automated>
  </verify>
  <acceptance_criteria>
    - `Dockerfile.agent-arm64` contains `ENV` ... `OMP_NUM_THREADS=1`
    - An inline comment names the dual-OpenMP conflict and the `LD_PRELOAD` fallback path under `tensorflow.libs`
    - The SUMMARY explicitly hands fix #4 runtime validation to plan 47-04 (real audio, not np.sin)
    - hadolint exits 0
  </acceptance_criteria>
  <done>The OpenMP env mitigation is baked and documented; its runtime proof is explicitly deferred to the arm64 parity job.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream sources → image build | essentia git + TF/numpy/pyyaml PyPI wheels + Debian apt pulled into the build |
| build → runtime | the compiled native libs + phaze package the agent will execute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Tampering | essentia source / TF wheel pin | mitigate | Pin essentia to a specific commit SHA (no moving master) and TensorFlow to `==2.20.0` exact; clone only from the official `github.com/MTG/essentia` repo |
| T-47-05 | Information Disclosure | image contents | mitigate | No secrets baked into the image; models mounted at runtime (`mkdir /models` only), never downloaded at build |
| T-47-06 | Denial of Service | crash-loop on missing runtime `.so` | mitigate | Keep the runtime apt set (libatomic1/ffmpeg/libsndfile1/libchromaprint-tools/libpq5) in the final stage; import-smoke validation owned by plan 47-02 |
| T-47-SC | Tampering | pip/apt installs in-image | accept | No NEW package dependencies introduced (RESEARCH Package Legitimacy Audit: all pre-existing/distro packages; slopcheck N/A); pins above bound the supply-chain surface |
</threat_model>

<verification>
- `Dockerfile.agent-arm64` passes hadolint at `failure-threshold: error`.
- Content gates: 3.13 base, exact TF pin, essentia SHA pin, runtime libs, OMP env, agent CMD, requires-python untouched.
- Native arm64 build + boot + essentia import are validated by plan 47-02; real-audio analyze (fix #4) by plan 47-04.
</verification>

<success_criteria>
Dockerfile.agent-arm64 exists, is hadolint-clean, reproducibly pins essentia+TF, scopes the 3.13 exception to this image without relaxing the repo's 3.14 contract, keeps the crash-loop-preventing runtime libs, and launches the agent role.
</success_criteria>

<output>
Create `.planning/phases/47-official-arm64-essentia-agent-image/47-01-SUMMARY.md` when done. Record the resolved `ESSENTIA_SHA`, the exact 3.13 reconciliation mechanism used, and the explicit hand-off of fix #4 runtime validation to plan 47-04.
</output>
