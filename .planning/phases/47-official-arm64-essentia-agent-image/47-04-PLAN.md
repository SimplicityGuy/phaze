---
phase: 47-official-arm64-essentia-agent-image
plan: 04
type: execute
wave: 3
depends_on: ["47-01", "47-02", "47-03"]
files_modified:
  - .github/workflows/docker-publish.yml
  - justfile
  - docs/arm64-agent-image.md
autonomous: true
requirements: [CLOUDIMG-01, CLOUDIMG-02, CLOUDIMG-03]
must_haves:
  truths:
    - "An x86 CI job produces golden-x86.json by running dump_analysis.py over the reference clip with models on the x86 image"
    - "A native-arm64 parity-guard job runs the SAME dump over the arm64 image and compares to the x86 golden, FAILING the build on any parity break"
    - "The arm64 image is built from the warmed cache (load), parity-checked, and ONLY THEN pushed to GHCR within the same parity-guard job — a parity-divergent image never reaches the registry (the push step is ordered AFTER the build-blocking compare)"
    - "The parity-guard job is the runtime validation of spike fix #4 (real audio through analyze_file, not np.sin) — proving no dual-OpenMP segfault on real workloads"
    - "Docs explain the 3.13 pin, the 4 fixes, and the build + parity commands"
  artifacts:
    - path: ".github/workflows/docker-publish.yml"
      provides: "parity-golden-x86 (golden artifact) + parity-guard (arm64 cache-build -> compare -> GATED registry push) jobs"
      contains: "parity-guard"
    - path: "docs/arm64-agent-image.md"
      provides: "arm64 agent image documentation (3.13 pin, 4 fixes, build/parity commands)"
    - path: "justfile"
      provides: "operator parity-check recipe"
      contains: "parity-check"
  key_links:
    - from: "parity-guard job"
      to: "scripts/parity/compare_analysis.py"
      via: "uv run python scripts/parity/compare_analysis.py golden-x86.json actual.json"
      pattern: "compare_analysis"
    - from: "parity-guard job"
      to: "build-arm64 + parity-golden-x86"
      via: "needs: [build-arm64, parity-golden-x86]"
      pattern: "needs"
    - from: "parity-guard gated push step"
      to: "GHCR -arm64 tags"
      via: "build-push-action push:true ordered after the build-blocking compare step"
      pattern: "needs.build-arm64.outputs.tags"
    - from: "parity-guard job"
      to: "phaze.services.analysis.analyze_file (fix #4 real-audio validation)"
      via: "docker run arm64 image -> dump_analysis.py over reference.wav"
      pattern: "dump_analysis"
---

<objective>
Close CLOUDIMG-03 (and the fix-#4 half of CLOUDIMG-01) by wiring the parity guard into CI: an x86 job that emits `golden-x86.json` from the reference clip, and a native-arm64 `parity-guard` job that runs the SAME `dump_analysis.py` over the arm64 image and compares to the golden via `compare_analysis.py`, failing the build on any break. This job is also the FIRST time spike fix #4 (dual-OpenMP) is validated on real audio through `analyze_file` rather than the spike's `np.sin`. Document the image (3.13 pin, the 4 fixes, build + parity commands).

Purpose: CLOUDIMG-03 — the build must fail if arm64 analysis diverges from x86. CLOUDIMG-01 — real-audio analyze must not segfault on arm64.
Output: two CI jobs in `docker-publish.yml`, a `just parity-check` recipe, and `docs/arm64-agent-image.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-official-arm64-essentia-agent-image/47-RESEARCH.md
@.planning/phases/47-official-arm64-essentia-agent-image/47-PATTERNS.md
@.planning/phases/47-official-arm64-essentia-agent-image/47-03-SUMMARY.md

<interfaces>
<!-- This plan CONSUMES the artifacts produced by plans 47-01/02/03. -->

From plan 47-01: Dockerfile.agent-arm64 (the image to validate; CMD = saq agent_worker; OMP_NUM_THREADS=1 baked).
From plan 47-02: docker-publish.yml build-arm64 job (runs-on ubuntu-24.04-arm) that LOADS (does NOT push) the image, warms `cache-to: type=gha,scope=arm64,mode=max`, and exposes the resolved `-arm64` tags as `needs.build-arm64.outputs.tags`. The parity-guard job rebuilds the SAME image from that cache (`cache-from: type=gha,scope=arm64`, `load: true`) — content-addressed cache => identical digest — then (after parity passes) pushes those exact tags WITH provenance/sbom.
From plan 47-03:
  scripts/parity/dump_analysis.py <audio> <models_dir> [--out]   # run INSIDE each image
  scripts/parity/compare_analysis.py <golden.json> <actual.json> [--atol]   # run on the RUNNER, non-zero exit on break
  scripts/parity/reference.wav                                    # the shared input
  just parity-golden-regen                                        # operator golden regen

Models provisioning (both jobs need ~33 .pb models, ~3GB — cache it):
  scripts/download-models.sh models   (justfile download-models recipe)
  cache with actions/cache (frozen-SHA) keyed on the model URL list

x86 image to dump the golden from: the api image built by the existing build-and-push job
  (ghcr.io/<owner>/phaze:<tag>) — essentia + phaze installed; mount the repo's scripts/ + the models dir.
Agent entrypoint note: scripts/parity/ is NOT COPYed into either image — mount the checkout
  (`-v "$PWD/scripts:/scripts" -v "$models:/models" <image> uv run python /scripts/parity/dump_analysis.py /scripts/parity/reference.wav /models`).

Frozen-SHA actions (reuse — all 40-hex pinned, repo mandate):
  actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0          # v7.0.0
  actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a   # v7.0.1
  actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
  actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae            # v5.0.5  (already used elsewhere in repo)
  docker/login-action@650006c6eb7dba73a995cc03b0b2d7f5ca915bee       # v4.2.0
  docker/setup-buildx-action@d7f5e7f509e45cec5c76c4d5afdd7de93d0b3df5 # v4.1.0
  docker/build-push-action@f9f3042f7e2789586610d6e8b85c8f03e5195baf  # v7.2.0
  docker/metadata-action@80c7e94dd9b9319bd5eb7a0e0fe9291e23a2a2e9    # v6.1.0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: parity-golden-x86 job (x86 analysis -> golden artifact) + parity-check just recipe</name>
  <read_first>
    - .github/workflows/docker-publish.yml (build-and-push job — x86 image + GHCR login + the env REGISTRY/IMAGE_NAME pattern; build-arm64 job added in 47-02)
    - .github/workflows/docker-validate.yml lines 64-72 (docker run assertion shape)
    - scripts/parity/dump_analysis.py + scripts/download-models.sh (from 47-03)
    - .planning/phases/47-official-arm64-essentia-agent-image/47-PATTERNS.md (parity-guard / golden generation; justfile parity-check)
    - .planning/phases/47-official-arm64-essentia-agent-image/47-RESEARCH.md (Pitfall 4 caching; Pattern 2)
  </read_first>
  <action>
    Add a `parity-golden-x86` job to `docker-publish.yml` on `ubuntu-latest` (x86), `permissions: { contents: read }`. Steps: checkout (frozen-SHA); restore/save the models via `actions/cache` (`27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5`) keyed on the model URL list, populating `./models` with `scripts/download-models.sh models` on cache miss; GHCR login (`if: github.event_name != 'pull_request'`) and pull the freshly-built x86 api image (or `needs: build-and-push`); produce the golden by DELEGATING to a `just` recipe (Warning 4 / MEMORY: workflows delegate to just) — call `just parity-dump <x86-image> ./models scripts/parity/golden-x86.json` (the thin recipe added below wraps the `docker run -v "$PWD/scripts:/scripts" -v "$PWD/models:/models" <image> uv run python /scripts/parity/dump_analysis.py /scripts/parity/reference.wav /models --out ...` invocation) INSTEAD of an inline `docker run` in the YAML; upload `golden-x86.json` via `actions/upload-artifact` (`043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1`). The buildx/login/cache/upload-artifact steps stay native action steps (cannot be just-wrapped), consistent with the existing docker-publish.yml. This is the authoritative golden producer (RESEARCH Pattern 2).
    Add two `[group('docker')]` recipes to `justfile` (bash, set -e, mirror image-push): (1) `parity-dump IMAGE MODELS="./models" OUT="scripts/parity/actual.json"` — runs `docker run -v "$PWD/scripts:/scripts" -v "$PWD/{{MODELS}}:/models" {{IMAGE}} uv run python /scripts/parity/dump_analysis.py /scripts/parity/reference.wav /models --out /scripts/parity/$(basename {{OUT}})` and copies the result to `{{OUT}}` — the SHARED dump path BOTH CI jobs (parity-golden-x86 + parity-guard) delegate to so the workflow calls just, not inline docker (Warning 4 / MEMORY); and (2) `parity-check TAG="latest"` — the operator mirror of the CI parity-guard: provision models, `just parity-dump <arm64-image>`, then `uv run python scripts/parity/compare_analysis.py scripts/parity/golden-x86.json scripts/parity/actual.json`.
    Pin every new action to a frozen commit SHA with a `# vX.Y.Z` comment (project mandate).
  </action>
  <verify>
    <automated>uv run python -c "import yaml; w=yaml.safe_load(open('.github/workflows/docker-publish.yml')); assert 'parity-golden-x86' in w['jobs']; b=open('.github/workflows/docker-publish.yml').read(); assert 'upload-artifact' in b; assert 'parity-dump' in b or 'dump_analysis' in b; print('OK')" && just --list 2>/dev/null | grep -q parity-dump && just --list 2>/dev/null | grep -q parity-check</automated>
  </verify>
  <acceptance_criteria>
    - `docker-publish.yml` has a `parity-golden-x86` job on `ubuntu-latest` that produces the golden by DELEGATING to `just parity-dump` (not an inline `docker run`) and uploads `golden-x86.json`
    - Models are provisioned via `actions/cache` + `scripts/download-models.sh` (not re-downloaded uncached every run)
    - `just --list` shows `parity-dump` AND `parity-check`
    - Every new `uses:` carries a frozen 40-hex SHA + version comment (upload-artifact `043fb46…# v7.0.1`, cache `27d5ce7…# v5.0.5`)
  </acceptance_criteria>
  <done>CI emits an x86 golden from the shared dump tool; operators have a local parity-check mirror.</done>
</task>

<task type="auto">
  <name>Task 2: parity-guard job (native arm64 compare, build-blocking) — also validates fix #4 on real audio</name>
  <read_first>
    - .github/workflows/docker-publish.yml (build-arm64 job from 47-02 — image tag outputs; parity-golden-x86 from Task 1)
    - scripts/parity/compare_analysis.py + dump_analysis.py (from 47-03 — the contract)
    - .planning/phases/47-official-arm64-essentia-agent-image/47-RESEARCH.md (Pitfall 2/3; Assumption A1 + A4; Validation Architecture signals table)
    - .planning/phases/47-official-arm64-essentia-agent-image/47-01-SUMMARY.md (fix #4 hand-off note; OMP_NUM_THREADS=1)
  </read_first>
  <action>
    Add a `parity-guard` job to `docker-publish.yml` on `ubuntu-24.04-arm` (native), `needs: [build-arm64, parity-golden-x86]`, `permissions: { contents: read, packages: write }` (the `packages: write` is for the FINAL gated push step (d) — NOT any pre-parity publish). Steps IN ORDER (Warning 1 — push is gated on parity, so a divergent image never reaches GHCR):
    (a) checkout; `docker/setup-buildx-action`; `actions/cache` (`27d5ce7…# v5.0.5`) + `scripts/download-models.sh` for models; `actions/download-artifact` (`3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1`) for `golden-x86.json`.
    (b) REBUILD the arm64 image from the warmed cache so it is local on this runner: `docker/build-push-action` with `file: Dockerfile.agent-arm64`, `platforms: linux/arm64`, `push: false` + `load: true`, `cache-from: type=gha,scope=arm64`, `tags: ${{ needs.build-arm64.outputs.tags }}`. The shared `scope=arm64` cache (warmed by build-arm64) makes this a near-instant replay — content-addressed cache => the SAME image digest build-arm64 import-smoked. (build-push-action cannot `load` + `push` together — Info 2 — so we load here, push in step (d).)
    (c) PARITY COMPARE (build-blocking): delegate the dump to `just parity-dump <first-of-needs.build-arm64.outputs.tags> ./models scripts/parity/actual.json` (Warning 4 / workflows delegate to just), then on the runner `uv run python scripts/parity/compare_analysis.py scripts/parity/golden-x86.json scripts/parity/actual.json --atol <EPSILON>`. A non-zero `compare_analysis.py` exit FAILS this step → the job stops here and step (d) is SKIPPED (CLOUDIMG-03 — a parity-divergent image is NEVER pushed).
    (d) GATED REGISTRY PUSH (only reached if (c) passed): `if: github.event_name != 'pull_request'`, GHCR login (`docker/login-action` `650006c6…# v4.2.0`), then `docker/build-push-action` with `push: true`, `provenance: true`, `sbom: true` (parity with the x86 job — RESEARCH §V14), `cache-from: type=gha,scope=arm64`, `tags: ${{ needs.build-arm64.outputs.tags }}`, `platforms: linux/arm64`, `file: Dockerfile.agent-arm64`. This is the cache replay of the validated image, now pushed WITH attestations — the TRUE parity gate (CLOUDIMG-02 "pushed by CI on a native arm64 runner" + CLOUDIMG-03 "fails the build on divergence" in one ordered job). Because steps after a failed step do not run, a red compare guarantees no push.
    Pick `EPSILON` empirically: this run produces the first real x86↔arm64 deltas — start at the comparator default, inspect the job's diff output, and set `--atol` just above the observed model-score noise floor; BPM/key must be EXACT (if BPM/key differ, that is a real divergence to investigate per Assumption A4, NOT a tolerance to widen). Record the chosen epsilon + observed deltas in the SUMMARY.
    CRITICAL (fix #4 / CLOUDIMG-01): because this docker run drives REAL audio (reference.wav) through the actual `analyze_file` (MonoLoader + RhythmExtractor2013 + KeyExtractor + musicnn/vggish/effnet + numpy aggregation) on arm64, a green run is the runtime proof that the dual-OpenMP mitigation (`OMP_NUM_THREADS=1`) holds. If the job SIGSEGVs (faulthandler native stack names libgomp/libomp), apply the LD_PRELOAD fallback documented in Dockerfile.agent-arm64 (plan 47-01) and note it.
  </action>
  <verify>
    <automated>uv run python -c "import yaml; w=yaml.safe_load(open('.github/workflows/docker-publish.yml')); j=w['jobs']['parity-guard']; assert j['runs-on']=='ubuntu-24.04-arm', j['runs-on']; assert set(['build-arm64','parity-golden-x86']).issubset(set(j['needs'])), j['needs']; b=open('.github/workflows/docker-publish.yml').read(); assert 'compare_analysis.py' in b; assert 'provenance: true' in b; assert 'load: true' in b; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `parity-guard` job runs on `ubuntu-24.04-arm` with `needs: [build-arm64, parity-golden-x86]`
    - It REBUILDS the arm64 image from `cache-from: type=gha,scope=arm64` with `load: true`/`push: false` (identical digest to build-arm64), then dumps via `just parity-dump` and compares against the downloaded golden
    - A non-zero `compare_analysis.py` exit fails the COMPARE step BEFORE the push step → the gated registry push is skipped (a divergent image never reaches GHCR — the Warning-1 gate)
    - The registry push is a LATER step (`push: true` + `provenance: true` + `sbom: true`, `if: github.event_name != 'pull_request'`) reached only when the compare passes — proving CLOUDIMG-02's push is parity-gated
    - All new actions (download-artifact `3e5f45b2…`, cache `27d5ce7…`) carry frozen 40-hex SHAs
    - The chosen `--atol` epsilon + observed deltas are recorded in the SUMMARY; BPM/key asserted exact
  </acceptance_criteria>
  <done>A native-arm64 job rebuilds the cached image, proves arm64 analysis matches x86 within tolerance (and fix #4 holds on real audio), and ONLY THEN pushes it to GHCR — the registry never receives a parity-divergent image.</done>
</task>

<task type="auto">
  <name>Task 3: Document the arm64 agent image</name>
  <read_first>
    - Dockerfile.agent-arm64 (from 47-01 — the 3.13 pin, 4 fixes, runtime libs, CMD)
    - .planning/phases/47-official-arm64-essentia-agent-image/47-RESEARCH.md (Summary; The Four Spike Fixes; Standard Stack)
    - git show spike/arm64-essentia-analysis:spike/arm64-essentia/README.md (the working-combo table + fix narrative — source material)
    - an existing service README for house style (e.g. services/audfprint/README.md if present, else the root README badge/marker conventions)
  </read_first>
  <action>
    Create `docs/arm64-agent-image.md` (GSD doc marker on line 1 per the project README convention). Document: WHY the agent image pins Python 3.13 (no cp314 TensorFlow/essentia aarch64 wheel exists) while main/api stays 3.14, and HOW the requires-python reconciliation is scoped to this image without relaxing the repo's 3.14 contract (decision D-47-PY from 47-01); the TF `2.20.0` + essentia commit-SHA pins (supply-chain); the four spike fixes (symlink repoint, LIBRARY_PATH, vendored libomp on LD_LIBRARY_PATH, dual-OpenMP OMP_NUM_THREADS=1) in plain language; the runtime-libs-or-crash-loop rule; the build commands (`just image-build-arm64` / the CI `build-arm64` job); the parity workflow (`just parity-dump`, `just parity-check`, `just parity-golden-regen`, the CI parity-guard gate — the arm64 image is built/loaded, parity-checked, and pushed to GHCR ONLY after parity passes, BPM/key exact + model-score epsilon); and the `-arm64` tag naming consumed by the Phase 51 cloud-agent compose. Keep README badges/marker style per project convention (single-line badges; GSD marker line 1).
  </action>
  <verify>
    <automated>test -f docs/arm64-agent-image.md && grep -q '3.13' docs/arm64-agent-image.md && grep -qi 'omp' docs/arm64-agent-image.md && grep -q 'parity' docs/arm64-agent-image.md && grep -q 'arm64' docs/arm64-agent-image.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/arm64-agent-image.md` exists with a GSD marker on line 1
    - Documents the 3.13 pin rationale + the scoped requires-python reconciliation, the TF/essentia pins, all 4 fixes, the build commands, and the parity workflow
    - References the `-arm64` tag naming for the Phase 51 cloud agent
  </acceptance_criteria>
  <done>The arm64 agent image has a current, accurate service doc (project README-per-service mandate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built arm64 image → release | the parity guard decides whether a build is allowed to publish |
| x86 golden artifact → arm64 compare | the golden crosses jobs as a workflow artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-08 | Tampering | a diverging arm64 image reaching GHCR | mitigate | The registry push is a step ORDERED AFTER the build-blocking compare in the same parity-guard job — a non-zero `compare_analysis.py` exit stops the job before the push step runs, so a divergent image (BPM/key exact + musicnn/vggish/effnet/danceability epsilon — full production surface, Pitfall 3) is NEVER pushed (Warning 1) |
| T-47-09 | Denial of Service | dual-OpenMP segfault on real workloads | mitigate | parity-guard runs REAL audio through analyze_file (not np.sin), surfacing a fix-#4 regression before release (Assumption A1) |
| T-47-03 | Tampering | new CI actions (cache/artifact) | mitigate | upload-artifact (`043fb46…# v7.0.1`), download-artifact (`3e5f45b2…# v8.0.1`), cache (`27d5ce7…# v5.0.5`) pinned to frozen 40-hex commit SHAs (project mandate) |
| T-47-02 | Elevation of Privilege | GHCR token in the arm64 guard job | mitigate | `permissions: { contents: read, packages: write }` only; login `if: github.event_name != 'pull_request'` |
</threat_model>

<verification>
- `docker-publish.yml` parses; `parity-golden-x86` (x86, uploads golden) and `parity-guard` (arm64, `needs` both, runs compare) jobs exist.
- `compare_analysis.py` is invoked (via `just parity-dump` + compare) with the downloaded golden + the arm64 actual; a non-zero exit blocks the build BEFORE the gated registry push step, so the push only runs on parity success.
- `just parity-check` present; `docs/arm64-agent-image.md` present and complete.
- A green parity-guard run is the recorded proof that fix #4 holds on real audio (epsilon + deltas in SUMMARY).
</verification>

<success_criteria>
CI builds the arm64 image from cache, fails the build if its analysis diverges from x86 (BPM/key exact, scores within epsilon) BEFORE any registry push, pushes the validated image to GHCR (with provenance/sbom) only on parity success, the same job proves fix #4 holds on real audio, operators have a local parity mirror, and the image is documented.
</success_criteria>

<output>
Create `.planning/phases/47-official-arm64-essentia-agent-image/47-04-SUMMARY.md` when done. Record the chosen epsilon, the observed x86↔arm64 deltas, whether BPM/key were exactly equal, and whether the LD_PRELOAD fix-#4 fallback was needed.
</output>
