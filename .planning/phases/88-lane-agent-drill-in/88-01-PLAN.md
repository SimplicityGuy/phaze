---
phase: 88-lane-agent-drill-in
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/templates/pipeline/partials/_detail_pane.html
  - src/phaze/templates/pipeline/partials/_lane_card.html
  - src/phaze/templates/admin/partials/agents_table.html
  - src/phaze/templates/pipeline/partials/analyze_workspace.html
  - src/phaze/templates/admin/agents.html
  - src/phaze/templates/shell/shell.html
  - src/phaze/routers/pipeline.py
  - src/phaze/routers/admin_agents.py
  - tests/analyze/routers/test_lane_drill_survival.py
  - tests/agents/routers/test_agent_drill_survival.py
autonomous: true
requirements: [DRILL-03]
must_haves:
  truths:
    - "D-01: the detail renders in a SIBLING #detail-pane that is a separate swap target OUTSIDE the polled #analyze-lanes / #agents-table-section regions, so the 5s outerHTML poll never clobbers it."
    - "D-02: selection is carried in a URL param (?lane={backend_id} / ?agent={agent_id}) set via hx-push-url; the polled list endpoints re-read the param and re-apply the selected-highlight after every swap, and a reload with the param renders the pane open."
    - "D-03: the shared shell scaffolds an own bounded hx-trigger=\"every 5s\" tick that is removed with the pane on dismiss (no orphan loop)."
    - "D-08: one shared _detail_pane.html shell owns the swap-target container, the ?param/hx-push-url wiring, open/dismiss, and the keyboard/a11y contract; lane and agent bodies are parameterized content slots."
    - "D-09: cards/rows are role=button tabindex=0 with Enter+Space activation and a visible focus-visible ring; the pane is role=region (NON-modal, no aria-modal/x-trap) with a visible ✕ Close AND Esc dismiss; dismissing clears the ?param and returns focus to the originating trigger by STABLE id."
  artifacts:
    - path: "src/phaze/templates/pipeline/partials/_detail_pane.html"
      provides: "Shared non-modal detail-pane shell (swap target, header + ✕ Close, D-09 keyboard/focus/dismiss JS, D-03 own-tick scaffold, resting empty state)"
      min_lines: 40
    - path: "src/phaze/templates/pipeline/partials/_lane_card.html"
      provides: "Lane-card drill-in trigger (role=button, stable id lane-trigger-{id}, hx-get/hx-target/hx-push-url, selected-ring from ?lane=)"
      contains: "lane-trigger-"
    - path: "src/phaze/templates/admin/partials/agents_table.html"
      provides: "Agent-row drill-in trigger (role=button, stable id agent-trigger-{id}, selected-ring from ?agent=) + #agents-table-section self-poll carries ?agent= via hx-vals"
      contains: "agent-trigger-"
  key_links:
    - from: "src/phaze/routers/pipeline.py"
      to: "_analyze_lanes.html selected_lane"
      via: "pipeline_stats_partial reads ?lane= (carried by the shell.html #pipeline-stats poll's hx-vals) and threads selected_lane into the grid include"
      pattern: "selected_lane"
    - from: "src/phaze/routers/admin_agents.py"
      to: "agents_table.html selected_agent"
      via: "page/table_partial read ?agent= (carried by the #agents-table-section self-poll's hx-vals) and thread selected_agent into the table include"
      pattern: "selected_agent"
    - from: "_lane_card.html / agents_table.html"
      to: "#detail-pane"
      via: "hx-get -> hx-target=#detail-pane hx-swap=innerHTML + hx-push-url ?lane=/?agent="
      pattern: "hx-target=\"#detail-pane\""
    - from: "src/phaze/templates/shell/shell.html #pipeline-stats"
      to: "/pipeline/stats?lane="
      via: "the single persistent chrome-level 5s poll element carries ?lane= via hx-vals so the auto tick re-applies the selected ring"
      pattern: "hx-vals"
---

<objective>
Build the DRILL-03 foundation: one shared, NON-modal `_detail_pane.html` shell plus the
keyboard-accessible, poll-surviving trigger + selection mechanism that BOTH wave-2 bodies
(lane detail, agent activity) slot into.

This plan makes lane cards and agent rows clickable drill-in triggers (`role=button`, Enter/Space,
focus ring), hosts a `#detail-pane` sibling swap target OUTSIDE each 5s `outerHTML` poll region,
and wires the `?lane=`/`?agent=` URL-param selection so the polled list re-applies the selected
highlight after every swap and a reload re-opens the detail (D-01/D-02/D-08/D-09). The pane's
own-tick live-refresh scaffold (D-03) is defined here; the two endpoint bodies land in wave 2.

Purpose: Interface-first — the shell contract (`#detail-pane` target id, `lane-trigger-{id}` /
`agent-trigger-{id}` stable ids, the `?param` protocol, the own-tick placeholder) is defined once
so plans 02/03 implement bodies against it with no re-litigation and no drift.
Output: `_detail_pane.html` (new), trigger + host-page edits, `?param` re-read on both polls, and
two RED-first survival test modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-lane-agent-drill-in/88-CONTEXT.md
@.planning/phases/88-lane-agent-drill-in/88-RESEARCH.md
@.planning/phases/88-lane-agent-drill-in/88-PATTERNS.md
@.planning/phases/88-lane-agent-drill-in/88-UI-SPEC.md

<interfaces>
<!-- Contract this plan DEFINES for wave-2 bodies. Do not re-derive downstream. -->
Shared shell:  src/phaze/templates/pipeline/partials/_detail_pane.html
  - Swap target id:            #detail-pane  (hx-swap="innerHTML")
  - Lane trigger stable id:    lane-trigger-{{ lane.id }}
  - Agent trigger stable id:   agent-trigger-{{ agent.id }}
  - URL params:                ?lane={backend_id}   ?agent={agent_id}
  - Lane push-url base:        /s/analyze?lane={id}   (analyze workspace served at /s/analyze — shell.py:99)
  - Agent push-url base:       /admin/agents?agent={id}
  - Own-tick placeholder:      wave-2 body carries hx-get={endpoint} hx-trigger="every 5s" hx-target="#detail-pane" hx-swap="innerHTML"
  - Pane ARIA:                 role="region" aria-labelledby="{header-id}" aria-live="polite"; header <h2 tabindex="-1">; ✕ Close aria-label="Close detail"
Poll param protocol (this plan wires the RE-READ; wave-2 wires the endpoints the triggers hx-get):
  - The lane poll is a SINGLE persistent chrome-level element — shell.html:196-200 `#pipeline-stats`
    (hx-get="/pipeline/stats" hx-trigger="every 5s"). _analyze_lanes.html has NO hx-get of its own;
    it is a pure OOB-swap target driven by that one shell element. So the ?lane= must be added to
    the #pipeline-stats element via hx-vals (RESEARCH Open Question 2 — htmx does NOT auto-append).
  - The agent poll IS a genuine self-poll: #agents-table-section (agents_table.html:20-27,
    hx-get="/admin/agents/_table"). The ?agent= hx-vals goes on that element itself.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 survival test scaffolds + shared _detail_pane.html shell</name>
  <files>src/phaze/templates/pipeline/partials/_detail_pane.html, tests/analyze/routers/test_lane_drill_survival.py, tests/agents/routers/test_agent_drill_survival.py</files>
  <read_first>
    - 88-PATTERNS.md §"_detail_pane.html (NEW, shared shell)" — the record_host.html focus-on-open, guarded Esc, and focus-return-by-STABLE-ID excerpts to copy (borrow discipline, DROP the modal trap).
    - 88-UI-SPEC.md §"Keyboard & A11y Contract" and §"Detail-pane states" — pane role=region NON-modal, aria-live=polite, ✕ Close, resting empty-state copy ("No lane selected" / "No agent selected"), "Last refreshed Ns ago" caption, degrade caption.
    - src/phaze/templates/shell/partials/record_host.html — Esc + $nextTick heading-focus discipline (borrow); note it IS modal (aria-modal/x-trap) — do NOT copy the trap.
    - src/phaze/templates/shell/partials/cmdk_modal.html — the $nextTick + getElementById(...).focus() deferral pattern to copy (id is DERIVED here, not captured).
    - src/phaze/templates/admin/partials/agents_table.html:82-109 — the Alpine x-init setInterval "last refreshed" countdown + the role="alert" refresh-error banner to reuse verbatim.
    - tests/agents/routers/test_admin_agents.py — smoke-app + client fixture + HTML-fragment / HX-Request assertion idiom.
  </read_first>
  <action>
    Create `_detail_pane.html` as the shared NON-modal shell. It renders an outer `#detail-pane`
    container with an Alpine `x-data` island exposing `show(id)` / `hide()` (per 88-PATTERNS.md excerpt):
    on OPEN move focus to a header `<h2 tabindex="-1">` (parks focus off the poll-doomed trigger,
    Pitfall 1); on DISMISS set `open=false`, clear the `?param` via `history.replaceState` to the base
    path, and `$nextTick(() => document.getElementById(this.selectedId)?.focus())` where `selectedId`
    is `'lane-trigger-'+id` or `'agent-trigger-'+id` (STABLE id, never a captured node). Add a guarded
    `@keydown.escape.window="if (open && !document.querySelector('[aria-modal=\"true\"]')) hide()"`.
    Pane markup: `role="region"`, `aria-labelledby` to the header id, `aria-live="polite"`, a visible
    `✕ Close` control (`aria-label="Close detail"`) that calls `hide()`. Include a `{% block body %}`-style
    slot (Jinja `{% if pane_body %}{% include pane_body %}{% endif %}`) so wave-2 bodies render inside;
    when no param is set, render the resting empty state from the UI-SPEC Copywriting table. Add the
    "Last refreshed Ns ago" caption (reuse agents_table.html:82-86 setInterval) and a degrade/refresh-error
    `role="alert"` caption slot ("Live data unavailable — retrying on the next tick." / "Detail refresh
    failed — retrying every 5s."). Reuse the frozen box tokens (`rounded-xl border … dark:bg-phaze-panel`
    `p-4`/`p-6`); introduce ZERO new colors/fonts. Do NOT add `aria-modal`, `x-trap`, or a backdrop
    (D-01/D-09 — non-modal). Add `hx-on::after-swap="if (window.Alpine) Alpine.initTree(this)"` on the
    body slot host per record_host.html:71 so a swapped-in body re-inits its Alpine islands.

    Create the two RED-first survival test modules (httpx AsyncClient, smoke-app idiom from the analog):
    - `tests/analyze/routers/test_lane_drill_survival.py`: assert the rendered `_lane_card.html` trigger
      markup carries `role="button"`, `tabindex="0"`, `aria-label`, `id="lane-trigger-..."`,
      `hx-get="/pipeline/lanes/..."`, `hx-target="#detail-pane"`, `hx-push-url` with `?lane=`, and the
      `onkeydown` Space handler; assert `GET /pipeline/stats?lane={id}` re-emits `aria-current="true"`
      + the `ring-2 ring-blue-500` selected class on the matching card (D-02). These reference wave-1
      trigger + poll edits landing in Tasks 2/3 — RED until then.
    - `tests/agents/routers/test_agent_drill_survival.py`: the same assertions for the `agents_table.html`
      `<tr>` trigger (`id="agent-trigger-..."`, `hx-get="/admin/agents/.../_activity"`, `?agent=`) and
      `GET /admin/agents/_table?agent={id}` re-emitting the selected-row highlight.
    Mark the real-endpoint-dependent cases (drill-in actually opening a body) with `pytest.mark.xfail`
    or leave for wave 2 — Task 2/3 assertions here are markup + poll-highlight only, fully satisfiable
    within this plan.
  </action>
  <acceptance_criteria>
    - Source: `src/phaze/templates/pipeline/partials/_detail_pane.html` contains `role="region"` and does NOT contain `aria-modal` or `x-trap`.
    - Source: `_detail_pane.html` contains `getElementById(` and `history.replaceState` (stable-id focus-return + param clear) and `@keydown.escape.window`.
    - Source: both new test files exist and import the smoke-app/client fixture; `uv run pytest tests/analyze/routers/test_lane_drill_survival.py tests/agents/routers/test_agent_drill_survival.py --collect-only` exits 0 (collectable).
    - Behavior (after Tasks 2/3): `uv run pytest tests/analyze/routers/test_lane_drill_survival.py tests/agents/routers/test_agent_drill_survival.py -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/analyze/routers/test_lane_drill_survival.py tests/agents/routers/test_agent_drill_survival.py --collect-only -q</automated>
  </verify>
  <done>_detail_pane.html shell exists (role=region, non-modal, stable-id focus-return, Esc, own-tick + degrade captions); two survival test modules collect and encode the DRILL-03 markup + poll-highlight assertions (RED until Tasks 2/3).</done>
</task>

<task type="auto">
  <name>Task 2: Trigger a11y wiring on lane cards + agent rows; host #detail-pane on both pages</name>
  <files>src/phaze/templates/pipeline/partials/_lane_card.html, src/phaze/templates/admin/partials/agents_table.html, src/phaze/templates/pipeline/partials/analyze_workspace.html, src/phaze/templates/admin/agents.html</files>
  <read_first>
    - 88-PATTERNS.md §"Trigger edits — _lane_card.html + agents_table.html <tr>" — the exact role=button/tabindex/aria-label/hx-* markup + the REQUIRED onkeydown Space handler; the focus-ring token `focus-visible:ring-2 focus-visible:ring-blue-500`.
    - 88-PATTERNS.md §"Host-page edits — analyze_workspace.html + agents.html" and RESEARCH Pitfall 4 — lane pane = sibling of #analyze-lanes INSIDE analyze_workspace.html but OUTSIDE the polled grid; agent pane = OUTSIDE #agents-table-section in agents.html (standalone page).
    - src/phaze/templates/pipeline/partials/_lane_card.html — the frozen box model (rounded-xl border … p-4 / mt-3 / mt-2 / h-1.5) and kind glyph/color tokens; the trigger attrs attach to the card root (:54) WITHOUT changing the box model.
    - src/phaze/templates/pipeline/partials/_analyze_lanes.html:18-20 — the #analyze-lanes OOB grid that swaps as a unit (the pane must NOT live inside it).
    - src/phaze/templates/admin/partials/agents_table.html:20-27,55 — the #agents-table-section outerHTML poll root and the <tr> (:55) the trigger attaches to (keep hover:bg-gray-50 dark:hover:bg-phaze-panel).
    - src/phaze/templates/admin/agents.html:6-19 — the {% block content %} + include site where the pane sits beside the table.
    - 88-UI-SPEC.md §"Responsive Contract" — pane = right-side sibling column on lg+, stacked below on < lg.
  </read_first>
  <action>
    Edit `_lane_card.html`: add to the card ROOT element `id="lane-trigger-{{ lane.id }}"`,
    `role="button"`, `tabindex="0"`, `aria-label="Open lane {{ lane.kind }} {{ lane.id }} detail"`,
    `cursor-pointer`, the `focus:outline-none focus-visible:ring-2 focus-visible:ring-blue-500
    ring-offset-2 dark:ring-offset-phaze-bg` focus ring, the conditional selected ring
    `{% if selected_lane == lane.id %}ring-2 ring-blue-500{% endif %}` + `{% if selected_lane == lane.id %}aria-current="true"{% endif %}`,
    `hx-get="/pipeline/lanes/{{ lane.id }}"`, `hx-target="#detail-pane"`, `hx-swap="innerHTML"`,
    `hx-push-url="/s/analyze?lane={{ lane.id }}"`, `hx-trigger="click, keyup[key=='Enter']"`, and the
    REQUIRED inline `onkeydown` Space handler that `preventDefault()`s scroll and re-fires activation
    (88-PATTERNS.md excerpt). Do NOT touch the frozen box model or kind tokens. Keep `lane.id`/`lane.kind`
    Jinja-autoescaped (never `|safe`).

    Edit `agents_table.html`: add the same trigger set to the `<tr>` (:55) with
    `id="agent-trigger-{{ agent.id }}"`, `aria-label="Open agent {{ agent.name }} activity"`,
    `hx-get="/admin/agents/{{ agent.id }}/_activity"`, `hx-push-url="/admin/agents?agent={{ agent.id }}"`,
    the selected ring/`aria-current` gated on `{% if selected_agent == agent.id %}`, keeping the existing
    `hover:bg-gray-50 dark:hover:bg-phaze-panel`. (The #agents-table-section hx-vals wiring lands in Task 3.)

    Host the pane: in `analyze_workspace.html` add `{% include "pipeline/partials/_detail_pane.html" %}`
    as a sibling of `#analyze-lanes` INSIDE the workspace but OUTSIDE the polled grid — a right-side
    column on `lg+`, stacked below on `< lg` (Tailwind grid/flex; reuse existing layout utilities). In
    `agents.html` add the same include in `{% block content %}` OUTSIDE `#agents-table-section`. Same
    partial, different include site.
  </action>
  <acceptance_criteria>
    - Source: `_lane_card.html` contains `id="lane-trigger-` and `role="button"` and `hx-target="#detail-pane"` and `onkeydown` (Space handler); does NOT contain `|safe` on lane id/kind.
    - Source: `agents_table.html` contains `id="agent-trigger-` and `role="button"` and `hx-get="/admin/agents/{{ agent.id }}/_activity"`.
    - Source: `analyze_workspace.html` and `agents.html` each contain an include of `_detail_pane.html` located outside the `#analyze-lanes` / `#agents-table-section` element respectively (grep the include line is not nested within the polled block).
    - Behavior: `uv run pytest tests/analyze/routers/test_lane_drill_survival.py -k markup -x` and `tests/agents/routers/test_agent_drill_survival.py -k markup -x` pass (trigger markup assertions green).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/analyze/routers/test_lane_drill_survival.py tests/agents/routers/test_agent_drill_survival.py -k markup -x -q</automated>
  </verify>
  <done>Lane cards and agent rows are keyboard-accessible role=button triggers pointing at #detail-pane with hx-push-url selection; the shared pane is hosted OUTSIDE both polled regions on both host pages; frozen box models untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Poll-survival — carry ?lane=/?agent= onto both polls and re-emit the selected highlight</name>
  <files>src/phaze/templates/shell/shell.html, src/phaze/templates/admin/partials/agents_table.html, src/phaze/routers/pipeline.py, src/phaze/routers/admin_agents.py</files>
  <read_first>
    - 88-PATTERNS.md §"Shared Patterns → Poll-survival" and RESEARCH Open Question 2 (LOAD-BEARING) — htmx 2.0.10 does NOT auto-append the pushed ?lane=/?agent= to the poll hx-get; the poll element must carry it explicitly via hx-vals='js:{lane: new URLSearchParams(location.search).get("lane")}'.
    - src/phaze/templates/shell/shell.html:196-200 — the ACTUAL lane poll: a SINGLE persistent chrome-level element `#pipeline-stats` (hx-get="/pipeline/stats" hx-trigger="every 5s [document.visibilityState === 'visible']" hx-swap="innerHTML" class="hidden"). _analyze_lanes.html has NO hx-get of its own — it is a pure OOB-swap target driven by this one element. The ?lane= hx-vals MUST be added HERE (not to any stats-bar/analyze scaffold element).
    - src/phaze/templates/admin/partials/agents_table.html:20-27 — the #agents-table-section element IS the genuine self-poll (hx-get="/admin/agents/_table" hx-trigger="every 5s"). The ?agent= hx-vals goes on this element itself.
    - src/phaze/routers/pipeline.py:685-745 — pipeline_stats_partial: it re-includes _analyze_lanes.html with oob=True each 5s (:737-740, get_backend_lane_snapshot at :740). Add a `lane: str | None = Query(None)` param, look it up in the snapshot id set, pass `selected_lane` into the grid include context.
    - src/phaze/routers/admin_agents.py:88-149 — page (:91) and table_partial (:126); both call _load_agents. Add `agent: str | None = Query(None)`, validate against loaded agent ids, thread `selected_agent` into the agents_table.html context.
    - src/phaze/templates/pipeline/partials/_analyze_lanes.html and _lane_card.html — how selected_lane must be threaded through the include so each card sees it (use {% with selected_lane=selected_lane %} or shared context).
    - MEMORY: routers' get_session never commits — these are read-only, no commit; Query param is operator-declared free text, keep autoescaped, prefer lookup-in-known-set over trusting raw param (V4/V5 ASVS). shell.html #pipeline-stats hx-vals is safe globally — location.search is empty outside /s/analyze?lane=, so `lane` resolves to null and highlights nothing on other pages.
  </read_first>
  <action>
    Wire the lane poll to carry ?lane= (RESEARCH Open Question 2). The lane poll is NOT in the analyze
    scaffold — it is the single persistent chrome-level `#pipeline-stats` element in `shell.html:196-200`.
    Add `hx-vals='js:{lane: new URLSearchParams(location.search).get("lane")}'` to THAT `#pipeline-stats`
    element so its 5s `/pipeline/stats` auto-tick sends the pushed `?lane=`. This is globally safe: on any
    page other than `/s/analyze?lane=…`, `location.search` has no `lane`, so `hx-vals` sends `lane: null`
    and nothing is highlighted.

    In `pipeline_stats_partial` add `lane: Annotated[str | None, Query()] = None`. After
    `get_backend_lane_snapshot(session)`, resolve `selected_lane = lane if any(l["id"] == lane for l in lanes) else None`
    (lookup-in-known-set — unknown id highlights nothing, never errors). Pass `selected_lane` into the
    `_analyze_lanes.html` include context so `_lane_card.html` re-emits `aria-current`/ring for the match.
    Also add the same `?lane=` acceptance to the analyze workspace render path (the `/s/analyze` host)
    so a reload with `?lane=` seeds `selected_lane` for the initial full grid.

    Wire the agent poll: `#agents-table-section` (agents_table.html:20-27) IS its own self-poll, so add
    `hx-vals='js:{agent: new URLSearchParams(location.search).get("agent")}'` to that element directly.
    In `admin_agents.py` `page` and `table_partial`, add `agent: Annotated[str | None, Query()] = None`;
    resolve `selected_agent` against the loaded agent id set; thread it into `agents_table.html`.
    No commit (read-only). Keep all ids autoescaped.
  </action>
  <acceptance_criteria>
    - Source: `src/phaze/templates/shell/shell.html` `#pipeline-stats` element contains `hx-vals` with `"lane"` (reading `location.search`).
    - Source: `src/phaze/templates/admin/partials/agents_table.html` `#agents-table-section` element contains `hx-vals` with `"agent"`.
    - Source: `routers/pipeline.py` `pipeline_stats_partial` signature contains a `lane` Query param and the body computes `selected_lane` via a lookup-in-known-set over the snapshot (grep `selected_lane`).
    - Source: `routers/admin_agents.py` `page` and `table_partial` both accept an `agent` Query param and thread `selected_agent`.
    - Behavior: `GET /pipeline/stats?lane={known_id}` fragment contains `aria-current="true"` on the matching card; `GET /pipeline/stats?lane=__nonexistent__` returns 200 with NO aria-current (never 500). Same for `/admin/agents/_table?agent=`.
    - Test: `uv run pytest tests/analyze/routers/test_lane_drill_survival.py tests/agents/routers/test_agent_drill_survival.py -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/analyze/routers/test_lane_drill_survival.py tests/agents/routers/test_agent_drill_survival.py -x -q</automated>
  </verify>
  <done>The persistent shell.html #pipeline-stats poll and the #agents-table-section self-poll each carry the pushed ?param via hx-vals; the poll endpoints re-emit the selected-highlight (aria-current + ring) for the matching card/row every swap; unknown/absent param highlights nothing and never 500s; a reload with the param seeds the highlight server-side.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → FastAPI (private LAN, no auth — documented posture, admin_agents.py:16) | Untrusted query params (`?lane=`, `?agent=`) and operator-declared ids/kinds/names cross here into templates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-01 | Tampering/Info-disclosure | `?lane=`/`?agent=` reflected into card/row highlight + trigger `aria-label` | mitigate | Values stay Jinja-autoescaped (default on); never `\|safe`/`\|tojson`; `selected_lane`/`selected_agent` resolved by lookup-in-known-set so an unknown/hostile id highlights nothing (T-71-05 precedent). |
| T-88-02 | DoS | The 5s poll re-render carrying the param | accept (low) | Param handling is an O(1) membership check against the already-loaded snapshot/agent list — no extra query, no unbounded work; the existing poll bound is unchanged. |
| T-88-SC | Tampering | npm/pip/cargo installs | accept (none) | This plan installs ZERO packages (RESEARCH §Package Legitimacy Audit — no `uv add`, no CDN script); no supply-chain surface. |

Residual risk: LOW/none — read-only param wiring over an already-authenticated operator console.
</threat_model>

<verification>
- `uv run pytest tests/analyze/routers/test_lane_drill_survival.py tests/agents/routers/test_agent_drill_survival.py -x` green.
- `just test-bucket analyze` and `just test-bucket agents` pass in isolation (export BOTH `TEST_DATABASE_URL` and `MIGRATIONS_TEST_DATABASE_URL`, ports 5433).
- `uv run ruff check . && uv run ruff format --check . && uv run mypy .` clean.
- No raw `FileRecord.state` render introduced (guard `test_no_raw_state_render.py` / `just docs-drift` green).
</verification>

<success_criteria>
- Lane cards and agent rows are `role=button` tabindex=0 triggers with Enter+Space activation and a visible focus ring (DRILL-03).
- The shared `_detail_pane.html` is a NON-modal `role=region` swap target hosted OUTSIDE both polled regions; Esc + ✕ Close dismiss, clear the `?param`, and return focus by stable id.
- The persistent `shell.html` `#pipeline-stats` poll and the `#agents-table-section` self-poll each carry the pushed `?lane=`/`?agent=` via hx-vals and re-emit the selected highlight after every swap; a reload with the param seeds the highlight (D-02, both DRILL-03 escape hatches used together).
- Zero new packages/colors/fonts; frozen box models untouched.
</success_criteria>

<output>
Create `.planning/phases/88-lane-agent-drill-in/88-01-SUMMARY.md` when done.
</output>
