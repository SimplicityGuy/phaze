---
phase: 86-proposals-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/services/proposal.py
  - src/phaze/services/proposal_queries.py
  - tests/shared/core/test_proposals_upsert.py
  - tests/review/services/test_proposal_queries.py
autonomous: true
requirements: [SIDECAR-03]

must_haves:
  truths:
    - "No service-layer proposal writer sets FileRecord.state (proposal.py store_proposals, proposal_queries.py update_proposal_status + bulk_update_status) — D-01 sites 1,2,3"
    - "store_proposals on a stale batch over an already-executed file leaves the executed proposal row untouched and is_applied() stays True — D-03, the MOVED-regression dissolved"
    - "proposal_queries still writes proposals.status for APPROVED/REJECTED (single-authority preserved) — D-01"
    - "The D-03 regression test is mutation-verified (re-adding a file.state write / regressing _TERMINAL_FILE_STATES is caught) — D-03"
  artifacts:
    - path: "src/phaze/services/proposal.py"
      provides: "store_proposals upsert with the _TERMINAL_FILE_STATES frozenset + file-load-and-guard block removed; FileState import dropped"
      contains: "async def store_proposals"
    - path: "src/phaze/services/proposal_queries.py"
      provides: "update_proposal_status + bulk_update_status writing only proposals.status; FileState import dropped"
      contains: "async def bulk_update_status"
    - path: "tests/shared/core/test_proposals_upsert.py"
      provides: "D-03 stale-batch-over-executed-file regression test asserting via is_applied() from an independent read"
      contains: "is_applied"
  key_links:
    - from: "tests/shared/core/test_proposals_upsert.py"
      to: "src/phaze/services/stage_status.py"
      via: "is_applied(session, file_id) — reads proposals, never file.state"
      pattern: "is_applied\\(session"
    - from: "src/phaze/services/proposal.py"
      to: "proposals table"
      via: "pg_insert on_conflict_do_update (upsert stays; state cascade gone)"
      pattern: "on_conflict_do_update"
---

<objective>
Delete the service-layer half of the proposal → `FileRecord.state` cascade (D-01 sites 1, 2, 3) so
`proposals.status` is the sole authority for review decisions, and prove the `store_proposals`
MOVED-regression bug is dissolved with a mutation-verified integration test (D-03).

Purpose: closes SIDECAR-03 for the two service modules; the drift-prone `file.state` mirror whose
`_TERMINAL_FILE_STATES` frozenset omitted `MOVED`/`UNCHANGED` (the bug) is removed entirely, leaving
the already-correct `uq_proposals_file_id_pending` partial index as the sole protection.

Output: `proposal.py` and `proposal_queries.py` with every `file.state` write removed and `FileState`
imports trimmed; `test_proposals_upsert.py` carrying the D-03 regression test; `test_proposal_queries.py`
with its now-false `.file.state` assertions dropped. Shared and review buckets green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-proposals-cutover/86-CONTEXT.md
@.planning/phases/86-proposals-cutover/86-RESEARCH.md
@.planning/phases/86-proposals-cutover/86-PATTERNS.md

<interfaces>
<!-- Reuse — do NOT reimplement. Extracted from live source. -->
From src/phaze/services/stage_status.py:
  async def is_applied(session: AsyncSession, file_id: uuid.UUID) -> bool
    # single scalar EXISTS over proposals.status == 'executed'; NEVER reads file.state.
    # This is the Phase-85 predicate kept stable so Phase 86 needs no rework — use it in D-03.

From src/phaze/services/proposal.py (what STAYS after deletion):
  - `select` import (line 14) — used at :402/:407 by load_companion_contents, KEEP
  - `uuid` import — KEEP
  - `FileRecord` in `from phaze.models.file import FileRecord, FileState` — KEEP FileRecord, DROP FileState
  - the pg_insert upsert block (:348-364) with on_conflict_do_update(index_where=(status=='pending')) — KEEP

From src/phaze/services/proposal_queries.py (what STAYS):
  - `FileRecord` (used :117 subquery, :130 sort_col) — KEEP FileRecord, DROP FileState
  - `proposal.status = new_status.value` (:163) — KEEP
  - `update(RenameProposal)...values(status=...)` (:183) + `return int(cursor_result.rowcount)` — KEEP
  - approve_pending_above_confidence (:194) reuses bulk_update_status — no change needed
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete proposal.py state cascade + add the D-03 regression test</name>
  <read_first>
    - src/phaze/services/proposal.py (sites 1a/1b/1c: :18 import, :35-39 frozenset, :366-373 load-and-guard block)
    - .planning/phases/86-proposals-cutover/86-PATTERNS.md § "src/phaze/services/proposal.py" and § "tests/shared/core/test_proposals_upsert.py" (D-03 skeleton, the commit-then-reselect idiom)
    - tests/shared/core/test_proposals_upsert.py (helpers _seed_file/_batch/_count :39-75; analog test_rerun_never_touches_approved_row :104-139; test_fresh_insert_stamps_pk :~157-159; test_rerun_does_not_regress_terminal_file_state :185-203)
    - src/phaze/services/stage_status.py:148-166 (is_applied signature)
  </read_first>
  <behavior>
    - D-03 test `test_stale_batch_does_not_disturb_executed_file`: seed a file (default state ANALYZED) + an EXECUTED RenameProposal, run a stale store_proposals batch, then from a fresh select assert the executed proposal row status is still EXECUTED and `await is_applied(session, file_id) is True`. Asserted after `await session.commit()` (conftest override reads uncommitted rows — project_get_session_never_commits).
    - test_fresh_insert_stamps_pk: keeps PK/status/path assertions, drops the `state == FileState.PROPOSAL_GENERATED` assertion (no writer stamps it anymore).
    - Mutation check: temporarily re-add a `file_record.state = FileState.PROPOSAL_GENERATED` write (or regress `_TERMINAL_FILE_STATES`) → the source-scan guard (Plan 03) and/or coverage would catch it; the D-03 test itself proves is_applied is unaffected either way (documents authority moved to proposals).
  </behavior>
  <action>
    In src/phaze/services/proposal.py DELETE, per D-01 site 1 and D-04: (1a) the `_TERMINAL_FILE_STATES: frozenset[FileState] = frozenset({...})` definition at ~:35-39 and its 4-line explanatory comment; (1b) the entire file-load-and-guard block at ~:366-373 — the `# Update file state -- forward-only (WR-04)...` comment, the `result = await session.execute(select(FileRecord).where(FileRecord.id == uuid.UUID(fid)))` load, `file_record = result.scalar_one_or_none()`, and the `if file_record is not None and file_record.state not in _TERMINAL_FILE_STATES: file_record.state = FileState.PROPOSAL_GENERATED` write (the `select(FileRecord)` exists only to write state — Claude's-discretion resolved to remove the dead load); KEEP `count += 1` and `return count`; (1c) drop `FileState` from `from phaze.models.file import FileRecord, FileState` leaving `from phaze.models.file import FileRecord`. Do NOT touch the `select` import (still used at :402/:407) or `uuid`. In tests/shared/core/test_proposals_upsert.py: add `test_stale_batch_does_not_disturb_executed_file` per the PATTERNS D-03 skeleton (new import `from phaze.services.stage_status import is_applied`; reuse `_seed_file`/`_batch`; seed a RenameProposal with `status=ProposalStatus.EXECUTED`); replace `test_rerun_does_not_regress_terminal_file_state` (premise deleted) with it; in `test_fresh_insert_stamps_pk` delete only the two-line `file_record` re-select + `assert ... == FileState.PROPOSAL_GENERATED`. KEEP the `FileState` import in the test file (still used by `_seed_file` as `state=FileState.ANALYZED`) and KEEP `test_rerun_never_touches_approved_row` unchanged.
  </action>
  <verify>
    <automated>uv run ruff check src/phaze/services/proposal.py && uv run pytest tests/shared/core/test_proposals_upsert.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_TERMINAL_FILE_STATES" src/phaze/services/proposal.py` returns 0
    - `grep -c "FileState" src/phaze/services/proposal.py` returns 0 (import + all uses gone); `grep -c "FileRecord" src/phaze/services/proposal.py` is > 0 (still imported/used)
    - `store_proposals` no longer contains `select(FileRecord)` (the dead load removed); the pg_insert upsert block is intact
    - `uv run ruff check src/phaze/services/proposal.py` passes with no F401 on FileState/select
    - `uv run pytest tests/shared/core/test_proposals_upsert.py::test_stale_batch_does_not_disturb_executed_file -x` passes and calls `is_applied(session, file_id)`
    - `just test-bucket shared` green in isolation
    - MUTATION-VERIFY (record in SUMMARY): re-add `file_record.state = FileState.PROPOSAL_GENERATED` guarded load to store_proposals → `uv run ruff check` fails (unused re-added import) OR Plan-03 guard would go RED → restore → GREEN. Record the RED observation.
  </acceptance_criteria>
  <done>proposal.py has zero FileState references and no state cascade; test_proposals_upsert.py carries the mutation-verified D-03 test; shared bucket green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete proposal_queries.py APPROVED/REJECTED file.state limbs + adapt its tests</name>
  <read_first>
    - src/phaze/services/proposal_queries.py (site 2 update_proposal_status :163-174; site 3 bulk_update_status :182-192; :20 import; approve_pending_above_confidence :194+)
    - .planning/phases/86-proposals-cutover/86-PATTERNS.md § "src/phaze/services/proposal_queries.py" and § "tests/review/services/test_proposal_queries.py"
    - tests/review/services/test_proposal_queries.py (test_update_proposal_status_approve :250-255 STAYS; ...approve_sets_file_state :258-264; ...reject_sets_file_state :267-273; test_bulk_update_status :287-297 STAYS; test_bulk_update_status_sets_file_state :300-316)
  </read_first>
  <action>
    In src/phaze/services/proposal_queries.py per D-01 sites 2+3: in `update_proposal_status` DELETE the `# Transition FileRecord.state alongside proposal status (APR-02)` comment and the `if new_status == ProposalStatus.APPROVED: proposal.file.state = FileState.APPROVED.value / elif new_status == ProposalStatus.REJECTED: proposal.file.state = FileState.REJECTED.value` block (~:164-168); KEEP `proposal.status = new_status.value` (:163), `await session.commit()`, and the selectinload re-fetch tail. In `bulk_update_status` DELETE the `# Transition FileRecord.state for all affected files (APR-02)` comment, the `file_state = FileState.APPROVED.value if ... else FileState.REJECTED.value` derivation, the `file_ids_stmt = select(RenameProposal.file_id)...` subquery, and the `file_update = update(FileRecord)...values(state=file_state)` + `await session.execute(file_update)` (~:185-189); KEEP the `update(RenameProposal)...values(status=...)` (:183), `await session.commit()`, and `return int(cursor_result.rowcount)`. After both removals `FileState` is unused — drop it from `from phaze.models.file import FileRecord, FileState` leaving `FileRecord` (still used at :117/:130). Note the `selectinload(RenameProposal.file)` in update_proposal_status STAYS (the returned proposal still exposes `.file`, just not written). In tests/review/services/test_proposal_queries.py: delete `assert result.file.state == FileState.APPROVED` and the `...REJECTED` assertion in the two `*_sets_file_state` tests (narrow each to its proposals.status coverage or delete the test if the status is already covered by test_update_proposal_status_approve); delete the file-refresh + `for f in files: assert f.state == FileState.APPROVED` block in `test_bulk_update_status_sets_file_state`. Keep `test_update_proposal_status_approve` and `test_bulk_update_status` as the retained proposals.status coverage. Drop the "transitions FileRecord.state (APR-02)" framing from renamed/kept test docstrings. Check and trim the `FileState` import in the test file if it becomes unused after removals.
  </action>
  <verify>
    <automated>uv run ruff check src/phaze/services/proposal_queries.py && uv run pytest tests/review/services/test_proposal_queries.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FileState" src/phaze/services/proposal_queries.py` returns 0; `grep -c "FileRecord" src/phaze/services/proposal_queries.py` is > 0
    - `grep -c "\.file\.state" src/phaze/services/proposal_queries.py` returns 0 and no `update(FileRecord).*values(state=" remains
    - `bulk_update_status` still ends `return int(cursor_result.rowcount)`; `update_proposal_status` still sets `proposal.status = new_status.value`
    - `uv run ruff check src/phaze/services/proposal_queries.py` passes (no F401)
    - `uv run pytest tests/review/services/test_proposal_queries.py -x` passes; no assertion reads `.file.state`
    - `just test-bucket review` green in isolation
  </acceptance_criteria>
  <done>proposal_queries.py writes only proposals.status; its tests assert status only; review bucket green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker/service → DB | store_proposals / update_proposal_status / bulk_update_status write proposal rows (no external input crosses here in this plan; callers already validated upstream) |

## STRIDE Threat Register

Honest minimal surface: this plan DELETES redundant internal writes; it adds no endpoint, no input
path, no new dependency. The only real risk is a correctness regression, not an attacker.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01 | Tampering | store_proposals upsert (removing the state block could accidentally weaken the pg_insert conflict guard) | mitigate | Delete ONLY the file-load-and-guard block; KEEP the `on_conflict_do_update(index_where=status=='pending')` upsert intact — the partial index `uq_proposals_file_id_pending` is now the sole protection (acceptance grep confirms upsert present) |
| T-86-02 | Repudiation | proposals.status remains the sole review-decision authority | accept | update_proposal_status still writes `proposals.status`; execution_log unchanged; no audit-trail regression |
| T-86-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this phase (RESEARCH § Package Legitimacy Audit: N/A) — no supply-chain surface |
</threat_model>

<verification>
- `uv run ruff check .` and `uv run mypy .` clean (no F401 on trimmed FileState imports).
- `just test-bucket shared` and `just test-bucket review` green in isolation.
- D-03 test present and mutation-verified (RED observation recorded in SUMMARY).
</verification>

<success_criteria>
- proposal.py and proposal_queries.py contain zero `FileState` references and zero `FileRecord.state` writes (D-01 sites 1,2,3).
- A stale store_proposals batch over an executed file leaves the executed proposal row untouched and `is_applied()` True (D-03).
- proposals.status remains written for APPROVED/REJECTED (sole authority preserved).
- SIDECAR-03 service-layer half complete.
</success_criteria>

<output>
Create `.planning/phases/86-proposals-cutover/86-01-SUMMARY.md` when done. Record the D-03 mutation-verify RED→restore observation.
</output>
