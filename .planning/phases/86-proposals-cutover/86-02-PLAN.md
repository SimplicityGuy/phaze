---
phase: 86-proposals-cutover
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/routers/agent_proposals.py
  - tests/review/routers/test_agent_proposals.py
autonomous: true
requirements: [SIDECAR-03]

must_haves:
  truths:
    - "The apply-PATCH handler writes zero FileRecord.state (both the apply-outcome limb and the idempotent replay branch stop touching file.state) — D-01 site 4, D-02 site 5"
    - "The apply-PATCH still persists current_path on a moved outcome and echoes body.file_state; wire response stays byte-identical (proposal_state/file_state/current_path all present) — D-02"
    - "The same-state idempotent replay returns 200 without reading file.state, echoing None — D-02, Claude's discretion"
    - "The cross-tenant 403 guard and the schema request shape (file_state field + current_path-required validator) are unchanged — RESEARCH § Security Domain"
  artifacts:
    - path: "src/phaze/routers/agent_proposals.py"
      provides: "patch_proposal_state with _FILE_FOLLOW deleted, no file.state writes/reads, response_file_state sourced from body.file_state; FileState import dropped, FileRecord kept"
      contains: "async def patch_proposal_state"
    - path: "tests/review/routers/test_agent_proposals.py"
      provides: "router tests asserting current_path persists, response echoes body.file_state, file.state UNCHANGED from seed, replay echoes None"
      contains: "file_state"
  key_links:
    - from: "src/phaze/routers/agent_proposals.py"
      to: "ProposalStateResponse.file_state"
      via: "response_file_state = body.file_state (request echo, never file_record.state)"
      pattern: "body\\.file_state"
    - from: "tests/review/routers/test_agent_proposals.py"
      to: "files.state seeded value"
      via: "positive guard asserting f.state == seed (proves cascade write gone)"
      pattern: "f\\.state =="
---

<objective>
Rework the agent apply-PATCH (`PATCH /api/internal/agent/proposals/{id}/state`) so it stops mirroring
proposal outcomes into `FileRecord.state` (D-01 site 4 + D-02 site 5) while keeping the wire contract
byte-identical: `ProposalStateResponse.file_state` echoes the request's `body.file_state` and the
`current_path` write survives. Adapt the router tests to the new echo semantics with a positive
"state unchanged from seed" guard.

Purpose: closes the router half of SIDECAR-03 — the last proposal→`file.state` cascade writer — without
changing the request schema (load-bearing) or the response bytes. The only caller
(`tasks/execution.py:205,269`) discards the response, so this is zero-risk (D-02).

Output: `agent_proposals.py` with `_FILE_FOLLOW` deleted, no `file.state` reads/writes, and the request
echo; `test_agent_proposals.py` asserting current_path + request echo + seed-unchanged + replay-None.
Review bucket green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-proposals-cutover/86-CONTEXT.md
@.planning/phases/86-proposals-cutover/86-RESEARCH.md
@.planning/phases/86-proposals-cutover/86-PATTERNS.md

<interfaces>
<!-- Extracted from live source. What STAYS / what the wire looks like. -->
From src/phaze/schemas/agent_proposals.py (UNCHANGED — do NOT edit this file):
  class ProposalStatePatch: proposal_state, file_state ('moved'|'unchanged'|None), current_path,
    error_message; extra="forbid"; validator `_require_path_when_moved` couples file_state=='moved'→current_path.
  class ProposalStateResponse: proposal_id, proposal_state, file_state: str|None, current_path: str|None.

From src/phaze/routers/agent_proposals.py (what STAYS):
  - `FileRecord` in `from phaze.models.file import FileRecord, FileState` — KEEP FileRecord (used :62/:71), DROP FileState
  - cross-tenant 403 guard (:71-76, `file_record.agent_id != agent.id`) — DO NOT TOUCH (V4 Access Control)
  - the 404 / 409-illegal-transition / one-commit structure — KEEP
  - `_PROPOSAL_TRANSITIONS` map (:41-43) — KEEP
  - `_FILE_FOLLOW` map (:47-50) — DELETE (only other FileState consumer, dead after site 4)

From src/phaze/tasks/execution.py:205,269 — the ONLY caller; `await api.patch_proposal_state(...)` with
  no assignment (discards response). D-02 zero-risk confirmed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rework patch_proposal_state to echo the request and stop writing file.state</name>
  <read_first>
    - src/phaze/routers/agent_proposals.py (:31 import; :47-50 _FILE_FOLLOW; :84-95 idempotent replay branch reading file_record.state at :88; :110-131 apply-outcome limb + final response)
    - src/phaze/routers/agent_proposals.py:1-21 (the module docstring — the "Side effects on the FileRecord" block documents the deleted `FileRecord.state=MOVED`/`=UNCHANGED` cascade and is now factually wrong)
    - .planning/phases/86-proposals-cutover/86-PATTERNS.md § "src/phaze/routers/agent_proposals.py" (site 4 + site 5 exact edits, Pitfall 3 — keep current_path)
    - .planning/phases/86-proposals-cutover/86-RESEARCH.md § "Common Pitfalls" (Pitfall 3 drop-current_path, Pitfall 5 replay-branch second reader) + § "Security Domain"
    - src/phaze/schemas/agent_proposals.py (confirm request shape stays — read only, do not edit)
  </read_first>
  <behavior>
    - moved PATCH (proposal_state=executed, file_state='moved', current_path set): 200; response body has file_state=='moved' (echoed from request) and current_path==the sent path; file_record.current_path persisted; file_record.state NOT written.
    - unchanged PATCH (proposal_state=failed, file_state='unchanged'): 200; response file_state=='unchanged'; file.state NOT written.
    - same-state idempotent replay (cur==new): 200; response file_state is None; no file.state read.
    - cross-tenant PATCH: still 403 before state-machine logic.
  </behavior>
  <action>
    In src/phaze/routers/agent_proposals.py per D-01 site 4 + D-02 site 5: DROP `FileState` from `from phaze.models.file import FileRecord, FileState` (keep `FileRecord`); DELETE the `_FILE_FOLLOW: dict[ProposalStatus, FileState] = {...}` map and its comment (~:45-50) — it is dead after the cascade removal. In the same-state idempotent branch (`if cur == new:` ~:84-95): stop reading `file_record.state` — remove the `file_state_str = file_record.state` assignment so `file_state` in the returned ProposalStateResponse is `None` (Claude's discretion resolved to None per RESEARCH Open Question 2); the `current_path_str = file_record.current_path` echo MAY stay (real path, not cascade) — keep it. In the apply-outcome limb (`if body.file_state is not None and file_record is not None:` ~:110-121): DELETE `new_file_state = FileState(body.file_state)` and `file_record.state = new_file_state.value`; set `response_file_state = body.file_state` (the request echo); KEEP the whole `if body.current_path is not None: file_record.current_path = body.current_path; response_current_path = body.current_path else: response_current_path = file_record.current_path` block (Pitfall 3 — current_path is the real move destination). Leave the final `ProposalStateResponse(proposal_id=..., proposal_state=new.value, file_state=response_file_state, current_path=response_current_path)` shape byte-identical. UPDATE the module docstring (`:1-21`): the "Side effects on the FileRecord (when body.file_state is provided)" block now documents the deleted cascade — remove the two `-> FileRecord.state=MOVED, current_path=...` / `-> FileRecord.state=UNCHANGED, current_path preserved` lines (and reword the block header) so no `FileRecord.state=` claim survives; KEEP the `current_path` behavior description (that side effect still happens) and note the response now echoes the request `file_state`. Also drop the stale "joint Proposal+FileRecord state transition" framing in the docstring's first line (it is now a proposal-status transition that only updates `current_path` on the file). DO NOT touch the cross-tenant 403 guard, the 404/409 logic, `session.commit()`, or `schemas/agent_proposals.py`.
  </action>
  <verify>
    <automated>uv run ruff check src/phaze/routers/agent_proposals.py && uv run mypy src/phaze/routers/agent_proposals.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FileState" src/phaze/routers/agent_proposals.py` returns 0; `grep -c "FileRecord" src/phaze/routers/agent_proposals.py` is > 0
    - `grep -c "_FILE_FOLLOW" src/phaze/routers/agent_proposals.py` returns 0
    - `grep -c "file_record.state" src/phaze/routers/agent_proposals.py` returns 0 (no write AND no read — both sites 4 and 5)
    - `grep -c "response_file_state = body.file_state" src/phaze/routers/agent_proposals.py` is >= 1 (request echo)
    - the final `ProposalStateResponse(...)` still lists `file_state=` and `current_path=` (wire byte-identical); `if body.current_path is not None: file_record.current_path` block still present
    - the cross-tenant `403` guard block (`file_record.agent_id != agent.id`) is unchanged
    - `grep -c "FileRecord.state=" src/phaze/routers/agent_proposals.py` returns 0 (the module docstring no longer documents the deleted cascade); the docstring still mentions `current_path`
    - `uv run ruff check` + `uv run mypy` pass with no F401 on FileState
  </acceptance_criteria>
  <done>patch_proposal_state writes/reads zero file.state, echoes body.file_state, keeps current_path + wire shape; cross-tenant guard + schema untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Adapt router tests — current_path, request echo, seed-unchanged, replay-None</name>
  <read_first>
    - tests/review/routers/test_agent_proposals.py (test_executed_joint_update :72-93; test_failed_joint_update :96-114; test_same_state_idempotent_no_op :117-136; the helper's seed default file_state at :42; the commit+expire_all+select idiom :87-90)
    - .planning/phases/86-proposals-cutover/86-PATTERNS.md § "tests/review/routers/test_agent_proposals.py" (keep :84/:108 echo, delete :92/:114 f.state, add positive seed-unchanged guard, replay None at :126-129)
    - .planning/phases/86-proposals-cutover/86-RESEARCH.md § "Test Impact Map" (rows for :84/:92/:108/:114/:118-135)
  </read_first>
  <action>
    In tests/review/routers/test_agent_proposals.py: in `test_executed_joint_update` KEEP `assert body["file_state"] == "moved"` and `assert body["current_path"] == "/new/proposed.mp3"` (response echo, D-02) and KEEP `assert f.current_path == "/new/proposed.mp3"` (real path survives, Pitfall 3); DELETE `assert f.state == FileState.MOVED.value`; ADD a positive guard asserting `f.state == FileState.APPROVED.value` (the helper's seeded default, :42) — i.e. state UNCHANGED from seed, proving the cascade write is gone (not merely absent). In `test_failed_joint_update` KEEP `assert body["file_state"] == "unchanged"`; DELETE `assert f.state == FileState.UNCHANGED.value`; ADD the same seed-unchanged positive guard. In `test_same_state_idempotent_no_op`: status-code asserts and the `p.status == EXECUTED` check are unchanged; the replay call sends no `file_state`, so ADD/ADJUST `assert body["file_state"] is None` on the replay leg (new None echo). Re-verify from an INDEPENDENT read using the existing `await session.commit(); session.expire_all()` idiom before the `f.state` selects (project_get_session_never_commits). Leave all 409/404/422/403/401 guard tests untouched. Keep the `FileState` import (still used by the seed-unchanged assertions).
  </action>
  <verify>
    <automated>uv run pytest tests/review/routers/test_agent_proposals.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "f.state == FileState.MOVED" tests/review/routers/test_agent_proposals.py` and `...UNCHANGED` both return 0
    - each of test_executed_joint_update / test_failed_joint_update contains a positive `assert f.state == FileState.APPROVED.value` (state unchanged from seed)
    - `body["file_state"] == "moved"` / `"unchanged"` echo assertions and `f.current_path` assertion still present
    - the replay test asserts `body["file_state"] is None`
    - `uv run pytest tests/review/routers/test_agent_proposals.py -x` passes
    - `just test-bucket review` green in isolation (together with Plan 01's proposal_queries changes after Wave 1 merge)
  </acceptance_criteria>
  <done>Router tests assert current_path persistence + request echo + seed-unchanged positive guard + replay None; review bucket green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → API (PATCH /state) | The one contract-adjacent surface: an authenticated file-server agent PATCHes a proposal outcome. Untrusted body crosses here; validated by the (unchanged) ProposalStatePatch schema. |

## STRIDE Threat Register

Honest surface: the wire contract stays byte-identical and the only caller discards the response. The
real risks are regressions in existing controls, not new attack surface.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-03 | Elevation of Privilege | cross-tenant guard (agent_proposals.py:71-76) | mitigate | DO NOT TOUCH the `file_record.agent_id != agent.id` → 403 block; acceptance grep confirms it is unchanged; the 403/401 guard tests stay green |
| T-86-04 | Tampering | ProposalStatePatch request shape (file_state field + _require_path_when_moved validator) | mitigate | schemas/agent_proposals.py is NOT edited; only the handler's value-source for file_state changes; `extra="forbid"` + validator preserved (V5 Input Validation) |
| T-86-05 | Tampering | dropping current_path at site 4 (Pitfall 3) would silently lose the moved-file path | mitigate | Delete ONLY the `file_record.state =` limb; keep the current_path write; a positive test asserts current_path persists on a moved PATCH |
| T-86-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase (RESEARCH § Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `uv run ruff check .` and `uv run mypy .` clean (no F401 on trimmed FileState import).
- `just test-bucket review` green in isolation.
- Response bytes unchanged: `ProposalStateResponse(proposal_state=, file_state=, current_path=)` shape preserved.
</verification>

<success_criteria>
- agent_proposals.py contains zero `FileState` references and zero `file_record.state` reads/writes (D-01 site 4 + D-02 site 5).
- Apply-PATCH echoes `body.file_state`, persists `current_path`, and keeps the wire response byte-identical (D-02).
- Same-state replay returns 200 echoing None without reading file.state.
- Cross-tenant 403 guard and request schema unchanged.
- SIDECAR-03 router half complete.
</success_criteria>

<output>
Create `.planning/phases/86-proposals-cutover/86-02-SUMMARY.md` when done.
</output>
