---
phase: 86-proposals-cutover
plan: 03
type: execute
wave: 2
depends_on: ["86-01", "86-02"]
files_modified:
  - tests/shared/test_proposals_cutover_source_scan.py
autonomous: true
requirements: [SIDECAR-03]

must_haves:
  truths:
    - "An AST source-scan guard proves zero FileRecord.state Store/Load and zero FileState write-target across proposal.py, proposal_queries.py, and agent_proposals.py — D-01, anti-drift insurance"
    - "The guard walks BOTH Call.args and Call.keywords (the Phase-83 blind spot) and keys on the exact attribute name 'state' (never a line-grep) — Pitfall 1"
    - "The guard is mutation-verified: permanently-encoded test_guard_flags_* RED cases for every syntactic form + a one-time real-source RED→restore observation — D-03, feedback_mutation_test_guard_tests"
    - "The guard resolves the three target source files from a repo root verified by an existence assert (not a blindly-trusted parents[] depth)"
  artifacts:
    - path: "tests/shared/test_proposals_cutover_source_scan.py"
      provides: "AST anti-drift guard over the three cutover source files with mutation + false-positive suites"
      contains: "def test_proposal_py_has_zero_state_writes"
  key_links:
    - from: "tests/shared/test_proposals_cutover_source_scan.py"
      to: "src/phaze/services/proposal.py + proposal_queries.py + routers/agent_proposals.py"
      via: "_violations(<file>.read_text()) == [] for each target"
      pattern: "_violations\\("
    - from: "tests/shared/test_proposals_cutover_source_scan.py"
      to: "ast.walk over Call.args AND Call.keywords"
      via: "_where_family_arg_violations walking both positional and keyword args"
      pattern: "\\.keywords"
---

<objective>
Add a mutation-verified AST source-scan guard proving the proposal→`FileRecord.state` cascade cannot
silently regress: zero `FileRecord.state` Store/Load and zero `FileState.<member>` write-target
occurrences across `services/proposal.py`, `services/proposal_queries.py`, and
`routers/agent_proposals.py`.

Purpose: standing anti-drift insurance behind SIDECAR-03. The behavioral tests (Plans 01/02) catch a
wrong value at the sites they exercise; this guard catches a `file.state` write/read reintroduced at a
NEW site those tests never touch — and, unlike a line-grep, cannot be fooled by multi-line SQLAlchemy or
a `.values(state=...)` splat (Pitfall 1; Phase 83 shipped two toothless guards this way).

Output: `tests/shared/test_proposals_cutover_source_scan.py`, modeled verbatim on the in-tree template,
with the mutation (`test_guard_flags_*`) and false-positive (`test_guard_ignores_*`) suites intact.

This plan runs in Wave 2 because it asserts ABSENCE across all three source files — the deletions in
Plans 01 and 02 must have landed first.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-proposals-cutover/86-CONTEXT.md
@.planning/phases/86-proposals-cutover/86-RESEARCH.md
@.planning/phases/86-proposals-cutover/86-PATTERNS.md
@.planning/phases/86-proposals-cutover/86-01-SUMMARY.md
@.planning/phases/86-proposals-cutover/86-02-SUMMARY.md

<interfaces>
<!-- The template to copy VERBATIM (helpers + mutation/false-positive suites), then retarget. -->
From tests/shared/test_reenqueue_reconcile_source_scan.py:
  _SRC_ROOT = Path(__file__).resolve().parents[2] / "src" / "phaze"   # this file is ALSO at tests/shared/
  _WHERE_FUNCS = frozenset({"where","filter","filter_by","having"})
  def _filerecord_bound_names(tree) -> set[str]
  def _state_reads(tree) -> list[ast.Attribute]            # .attr == "state" off a FileRecord-bound name
  def _filestate_occurrences(tree) -> list[ast.Attribute]  # FileState.<member> (catches removed writes)
  def _getattr_state_calls(tree) -> list[ast.Call]         # getattr(f, "state")
  def _where_family_arg_violations(tree) -> list[...]       # walks BOTH Call.args and Call.keywords
  def _violations(source: str) -> list[ast.AST]            # union of the above
  def _lines(nodes) -> list[object]
  # negative suites: test_guard_flags_* (RED on crafted strings) / test_guard_ignores_* (GREEN)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the AST source-scan guard over the three cutover files</name>
  <read_first>
    - tests/shared/test_reenqueue_reconcile_source_scan.py (the FULL template: docstring, helpers :59-187, real-source guards :187-223, mutation suite :225-323, false-positive suite :325-357)
    - .planning/phases/86-proposals-cutover/86-PATTERNS.md § "tests/shared/test_proposals_cutover_source_scan.py" (path scaffold + walker reuse + caveat)
    - .planning/phases/86-proposals-cutover/86-RESEARCH.md § "Reusable Assets" (template pointer) + § "Common Pitfalls" (Pitfall 1, Pitfall 5)
    - src/phaze/services/proposal.py, src/phaze/services/proposal_queries.py, src/phaze/routers/agent_proposals.py (confirm CLEAN ABSENCE post-deletion — zero .state / FileState occurrences before asserting == [])
  </read_first>
  <action>
    Create tests/shared/test_proposals_cutover_source_scan.py by copying the template
    tests/shared/test_reenqueue_reconcile_source_scan.py VERBATIM for its helpers (`_filerecord_bound_names`,
    `_state_reads`, `_filestate_occurrences`, `_getattr_state_calls`, `_where_family_arg_violations`,
    `_violations`, `_lines`) and its `_WHERE_FUNCS` frozenset and both negative suites
    (`test_guard_flags_*` RED cases for `f.state = X`, `.values(state=X)`, `getattr(f,"state")`,
    positional `.where(...)`, chained `.where(<Compare>)`; `test_guard_ignores_*` GREEN cases for a
    `.status` read, a `FileRecord.id` read, and a docstring `state` mention). Retarget the three module
    constants to `_PROPOSAL = _SRC_ROOT / "services" / "proposal.py"`,
    `_PROPOSAL_QUERIES = _SRC_ROOT / "services" / "proposal_queries.py"`,
    `_AGENT_PROPOSALS = _SRC_ROOT / "routers" / "agent_proposals.py"`. RESOLVE `_SRC_ROOT` EMPIRICALLY, not
    by copying a hard-coded `parents[N]`: this new file sits at `tests/shared/` — the SAME depth as the
    template — so the repo root is `parents[2]` and `_SRC_ROOT = Path(__file__).resolve().parents[2] / "src" / "phaze"`.
    NOTE: PATTERNS.md and the phase guidance say `parents[1]`; that is an ERROR (it assumed the template
    was one dir deeper, but the template is at the identical `tests/shared/` depth and uses `parents[2]`).
    Do NOT trust either number blindly — add a module-level `assert _PROPOSAL.exists(), _PROPOSAL` (and for
    the other two) so a wrong root fails loudly with the resolved path instead of silently scanning nothing.
    Write three real-source guards — `test_proposal_py_has_zero_state_writes`,
    `test_proposal_queries_has_zero_state_writes`, `test_agent_proposals_has_zero_state_writes` — each
    asserting `_violations(<file>.read_text(encoding="utf-8")) == []` with a message naming the offending
    lines via `_lines(...)`. Clean absence holds for all three (RESEARCH confirmed) so no allow-list is
    needed; do NOT add the dedup-scanner allow-list variant.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/test_proposals_cutover_source_scan.py -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/shared/test_proposals_cutover_source_scan.py exists with a module-level `assert _PROPOSAL.exists()` (and the other two targets) that fails loudly on a wrong repo root
    - `_SRC_ROOT` resolves so that `(_SRC_ROOT / "services" / "proposal.py").exists()` is True (i.e. `parents[2]` for a tests/shared/ file — NOT parents[1])
    - the guard walks both positional and keyword args: `grep -c "\.keywords" tests/shared/test_proposals_cutover_source_scan.py` is >= 1 and it is NOT a line/regex grep of the source (uses `ast.walk`)
    - three real-source guards each assert `_violations(...) == []` and all pass against the post-Plan-01/02 files
    - the mutation suite is present: `grep -c "def test_guard_flags_" tests/shared/test_proposals_cutover_source_scan.py` is >= 4 and the false-positive suite `def test_guard_ignores_` is >= 2 (these permanently encode the RED directions)
    - `uv run pytest tests/shared/test_proposals_cutover_source_scan.py -x` passes; `just test-bucket shared` green in isolation
    - MUTATION-VERIFY (record RED→restore in SUMMARY): (a) inject `file_record.state = FileState.MOVED` into agent_proposals.py → run the guard → `test_agent_proposals_has_zero_state_writes` goes RED → restore → GREEN; (b) inject `update(FileRecord).where(...).values(state=...)` into proposal_queries.py → guard RED → restore → GREEN. Confirm a legitimate `.status`/`.id` read is NOT flagged (the test_guard_ignores_* GREEN cases).
  </acceptance_criteria>
  <done>The AST guard over all three cutover files passes at clean absence, walks args+keywords, resolves its root via an existence-asserted path, and is mutation-verified (RED→restore recorded).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — test-only) | DB-free AST scan over local source files; adds no runtime surface, no input path, no dependency. |

## STRIDE Threat Register

This plan adds a test file only. There is no attacker surface. The one honest risk is a TOOTHLESS guard
(a guard that is GREEN but never actually catches a regression) — the anti-goal called out in project
memory `feedback_mutation_test_guard_tests`.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-06 | Tampering (self) | a toothless anti-drift guard (wrong repo root → scans nothing → always GREEN; or a line-grep blind to multi-line SQLAlchemy) | mitigate | Existence-assert the resolved paths (fail loud on wrong root); use `ast.walk` over Call.args AND Call.keywords (never a grep); keep the permanently-encoded `test_guard_flags_*` RED suite + a one-time real-source RED→restore observation recorded in SUMMARY |
| T-86-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this phase (RESEARCH § Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `uv run pytest tests/shared/test_proposals_cutover_source_scan.py -x` passes.
- `just test-bucket shared` green in isolation.
- Full suite green + 90% coverage floor before `/gsd:verify-work`.
- Mutation RED→restore for the AST guard recorded in the SUMMARY.
</verification>

<success_criteria>
- A mutation-verified AST guard proves zero `FileRecord.state` Store/Load and zero `FileState` write-target across all three cutover files (D-01 anti-drift).
- The guard resolves its target files via an existence-asserted repo root and walks both positional and keyword call args (Pitfall 1 closed).
- SIDECAR-03 anti-drift insurance complete; the cascade cannot silently regress.
</success_criteria>

<output>
Create `.planning/phases/86-proposals-cutover/86-03-SUMMARY.md` when done. Record the AST-guard mutation-verify RED→restore observation and note the corrected `parents[2]` root (PATTERNS.md parents[1] was an error).
</output>
