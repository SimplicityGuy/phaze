---
phase: 65-calver-adoption
plan: 02
type: execute
wave: 2
depends_on: ["65-01"]
files_modified:
  - .github/workflows/ci.yml
  - pyproject.toml
  - uv.lock
  - .planning/MILESTONES.md
  - .planning/ROADMAP.md
  - docs/deployment.md
  - docs/configuration.md
  - docs/arm64-agent-image.md
  - docs/cloud-burst.md
  - docker-compose.agent.yml
  - docker-compose.cloud-agent.yml
autonomous: true
requirements: [VER-01, VER-02, VER-03, VER-04]

must_haves:
  truths:
    - "ci.yml `on: push: tags` is `[0-9]+.[0-9]+.[0-9]+` (quoted, CalVer-only) and no longer contains `v*.*.*` (D-02); branch CI preserved (D-03)."
    - "pyproject.toml `version = \"2026.7.0\"` (bare CalVer, D-01) and uv.lock's phaze entry is synced via `uv lock` (never hand-edited)."
    - "MILESTONES.md carries a Milestone|Version|Date mapping table with v1.0..v7.0 rows verbatim (D-10) and an `Engineering Improvements | 2026.7.0` row (D-11); all existing `## vN.M …` detail sections left verbatim (D-13). The table + convention prose make explicit that milestone NAMES are decoupled from version NUMBERS — multiple milestones or same-month patch releases simply increment REVISION within the `YYYY.MM` (D-08)."
    - "docs/deployment.md forward-looking pin/rollback/tag-strategy examples read bare CalVer `2026.7.0` / `:<version>` and document `YYYY.MM.REVISION` + no-leading-zero month + per-month zero-based REVISION (D-07/D-12), keeping the 3-part-tag rationale; historical `v4.0 …` labels left verbatim (D-13)."
    - "The annotated-tag-PUSH-triggers-GHCR-publish invariant + delete-recreate recipe are stated in docs prose (D-04); docker-publish.yml metadata-action is UNCHANGED (D-05/D-06); a memory-update note for project_release_procedure.md is surfaced in the SUMMARY."
    - "README VER-02 decision documented: no version badge exists, so the clause is satisfied by deployment.md CalVer prose — no badge added or re-added (one-line badge rule)."
    - "All four guard tests from Plan 01 are GREEN and the full suite passes."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CalVer-only tag trigger"
      contains: "[0-9]+.[0-9]+.[0-9]+"
    - path: "pyproject.toml"
      provides: "CalVer version source of truth"
      contains: "2026.7.0"
    - path: ".planning/MILESTONES.md"
      provides: "Milestone↔Version mapping table"
      contains: "Milestone"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "tests/agents/deployment/test_agent_compose.py"
      via: "identical CalVer glob literal in the yaml value and the test assertion"
      pattern: "\\[0-9\\]\\+\\.\\[0-9\\]\\+\\.\\[0-9\\]\\+"
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv lock sync of the phaze entry"
      pattern: "2026\\.7\\.0"
---

<objective>
Turn the Plan 01 RED gates GREEN: swap the ci.yml tag glob to the CalVer-only pattern, bump the version source of truth, add the MILESTONES mapping table, and rewrite only the forward-looking CalVer examples in docs — while leaving all historical record and the docker-publish machinery untouched.

Purpose: This adopts CalVer across every forward-looking surface. The ci.yml glob is the single highest-consequence edit (a wrong glob = silent no-publish, the v4.0.2/v7.0 GOTCHA class); the Plan 01 guard now catches that in CI.
Output: CalVer live in ci.yml + pyproject/uv.lock + MILESTONES table + docs; publish invariant preserved; memory-update note surfaced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/65-calver-adoption/65-CONTEXT.md
@.planning/phases/65-calver-adoption/65-RESEARCH.md
@.planning/phases/65-calver-adoption/65-PATTERNS.md
@.planning/phases/65-calver-adoption/65-01-SUMMARY.md

<interfaces>
<!-- The glob literal MUST be byte-identical between ci.yml and the Plan-01 test. -->
CalVer glob (docs-cited, GHA filter syntax): [0-9]+.[0-9]+.[0-9]+   # `.` is a LITERAL dot in GHA globs, not regex; `+`=one-or-more, `[]`=range. Quote in YAML (value starts with `[`).
First CalVer tag: 2026.7.0   # bare, no `v` prefix (D-01); no leading-zero month (D-07)

docker-publish.yml metadata-action — DO NOT EDIT (RESEARCH: type=semver already parses 2026.7.0 → {{version}}=2026.7.0, {{major}}.{{minor}}=2026.7; type=ref,event=tag → 2026.7.0).

MILESTONES seed rows (source-of-truth from the `## vN.M …` headers):
  v1.0 MVP 2026-03-30 · v2.0 2026-04-02 · v3.0 2026-04-04 · v4.0 2026-05-17 · v5.0 2026-06-26 · v6.0 2026-06-29 · v7.0 2026-07-02 · Engineering Improvements 2026.7.0 (release date TBD)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap the ci.yml CalVer glob + bump pyproject version + uv lock</name>
  <files>.github/workflows/ci.yml, pyproject.toml, uv.lock</files>
  <read_first>
    - .github/workflows/ci.yml lines 4-15 (the `on:` block; line 13 `tags: ["v*.*.*"]`; lines 6-11 comment references "v*.*.* tags"). LEAVE the `detect-changes` `REF_TYPE == "tag"` force branch (lines ~50-59) — verified glob-agnostic (D-03).
    - pyproject.toml line 7 (`version = "7.0.0"`).
    - 65-RESEARCH.md §Pattern 1 (the pinned glob + YAML-quoting rule) + Pitfall 1 (wrong/unquoted glob) + Pitfall 4 (pyproject/uv.lock drift).
  </read_first>
  <action>
    In ci.yml line 13, replace `tags: ["v*.*.*"]` with `tags: ["[0-9]+.[0-9]+.[0-9]+"]` — quoted (a YAML value starting with `[` parses as a flow sequence if unquoted), CalVer-only, legacy `v*.*.*` dropped (D-02). Update the lines 6-11 comment that says "v*.*.* tags" to name the CalVer glob instead. Do NOT touch `detect-changes` (D-03) or docker-publish.yml (D-05/D-06).
    In pyproject.toml line 7, change `version = "7.0.0"` to `version = "2026.7.0"` (bare CalVer, D-01; bump-in-phase per RESEARCH Open-Q2 so guard tests exercise the final state — the actual annotated tag remains a milestone-completion step). Then run `uv lock` to sync the `phaze` entry in uv.lock — NEVER hand-edit uv.lock (Pitfall 4).
  </action>
  <acceptance_criteria>
    - `grep -c 'v\*\.\*\.\*' .github/workflows/ci.yml` returns 0 (glob and comment both de-`v`-ed).
    - ci.yml contains the exact literal: `grep -F '"[0-9]+.[0-9]+.[0-9]+"' .github/workflows/ci.yml` returns a match.
    - `grep -n '^version = "2026.7.0"' pyproject.toml` returns a match.
    - `grep -c '2026.7.0' uv.lock` is ≥1 (uv lock synced the phaze entry); `git diff --name-only` includes uv.lock.
    - `uv run pytest tests/agents/deployment/test_agent_compose.py::test_ci_workflow_triggers_on_version_tags tests/agents/deployment/test_agent_compose.py::test_ci_detect_changes_forces_code_changed_on_tags -x` PASSES (glob GREEN, D-03 gate still green).
    - `pre-commit run actionlint --files .github/workflows/ci.yml` passes (ci.yml still valid).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/agents/deployment/test_agent_compose.py::test_ci_workflow_triggers_on_version_tags -x</automated>
  </verify>
  <done>ci.yml triggers on the CalVer-only glob (no `v*.*.*`), branch CI + detect-changes intact, pyproject at 2026.7.0 with uv.lock synced via `uv lock`; the retargeted glob test is GREEN and actionlint passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add the MILESTONES mapping table + confirm ROADMAP conformance</name>
  <files>.planning/MILESTONES.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/MILESTONES.md (top + the `## vN.M Name (Shipped: date)` detail headers — the row source-of-truth; the file currently has ZERO markdown tables).
    - .planning/RETROSPECTIVE.md lines ~231-236 (the closest `| Milestone | … |` table idiom to mirror: header row + `|---|` separator + `vN.M`-keyed rows).
    - 65-RESEARCH.md §Mapping Table (the seed table) + §ROADMAP.md ("already largely conformant").
    - .planning/ROADMAP.md milestone header (line ~18) + phase-status table rows 63-66 + the "Adopt CalVer" backlog note.
  </read_first>
  <action>
    In MILESTONES.md, add a `## Milestone ↔ Version Mapping` section with a `| Milestone | Version | Date |` table placed NEAR THE TOP, BEFORE the first `## vN.M …` detail section. Seed rows verbatim from the detail headers: `MVP | v1.0 | 2026-03-30`, `Metadata Enrichment & Tracklist Integration | v2.0 | 2026-04-02`, `Cross-Service Intelligence & File Enrichment | v3.0 | 2026-04-04`, `Distributed Agents | v4.0 | 2026-05-17`, `Cloud Burst Analysis | v5.0 | 2026-06-26`, `Kubernetes Burst Analysis | v6.0 | 2026-06-29`, `UI Redesign — DAG-Centric Hybrid Console | v7.0 | 2026-07-02`, and the new `Engineering Improvements | 2026.7.0 | _(release date TBD)_` row (D-09/D-10/D-11: historical `vN.M` verbatim, new row bare CalVer, current milestone referenced by name). Add one prose line above/below the table documenting the convention: milestones are NAMED, releases are DATED CalVer `YYYY.MM.REVISION` with no leading-zero month; REVISION is a per-month zero-based counter (Nth release within `YYYY.MM`, resets each calendar month) that supports same-month patch releases (D-07/D-08). LEAVE every existing `## vN.M …` detail section verbatim (D-13).
    In ROADMAP.md: verify-only. Confirm the milestone header + phase-status rows 63-66 already read `2026.7.0` and that the "Adopt CalVer" backlog note reads as scheduled (it does: `_[SCHEDULED as … Phase 65 …]_`). Edit ONLY if any forward-looking text implies a NEW `vN.M`; otherwise leave ROADMAP unchanged (the concrete VER-04 artifact is the MILESTONES table). Do NOT rename the compound "2026.7.0 Engineering Improvements" milestone name (D-11: no rename churn).
  </action>
  <acceptance_criteria>
    - `grep -c '^|' .planning/MILESTONES.md` is now ≥9 (header + separator + 8 rows; previously 0).
    - Header row present: `grep -n 'Milestone' .planning/MILESTONES.md | grep -F 'Version' | grep -F 'Date'` returns a match.
    - All historical rows verbatim: `for v in v1.0 v2.0 v3.0 v4.0 v5.0 v6.0 v7.0; do grep -q "$v" .planning/MILESTONES.md || echo "MISSING $v"; done` prints nothing.
    - New row present: `grep -F '2026.7.0' .planning/MILESTONES.md` returns a match; REVISION-convention prose present: `grep -F 'YYYY.MM.REVISION' .planning/MILESTONES.md` returns a match.
    - `uv run pytest tests/agents/deployment/test_agent_compose.py::test_milestones_mapping_table_intact -x` PASSES.
    - `git diff .planning/ROADMAP.md` is empty OR contains only a scheduled/done wording fix (no new `vN.M` introduced).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/agents/deployment/test_agent_compose.py::test_milestones_mapping_table_intact -x</automated>
  </verify>
  <done>MILESTONES.md has the Milestone|Version|Date table (historical `vN.M` verbatim + 2026.7.0 row) and REVISION-convention prose; detail sections untouched; ROADMAP confirmed conformant; the mapping-table guard is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite forward-looking CalVer doc examples + README/compose decisions</name>
  <files>docs/deployment.md, docs/configuration.md, docs/arm64-agent-image.md, docs/cloud-burst.md, docker-compose.agent.yml, docker-compose.cloud-agent.yml</files>
  <read_first>
    - docs/deployment.md lines 262, 348-351, 370, 398-415, 475-482, 505 (grep-anchor on the STRINGS `PHAZE_IMAGE_TAG=v4.0.0`, `:v<version>`, `3-part`, `vX.Y.Z`, `v*.*.*` — line numbers approximate per RESEARCH A1). Lines 2/4 "Phaze v4.0 …" header = feature-era label, LEAVE (D-13).
    - docs/configuration.md (~291, 350 `PHAZE_IMAGE_TAG` examples), docs/arm64-agent-image.md (~128-129 build example), docs/cloud-burst.md (~215, 250 pin/build examples).
    - docker-compose.agent.yml comments ~lines 27-28, docker-compose.cloud-agent.yml comments ~lines 35-37 (`image:` lines use `${PHAZE_IMAGE_TAG:-latest}` indirection — LEAVE those). The comment blocks ALSO carry the docker-publish output phrasing `` `:latest` and `:v<version>` `` (agent ~line 28) and `` `:v<version>-arm64` `` (cloud-agent ~line 37) — these become factually wrong under D-06 (bare tag emits `:2026.7.0`/`:2026.7.0-arm64`) and MUST be de-`v`-ed.
    - 65-RESEARCH.md §Full Edit Surface (the file→line→change/leave table) + §Release Procedure (D-04 invariant + memory note) + §Open Questions (Q1 README, Q3 cloud-burst scope). 65-PATTERNS.md §No Analog Found + §Memory-update note.
  </read_first>
  <action>
    Apply the D-12 (forward-looking → rewrite) / D-13 (historical → leave) rule string-by-string. In docs/deployment.md: rewrite `PHAZE_IMAGE_TAG=v4.0.0` pin/rollback examples (~262, 370, 403, 479, 505) to `2026.7.0`; change "both `:latest` and `:v<version>`" phrasings (~348, 409, 482) to `:<version>` (bare CalVer); rewrite the 3-part-tag rationale (~349-351, 482) to bare 3-part CalVer `YYYY.MM.REVISION` (e.g. `2026.7.0`) matched by the CalVer glob, KEEPING the 3-part requirement rationale (still needed so `{{version}}`/`{{major}}.{{minor}}` resolve). ADD a short forward-looking prose block to the release/tag-strategy section documenting: bare CalVer tags (no `v`, first `2026.7.0`), no-leading-zero month, per-month zero-based REVISION convention (D-07), and that the annotated-tag PUSH triggers GHCR publish with the `git push --delete origin <tag>` + recreate recovery recipe preserved under the new scheme (D-04). For the `git checkout v4.0.0` rollback example (~415), rewrite the illustrative tag to `2026.7.0` but note rolling back to a pre-CalVer release legitimately still uses its old `v4.0.0` tag (mechanism unchanged). In docs/configuration.md, docs/arm64-agent-image.md, docs/cloud-burst.md: rewrite forward-looking `v4.0.0`/`v5.0.0` pin/build EXAMPLES to `2026.7.0` (arm64/cloud-burst use `2026.7.0-arm64` where the `-arm64` suffix appears; suffix mechanism unchanged); LEAVE feature-era labels ("v5.0 Cloud Burst", "foundation of the v5.0 …") verbatim (D-13). In both compose files, rewrite the `PHAZE_IMAGE_TAG=v4.0.0`/`v5.0.0` example COMMENTS to `2026.7.0`, AND rewrite the docker-publish-output comment phrasing so it stays factually correct under D-06's bare tag: `:v<version>` → `:<version>` (docker-compose.agent.yml ~line 28) and `:v<version>-arm64` → `:<version>-arm64` (docker-compose.cloud-agent.yml ~line 37) — same de-`v` fix already applied to docs/deployment.md lines ~348/409/482 and the module docstring; LEAVE the `image: ${PHAZE_IMAGE_TAG:-latest}` lines untouched.
    README decision (VER-02, RESEARCH Open-Q1): make NO edit to README.md — there is no version/release badge to update; the VER-02 README clause is satisfied by the deployment.md CalVer procedure prose. Do NOT add or re-add a badge (one-line badge rule). Record this decision explicitly in the SUMMARY.
    Memory-update note (cannot be edited from the repo): surface in the SUMMARY that the external memory `project_release_procedure.md` must be updated to bare CalVer tags, the new glob, first tag `2026.7.0`, per-month zero-based REVISION, and the `release/2026.7.0` branch name — annotated-tag + delete-recreate GOTCHA carry over verbatim (D-04).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/agents/deployment/test_agent_compose.py::test_calver_scheme_documented -x` PASSES (docs now document `YYYY.MM.REVISION` + `2026.7.0` + REVISION convention).
    - No forward-looking `v4.0.0`/`v5.0.0` PIN/BUILD examples remain: `grep -nE 'PHAZE_IMAGE_TAG=v[0-9]' docs/deployment.md docs/configuration.md docs/cloud-burst.md docker-compose.agent.yml docker-compose.cloud-agent.yml` returns nothing.
    - deployment.md no longer instructs the next release with `v*.*.*`/`:v<version>`: `grep -c ':v<version>' docs/deployment.md` returns 0; `grep -c 'v\*\.\*\.\*' docs/deployment.md` returns 0.
    - Historical labels preserved: `grep -q 'v4.0' docs/deployment.md` still returns a match (the "Phaze v4.0 …" header is a feature-era label left verbatim per D-13) — confirm by review it is a header/label, not a release instruction.
    - `image:` indirection untouched: `grep -c '${PHAZE_IMAGE_TAG:-latest}' docker-compose.agent.yml` unchanged from pre-edit; `git diff docker-compose.agent.yml` shows only comment-line changes.
    - Compose comments de-`v`-ed: `grep -c ':v<version>' docker-compose.agent.yml` returns 0 AND `grep -c ':v<version>-arm64' docker-compose.cloud-agent.yml` returns 0 (bare-tag phrasing under D-06).
    - `git diff --stat README.md` shows README.md UNCHANGED (no badge added/removed).
    - `pre-commit run --all-files` passes (yamllint/actionlint/markdown hooks green on all edited files).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/agents/deployment/test_agent_compose.py::test_calver_scheme_documented -x</automated>
  </verify>
  <done>Forward-looking CalVer examples rewritten across docs + compose comments (D-12) with all historical/feature-era labels left verbatim (D-13); release prose states the D-04 invariant + REVISION convention; README intentionally unchanged with the decision recorded; memory-update note surfaced; the scheme-documented guard is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git tag push → GitHub Actions `on: push: tags` gate | The tags glob is the sole gate deciding whether a tag ref runs CI/publish at all. |
| CI build → GHCR image publish | Published `:latest` / `:2026.7.0` / `:2026.7` image set is the integrity artifact consumers pin via `PHAZE_IMAGE_TAG`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Denial of Service (availability) / Tampering (integrity) | ci.yml `on: push: tags` glob (Task 1) | mitigate | Use the docs-cited CalVer-only glob `[0-9]+.[0-9]+.[0-9]+` (quoted). It matches `2026.7.0`/`2026.12.10`, rejects `main`/`v2026.7.0`/`2026.7`/`2026.7.0-rc1` — so it neither fails to publish a CalVer release (availability) nor over-matches an unintended ref (integrity/supply-chain). Guarded by the Plan-01 `test_ci_workflow_triggers_on_version_tags` (present + `v*.*.*`-absent) and actionlint on the edited ci.yml. |
| T-65-02 | Repudiation (auditability) | annotated-tag-PUSH-triggers-publish invariant, D-04 (Task 3 docs) | mitigate | The release prose keeps the annotated-tag (`git tag -a`) + `git push --delete origin <tag>`/recreate recipe, so every release is a fresh auditable tag-push event. `test_ci_detect_changes_forces_code_changed_on_tags` (unchanged) proves the tag-ref force branch still fires. docker-publish.yml is left untouched (D-05/D-06), preserving the deterministic `:latest`+`:<version>`+`:<major>.<minor>` publish set. |
| T-65-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (`uv lock` only re-syncs the existing `phaze` version entry; no new dependency). RESEARCH §Package Legitimacy Audit: not applicable. |
</threat_model>

<verification>
- All four Plan-01 guards GREEN: `uv run pytest tests/agents/deployment/test_agent_compose.py -x`.
- Full suite: `uv run pytest` (or `just test-bucket agents` + affected buckets) green.
- `pre-commit run --all-files` (frozen-SHA hooks; actionlint validates ci.yml, yamllint the compose files).
- Manual D-12/D-13 review: every rewritten string instructs the NEXT release; no historical/feature-era label (`v4.0 shipped`, `since vX`, `v5.0 Cloud Burst`) was altered.
- SUMMARY surfaces the `project_release_procedure.md` memory-update note (D-04) and the README no-badge decision.
</verification>

<success_criteria>
- ci.yml on the CalVer-only glob; pyproject `2026.7.0` + uv.lock synced (VER-01/VER-02).
- MILESTONES.md mapping table with historical `vN.M` verbatim + `2026.7.0` row; REVISION convention documented (VER-01/VER-04).
- Forward-looking docs/compose examples read CalVer; docker-publish.yml + `image:` indirection + README untouched; historical record intact (VER-02/VER-03).
- Publish invariant (D-04) preserved in prose; memory-update note surfaced.
- All guard tests + full suite green; pre-commit clean.
</success_criteria>

<output>
Create `.planning/phases/65-calver-adoption/65-02-SUMMARY.md` when done. Include: (1) the README no-badge VER-02 decision, (2) the `project_release_procedure.md` memory-update note (D-04), (3) confirmation docker-publish.yml was left unchanged.
</output>
