---
phase: 65-calver-adoption
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/agents/deployment/test_agent_compose.py
autonomous: true
requirements: [VER-01, VER-02, VER-03, VER-04]

must_haves:
  truths:
    - "The retargeted glob guard test asserts the CalVer pattern `[0-9]+.[0-9]+.[0-9]+` is present in ci.yml AND that legacy `v*.*.*` is absent (D-02)."
    - "A new MILESTONES mapping-table guard test asserts a Milestone|Version|Date table exists with historical v1.0..v7.0 rows verbatim (D-09/D-10) plus the 2026.7.0 row (D-01/D-11)."
    - "A new CalVer-scheme doc guard test asserts docs/deployment.md and/or MILESTONES.md document `YYYY.MM.REVISION`, `2026.7.0`, no-leading-zero month, and the per-month zero-based REVISION convention (VER-01/D-07)."
    - "The docker-publish guard test's docstring reads bare CalVer `:<version>` (D-06) while its assertion body is unchanged (not weakened)."
    - "All four test changes collect without import/syntax error; the three CalVer-gating assertions fail RED against the current repo (proving live gates); go GREEN only after Plan 02."
  artifacts:
    - path: "tests/agents/deployment/test_agent_compose.py"
      provides: "Retargeted glob guard + 2 new structural guards + docstring fix"
      contains: "test_milestones_mapping_table_intact"
  key_links:
    - from: "tests/agents/deployment/test_agent_compose.py"
      to: ".github/workflows/ci.yml"
      via: "CI_WORKFLOW_PATH yaml.safe_load, asserts CalVer glob literal present"
      pattern: "\\[0-9\\]\\+\\.\\[0-9\\]\\+\\.\\[0-9\\]\\+"
    - from: "tests/agents/deployment/test_agent_compose.py"
      to: ".planning/MILESTONES.md"
      via: "new MILESTONES_PATH .read_text() substring assertions"
      pattern: "MILESTONES_PATH"
---

<objective>
Author the RED test gate for CalVer adoption: retarget the one real glob guard test to the CalVer pattern, add two new structural guards (mapping-table + scheme-documented), and correct the docker-publish docstring — all in the single existing module `tests/agents/deployment/test_agent_compose.py`.

Purpose: These tests are the executable definition of "CalVer is adopted." They land RED against the current `v*.*.*` repo and become GREEN only after Plan 02 edits ci.yml / MILESTONES.md / docs. This is the highest-consequence phase edit's safety net (a wrong CI glob = silent no-publish).
Output: 4 test changes (1 retarget + 2 new + 1 docstring) in one file; RED against current repo, collect-clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/65-calver-adoption/65-CONTEXT.md
@.planning/phases/65-calver-adoption/65-RESEARCH.md
@.planning/phases/65-calver-adoption/65-PATTERNS.md

<interfaces>
<!-- Reuse these existing module members verbatim — do NOT re-derive them. -->

Existing module-level constants (tests/agents/deployment/test_agent_compose.py lines 39-42):
  CI_WORKFLOW_PATH = Path(__file__).resolve().parents[3] / ".github" / "workflows" / "ci.yml"
  PUBLISH_WORKFLOW_PATH = Path(__file__).resolve().parents[3] / ".github" / "workflows" / "docker-publish.yml"
  (parents[3] == repo root from tests/agents/deployment/)

Existing helper (lines 292-301) — REUSE, do not duplicate:
  _load_ci_workflow_triggers(data) -> dict   # handles PyYAML parsing bare `on:` as boolean True key

Parse idiom: YAML/workflow -> yaml.safe_load(PATH.read_text()); Markdown/prose -> PATH.read_text() + `in`/`any(...)` substring asserts.
Every assertion in this file ends with a fix-instruction message naming what to restore and where — match that idiom.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retarget the glob guard test + fix the docker-publish docstring</name>
  <files>tests/agents/deployment/test_agent_compose.py</files>
  <read_first>
    - tests/agents/deployment/test_agent_compose.py lines 316-339 (`test_ci_workflow_triggers_on_version_tags`, the real critical test — line 332 asserts `"v*.*.*"`), lines 173-206 (`test_docker_publish_workflow_tags_both_latest_and_version`), lines 1-30 (module docstring mentioning `:v<version>`), lines 292-301 (`_load_ci_workflow_triggers`).
    - 65-PATTERNS.md "RETARGET test_ci_workflow_triggers_on_version_tags" block + 65-RESEARCH.md §Guard Tests (line 151) + §Code Examples (retargeted assertion).
  </read_first>
  <action>
    In `test_ci_workflow_triggers_on_version_tags` (line ~332): replace the assertion `any("v*.*.*" in str(t) for t in tags)` with a CalVer check. Introduce a local `CALVER_GLOB = "[0-9]+.[0-9]+.[0-9]+"` and assert `isinstance(tags, list) and any(CALVER_GLOB in str(t) for t in tags)` with a fix-instruction message naming the CalVer glob (per D-02). Immediately ADD the negative assertion `assert not any("v*.*.*" in str(t) for t in tags)` with message "legacy v*.*.* glob must be dropped (D-02: CalVer-only)". KEEP the branch-preservation block (lines 336-339) byte-for-byte. REUSE `_load_ci_workflow_triggers` — do not re-solve the bare-`on:` PyYAML quirk. Rewrite the test's docstring (lines 316-324) from "3-part semver tag" / `PHAZE_IMAGE_TAG=vX.Y.Z` wording to bare CalVer `YYYY.MM.REVISION` / first tag `2026.7.0` wording.
    In `test_docker_publish_workflow_tags_both_latest_and_version`: update ONLY its docstring (lines 174-185) changing `:v<version>` to bare `:<version>` per D-06 — do NOT touch the assertion body (lines 196-197 check `type=semver`/`type=ref,event=tag`, never a literal `v`; D-06: "retarget, don't weaken"). Also fix the module-level docstring reference to `:v<version>` (lines ~22-23) to `:<version>`.
    Keep `test_ci_detect_changes_forces_code_changed_on_tags` (line 342, proves D-03 ref-type gate) UNCHANGED — it already proves the tag-force branch keeps firing for a CalVer tag.
  </action>
  <acceptance_criteria>
    - `grep -n 'CALVER_GLOB = "\[0-9\]+\.\[0-9\]+\.\[0-9\]+"' tests/agents/deployment/test_agent_compose.py` returns a match.
    - `grep -c 'v\*\.\*\.\*' tests/agents/deployment/test_agent_compose.py` shows the string now appears ONLY inside the new negative assertion (`not any("v*.*.*" ...)`) and its message — not in any positive `any(...)` assertion.
    - The docstrings no longer contain `:v<version>`: `grep -c ':v<version>' tests/agents/deployment/test_agent_compose.py` returns 0.
    - `uv run pytest tests/agents/deployment/test_agent_compose.py::test_ci_workflow_triggers_on_version_tags -x` exits NON-ZERO (RED) now — the failure message names the CalVer glob (expected: ci.yml still declares `v*.*.*`; Plan 02 turns it GREEN).
    - `uv run pytest tests/agents/deployment/test_agent_compose.py::test_docker_publish_workflow_tags_both_latest_and_version tests/agents/deployment/test_agent_compose.py::test_ci_detect_changes_forces_code_changed_on_tags -x` PASSES (both unchanged assertions stay green).
    - `uv run ruff check tests/agents/deployment/test_agent_compose.py` clean.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/agents/deployment/test_agent_compose.py::test_docker_publish_workflow_tags_both_latest_and_version -x</automated>
  </verify>
  <done>Glob test retargeted to CalVer (positive + CalVer-only negative), branch block preserved, both docstrings de-`v`-ed; retargeted test is RED, docker-publish + detect-changes tests green, ruff clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add the MILESTONES mapping-table + CalVer-scheme-documented guards</name>
  <files>tests/agents/deployment/test_agent_compose.py</files>
  <read_first>
    - tests/agents/deployment/test_agent_compose.py lines 39-42 (path-constant idiom), lines 398-428 (`test_cleanup_package_list_matches_published_images` — the set/membership structural-guard analog with a rich divergence message).
    - 65-PATTERNS.md "NEW test_milestones_mapping_table_intact" + "NEW test_calver_scheme_documented" blocks; 65-RESEARCH.md §Validation Architecture Test Map (VER-01, VER-04 rows) + §Mapping Table (the seed table with v1.0..v7.0 rows).
    - .planning/MILESTONES.md (the `## vN.M …` detail headers are the source-of-truth for row names/dates).
  </read_first>
  <action>
    Add two module-level path constants beside lines 39-42, same `Path(__file__).resolve().parents[3] / ...` style: `MILESTONES_PATH = ... / ".planning" / "MILESTONES.md"` (note: repo-root `.planning/`, NOT the phase dir) and `DEPLOYMENT_DOC_PATH = ... / "docs" / "deployment.md"`.
    Add `test_milestones_mapping_table_intact() -> None` (VER-04): `assert MILESTONES_PATH.exists()` guard, `text = MILESTONES_PATH.read_text()`, then assert (a) a mapping-table header with columns `Milestone`, `Version`, `Date` is present (robust to surrounding whitespace — assert each column token appears on a `|`-delimited header line, do not pin exact formatting), (b) each historical version string `v1.0`, `v2.0`, `v3.0`, `v4.0`, `v5.0`, `v6.0`, `v7.0` appears verbatim (D-10), and (c) the bare `2026.7.0` row is present (D-01/D-11). Give a rich fix-instruction failure message naming exactly which token is missing (mirror the analog's set-difference diagnostic style). Assert substring membership — do NOT re-implement Markdown table parsing.
    Add `test_calver_scheme_documented() -> None` (VER-01): read both `DEPLOYMENT_DOC_PATH` and `MILESTONES_PATH`; assert the combined text contains `YYYY.MM.REVISION`, `2026.7.0`, wording establishing the no-leading-zero month, and wording establishing the per-month zero-based REVISION convention (D-07). Accept the documentation living in EITHER file (combined-text membership) so Plan 02 can place the prose where it reads best. Fix-instruction message points at docs/deployment.md + MILESTONES.md.
    Both tests need only already-imported `Path`/`yaml`/`re` — add no new imports.
  </action>
  <acceptance_criteria>
    - `grep -n 'MILESTONES_PATH =' tests/agents/deployment/test_agent_compose.py` and `grep -n 'DEPLOYMENT_DOC_PATH =' tests/agents/deployment/test_agent_compose.py` each return a match using `parents[3]`.
    - `uv run pytest tests/agents/deployment/test_agent_compose.py --collect-only -q` lists both `test_milestones_mapping_table_intact` and `test_calver_scheme_documented` (collection clean — no ImportError/SyntaxError).
    - `uv run pytest tests/agents/deployment/test_agent_compose.py::test_milestones_mapping_table_intact tests/agents/deployment/test_agent_compose.py::test_calver_scheme_documented -x` exits NON-ZERO (RED) now — MILESTONES.md has no mapping table and the CalVer scheme prose is not yet written (Plan 02 turns both GREEN).
    - `uv run ruff check tests/agents/deployment/test_agent_compose.py` clean (import order, no unused).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/agents/deployment/test_agent_compose.py --collect-only -q</automated>
  </verify>
  <done>Two new structural guards added with `parents[3]` path constants and fix-instruction messages; both collect cleanly and are RED against the current repo; ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git tag push → GitHub Actions `on: push: tags` gate | Whoever can push a tag triggers (or fails to trigger) the CI/publish pipeline. The tags glob is the sole gate for tag refs. |
| CI build → GHCR image publish | The published image set (`:latest`, `:2026.7.0`, `:2026.7`) is the integrity artifact consumers pull. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Denial of Service (availability) / Tampering | `.github/workflows/ci.yml` `on: push: tags` glob | mitigate | This plan's retargeted `test_ci_workflow_triggers_on_version_tags` asserts the CalVer glob is present AND `v*.*.*` is absent, so a dropped/wrong/over-broad glob fails CI before a release. Complemented by actionlint (pre-commit) validating ci.yml syntax. |
| T-65-02 | Repudiation (auditability) | annotated-tag-PUSH-triggers-publish invariant (D-04) | mitigate | `test_ci_detect_changes_forces_code_changed_on_tags` (kept unchanged) proves the tag-ref force branch still fires; the retargeted trigger test proves the tag gate still exists. Together they preserve the auditable "annotated tag push → publish" path under the new scheme. |
| T-65-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH §Package Legitimacy Audit: not applicable). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages to gate. |
</threat_model>

<verification>
- `uv run pytest tests/agents/deployment/test_agent_compose.py --collect-only -q` — all tests collect (no syntax/import breakage).
- RED proof: the three CalVer-gating tests (`test_ci_workflow_triggers_on_version_tags`, `test_milestones_mapping_table_intact`, `test_calver_scheme_documented`) fail now; they are the executable definition of "CalVer adopted" that Plan 02 satisfies.
- `uv run ruff check tests/agents/deployment/test_agent_compose.py` + `uv run ruff format --check tests/agents/deployment/test_agent_compose.py` clean.
</verification>

<success_criteria>
- Glob guard retargeted to `[0-9]+.[0-9]+.[0-9]+` (present) + `v*.*.*` (absent) per D-02; branch-CI preservation intact.
- Two new structural guards added matching the same-file idiom (`parents[3]`, exists-guard, `.read_text()`, substring asserts, fix-instruction messages).
- docker-publish + module docstrings de-`v`-ed (D-06) with assertion bodies unchanged.
- The three CalVer gates are RED against current repo (live gates); collection is clean; ruff clean.
</success_criteria>

<output>
Create `.planning/phases/65-calver-adoption/65-01-SUMMARY.md` when done.
</output>
