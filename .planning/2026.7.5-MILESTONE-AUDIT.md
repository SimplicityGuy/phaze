---
milestone: 2026.7.5
milestone_name: Parallel Enrich DAG (Retire Linear FileState)
audited: 2026-07-13
status: tech_debt
scores:
  requirements: 42/42
  phases: 14/14
  integration: 6/6 seams verified
  flows: 1/1 E2E flow complete
gaps: {}  # No critical blockers
deferred_operational_gate:  # Deployment-gated; non-blocking for closure, blocking for prod deploy
  - id: "MIG-02 / SC-3 live-corpus shadow-compare"
    phases: [79, 84]
    note: "Committed re-runnable gate is built + verified in CI on synthetic corpus. The live 200K-corpus restore run is deferred by design (decision D-02) — no live dump exists in the worktree. Must run green on the DRAINED corpus before migration 039 deploys. Tracked in 79-HUMAN-UAT.md and 84-06-DEFERRED.md."
  - id: "MIG-04 / 039 real-corpus rehearsal"
    phase: 90
    note: "Restore prod snapshot → apply 032→038 → shadow-compare green (drained) → run 039 → assert files_state_archive populated + files.state/ix_files_state gone → downgrade() restores verbatim → record ACCESS EXCLUSIVE lock + DDL timing. Prod is at Alembic 031; 039 must never be prod's first migration. Tracked in 90-HUMAN-UAT.md."
  - id: "READ-05 live tag/CUE write UAT"
    phase: 85
    note: "On a real deploy, an actually-applied file surfaces in Tag-write/CUE review workspaces and write/generate produce real filesystem output (dead before this phase). Filesystem-mutating; verify against live corpus. Tracked in 85-HUMAN-UAT.md."
tech_debt:
  - phase: 82-counts-pending-set-cutover
    items:
      - "PERF-02: /pipeline/stats poll at 200K synthetic scale measured OVER budget; DENORM-01 (denormalized stage-bitmap column) NO-GO/deferred to v2. Recommended follow-up: parallelize the three _safe_bucket_counts reads in get_stage_progress via asyncio.gather before considering denormalization. Non-blocking (licensed by PERF-02's own YAGNI-default measurement)."
  - phase: 81-per-stage-failure-persistence-retry-paths
    items:
      - "WR-01/WR-02 (deferred-items.md): open code-review warnings — a Phase-80 recovery-cutover follow-up + a metadata payload-retention edge case. Intentionally out of Phase 81 fix scope."
  - phase: 83-cloud-routing-sidecar-cutover
    items:
      - "83-01/83-03: pre-existing non-hermetic test-infra flakes (pk_agents / bucket-pollution). Not regressions."
      - "83-06: backfill-held compute file mis-routes to local (not stranded) — display/routing edge, out of gap-closure scope."
      - "backends.py:563-566: duplicated comment block in KueueBackend.reconcile (cosmetic, pre-existing)."
  - phase: 85-executed-gate-revival
    items:
      - "WR-01..04 (todos/pending/wr-01-review-builder-limit-before-filter.md): .limit()-before-Python-qualifier-filter in review.get_tagwrite_review_rows / tags.bulk_write_no_discrepancies / review.get_cue_review_cards. Accepted as tracked follow-up debt (operator decision 2026-07-10)."
  - phase: 90-destructive-migration-writer-removal
    items:
      - "agent_files.py:~133 stale comment references a state=DISCOVERED stamp that no longer exists (comment-only, passes anti-drift guard, zero runtime effect). Doc-hygiene residue from MIG-04. Found by integration checker."
nyquist:
  compliant_phases: [77, 78, 79, 80, 81, 83, 84, 85, 86, 87, 88, 89, 90]
  partial_phases: [82]
  missing_phases: []
  overall: "13/14 compliant; Phase 82 nyquist_compliant:false is the PERF-02 over-budget measurement finding (VALIDATION status: verified, wave_0_complete: true) — a tracked non-blocking follow-up, not a test-coverage gap."
---

# Milestone 2026.7.5 — Parallel Enrich DAG — Audit Report

**Verdict:** All 42 requirements satisfied, all 14 phases verified, cross-phase integration and E2E flow clean. No critical blockers. Status is **tech_debt** (not `passed`) only because a set of tracked, non-blocking follow-ups and a deployment-gated operational rehearsal remain outstanding.

## Milestone goal

Make the enrich pipeline truly per-file parallel by deleting the linear `FileState` enum and deriving per-file, per-stage status (`not_started`/`in_flight`/`done`/`failed`) from the output tables that already exist. Executed as a live-corpus data-model migration, seam by seam: additive `032` → standing shadow-compare gate → readers-before-writers cutover → destructive `039` (drop `files.state`).

## Requirements coverage — 42/42 satisfied (3-source cross-reference)

Every REQ-ID was cross-referenced across (1) REQUIREMENTS.md traceability table, (2) each phase's VERIFICATION.md requirements-coverage table, and (3) SUMMARY.md `requirements_completed` frontmatter.

- **Traceability table:** all 42 marked `[x] Complete`.
- **VERIFICATION tables:** all 42 marked `✓ SATISFIED` with concrete code/test evidence.
- **SUMMARY frontmatter:** covered most; a subset (DERIV-04, INFLIGHT-01..03, READ-01/02, UI-01, LEGACY-01..03) was declared in plan `requirements:` frontmatter rather than the SUMMARY field — each was manually confirmed `✓ SATISFIED` in the phase VERIFICATION. No downgrade to `partial`.
- **Orphan detection:** 0 orphans. Every REQ-ID maps to a phase and appears in that phase's VERIFICATION.

| Group | REQs | Phase(s) | Status |
|-------|------|----------|--------|
| DERIV | 01–05 | 78 | ✓ satisfied |
| ELIG | 01–04 | 78 | ✓ satisfied |
| INFLIGHT | 01–03 | 78 | ✓ satisfied |
| FAIL | 01–04 | 81 | ✓ satisfied |
| READ | 01–05 | 82, 80, 84, 85 | ✓ satisfied |
| SIDECAR | 01–03 | 83, 84, 86 | ✓ satisfied |
| UI | 01–05 | 87 | ✓ satisfied |
| PERF | 01–02 | 77, 82 | ✓ satisfied (PERF-02 = over-budget finding, tech debt below) |
| MIG | 01–04 | 77, 79, 90 | ✓ satisfied (MIG-02/04 live run = operational gate below) |
| LEGACY | 01–03 | 89 | ✓ satisfied |
| PRIO | 01 | 87 | ✓ satisfied |
| DRILL | 01–03 | 88 | ✓ satisfied |

Explicitly deferred to v2: **DENORM-01** (denormalized stage-bitmap column) — YAGNI; PERF-02 measured, deferred pending `asyncio.gather` parallelization first.

## Phase verifications — 14/14 passed

All of 77–90 have a VERIFICATION.md with `status: passed`. No unverified phases.

## Cross-phase integration — 6/6 seams verified, E2E flow complete

Integration checker (gsd-integration-checker) confirmed:

1. **Single source of truth** — `enums/stage.py` + `services/stage_status.py` consumed by 6 and 11 modules respectively (routers, services, tasks). No live-code read of `FileRecord.state`/`files.state`; all ~26 matches are docstrings/comments. `mypy .` clean (208 files); anti-drift guard `test_no_filestate_guard.py` is a real tokenize-based guard with a recorded RED→GREEN mutation.
2. **Stage independence** — `ELIGIBILITY_DAG` has empty upstreams for metadata/analyze/fingerprint; three independent `eligible_clause` pending-set queries. Cross-stage deadlock dissolved.
3. **UI routes wired + reachable** — `/s/files`, `/pipeline/lanes/{backend_id}`, `/admin/agents/{agent_id}/_activity`, retry endpoints, `eligibility_trace`, `POST /pipeline/stages/{stage}/priority` all mounted and consumed by HTMX templates.
4. **Migration chain linear, single head `039`** — 001→039 no branches; zero executable `saq_jobs` references.
5. **Failure markers → counts → UI retry** — `failed_at` markers feed `failed_clause` → four-bucket counts → retry affordances.
6. **`applied()` / `proposals.status` authority** — sole authority for review/apply; `_TERMINAL_FILE_STATES` fully removed from `src/`.

**E2E:** discovered → 3 parallel enrich stages → tracklist/propose (gated on metadata+analyze only) → review → apply → tag/CUE write — traced end-to-end, zero breaks.

## Outstanding items

### Deployment-gated operational gate (non-blocking for closure, BLOCKING for prod deploy)
All three pending HUMAN-UAT items converge on one deploy-time rehearsal: restore a prod snapshot → apply `032→038` → shadow-compare green on the **drained** corpus → run `039` → verify archive + column drop + `downgrade()` → then live tag/CUE write behavior surfaces. Prod is at Alembic **031**; the milestone's `032→039` are all unreleased.

### Tech debt (tracked, non-blocking) — see frontmatter
PERF-02 over-budget / DENORM-01 deferred (P82); WR-01/02 (P81); test-infra flakes + routing edge (P83); review-builder `.limit()`-before-filter WR-01..04 (P85); stale comment `agent_files.py:~133` (P90).

## Recommendation

The milestone met its definition of done in code. Proceed to **`/gsd:complete-milestone 2026.7.5`** (archive + tag), accepting the tracked tech debt into the backlog. Schedule the **039 real-corpus rehearsal + drained-corpus shadow-compare** as the pre-deploy operator gate before rolling `032→039` to production.
