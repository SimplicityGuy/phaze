---
phase: quick-260613-t7k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/templates/pipeline/partials/dag_canvas.html
  - src/phaze/services/pipeline.py
  - src/phaze/routers/pipeline.py
  - src/phaze/templates/base.html
  - tests/test_dag_canvas_render.py
  - tests/test_pipeline_dag_context.py
  - tests/test_services/test_pipeline.py
autonomous: true
requirements:
  - QUICK-260613-t7k-FIX1-widen-dag-chips
  - QUICK-260613-t7k-FIX2-per-stage-enqueue-gating
user_setup: []

must_haves:
  truths:
    - "The Phase-38 per-stage control row (Pause + ▲ Higher / 50 / ▼ Lower) renders fully inside each agent chip — '▼ Lower' is no longer clipped to 'Low'."
    - "Each agent stage's enqueue button (Metadata, Analyze, Fingerprint) is blocked ONLY when ITS OWN stage has in-flight jobs — not when any other agent stage is busy."
    - "When Analyze is running, Metadata and Fingerprint enqueue buttons stay enabled (they can run in parallel)."
    - "The DAG canvas + the 5s /pipeline/stats poll seed the three per-stage busy counts into $store.pipeline so the gating reacts live."
    - "All existing DAG/stats tests stay green; new tests lock both fixes."
  artifacts:
    - path: "src/phaze/templates/pipeline/partials/dag_canvas.html"
      provides: "Widened NODE_LAYOUT (w=240, recomputed columns, grown canvas/SVG) + per-stage busy gates"
    - path: "src/phaze/services/pipeline.py"
      provides: "get_stage_busy_counts — degrade-safe per-stage in-flight count from saq_jobs"
    - path: "src/phaze/routers/pipeline.py"
      provides: "metadataBusy/analyzeBusy/fingerprintBusy seeded into the dag context map"
  key_links:
    - from: "src/phaze/routers/pipeline.py:_build_dag_context"
      to: "src/phaze/services/pipeline.py:get_stage_busy_counts"
      via: "await call, results folded into the dag dict"
      pattern: "get_stage_busy_counts"
    - from: "src/phaze/templates/pipeline/partials/dag_canvas.html:nodes getter"
      to: "$store.pipeline.metadataBusy / analyzeBusy / fingerprintBusy"
      via: "per-stage blocked gate"
      pattern: "s\\.(metadata|analyze|fingerprint)Busy > 0"
---

<objective>
Two related pipeline-DAG fixes in one plan, each its own atomic task with its own tests.

FIX 1 (Task 1): the Phase-38 per-stage control row overflows the hard-coded 180px node chips, clipping "▼ Lower" to "Low". Widen the chips and recompute the fixed-coordinate grid so nothing overlaps and the anchor-derived SVG edges still land cleanly.

FIX 2 (Task 2): the three agent-stage enqueue buttons (Metadata/Analyze/Fingerprint) are all gated on a single global `agentBusy` (queued+active summed across ALL agent queues). When one stage runs, the other two lock with "Agent busy" and cannot run in parallel. Gate each stage on ITS OWN in-flight count instead.

Purpose: restore the per-stage operator control UX (visible controls + true parallelism across the three agent stages).
Output: corrected dag_canvas layout, a new degrade-safe per-stage busy service, stats-context plumbing, and updated/added tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Primary target template (BOTH fixes touch it)
@src/phaze/templates/pipeline/partials/dag_canvas.html

# FIX 2 plumbing
@src/phaze/services/pipeline.py
@src/phaze/routers/pipeline.py
@src/phaze/services/stage_control.py
@src/phaze/tasks/_shared/stage_control.py
@src/phaze/templates/pipeline/partials/stats_bar.html

# Tests that must stay green / be updated
@tests/test_dag_canvas_render.py
@tests/test_pipeline_dag_context.py
@tests/integration/test_stage_pause.py

<interfaces>
<!-- Per-stage in-flight counts come from the SAQ Postgres broker table `saq_jobs`, reachable
     through the API-process SQLAlchemy AsyncSession (services/stage_control.py already issues
     raw `UPDATE saq_jobs ...` through the same AsyncSession — confirms the table is on the same
     Postgres DB the session targets). `saq_jobs` has NO `function` column; the deterministic key
     is `<function>:<file_id>` (Phase 35), so per-stage filtering is by key prefix. -->

From phaze.tasks._shared.stage_control:
  STAGE_TO_FUNCTION: dict[str, str] = {
    "metadata": "extract_file_metadata",
    "analyze": "process_file",
    "fingerprint": "fingerprint_file",
  }
  _FUNCTION_TO_STAGE: dict[str, str]  # exact inverse (module-private; re-derive locally if needed)

Existing degrade-safe pattern to mirror (services/pipeline.py):
  async def get_stage_controls(session) -> dict[str, dict[str, int | bool]]
    # try: SELECT ... ; except: warn -> guarded rollback -> return defaults. NEVER raises into the 5s poll.

Existing raw saq_jobs query style (services/stage_control.py):
  _SET_PRIORITY_SQL = text("UPDATE saq_jobs SET priority = :p WHERE status = 'queued' AND key LIKE :pfx")
  # status values observed in saq_jobs: 'queued', 'active' (see tests/integration/test_stage_pause.py).

Router seam (routers/pipeline.py:_build_dag_context) — folds sources into the `dag` dict that
both the full-page canvas seeds (id="dag-seed-<key>") and the /pipeline/stats OOB poll re-push.
The Phase-38 stage-control keys already ride this exact dag.items() loop with ZERO stats_bar.html edits.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen DAG node chips so the per-stage control row fits</name>
  <files>src/phaze/templates/pipeline/partials/dag_canvas.html, tests/test_dag_canvas_render.py</files>
  <behavior>
    - All 9 NODE_LAYOUT nodes render at width 240 (was 180); "▼ Lower" no longer clips.
    - The 9 anchor-derived bézier edges still render (regex still finds 9 `<path d="M ..C ..">`).
    - Column-1 chips still pass the existing vertical non-overlap guard (y values unchanged).
    - Column-1 (x=392) and column-2 (x=760) chips do NOT horizontally overlap (col1 right edge 632 < col2 left edge 760).
  </behavior>
  <action>
    In `dag_canvas.html`, replace the `NODE_LAYOUT` map (currently every node `w: 180`, columns at x 24/320/624/928) with widened, re-gridded coordinates. Keep ALL `y` and `h` values exactly as they are now (the Phase-38 vertical layout + overlap guard already pass). Set every node `w` to 240 and shift the four columns to x = 24 (col0), 392 (col1), 760 (col2), 1128 (col3). Resulting map:
      discovery   x:24   y:467 w:240 h:130
      metadata    x:392  y:24  w:240 h:250
      analyze     x:392  y:300 w:240 h:250
      fingerprint x:392  y:576 w:240 h:250
      scan_search x:392  y:852 w:240 h:126
      proposals   x:760  y:200 w:240 h:174
      scrape      x:760  y:858 w:240 h:114
      execute     x:1128 y:222 w:240 h:130
      match       x:1128 y:858 w:240 h:114
    Rationale to encode (each column gap is 128px: e.g. col0 right edge 264 → col1 left 392): the gap exceeds the ±60 bézier control-point offset used in the edge `d` path (`sx + 60` / `tx - 60` at the SVG path line), so `sx+60 < tx-60` holds for every edge and the curves stay clean. The edge `d` strings are DERIVED from NODE_LAYOUT anchors, so they auto-recompute — do NOT hand-edit any path.
    Then grow the canvas + SVG to fit col3's new right edge (1368 + 24px margin = 1392). Change the canvas wrapper inline style width `1132px` → `1392px` (height stays `1000px`), and the `<svg>` `width="1132"` → `width="1392"` and `viewBox="0 0 1132 1000"` → `viewBox="0 0 1392 1000"` (height stays 1000).
    Update the NODE_LAYOUT comment block (the `Phase-38 RECOMPUTE` paragraph): add the WIDTH rationale — agent chips widened 180→240px because the Phase-38 control row (Pause button + ▲ Higher / value / ▼ Lower stepper) overflowed 180px and clipped "▼ Lower" to "Low"; downstream columns re-gridded to preserve ≥60px bézier-control clearance; canvas/SVG grown 1132→1392 wide. Keep the existing height/gutter rationale text accurate.
    Add a render test `test_topology_chips_widened_to_240_and_columns_do_not_overlap` to `tests/test_dag_canvas_render.py`: render via `_render_canvas()`, assert every `id="node-..."` chip carries `width: 240px` (count of `width: 240px` == 9), and assert column-1 vs column-2 do not horizontally overlap by parsing the `left: Npx` of `node-metadata` (392) and `node-proposals` (760) and asserting `760 >= 392 + 240`.
    Do NOT touch the gating logic or any FIX-2 surface in this task.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dag_canvas_render.py -q</automated>
  </verify>
  <done>NODE_LAYOUT is w=240 with columns 24/392/760/1128; canvas/SVG are 1392×1000; the 9-edge bézier render test, the existing vertical non-overlap guard, and the new width/horizontal-overlap test all pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-stage enqueue gating (replace global agentBusy)</name>
  <files>src/phaze/services/pipeline.py, src/phaze/routers/pipeline.py, src/phaze/templates/base.html, src/phaze/templates/pipeline/partials/dag_canvas.html, tests/test_services/test_pipeline.py, tests/test_pipeline_dag_context.py, tests/test_dag_canvas_render.py</files>
  <behavior>
    - get_stage_busy_counts(session) returns {"metadata": int, "analyze": int, "fingerprint": int} = count of saq_jobs rows with status in ('queued','active') whose key matches that stage's function prefix.
    - On ANY DB error it degrades to all-zeros (warn → guarded rollback → zeros) and never raises (mirrors get_stage_controls).
    - _build_dag_context emits metadataBusy/analyzeBusy/fingerprintBusy as ints in the dag map (so they ride the dag.items() seed + OOB loop with no stats_bar.html edit).
    - The metadata/analyze/fingerprint enqueue gates read their OWN busy key (s.metadataBusy / s.analyzeBusy / s.fingerprintBusy), not s.agentBusy; the "Agent busy" reason copy is preserved per-stage.
    - With analyzeBusy>0 but metadataBusy==0 and fingerprintBusy==0, only Analyze is blocked.
  </behavior>
  <action>
    1. Service (`src/phaze/services/pipeline.py`): add `async def get_stage_busy_counts(session: AsyncSession) -> dict[str, int]`. Build the per-stage in-flight count from `saq_jobs` using one static, bound-param SQL grouped query, then bucket by the deterministic-key function prefix. Recommended: `SELECT split_part(key, ':', 1) AS fn, COUNT(*) AS n FROM saq_jobs WHERE status IN ('queued','active') GROUP BY fn`, then map each `fn` back to its stage via the inverse of `STAGE_TO_FUNCTION` (import `STAGE_TO_FUNCTION` from `phaze.tasks._shared.stage_control`; build the inverse locally). Seed the result `{ "metadata": 0, "analyze": 0, "fingerprint": 0 }` and overlay matched fn buckets; non-stage functions (generate_proposals, scan_directory, …) are ignored. Use `sqlalchemy.text` with NO interpolated user input — the only literals are the static allowlist function names from the constants module (T-SC). Wrap in the SAME degrade discipline as `get_stage_controls`: `try` the SELECT; `except` → `logger.warning("stage_busy_degraded", exc_info=True)`, guarded `await session.rollback()`, return all-zeros. Document that a paused stage's parked rows (status still 'queued', scheduled=SENTINEL) DO count as busy — an accepted, documented behavior consistent with the prior global agentBusy meaning of "has a backlog."
    2. Router (`src/phaze/routers/pipeline.py` `_build_dag_context`): after the existing `get_stage_controls` overlay, `busy = await get_stage_busy_counts(session)` and add `dag["metadataBusy"] = int(busy["metadata"])`, `dag["analyzeBusy"] = int(busy["analyze"])`, `dag["fingerprintBusy"] = int(busy["fingerprint"])`. Add the import to the existing `from phaze.services.pipeline import (...)` block. Add a short comment noting these replace the global agentBusy gate and ride the dag.items() seed/OOB loop (no stats_bar.html change).
    3. Store seed (`src/phaze/templates/base.html`): in the `Alpine.store('pipeline', { ... })` literal, add `metadataBusy: 0, analyzeBusy: 0, fingerprintBusy: 0` next to the existing `agentBusy: 0` (keep agentBusy — it is still seeded and the queue_progress card path uses agent_busy server-side).
    4. Template gates (`dag_canvas.html` nodes getter, the three `mk(...)` calls): change `blocked: s.discovered === 0 || s.agentBusy > 0` and its `reason` ternary to read the per-stage key — metadata→`s.metadataBusy > 0`, analyze→`s.analyzeBusy > 0`, fingerprint→`s.fingerprintBusy > 0`. Keep the `'Agent busy'` reason string verbatim and the `s.discovered === 0` / `'No files discovered'` branch. Leave `analyzeActive: s.analyzeActive` (the ACTIVE pill source) unchanged. Update the inline comment that documents the gate to explain the global-agentBusy→per-stage change (the old single flag locked all three agent stages together; each now gates on its own in-flight count so they run in parallel).
    5. Tests:
       - `tests/test_dag_canvas_render.py`: add `"metadataBusy", "analyzeBusy", "fingerprintBusy"` to `_DAG_KEYS`. Update `test_gating_predicates_use_busy_and_dependency_gates` to assert the nodes block contains `s.metadataBusy > 0`, `s.analyzeBusy > 0`, `s.fingerprintBusy > 0` (and still `s.controllerBusy > 0`, `s.analyzed === 0`, `s.approved === 0`); drop the `s.agentBusy > 0` assertion. Add `test_gating_agent_stages_gate_on_own_busy_count`: render with a `dag` where `analyzeBusy=1` (others 0) and assert the rendered nodes getter still gates each stage on its own key (string-level assertion against the source is sufficient — the per-stage independence is structural).
       - `tests/test_pipeline_dag_context.py`: add the 3 busy keys to `_NEW_STORE_KEYS` (this drives the store-literal seed test against base.html, the int-key context test against `_build_dag_context`, and the OOB-seed poll test in one edit).
       - `tests/test_services/test_pipeline.py`: add a unit test for `get_stage_busy_counts` degrade path using a fake session whose `execute` raises (mirror `test_get_stage_controls_degrades_on_db_error`): assert it returns `{"metadata":0,"analyze":0,"fingerprint":0}`, rolled back, no raise. (A live-saq_jobs count assertion belongs in tests/integration using the `stage_env` fixture pattern from `tests/integration/test_stage_pause.py` — add an integration test there ONLY if it runs in the standard suite; otherwise the degrade unit test + the structural gating tests are the contract.)
  </action>
  <verify>
    <automated>uv run pytest tests/test_dag_canvas_render.py tests/test_pipeline_dag_context.py tests/test_services/test_pipeline.py -q</automated>
  </verify>
  <done>get_stage_busy_counts exists and is degrade-safe; _build_dag_context seeds the 3 per-stage busy keys; base.html store + dag_canvas gates read per-stage busy with the "Agent busy" copy preserved; updated + new tests pass and the global agentBusy gate is gone from the nodes getter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → /pipeline routes | Existing untrusted surface; this change adds NO new request input. |
| API process → saq_jobs (Postgres) | New read of the SAQ broker table for per-stage counts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-t7k-01 | Injection (Tampering) | get_stage_busy_counts saq_jobs SELECT | mitigate | Static `sqlalchemy.text` query; no operator input interpolated. Stage→function names come only from the `STAGE_TO_FUNCTION` constants allowlist (mirrors the Phase-37 T-37-01 discipline). |
| T-t7k-02 | Denial of Service | 5s /pipeline/stats poll | mitigate | get_stage_busy_counts wraps the SELECT in try/except → warn → guarded rollback → all-zeros, exactly like get_stage_controls / _safe_count, so a saq_jobs hiccup degrades to 0 instead of 500ing the hot poll path. |
| T-t7k-03 | Information disclosure | DAG render | accept | Counts are aggregate ints already exposed via the existing agentBusy/dag seeds; no new sensitive data. |
</threat_model>

<verification>
- `uv run pytest tests/test_dag_canvas_render.py tests/test_pipeline_dag_context.py tests/test_services/test_pipeline.py -q` passes.
- `uv run ruff check . && uv run ruff format --check .` clean (150-char lines, double quotes).
- `uv run mypy .` clean (type hints on the new service function; `dict[str, int]` return).
- Full suite + coverage stays ≥85%: `uv run pytest --cov --cov-report=term-missing -q`.
- `pre-commit run --all-files` passes (never bypass hooks).
</verification>

<success_criteria>
- "▼ Lower" renders in full inside every agent chip (chips are 240px wide; canvas/SVG 1392×1000; 9 bézier edges intact).
- Running one agent stage no longer blocks the other two: each enqueue button gates on its own metadataBusy/analyzeBusy/fingerprintBusy, with the "Agent busy" reason preserved.
- The three per-stage busy counts seed into $store.pipeline on full-page load AND re-push on every 5s /pipeline/stats poll (via the existing dag.items() loop, no stats_bar.html edit).
- All prior DAG/stats tests stay green; new tests lock both fixes; coverage ≥85%.
</success_criteria>

<output>
Create `.planning/quick/260613-t7k-widen-pipeline-dag-node-chips-and-make-m/260613-t7k-SUMMARY.md` when done.
</output>
