---
task: 260707-c9o
title: Add a y-axis (BPM) scale + labels to the "BPM (fine windows)" chart
type: quick
mode: execute
files_modified:
  - src/phaze/routers/proposals.py
  - src/phaze/routers/record.py
  - src/phaze/templates/proposals/partials/analysis_timeline.html
  - tests/review/routers/test_proposals.py
  - tests/shared/core/test_record_palette_agents.py
---

<objective>
The "BPM (fine windows)" chart renders as a bare stretched polyline with NO y-axis
and NO numeric labels — a reader cannot tell what BPM range the line spans. Surface
the min/max BPM (already computed and discarded inside the helper) and render them as
HTML labels alongside the chart on BOTH surfaces that show it.

Purpose: make the BPM chart readable (max at top, min at bottom).
Output: refactored `_bpm_spark` helper returning `(points, lo, hi, count)`; a gutter
label column added to the shared `analysis_timeline.html` partial; both context builders
updated; unit + render tests; all quality gates green.

Key preconditions confirmed by inspection:
- `record_body.html` does NOT duplicate the chart markup — it `{% include %}`s
  `proposals/partials/analysis_timeline.html` VERBATIM (line 59). So there is exactly
  ONE template to edit. Both context builders must pass the new lo/hi.
- `_bpm_polyline_points` (proposals.py:41-60) already computes `lo, hi = min/max(bpms)`
  then throws them away — the numbers are free.
- The helper has THREE call sites, all needing an update when the return type changes:
  proposals.py:112 (row sparkline — needs only `.points`), proposals.py:301 (proposals
  timeline context), record.py:120 (record-view context). record.py imports the helper
  from proposals (record.py:33).
- The XSS-hardening invariant lives at proposals.py:32-36: SVG coordinate attributes
  stay numeric-only so no essentia-derived string reaches an SVG geometry attribute.

Out of scope (DO NOT TOUCH): Key/Mood/Style ribbons, the Deepen button, the sampled
badge, the compact row sparkline's own visual (`proposal_row.html`).
</objective>

<context>
@.planning/STATE.md
@src/phaze/routers/proposals.py
@src/phaze/routers/record.py
@src/phaze/templates/proposals/partials/analysis_timeline.html
@src/phaze/templates/record/record_body.html
@tests/review/routers/test_proposals.py
@tests/shared/core/test_record_palette_agents.py

<interfaces>
Current helper (proposals.py:41-60), returns only the points string:

  def _bpm_polyline_points(windows, total_sec, width, height) -> str

Existing hardening comment to preserve (proposals.py:32-36):
  # coordinate attributes stay numeric-only so no essentia-derived string ever
  # reaches an SVG geometry attribute (XSS hardening, RESEARCH V5 / T-31-06-01).

Three call sites (all update on return-type change):
  proposals.py:112  sparklines[str(fid)] = _bpm_polyline_points(windows, total_sec, SPARK_W, SPARK_H)  # dict[str,str] — uses points only
  proposals.py:301  "bpm_points": _bpm_polyline_points(fine, total_sec, TIMELINE_W, TIMELINE_H)
  record.py:120     "bpm_points": _bpm_polyline_points(fine, total_sec, TIMELINE_W, TIMELINE_H)

record.py:33 imports: from phaze.routers.proposals import TIMELINE_H, TIMELINE_W, _bpm_polyline_points, _ribbons

Existing template gate (analysis_timeline.html:24-33):
  <h4 ...>BPM (fine windows)</h4>
  {% if bpm_points %}
    <svg viewBox="0 0 {{ timeline_w }} {{ timeline_h }}" preserveAspectRatio="none" ...>
      <polyline points="{{ bpm_points }}" .../>
  {% else %}
    <p ...>No BPM data.</p>
  {% endif %}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor helper to surface lo/hi and update all call sites + context builders</name>
  <files>src/phaze/routers/proposals.py, src/phaze/routers/record.py, tests/review/routers/test_proposals.py</files>
  <behavior>
    New unit tests for the refactored helper (place in tests/review/routers/test_proposals.py,
    bucket "review"; the helper is a pure sync function — no DB needed):
    - Normal range: fine windows with bpm 120 and 128 → result.points is a non-empty
      "x,y x,y" string; result.lo == 120.0; result.hi == 128.0; result.count == 2.
    - Flat line: all windows bpm == 120 → result.lo == result.hi == 120.0 (span <= 0
      surfaces the single value, not a bogus min≠max); points still non-empty;
      y coords land at height/2 (unchanged existing flat-line behavior).
    - Empty: no windows with bpm (or total_sec <= 0) → result.points == ""; result.lo is
      None; result.hi is None; result.count == 0.
  </behavior>
  <action>
    Introduce a NamedTuple carrying the numbers the helper already computes:
    `class BpmSpark(NamedTuple): points: str; lo: float | None; hi: float | None; count: int`
    (import NamedTuple from typing). Rename `_bpm_polyline_points` → `_bpm_spark` and change
    its return type to `BpmSpark`. Keep the coordinate math IDENTICAL (same rounding, same
    `span <= 0 → height/2` flat-line handling, same "higher BPM sits higher" mapping) so the
    rendered `points` string is byte-for-byte unchanged. Preserve the XSS-hardening comment at
    proposals.py:32-36 and its intent: `lo`/`hi` are numeric `float`s from `w.bpm` (never
    essentia strings) and are NOT written into any SVG geometry attribute — they exist only for
    HTML label text rendered in Task 2. Return `BpmSpark("", None, None, 0)` on the empty/no-data
    path; otherwise `BpmSpark(points, lo, hi, len(pairs))`.

    Update the three call sites:
    - proposals.py:112 (row sparkline): `sparklines[str(fid)] = _bpm_spark(windows, total_sec, SPARK_W, SPARK_H).points`
      (dict stays `dict[str, str]`; the row sparkline is unchanged — out of scope visually).
    - proposals.py timeline context (~301): build `spark = _bpm_spark(fine, total_sec, TIMELINE_W, TIMELINE_H)`
      then pass `"bpm_points": spark.points, "bpm_lo": spark.lo, "bpm_hi": spark.hi` in the context dict.
    - record.py context (~120): same — build `spark = _bpm_spark(...)`, pass the same three keys.
      Update the record.py:33 import to `_bpm_spark` (and re-export/keep `TIMELINE_*`, `_ribbons`).

    mypy will enforce the new return type across all call sites — resolve every `.points`/`.lo`/`.hi`
    access. No behavior change to the sparkline dict, ribbons, badge, or the points geometry.
  </action>
  <verify>
    <automated>uv run pytest tests/review/routers/test_proposals.py -x -q && uv run mypy src/phaze/routers/proposals.py src/phaze/routers/record.py</automated>
  </verify>
  <done>`_bpm_spark` returns `BpmSpark(points, lo, hi, count)`; all 3 call sites updated; both
  context builders pass `bpm_points`/`bpm_lo`/`bpm_hi`; new unit tests (normal/flat/empty) pass;
  mypy clean; the `points` geometry string is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Render max/min BPM labels alongside the chart + render assertions on both surfaces</name>
  <files>src/phaze/templates/proposals/partials/analysis_timeline.html, tests/review/routers/test_proposals.py, tests/shared/core/test_record_palette_agents.py</files>
  <action>
    Edit ONLY the BPM block (lines 24-33) of the shared partial `analysis_timeline.html`; leave
    the Key/Mood/Style lanes and the reveal script untouched. Inside the existing
    `{% if bpm_points %}` branch, wrap the `<svg>` in a horizontal flex row with a small numeric
    gutter column so labels sit OUTSIDE the `preserveAspectRatio="none"` SVG (text inside that SVG
    would be distorted). Render the gutter as HTML text (autoescaped, numeric):
    - When `bpm_lo` and `bpm_hi` differ: a narrow left (or right) column, `flex flex-col
      justify-between`, matching the chart's `h-16`, showing `{{ bpm_hi | round | int }}` at the
      top and `{{ bpm_lo | round | int }}` at the bottom. Optionally a faint mid value
      `{{ ((bpm_lo + bpm_hi) / 2) | round | int }}` centered (nice-to-have; skip if it complicates layout).
    - When `bpm_lo == bpm_hi` (flat line): render a SINGLE centered value `{{ bpm_hi | round | int }}`
      (no bogus min≠max range).
    Style the labels with the same Tailwind tokens already used by the sibling `<h4>` /
    empty-state text (`text-xs text-gray-400 dark:text-gray-500`, tabular/monospace optional) so
    dark-mode stays consistent. Keep the SVG's `viewBox`/`preserveAspectRatio`/`polyline` exactly
    as-is (numeric geometry unchanged — hardening invariant). The `{% else %}` "No BPM data."
    branch is unchanged. Add a screen-reader-friendly unit suffix in a title/aria on the gutter
    (e.g. aria-label "BPM range {{ bpm_lo|round|int }} to {{ bpm_hi|round|int }}") — optional but
    consistent with the existing `aria-label="BPM over time"`.

    Because `record_body.html` includes this partial verbatim, this single edit covers BOTH
    surfaces.

    Render assertions:
    - tests/review/routers/test_proposals.py (bucket "review"): extend the existing
      `test_timeline_with_windows` (or add a focused sibling) to GET `/proposals/{id}/timeline`
      and assert the rendered text contains the max ("128") and min ("120") integer labels from
      the seeded fine windows (bpm 120.0/128.0 already seeded at lines 29-30). Keep the existing
      `<polyline>` assertion.
    - tests/shared/core/test_record_palette_agents.py (bucket "shared"): using the existing
      `seed_file_with_windows` fixture + `GET /record/{file.id}` (HX-Request), assert the same
      numeric labels render — this proves record.py's context builder passes `bpm_lo`/`bpm_hi`.
      (If `seed_file_with_windows` does not seed fine-tier bpm windows, seed a file with two
      fine windows bpm 120/128 inline instead, mirroring test_proposals.py's `add_analysis_windows`.)
  </action>
  <verify>
    <automated>uv run pytest tests/review/routers/test_proposals.py tests/shared/core/test_record_palette_agents.py -q && uv run ruff check . && uv run ruff format --check . && uv run mypy .</automated>
  </verify>
  <done>The proposals timeline and the record view both render numeric max (top) and min
  (bottom) BPM labels as HTML outside the stretched SVG; flat-line shows a single value; empty
  state still shows "No BPM data."; ribbons/badge/Deepen untouched; both render tests pass;
  ruff + mypy clean.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/review/routers/test_proposals.py tests/shared/core/test_record_palette_agents.py` green
  (or run the affected buckets: `just test-bucket review` and `just test-bucket shared`).
- `uv run ruff check .`, `uv run ruff format --check .`, `uv run mypy .` all clean.
- Maintain the 90% coverage gate (helper unit test + both render tests cover the new branches).
- `pre-commit run --all-files` passes; commit WITHOUT `--no-verify`.
- Manual sanity: the "BPM (fine windows)" chart shows a max value at the top and a min value at
  the bottom on both the proposals timeline expand and the record slide-in.
</verification>

<success_criteria>
1. `_bpm_spark` returns `BpmSpark(points, lo, hi, count)`; the `points` geometry is unchanged and
   still the only essentia-derived data reaching an SVG attribute (hardening preserved).
2. The shared `analysis_timeline.html` partial renders numeric max/min BPM labels as HTML gutter
   text (not `<text>` inside the `preserveAspectRatio="none"` SVG); flat-line → single value;
   empty → unchanged "No BPM data."
3. Both surfaces (proposals timeline + record view) show the scale via the single partial edit +
   both context builders passing lo/hi.
4. All three call sites of the renamed helper compile under mypy; row sparkline visually unchanged.
5. Key/Mood/Style ribbons, Deepen button, and sampled badge untouched.
6. Tests (unit: normal/flat/empty; render: both surfaces) pass; ruff/mypy/pre-commit clean; 90%
   coverage held; no `--no-verify`.
</success_criteria>

<output>
Update `.planning/STATE.md` Quick Tasks Completed table with the 260707-c9o row when done.
</output>
