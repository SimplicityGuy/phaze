---
phase: quick-260707-c3a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/templates/shell/partials/rail.html
  - tests/shared/core/test_rail_narrow_width.py
  - tests/shared/core/test_shell_routes.py
autonomous: true
requirements: [QUICK-260707-c3a]
must_haves:
  truths:
    - "The pipeline-rail header shows the 'Pipeline' eyebrow with NO adjacent '+ Scan' button."
    - "The Discover rail node (hx-get=/s/discover) still renders and still works."
    - "Full shell/rail test suite passes with the button removed (counts updated in lockstep)."
    - "Pre-commit hooks pass without --no-verify."
  artifacts:
    - path: "src/phaze/templates/shell/partials/rail.html"
      provides: "Pipeline rail with the redundant header Scan button removed, eyebrow kept"
      contains: ">Pipeline<"
  key_links:
    - from: "src/phaze/templates/shell/partials/rail.html"
      to: "/s/discover"
      via: "Discover rail node (data-rail-stage=discover) — the surviving, canonical entry"
      pattern: "data-rail-stage=\"discover\""
status: complete
---

<objective>
Remove the redundant pipeline-header "+ Scan" button from the DAG rail. The button only
issued `hx-get="/s/discover"` — byte-identical to the Discover rail node directly below it,
and NOT an actual scan trigger (the real Scan control lives on the Discover screen via
`trigger_scan_card.html` → `POST /pipeline/scans`). So it is pure duplication.

Purpose: Cut a confusing, duplicate CTA from the navigation spine. Keep the "Pipeline" eyebrow.
Output: Trimmed `rail.html` + two test files updated to reflect one-fewer navigable node.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@src/phaze/templates/shell/partials/rail.html
@tests/shared/core/test_rail_narrow_width.py
@tests/shared/core/test_shell_routes.py

<notes>
- Tailwind template: `src/phaze/static/css/app.css` is GITIGNORED and rebuilt at image
  build / via `just tailwind`. Removing a template element needs NO CSS rebuild step.
- The rail has 15 navigable nodes today: 12 `/s/<id>` stage buttons + 2 below-line links
  (audit, agents) + the header "+ Scan" CTA. After removal → 14. Two structural guards
  in `test_rail_narrow_width.py` hard-code `>= 15` and MUST drop to `>= 14`.
- `test_shell_routes.py:112` asserts `body.count('hx-target="#stage-workspace"') >= 12`
  (the 12 stages). Removing the CTA leaves exactly 12 → assertion STILL passes; only the
  trailing "-- the +Scan CTA adds one more" comment goes stale. Fix the comment.
- Nothing else references the button: no id, no JS handler. `/s/discover` stays in use by
  the surviving Discover node. `docs/api.md` "Scan" mentions are the unrelated pipeline
  Scan/Search nodes (`/pipeline/scans`, `/pipeline/scan-live-sets`) — do NOT touch.
- The design-asset HTML under `docs/superpowers/specs/2026-06-28-ui-redesign-assets/` are
  static prototypes — do NOT touch.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the redundant "+ Scan" CTA from rail.html, keep the Pipeline eyebrow</name>
  <files>src/phaze/templates/shell/partials/rail.html</files>
  <action>
In the eyebrow wrapper `<div>` (currently ~lines 46-56), delete ONLY the entire
`<button type="button" ... title="Scan" ...>...</button>` element (its `hx-get="/s/discover"`
opening tag, the inline `<svg>` plus-glyph, and the `<span class="max-lg:sr-only">Scan</span>`,
through its closing `</button>`).

KEEP the `<span ...>Pipeline</span>` eyebrow verbatim.

Update the preceding Jinja comment (currently `{# Eyebrow + primary "+ Scan" CTA (opens the
Discover/scan surface). #}`) to describe just the eyebrow, e.g.
`{# Pipeline eyebrow (section label for the DAG rail). #}` — no dangling reference to a Scan CTA.

The wrapper `<div>` now holds a single child (the eyebrow). `justify-between` with one child
still left-aligns correctly; you MAY simplify the flex classes for clarity (e.g. drop
`justify-between`), but do not change padding/`max-lg:` collapse behavior or the eyebrow's own
classes. Keep the edit self-contained to this div — leave the `<nav>` and all rail nodes untouched.
  </action>
  <verify>
    <automated>cd "$PROJECT_ROOT" 2>/dev/null; rg -n 'title="Scan"|>Scan<' src/phaze/templates/shell/partials/rail.html; test $? -ne 0 && echo "OK: no Scan button" || (echo "FAIL: Scan button still present" && exit 1); rg -q '>Pipeline<' src/phaze/templates/shell/partials/rail.html && echo "OK: eyebrow kept"; rg -q 'data-rail-stage="discover"' src/phaze/templates/shell/partials/rail.html && echo "OK: Discover node intact"</automated>
  </verify>
  <done>rail.html contains the "Pipeline" eyebrow, NO "+ Scan" button, an updated comment, and the Discover rail node still intact.</done>
</task>

<task type="auto">
  <name>Task 2: Update structural guards to the new 14-node count and run tests + pre-commit</name>
  <files>tests/shared/core/test_rail_narrow_width.py, tests/shared/core/test_shell_routes.py</files>
  <action>
In `tests/shared/core/test_rail_narrow_width.py`:
- `test_glyphs_present`: change the glyph-count assertion from `>= 15` to `>= 14`
  (the removed CTA carried one inline-SVG glyph). Update its docstring/message to match.
- `test_titles_present`: change the navigable-node assertion from `>= 15` to `>= 14`
  and update its message.
- Update the module docstring and helper docstrings that enumerate nodes as
  "12 stage buttons + the 2 below-line links + the '+ Scan' CTA" (module docstring ~line 17-18,
  `_navigable_node_tags` ~line 49-50, `_label_span_attrs` ~line 62) to drop the "+ Scan" CTA
  and reflect 14 navigable nodes (12 stage buttons + 2 below-line links).
- Leave `test_labels_sr_only_not_hidden` (`>= 14`) and `test_focus_and_current_preserved`
  (`>= 12` stage buttons) AS-IS — both still hold after removal (14 labels remain; the removed
  CTA was not a `data-rail-stage` button). Do NOT weaken these bounds.

In `tests/shared/core/test_shell_routes.py`:
- Around line 112, update the stale comment `-- the +Scan CTA adds one more.` to reflect that
  the count is now exactly the 12 stage nodes. The assertion `>= len(_RAIL_STAGES)` is
  unchanged and still passes.

Then run the affected tests and pre-commit.
  </action>
  <verify>
    <automated>cd "$PROJECT_ROOT"; uv run pytest tests/shared/core/test_rail_narrow_width.py tests/shared/core/test_shell_routes.py -q && uv run pytest tests/shared/core/test_a11y_guards.py -q && pre-commit run --files src/phaze/templates/shell/partials/rail.html tests/shared/core/test_rail_narrow_width.py tests/shared/core/test_shell_routes.py</automated>
  </verify>
  <done>Both updated test files pass; a11y guard passes; pre-commit is green on all three changed files with no --no-verify.</done>
</task>

</tasks>

<verification>
- `rg 'title="Scan"|>Scan<' src/phaze/templates/shell/partials/rail.html` returns nothing.
- `rg '>Pipeline<' src/phaze/templates/shell/partials/rail.html` returns the eyebrow.
- `rg 'data-rail-stage="discover"' src/phaze/templates/shell/partials/rail.html` still matches.
- `uv run pytest tests/shared/core/test_rail_narrow_width.py tests/shared/core/test_shell_routes.py tests/shared/core/test_a11y_guards.py -q` all pass.
- `pre-commit run --files <changed files>` passes (no --no-verify).
</verification>

<success_criteria>
The pipeline rail renders the "Pipeline" eyebrow with no adjacent duplicate Scan button; the
Discover node remains the single canonical `/s/discover` entry; the rail structural guards and
shell-route tests pass with counts updated to 14 navigable nodes; pre-commit is green.
</success_criteria>

<output>
Create `.planning/quick/260707-c3a-remove-redundant-pipeline-header-scan-bu/260707-c3a-SUMMARY.md` when done.
</output>
