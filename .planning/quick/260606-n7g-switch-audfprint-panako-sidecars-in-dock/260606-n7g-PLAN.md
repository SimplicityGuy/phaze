---
phase: quick-260606-n7g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.agent.yml
  - docs/deployment.md
  - tests/test_deployment/test_agent_compose.py
autonomous: true
requirements: [QUICK-260606-n7g]
must_haves:
  truths:
    - "audfprint and panako services pull from GHCR (image:), not build from local source"
    - "The original build: block survives as a commented-out dev-only fallback on both sidecars"
    - "Operators no longer need the full phaze source on the file server to run the sidecars"
    - "docs/deployment.md describes all four agent services pulling from GHCR with PHAZE_IMAGE_TAG"
    - "A test catches any regression back to build: on the sidecars"
  artifacts:
    - path: "docker-compose.agent.yml"
      provides: "audfprint/panako using image: ghcr.io/simplicityguy/phaze/<name>:${PHAZE_IMAGE_TAG:-latest}"
      contains: "ghcr.io/simplicityguy/phaze/audfprint"
    - path: "docs/deployment.md"
      provides: "Updated deployment tables reflecting GHCR-pulled sidecars"
    - path: "tests/test_deployment/test_agent_compose.py"
      provides: "Guard asserting all four agent services reference a GHCR image with PHAZE_IMAGE_TAG"
  key_links:
    - from: "docker-compose.agent.yml"
      to: "ghcr.io/simplicityguy/phaze/audfprint, /panako"
      via: "image: directive with ${PHAZE_IMAGE_TAG:-latest}"
      pattern: "ghcr\\.io/simplicityguy/phaze/(audfprint|panako):\\$\\{PHAZE_IMAGE_TAG"
---

<objective>
Switch the `audfprint` and `panako` fingerprint sidecars in `docker-compose.agent.yml` from local `build:` to pulling published GHCR images, mirroring how `worker`/`watcher` already pull `ghcr.io/simplicityguy/phaze:${PHAZE_IMAGE_TAG:-latest}`. Reconcile the stale "not on GHCR / built locally" claims in the compose header, `docs/deployment.md`, and the test docstrings/assertions.

Purpose: `docker-publish.yml` already builds AND pushes the sidecars (matrix `image_suffix: /audfprint`, `/panako`), so `ghcr.io/simplicityguy/phaze/audfprint:latest` + `.../panako:latest` are pullable today. Forcing local builds means every file-server host needs the full phaze source context for two sidecars — unnecessary friction.

Output: Updated compose file (GHCR images + commented dev fallback), corrected docs, and a regression guard test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@docker-compose.agent.yml
@docs/deployment.md
@tests/test_deployment/test_agent_compose.py

<facts>
Confirmed GHCR image paths (from .github/workflows/docker-publish.yml matrix):
- api/worker/watcher → ghcr.io/simplicityguy/phaze        (bare repo, image_suffix "")
- audfprint          → ghcr.io/simplicityguy/phaze/audfprint  (image_suffix "/audfprint")
- panako             → ghcr.io/simplicityguy/phaze/panako     (image_suffix "/panako")

Existing worker/watcher reference (the pattern to mirror):
  image: ghcr.io/simplicityguy/phaze:${PHAZE_IMAGE_TAG:-latest}

Current sidecar shape (audfprint shown; panako identical with panako_data + Dockerfile.panako):
  audfprint:
    build:
      context: .
      dockerfile: services/audfprint/Dockerfile.audfprint
    volumes:
      - "${SCAN_PATH:?SCAN_PATH required}:/data/music:ro"
      - audfprint_data:/data/fprint
    restart: unless-stopped

INVARIANTS that must stay green (tests/test_deployment/test_agent_compose.py):
- services set is exactly {worker, watcher, audfprint, panako}
- no DATABASE_URL / POSTGRES_* / depends_on: postgres on any agent service
- worker has PHAZE_ROLE=agent
- every SCAN_PATH mount keeps the fail-fast ${SCAN_PATH:?...} form on ALL services
- the sidecar Dockerfiles + docker-validate.yml hadolint matrix STAY (CI still builds+publishes)
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch sidecars to GHCR images in docker-compose.agent.yml</name>
  <files>docker-compose.agent.yml</files>
  <action>
For BOTH the `audfprint` and `panako` services, replace the `build:` block with an `image:` directive that pulls from GHCR, mirroring the worker/watcher style exactly:
- audfprint → `image: ghcr.io/simplicityguy/phaze/audfprint:${PHAZE_IMAGE_TAG:-latest}`
- panako    → `image: ghcr.io/simplicityguy/phaze/panako:${PHAZE_IMAGE_TAG:-latest}`

KEEP the original `build:` block but COMMENTED OUT directly beneath the new `image:` line, labeled as a local-dev override so developers can still build from source. Use a comment like:
  `# Local-dev override — build from source instead of pulling GHCR:`
followed by the commented `build:` / `context:` / `dockerfile:` lines (2-space indent preserved under the comment hash so re-enabling is a clean uncomment).

PRESERVE on both services unchanged: the `volumes:` block (the `${SCAN_PATH:?SCAN_PATH required}:/data/music:ro` fail-fast mount AND the `audfprint_data:`/`panako_data:` named-volume mount) and `restart: unless-stopped`. Do NOT touch worker, watcher, the top-level `volumes:` section, or the services set.

Update the file header comment block:
- Lines ~11-12: change the audfprint/panako descriptions from "fingerprint sidecar (build context, not on GHCR per D-15)" to e.g. "fingerprint sidecar (PHAZE_ROLE n/a). Pulls from GHCR (audfprint/panako image)."
- Lines ~8-12 / the D-15/D-16 notes: state that ALL FOUR agent images pull from GHCR and the sidecar `build:` is a commented dev-only fallback. Remove the stale "build context, not on GHCR" framing. Keep the D-16 image-tag-pinning note intact.

Match existing yamllint-strict formatting: leading `---`, 2-space indent, double-quote style on volume strings as already present, bare `image:` value (worker/watcher are unquoted — match that).
  </action>
  <verify>
    <automated>docker compose -f docker-compose.agent.yml --env-file <(printf 'SCAN_PATH=/x\nPHAZE_IMAGE_TAG=latest\n') config --quiet && grep -Eq 'ghcr\.io/simplicityguy/phaze/audfprint:\$\{PHAZE_IMAGE_TAG:-latest\}' docker-compose.agent.yml && grep -Eq 'ghcr\.io/simplicityguy/phaze/panako:\$\{PHAZE_IMAGE_TAG:-latest\}' docker-compose.agent.yml</automated>
  </verify>
  <done>Both sidecars use `image:` pulling the correct GHCR path with `${PHAZE_IMAGE_TAG:-latest}`; the commented dev-only `build:` fallback is present beneath each; volumes/restart preserved; header comment no longer claims "not on GHCR"; `docker compose config --quiet` parses cleanly with placeholder env.</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/deployment.md tables and prose</name>
  <files>docs/deployment.md</files>
  <action>
Update the stale claims so they reflect the GHCR-pulled sidecars. Keep the line-1 `<!-- generated-by: gsd-doc-writer -->` marker.

1. "Deployment Targets" table (the `docker-compose.agent.yml` row, ~line 18): change the Notes cell so it no longer says "sidecars build locally". State that all four services (`worker`/`watcher`/`audfprint`/`panako`) pull from GHCR via `PHAZE_IMAGE_TAG`.

2. "File-server services" table (~lines 36-39): update the `audfprint` and `panako` rows' "Image / build" + last column:
   - `audfprint` Image/build → `ghcr.io/simplicityguy/phaze/audfprint:${PHAZE_IMAGE_TAG:-latest}`
   - `panako` Image/build    → `ghcr.io/simplicityguy/phaze/panako:${PHAZE_IMAGE_TAG:-latest}`
   - Replace "Not on GHCR — built on the file-server host" with "Fingerprint sidecar. Pulls from GHCR." (or equivalent — same framing as worker/watcher rows).

3. Scan the rest of the doc for prose implying operators need the full source on the file server for the sidecars and correct it. NOTE: Step 4 (~line 161) currently `git clone`s on the file-server host. Adjust the surrounding wording so it's clear the clone is only for the compose file + `.env` template (and optional dev source builds), not a hard requirement to build the sidecars — keep the `git clone` step (operators still need the compose file) but drop any "needed to build the sidecars" implication. Leave the Build Pipeline section's description of `docker-publish.yml` publishing the sidecars under `/audfprint` and `/panako` intact (it is already correct). Leave `docker-validate.yml`'s description of building+linting the sidecar Dockerfiles intact (CI still does this).
  </action>
  <verify>
    <automated>grep -q 'ghcr.io/simplicityguy/phaze/audfprint' docs/deployment.md && grep -q 'ghcr.io/simplicityguy/phaze/panako' docs/deployment.md && ! grep -q 'Not on GHCR' docs/deployment.md && head -1 docs/deployment.md | grep -q 'generated-by: gsd-doc-writer'</automated>
  </verify>
  <done>Both deployment tables reference the GHCR sidecar images with `PHAZE_IMAGE_TAG`; no "Not on GHCR" / "built on the file-server host" claims remain for the sidecars; prose no longer implies operators must build sidecars from source; line-1 doc marker intact; Build Pipeline + docker-validate descriptions unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile test_agent_compose.py and add GHCR regression guard</name>
  <files>tests/test_deployment/test_agent_compose.py</files>
  <action>
Reconcile the test file with the change and add a regression guard. No existing test asserts the sidecars use `build:` (the suite covers: services set, no-postgres, worker PHAZE_ROLE, fail-fast SCAN_PATH, publish-workflow tags) — so the services-set test and all four invariant tests stay intact and must remain green.

1. Update the module docstring (lines 1-25) wording that frames audfprint/panako as build-context/"not on GHCR per D-15" so it instead notes all four agent services pull from GHCR. Do not weaken the documented invariants.

2. ADD a new test, e.g. `test_all_agent_services_pull_from_ghcr`, that loads the compose via the existing `_load_agent_compose()` helper and asserts EACH of the four services declares an `image:` value matching `ghcr.io/simplicityguy/phaze` AND containing `PHAZE_IMAGE_TAG`. Assert specifically that `audfprint`'s image contains `phaze/audfprint` and `panako`'s contains `phaze/panako` (parallel to worker/watcher pulling the bare `phaze` repo). This catches a regression back to `build:` (a service with only `build:` and no `image:` fails the guard). Use `yaml.safe_load` semantics already in the file — the raw `${PHAZE_IMAGE_TAG:-latest}` token is visible since safe_load does not interpolate (consistent with the existing SCAN_PATH test).

Match the file's existing style: type hints on the test function (`-> None`), 150-char line length, double quotes, descriptive assertion messages.
  </action>
  <verify>
    <automated>uv run pytest tests/test_deployment/test_agent_compose.py -q && uv run ruff check tests/test_deployment/test_agent_compose.py && uv run mypy tests/test_deployment/test_agent_compose.py</automated>
  </verify>
  <done>New `test_all_agent_services_pull_from_ghcr` (or equivalent) asserts all four services use a `ghcr.io/simplicityguy/phaze...` image with `PHAZE_IMAGE_TAG`, with sidecars on the `/audfprint` and `/panako` sub-paths; docstring no longer claims sidecars are "not on GHCR"; all pre-existing tests (services set, no-postgres, PHAZE_ROLE, fail-fast SCAN_PATH, publish tags) still pass; ruff + mypy clean.</done>
</task>

</tasks>

<verification>
Full gate before commit:
- `docker compose -f docker-compose.agent.yml --env-file <(printf 'SCAN_PATH=/x\nPHAZE_IMAGE_TAG=latest\n') config --quiet` parses
- `uv run pytest tests/test_deployment/test_agent_compose.py -q` all green
- `pre-commit run --all-files` passes (yamllint strict, check-jsonschema, ruff, mypy, actionlint) — NEVER use --no-verify
</verification>

<success_criteria>
- audfprint/panako pull `ghcr.io/simplicityguy/phaze/{audfprint,panako}:${PHAZE_IMAGE_TAG:-latest}`; commented dev-only `build:` fallback retained on both
- services set unchanged {worker, watcher, audfprint, panako}; SCAN_PATH fail-fast + named volumes + restart preserved; no DATABASE_URL/postgres added
- compose header, docs/deployment.md tables/prose, and test docstring all consistent: all four agent images come from GHCR
- regression guard test asserts the GHCR image refs; full suite + pre-commit green
- sidecar Dockerfiles and docker-validate.yml untouched (CI still builds+publishes)
</success_criteria>

<output>
Create `.planning/quick/260606-n7g-switch-audfprint-panako-sidecars-in-dock/260606-n7g-SUMMARY.md` when done
</output>
