---
quick_id: 260714-hb3
status: complete
---

# Quick Task 260714-hb3: Bump version to 2026.7.6

## Task

Bump the project version 2026.7.5 → 2026.7.6 for the 2026.7.6 CalVer patch release. This patch carries the merged cloud-analysis empty-results fix (#245) so a new `ghcr.io/simplicityguy/phaze` + `phaze/job` image can be published (the annotated `2026.7.6` tag push triggers `ci.yml` → `docker-publish.yml`).

## Tasks

1. `pyproject.toml` — set `version = "2026.7.6"`.
   - verify: `uv run python -c "...['project']['version']"` prints `2026.7.6`.
   - done: version line bumped.
2. `uv.lock` — `uv lock`; the ONLY change must be the phaze package's own version line (no dependency re-resolution).
   - verify: `git diff uv.lock` shows a single `-version = "2026.7.5"` / `+version = "2026.7.6"`.
   - done: lock updated.

No git tag here — the annotated `2026.7.6` tag is pushed after this merges to `main`.
