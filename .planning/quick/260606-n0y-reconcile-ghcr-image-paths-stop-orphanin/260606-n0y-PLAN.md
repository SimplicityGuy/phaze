---
phase: quick-260606-n0y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/cleanup-images.yml
  - docs/deployment.md
  - tests/test_deployment/test_agent_compose.py
autonomous: true
requirements: [GHCR-RECONCILE]
must_haves:
  truths:
    - "cleanup-images.yml prunes the canonical bare `phaze` package, not the orphan `phaze/api`"
    - "cleanup-images.yml matrix exactly matches the set of packages docker-publish.yml publishes"
    - "docs/deployment.md states which GHCR image path is authoritative and which is deprecated"
    - "A guard test fails if the cleanup package list diverges from the published image-suffix set"
  artifacts:
    - path: ".github/workflows/cleanup-images.yml"
      provides: "Cleanup matrix targeting {phaze, phaze/audfprint, phaze/panako}"
      contains: "- phaze"
    - path: "docs/deployment.md"
      provides: "Authoritative image-path statement in Build Pipeline section"
    - path: "tests/test_deployment/test_agent_compose.py"
      provides: "Guard test asserting publish/cleanup package-set parity"
  key_links:
    - from: "tests/test_deployment/test_agent_compose.py"
      to: ".github/workflows/cleanup-images.yml"
      via: "yaml.safe_load matrix.package"
      pattern: "cleanup-images"
    - from: "tests/test_deployment/test_agent_compose.py"
      to: ".github/workflows/docker-publish.yml"
      via: "yaml.safe_load matrix.include image_suffix"
      pattern: "image_suffix"
---

<objective>
Reconcile GHCR image paths so the cleanup workflow prunes what is actually published.

The orphan `ghcr.io/simplicityguy/phaze/api` is a stale leftover from a pre-D-15 convention; docker-publish.yml already publishes the api/worker/watcher image to the bare repo `phaze` (via `image_suffix: ""`). The bug is that cleanup-images.yml still prunes the dead `phaze/api` path and never prunes the canonical bare `phaze` package, so its untagged/old versions accumulate forever.

Purpose: Make cleanup target the live packages, document the authoritative path, and add a guard test so the publish and cleanup lists cannot silently drift again.
Output: Updated cleanup workflow, deployment docs note, and a new parity guard test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
docker-publish.yml matrix (.github/workflows/docker-publish.yml, jobs.build-and-push.strategy.matrix.include):
- name: api,       image_suffix: ""          -> package `phaze`
- name: audfprint, image_suffix: "/audfprint" -> package `phaze/audfprint`
- name: panako,    image_suffix: "/panako"    -> package `phaze/panako`

Published-package derivation: package = "phaze" + image_suffix, with the bare-repo
empty suffix collapsing to "phaze" (the repo name, which is the ghcr-cleanup-action
`package:` value for the bare-repo image).

cleanup-images.yml matrix (CURRENT, buggy): package list = {phaze/api, phaze/audfprint, phaze/panako}.
Target list: {phaze, phaze/audfprint, phaze/panako}.

Existing test helpers in tests/test_deployment/test_agent_compose.py:
- PUBLISH_WORKFLOW_PATH = repo_root/.github/workflows/docker-publish.yml
- uses `import yaml` + `yaml.safe_load(path.read_text())`
- module-level path constants computed via Path(__file__).resolve().parents[2]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Point cleanup-images.yml at the published packages</name>
  <files>.github/workflows/cleanup-images.yml</files>
  <action>In the `jobs.cleanup.strategy.matrix.package` list, replace the orphan entry `- phaze/api` with `- phaze` (the canonical bare-repo package), keeping `- phaze/audfprint` and `- phaze/panako`. Preserve all existing `with:` values: `delete-partial-images: true`, `delete-untagged: true`, `keep-n-tagged: 2`, `older-than: 30 days`. These remain correct for the bare `phaze` package — `keep-n-tagged: 2` + `older-than: 30 days` prunes only old/untagged versions and does NOT aggressively delete released `:v<version>`/`:latest` tags. Match existing yamllint-strict formatting exactly: leading `---`, two-space indentation, the `🧹` emoji in the step name, and quoting style. Do not touch triggers, concurrency, permissions, or the action SHA pin.</action>
  <verify>
    <automated>uv run python -c "import yaml; d=yaml.safe_load(open('.github/workflows/cleanup-images.yml')); pkgs=set(d['jobs']['cleanup']['strategy']['matrix']['package']); assert pkgs=={'phaze','phaze/audfprint','phaze/panako'}, pkgs; print('ok')"</automated>
  </verify>
  <done>Cleanup matrix package list is exactly {phaze, phaze/audfprint, phaze/panako}; `phaze/api` is gone; action config values unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Document the authoritative image path in deployment.md</name>
  <files>docs/deployment.md</files>
  <action>In the `### docker-publish.yml (build + push to GHCR)` subsection of the `## Build Pipeline` section (currently around line 257-266), add a short, explicit "Authoritative image paths" statement (a sentence or compact bullet group) right after the existing bullet that explains the bare-repo api publish (the `The `api` image publishes to the bare repo URL...` bullet, line 262). State plainly: `ghcr.io/simplicityguy/phaze` is the authoritative api/worker/watcher image; `ghcr.io/simplicityguy/phaze/audfprint` and `ghcr.io/simplicityguy/phaze/panako` are the sidecar images; `ghcr.io/simplicityguy/phaze/api` is a deprecated/orphaned path that must NOT be pulled or referenced. Keep the existing line-1 `<!-- generated-by: gsd-doc-writer -->` marker intact and do not reorder other sections.</action>
  <verify>
    <automated>grep -q "deprecated" docs/deployment.md && grep -q "phaze/api" docs/deployment.md && head -1 docs/deployment.md | grep -q "generated-by: gsd-doc-writer" && echo ok</automated>
  </verify>
  <done>Build Pipeline section names the authoritative bare-repo path, the two sidecar paths, and explicitly flags `phaze/api` as deprecated/do-not-use; gsd-doc-writer marker still on line 1.</done>
</task>

<task type="auto">
  <name>Task 3: Add publish/cleanup package-set parity guard test</name>
  <files>tests/test_deployment/test_agent_compose.py</files>
  <action>Add a module-level path constant `CLEANUP_WORKFLOW_PATH = Path(__file__).resolve().parents[2] / ".github" / "workflows" / "cleanup-images.yml"` alongside the existing `PUBLISH_WORKFLOW_PATH`. Add a new test `test_cleanup_package_list_matches_published_images` that: (1) loads docker-publish.yml via `yaml.safe_load`, reads `jobs.build-and-push.strategy.matrix.include`, and derives the published package set by computing `("phaze" + entry["image_suffix"]).rstrip("/")` for each entry — i.e. `{"phaze", "phaze/audfprint", "phaze/panako"}` from suffixes `{"", "/audfprint", "/panako"}`; (2) loads cleanup-images.yml and reads `jobs.cleanup.strategy.matrix.package` into a set; (3) asserts the two sets are equal with a message that names the symmetric difference so a future drift in either workflow is pinpointed. Reuse the existing `import yaml` and Path conventions; do not add new dependencies. Note the PyYAML `on:`→`True` boolean-key gotcha is not relevant here (reading `jobs`/`strategy`, not `on`).</action>
  <verify>
    <automated>uv run pytest tests/test_deployment/test_agent_compose.py::test_cleanup_package_list_matches_published_images -x -q</automated>
  </verify>
  <done>New parity test passes against the fixed workflows and would fail (with a symmetric-difference message) if either the publish image_suffix set or the cleanup package set changes without the other.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_deployment/test_agent_compose.py -q` — all tests pass, including the new parity guard.
- `pre-commit run --all-files` — actionlint, yamllint, check-jsonschema, ruff, mypy all pass on the changed workflow/docs/test. NEVER use --no-verify.
</verification>

<success_criteria>
- cleanup-images.yml prunes {phaze, phaze/audfprint, phaze/panako}; the orphan `phaze/api` is no longer referenced.
- docs/deployment.md authoritatively documents the canonical vs deprecated image paths.
- A guard test enforces publish/cleanup package-set parity and is green.
- All frozen-SHA pre-commit hooks pass; tests pass via `uv run pytest`.
</success_criteria>

<output>
Create `.planning/quick/260606-n0y-reconcile-ghcr-image-paths-stop-orphanin/260606-n0y-SUMMARY.md` when done.

In the SUMMARY, record the OUT-OF-SCOPE maintainer follow-up (one-time ops action, NOT a repo change): deleting the stale `ghcr.io/simplicityguy/phaze/api` package from GHCR requires package-admin auth via the GitHub package UI or:
`gh api --method DELETE /user/packages/container/phaze%2Fapi`
(or the org-scoped `/orgs/simplicityguy/packages/container/phaze%2Fapi` variant). Note this cannot be done from the repo and must be performed manually by the maintainer.
</output>
