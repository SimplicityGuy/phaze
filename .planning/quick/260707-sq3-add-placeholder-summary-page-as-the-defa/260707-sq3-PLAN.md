---
phase: 260707-sq3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/templates/shell/partials/summary_placeholder.html
  - src/phaze/routers/shell.py
  - src/phaze/templates/shell/partials/rail.html
  - tests/shared/core/test_shell_routes.py
autonomous: true
requirements: [SQ3-01, SQ3-02, SQ3-03]
must_haves:
  truths:
    - "Visiting / renders the Summary placeholder inside the existing shell chrome, not the Analyze dashboard"
    - "Analyze stays reachable at /s/analyze and still renders its real workspace (#analyze-lanes)"
    - "GET /s/summary renders the same placeholder (full shell on direct nav, bare fragment on HX swap)"
    - "The rail shows a Summary node that is the active node (aria-current=page) when on /"
    - "The chrome /pipeline/stats poll on / finds a landing target for every OOB fragment it emits (no htmx:oobErrorNoTarget spam)"
  artifacts:
    - path: "src/phaze/templates/shell/partials/summary_placeholder.html"
      provides: "Static Summary placeholder fragment composed against the shared workspace scaffold"
      contains: "_workspace_scaffold.html"
    - path: "src/phaze/routers/shell.py"
      provides: "STAGE_PARTIALS['summary'] entry + shell_home repointed to the summary stage"
      contains: "\"summary\": \"shell/partials/summary_placeholder.html\""
    - path: "src/phaze/templates/shell/partials/rail.html"
      provides: "Summary rail node (landing entry, top of rail)"
      contains: "data-rail-stage=\"summary\""
    - path: "tests/shared/core/test_shell_routes.py"
      provides: "Root-renders-Summary + Analyze-still-reachable behavioral coverage"
  key_links:
    - from: "src/phaze/routers/shell.py::shell_home"
      to: "_render_stage(request, \"summary\", session)"
      via: "direct call"
      pattern: "_render_stage\\(request, \"summary\""
    - from: "src/phaze/routers/shell.py::STAGE_PARTIALS"
      to: "shell/partials/summary_placeholder.html"
      via: "static string literal (dead-template guard entry root)"
      pattern: "shell/partials/summary_placeholder\\.html"
    - from: "src/phaze/templates/shell/partials/summary_placeholder.html"
      to: "pipeline/partials/_workspace_poll_seeds.html"
      via: "the _workspace_scaffold.html macro (transitively includes the OOB seed host)"
      pattern: "_workspace_scaffold\\.html"
    - from: "src/phaze/templates/shell/partials/rail.html summary node"
      to: "/s/summary"
      via: "hx-get + hx-target=#stage-workspace"
      pattern: "hx-get=\"/s/summary\""
---

<objective>
Repoint the app's default landing route (`GET /`) from the Analyze stage to a NEW placeholder
"Summary" stage, add a Summary rail node, and keep Analyze fully reachable at `/s/analyze`.

Purpose: reserve the landing slot for a future at-a-glance overview. This change is SCAFFOLDING
ONLY — the Summary page's real content is specced in a future release.

Output: one new static shell partial, a `"summary"` key in `STAGE_PARTIALS`, a repointed
`shell_home`, a new rail node, and reconciled/extended shell route tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@src/phaze/routers/shell.py
@src/phaze/templates/shell/partials/rail.html
@src/phaze/templates/pipeline/partials/_workspace_scaffold.html
@src/phaze/templates/pipeline/partials/metadata_workspace.html
@tests/shared/core/test_shell_routes.py
</context>

<interfaces>
<!-- Extracted from the codebase during planning. Use these directly — no exploration needed. -->

**`src/phaze/routers/shell.py` (verified line numbers)**
- `STAGE_PARTIALS: dict[str, str]` at line 70; 12 keys today, closes at line 118.
  Every value is a STATIC string literal — `stage` is NEVER spliced into a template path
  (threat T-57-01). These literals are ALSO the dead-template guard's entry roots.
- `_render_stage(request, stage, session)` at line 145. Builds a base context of
  `request` / `stage` / `stage_partial` (= `STAGE_PARTIALS[stage]`) / `oob_counts=False` /
  `force_local`, then an `if stage == "analyze": ... elif stage == "discover": ...` chain of
  per-stage DB context. **A stage with no branch gets the base context only — exactly what the
  static Summary placeholder needs (zero DB reads, zero new context keys).**
  Fragment fork at lines 277-279: `HX-Request: true` → `shell/_stage_fragment.html`,
  otherwise → `shell/shell.html`.
- `shell_home` at lines 282-285 — currently `return await _render_stage(request, "analyze", session)`.
- `shell_stage` at lines 288-297 — whitelists `stage` against `STAGE_PARTIALS`, else 404.
  Once `"summary"` is a key, `GET /s/summary` works with **no route change**.

**`src/phaze/templates/shell/shell.html`**
- line 165: `<div id="stage-workspace" data-stage="{{ stage }}" ...>{% include stage_partial %}</div>`
- line 197: `hx-get="/pipeline/stats"` — the ONE persistent 5s chrome poll. It runs on EVERY
  stage, including Summary.

**`src/phaze/templates/pipeline/partials/_workspace_scaffold.html`** — the macro every stage
workspace composes against:
- `{% macro workspace(title, subcount='', actions='', x_data='', cloud_cards=false) %}`
- Emits EXACTLY ONE `<h1 tabindex="-1">{{ title }}</h1>` (the shell's post-swap focus target).
- Uses a `<div>`, never `<header>` — the bare-fragment guard asserts the substring `"<head"`
  is absent from a stage fragment.
- Line 40: always `{% include "pipeline/partials/_workspace_poll_seeds.html" %}` with
  `workspace_has_cloud_cards = cloud_cards`. Default `cloud_cards=false` → hidden cloud-card
  sinks are emitted.
- Consumer idiom (`metadata_workspace.html`): `{% import "pipeline/partials/_workspace_scaffold.html" as ws %}`
  then `{% call ws.workspace(title="METADATA", ...) %}` ... `{% endcall %}`.

**Rail node markup contract (`rail.html`)** — a top-level node (Discover, lines 54-63) is a
`<button type="button">` carrying, in order:
`data-rail-stage="<id>"`, `hx-get="/s/<id>" hx-target="#stage-workspace" hx-swap="innerHTML" hx-push-url="true"`,
`title="<Label>"`, `{% if stage == '<id>' %}aria-current="page"{% endif %}`, and the class string
`rail-node w-[calc(100%-1rem)] mx-2 rounded-lg px-3 py-2.5 flex items-center gap-3 text-left transition-colors hover:bg-gray-100 dark:hover:bg-white/5 focus:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 aria-[current=page]:bg-blue-500/10 aria-[current=page]:shadow-[inset_3px_0_0_var(--color-blue-500)] max-lg:justify-center max-lg:px-0`.
Its children: an inline SVG glyph, then a label
`<span class="flex-1 text-sm text-gray-700 dark:text-gray-200 max-lg:sr-only">Label</span>`,
then OPTIONALLY a count `<span class="font-mono text-xs text-gray-500 max-lg:hidden" x-text="$store.pipeline.<key>">0</span>`.

Glyph wrapper contract (asserted by `test_rail_narrow_width.py::test_glyphs_present`):
`<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="w-5 h-5 shrink-0" aria-hidden="true">`
</interfaces>

<pre_flight_findings>
Planning-time investigation established the following. Treat them as verified constraints.

1. **The chrome poll runs on Summary too.** `hx-get="/pipeline/stats"` lives in `shell.html`
   (chrome), NOT in the analyze partial. Every 5s `stats_bar.html` re-emits `hx-swap-oob`
   fragments (the `dag-seed-*` store seeds, the five legacy `*-files-ready` / `*-busy-seed`
   ids, `#straggler-failed-card`, and the six v6.0 cloud-state cards + `#analyze-lanes`).
   htmx OOB swaps land ONLY on ids ALREADY in the DOM. **If Summary omits the seed host,
   htmx logs `htmx:oobErrorNoTarget` every 5 seconds on the app's landing page.**

2. **This is also a hidden test breakage.**
   `tests/shared/core/test_enrich_analyze_workspaces.py::test_shell_sinks_legacy_oob_fragments`
   asserts `'id="straggler-failed-card"' in (await client.get("/")).text`. That id is supplied
   today by the analyze workspace's scaffold. Repointing `/` to a bare Summary partial makes
   this test FAIL. **Composing the shared `_workspace_scaffold.html` macro fixes both the
   runtime bug and the test in one move** — this is the single most important instruction in
   this plan. Do not hand-roll the placeholder markup.

3. **Structural rail guards are all `>=` thresholds, so the added node keeps them green
   automatically — provided it follows the node contract exactly.** No count reconciliation
   is needed:
   - `test_rail_narrow_width.py`: labels `>= 14`, glyphs `>= 14`, titles `>= 14`,
     stage buttons `>= 12`. (Today: 12 stage buttons + 2 below-line links = 14.)
   - `test_a11y_guards.py::test_rail_nodes_carry_aria_current_and_focus_visible` splits the
     whole file on `data-rail-stage` and requires EVERY chunk to have `aria-current="page"`
     and a `focus-visible:` class.
   The new node must therefore carry: a `title=`, a `w-5 h-5` / `viewBox="0 0 24 24"` /
   `stroke="currentColor"` / `aria-hidden="true"` glyph, a label span with `max-lg:sr-only`
   and NEVER `max-lg:hidden`, a `focus-visible:` class, and the `aria-current="page"` idiom.

4. **Two tests in `test_shell_routes.py` hard-code Analyze as the `/` default and WILL fail.**
   These are the only genuinely-breaking assertions, and Task 3 fixes them:
   - `test_root_renders_shell_analyze_default` — asserts `data-stage="analyze"` + `id="analyze-lanes"` on `GET /`.
   - `test_rail_nodes_wired` — asserts `re.search(r'data-rail-stage="analyze"[^>]*aria-current="page"', body)` on `GET /`.

5. **These chrome-level assertions on `GET /` stay green unchanged** (all satisfied by
   `shell.html` chrome or by the scaffold, once finding #2 is honored):
   `body.count('hx-get="/pipeline/stats"') == 1` (3 files), `"notYetEnriched: 0"` /
   `"computeOnline: 0"` (chrome, `shell.html:144`), `body.count("Alpine.store('pipeline'") == 1`,
   `'id="cmdk-trigger"'`, `'href="/admin/agents"'`.

6. **`test_workspaces_sink_cloud_card_oob_fragments` iterates only
   `("discover","metadata","fingerprint","analyze")`** — Summary is not in that tuple, so no
   change is required there. (The scaffold gives Summary exactly one of each sink id anyway.)

7. **Dead-template guard.** `tests/shared/core/test_dead_template_guard.py` derives entry roots
   from `"...html"` string literals in `src/phaze/routers/` and walks
   `extends`/`include`/`import`. Adding `"shell/partials/summary_placeholder.html"` to
   `STAGE_PARTIALS` makes the new partial an entry root; the guard then requires that the file
   actually exist. Task 1 satisfies both halves.

8. **Empty-state interaction.** `_render_stage` swaps in `empty_state.html` ONLY inside the
   `stage == "analyze"` branch when the file count is 0. Summary has no branch, so it is never
   affected. Conversely, any test asserting `#analyze-lanes` on `/s/analyze` MUST seed a file
   first (`await make_file()`), or the empty state renders instead.

9. **Legacy `/pipeline/` 302-redirects to `/`** (`pipeline.py:601`), so it now lands on Summary.
   This is acceptable for the placeholder. No code change.
</pre_flight_findings>

<tasks>

<task type="auto">
  <name>Task 1: Add the Summary placeholder partial and repoint GET / to it</name>
  <files>src/phaze/templates/shell/partials/summary_placeholder.html, src/phaze/routers/shell.py</files>
  <action>
Create `src/phaze/templates/shell/partials/summary_placeholder.html` as a content-only fragment
(NO `<html>`, NO `<head>`, NO `{% extends %}`).

It MUST compose the shared workspace scaffold rather than hand-rolling markup — this is what
supplies the hidden OOB seed-target host that the chrome `/pipeline/stats` poll requires on every
stage (see pre_flight_findings #1 and #2). Follow the `metadata_workspace.html` idiom exactly:
import `pipeline/partials/_workspace_scaffold.html` as `ws`, then `{% call ws.workspace(title="SUMMARY") %}`
with the placeholder body inside the call block, closed by `{% endcall %}`. Pass NO `subcount`,
NO `actions`, NO `x_data`, and leave `cloud_cards` at its `false` default so the hidden cloud-card
sinks are emitted.

The scaffold already renders the single `<h1 tabindex="-1">SUMMARY</h1>` focus target, so the body
must NOT contain another `<h1>`. Body content is a centered, muted placeholder in the existing
Tailwind/dark idiom (mirror the spacing and color tokens used by `empty_state.html`: a
`grid place-items-center p-6` wrapper, `text-sm text-gray-500 dark:text-gray-400 leading-relaxed`
copy). Put a stable, greppable test marker attribute `data-summary-placeholder` on the outermost
element of the body. Copy should say, in substance, that an at-a-glance overview of the collection
and pipeline is coming in a future release, and point the reader at the rail to jump into a stage.

SCOPE GUARD — the placeholder is deliberately inert: add NO widgets, NO metrics, NO counts, NO
`x-text` store bindings, NO `$store.pipeline` keys, NO `hx-get`/`hx-trigger`/`setInterval` (a second
poll loop would violate the single-poll discipline), and NO DB queries or new context keys.

Open the file with a Jinja comment `{# ... #}` in the house style, stating that this is a Phase-
placeholder whose real content is deferred to a future release, that it composes the scaffold so
the OOB seed host rides in, and that it intentionally has no dynamic data.

Then in `src/phaze/routers/shell.py`:
  1. Add a `"summary": "shell/partials/summary_placeholder.html"` entry to `STAGE_PARTIALS`
     (line 70). Place it FIRST in the dict so key order matches the new rail order. The value MUST
     be a static string literal (threat T-57-01, and it doubles as the dead-template guard entry
     root — pre_flight_findings #7). Precede it with a short comment in the surrounding style
     noting it is the `/` landing placeholder with no DB-backed context.
  2. Repoint `shell_home` (line ~283) to `return await _render_stage(request, "summary", session)`
     and update its docstring — it currently reads "Analyze default"; it must now say the shell root
     renders the Summary landing placeholder, and note that Analyze remains at `/s/analyze`.
  3. Add NO `elif stage == "summary"` branch to `_render_stage`. The base context is sufficient and
     deliberate: Summary performs zero DB reads.
  4. Update the module docstring (lines 13-17), which currently asserts "The Analyze node (the `/`
     default)". Correct it to state that `/` renders the Summary placeholder and Analyze is one rail
     click away at `/s/analyze`.

Leave the `analyze` key, its `_render_stage` branch, and the empty-state swap completely untouched.
  </action>
  <verify>
    <automated>uv run python -c "from phaze.routers.shell import STAGE_PARTIALS as S; assert S['summary'] == 'shell/partials/summary_placeholder.html', S; assert S['analyze'] == 'pipeline/partials/analyze_workspace.html', S; print('STAGE_PARTIALS ok', len(S), 'stages')" && uv run pytest tests/shared/core/test_dead_template_guard.py -q</automated>
  </verify>
  <done>
`STAGE_PARTIALS` has both `summary` and `analyze`; the new partial exists, imports
`_workspace_scaffold.html`, and contains `data-summary-placeholder`; `shell_home` calls
`_render_stage(request, "summary", session)`; the dead-template guard passes (the new entry-root
literal resolves to a real, reachable template).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the Summary rail node as the landing entry</name>
  <files>src/phaze/templates/shell/partials/rail.html</files>
  <action>
Insert a Summary rail node as the FIRST node inside the existing `<nav aria-label="Pipeline stages">`
(immediately after the `<nav ...>` open tag on line 51, before the `{# 1 — Discover ... #}` node).

Copy the Discover top-level node (lines 54-63) VERBATIM as the structural template and change only:
`data-rail-stage="summary"`, `hx-get="/s/summary"`, `title="Summary"`,
`{% if stage == 'summary' %}aria-current="page"{% endif %}`, the label text `Summary`, and the glyph.
Keep `hx-target="#stage-workspace" hx-swap="innerHTML" hx-push-url="true"` and Discover's full class
string unchanged (it already carries `focus-visible:`, the `aria-[current=page]:` active affordances,
and the `max-lg:` collapse classes).

Two deliberate deviations from Discover:
  * OMIT the trailing count `<span x-text="$store.pipeline...">` entirely. Summary has NO backing
    store key, and the rail's locked live-vs-static rule forbids inventing one. Match the Track-ID
    node (lines 111-119), which is label-only.
  * The label span keeps `max-lg:sr-only` and MUST NOT use `max-lg:hidden` (hard CUT-04 ↔ CUT-01
    contract: the collapsed 64px strip stays screen-reader-navigable).

Glyph: heroicons v2 OUTLINE `chart-pie` (MIT), which reads as an overview/summary mark. Use the exact
wrapper the other glyphs use — `viewBox="0 0 24 24"`, `fill="none"`, `stroke="currentColor"`,
`stroke-width="1.5"`, `stroke-linecap="round"`, `stroke-linejoin="round"`, `class="w-5 h-5 shrink-0"`,
`aria-hidden="true"` — wrapping these two paths:
  - `M10.5 6a7.5 7.5 0 1 0 7.5 7.5h-7.5V6Z`
  - `M13.5 10.5H21A7.5 7.5 0 0 0 13.5 3v7.5Z`

Because Summary now occupies the first slot, add `mt-1.5` to the Discover node's class string so its
top spacing matches the other top-level nodes (Propose already carries `mt-1.5`). Purely visual;
change nothing else about Discover.

Also extend the file's header Jinja comment (the node-order map at lines 5-13) to list `summary` as
node 0 — the landing/overview entry that `/` renders — keeping that comment an accurate map of the
rail. Note there that Summary is a placeholder carrying no store key.

Rationale for placement (record it in the node's own inline `{# ... #}` comment): Summary is the `/`
landing page, so it reads first; it sits inside the existing Pipeline `<nav>` landmark rather than
above the eyebrow so that every navigable rail node stays within one labelled nav landmark.

Do NOT touch the Analyze node — it keeps `hx-get="/s/analyze"` and stays one click away.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/core/test_rail_narrow_width.py tests/shared/core/test_a11y_guards.py -q</automated>
  </verify>
  <done>
Both pure-filesystem structural guards pass, proving the new node satisfies every rail contract:
it has a `title`, an `aria-hidden` 24×24 `currentColor` `w-5 h-5` glyph, a `max-lg:sr-only`
(never `max-lg:hidden`) label, a `focus-visible:` ring, and the `aria-current="page"` idiom.
`rail.html` contains `data-rail-stage="summary"` and `hx-get="/s/summary"`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the Analyze-default tests and cover the new Summary landing behavior</name>
  <files>tests/shared/core/test_shell_routes.py</files>
  <action>
Two existing tests hard-code Analyze as the `/` default (pre_flight_findings #4). Reconcile them,
then add positive coverage for the new behavior.

First, before renaming anything, run `grep -rn "test_root_renders_shell_analyze_default" .` and
update every reference you find (at minimum the function→requirement map in this module's own
docstring, lines 11-17).

1. `_RAIL_STAGES` (line 33): prepend `"summary"` so the list is the 13 navigable stages in rail
   order. Update its comment from "The 12 navigable rail-node ids" to 13. `test_rail_nodes_wired`
   then also asserts `hx-get="/s/summary"` is wired, and its
   `body.count('hx-target="#stage-workspace"') >= len(_RAIL_STAGES)` assertion still holds (the rail
   now renders 13 such nodes).

2. Rename `test_root_renders_shell_analyze_default` → `test_root_renders_shell_summary_default` and
   rewrite its body. It no longer needs the `make_file` fixture (the empty-state swap is confined to
   the analyze branch), so drop that parameter and the `await make_file()` seed. Assert: `GET /`
   returns 200 (a render, NOT a redirect); the body contains `id="stage-workspace"`,
   `data-stage="summary"`, and the placeholder marker `data-summary-placeholder`. Also assert the
   Analyze workspace is NOT what rendered — `id="analyze-lanes"` must be absent from the swap target's
   real content. Careful: the scaffold's hidden sink host emits an EMPTY `<div id="analyze-lanes"></div>`
   on every non-analyze workspace, so a bare substring check would be wrong. Assert instead that the
   Analyze lane grid is inert here by checking the sink shape: `'<div id="analyze-lanes"></div>'` is
   present (the hidden sink) — or, more robustly and preferably, simply assert
   `'data-stage="analyze"' not in body`. Rewrite the docstring to describe the Summary default.

3. Add `test_analyze_still_reachable_at_s_analyze`. It MUST seed a file first via
   `await make_file()` or the first-run empty state renders instead of the dashboard
   (pre_flight_findings #8). Assert `GET /s/analyze` returns 200 and the body carries
   `data-stage="analyze"` plus `id="analyze-lanes"`, and that the analyze rail node is the active one
   there — reuse the existing regex idiom:
   `re.search(r'data-rail-stage="analyze"[^>]*aria-current="page"', body)`.

4. Add `test_summary_stage_route_and_fragment`. Assert `GET /s/summary` (no HX header) returns 200
   and carries `id="stage-workspace"` + `data-summary-placeholder`; and that
   `GET /s/summary` with `headers={"HX-Request": "true"}` returns 200, contains
   `data-summary-placeholder`, and contains neither `"<html"` nor `"<head"` (the bare-fragment
   contract). Also assert the fragment carries `id="straggler-failed-card"` — the OOB seed host that
   keeps the chrome poll clean on the landing page (this is the regression guard for
   pre_flight_findings #1/#2). Finally assert the fragment starts no second poll loop:
   `'hx-trigger="every'` and `"setInterval"` are both absent.

5. `test_rail_nodes_wired`: change ONLY its final active-node assertion from `analyze` to `summary`
   — on `GET /` the active rail node is now Summary. The regex becomes
   `re.search(r'data-rail-stage="summary"[^>]*aria-current="page"', body)` with a message noting the
   summary rail node must carry `aria-current="page"` on the shell root. Update the docstring line
   that reads "The `/` default marks the analyze node `aria-current=\"page\"`". Leave the
   `_RAIL_STAGES` hx-get loop and the hx-target/hx-swap/push-url assertions as they are.

Leave `test_stage_fragment_is_bare`, `test_unknown_stage_404`, `test_tabbar_removed_header_present`,
and `test_theme_and_store_preserved` untouched — pre_flight_findings #5 confirms they assert on
chrome that is unchanged by this repoint.

Follow the file's existing style: `from __future__ import annotations`, `@pytest.mark.asyncio`,
`client: AsyncClient` typed via the `TYPE_CHECKING` import, and the `make_file` fixture typed with
the module's existing `# type: ignore[no-untyped-def]` idiom where used.
  </action>
  <verify>
    <automated>just test-db && TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_test" MIGRATIONS_TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_migrations_test" uv run pytest tests/shared/core/test_shell_routes.py tests/shared/core/test_enrich_analyze_workspaces.py tests/shared/core/test_identify_workspaces.py tests/shared/core/test_review_apply_workspaces.py tests/shared/core/test_a11y_guards.py tests/shared/core/test_rail_narrow_width.py tests/shared/core/test_dead_template_guard.py -q</automated>
  </verify>
  <done>
All seven suites pass. `GET /` renders the Summary placeholder as the active rail node; `/s/analyze`
still renders the real Analyze workspace; `/s/summary` serves both the full shell and a bare fragment
carrying the OOB seed host; and `test_shell_sinks_legacy_oob_fragments` (the hidden breakage from
pre_flight_findings #2) is green because Summary composes the shared scaffold.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `GET /s/{stage}` | User-controlled `stage` path segment reaches the router |
| template context → HTML | Rendered values cross into the response body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-SQ3-01 | Tampering | `shell_stage` / `STAGE_PARTIALS` | mitigate | Preserve T-57-01 verbatim: `"summary"` is added as a STATIC string-literal VALUE in the `STAGE_PARTIALS` dict, and `stage` is matched against the dict keys (404 on miss) rather than ever being interpolated into a template path. Task 1 explicitly forbids a dynamic path. |
| T-SQ3-02 | Information Disclosure | `summary_placeholder.html` | accept | The placeholder renders only static author-written copy — no DB reads, no request-derived values, no new context keys cross the boundary. Jinja2 autoescape remains on; nothing uses `\| safe`. Nothing to disclose. |
| T-SQ3-03 | Denial of Service | chrome `/pipeline/stats` 5s poll on `/` | mitigate | Summary composes `_workspace_scaffold.html`, which includes `_workspace_poll_seeds.html`, so every OOB fragment the single chrome poll emits has a pre-existing landing target — preventing an `htmx:oobErrorNoTarget` log storm (6+ errors per tick) on the app's landing page. Task 3 adds a regression assertion on `#straggler-failed-card`. The placeholder adds NO second poll loop (`hx-trigger="every"` / `setInterval` are explicitly forbidden and asserted absent). |
| T-SQ3-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in this task — zero new dependencies. No legitimacy audit required. |

Spoofing / Repudiation / Elevation-of-Privilege: not applicable. This change adds no authentication
surface, no state mutation, no audited action, and no new endpoint (the `summary` stage is served by
the pre-existing, already-whitelisted `GET /s/{stage}` route).
</threat_model>

<verification>
Full-gate checks before considering the task done:

1. Behavioral + structural suites (needs the ephemeral test DB on port 5433):
   `just test-db`, then export
   `TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_test"` and
   `MIGRATIONS_TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_migrations_test"`,
   then run the seven suites listed in Task 3's verify.

2. Whole-bucket regression (both changed test files and every `GET /` assertion live in the
   `shared` bucket): `just test-bucket shared`.

3. Lint / format / types: `uv run ruff check .`, `uv run ruff format --check .`, `uv run mypy .`.

4. Docs-drift traceability guard: `just docs-drift`.

5. Pre-commit (never bypass with `--no-verify`): `pre-commit run --all-files`.

Manual sanity (optional, not a gate): the Tailwind classes used are all already present elsewhere in
`rail.html` / `empty_state.html`, so no `just tailwind` rebuild is needed for new utilities.
</verification>

<success_criteria>
- `GET /` returns 200 and renders the Summary placeholder (`data-stage="summary"`,
  `data-summary-placeholder`) inside the unchanged shell chrome — no redirect.
- `GET /s/summary` renders the full shell on direct nav and a bare fragment (no `<html>`/`<head>`)
  on an `HX-Request: true` swap.
- `GET /s/analyze` still returns 200 with `data-stage="analyze"` and `id="analyze-lanes"`, and its
  rail node is the active one there.
- The rail renders a Summary node first, with `aria-current="page"` on `/`, a heroicons glyph, a
  `max-lg:sr-only` label, and no `$store.pipeline` key.
- The Summary page supplies a landing target for every OOB fragment the single chrome poll emits
  (`#straggler-failed-card` present) and starts no second poll loop.
- `just test-bucket shared` is green; ruff, mypy, `just docs-drift`, and pre-commit all pass.
- No new dependencies, no new DB queries, no new context keys, no new endpoint, and no summary
  widgets/metrics were added.
</success_criteria>

<output>
Create `.planning/quick/260707-sq3-add-placeholder-summary-page-as-the-defa/260707-sq3-SUMMARY.md` when done
</output>
