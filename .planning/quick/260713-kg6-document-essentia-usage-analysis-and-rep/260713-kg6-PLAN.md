---
quick_id: 260713-kg6
slug: document-essentia-usage-analysis-and-rep
date: 2026-07-13
status: complete
---

# Quick Task 260713-kg6: Document essentia usage analysis & replacement research

## Description

Capture this session's investigation into essentia usage in phaze and whether a
less compute-intensive replacement exists (without losing features). Deliverable
is a single reference doc under `docs/` plus an index link in `docs/README.md`.

The content is a synthesis of source-tracing done in this session (`analysis.py`,
`proposal.py`, `models/analysis.py`) plus web research (essentia / librosa /
madmom / aubio / MIRFLEX) and Context7-confirmed essentia tempo-algorithm facts.
Authored inline by the orchestrator because the findings live in conversation
context, not in a form a fresh executor could reconstruct.

## Tasks

1. **Write `docs/essentia-analysis.md`** — usage map, compute profile, feature
   surface that must be preserved, replacement-landscape verdict, ranked
   feature-preserving recommendations (#1 tempo retune + confidence caveat,
   #2 variant pruning, #3 decode-once = not worth it, #4 ONNX), and cited sources.
   - verify: file exists, renders, links resolve
   - done: doc committed
2. **Add index row to `docs/README.md`** under the Reference section.
   - verify: link path resolves to the new doc
   - done: committed in the same change

## Notes

- Analysis-only task; no source-code behavior changes.
- Recommendations are documented as candidates, NOT executed — any implementation
  (e.g. tempo retune) is a separate GSD phase with parity validation.
