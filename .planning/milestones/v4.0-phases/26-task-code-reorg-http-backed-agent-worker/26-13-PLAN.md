---
phase: 26
plan: 13
type: execute
wave: 6
depends_on:
  - "26-09"
  - "26-10"
  - "26-11"
  - "26-12"
files_modified:
  - docker-compose.yml
  - src/phaze/tasks/worker.py
  - src/phaze/tasks/session.py
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/phases/25-internal-agent-http-api-bearer-auth/25-CONTEXT.md
autonomous: true
requirements:
  - OPS-01
tags:
  - docker
  - cleanup
  - docs
must_haves:
  truths:
    - "`src/phaze/tasks/worker.py` is DELETED (D-04, D-08)"
    - "`src/phaze/tasks/session.py` is DELETED (D-06, D-08)"
    - "`docker-compose.yml` worker service `command:` line is exactly `uv run saq phaze.tasks.controller.settings` (D-04)"
    - "`docker-compose.yml` worker service `environment:` block includes `PHAZE_ROLE=control` (OPS-01)"
    - "`docker-compose.yml` worker service does NOT depend on `audfprint` or `panako` (controller is fileless — D-04 worker.command commentary)"
    - "Documentation sweep: every reference to `lux_worker` in `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/STATE.md`, `.planning/PROJECT.md`, and `.planning/phases/25-internal-agent-http-api-bearer-auth/25-CONTEXT.md` is replaced with `controller` (D-33)"
    - "No code or doc file in the repo contains the string `phaze.tasks.worker.settings` or `phaze.tasks.lux_worker` after this plan ships (any remaining mention is a docstring reference to the old module which must be flagged in SUMMARY)"
    - "`uv run pytest -x -q --no-cov` exits 0 (full test suite passes; deletions don't regress)"
    - "`docker compose config -q` exits 0 (compose file is valid YAML + valid compose schema)"
    - "`uv run mypy src/` exits 0 (no broken imports after worker.py / session.py removal)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "worker service command updated to controller.settings + PHAZE_ROLE=control env"
      contains: "phaze.tasks.controller.settings"
    - path: ".planning/ROADMAP.md"
      provides: "lux_worker references replaced with controller"
      contains: "controller"
  key_links:
    - from: "docker-compose worker.command"
      to: "phaze.tasks.controller.settings (Plan 09)"
      via: "saq CLI invocation; PHAZE_ROLE=control env makes get_settings() return ControlSettings"
      pattern: "phaze\\.tasks\\.controller\\.settings"
    - from: "deletion of worker.py + session.py"
      to: "absence of legacy SAQ entry-point"
      via: "no remaining caller imports phaze.tasks.worker or phaze.tasks.session after this plan"
      pattern: "phaze\\.tasks\\.(worker|session)"
---

<objective>
Finalize Phase 26: delete the legacy `phaze.tasks.worker` and `phaze.tasks.session` modules, update `docker-compose.yml` to launch the new `controller.settings` SAQ module under `PHAZE_ROLE=control`, and sweep the planning docs to replace `lux_worker` with `controller` (the hostname-leaking name was D-02's #1 rename target). This is the closing-housekeeping plan.

Purpose: Without this, the legacy worker.py still exists as orphaned code, docker-compose still points at it (so a fresh `docker compose up` fails), and the docs still reference the abandoned `lux_worker` name. Plan 13 is what makes the phase shippable.

Output: Two file deletions, one docker-compose edit, five doc-sweep edits.

Implements decisions: D-04 (worker.py deleted, docker-compose updated to controller), D-06 (session.py deleted), D-33 (lux_worker→controller doc sweep across 5 files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-task-code-reorg-http-backed-agent-worker/26-CONTEXT.md
@docker-compose.yml
@src/phaze/tasks/worker.py
@src/phaze/tasks/session.py
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/phases/25-internal-agent-http-api-bearer-auth/25-CONTEXT.md

<interfaces>
<!-- Files being deleted -->
- src/phaze/tasks/worker.py — 115 lines, replaced by controller.py + agent_worker.py
- src/phaze/tasks/session.py — 5 lines, deprecated stub from v1.0 era

<!-- docker-compose worker service shape (current) -->
The `worker` service currently runs `uv run saq phaze.tasks.worker.settings`. Phase 26 replaces this with `uv run saq phaze.tasks.controller.settings` and adds `PHAZE_ROLE=control` to env.

<!-- Documentation files to sweep -->
The string "lux_worker" appears in several planning docs (per D-02 and D-33). Replace with "controller". CASE-SENSITIVE substitution. Do NOT touch git history or COMMIT messages; only present-tense doc files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy worker.py and session.py; update docker-compose.yml to controller.settings</name>
  <read_first>
    - src/phaze/tasks/worker.py (about to be deleted — confirm no caller imports it)
    - src/phaze/tasks/session.py (about to be deleted — confirm no caller imports it)
    - docker-compose.yml (find current worker service command)
  </read_first>
  <files>
docker-compose.yml,
src/phaze/tasks/worker.py,
src/phaze/tasks/session.py
  </files>
  <action>
**Step 1: Pre-flight caller grep.** Confirm nothing in `src/` or `tests/` still imports `phaze.tasks.worker` or `phaze.tasks.session`:
```
grep -rn "from phaze.tasks.worker\|from phaze.tasks.session\|import phaze.tasks.worker\|import phaze.tasks.session" src/ tests/ 2>/dev/null
```
Expected: 0 matches. If any matches exist, those callers were missed in Plans 09-12 — STOP and document the gap, then fix the caller before deleting.

**Step 2: Delete the two files.**
```
git rm src/phaze/tasks/worker.py
git rm src/phaze/tasks/session.py
```

If `git rm` complains (e.g., file is untracked), use `rm` and `git add -u` afterwards.

**Step 3: Update docker-compose.yml worker service.**

Current state (verify before editing):
```bash
grep -A 20 "^  worker:" docker-compose.yml
```

Edit the worker service block:
1. Change `command:` from `uv run saq phaze.tasks.worker.settings` (or whatever form it currently takes) TO `uv run saq phaze.tasks.controller.settings`.
2. Add `PHAZE_ROLE=control` to the worker service's `environment:` block (or `env_file:`, whichever convention the project uses).
3. Remove `audfprint` and `panako` from `depends_on:` for the worker service if they appear there — the controller is fileless and doesn't need them. Other services (e.g., `api`) keep their existing depends_on lines.

Read the current docker-compose.yml and write the modified version. Example diff for the worker service:
```yaml
  worker:
    build:
      context: .
      dockerfile: Dockerfile
    command: uv run saq phaze.tasks.controller.settings   # WAS: phaze.tasks.worker.settings
    environment:
      MODELS_PATH: /models
      PHAZE_ROLE: control                                  # NEW (Phase 26 D-01)
    env_file: .env
    depends_on:
      - postgres
      - redis
      # Removed: audfprint, panako (controller is fileless per D-04)
    volumes:
      # Keep existing volume mounts; Phase 29 will strip /data /models from the application server
      - ./src:/app/src
```

The exact form depends on the current docker-compose.yml — DO NOT reflexively delete `volumes:` entries unless they ARE in worker.

**Step 4: Verify.**
```
docker compose config -q && echo "compose valid"
uv run mypy src/ 2>&1 | tail -5   # no broken imports after deletion
uv run pytest -x -q --no-cov 2>&1 | tail -10  # no regressions
```

If `uv run pytest` fails because some test imports `phaze.tasks.worker` (we said no callers exist, but a test might), update or skip the test and document in SUMMARY.
  </action>
  <verify>
    <automated>test ! -f src/phaze/tasks/worker.py && echo "worker.py deleted"; test ! -f src/phaze/tasks/session.py && echo "session.py deleted"; grep -rn "from phaze.tasks.worker\|from phaze.tasks.session\|import phaze.tasks.worker\|import phaze.tasks.session" src/ tests/ 2>/dev/null | grep -v __pycache__ | wc -l; grep -c "phaze.tasks.controller.settings" docker-compose.yml; grep -c "PHAZE_ROLE.*control" docker-compose.yml; docker compose config -q 2>&1 | tail -3; uv run mypy src/ 2>&1 | tail -3; uv run pytest -x -q --no-cov 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/phaze/tasks/worker.py` exits 0 (file deleted)
    - `test ! -f src/phaze/tasks/session.py` exits 0 (file deleted)
    - `grep -rn "from phaze.tasks.worker\\|from phaze.tasks.session\\|import phaze.tasks.worker\\|import phaze.tasks.session" src/ tests/ 2>/dev/null | grep -v __pycache__ | wc -l` returns `0` (no leftover callers)
    - `grep -c "phaze.tasks.controller.settings" docker-compose.yml` returns at least `1`
    - `grep -c "phaze.tasks.worker.settings" docker-compose.yml` returns `0` (old reference removed)
    - `grep -E "PHAZE_ROLE.*control\|PHAZE_ROLE=control" docker-compose.yml | wc -l` returns at least `1`
    - `docker compose config -q` exits 0 (valid compose file)
    - `uv run mypy src/` exits 0
    - `uv run pytest -x -q --no-cov` exits 0
  </acceptance_criteria>
  <done>Legacy worker.py and session.py deleted; docker-compose.yml worker service uses controller.settings with PHAZE_ROLE=control; full test suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Doc sweep — replace lux_worker with controller across planning docs (D-33)</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 26/27/28/29 entries reference lux_worker)
    - .planning/REQUIREMENTS.md (TASK-01, TASK-02 reference lux_worker)
    - .planning/STATE.md (accumulated decisions may mention lux_worker)
    - .planning/PROJECT.md (milestone description)
    - .planning/phases/25-internal-agent-http-api-bearer-auth/25-CONTEXT.md (references phaze-agent-<id> queue and lux_worker)
  </read_first>
  <files>
.planning/ROADMAP.md,
.planning/REQUIREMENTS.md,
.planning/STATE.md,
.planning/PROJECT.md,
.planning/phases/25-internal-agent-http-api-bearer-auth/25-CONTEXT.md
  </files>
  <action>
**Step 1: Find every occurrence of `lux_worker` in the target files.**

```bash
grep -rn "lux_worker" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md .planning/PROJECT.md .planning/phases/25-internal-agent-http-api-bearer-auth/25-CONTEXT.md
```

For each match, decide the contextually-correct replacement (almost always `controller`, but verify per line — e.g., "`lux_worker` service" → "`controller` service"; "`phaze.tasks.lux_worker`" → "`phaze.tasks.controller`").

**Step 2: Edit each file.** Use sensible word-boundary substitution:

| Old pattern | New pattern |
|-------------|-------------|
| `lux_worker` | `controller` |
| `phaze.tasks.lux_worker` | `phaze.tasks.controller` |
| `phaze-lux-worker` (if present) | `phaze-controller` |
| `lux worker` (with space) | `controller` |

**DO NOT** change:
- Filenames in `.planning/phases/<n>/<m>-SUMMARY.md` — those are historical records.
- Git commit messages — historical.
- Any reference in the BODY of a SUMMARY.md that talks about what was actually shipped at that time.

**DO** change:
- Forward-looking phase descriptions in ROADMAP.md (Phase 27, 28, 29).
- Requirement text in REQUIREMENTS.md.
- Decisions in STATE.md accumulated context.
- Architectural overview in PROJECT.md.
- Phase 25 CONTEXT.md references to the future phase 26 module name.

**Step 3: Verify.**

```bash
# All occurrences replaced
grep -rn "lux_worker" .planning/ 2>/dev/null | grep -v -E "/[0-9]+-[0-9]+-SUMMARY\\.md|/25-VERIFICATION\\.md|/26-RESEARCH\\.md" | wc -l
# Expected: 0 -- but may be non-zero if SUMMARY.md / VERIFICATION.md / RESEARCH.md historical refs exist (these are OK to leave)
```

If grep finds `lux_worker` in historical SUMMARY files, leave them — they document what was planned at the time.

**Step 4: Spot-check ROADMAP.md for Phase 26 module name consistency.**
```bash
grep -A 5 "Phase 26" .planning/ROADMAP.md | head -20
```
The phase description should mention `phaze.tasks.controller` and `phaze.tasks.agent_worker` (not lux_worker).

**Step 5: Spot-check REQUIREMENTS.md TASK-01.**
```bash
grep -A 3 "TASK-01\|TASK-02" .planning/REQUIREMENTS.md
```
TASK-02 specifically mentions "fileless SAQ tasks ... run only on the application-server worker" — this should now reference `controller` (or be neutral).

This is a doc-only commit; create a single commit at the end of the task with message:
```
docs(v4.0): replace hostname-leaked lux_worker with role-neutral controller

Per Phase 26 D-33, replaces every forward-looking reference to phaze.tasks.lux_worker
with phaze.tasks.controller across ROADMAP.md, REQUIREMENTS.md, STATE.md, PROJECT.md,
and 25-CONTEXT.md. Historical SUMMARY records preserved.
```
  </action>
  <verify>
    <automated>grep -rn "lux_worker" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md .planning/PROJECT.md .planning/phases/25-internal-agent-http-api-bearer-auth/25-CONTEXT.md 2>/dev/null | wc -l; grep -c "controller" .planning/ROADMAP.md; grep -c "phaze.tasks.controller" .planning/ROADMAP.md .planning/REQUIREMENTS.md 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "lux_worker" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md .planning/PROJECT.md .planning/phases/25-internal-agent-http-api-bearer-auth/25-CONTEXT.md 2>/dev/null | wc -l` returns `0` (all forward-looking refs replaced)
    - `grep -c "phaze.tasks.controller\\|controller" .planning/ROADMAP.md` returns at least `2` (Phase 26 entry mentions controller + agent_worker)
    - `grep "Phase 26" .planning/ROADMAP.md | head -1` exits 0 (line still exists; only content changed)
    - The doc-sweep commit exists in git log with a `docs(v4.0):` prefix
  </acceptance_criteria>
  <done>All forward-looking `lux_worker` references in 5 planning docs replaced with `controller`; historical SUMMARY records untouched; commit created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker compose env → controller startup | `PHAZE_ROLE=control` is an operator-set env var. Read by `phaze.config.get_settings()` to dispatch to ControlSettings. Misconfig (PHAZE_ROLE unset or typo'd) → ControlSettings is constructed by default (no required-field validator), so misconfig falls through silently to control role. Acceptable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-13-D | Denial of Service | Deletion of `worker.py` breaks a missed caller (e.g., a tools script or a CI workflow) | mitigate | Pre-flight grep before deletion catches in-repo callers. CI runs the full test suite — any orphaned import surfaces at test collection. External callers (e.g., a sysadmin's helper script) are out of scope for the project's grep. |
| T-26-13-T | Tampering | Doc-sweep accidentally edits a historical SUMMARY.md | mitigate | The grep filter `-v -E "SUMMARY\\.md\|VERIFICATION\\.md"` excludes historical records. The executor is instructed to only edit the 5 named files. |
| T-26-13-E | Elevation | docker-compose.yml accidentally retains both `worker.settings` AND `controller.settings` commands | mitigate | Acceptance criterion `grep -c "phaze.tasks.worker.settings" docker-compose.yml == 0` catches this. |
</threat_model>

<verification>
After both tasks complete:
```
# (1) Files deleted, no callers
test ! -f src/phaze/tasks/worker.py && test ! -f src/phaze/tasks/session.py

# (2) docker-compose updated
grep -c "phaze.tasks.controller.settings" docker-compose.yml   # 1
grep -c "phaze.tasks.worker.settings" docker-compose.yml       # 0
docker compose config -q                                        # valid

# (3) Doc sweep complete
grep -rn "lux_worker" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md .planning/PROJECT.md .planning/phases/25-internal-agent-http-api-bearer-auth/25-CONTEXT.md | wc -l   # 0

# (4) Full test suite still green
uv run pytest -x -q --no-cov
uv run mypy src/
uv run ruff check .
```

Manual smoke (per VALIDATION.md):
```
# Bring up the controller and verify SAQ logs the new settings module path
docker compose up -d worker
docker compose logs --tail 50 worker | grep -i "controller.settings"
# Expected: 1+ matches showing SAQ starting up with the controller settings module
```
</verification>

<success_criteria>
Plan 13 is complete when:
- [ ] `src/phaze/tasks/worker.py` deleted
- [ ] `src/phaze/tasks/session.py` deleted
- [ ] docker-compose.yml worker service uses `phaze.tasks.controller.settings` + `PHAZE_ROLE=control`
- [ ] No forward-looking `lux_worker` references in ROADMAP.md, REQUIREMENTS.md, STATE.md, PROJECT.md, 25-CONTEXT.md
- [ ] Historical SUMMARY records untouched
- [ ] Full test suite passes
- [ ] docker compose config validates
- [ ] mypy + ruff clean across the repo
- [ ] Two commits created: one for code (deletions + compose), one for docs (lux_worker → controller)
- [ ] Pre-commit hooks all green; no `--no-verify` used
</success_criteria>

<output>
Create `.planning/phases/26-task-code-reorg-http-backed-agent-worker/26-13-SUMMARY.md` recording the deletion + doc sweep details, the docker-compose smoke-test outcome, and any historical refs intentionally left alone.
</output>
