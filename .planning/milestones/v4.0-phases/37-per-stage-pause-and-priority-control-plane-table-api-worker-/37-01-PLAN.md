---
phase: 37-per-stage-pause-and-priority-control-plane-table-api-worker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/models/pipeline_stage_control.py
  - src/phaze/models/__init__.py
  - alembic/versions/020_add_pipeline_stage_control.py
  - src/phaze/tasks/_shared/stage_control.py
  - tests/test_migrations/test_020.py
autonomous: true
requirements: [REQ-37-1, REQ-37-2, REQ-37-3]
nyquist_compliant: true

# HARD CROSS-PHASE DEPENDENCY: Phase 36 (Postgres queue backend + saq_jobs table +
# build_pipeline_queue factory). This plan does NOT touch saq_jobs and does not import
# the queue, so it can be authored against the Phase-36 contract, but the integration
# substrate it underpins assumes Phase 36 has shipped.

must_haves:
  truths:
    - "pipeline_stage_control table exists with exactly 3 seeded rows (metadata/analyze/fingerprint), paused=false, priority=50"
    - "the priority column rejects any value outside 0..100 at the database layer (CHECK constraint)"
    - "STAGE_TO_FUNCTION maps the 3 stage labels to their registered SAQ function names and SENTINEL is one shared constant 9999999999"
    - "the stage_control constants module imports cleanly with NO sqlalchemy.ext.asyncio / phaze.database import (agent-boundary-safe)"
  artifacts:
    - path: "src/phaze/models/pipeline_stage_control.py"
      provides: "PipelineStageControl ORM model (stage PK, paused, priority, timestamps, CHECK 0-100)"
      contains: "pipeline_stage_control"
    - path: "alembic/versions/020_add_pipeline_stage_control.py"
      provides: "Migration 020 creating + seeding the control table, revises 019"
      contains: "pipeline_stage_control"
    - path: "src/phaze/tasks/_shared/stage_control.py"
      provides: "STAGE_TO_FUNCTION / _FUNCTION_TO_STAGE / SENTINEL canonical constants"
      contains: "SENTINEL"
    - path: "tests/test_migrations/test_020.py"
      provides: "Real-PG upgrade/downgrade + seed + CHECK proof for migration 020"
      contains: "test_020"
  key_links:
    - from: "src/phaze/models/__init__.py"
      to: "phaze.models.pipeline_stage_control.PipelineStageControl"
      via: "import for Alembic autogenerate discovery"
      pattern: "PipelineStageControl"
    - from: "alembic/versions/020_add_pipeline_stage_control.py"
      to: "migration 019"
      via: "down_revision chain"
      pattern: "down_revision.*019"
---

<objective>
Establish the durable control-plane substrate for per-stage pause/priority: the `pipeline_stage_control` app table (model + seeded migration 020) and the canonical `STAGE_TO_FUNCTION` / `_FUNCTION_TO_STAGE` / `SENTINEL` constants that every later plan (hook, service, endpoints, tests) imports.

Purpose: Interface-first — create the single source of truth for the 3 stage labels, their registered SAQ function names, and the park sentinel BEFORE any code consumes them, so downstream plans wire against fixed contracts instead of re-deriving them. The DB CHECK (`priority BETWEEN 0 AND 100`) and the seeded defaults (`paused=false, priority=50`) encode REQ-37-2's clamp guarantee and the resume/pause baseline at the schema layer.

Output: `PipelineStageControl` model registered for autogenerate; Alembic migration 020 (table + 3 seed rows + CHECK), proven by `tests/test_migrations/test_020.py`; a DB-free `stage_control.py` constants module.

NOTE — hard dependency on Phase 36: this phase's control plane operates on the Postgres-backed `saq_jobs` table and `build_pipeline_queue` factory delivered in Phase 36. This plan touches neither (it only adds an app table + constants), so it is authorable now, but it MUST land after Phase 36 ships.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v4.0-phases/37-per-stage-pause-and-priority-control-plane-table-api-worker-/37-RESEARCH.md
@.planning/milestones/v4.0-phases/37-per-stage-pause-and-priority-control-plane-table-api-worker-/37-PATTERNS.md
@.planning/milestones/v4.0-phases/37-per-stage-pause-and-priority-control-plane-table-api-worker-/37-VALIDATION.md

<interfaces>
<!-- Extracted from codebase. Executor: use these directly; no exploration needed. -->

Model base (src/phaze/models/base.py):
- `Base(DeclarativeBase)` carries the naming-convention MetaData; `ck` template = `ck_%(table_name)s_%(constraint_name)s` so `CheckConstraint(..., name="priority_range")` auto-names `ck_pipeline_stage_control_priority_range`.
- `TimestampMixin` gives `created_at` + `updated_at` for free; `updated_at` already carries `onupdate=func.now()` (the research-required updated_at, zero extra code).

Model analog (src/phaze/models/scan_batch.py): `class ScanBatch(TimestampMixin, Base)`, `__tablename__`, `mapped_column(...)`, `__table_args__` tuple with `Index`/`text(...)` server_defaults.

CHECK-constraint analog (src/phaze/models/agent.py:33-38):
- `__table_args__ = (CheckConstraint("id ~ '...'", name="id_charset"),)` — copy this tuple shape, swap to `CheckConstraint("priority BETWEEN 0 AND 100", name="priority_range")`.

Model registry (src/phaze/models/__init__.py:1-38): import each model, list in `__all__`. Add `PipelineStageControl` in alphabetical position. This is what makes Alembic autogenerate find the table.

Migration analogs:
- `alembic/versions/018_add_analysis_window_table.py` — bare-number `revision`/`down_revision` strings, `op.create_table(...)` with `op.f(...)`-named constraints, downgrade = exact reverse.
- `alembic/versions/012_add_agents_table_and_backfill.py:55-101` — SEED via `op.get_bind().execute(sa.text("INSERT ... VALUES (:p)"), {params})` (NOT `op.bulk_insert`); bound params, no f-string interpolation.
- Latest migration on disk is `019_add_proposals_pending_unique_index.py` → 020 sets `down_revision = "019"`.

Stage → registered function → key prefix (37-RESEARCH, verified vs _KEY_BUILDERS in tasks/_shared/deterministic_key.py):
- `metadata`  → `extract_file_metadata` → `key LIKE 'extract_file_metadata:%'`
- `analyze`   → `process_file`          → `key LIKE 'process_file:%'`
- `fingerprint` → `fingerprint_file`    → `key LIKE 'fingerprint_file:%'`
- SENTINEL = 9999999999 (epoch year 2286; far beyond any real `scheduled`).

Migration test analog (tests/test_migrations/test_migration_018.py): module-by-path loader; `asyncio.to_thread(downgrade_to/upgrade_to, cfg, rev)`; assert via `to_regclass` / `information_schema`. Harness helpers (`upgrade_to`, `downgrade_to`, `_build_alembic_config`, `MIGRATIONS_TEST_DATABASE_URL`) come from `tests/test_migrations/conftest.py`. Run via `just integration-test` (ephemeral PG on 5433).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PipelineStageControl model + registry registration</name>
  <files>src/phaze/models/pipeline_stage_control.py, src/phaze/models/__init__.py</files>
  <read_first>
    - src/phaze/models/scan_batch.py (full — simple PK table + __table_args__ + TimestampMixin)
    - src/phaze/models/agent.py:20-45 (CheckConstraint in __table_args__ shape)
    - src/phaze/models/base.py (Base naming convention + TimestampMixin onupdate)
    - src/phaze/models/__init__.py (the whole registry file — alphabetical import + __all__)
    - 37-RESEARCH.md §"Pattern 1: pipeline_stage_control model" and §"Verified saq_jobs Column Contract"
  </read_first>
  <action>
    Create `src/phaze/models/pipeline_stage_control.py` defining `class PipelineStageControl(TimestampMixin, Base)` with `__tablename__ = "pipeline_stage_control"` and columns: `stage: Mapped[str] = mapped_column(String(32), primary_key=True)` (values metadata|analyze|fingerprint), `paused: Mapped[bool] = mapped_column(Boolean, nullable=False, server_default=text("false"))`, `priority: Mapped[int] = mapped_column(SmallInteger, nullable=False, server_default=text("50"))`. Use `SmallInteger` to match `saq_jobs.priority` (SMALLINT). `created_at`/`updated_at` come from `TimestampMixin` — do not redeclare. Add `__table_args__ = (CheckConstraint("priority BETWEEN 0 AND 100", name="priority_range"),)` — the naming convention turns this into `ck_pipeline_stage_control_priority_range`. Then register the model in `src/phaze/models/__init__.py`: add the import in alphabetical position and add `"PipelineStageControl"` to `__all__` (this is what makes Alembic autogenerate discover the table). Do NOT add any foreign keys, indexes, or relationship to `saq_jobs` — this is a standalone app table.
  </action>
  <verify>
    <automated>uv run mypy src/phaze/models/pipeline_stage_control.py && uv run python -c "from phaze.models import PipelineStageControl; assert PipelineStageControl.__tablename__=='pipeline_stage_control'; assert 'priority' in PipelineStageControl.__table__.columns"</automated>
  </verify>
  <acceptance_criteria>
    - `from phaze.models import PipelineStageControl` resolves (registered in `__all__`)
    - `PipelineStageControl.__tablename__ == "pipeline_stage_control"`; `stage` is the primary key; `priority` is `SmallInteger` with server_default 50; `paused` is `Boolean` server_default false
    - `__table_args__` contains a CHECK named (via convention) `ck_pipeline_stage_control_priority_range`
    - `uv run mypy src/phaze/models/pipeline_stage_control.py` passes
  </acceptance_criteria>
  <done>The control-table ORM model exists, inherits free timestamps, enforces the priority range in DDL, and is discoverable by Alembic autogenerate.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 020 — create + seed pipeline_stage_control + migration test</name>
  <files>alembic/versions/020_add_pipeline_stage_control.py, tests/test_migrations/test_020.py</files>
  <read_first>
    - alembic/versions/018_add_analysis_window_table.py (header block, revision/down_revision bare strings, op.create_table with op.f() names, downgrade mirror)
    - alembic/versions/012_add_agents_table_and_backfill.py:55-101 (seed via op.get_bind().execute(sa.text(INSERT), {params}))
    - alembic/versions/019_add_proposals_pending_unique_index.py (current head — 020 chains off "019"; static-SQL-constant + "no user input interpolated" comment idiom)
    - tests/test_migrations/test_migration_018.py (full — module-by-path loader, to_thread upgrade/downgrade, catalog-SQL asserts)
    - tests/test_migrations/conftest.py (upgrade_to / downgrade_to / _build_alembic_config / MIGRATIONS_TEST_DATABASE_URL helpers + the settings-singleton patch gotcha)
  </read_first>
  <action>
    Create `alembic/versions/020_add_pipeline_stage_control.py` with `revision = "020"`, `down_revision = "019"`. In `upgrade()`: `op.create_table("pipeline_stage_control", ...)` with the 3 data columns + `created_at`/`updated_at` (server_default `sa.func.now()`), `sa.PrimaryKeyConstraint("stage", name=op.f("pk_pipeline_stage_control"))`, and `sa.CheckConstraint("priority BETWEEN 0 AND 100", name=op.f("ck_pipeline_stage_control_priority_range"))`. Then seed the 3 rows via `op.get_bind().execute(sa.text("INSERT INTO pipeline_stage_control (stage, paused, priority, created_at, updated_at) VALUES (:stage, false, 50, NOW(), NOW())"), {...})` — one execute per stage label, bound params only, NO f-string interpolation (threat T-37-01; mirror the 012/019 idiom and add the "no user input interpolated" comment). `downgrade()` = `op.drop_table("pipeline_stage_control")`. CRITICAL (37-RESEARCH §Anti-Patterns): 020 touches ONLY `pipeline_stage_control` — it must never reference `saq_jobs` (SAQ owns that table via its own init_db). Then create `tests/test_migrations/test_020.py` mirroring `test_migration_018.py`: load 020 by path, assert bare-number `revision="020"`/`down_revision="019"`, drive `downgrade_to(cfg,"019")`→`upgrade_to(cfg,"020")` off `asyncio.to_thread`, assert the table exists via `to_regclass('pipeline_stage_control')`, assert `SELECT count(*) FROM pipeline_stage_control == 3` with each stage present, assert the CHECK rejects `priority=200` (expect an IntegrityError/CheckViolation on an UPDATE/INSERT), then assert downgrade drops the table cleanly.
  </action>
  <verify>
    <automated>uv run pytest tests/test_migrations/test_020.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_migrations/test_020.py -x` exits 0 (requires ephemeral PG via `just test-db`)
    - upgrade creates `pipeline_stage_control` with exactly 3 seeded rows (metadata/analyze/fingerprint), `paused=false`, `priority=50`
    - a write with `priority` outside 0..100 raises a CHECK/IntegrityError
    - downgrade drops the table; `to_regclass` returns NULL afterward
    - migration references ONLY `pipeline_stage_control`, never `saq_jobs`
  </acceptance_criteria>
  <done>Migration 020 creates and seeds the control table with a DB-enforced priority range and a clean reversible downgrade, proven by a real-PG migration test.</done>
</task>

<task type="auto">
  <name>Task 3: Canonical stage-control constants module (DB-free)</name>
  <files>src/phaze/tasks/_shared/stage_control.py</files>
  <read_first>
    - src/phaze/tasks/_shared/deterministic_key.py:37-83 (module conventions: __future__ annotations, structlog logger, TYPE_CHECKING saq import, __all__; the _KEY_BUILDERS dict whose 3 stage entries are the source of truth for STAGE_TO_FUNCTION)
    - src/phaze/tasks/_shared/queue_defaults.py (minimal DB-free hook-module shell)
    - 37-RESEARCH.md §"Stage → Registered-Function-Name → Key-Prefix Map" and §"Safe SENTINEL value"
  </read_first>
  <action>
    Create `src/phaze/tasks/_shared/stage_control.py` containing ONLY the canonical constants in this plan (the `apply_stage_control` hook + TTL cache arrive in Plan 02 — interface-first). Define `STAGE_TO_FUNCTION = {"metadata": "extract_file_metadata", "analyze": "process_file", "fingerprint": "fingerprint_file"}`, its inverse `_FUNCTION_TO_STAGE = {v: k for k, v in STAGE_TO_FUNCTION.items()}`, and `SENTINEL = 9999999999` (single shared park constant — the hook, pause endpoint, and resume guard all import this exact value; never recompute it). Mirror `deterministic_key.py` module conventions (`from __future__ import annotations`, `structlog` logger, explicit `__all__` listing the three names). CRITICAL boundary rule (37-RESEARCH Pitfall 4): this module must NOT import `phaze.database`, `phaze.tasks.session`, or `sqlalchemy.ext.asyncio` — keep it pure-constants so the agent worker can import it. The import-boundary test arrives in Plan 02; author this module to satisfy it now.
  </action>
  <verify>
    <automated>uv run python -c "from phaze.tasks._shared.stage_control import STAGE_TO_FUNCTION, _FUNCTION_TO_STAGE, SENTINEL; assert SENTINEL==9999999999; assert set(STAGE_TO_FUNCTION)=={'metadata','analyze','fingerprint'}; assert _FUNCTION_TO_STAGE['process_file']=='analyze'; import sys; assert 'sqlalchemy.ext.asyncio' not in sys.modules"</automated>
  </verify>
  <acceptance_criteria>
    - `STAGE_TO_FUNCTION` maps the 3 stages to `extract_file_metadata`/`process_file`/`fingerprint_file` exactly
    - `_FUNCTION_TO_STAGE` is the exact inverse; `SENTINEL == 9999999999`
    - importing the module does NOT pull `sqlalchemy.ext.asyncio` / `phaze.database` into `sys.modules`
    - `uv run mypy src/phaze/tasks/_shared/stage_control.py` passes
  </acceptance_criteria>
  <done>The single source of truth for stage labels, their SAQ function names, and the park sentinel exists as a DB-free module ready for the Plan-02 hook and the Plan-02/03/04 consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration author → DDL | migration 020 writes seed rows; any interpolated value is a tampering surface |
| operator intent → DB constraint | the priority value must stay dequeueable (0..100) regardless of later endpoint clamping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering (SQL injection) | migration 020 seed INSERT | mitigate | Seed via `op.get_bind().execute(sa.text(...), {params})` with bound params only; no f-string interpolation (mirror 012/019 idiom + "no user input interpolated" comment) |
| T-37-02 | Denial of Service (pipeline stall) | `priority` column | mitigate | DB `CHECK (priority BETWEEN 0 AND 100)` keeps every stage priority inside SAQ's dequeue window `priority BETWEEN 0 AND 32767`, so a stage can never be driven silently un-dequeueable at the schema layer |
| T-37-SC | Tampering (supply chain) | dependencies | accept | No new packages installed in this phase (uses existing SQLAlchemy/Alembic). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `uv run pytest tests/test_migrations/test_020.py -x` green (real PG via `just test-db`)
- `uv run python -c "from phaze.models import PipelineStageControl"` succeeds
- `uv run python -c "from phaze.tasks._shared.stage_control import STAGE_TO_FUNCTION, SENTINEL"` succeeds, no sqlalchemy pulled
- `uv run ruff check . && uv run mypy src/phaze` clean for touched files
</verification>

<success_criteria>
- `pipeline_stage_control` table + model exist, seeded with 3 rows, CHECK-bounded priority, free timestamps
- Migration 020 reversible and proven by a real-PG test
- `STAGE_TO_FUNCTION` / `_FUNCTION_TO_STAGE` / `SENTINEL` available as DB-free constants for all downstream plans
</success_criteria>

<output>
Create `.planning/milestones/v4.0-phases/37-per-stage-pause-and-priority-control-plane-table-api-worker-/37-01-SUMMARY.md` when done
</output>
