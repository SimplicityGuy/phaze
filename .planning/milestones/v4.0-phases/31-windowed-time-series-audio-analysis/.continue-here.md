# Phase 31 — Resume Here (blocked on 31-01 homelab spike checkpoint)

**Paused:** 2026-06-10, after Wave 2. Branch: `gsd/phase-31-windowed-time-series-audio-analysis`.

## Status

| Plan | Wave | Status |
|------|------|--------|
| 31-02 (AnalysisWindow model + migration 018) | 1 | ✅ complete, merged |
| 31-03 (agent wire contract + idempotent window replace) | 2 | ✅ complete, merged |
| 31-06 (review-UI BPM sparkline + SVG timeline) | 2 | ✅ complete, merged |
| 31-01 (mandatory spike) | 1 | ⏸ Task 1 script written; **Task 2 = BLOCKING homelab checkpoint** |
| 31-04 (analyze_file segmented rewrite) | 2 | 🔒 blocked — needs 31-01 locked decode strategy |
| 31-05 (process_file wiring + retry/timeout tune) | 3 | 🔒 blocked — needs 31-04 |

## The blocking checkpoint (31-01 Task 2 — human-verify)

The spike script `scripts/spike_windowed_analysis.py` (untracked, throwaway) must run on the
homelab agent against a REAL ≥2h VBR concert file to lock the decode strategy that Plan 04 implements.

**Operator steps:**
1. `scp scripts/spike_windowed_analysis.py datum@nox:/tmp/` (or datum@lux) — wherever the archive + `/models` live.
2. Pick a real ≥2h mp3/m4a from the 11,428-file archive.
3. Run inside the agent env: `uv run scripts/spike_windowed_analysis.py /path/to/real_2h_set.mp3 --models-dir /models`
4. Confirm: (A1) per-window `decode_seconds` roughly constant (NOT climbing with window_index);
   (c) peak RSS flat (< ~1.5 GB); (d) acceptable `sec/hour-of-audio` for 8× concurrency.
5. Paste the SUMMARY block numbers back + state the locked choice.

**Resume signal:** paste the spike's real-file numbers and type the locked decode choice —
`EasyLoader-primary` (if decode time constant) or `decode+Resample-hybrid` (if it climbs linearly).

## On resume (orchestrator)

1. Write `31-01-SUMMARY.md` with a "Decode Decision Log" section (real numbers + locked strategy).
   Mark 31-01 complete. (Spike script stays untracked/throwaway.)
2. Execute **31-04** (worktree, opus) — its `analyze_file` rewrite reads the locked decode choice.
3. After 31-04 merges, execute **31-05** (worktree, opus) — Wave 3.
4. Run phase verification (gsd-verifier) once no incomplete plans remain.

No `.continue-here` blocking anti-patterns recorded.
