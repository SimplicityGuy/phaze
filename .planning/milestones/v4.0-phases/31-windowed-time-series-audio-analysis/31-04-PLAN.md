---
phase: 31-windowed-time-series-audio-analysis
plan: 04
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - src/phaze/config.py
  - src/phaze/services/analysis.py
  - tests/test_services/test_analysis.py
  - tests/test_config/test_agent_settings_windows.py
autonomous: true
requirements: [ANL-01]

must_haves:
  truths:
    - "analyze_file decodes a multi-hour file per-window (no whole-file MonoLoader) — no OnsetDetectionGlobal crash, bounded memory"
    - "Each file yields a two-tier time-series: fine windows (BPM+key, 30s) and coarse windows (mood/style/danceability, 180s)"
    - "Representative aggregates (median BPM, modal key, dominant mood/style, mean danceability) are still produced for the existing analysis row"
    - "A window that raises is logged and skipped — it never fails the whole file"
    - "Window sizes are configurable via AgentSettings (defaults 30/180/15s)"
    - "Trailing partial windows shorter than analysis_fine_min_sec are dropped (except window 0)"
  artifacts:
    - path: "src/phaze/services/analysis.py"
      provides: "Rewritten per-window analyze_file + aggregate reduction helpers"
      contains: "EasyLoader"
    - path: "src/phaze/config.py"
      provides: "analysis_fine_window_sec / analysis_coarse_window_sec / analysis_fine_min_sec AgentSettings"
      contains: "analysis_fine_window_sec"
  key_links:
    - from: "analyze_file return dict"
      to: "tasks/functions.py::process_file"
      via: "{**aggregates, 'windows': [...]} plain-dict shape"
      pattern: "\"windows\""
    - from: "coarse window loop"
      to: "_classifier_cache"
      via: "_get_classifier/_predict_single reused (inference-only, no per-window graph reload)"
      pattern: "_predict_single|_get_classifier"
---

<objective>
Rewrite `analyze_file()` from whole-file decode to segmented per-window analysis — the core crash/OOM fix. Replace the two whole-file `MonoLoader()` calls with the spike-locked decode strategy (EasyLoader-primary unless Plan 01's decision log locked the decode+Resample hybrid), running `RhythmExtractor2013(multifeature)` + `KeyExtractor` per 30s fine window and the existing 34 TF model sets per 180s coarse window. Derive the four locked aggregates and return `{**aggregates, "windows": [...]}`. Add the three window-config `AgentSettings`. Keep all existing helpers (`MODEL_SETS`, `_classifier_cache`, `_get_classifier`, `_predict_single`, `derive_mood`, `derive_style`, `_suppress_essentia_logging`) and stay synchronous for `run_in_process_pool`.

Purpose: no essentia algorithm ever sees more than one short window, so the `OnsetDetectionGlobal` overflow can't happen and RSS stays bounded — fixing the live incident (0 of 11,428 files analyzed) while upgrading the data to a time-series.

Output: per-window `analyze_file`, pure-Python aggregate helpers, 3 config fields, and unit tests (window boundaries, aggregates, failure isolation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-RESEARCH.md
@.planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-PATTERNS.md
@.planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-01-SUMMARY.md
@src/phaze/services/analysis.py
@src/phaze/config.py

<interfaces>
<!-- Decode strategy LOCKED in 31-01-SUMMARY "Decode Decision Log" — read it before coding. -->
<!-- EasyLoader window loop (RESEARCH Pattern 1 l.166-199): es.EasyLoader(filename, sampleRate, startTime, endTime)() -->
<!-- KEEP: MODEL_SETS, _classifier_cache, _get_classifier, _predict_single (l.149-153), derive_mood, derive_style. -->
<!-- REPLACE only the two MonoLoader calls (~l.233, ~l.245) — see PATTERNS l.168-179. -->
<!-- Aggregates (RESEARCH l.275-304): median(fine.bpm); duration-weighted Counter mode(fine.key); -->
<!--   time-weighted dominant(coarse.mood/style); mean(coarse.danceability). Pure-Python, no essentia. -->
<!-- Duration: es.MetadataReader / es.AudioLoader or ffprobe — NEVER MonoLoader. -->
<!-- AgentSettings analog: config.py l.355-384 (AliasChoices int fields). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Window-config AgentSettings + pure-Python aggregate helpers + window dataclasses</name>
  <files>src/phaze/config.py, src/phaze/services/analysis.py, tests/test_services/test_analysis.py, tests/test_config/test_agent_settings_windows.py</files>
  <read_first>
    - src/phaze/config.py (l.355-384 — AgentSettings int-field + AliasChoices copy-source)
    - .planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-RESEARCH.md (Code Examples l.275-304 — exact aggregate reductions; Pitfall 2 l.241-246 — drop conf==0.0 windows from aggregate)
    - .planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-PATTERNS.md (l.233-250 — AgentSettings 3-field copy shape)
    - tests/test_services/test_analysis.py (existing essentia-mock conventions; aggregate tests need NO mock)
  </read_first>
  <behavior>
    - aggregate_bpm([w(bpm=120), w(bpm=124), w(bpm=121)]) == round(median, 1) == 121.0; empty -> None.
    - aggregate_key with duration-weighted windows returns the key with the most total duration.
    - aggregate_dominant(coarse, "mood") returns the time-weighted dominant label; empty -> None.
    - aggregate_danceability returns mean; empty -> None.
    - AgentSettings exposes analysis_fine_window_sec=30, analysis_coarse_window_sec=180, analysis_fine_min_sec=15, overridable via PHAZE_ANALYSIS_* env vars.
  </behavior>
  <action>
    In config.py AgentSettings add three int fields with AliasChoices exactly per PATTERNS l.233-250: `analysis_fine_window_sec` (default 30, alias PHAZE_ANALYSIS_FINE_WINDOW_SEC), `analysis_coarse_window_sec` (default 180, alias PHAZE_ANALYSIS_COARSE_WINDOW_SEC), `analysis_fine_min_sec` (default 15, alias PHAZE_ANALYSIS_FINE_MIN_SEC), each with a description. In services/analysis.py add lightweight window value containers (a `FineWindow`/`CoarseWindow` dataclass or NamedTuple with the fields from the data model) and the four pure-Python aggregate helpers (`aggregate_bpm` median, `aggregate_key` duration-weighted Counter mode, `aggregate_dominant` time-weighted for mood/style, `aggregate_danceability` mean) per RESEARCH l.275-304. Per RESEARCH Pitfall 2, exclude `confidence == 0.0` fine windows (and None bpm) from the BPM aggregate while still recording the raw window. Write the aggregate unit tests (NO essentia mock needed — cheapest high-value coverage, `-k aggregate`) and config-field tests in tests/test_config/test_agent_settings_windows.py.
  </action>
  <acceptance_criteria>
    - `grep -nE "analysis_fine_window_sec|analysis_coarse_window_sec|analysis_fine_min_sec" src/phaze/config.py` shows all three with `AliasChoices("PHAZE_ANALYSIS_...")`.
    - `grep -nE "def aggregate_bpm|def aggregate_key|def aggregate_dominant|def aggregate_danceability" src/phaze/services/analysis.py` shows all four.
    - `uv run pytest tests/test_services/test_analysis.py -k aggregate -x` passes WITHOUT mocking essentia.
    - `uv run pytest tests/test_config/test_agent_settings_windows.py -x` passes.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_services/test_analysis.py -k aggregate tests/test_config/test_agent_settings_windows.py -x</automated>
  </verify>
  <done>Three window-config fields exist; four aggregate reductions implemented + pure-Python tested; window containers defined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite analyze_file body to segmented per-window decode (the crash/OOM fix)</name>
  <files>src/phaze/services/analysis.py, tests/test_services/test_analysis.py</files>
  <read_first>
    - src/phaze/services/analysis.py (l.230-276 — the whole-file MonoLoader body being REPLACED; l.149-153 _predict_single to KEEP; l.15-18 TF_CPP_MIN_LOG_LEVEL env to keep)
    - .planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-RESEARCH.md (Pattern 1 l.166-199 window loop + trailing-partial drop; Pattern 2 l.189-199 failure isolation; Pitfall 4 l.255-261 two-pass vs Resample; Pitfall 5 l.263-266 keep classifier cache; Pitfall 6 l.268-272 no danceability column on analysis)
    - .planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-PATTERNS.md (l.164-204 — rewrite-in-place copy-source)
    - .planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-01-SUMMARY.md (LOCKED decode strategy — EasyLoader-primary vs decode+Resample-hybrid)
  </read_first>
  <behavior>
    - analyze_file over a mocked-essentia file produces fine windows at [0,30),[30,60)... and coarse windows at [0,180),[180,360)...
    - A trailing window shorter than analysis_fine_min_sec (15s) is dropped, EXCEPT window 0 (very short tracks keep one window).
    - When RhythmExtractor2013 raises on one window, that window is logged + skipped and the remaining windows + aggregates still return (failure isolation, `-k failure_isolation`).
    - The return dict contains all existing aggregate keys PLUS "windows": a flat list of fine + coarse window dicts.
    - No `es.MonoLoader(...)` call decodes the whole file (the two original calls are gone).
  </behavior>
  <action>
    Rewrite ONLY the body of analyze_file (keep its signature, synchronicity, and all module-level helpers/caches). Determine total duration via `es.MetadataReader`/`es.AudioLoader` or ffprobe (NEVER MonoLoader). Implement the fine pass: loop 30s windows via the locked decode strategy at sampleRate=44100, drop the trailing window if `(end-start) < analysis_fine_min_sec and idx > 0`, run `es.RhythmExtractor2013(method="multifeature")` + `es.KeyExtractor(profileType="edma")` per buffer, build FineWindow records. Implement the coarse pass: loop 180s windows at sampleRate=16000 (or decode-44.1k+`es.Resample(44100,16000)` if Plan 01 locked the hybrid), run the existing MODEL_SETS via `_predict_single` (reuse `_classifier_cache` — inference-only, per Pitfall 5), apply `derive_mood`/`derive_style` per window, build CoarseWindow records (incl. `features` JSONB). Wrap each window's essentia calls in `try/except Exception` + `log.warning(..., exc_info=True)` + `continue` (`# noqa: BLE001`) so one bad window never fails the file (Pattern 2). Derive the four aggregates via the Task 1 helpers. Return `{"bpm":..., "musical_key":..., "mood":..., "style":..., "danceability":..., "features":..., "windows": [...fine dicts..., ...coarse dicts...]}` — keep the existing aggregate keys (note Pitfall 6: `danceability` still funnels into `features` on the analysis row downstream; the per-window danceability lives on the window). Each window dict must carry `tier, window_index, start_sec, end_sec` + its tier-specific fields, ready for AnalysisWindowPayload(**w). Add window-boundary + failure-isolation tests mocking `EasyLoader`/`RhythmExtractor2013`/`KeyExtractor` the same way the existing test mocks essentia.
  </action>
  <acceptance_criteria>
    - `grep -c "MonoLoader(filename=file_path" src/phaze/services/analysis.py` is 0 for whole-file calls (the two original whole-file decodes removed; MonoLoader may remain only if used for a bounded final-window case).
    - `grep -nE "EasyLoader|Resample" src/phaze/services/analysis.py` shows the locked decode primitive in use.
    - `grep -nE "# noqa: BLE001" src/phaze/services/analysis.py` shows per-window isolation try/except.
    - `grep -nE "\"windows\"" src/phaze/services/analysis.py` shows the new return key.
    - `uv run pytest tests/test_services/test_analysis.py -k "window_boundaries or failure_isolation" -x` passes.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_services/test_analysis.py -x</automated>
  </verify>
  <done>analyze_file decodes per-window with the locked strategy, isolates window failures, produces two-tier windows + aggregates, and the whole-file MonoLoader calls are gone; service tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted audio bytes -> essentia decode | Archive files decoded inside the ProcessPool worker; `original_path` is already containment-checked against scan_roots upstream. |
| worker memory | Multi-hour / 11 GB files must not exhaust RAM under 8x concurrency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04-01 | Denial of Service (memory exhaustion) | analyze_file decode | mitigate | Per-window segmented decode holds only one ~5-11 MB buffer per pass — never the whole file (RESEARCH measured flat RSS). Validated by Plan 01 spike before this rewrite. HIGH-severity OOM surface bounded. |
| T-31-04-02 | Denial of Service (crash/retry churn) | RhythmExtractor2013 on long audio | mitigate | No buffer exceeds one 30s window -> OnsetDetectionGlobal never overflows; per-window try/except isolates any residual failure so it can't dead-letter the file. |
| T-31-04-03 | Tampering (malformed audio) | EasyLoader on untrusted file | accept | Existing analysis threat surface; essentia handles decode errors; a failing window is logged + skipped, not fatal. |
| T-31-04-SC | Tampering | dependency installs | mitigate | Zero new packages (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `uv run pytest tests/test_services/test_analysis.py tests/test_config/test_agent_settings_windows.py -x` green.
- `uv run mypy src/phaze/services/analysis.py src/phaze/config.py` clean.
- `grep -c` gate confirms whole-file MonoLoader decodes removed.
</verification>

<success_criteria>
- analyze_file produces two-tier windows + four locked aggregates, per-window-isolated, with the spike-locked decode strategy and no whole-file decode.
- Window sizes configurable via the three new AgentSettings.
- Aggregate reductions unit-tested without essentia.
</success_criteria>

<output>
Create `.planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-04-SUMMARY.md` when done.
</output>
