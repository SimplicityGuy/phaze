---
phase: 31-windowed-time-series-audio-analysis
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/models/analysis.py
  - alembic/versions/018_add_analysis_window_table.py
  - tests/test_models/test_analysis_window.py
  - tests/test_migrations/test_migration_018.py
autonomous: true
requirements: [ANL-01]

must_haves:
  truths:
    - "A new analysis_window table exists with a 1:many FK to files (ON DELETE CASCADE)"
    - "The schema supports cross-archive queries (files that ever exceed N BPM) via a partial index on bpm WHERE tier='fine'"
    - "Deleting a file cascades to delete its analysis_window rows (no orphans)"
  artifacts:
    - path: "src/phaze/models/analysis.py"
      provides: "AnalysisWindow(TimestampMixin, Base) ORM model"
      contains: "class AnalysisWindow"
    - path: "alembic/versions/018_add_analysis_window_table.py"
      provides: "Additive create-table + indexes migration"
      contains: "revision"
  key_links:
    - from: "AnalysisWindow.file_id"
      to: "files.id"
      via: "ForeignKey(ondelete=CASCADE), index=True, NOT unique (1:many)"
      pattern: "ForeignKey.*files.id.*ondelete=.?CASCADE"
    - from: "alembic 018"
      to: "017"
      via: "down_revision bare-number string"
      pattern: "down_revision.*=.*\"017\""
---

<objective>
Add the `AnalysisWindow` ORM model (child of `files`, 1:many, `ON DELETE CASCADE`) and an additive Alembic migration `018` that creates the `analysis_window` table plus its query indexes. This is the queryable child-table half of locked decision "Storage = option B" and the source of the new cross-archive queryability requirement.

Purpose: per-window time-series rows must be persistable and queryable ("files that ever exceed N BPM", filter/sort by mood/style) without touching the existing 1:1 `analysis` aggregate row.

Output: `AnalysisWindow` in `models/analysis.py`, migration `018`, model + migration tests. No data migration — the table is new and empty (0 rows).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-PATTERNS.md
@.planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-RESEARCH.md
@src/phaze/models/analysis.py
@src/phaze/models/base.py

<interfaces>
<!-- From 31-PATTERNS.md (copy-source). AnalysisResult lives in models/analysis.py l.12-25. -->
<!-- TimestampMixin (models/base.py:24-28) auto-adds created_at/updated_at — do NOT redeclare. -->
<!-- Base.metadata naming convention (models/base.py:9-21) auto-generates index/FK names. -->
<!-- New columns: id UUID PK (default=uuid.uuid4); file_id UUID FK files.id ondelete=CASCADE, index=True, NOT unique; -->
<!--   tier String; window_index int; start_sec/end_sec Float; -->
<!--   FINE: bpm Float|None, musical_key String(10)|None; -->
<!--   COARSE: mood String(50)|None, style String(50)|None, danceability Float|None, features JSONB|None -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AnalysisWindow model + CASCADE on AnalysisResult.file_id</name>
  <files>src/phaze/models/analysis.py, tests/test_models/test_analysis_window.py</files>
  <read_first>
    - src/phaze/models/analysis.py (AnalysisResult l.12-25 — exact same-file analog: TimestampMixin/Base order, default=uuid.uuid4 PK, Float/String/JSONB usage)
    - src/phaze/models/base.py (l.9-28 — naming convention + TimestampMixin)
    - .planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-PATTERNS.md (l.26-56 — the copy-this model snippet incl. the CASCADE FK form)
    - tests/test_models/test_core_models.py (existing model-test conventions for this repo)
  </read_first>
  <behavior>
    - AnalysisWindow(TimestampMixin, Base) instantiates with tier="fine", window_index=0, start_sec/end_sec, bpm, musical_key and persists.
    - AnalysisWindow.file_id FK is NOT unique (two windows can share a file_id) and is indexed.
    - created_at/updated_at are auto-provided by TimestampMixin (not redeclared).
  </behavior>
  <action>
    Add `class AnalysisWindow(TimestampMixin, Base)` to src/phaze/models/analysis.py with `__tablename__ = "analysis_window"`. Columns exactly per the interfaces block: `id` UUID PK `default=uuid.uuid4`; `file_id` UUID `ForeignKey("files.id", ondelete="CASCADE")`, `index=True`, `nullable=False`, NOT `unique`; `tier` String; `window_index` int; `start_sec`/`end_sec` Float; fine-only `bpm` Float|None + `musical_key` String(10)|None; coarse-only `mood` String(50)|None + `style` String(50)|None + `danceability` Float|None + `features` JSONB|None. Reuse the existing imports (Float, ForeignKey, String, JSONB, UUID, Mapped, mapped_column). Also ADD `ondelete="CASCADE"` to the existing `AnalysisResult.file_id` ForeignKey per the locked CASCADE decision (keep its `unique=True`). Write tests/test_models/test_analysis_window.py asserting instantiation, non-unique indexed file_id, and that TimestampMixin columns exist. Do NOT redeclare created_at/updated_at.
  </action>
  <acceptance_criteria>
    - `grep -n "class AnalysisWindow(TimestampMixin, Base)" src/phaze/models/analysis.py` matches.
    - `grep -nE "ForeignKey\(\"files.id\", ondelete=\"CASCADE\"\)" src/phaze/models/analysis.py` matches for BOTH AnalysisWindow.file_id and AnalysisResult.file_id.
    - AnalysisWindow.file_id mapped_column has `index=True` and NO `unique=True`.
    - `uv run pytest tests/test_models/test_analysis_window.py -x` passes.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_models/test_analysis_window.py -x</automated>
  </verify>
  <done>AnalysisWindow model exists with the locked columns + CASCADE FK; AnalysisResult.file_id also gains CASCADE; model test green.</done>
</task>

<task type="auto">
  <name>Task 2: Additive Alembic migration 018 — create analysis_window + indexes</name>
  <files>alembic/versions/018_add_analysis_window_table.py, tests/test_migrations/test_migration_018.py</files>
  <read_first>
    - alembic/versions/017_add_scan_batches_last_progress_at.py (l.26-37 — revision header shape; revision="018", down_revision="017", BARE NUMBERS not long names)
    - alembic/versions/007_add_fingerprint_results_table.py (l.23-43 — child-table create_table + op.f() constraint names + downgrade drop order)
    - alembic/versions/012_add_agents_table_and_backfill.py (l.104-110 — postgresql_where partial-index idiom)
    - .planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-PATTERNS.md (l.59-105 — composed copy-source for this migration)
    - tests/test_migrations/ (existing migration-test conventions)
  </read_first>
  <action>
    Create alembic/versions/018_add_analysis_window_table.py. Header: `revision = "018"`, `down_revision = "017"` (bare numbers — RESEARCH/PATTERNS correction: do NOT use the long `017_add_...` form). `upgrade()`: `op.create_table("analysis_window", ...)` with all columns from Task 1 (id PK, file_id UUID NOT NULL, tier String, window_index Integer, start_sec/end_sec Float, bpm Float, musical_key String(10), mood String(50), style String(50), danceability Float, features JSONB, created_at/updated_at with server_default now()); `sa.ForeignKeyConstraint(["file_id"], ["files.id"], name=op.f(...), ondelete="CASCADE")`; `sa.PrimaryKeyConstraint("id", name=op.f(...))`. Then create indexes: composite `op.create_index("ix_analysis_window_file_tier_idx", "analysis_window", ["file_id","tier","window_index"])`; partial `ix_analysis_window_bpm_fine` on `["bpm"]` with `postgresql_where=sa.text("tier = 'fine'")`; partial `ix_analysis_window_dance_coarse` on `["danceability"]` with `postgresql_where=sa.text("tier = 'coarse'")`; plain indexes on `["mood"]` and `["style"]`. `downgrade()`: drop the indexes then `op.drop_table("analysis_window")` (mirror 007 l.40-43). NO data migration. Add tests/test_migrations/test_migration_018.py asserting revision/down_revision strings and that upgrade creates the table + the partial indexes (follow the existing migration-test pattern in tests/test_migrations/).
  </action>
  <acceptance_criteria>
    - `grep -nE '^revision.*=.*"018"' alembic/versions/018_add_analysis_window_table.py` matches and `grep -nE 'down_revision.*=.*"017"'` matches.
    - `grep -c "postgresql_where=sa.text(\"tier = 'fine'\")" alembic/versions/018_add_analysis_window_table.py` is 1 (the bpm partial index).
    - `grep -nE 'ondelete=.?CASCADE' alembic/versions/018_add_analysis_window_table.py` matches.
    - `uv run alembic heads` reports a single head (018) — no branch.
    - `uv run pytest tests/test_migrations/test_migration_018.py -x` passes.
  </acceptance_criteria>
  <verify>
    <automated>uv run alembic heads 2>&1 | grep -q "018" && uv run pytest tests/test_migrations/test_migration_018.py -x</automated>
  </verify>
  <done>Migration 018 creates analysis_window + composite/partial/label indexes additively; single alembic head; migration test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application server -> PostgreSQL | DDL/schema change; no external input crosses here in this plan. |
| file deletion -> child rows | A file delete must not orphan analysis_window rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02-01 | Integrity (orphaned children) | analysis_window.file_id FK | mitigate | `ON DELETE CASCADE` on the FK (locked decision) — deleting a file removes its windows; asserted by model/migration. |
| T-31-02-02 | Tampering (bad migration / branch) | alembic 018 head | mitigate | Bare-number `down_revision="017"`; `alembic heads` single-head check prevents an unmerged branch. |
| T-31-02-SC | Tampering | dependency installs | mitigate | Zero new packages (RESEARCH Package Legitimacy Audit — no install). |
</threat_model>

<verification>
- `uv run alembic upgrade head` then `uv run alembic downgrade -1` round-trips cleanly (additive create/drop).
- `uv run pytest tests/test_models/test_analysis_window.py tests/test_migrations/test_migration_018.py -x` green.
- `uv run mypy src/phaze/models/analysis.py` clean.
</verification>

<success_criteria>
- analysis_window table + the 5 indexes (1 composite, 2 partial, 2 label) exist after migration.
- Single alembic head (018) on top of 017.
- CASCADE FK present on both AnalysisWindow.file_id and AnalysisResult.file_id.
</success_criteria>

<output>
Create `.planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-02-SUMMARY.md` when done.
</output>
