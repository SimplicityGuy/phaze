---
phase: 31-windowed-time-series-audio-analysis
plan: 05
type: execute
wave: 3
depends_on: ["31-03", "31-04"]
files_modified:
  - src/phaze/tasks/functions.py
  - src/phaze/routers/pipeline.py
  - tests/test_tasks/test_functions.py
  - tests/test_routers/test_pipeline.py
autonomous: true
requirements: [ANL-01]

must_haves:
  truths:
    - "process_file forwards the per-window time-series from analyze_file to the API via the windows payload"
    - "process_file still does NOT import phaze.database / phaze.models / sqlalchemy (import-boundary preserved)"
    - "A long-file analysis job no longer churns 4 full re-analyses on failure (retries lowered, generous/unbounded timeout)"
  artifacts:
    - path: "src/phaze/tasks/functions.py"
      provides: "windows=[AnalysisWindowPayload(**w) ...] built from analyze_file return dict"
      contains: "AnalysisWindowPayload"
    - path: "src/phaze/routers/pipeline.py"
      provides: "process_file enqueue with explicit timeout=0 + bounded retries"
      contains: "timeout=0"
  key_links:
    - from: "analyze_file return dict 'windows'"
      to: "AnalysisWritePayload.windows"
      via: "[AnalysisWindowPayload(**w) for w in analysis.get('windows', [])] — built from plain dict, NOT ORM"
      pattern: "AnalysisWindowPayload\\(\\*\\*"
    - from: "process_file enqueue (pipeline.py)"
      to: "SAQ job policy"
      via: "explicit timeout=0, retries=2 (avoids apply_project_job_defaults clobber of retries==1)"
      pattern: "timeout=0"
---

<objective>
Wire the new per-window output end-to-end: `process_file` builds the `windows` list from `analyze_file`'s plain-dict return and includes it in the `AnalysisWritePayload` PUT; and the `process_file` enqueue sites are tuned so a single bad window/long file no longer triggers four full re-analyses (lower retries, generous/unbounded timeout).

Purpose: this closes the loop — Plan 04 produces windows, Plan 03 persists them, and this plan transports them — plus it removes the retry-churn that compounded the original incident (4x CPU per long file before dead-lettering).

Output: extended `process_file` payload build (preserving the D-25 import-boundary), job-policy tuning at the `process_file` enqueue sites, and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-PATTERNS.md
@.planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-RESEARCH.md
@src/phaze/tasks/functions.py
@src/phaze/routers/pipeline.py
@tests/test_task_split.py

<interfaces>
<!-- process_file payload build: tasks/functions.py l.114-148 (keep _features_to_mood_dict/_features_to_style_dict l.62-111). -->
<!-- D-25 import boundary (module docstring + tests/test_task_split.py): tasks/functions.py MUST NOT import -->
<!--   phaze.database / phaze.models / sqlalchemy — build windows from the plain analyze_file dict, NOT ORM objects. -->
<!-- AnalysisWindowPayload from schemas/agent_analysis.py (Plan 03). -->
<!-- process_file enqueue sites: routers/pipeline.py l.67, l.92-96, l.221-225 (Phase 30 enqueue_router path). -->
<!-- Precedent for explicit timeout=0: routers/pipeline_scans.py:415-423 (scan_directory, incident 260609-glv). -->
<!-- GOTCHA: apply_project_job_defaults (queue_defaults.py l.62-86) sets retries=worker_max_retries(4) ONLY when -->
<!--   job.retries == _SAQ_DEFAULT_RETRIES (==1). So passing retries=1 is CLOBBERED to 4. Pass retries=2 to stay in -->
<!--   the locked 1-2 band without being overridden. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: process_file builds the windows payload from the analyze_file dict</name>
  <files>src/phaze/tasks/functions.py, tests/test_tasks/test_functions.py</files>
  <read_first>
    - src/phaze/tasks/functions.py (l.114-148 — existing PUT payload build to extend; l.62-111 _features_to_mood_dict/_features_to_style_dict to keep; l.38-44 _load_analyze_file deferred-import guard; module docstring D-25 import ban)
    - .planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-PATTERNS.md (l.208-229 — exact extend-in-place snippet + the import-boundary constraint)
    - tests/test_task_split.py (the import-boundary gate that MUST keep passing)
    - tests/test_tasks/test_functions.py (existing process_file test harness — analyze_file + api client mocked)
  </read_first>
  <behavior>
    - Given analyze_file returns a dict with "windows": [fine_dict, coarse_dict], process_file calls api.put_analysis with AnalysisWritePayload whose windows == [AnalysisWindowPayload(fine), AnalysisWindowPayload(coarse)].
    - When analyze_file returns no "windows" key, windows defaults to [] (or None) and the existing aggregate fields are still sent.
    - tasks/functions.py still does NOT import phaze.database / phaze.models / sqlalchemy (test_task_split stays green).
  </behavior>
  <action>
    Extend the existing AnalysisWritePayload construction in process_file (l.132-147) to add `windows=[AnalysisWindowPayload(**w) for w in analysis.get("windows", [])]`, importing `AnalysisWindowPayload` from phaze.schemas.agent_analysis (a schema/pydantic import — allowed; NOT an ORM/database import). Build each payload from the plain dict `w` (NOT from any ORM object) so the D-25 import boundary is preserved. Keep the `_features_to_mood_dict`/`_features_to_style_dict` aggregate->wire-dict conversion for the aggregate fields unchanged. Extend tests/test_tasks/test_functions.py to assert the windows are forwarded and shaped correctly.
  </action>
  <acceptance_criteria>
    - `grep -nE "windows=\\[AnalysisWindowPayload\\(\\*\\*w\\) for w in analysis.get\\(\"windows\"" src/phaze/tasks/functions.py` matches.
    - `grep -nE "import (phaze.database|phaze.models|sqlalchemy)" src/phaze/tasks/functions.py` returns NOTHING.
    - `uv run pytest tests/test_task_split.py -x` still passes (import boundary intact).
    - `uv run pytest tests/test_tasks/test_functions.py -x` passes including the windows-forwarded assertion.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_tasks/test_functions.py tests/test_task_split.py -x</automated>
  </verify>
  <done>process_file forwards the per-window time-series via AnalysisWritePayload.windows built from plain dicts; import boundary preserved; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lower process_file job churn — explicit timeout=0 + bounded retries at enqueue sites</name>
  <files>src/phaze/routers/pipeline.py, tests/test_routers/test_pipeline.py</files>
  <read_first>
    - src/phaze/routers/pipeline.py (l.45-96 + l.221-225 — the three process_file enqueue sites via the Phase-30 enqueue_router path)
    - src/phaze/routers/pipeline_scans.py (l.415-423 — explicit timeout=0 enqueue precedent, incident 260609-glv)
    - src/phaze/tasks/_shared/queue_defaults.py (l.62-86 — apply_project_job_defaults; note retries==1 is clobbered to worker_max_retries)
    - tests/test_routers/test_pipeline.py (existing enqueue-assertion harness)
  </read_first>
  <behavior>
    - Every process_file enqueue passes timeout=0 (unbounded — long sets legitimately run many minutes; consistent with the prior bulk-scan decision).
    - Every process_file enqueue passes retries=2 (within the locked 1-2 band; retries=1 would be silently overridden to 4 by apply_project_job_defaults — see gotcha).
    - The resulting enqueued job carries timeout=0 and retries=2 after the before_enqueue hook runs.
  </behavior>
  <action>
    At each of the three process_file enqueue sites in routers/pipeline.py (l.67 direct `queue.enqueue("process_file", ...)` and the two `enqueue_router`/routed-queue sites near l.92-96 and l.221-225), pass explicit `timeout=0` and `retries=2`, mirroring pipeline_scans.py:423. Use `retries=2` (NOT 1) because apply_project_job_defaults only fills jobs still at the SAQ default `retries==1`, so an explicit `retries=1` is clobbered to worker_max_retries(4) — `retries=2` is honored and stays in the locked 1-2 band. Document this gotcha in a brief inline comment referencing queue_defaults.py. Add/extend tests in tests/test_routers/test_pipeline.py asserting the enqueue kwargs (timeout=0, retries=2) for process_file at all sites, and (if the harness allows) that the hook leaves retries=2 intact.
  </action>
  <acceptance_criteria>
    - `grep -c "timeout=0" src/phaze/routers/pipeline.py` is >= the number of process_file enqueue sites (3).
    - `grep -nE "retries=2" src/phaze/routers/pipeline.py` matches at the process_file enqueue sites (NOT retries=1).
    - `uv run pytest tests/test_routers/test_pipeline.py -x` passes including the timeout=0/retries=2 assertions.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_routers/test_pipeline.py -x</automated>
  </verify>
  <done>process_file enqueues with timeout=0 + retries=2 at all sites; no more 4x re-analysis churn; tests assert the policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent worker -> internal API | process_file PUTs analysis (existing bearer-auth client path). |
| control plane -> SAQ/Redis | enqueue policy (timeout/retries) for process_file jobs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05-01 | Denial of Service (retry churn) | process_file job policy | mitigate | retries lowered to 2 (from 4) so a window-isolated failure cannot trigger four full multi-hour re-analyses (the original incident's CPU churn); timeout=0 stops the fixed-timeout kill-then-retry loop. |
| T-31-05-02 | Tampering (import-boundary regression) | tasks/functions.py | mitigate | windows built from plain dicts, no ORM import; tests/test_task_split.py gate keeps phaze.database/models/sqlalchemy out of the worker import graph (D-25). |
| T-31-05-SC | Tampering | dependency installs | mitigate | Zero new packages (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `uv run pytest tests/test_tasks/test_functions.py tests/test_task_split.py tests/test_routers/test_pipeline.py -x` green.
- `uv run mypy src/phaze/tasks/functions.py src/phaze/routers/pipeline.py` clean.
- Import-boundary gate (test_task_split) still passing.
</verification>

<success_criteria>
- process_file transports the per-window time-series without breaking the D-25 import boundary.
- process_file enqueues with timeout=0 + retries=2 at every site.
</success_criteria>

<output>
Create `.planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-05-SUMMARY.md` when done.
</output>
