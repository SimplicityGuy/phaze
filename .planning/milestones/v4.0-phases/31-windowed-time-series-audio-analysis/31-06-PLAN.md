---
phase: 31-windowed-time-series-audio-analysis
plan: 06
type: execute
wave: 2
depends_on: ["31-02"]
files_modified:
  - src/phaze/routers/proposals.py
  - src/phaze/templates/proposals/proposal_row.html
  - src/phaze/templates/proposals/partials/analysis_timeline.html
  - tests/test_routers/test_proposals.py
autonomous: true
requirements: [ANL-01]

must_haves:
  truths:
    - "A review-list row shows a compact server-rendered SVG BPM sparkline + an expand control"
    - "Clicking expand HTMX-loads a multi-lane timeline (BPM polyline + key/mood/style ribbons) for that file's windows"
    - "The timeline endpoint queries analysis_window scoped strictly by file_id, behind the existing admin auth"
    - "essentia-derived label strings (key/mood/style) render escaped — no SVG/HTML injection"
  artifacts:
    - path: "src/phaze/templates/proposals/partials/analysis_timeline.html"
      provides: "Multi-lane SVG/CSS timeline fragment (no charting lib)"
      min_lines: 25
    - path: "src/phaze/routers/proposals.py"
      provides: "HTMX timeline fragment endpoint querying AnalysisWindow by file_id"
      contains: "AnalysisWindow"
  key_links:
    - from: "proposal_row.html expand control"
      to: "timeline endpoint"
      via: "hx-get into a hidden sibling row (proposal_row.html:74-84 pattern)"
      pattern: "hx-get=.*timeline"
    - from: "timeline endpoint"
      to: "analysis_window rows"
      via: "select(AnalysisWindow).where(file_id==...).order_by(tier, window_index)"
      pattern: "AnalysisWindow.file_id =="
---

<objective>
Add the review-UI half of the feature: a compact BPM sparkline + expand control in each proposal review row, and an HTMX-loaded multi-lane timeline fragment (BPM `<polyline>` + width-proportional colored key/mood/style ribbons) rendered entirely server-side in SVG/CSS — no JS charting library. A new endpoint returns the fragment for a file's `analysis_window` series.

Purpose: makes the new time-series visible/reviewable. Note: the UI-SPEC step was intentionally skipped for this phase (`--skip-ui`), so this plan builds the UI directly from the design spec's UI section + the PATTERNS.md HTMX lazy-expand analog.

Output: timeline endpoint + fragment template + sparkline-in-row, with admin auth, file_id scoping, and XSS-safe label rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-PATTERNS.md
@docs/superpowers/specs/2026-06-10-windowed-analysis-design.md
@src/phaze/routers/proposals.py
@src/phaze/templates/proposals/proposal_row.html
@src/phaze/templates/proposals/row_detail.html

<interfaces>
<!-- HTMX lazy-expand analog (EXACT structural match): -->
<!--   routers/proposals.py::row_detail l.147-161 (endpoint shape, TemplateResponse, get_session) -->
<!--   templates/proposals/proposal_row.html:74-84 (hx-get into hidden sibling <tr>) -->
<!--   templates/proposals/row_detail.html:30-40 (fragment un-hides its parent <tr>) -->
<!-- AnalysisWindow model from Plan 02 (models/analysis.py); fields: tier, window_index, start_sec, end_sec, -->
<!--   bpm, musical_key (fine), mood, style, danceability, features (coarse). -->
<!-- Router setup: proposals.py:23-25 (TEMPLATES_DIR, Jinja2Templates). Same admin auth as the rest of the review UI. -->
<!-- NO charting analog exists — build SVG from scratch: numeric-only geometry attributes, autoescaped labels. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Timeline fragment endpoint + multi-lane SVG/CSS template</name>
  <files>src/phaze/routers/proposals.py, src/phaze/templates/proposals/partials/analysis_timeline.html, tests/test_routers/test_proposals.py</files>
  <read_first>
    - src/phaze/routers/proposals.py (l.23-25 templates setup; l.147-161 row_detail endpoint analog; l.69-72 HX-Request split convention; existing admin-auth dependency on the router)
    - .planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-PATTERNS.md (l.254-290 — endpoint + fragment analogs; "SVG/CSS timeline — NO direct analog" build-from-scratch guidance + security note)
    - docs/superpowers/specs/2026-06-10-windowed-analysis-design.md (UI section l.91-94 — BPM polyline + flexed colored ribbons proportional to window duration)
    - src/phaze/models/analysis.py (AnalysisWindow fields — added by Plan 02)
  </read_first>
  <action>
    Add an endpoint to routers/proposals.py (e.g. `GET /proposals/{proposal_id}/timeline` or `/files/{file_id}/timeline`, response_class=HTMLResponse) that resolves the file_id, queries `select(AnalysisWindow).where(AnalysisWindow.file_id == file_id).order_by(AnalysisWindow.tier, AnalysisWindow.window_index)`, and returns `templates.TemplateResponse(name="proposals/partials/analysis_timeline.html", ...)`. It MUST sit behind the SAME admin auth as the rest of the review UI (reuse the router's existing auth dependency) and scope strictly by file_id (RESEARCH V4 access-control). Import AnalysisWindow from phaze.models.analysis. Create templates/proposals/partials/analysis_timeline.html rendering: a BPM `<svg>` with a single `<polyline>` whose `points` are computed from fine windows mapped onto a shared time axis (numeric-only coordinate attributes); then key/mood/style lanes as horizontal rows of flexed colored `<div>` ribbons whose width is proportional to `(end_sec - start_sec)` and whose label text (musical_key/mood/style) is rendered through normal Jinja `{{ ... }}` autoescaping (NEVER `| safe` on essentia-derived strings). Empty-state: render a "no analysis windows" message when the series is empty. Add tests to tests/test_routers/test_proposals.py: 200 + fragment for a file with windows; 200 + empty-state for none; an XSS test passing a window with a `mood` like `<script>alert(1)</script>` and asserting the response body contains the escaped form (`&lt;script&gt;`), not the raw tag.
  </action>
  <acceptance_criteria>
    - `grep -nE "AnalysisWindow.file_id == file_id" src/phaze/routers/proposals.py` matches (file_id scoping).
    - `grep -nE "<polyline" src/phaze/templates/proposals/partials/analysis_timeline.html` matches.
    - `grep -c "| safe" src/phaze/templates/proposals/partials/analysis_timeline.html` is 0 for any label-text usage (no unescaped essentia label).
    - `uv run pytest tests/test_routers/test_proposals.py -k "timeline" -x` passes, including the `&lt;script&gt;` escaping assertion.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_routers/test_proposals.py -k "timeline" -x</automated>
  </verify>
  <done>Timeline endpoint queries windows by file_id behind admin auth; SVG/CSS fragment renders BPM polyline + proportional ribbons with escaped labels; endpoint + XSS tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Compact BPM sparkline + HTMX expand control in the review row</name>
  <files>src/phaze/templates/proposals/proposal_row.html, src/phaze/routers/proposals.py, tests/test_routers/test_proposals.py</files>
  <read_first>
    - src/phaze/templates/proposals/proposal_row.html (l.74-84 — exact hx-get-into-hidden-sibling-row expand pattern to copy)
    - src/phaze/templates/proposals/row_detail.html (l.30-40 — fragment-reveal self-unhide script, copy if using hidden-sibling-row)
    - .planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-PATTERNS.md (l.277-290 — expand-trigger analog + sparkline build note)
    - src/phaze/routers/proposals.py (the list/row context build — to pass fine-window BPMs or aggregate for the inline sparkline)
  </read_first>
  <action>
    Add to proposal_row.html a compact inline SVG BPM sparkline (a small fixed-size `<svg>` with a `<polyline>` over the file's fine-window BPMs, or a flat baseline if only the aggregate is available) and an expand control that copies the proposal_row.html:74-84 pattern: `hx-get="/proposals/{{ proposal.id }}/timeline"` with `hx-target="#timeline-{{ proposal.id }}"`, `hx-swap="innerHTML"`, plus a hidden sibling `<tr id="timeline-{{ proposal.id }}" class="hidden">`. If the row context lacks fine-window BPMs, extend the proposals list query/context in proposals.py minimally to supply them (or render the sparkline from the aggregate bpm as a degenerate single-point/flat line). Keep all label text autoescaped. Add a test asserting the row renders the sparkline `<svg>` and the `hx-get=".../timeline"` expand control.
  </action>
  <acceptance_criteria>
    - `grep -nE "hx-get=\"/proposals/.*timeline" src/phaze/templates/proposals/proposal_row.html` matches.
    - `grep -nE "id=\"timeline-" src/phaze/templates/proposals/proposal_row.html` matches (hidden sibling target).
    - `grep -c "<svg" src/phaze/templates/proposals/proposal_row.html` is >= 1 (the sparkline).
    - `uv run pytest tests/test_routers/test_proposals.py -k "row or sparkline or timeline" -x` passes.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_routers/test_proposals.py -x</automated>
  </verify>
  <done>Review row shows a BPM sparkline + expand control that lazy-loads the timeline fragment via HTMX; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser -> review UI | The timeline endpoint must enforce the same admin auth as the rest of the review UI. |
| analysis_window data -> rendered HTML/SVG | essentia-derived label strings (key/mood/style) flow into the page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-06-01 | Tampering / XSS | analysis_timeline.html label rendering | mitigate | All essentia-derived label text rendered via Jinja2 autoescaping (no `| safe`); SVG geometry attributes are numeric-only. Asserted by the `&lt;script&gt;` escaping test. |
| T-31-06-02 | Information Disclosure / Broken Access Control | timeline endpoint | mitigate | Endpoint reuses the review UI's existing admin auth dependency and scopes strictly by file_id (ASVS V4). |
| T-31-06-SC | Tampering | dependency installs | mitigate | Zero new packages — SVG/CSS only, no charting lib (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `uv run pytest tests/test_routers/test_proposals.py -x` green (timeline + sparkline + XSS escaping).
- `uv run mypy src/phaze/routers/proposals.py` clean.
- Manual (per VALIDATION Manual-Only): load review list, confirm sparkline; click expand, confirm multi-lane timeline fragment loads.
</verification>

<success_criteria>
- Compact sparkline + expand control in each review row.
- HTMX timeline fragment renders BPM polyline + proportional key/mood/style ribbons from a file's windows.
- Admin-auth + file_id scoping + autoescaped labels enforced.
</success_criteria>

<output>
Create `.planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-06-SUMMARY.md` when done.
</output>
