---
phase: 31-windowed-time-series-audio-analysis
plan: 03
type: execute
wave: 2
depends_on: ["31-02"]
files_modified:
  - src/phaze/schemas/agent_analysis.py
  - src/phaze/routers/agent_analysis.py
  - tests/test_schemas/test_agent_analysis.py
  - tests/test_routers/test_agent_analysis.py
autonomous: true
requirements: [ANL-01]

must_haves:
  truths:
    - "An agent can PUT a file's analysis aggregates AND its window time-series in one request"
    - "Re-PUTing the same windows REPLACES them (no duplicate child rows) — PUT stays idempotent"
    - "An aggregate-only PUT (windows omitted) does NOT wipe existing windows (partial-PUT preserved)"
    - "Oversized or malformed window payloads are rejected at the wire boundary"
  artifacts:
    - path: "src/phaze/schemas/agent_analysis.py"
      provides: "AnalysisWindowPayload + windows field on AnalysisWritePayload"
      contains: "class AnalysisWindowPayload"
    - path: "src/phaze/routers/agent_analysis.py"
      provides: "put_analysis child-row replace (delete-by-file_id + bulk pg_insert)"
      contains: "AnalysisWindow"
  key_links:
    - from: "put_analysis"
      to: "analysis_window rows"
      via: "delete(AnalysisWindow).where(file_id==path_file_id) then pg_insert, guarded on body.windows is not None"
      pattern: "delete\\(AnalysisWindow\\).*file_id"
    - from: "AnalysisWritePayload.windows"
      to: "AnalysisWindowPayload"
      via: "list[...] | None = None default (partial-PUT)"
      pattern: "windows.*list.*AnalysisWindowPayload.*None"
---

<objective>
Extend the agent wire contract and the `PUT /api/internal/agent/analysis/{file_id}` handler so an agent submits per-window time-series alongside the existing aggregates, and so the PUT idempotently REPLACES a file's `analysis_window` rows (delete-by-`file_id` + bulk insert in the same transaction). Aggregate-only PUTs must not touch windows (partial-PUT contract preserved).

Purpose: this is the persistence path that turns Plan 04's per-window output into queryable rows, while keeping the existing aggregate upsert and partial-PUT semantics exactly as today.

Output: `AnalysisWindowPayload` + `windows` field in `schemas/agent_analysis.py`; child-replace logic appended to `put_analysis`; schema round-trip + router idempotency tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-PATTERNS.md
@.planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-RESEARCH.md
@src/phaze/schemas/agent_analysis.py
@src/phaze/routers/agent_analysis.py
@src/phaze/models/analysis.py

<interfaces>
<!-- AnalysisWritePayload lives in schemas/agent_analysis.py l.21-31 (model_config extra="forbid"). -->
<!-- AnalysisWindow ORM model added by Plan 02 (models/analysis.py). -->
<!-- Existing upsert: routers/agent_analysis.py l.117-132 (pg_insert + on_conflict_do_update, explicit id=uuid.uuid4 stamp). -->
<!-- Auth (unchanged): agent: Annotated[Agent, Depends(get_authenticated_agent)]; agent_id NEVER from body (AUTH-01). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AnalysisWindowPayload + windows field to AnalysisWritePayload</name>
  <files>src/phaze/schemas/agent_analysis.py, tests/test_schemas/test_agent_analysis.py</files>
  <read_first>
    - src/phaze/schemas/agent_analysis.py (l.21-31 — AnalysisWritePayload analog: ConfigDict(extra="forbid"), Field(default=None, ge=...))
    - .planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-PATTERNS.md (l.109-124 — exact copy patterns + bounds)
    - .planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-RESEARCH.md (Security Domain V5 + DoS threat row — tier Literal, numeric ge guards, bounded list length)
    - tests/test_schemas/test_agent_analysis.py (existing round-trip test conventions)
  </read_first>
  <behavior>
    - AnalysisWindowPayload(tier="fine", window_index=0, start_sec=0.0, end_sec=30.0, bpm=120.0, musical_key="C major") round-trips via model_dump()/model_validate().
    - tier outside {"fine","coarse"} raises ValidationError.
    - window_index < 0 or start_sec < 0 raises ValidationError.
    - Extra/unknown fields raise (extra="forbid").
    - AnalysisWritePayload with windows omitted leaves windows None (partial-PUT).
  </behavior>
  <action>
    Add `class AnalysisWindowPayload(BaseModel)` to schemas/agent_analysis.py with `model_config = ConfigDict(extra="forbid")`; fields: `tier: Literal["fine", "coarse"]`; `window_index: int = Field(ge=0)`; `start_sec: float = Field(ge=0.0)`; `end_sec: float = Field(ge=0.0)`; fine-optional `bpm: float | None = Field(default=None, ge=0.0)` + `musical_key: str | None = None`; coarse-optional `mood: str | None = None`, `style: str | None = None`, `danceability: float | None = Field(default=None, ge=0.0, le=1.0)`, `features: dict | None = None`. Add to AnalysisWritePayload: `windows: list[AnalysisWindowPayload] | None = Field(default=None, max_length=50000)` (the `| None` default preserves partial-PUT; `max_length` bounds the DoS-via-huge-bulk-insert threat — a 24h file at 30s windows is ~2,880 fine windows, so 50000 is generous). Extend tests/test_schemas/test_agent_analysis.py with `-k window` round-trip + rejection cases.
  </action>
  <acceptance_criteria>
    - `grep -n "class AnalysisWindowPayload" src/phaze/schemas/agent_analysis.py` matches.
    - `grep -nE "tier:\\s*Literal\\[\"fine\", \"coarse\"\\]" src/phaze/schemas/agent_analysis.py` matches.
    - `grep -nE "windows:\\s*list\\[AnalysisWindowPayload\\] \\| None" src/phaze/schemas/agent_analysis.py` matches with `max_length=`.
    - `uv run pytest tests/test_schemas/test_agent_analysis.py -k window -x` passes.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_schemas/test_agent_analysis.py -k window -x</automated>
  </verify>
  <done>AnalysisWindowPayload + bounded windows field exist; tier-Literal + numeric guards enforced; round-trip + rejection tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: put_analysis replaces child rows idempotently (delete + bulk insert)</name>
  <files>src/phaze/routers/agent_analysis.py, tests/test_routers/test_agent_analysis.py</files>
  <read_first>
    - src/phaze/routers/agent_analysis.py (l.117-132 — existing aggregate upsert to KEEP; l.51-113 _summarize_dict_to_string + features funnel to leave untouched; l.67-73 auth deps)
    - .planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-PATTERNS.md (l.128-161 — exact child-replace snippet, guarded on body.windows is not None)
    - .planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-RESEARCH.md (l.306-323 — idempotent child-row replace; explicit id=uuid.uuid4 stamp since pg_insert bypasses Python default)
    - tests/test_routers/test_agent_analysis.py (existing PUT test harness + auth fixtures)
  </read_first>
  <behavior>
    - PUT with windows=[w0,w1] inserts exactly 2 analysis_window rows for the path file_id.
    - Re-PUT with windows=[w0',w1',w2'] results in exactly 3 rows (old 2 deleted, not duplicated) — idempotent replace.
    - PUT with windows omitted (None) leaves existing windows untouched (partial-PUT).
    - PUT with windows=[] deletes all windows for the file (explicit empty replace).
    - The DELETE is scoped strictly to the PATH file_id (no cross-file deletion).
  </behavior>
  <action>
    In put_analysis, AFTER the existing aggregate `pg_insert(AnalysisResult)` upsert and BEFORE the final `await session.commit()`, append the child-replace: `if body.windows is not None:` -> `await session.execute(delete(AnalysisWindow).where(AnalysisWindow.file_id == file_id))` then, if `body.windows` is non-empty, `await session.execute(pg_insert(AnalysisWindow).values([{"id": uuid.uuid4(), "file_id": file_id, **w.model_dump()} for w in body.windows]))`. Use the PATH `file_id` (never any id from the body) for both the delete predicate and each row's file_id (cross-file-deletion mitigation, AUTH-01). Keep the single transaction (one commit covers aggregate upsert + child replace). Import `delete` from sqlalchemy and `AnalysisWindow` from phaze.models.analysis. Leave the aggregate upsert + features funnel exactly as-is. Add idempotency + partial-PUT + path-scoping tests to tests/test_routers/test_agent_analysis.py (`-k analysis_window_idempotent`).
  </action>
  <acceptance_criteria>
    - `grep -nE "delete\\(AnalysisWindow\\).where\\(AnalysisWindow.file_id == file_id\\)" src/phaze/routers/agent_analysis.py` matches.
    - The child-replace block is guarded by `if body.windows is not None:` (grep confirms the guard precedes the delete).
    - Each inserted row dict stamps `"id": uuid.uuid4()` and `"file_id": file_id` (path id, not body).
    - `uv run pytest tests/test_routers/test_agent_analysis.py -k analysis_window_idempotent -x` passes, including the re-PUT-replaces (no duplicate) assertion and the windows-omitted-preserves assertion.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_routers/test_agent_analysis.py -k "window or idempotent" -x</automated>
  </verify>
  <done>put_analysis idempotently replaces a file's windows in the same transaction as the aggregate upsert; partial-PUT preserved; delete scoped to path file_id; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent (bearer auth) -> internal API | `PUT /api/internal/agent/analysis/{file_id}` already on the agent auth boundary; body is untrusted, agent_id never from body. |
| API -> PostgreSQL bulk insert | Window list is bulk-inserted; size/shape must be bounded before the DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03-01 | Tampering (cross-file deletion) | put_analysis DELETE predicate | mitigate | DELETE/insert use the PATH `file_id` only; body carries no file/window-owner id (AUTH-01). Path-scoping asserted by test. |
| T-31-03-02 | Denial of Service (oversized bulk insert) | AnalysisWritePayload.windows | mitigate | `max_length=50000` on the windows list + per-field `ge` guards in pydantic (`extra="forbid"`). HIGH-severity DoS surface bounded at the wire boundary. |
| T-31-03-03 | Spoofing (forged agent) | PUT endpoint auth | accept | Unchanged existing `Depends(get_authenticated_agent)` bearer auth — no new surface (ASVS V2). |
| T-31-03-SC | Tampering | dependency installs | mitigate | Zero new packages (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `uv run pytest tests/test_schemas/test_agent_analysis.py tests/test_routers/test_agent_analysis.py -x` green.
- `uv run mypy src/phaze/schemas/agent_analysis.py src/phaze/routers/agent_analysis.py` clean.
- Re-PUT idempotency test proves no duplicate child rows.
</verification>

<success_criteria>
- Wire contract carries windows with bounded length + tier Literal + numeric guards.
- put_analysis replaces windows idempotently, scoped to the path file_id, in one transaction.
- Aggregate-only partial PUT leaves windows intact.
</success_criteria>

<output>
Create `.planning/milestones/v4.0-phases/31-windowed-time-series-audio-analysis/31-03-SUMMARY.md` when done.
</output>
