---
phase: 35-pipeline-determinism-idempotency-per-job-type-observability
plan: 04
type: execute
wave: 2
depends_on: ["35-01", "35-03"]
files_modified:
  - src/phaze/templates/base.html
  - src/phaze/routers/pipeline.py
  - src/phaze/templates/pipeline/partials/stats_bar.html
  - tests/test_pipeline_dag_context.py
autonomous: true
requirements: [OBSERV]

must_haves:
  truths:
    - "The dashboard full-page render seeds every per-node store key from the server context (no binding reads undefined before the first poll)"
    - "The 5s /pipeline/stats poll re-pushes fresh per-node counts via OOB-swapped x-init seeds using the SAME ids as the full-page seed"
    - "Per-node done-counts come from get_stage_progress (DB-truth) reconciled with the maintained counters; in-flight/busy comes from get_queue_activity"
    - "The dashboard read never 500s when Redis counters or queue depth are unavailable (degrade gracefully)"
  artifacts:
    - path: "src/phaze/templates/base.html"
      provides: "$store.pipeline extended with all per-node sub-keys, all seeded to 0"
      contains: "Alpine.store('pipeline'"
    - path: "src/phaze/routers/pipeline.py"
      provides: "dashboard() + pipeline_stats_partial() contexts carrying per-node done/total/active + counters"
      contains: "get_stage_progress"
  key_links:
    - from: "src/phaze/routers/pipeline.py pipeline_stats_partial"
      to: "get_stage_progress + read_counters + get_queue_activity"
      via: "context dict consumed by stats_bar.html OOB seeds"
      pattern: "get_stage_progress"
    - from: "src/phaze/templates/pipeline/partials/stats_bar.html"
      to: "$store.pipeline per-node keys"
      via: "hx-swap-oob x-init seeds gated behind oob_counts"
      pattern: "hx-swap-oob"
---

<objective>
Plumb the per-job-type observability data into the existing Phase-34 store + poll mechanism so the DAG canvas (35-05) has live, seeded data to bind to. Extend `$store.pipeline` with per-node sub-keys (all seeded on full-page render), and populate both the `dashboard()` full-page context and the `pipeline_stats_partial()` 5s-poll context from `get_stage_progress` (35-03, DB-truth), the maintained counters (35-01), and the existing `get_queue_activity`. Reuse the exact OOB-swap seed pattern (no new poll loop, no SSE).

Purpose: Separating data plumbing from the SVG canvas keeps each within budget. This plan delivers the store + context contract; 35-05 consumes it to render nodes.
Output: Extended `$store.pipeline`, both router contexts carrying per-node data, and the OOB seed paragraphs in `stats_bar.html` that re-push counts on every poll.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-UI-SPEC.md
@.planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-PATTERNS.md

<interfaces>
<!-- Store to extend (single line) -->
From src/phaze/templates/base.html:97:
  Alpine.store('pipeline', { discovered: 0, analyzed: 0, metadataExtracted: 0, agentBusy: 0, controllerBusy: 0 });

<!-- The two router contexts to populate -->
From src/phaze/routers/pipeline.py:
  async def dashboard(...) -> templates.TemplateResponse(name="pipeline/dashboard.html", context=...)         # full-page seed (:151,:195)
  async def pipeline_stats_partial(...) -> templates.TemplateResponse(name="pipeline/partials/stats_bar.html", context={... "oob_counts": True ...})  # 5s poll (:199,:212)

<!-- Data sources (from 35-01 + 35-03 + existing) -->
From src/phaze/services/pipeline.py:
  async def get_stage_progress(session) -> dict[str, dict[str,int|None]]    # 35-03 — per-node DB-truth done/total
  async def get_queue_activity(app_state, session) -> dict[str,int]         # existing — agent_busy/controller_busy/agent_active/controller_active
From src/phaze/services/pipeline_counters.py:
  async def read_counters(redis) -> dict[str, dict[str,int]]                # 35-01 — enqueued/completed per function

<!-- The established OOB-seed contract to mirror EXACTLY -->
From src/phaze/templates/pipeline/partials/stats_bar.html:39-59:
  {% if oob_counts %}
  <p id="agent-busy-seed" hx-swap-oob="true" x-init="$store.pipeline.agentBusy = {{ agent_busy }}" class="hidden"></p>
  ...   # OOB seeds fire ONLY on the poll (oob_counts truthy); omitted on the full-page include to avoid duplicate ids
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend $store.pipeline with all per-node sub-keys (seeded)</name>
  <files>src/phaze/templates/base.html</files>
  <read_first>
    - src/phaze/templates/base.html:70-100 (the existing Alpine.store('pipeline', {...}) and store('theme') for the registration pattern)
    - .planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-UI-SPEC.md ("Store extension" — the exact per-node sub-key list)
  </read_first>
  <action>
    Extend the `Alpine.store('pipeline', {...})` object literal at base.html:97 to add every per-node sub-key from the UI-SPEC "Store extension": `metadataDone, metadataTotal, fingerprintDone, fingerprintTotal, analyzeDone, analyzeTotal, analyzeActive, tracklistDone, scrapeDone, scrapeTotal, matchDone, matchTotal, proposalsDone, proposalsTotal, approved, executedDone, executedTotal` alongside the existing `discovered, analyzed, metadataExtracted, agentBusy, controllerBusy`. Initialize EVERY key to 0 so no binding reads `undefined` before the first poll. Do not remove the existing keys (the Phase-34 gating still reads discovered/analyzed/agentBusy/controllerBusy). Note the topology correction (UI-SPEC line 243): Fingerprint gates on `discovered`, not `metadataExtracted` — the store keeps both; the gating change is applied in 35-05.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pipeline_dag_context.py -k store -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -o "Alpine.store('pipeline'.*});" src/phaze/templates/base.html` contains analyzeActive, metadataDone, proposalsTotal, executedDone (sample of the new keys)
    - No existing key removed: `grep -c "discovered:\|analyzed:\|agentBusy:\|controllerBusy:" src/phaze/templates/base.html` ≥ 1
    - Every new key initialized to 0 (no key left undefined)
    - Dashboard page still renders 200 in the existing render test
  </acceptance_criteria>
  <done>$store.pipeline carries all per-node sub-keys, each seeded to 0, with the Phase-34 keys preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Populate dashboard() + pipeline_stats_partial() contexts + stats_bar OOB seeds</name>
  <files>src/phaze/routers/pipeline.py, src/phaze/templates/pipeline/partials/stats_bar.html, tests/test_pipeline_dag_context.py</files>
  <read_first>
    - src/phaze/routers/pipeline.py:151-220 (dashboard() + pipeline_stats_partial() — existing context build, the oob_counts flag, how get_queue_activity/queue_progress_percent feed today)
    - src/phaze/templates/pipeline/partials/stats_bar.html (full file — the oob_counts gate + hx-swap-oob x-init seed pattern to mirror for the new per-node seeds)
    - src/phaze/services/pipeline.py (get_stage_progress from 35-03, get_queue_activity)
    - src/phaze/services/pipeline_counters.py (read_counters from 35-01)
    - .planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-UI-SPEC.md ("Live-Update Behavior" + "Counter / Denominator Display Rule" — which value feeds which store key; Scan/Search uses the maintained enqueued counter as its only soft total)
  </read_first>
  <action>
    In BOTH `dashboard()` and `pipeline_stats_partial()`: call `get_stage_progress(session)` (DB-truth done/total per node) and `read_counters(redis)` (35-01, reconcile + the Scan/Search soft-denominator) and the existing `get_queue_activity(app_state, session)` (agent_active/controller_active for the per-node ACTIVE state). Build a per-node context mapping each store key to its value: `done` from get_stage_progress (authority); `total` from get_stage_progress except Scan/Search where `total` stays the em-dash sentinel and the maintained `enqueued` counter may render as a soft hint; `analyzeActive`/busy from get_queue_activity. Reconcile-on-read (D-03): the displayed done is the DB-truth count, counters are the cache/backstop. Obtain the redis handle from `app.state.controller_queue.redis` (or the wired handle) and wrap the counter read in try/except → degrade to the DB values, never 500 the poll (mirror get_queue_activity isolation). Pass `oob_counts=True` only from `pipeline_stats_partial()` (already the case).

    NOTE — role of the maintained `completed` counter (plan-checker W4): `read_counters` returns BOTH `enqueued` and `completed` per function, but NO node's rendered `done` is driven by `completed`. Every node renders `done` from `get_stage_progress` (DB-truth) per D-03. The `completed` counter (maintained by 35-01's `increment_completed` to satisfy D-02's "maintained per-function counters") is deliberately a reconcile/backstop cache, NOT a rendered value — it is NOT dead code. Wire it as a DOCUMENTED degrade-fallback: when a per-node DB stage source in get_stage_progress has degraded to `done == 0` (its try/except fired) AND the matching `completed` counter is > 0, render the `completed` value as the fallback `done` (and mark the node soft/stale if the UI-SPEC defines such a state). When the DB source is healthy, DB-truth always wins. This makes `completed` a real backstop the verifier can exercise, satisfying D-02 while preserving D-03's DB-as-authority rule.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pipeline_dag_context.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "get_stage_progress" src/phaze/routers/pipeline.py` ≥ 1 (called in both contexts)
    - `grep -c "hx-swap-oob" src/phaze/templates/pipeline/partials/stats_bar.html` increased to cover the new per-node seeds (one per new store key)
    - `uv run pytest tests/test_pipeline_dag_context.py` passes — including the forced-counter-failure-still-200 test AND a test exercising the `completed` degrade-fallback (DB stage source degraded to 0 → completed counter renders as fallback done)
    - `curl`-style render test: GET /pipeline/stats returns the OOB seed ids; GET /pipeline (dashboard) seeds the same keys in the full-page context
    - `uv run mypy .` clean; `uv run ruff check .` clean
  </acceptance_criteria>
  <done>Both router contexts carry per-node done/total/active from get_stage_progress + read_counters + get_queue_activity with failure isolation; the maintained `completed` counter is wired as a documented degrade-fallback (not dead code); stats_bar.html emits matching OOB seeds on the poll; the dashboard read never 500s on a counter/queue outage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → FastAPI dashboard | Unauthenticated at app layer (reverse-proxy realm); receives per-node counts |
| dashboard poll → Postgres + Redis | get_stage_progress (DB) + read_counters (Redis) + get_queue_activity (Redis) every 5s |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-09 | Denial of Service / Availability | counter/queue read inside the 5s poll | mitigate | The counter read is wrapped in per-source try/except degrading to DB-truth values (mirror get_queue_activity); a Redis outage never 500s the poll |
| T-35-08 | Information Disclosure | per-node counts in context/poll | accept | Internal admin dashboard behind reverse-proxy realm (Phase 33 precedent); aggregate counts only |
| T-35-11 | Tampering (XSS) | counts injected into x-init / template | mitigate | Values are server-computed integers rendered through Jinja autoescape; no user-controlled string reaches the template |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | Template + router code only; no package installs |
</threat_model>

<verification>
- `uv run pytest` full suite green; phase-module coverage ≥85%
- `uv run mypy .` and `uv run ruff check .` clean; `pre-commit run --all-files` passes
- /pipeline and /pipeline/stats both render 200 with all per-node keys seeded; counter outage degrades to DB-truth without 500
</verification>

<success_criteria>
- $store.pipeline has every per-node key, seeded on full-page render and refreshed by the OOB poll
- Both router contexts carry DB-truth done/total reconciled with maintained counters + queue activity
- The maintained `completed` counter serves as a documented degrade-fallback (D-02 satisfied, D-03 authority preserved)
- The store-key + OOB-seed-id contract is documented for 35-05 to mirror in the static canvas
</success_criteria>

<output>
Create `.planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-04-SUMMARY.md` when done — INCLUDE the exact per-node store-key list and OOB seed ids so 35-05 mirrors them in the full-page canvas seeds.
</output>
