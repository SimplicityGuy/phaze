---
phase: 35-pipeline-determinism-idempotency-per-job-type-observability
plan: 05
type: execute
wave: 3
depends_on: ["35-04"]
files_modified:
  - src/phaze/templates/pipeline/partials/dag_canvas.html
  - src/phaze/templates/pipeline/dashboard.html
  - src/phaze/templates/pipeline/partials/stage_cards.html
  - src/phaze/templates/pipeline/partials/processing_card.html
  - tests/test_dag_canvas_render.py
autonomous: true
requirements: [OBSERV]

must_haves:
  truths:
    - "The pipeline dashboard renders a single 9-node SVG DAG; the Phase-34 stage_cards.html and processing_card.html are removed"
    - "Edges are drawn from node anchor points computed from one layout map — Metadata and Analyze converge into Proposals; Fingerprint and the tracklist subgraph do NOT (edge honesty visually verifiable)"
    - "Each node shows live count + per-stage bar + state pill + a trigger gated by upstream deps AND agent/controller-busy, bound to $store.pipeline (no new poll)"
    - "Triggers POST only to EXISTING endpoints (no net-new trigger endpoint); a < sm viewport falls back to a topologically-ordered <ol> that is also the screen-reader text equivalent"
  artifacts:
    - path: "src/phaze/templates/pipeline/partials/dag_canvas.html"
      provides: "SVG edge layer + 9 stage-chip nodes + states/gating + full-page store seeds + stacked <ol> fallback"
      contains: "Pipeline stage graph"
    - path: "src/phaze/templates/pipeline/dashboard.html"
      provides: "DAG canvas included; legacy stage_cards/processing_card includes removed"
      contains: "dag_canvas.html"
  key_links:
    - from: "dag_canvas.html node triggers"
      to: "/pipeline/analyze, /pipeline/extract-metadata, /pipeline/fingerprint, /pipeline/proposals, /proposals/"
      via: "hx-post to existing endpoints + :disabled store gates"
      pattern: "hx-post=\"/pipeline/"
    - from: "dag_canvas.html bindings"
      to: "$store.pipeline per-node keys"
      via: "x-text / :style width / :disabled reading the store"
      pattern: "\\$store.pipeline"
---

<objective>
Render the single coherent pipeline DAG (D-01, sketch 001 Variant B) that REPLACES the Phase-34 layout. Author the SVG node-edge canvas per the approved UI-SPEC: 9 stage chips with live counts/bars/state pills, anchor-derived bézier edges that honestly show Analyze+Metadata→Proposals (and NOT Fingerprint/tracklist→Proposals), per-node gated triggers wired to the EXISTING endpoints, dark-mode classes throughout, and a stacked `<ol>` that doubles as the < sm fallback and the screen-reader text equivalent. Remove `stage_cards.html` + `processing_card.html`.

Purpose: This is the operator-facing payoff — one graph showing the parallel pipeline tiers advancing independently, with honest topology and self-healing counts.
Output: `dag_canvas.html`, the updated `dashboard.html` shell, the two legacy partials removed, and render tests asserting edge honesty + gating + the denominator em-dash rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-UI-SPEC.md
@.planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-STAGE-DEPENDENCIES.md
@.planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-PATTERNS.md
@.planning/sketches/001-pipeline-dag-view/README.md
@.planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-04-SUMMARY.md

<interfaces>
<!-- The TWO load-bearing analog patterns (copy mechanics, author SVG fresh) -->
From src/phaze/templates/pipeline/partials/stage_cards.html:12-33:
  per-button trigger: x-data="{ loading: false }" + @click="loading = true" + hx-post + *-response slot + :disabled store gate + disabled:opacity-50
From src/phaze/templates/pipeline/partials/stats_bar.html:39-59:
  static-vs-OOB split: full-page in-place x-init seeds; poll OOB seeds reuse the SAME ids gated behind oob_counts

<!-- Store keys + OOB seed ids to mirror in the full-page seeds — from 35-04 SUMMARY -->
  $store.pipeline: discovered, metadataDone/metadataTotal, fingerprintDone/fingerprintTotal,
                   analyzeDone/analyzeTotal/analyzeActive, tracklistDone, scrapeDone/scrapeTotal,
                   matchDone/matchTotal, proposalsDone/proposalsTotal, approved, executedDone/executedTotal,
                   agentBusy, controllerBusy, analyzed, metadataExtracted

<!-- Existing trigger endpoints (NO net-new endpoint — deferred-ideas constraint) -->
  POST /pipeline/analyze · POST /pipeline/extract-metadata · POST /pipeline/fingerprint · POST /pipeline/proposals
  <a href="/proposals/"> (Execute node = navigational) · existing scan trigger (Discovery Rescan)

<!-- dashboard.html includes to change (remove the two legacy partials, add the canvas) -->
From src/phaze/templates/pipeline/dashboard.html:
  :20 {% include "pipeline/partials/processing_card.html" %}   ← REMOVE
  :23-24 #pipeline-stats div (5s poll, includes stats_bar.html)  ← KEEP (still carries OOB seeds)
  :29 {% include "pipeline/partials/stage_cards.html" %}        ← REMOVE, replace with dag_canvas.html
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: dag_canvas.html — layout map, anchor-derived SVG edges, 9 node chips</name>
  <files>src/phaze/templates/pipeline/partials/dag_canvas.html</files>
  <read_first>
    - .planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-UI-SPEC.md (FULL — Node anatomy, "The 9 Nodes + Topology", "Edge construction contract", color palette, spacing, typography, dark mode)
    - .planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-STAGE-DEPENDENCIES.md (the authoritative node/edge topology + tiers)
    - .planning/sketches/001-pipeline-dag-view/README.md (Variant B intent; variant-b-graph-canvas.png is the dark reference)
    - src/phaze/templates/pipeline/partials/stage_cards.html (node chip + trigger mechanics analog)
    - src/phaze/templates/base.html (phaze color tokens: bg-phaze-bg/panel/border, dark: variant, Inter/font, focus-ring conventions)
  </read_first>
  <action>
    Author `dag_canvas.html`. Define ONE source-of-truth layout map (Jinja context dict OR an inline JS const) `NODE_LAYOUT = { "discovery": {x,y,w,h}, "metadata": {...}, "analyze": {...}, "fingerprint": {...}, "scan_search": {...}, "scrape": {...}, "match": {...}, "proposals": {...}, "execute": {...} }` using the suggested column layout (col0 Discovery · col1 {Metadata, Analyze, Fingerprint, Scan/Search stacked} · col2 {Proposals, Scrape} · col3 {Execute, Match}). Emit the SVG `<svg>` edge layer ONCE: for each edge in the authoritative edge list (Discovery→{Metadata,Analyze,Fingerprint,Scan/Search}; Metadata→Proposals; Analyze→Proposals; Proposals→Execute; Scan/Search→Scrape; Scrape→Match) compute the cubic-bézier `d` from `source.rightCenter → target.leftCenter` using `M {sx},{sy} C {sx+Δ},{sy} {tx-Δ},{ty} {tx},{ty}` with Δ≈60 — DERIVED from the layout map, never hand-typed coords. EDGE HONESTY: NO edge from Fingerprint or the tracklist subgraph into Proposals — only Metadata and Analyze converge there. Mark the SVG `aria-hidden="true"` and the canvas container `role="group" aria-label="Pipeline stage graph"`.

    Render the 9 node chips (full layout for Discovery/Analyze/Proposals/Execute; compact for Metadata/Fingerprint/Scan-Search; Scrape/Match compact display-only). Each chip: 3px stage-color top-stripe (cyan/sky/amber/teal/purple/green/rose per the palette table), stage-name label (12px/600 stage color), state pill, count (18px/600 tabular-nums) bound `x-text` to the store done key, denominator `/ {total}` EXCEPT Scan/Search which renders a literal `—` em-dash with NO determinate bar (Counter/Denominator Display Rule), and a 5px progress bar with `:style` width = done/total %. Every node carries BOTH light and `dark:` classes. Add the full-page in-place store seeds (`x-init="$store.pipeline.X = {{ ctx.X }}"`) for EVERY per-node key — mirroring the OOB seed ids documented in 35-04 SUMMARY so the page is correct pre-first-poll. Do NOT author triggers/gating yet (Task 2).
  </action>
  <verify>
    <automated>uv run pytest tests/test_dag_canvas_render.py -k "topology or render" -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "aria-label=\"Pipeline stage graph\"" src/phaze/templates/pipeline/partials/dag_canvas.html` ≥ 1
    - Edge-honesty source assertion: the template's edge list contains `metadata`→`proposals` and `analyze`→`proposals` but NOT `fingerprint`→`proposals` nor any tracklist node→`proposals` (verifiable by grep on the edge-list definition)
    - All 9 node ids present; Scan/Search renders `—` (em-dash) and no determinate bar width
    - Every node has a `dark:` class; counts use `tabular-nums`
    - Full-page store seeds present for every per-node key (mirrors 35-04 OOB ids)
    - `uv run pytest tests/test_dag_canvas_render.py -k "topology or render"` passes
  </acceptance_criteria>
  <done>dag_canvas.html renders the 9-node graph with anchor-derived SVG edges (honest topology), stage-colored chips, store-bound counts/bars, the Scan/Search em-dash rule, dark-mode classes, and full-page seeds.</done>
</task>

<task type="auto">
  <name>Task 2: Node states, gated triggers, OOB-stable bindings, stacked <ol> fallback</name>
  <files>src/phaze/templates/pipeline/partials/dag_canvas.html</files>
  <read_first>
    - .planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-UI-SPEC.md ("Node States", "Trigger Gating Contract" + LOCKED disabled-reason strings, "Live-Update Behavior", "Dark Mode/Responsive/Accessibility", "Copywriting Contract")
    - src/phaze/templates/pipeline/partials/stage_cards.html:12-33 (the x-data loading + @click + hx-post + *-response + :disabled gate to reuse)
    - dag_canvas.html (the node frames from Task 1)
  </read_first>
  <action>
    Add the four node states (complete/active/idle/disabled) driven by store values: pill text (`DONE`/`{N} ACTIVE`/`READY`/`WAITING`/`GATED`), the active-state stage-color ring+glow on the in-flight node, and `opacity-60` on disabled. Color is NEVER the only signal — include the state pill text + a status icon (check/spinner-pulse/lock/clock).

    Wire triggers to EXISTING endpoints only (deferred-ideas: no net-new endpoint): Discovery `Rescan Files`→existing scan POST; Extract Metadata→`POST /pipeline/extract-metadata`; Analyze→`POST /pipeline/analyze`; Fingerprint→`POST /pipeline/fingerprint`; Proposals→`POST /pipeline/proposals`; Execute→`<a href="/proposals/">` (navigational). Scrape/Match are display-only (no button). Scan/Search `Scan Tracklists` renders the interactive trigger ONLY if a bulk pipeline-level endpoint already exists; if only per-file endpoints exist, render it display-only and OMIT the `Scan Tracklists` label (confirm which path applies before emitting the button). Apply the LOCKED `:disabled` predicates per the gating table: Metadata/Analyze/Fingerprint/Scan-Search → `loading || store.discovered === 0 || store.agentBusy > 0`; Proposals → `loading || store.analyzed === 0 || store.controllerBusy > 0`; Execute → `store.approved === 0`. TOPOLOGY CORRECTION (UI-SPEC line 243): Fingerprint gates on `store.discovered` (NOT `metadataExtracted`). Disabled button label IS the reason string (LOCKED copy: `No files discovered`/`Agent busy`/`Controller busy`/`Waiting on Analyze`/`Needs proposals`/`Needs tracklist`); add an informative `:aria-label` that appends the reason when disabled. Keep the existing per-button `*-response` HTMX slot and the `Couldn't enqueue. Retry.` inline error. Reuse `x-data="{ loading:false }"` + `@click="loading=true"`. The SVG layer + node frames + buttons render ONCE (never OOB-swapped — only the store-bound counts/pills/bars/`:disabled` update via the existing 5s poll).

    Author the stacked `<ol>` (topological order Discovery→Metadata→Analyze→Fingerprint→Scan/Search→Scrape→Match→Proposals→Execute), each `<li>` stating name, `done / total` (or `done / —`), state, and disabled reason. Make it `sr-only` at ≥ sm and visible at < sm (the SVG canvas hidden < sm), so it is BOTH the phone fallback AND the screen-reader text equivalent. Ensure DOM order of trigger elements equals topological order (Tab traversal), focus ring `focus-visible:ring-2 focus-visible:ring-blue-500`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dag_canvas_render.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hx-post=\"/pipeline/" src/phaze/templates/pipeline/partials/dag_canvas.html` covers extract-metadata, analyze, fingerprint, proposals (no other POST targets); no net-new endpoint path appears
    - Fingerprint `:disabled` reads `store.discovered` (NOT `metadataExtracted`): `grep -n "Fingerprint" -A8 src/phaze/templates/pipeline/partials/dag_canvas.html` shows the discovered gate
    - All LOCKED disabled-reason strings + state-pill strings present verbatim
    - The stacked `<ol>` exists with `sr-only` at ≥ sm and 9 `<li>` items in topological order
    - `uv run pytest tests/test_dag_canvas_render.py` passes
  </acceptance_criteria>
  <done>Nodes show all four states (text+icon+color), triggers gate on the LOCKED predicates (Fingerprint on discovered) wired to existing endpoints only, the <ol> serves as fallback + text equivalent, and bindings update via the existing poll without swapping buttons/SVG.</done>
</task>

<task type="auto">
  <name>Task 3: Swap dashboard.html to the canvas, remove legacy partials, render tests</name>
  <files>src/phaze/templates/pipeline/dashboard.html, src/phaze/templates/pipeline/partials/stage_cards.html, src/phaze/templates/pipeline/partials/processing_card.html, tests/test_dag_canvas_render.py</files>
  <read_first>
    - src/phaze/templates/pipeline/dashboard.html (the includes: processing_card :20, pipeline-stats div :23-24, stage_cards :29)
    - src/phaze/templates/pipeline/partials/stats_bar.html (confirm it still carries the per-node OOB seeds from 35-04 — KEEP the #pipeline-stats poll div)
    - grep for any other references to stage_cards.html / processing_card.html before deleting (templates, routers, tests)
  </read_first>
  <action>
    In `dashboard.html` remove the `{% include "pipeline/partials/processing_card.html" %}` (:20) and the `{% include "pipeline/partials/stage_cards.html" %}` (:29); add `{% include "pipeline/partials/dag_canvas.html" %}` in the stage_cards slot. KEEP the `#pipeline-stats` 5s-poll div including `stats_bar.html` (it now carries the per-node OOB seeds from 35-04 — the canvas is static, the seeds drive the live updates). Delete the files `stage_cards.html` and `processing_card.html` (D-01 removal) AFTER confirming no remaining template/router/test references them (grep first; update any stragglers).

    Extend `tests/test_dag_canvas_render.py` to the integration level: GET /pipeline returns 200 and the response contains the DAG canvas (`aria-label="Pipeline stage graph"`), all 9 node labels, and NO `processing_card`/`stage_cards` markers; the edge-honesty assertion (Metadata+Analyze→Proposals, not Fingerprint/tracklist); the Scan/Search em-dash; and that GET /pipeline/stats still returns the per-node OOB seeds. Assert the two legacy partial files no longer exist.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dag_canvas_render.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/phaze/templates/pipeline/partials/stage_cards.html && test ! -f src/phaze/templates/pipeline/partials/processing_card.html` (both removed)
    - `grep -rc "stage_cards.html\|processing_card.html" src/phaze/templates src/phaze/routers` == 0 (no dangling references)
    - `grep -c "dag_canvas.html" src/phaze/templates/pipeline/dashboard.html` == 1
    - `uv run pytest tests/test_dag_canvas_render.py` passes; GET /pipeline renders the DAG 200; GET /pipeline/stats still emits per-node OOB seeds
    - Full suite: `uv run pytest` green; `uv run mypy .` clean; `uv run ruff check .` clean; `pre-commit run --all-files` passes
  </acceptance_criteria>
  <done>dashboard.html renders the DAG canvas with the legacy stage_cards/processing_card partials removed and no dangling references; the 5s poll still feeds per-node OOB seeds; integration render tests prove topology + removal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → FastAPI pipeline dashboard | Unauthenticated at app layer (reverse-proxy realm); renders the DAG + trigger buttons |
| DAG trigger button → existing pipeline endpoints | hx-post crosses to enqueue endpoints (Phase 34, already present) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-10 | Tampering / Elevation of Privilege | DAG trigger surface | mitigate | Nodes POST ONLY to existing Phase-34 endpoints (analyze/extract-metadata/fingerprint/proposals) and navigate to /proposals/; NO net-new trigger endpoint is introduced — verify no new unauthenticated trigger surface (planning_context security note) |
| T-35-11 | Tampering (XSS) | counts/labels rendered into the template | mitigate | All dynamic values are server-computed integers via Jinja autoescape; SVG `d` strings are derived from a server-side layout map, not user input |
| T-35-08 | Information Disclosure | pipeline counts in the canvas | accept | Internal admin dashboard behind reverse-proxy realm (Phase 33 precedent); non-sensitive aggregates |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | Templates + tests only; no package installs, no CDN dependency change (self-hosted Tailwind/HTMX/Alpine already vendored) |
</threat_model>

<verification>
- `uv run pytest` full suite green; phase-module coverage ≥85%
- `uv run mypy .` and `uv run ruff check .` clean; `pre-commit run --all-files` passes
- GET /pipeline renders the 9-node DAG; edge honesty (Metadata+Analyze→Proposals only) is grep-verifiable; legacy partials removed with no dangling references
- < sm fallback `<ol>` present and sr-only at ≥ sm
</verification>

<success_criteria>
- One coherent DAG replaces the Phase-34 stage_cards + processing_card (D-01)
- Honest parallel topology: Metadata ∥ Fingerprint ∥ Analyze ∥ tracklist fan out from Discovery; only Metadata + Analyze converge into Proposals
- Live counts/bars/states/gating bind to $store.pipeline via the existing 5s poll (no new poll, no SSE); triggers reuse existing endpoints only
- Accessible (text-equivalent <ol>, real focusable buttons, color-independent state, dark mode)
</success_criteria>

<output>
Create `.planning/phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-05-SUMMARY.md` when done.
</output>
