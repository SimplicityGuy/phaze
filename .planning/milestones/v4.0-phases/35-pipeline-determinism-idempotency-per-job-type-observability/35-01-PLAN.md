---
phase: 35-pipeline-determinism-idempotency-per-job-type-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/tasks/_shared/deterministic_key.py
  - src/phaze/services/pipeline_counters.py
  - src/phaze/main.py
  - src/phaze/services/agent_task_router.py
  - src/phaze/tasks/controller.py
  - src/phaze/tasks/agent_worker.py
  - src/phaze/routers/agent_files.py
  - src/phaze/services/ingestion.py
  - tests/_queue_fakes.py
  - tests/test_deterministic_key.py
  - tests/test_pipeline_counters.py
  - tests/test_no_auto_metadata_enqueue.py
autonomous: true
requirements: [SCHED, MANUAL-META, OBSERV]

must_haves:
  truths:
    - "D-02: per-node progress is backed by maintained Redis per-function counters (enqueued + completed), reconciled against DB truth on read"
    - "D-06: extract_file_metadata is operator-triggered only — both auto-enqueue paths (agent_files.py, ingestion.py) are removed"
    - "Every routable SAQ task (CONTROLLER_TASKS ∪ AGENT_TASKS) is enqueued with a deterministic key `<function>:<natural_id>` set centrally — no call site can drift to a random uuid key"
    - "Re-enqueuing an already-queued job for the same natural id is a dedup no-op (no duplicate queue item)"
    - "A maintained Redis counter tracks enqueued + completed per pipeline function"
    - "Discovery (agent file upsert) and legacy scan no longer auto-enqueue extract_file_metadata — metadata extraction is operator-triggered only"
  artifacts:
    - path: "src/phaze/tasks/_shared/deterministic_key.py"
      provides: "apply_deterministic_key before_enqueue hook + _KEY_BUILDERS registry + increment_completed after_process hook"
      contains: "_KEY_BUILDERS"
    - path: "src/phaze/services/pipeline_counters.py"
      provides: "Redis INCR-backed enqueued/completed counters + read helper"
      contains: "phaze:pipeline:"
    - path: "tests/test_deterministic_key.py"
      provides: "drift-guard test asserting every routable task name produces a deterministic key"
  key_links:
    - from: "src/phaze/main.py"
      to: "apply_deterministic_key"
      via: "register_before_enqueue on controller_queue"
      pattern: "register_before_enqueue\\(apply_deterministic_key\\)"
    - from: "src/phaze/tasks/controller.py"
      to: "increment_completed"
      via: "after_process kwarg in settings dict"
      pattern: "after_process"
---

<objective>
Generalize the Phase-32 `process_file` deterministic-key fix to the WHOLE pipeline, enforced CENTRALLY in the enqueue layer so no call site can drift back to random uuid keys (D-05, locked decision A). Back the per-job-type progress UI with maintained Redis counters (D-02, locked decision B). Make metadata extraction operator-triggered only by removing both auto-enqueue paths (D-06).

Purpose: The 2026-06-11 queue-doubling incident proved random-uuid jobs cannot dedup against deterministic re-enqueue. Centralizing key construction at the single `before_enqueue` chokepoint guarantees every task is schedule-safe by construction.
Output: A central `apply_deterministic_key` hook + `_KEY_BUILDERS` registry, a maintained-counters service, the `after_process` completion hook wired into both workers, a loud drift-guard test, and removal of the two auto-extract paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-RESEARCH.md
@.planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-PATTERNS.md
@.planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-CONTEXT.md

<interfaces>
<!-- Proven before_enqueue mutation seam (RESEARCH Q1 VERIFIED a hook can set job.key). Copy this shape. -->
From src/phaze/tasks/_shared/queue_defaults.py:
  async def apply_project_job_defaults(job: Job) -> None  # mutates job.timeout/retries/ttl in place; SAQ awaits before persisting to Redis

<!-- The four register seams that already register apply_project_job_defaults — the new key hook registers on ALL FOUR -->
  src/phaze/main.py:103           _app.state.controller_queue.register_before_enqueue(apply_project_job_defaults)
  src/phaze/services/agent_task_router.py:99   queue.register_before_enqueue(apply_project_job_defaults)
  src/phaze/tasks/controller.py:139            queue.register_before_enqueue(apply_project_job_defaults)
  src/phaze/tasks/agent_worker.py:185          queue.register_before_enqueue(apply_project_job_defaults)

<!-- The two worker settings dicts where after_process (a Worker constructor kwarg, NOT a register_* call) is wired -->
  src/phaze/tasks/controller.py     settings = { "queue":..., "functions":[...], "startup":..., "shutdown":... }   (lines 142-166)
  src/phaze/tasks/agent_worker.py   settings = { ... }                                                            (lines 188-208)

<!-- Routable task sets the drift-guard test iterates -->
From src/phaze/services/enqueue_router.py:
  CONTROLLER_TASKS = {generate_proposals, search_tracklist, scrape_and_store_tracklist, match_tracklist_to_discogs, refresh_tracklists}
  AGENT_TASKS      = {process_file, extract_file_metadata, fingerprint_file, scan_live_set, scan_directory, execute_approved_batch}

<!-- Natural-id template already in repo (keep as documented per-task helper) -->
From src/phaze/services/analysis_enqueue.py:32:
  def process_file_job_key(file_id: uuid.UUID) -> str:  # returns f"process_file:{file_id}"

<!-- Redis INCR/EXPIRE precedent for maintained counters -->
From src/phaze/services/proposal.py:221:
  async def check_rate_limit(redis_pool, max_rpm) -> None  # key="phaze:llm:rpm"; redis.incr(key); if count==1: redis.expire(key,60)

<!-- DedupFakeQueue already models SAQ dedup no-op on duplicate key (returns None on a live key) -->
From tests/_queue_fakes.py:176: DedupFakeQueue, DedupFakeTaskRouter, install_fake_queues, wire_fakes, seed_active_agent
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Central deterministic-key hook + maintained-counters service + completion hook</name>
  <files>src/phaze/tasks/_shared/deterministic_key.py, src/phaze/services/pipeline_counters.py</files>
  <read_first>
    - src/phaze/tasks/_shared/queue_defaults.py (the proven before_enqueue mutation seam to mirror — hook body shape, __all__, TYPE_CHECKING import of saq.Job)
    - src/phaze/services/analysis_enqueue.py (process_file_job_key template; the registry's process_file entry MUST compute the IDENTICAL string `process_file:<file_id>` so the existing keyed path stays a no-op-equivalent — RESEARCH Q1)
    - src/phaze/services/proposal.py:221-241 (check_rate_limit Redis INCR/EXPIRE precedent to mirror for counters)
    - .planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-RESEARCH.md (Q1 natural-id map + Q2 after_process hook signature; Q3 generate_proposals batch-hash rationale)
    - .planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-PATTERNS.md (§1, §2 — hook + counter pattern excerpts)
  </read_first>
  <behavior>
    - Given a job for function `extract_file_metadata` with kwargs {file_id: X}, after apply_deterministic_key runs, job.key == "extract_file_metadata:X"
    - Given a job for `scrape_and_store_tracklist` with kwargs {tracklist_id: T}, job.key == "scrape_and_store_tracklist:T"
    - Given a job for `generate_proposals` with kwargs {file_ids: [B,A,C]}, job.key == "generate_proposals:" + sha256 of the SORTED ids (order-independent: [A,B,C] and [C,B,A] produce the same key)
    - Given a job whose function is NOT in _KEY_BUILDERS, apply_deterministic_key leaves job.key unchanged (random uuid default)
    - apply_deterministic_key sets the key UNCONDITIONALLY for registered functions, overriding any caller-supplied key (anti-drift)
    - increment_completed bumps phaze:pipeline:completed:<function> ONLY when ctx["job"].status == Status.COMPLETE
  </behavior>
  <action>
    Create `src/phaze/tasks/_shared/deterministic_key.py`. Define module-level `_KEY_BUILDERS: dict[str, Callable[[dict], str]]` with EXACTLY these 8 entries (natural ids VERIFIED present in payload — RESEARCH Q1 table): process_file→k["file_id"], extract_file_metadata→k["file_id"], fingerprint_file→k["file_id"], scan_live_set→k["file_id"], search_tracklist→k["file_id"], scrape_and_store_tracklist→k["tracklist_id"], match_tracklist_to_discogs→k["tracklist_id"], generate_proposals→a batch-hash helper `_hash_ids(file_ids)` returning `hashlib.sha256(",".join(sorted(str(i) for i in file_ids)).encode()).hexdigest()`. Define `async def apply_deterministic_key(job: Job) -> None`: look up `_KEY_BUILDERS.get(job.function)`; if present, set `job.key = f"{job.function}:{build(job.kwargs or {})}"` AND fold in the enqueued counter increment by calling the counters-service incr helper (best-effort: wrap in try/except, log on failure — never block the enqueue). Stringify uuids before interpolation. Also define `async def increment_completed(ctx: dict) -> None`: read `job = ctx["job"]`; if `job.status == Status.COMPLETE` (import `from saq.job import Status`), call the counters completed-incr helper (best-effort try/except). Export both in `__all__`.

    Create `src/phaze/services/pipeline_counters.py`. Namespace keys `phaze:pipeline:enqueued:<function>` and `phaze:pipeline:completed:<function>`. Provide `async def incr_enqueued(redis, function: str) -> None`, `async def incr_completed(redis, function: str) -> None` (plain INCR, no EXPIRE — these are durable cumulative caches reconciled against DB-truth per D-03, NOT a rolling window like check_rate_limit), and `async def read_counters(redis) -> dict[str, dict[str, int]]` returning `{function: {"enqueued": N, "completed": M}}` for the 8 known functions (MGET / pipeline the reads). The before_enqueue hook obtains the redis handle from the job's queue (`job.queue.redis`) — verify the attribute path against the installed SAQ Job/Queue and degrade silently if absent. DB is authority; counters are a fast cache (D-03).

    NOTE — ACCEPTED enqueued upward drift (plan-checker W3): apply_deterministic_key runs in `before_enqueue`, which fires PRE-dedup. A duplicate-key re-enqueue that Redis dedup subsequently no-ops STILL bumps `enqueued`, so the `enqueued` counter drifts monotonically upward and never self-corrects. This drift is ACCEPTED, not a bug to fix: the Scan/Search node's `{done}/{enqueued}` is a NON-AUTHORITATIVE soft hint only — the UI-SPEC "Counter / Denominator Display Rule" renders the em-dash `—` as the real denominator for that node, and the DB reconcile `get_stage_progress` (35-03) owns EVERY rendered `done` value across all nodes. `enqueued` is never a rendering authority, so its upward drift has no user-visible correctness impact. Do NOT add pre-dedup detection logic to "fix" this — the increment stays folded into apply_deterministic_key.

    NOTE — intent of the `completed` counter (plan-checker W4): `increment_completed` maintains `phaze:pipeline:completed:<function>` to satisfy D-02's mandate for MAINTAINED per-function counters. No node renders this value directly — every node's `done` renders from `get_stage_progress` (DB-truth) per D-03 (counters are a cache, DB is the rendering authority). `completed` is therefore deliberately a reconcile/backstop cache, NOT dead code: it exists to satisfy D-02 while the DB owns rendering per D-03. 35-04 documents how `read_counters` feeds reconcile-on-read without overriding the DB-truth `done`.
  </action>
  <verify>
    <automated>uv run mypy src/phaze/tasks/_shared/deterministic_key.py src/phaze/services/pipeline_counters.py && uv run ruff check src/phaze/tasks/_shared/deterministic_key.py src/phaze/services/pipeline_counters.py</automated>
  </verify>
  <acceptance_criteria>
    - The pytest gate for these modules lands in Task 2 (test files are authored there); Task 1's gate is STRUCTURAL — mypy + ruff + the grep assertions below
    - `grep -c "_KEY_BUILDERS" src/phaze/tasks/_shared/deterministic_key.py` ≥ 1 and the dict literal has exactly 8 keys
    - `grep -n "process_file:" src/phaze/tasks/_shared/deterministic_key.py` OR the f-string `f"{job.function}:` is present (key format `<function>:<natural_id>`)
    - `grep -n "phaze:pipeline:enqueued\|phaze:pipeline:completed" src/phaze/services/pipeline_counters.py` matches both namespaces
    - `uv run mypy src/phaze/tasks/_shared/deterministic_key.py src/phaze/services/pipeline_counters.py` clean
    - `uv run ruff check src/phaze/tasks/_shared/deterministic_key.py src/phaze/services/pipeline_counters.py` clean
  </acceptance_criteria>
  <done>deterministic_key.py exposes apply_deterministic_key (8-entry registry, batch-hash for generate_proposals) + increment_completed; pipeline_counters.py exposes incr_enqueued/incr_completed/read_counters; the enqueued-drift and completed-counter intents are documented in-task; mypy + ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the key hook on all four seams + after_process in both workers + drift-guard test</name>
  <files>src/phaze/main.py, src/phaze/services/agent_task_router.py, src/phaze/tasks/controller.py, src/phaze/tasks/agent_worker.py, tests/_queue_fakes.py, tests/test_deterministic_key.py, tests/test_pipeline_counters.py</files>
  <read_first>
    - src/phaze/main.py:103 (existing register_before_enqueue(apply_project_job_defaults) — add the key hook alongside)
    - src/phaze/services/agent_task_router.py:99 (per-agent queue register seam)
    - src/phaze/tasks/controller.py:139,142-166 (register seam + settings dict for after_process)
    - src/phaze/tasks/agent_worker.py:185,188-208 (register seam + settings dict for after_process)
    - src/phaze/services/enqueue_router.py:44-75 (CONTROLLER_TASKS ∪ AGENT_TASKS — the universe the drift-guard iterates)
    - tests/_queue_fakes.py:176-220 (DedupFakeQueue — already captures the key kwarg; extend for hook-applied key assertions)
    - src/phaze/tasks/_shared/deterministic_key.py (the hook created in Task 1)
  </read_first>
  <behavior>
    - For EVERY name in CONTROLLER_TASKS ∪ AGENT_TASKS, the name is either in _KEY_BUILDERS OR in an explicit documented _UNKEYED_TASKS allow-list; a name in neither FAILS the drift-guard test loud
    - _UNKEYED_TASKS documents WHY each excluded task is unkeyed (refresh_tracklists = cron-only periodic; scan_directory = repeated scans intentionally distinct; execute_approved_batch = keyed elsewhere / optional per RESEARCH Q4)
    - After both hooks are registered on a DedupFakeQueue, enqueuing the same (function, natural_id) twice yields one live job (second enqueue dedup no-ops to None)
  </behavior>
  <action>
    In all FOUR seams (main.py, agent_task_router.py, controller.py, agent_worker.py) add `queue.register_before_enqueue(apply_deterministic_key)` immediately after the existing `apply_project_job_defaults` registration. Order vs the defaults hook is irrelevant (disjoint fields). Import `apply_deterministic_key` and `increment_completed` from `phaze.tasks._shared.deterministic_key`.

    In `tasks/controller.py` and `tasks/agent_worker.py` add `"after_process": increment_completed` to the `settings` dict (it is a Worker constructor kwarg per RESEARCH Q2 — goes in the dict, NOT a register_* call). Wire it in BOTH (the agent worker is a separate container but shares the central Redis, so its INCR lands in the same counter the dashboard reads).

    Create `tests/test_deterministic_key.py`: (1) unit tests for the Task-1 behaviors (per-function key format, batch-hash order-independence, unregistered-function leaves key, unconditional override); (2) the DRIFT-GUARD test — import CONTROLLER_TASKS ∪ AGENT_TASKS from enqueue_router; assert each name ∈ (_KEY_BUILDERS.keys() | _UNKEYED_TASKS); define `_UNKEYED_TASKS` in the test (or as a documented constant in deterministic_key.py) so a NEW routable task absent from both FAILS loud. Create `tests/test_pipeline_counters.py`: assert incr_enqueued/incr_completed bump the right namespaced key and read_counters returns the merged dict (use a fake/fakeredis or a DedupFakeQueue-attached stub). Extend `tests/_queue_fakes.py` only if the counter-hook capture needs a redis stub on the fake queue.
  </action>
  <verify>
    <automated>uv run pytest tests/test_deterministic_key.py tests/test_pipeline_counters.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "register_before_enqueue(apply_deterministic_key)" src/phaze/main.py src/phaze/services/agent_task_router.py src/phaze/tasks/controller.py src/phaze/tasks/agent_worker.py` shows 1 in EACH of the four files (4 total)
    - `grep -c "after_process" src/phaze/tasks/controller.py src/phaze/tasks/agent_worker.py` shows ≥1 in each
    - The drift-guard test fails if a fabricated task name is added to AGENT_TASKS without a _KEY_BUILDERS / _UNKEYED_TASKS entry (verify by temporarily asserting the test logic covers the full union)
    - `uv run pytest tests/test_deterministic_key.py tests/test_pipeline_counters.py` passes
    - `uv run mypy .` clean; `uv run ruff check .` clean
  </acceptance_criteria>
  <done>The key hook is registered on all four before_enqueue seams; increment_completed is wired into both worker settings dicts; the drift-guard test enforces a deterministic key (or documented exemption) for every routable task; full unit suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Remove both auto metadata-extraction paths (D-06) + regression tests</name>
  <files>src/phaze/routers/agent_files.py, src/phaze/services/ingestion.py, tests/test_no_auto_metadata_enqueue.py</files>
  <read_first>
    - src/phaze/routers/agent_files.py:130-162 (the "Auto-enqueue extract_file_metadata for INSERTed music/video files" block — delete it; KEEP the upsert at :126 and the `return FileUpsertResponse(...)`)
    - src/phaze/services/ingestion.py:183-191 (the "Auto-enqueue tag extraction for newly discovered files (per D-09)" block — delete it; keep the bulk_upsert + scan-complete marking)
    - .planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-PATTERNS.md (§7 — exact deletion boundaries)
    - .planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-STAGE-DEPENDENCIES.md:16,50 (soft-dep note: search_tracklist falls back to filename parsing when metadata absent — manual-only metadata does NOT hard-break the tracklist branch)
  </read_first>
  <action>
    In `routers/agent_files.py` delete the entire auto-enqueue block (the `task_router = request.app.state.task_router`, `enqueued = 0`, the `for row in rows:` loop calling `enqueue_for_agent("extract_file_metadata", ...)`, and its trailing comment). Preserve the upsert execute/commit and the `FileUpsertResponse` return. Remove any now-unused imports (ExtractMetadataPayload, EXTENSION_MAP, _EXTRACTABLE, FileCategory) flagged by ruff. If `FileUpsertResponse` carried an `enqueued` field sourced from the deleted loop, set it to 0 or drop it per the response model — check the model first and keep the response schema valid.

    In `services/ingestion.py` delete the `if queue is not None and file_records:` auto-enqueue block (per D-09). If the `queue` parameter becomes unused after removal, leave the parameter for signature stability but stop calling enqueue, OR remove it and update callers — choose the minimal-blast option and update any direct callers + tests accordingly.

    Create `tests/test_no_auto_metadata_enqueue.py`: assert (1) the agent file-upsert endpoint does NOT enqueue extract_file_metadata (DedupFakeQueue/router records zero extract_file_metadata enqueues after an insert that previously would have triggered it); (2) the legacy ingestion scan path does NOT enqueue extract_file_metadata. These are the operator-controlled-metadata (MANUAL-META) regression guards.
  </action>
  <verify>
    <automated>uv run pytest tests/test_no_auto_metadata_enqueue.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "extract_file_metadata" src/phaze/routers/agent_files.py` == 0 (no enqueue of metadata in the file-upsert handler)
    - `grep -v '^#' src/phaze/services/ingestion.py | grep -c "enqueue(\"extract_file_metadata\"\|enqueue('extract_file_metadata'" ` == 0
    - `uv run pytest tests/test_no_auto_metadata_enqueue.py` passes
    - Existing agent_files / ingestion tests still pass: `uv run pytest tests/ -k "agent_files or ingestion"`
    - `uv run mypy .` clean; `uv run ruff check .` clean (no unused-import or unused-arg leftovers)
  </acceptance_criteria>
  <done>Neither the agent file-upsert handler nor the legacy ingestion scan enqueues extract_file_metadata; regression tests prove metadata is operator-triggered only; existing suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → FastAPI admin UI | Unauthenticated at app layer by design (reverse-proxy internal realm, Phase 33 precedent) |
| FastAPI / worker → Redis (enqueue) | Job key + counter writes cross here |
| worker → Postgres | DB writes (out of scope for this plan; covered in 35-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | enqueue layer | mitigate | apply_deterministic_key sets job.key UNCONDITIONALLY for registered functions, overriding any caller-supplied key; drift-guard test fails loud on any routable task without a key builder |
| T-35-02 | Denial of Service | Redis counter keys | accept | Counter cardinality is bounded to the 8 fixed function names; no user-controlled key component, no unbounded growth |
| T-35-03 | Tampering / Elevation | D-06 removal | mitigate | Removing auto-enqueue introduces NO new endpoint; the manual extract-metadata trigger endpoint (Phase 34) is unchanged — verify no new unauthenticated trigger surface |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | Code-only change; no package installs in this plan (no Package Legitimacy Gate required) |
</threat_model>

<verification>
- `uv run pytest` full suite green; phase-touched modules retain ≥85% coverage
- `uv run mypy .` and `uv run ruff check .` clean
- `pre-commit run --all-files` passes (NEVER --no-verify)
- Drift-guard test enforces a deterministic key (or documented exemption) for every name in CONTROLLER_TASKS ∪ AGENT_TASKS
</verification>

<success_criteria>
- Every routable task is keyed `<function>:<natural_id>` centrally; re-enqueue of a live (function, natural_id) dedup no-ops
- generate_proposals keyed by order-independent batch-hash of file_ids (per-file idempotency owned by 35-02 upsert, not the key)
- enqueued + completed maintained counters exist per function (Redis), reconcilable against DB-truth on read (consumed in 35-03/35-04)
- Neither discovery nor legacy scan auto-enqueues extract_file_metadata
</success_criteria>

<output>
Create `.planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-01-SUMMARY.md` when done.
</output>
