---
phase: 35-pipeline-determinism-idempotency-per-job-type-observability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/019_add_proposals_pending_unique_index.py
  - src/phaze/models/proposal.py
  - src/phaze/services/proposal.py
  - tests/test_proposals_upsert.py
  - tests/test_migration_019_dedupe.py
autonomous: true
requirements: [IDEMP]

must_haves:
  truths:
    - "Re-running Generate Proposals for a file overwrites its PENDING proposal in place — never creates a second pending row for the same file"
    - "An APPROVED / EXECUTED / REJECTED / FAILED proposal is NEVER touched by a re-run (human approvals are structurally protected)"
    - "The pending-uniqueness guard is enforced at the DB level (partial unique index), not only in app code"
    - "Migration 019 collapses pre-existing duplicate pending rows BEFORE creating the unique index, so it does not abort on the live 11,428-file archive"
  artifacts:
    - path: "alembic/versions/019_add_proposals_pending_unique_index.py"
      provides: "dedupe-then-index migration (two ordered ops), down_revision=018"
      contains: "uq_proposals_file_id_pending"
    - path: "src/phaze/models/proposal.py"
      provides: "partial unique Index in __table_args__ kept in sync with the migration"
      contains: "uq_proposals_file_id_pending"
    - path: "src/phaze/services/proposal.py"
      provides: "store_proposals as on_conflict_do_update keyed on (file_id) WHERE status='pending'"
      contains: "on_conflict_do_update"
  key_links:
    - from: "src/phaze/services/proposal.py"
      to: "uq_proposals_file_id_pending"
      via: "pg_insert(...).on_conflict_do_update index_elements=[file_id] index_where=status=='pending'"
      pattern: "on_conflict_do_update"
---

<objective>
Make `generate_proposals` idempotent (D-04): one active proposal per file, re-runs overwrite the PENDING proposal in place, human-approved proposals are never discarded. Convert the lone non-idempotent task write (`store_proposals` raw INSERTs) to a partial-index upsert, and ship the blocking migration that dedupes existing pending rows BEFORE adding the unique index. Verify the other two write targets named in scope (`execution_log` already idempotent; `tag_write_log` intentionally append-only) need no change.

Purpose: Re-running proposals today appends duplicate PENDING rows. Generalized idempotency means no re-run ever duplicates DB rows — the second half of the determinism guarantee (the first half, schedule-safe keys, is 35-01).
Output: Alembic revision 019 (dedupe → partial unique index), the model `__table_args__` sync, the `store_proposals` upsert, and an audit confirming execution_log/tag_write_log are correct as-is.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-RESEARCH.md
@.planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-PATTERNS.md

<interfaces>
<!-- Current proposals model — PK id is Python-side uuid4 default (pg_insert BYPASSES it: must stamp explicitly) -->
From src/phaze/models/proposal.py:
  class RenameProposal(TimestampMixin, Base): __tablename__ = "proposals"
    id: Mapped[uuid.UUID] = mapped_column(..., primary_key=True, default=uuid.uuid4)   # Python-side default — pg_insert bypasses
    file_id: Mapped[uuid.UUID] = ForeignKey("files.id")    # plain FK, NOT unique today
    status: Mapped[str] = mapped_column(String(20), default=ProposalStatus.PENDING)    # "pending"/"approved"/"rejected"/"executed"/"failed"
    __table_args__ = (Index("ix_proposals_status", "status"),)

<!-- The lone non-idempotent write to convert (services/proposal.py:289-298) -->
  async def store_proposals(session, file_ids, batch_response, files_context) -> int
    # currently: RenameProposal(...) + session.add(record) per proposal (fresh uuid each run)
    # also transitions each file's state to FileState.PROPOSAL_GENERATED — PRESERVE this

<!-- House-style upsert analog to mirror -->
From src/phaze/routers/agent_metadata.py:56-69 / agent_fingerprint.py:40:
  stmt = pg_insert(Model).values([payload]).on_conflict_do_update(index_elements=["file_id"], set_={...})

<!-- Partial-index-via-postgresql_where idiom (latest revision is 018) -->
From alembic/versions/018_add_analysis_window_table.py:
  op.create_index("ix_...", "analysis_window", ["bpm"], postgresql_where=sa.text("tier = 'fine'"))

<!-- Already-idempotent reference (VERIFY ONLY, no change) -->
From src/phaze/routers/agent_execution.py:77:
  pg_insert(ExecutionLog).values([payload]).on_conflict_do_nothing(index_elements=["id"])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 019 (dedupe → partial unique index) + model __table_args__ sync</name>
  <files>alembic/versions/019_add_proposals_pending_unique_index.py, src/phaze/models/proposal.py</files>
  <read_first>
    - alembic/versions/018_add_analysis_window_table.py (revision/down_revision pattern, postgresql_where partial-index idiom, downgrade drop-index shape)
    - src/phaze/models/proposal.py (existing __table_args__ Index to extend)
    - .planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-RESEARCH.md (Q3 — the BLOCKING data hazard + the exact two-op order)
    - .planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-PATTERNS.md (§5 — migration shape + model sync excerpt)
  </read_first>
  <action>
    Create `alembic/versions/019_add_proposals_pending_unique_index.py` with `revision = "019"`, `down_revision = "018"`. `upgrade()` runs TWO ordered ops:
    (1) Collapse existing duplicate PENDING rows to one-per-file, keeping the most-recent `created_at`: an `op.execute(sa.text(...))` DELETE using `row_number() OVER (PARTITION BY file_id ORDER BY created_at DESC)` over `proposals WHERE status = 'pending'`, deleting rows where rn > 1. This MUST run first or op 2 aborts on the live archive (RESEARCH Q3).
    (2) `op.create_index("uq_proposals_file_id_pending", "proposals", ["file_id"], unique=True, postgresql_where=sa.text("status = 'pending'"))`.
    `downgrade()` drops `uq_proposals_file_id_pending` (mirror 018's downgrade; the dedupe is not reversible — document that in a comment).

    In `src/phaze/models/proposal.py` add to `__table_args__` a matching `Index("uq_proposals_file_id_pending", "file_id", unique=True, postgresql_where=text("status = 'pending'"))` so autogenerate/ORM stays in sync (import `text` from sqlalchemy). Keep the existing `ix_proposals_status`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_migration_019_dedupe.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "down_revision" alembic/versions/019_add_proposals_pending_unique_index.py` shows `down_revision = "018"`
    - `grep -c "row_number() OVER" alembic/versions/019_add_proposals_pending_unique_index.py` ≥ 1 (the dedupe op precedes the create_index op in source order)
    - `grep -c "uq_proposals_file_id_pending" alembic/versions/019_add_proposals_pending_unique_index.py src/phaze/models/proposal.py` ≥ 1 in EACH file
    - `uv run alembic upgrade head` succeeds against the integration test DB (and `uv run alembic downgrade -1` then `upgrade head` round-trips)
    - Migration test proves: seed ≥2 pending rows for one file_id + ≥1 approved row → after upgrade exactly 1 pending row remains for that file_id AND the approved row is untouched
    - `uv run mypy .` clean; `uv run ruff check .` clean
  </acceptance_criteria>
  <done>Revision 019 dedupes pending duplicates then creates the partial unique index; the model __table_args__ mirrors it; upgrade/downgrade round-trips on the test DB; dedupe-preserves-approved is proven.</done>
</task>

<task type="auto">
  <name>Task 2: store_proposals → partial-index upsert + execution_log/tag_write_log audit</name>
  <files>src/phaze/services/proposal.py, tests/test_proposals_upsert.py</files>
  <read_first>
    - src/phaze/services/proposal.py:249-307 (store_proposals current raw-INSERT body — convert; PRESERVE the FileState.PROPOSAL_GENERATED transition and the path normalization)
    - src/phaze/routers/agent_metadata.py:56-69 (on_conflict_do_update analog; PK-stamp gotcha reference)
    - src/phaze/routers/agent_execution.py:77 (execution_log on_conflict_do_nothing — VERIFY only)
    - src/phaze/models/tag_write_log.py (docstring: intentionally append-only — DO NOT add an upsert)
    - .planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-RESEARCH.md (Q3 exact upsert shape + Q4 audit conclusions)
    - .planning/milestones/v4.0-phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-PATTERNS.md (§4 — D-04 upsert + PK-stamp gotcha)
  </read_first>
  <action>
    Convert `store_proposals` to build each row dict and issue `pg_insert(RenameProposal).values(**row).on_conflict_do_update(index_elements=["file_id"], index_where=(RenameProposal.status == "pending"), set_={...})`. The `set_` updates `proposed_filename`, `proposed_path`, `confidence`, `context_used`, `reason`, and `updated_at=func.now()` — do NOT put `id` or `file_id` in `set_` (ON CONFLICT DO UPDATE keeps the existing row's id/file_id). PK-STAMP GOTCHA: `pg_insert(...).values()` BYPASSES the Python-side `default=uuid.uuid4`, so explicitly stamp `row["id"] = uuid.uuid4()` for the INSERT branch or the fresh insert raises NotNullViolationError (PATTERNS §4 / agent_metadata analog). Set `status=ProposalStatus.PENDING` on insert. Keep the existing per-file `FileState.PROPOSAL_GENERATED` transition and the proposed_path `//`-collapse normalization. The `index_where=status=='pending'` is what makes the conflict fire ONLY against an existing pending row (overwrites it) while APPROVED/EXECUTED/etc rows fall outside the index and are never a conflict target (D-04, DB-level guard).

    Audit `execution_log` and `tag_write_log`: add a short module/test comment or docstring note confirming execution_log is already idempotent (on_conflict_do_nothing on id) and tag_write_log is intentionally append-only (no upsert — adding one would erase the audit trail, RESEARCH Q4). No code change to either.

    Create `tests/test_proposals_upsert.py`: (1) calling store_proposals twice for the same file produces exactly ONE pending row, with the second call's content (overwrite-in-place); (2) an APPROVED row for the same file is untouched after a re-run AND no second pending row appears alongside it; (3) the PK-stamp path works (fresh insert succeeds, no NotNullViolation).
  </action>
  <verify>
    <automated>uv run pytest tests/test_proposals_upsert.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "on_conflict_do_update" src/phaze/services/proposal.py` ≥ 1 and `grep -c "session.add(record)" src/phaze/services/proposal.py` == 0 (raw INSERT removed)
    - `grep -n "index_where" src/phaze/services/proposal.py` shows the status=='pending' guard
    - `grep -n "row\[.id.\] = \|values(id=\|\"id\":" src/phaze/services/proposal.py` confirms the explicit PK stamp on insert
    - `uv run pytest tests/test_proposals_upsert.py` passes — double-run yields one pending row; approved row preserved
    - execution_log and tag_write_log unchanged (`git diff --stat` shows no edits to agent_execution.py / tag_writer.py / models/tag_write_log.py)
    - `uv run pytest tests/ -k "proposal"` green; `uv run mypy .` clean; `uv run ruff check .` clean
  </acceptance_criteria>
  <done>store_proposals upserts on the (file_id) WHERE status='pending' partial index with explicit PK stamping; double-runs overwrite the pending row and protect approvals; execution_log/tag_write_log audited as correct-as-is with documented rationale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → Postgres (proposals write) | The generate_proposals task writes/overwrites proposal rows here |
| migration → Postgres (live archive) | Revision 019 mutates the 11,428-file archive's proposals table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04 | Tampering | migration dedupe DELETE | mitigate | Static parameterless SQL (row_number window over a fixed table); no user input interpolated — no injection surface |
| T-35-05 | Tampering | store_proposals upsert | mitigate | Partial index `WHERE status='pending'` enforces at the DB level that only PENDING rows are overwritten; APPROVED/EXECUTED rows are outside the index and never a conflict target (human approvals structurally protected) |
| T-35-06 | Denial of Service / Availability | migration on live archive | mitigate | Op 1 (dedupe) runs BEFORE op 2 (create unique index) so the index build cannot abort on pre-existing duplicate pending rows; downgrade documented |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | Code + migration only; no package installs (no Package Legitimacy Gate required) |
</threat_model>

<verification>
- `uv run alembic upgrade head` succeeds on the integration DB; downgrade/upgrade round-trips
- `uv run pytest` full suite green; proposal-module coverage ≥85%
- `uv run mypy .` and `uv run ruff check .` clean; `pre-commit run --all-files` passes
- execution_log and tag_write_log files show no diff (audit-only)
</verification>

<success_criteria>
- One active proposal per file enforced at the DB level; re-runs overwrite pending in place and never touch approvals
- Migration 019 is safe to apply to the live archive (dedupe-before-index)
- The proposals table is the last non-idempotent task write to be fixed; the pipeline's DB-write idempotency is now complete (with tag_write_log intentionally append-only)
</success_criteria>

<output>
Create `.planning/phases/35-pipeline-determinism-idempotency-per-job-type-observability/35-02-SUMMARY.md` when done.
</output>
