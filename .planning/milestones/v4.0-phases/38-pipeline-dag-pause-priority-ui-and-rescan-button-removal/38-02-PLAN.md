---
phase: 38-pipeline-dag-pause-priority-ui-and-rescan-button-removal
plan: 02
type: execute
wave: 2
depends_on: ["38-01", "37-04"]
# Intra-phase: depends on 38-01 (both touch dag_canvas.html + test_dag_canvas_render.py) → wave 2.
# Cross-phase: the controls POST to Phase 37's /pipeline/stages/{stage}/{priority,pause,resume}
# endpoints (JSON {stage,priority,paused}). They render and bind regardless; they do nothing
# useful until Phase 37 ships. Store keys they bind to are provided by 38-03 (file-disjoint).
files_modified:
  - src/phaze/templates/pipeline/partials/dag_canvas.html
  - tests/test_dag_canvas_render.py
autonomous: true
requirements: [REQ-38-1, REQ-38-2]
nyquist_compliant: true

must_haves:
  truths:
    - "Each agent chip (metadata/analyze/fingerprint) renders a Pause and a Resume button gated by x-show on <stage>Paused, each with a STATIC hx-post to /pipeline/stages/{stage}/pause|resume"
    - "Each agent chip renders ▲ Higher (hx-vals {delta:-10}) and ▼ Lower (hx-vals {delta:10}) steppers posting to /pipeline/stages/{stage}/priority, with the raw number bound to <stage>Priority"
    - "▲ is disabled when <stage>Priority <= 0; ▼ is disabled when <stage>Priority >= 100"
    - "Every control uses hx-swap=none + hx-disabled-elt=this; the container @htmx:after-request parses the JSON response and writes the store authoritatively (no optimistic mutation)"
    - "The controls are NOT gated by agentBusy / nodes.<node>.blocked"
    - "The taller agent chips do not overlap (NODE_LAYOUT recomputed; canvas/svg height grown); the <ol> text mirror gains ' — paused' / ' — priority N' for the 3 agent stages"
  artifacts:
    - path: "src/phaze/templates/pipeline/partials/dag_canvas.html"
      provides: "stage_controls(stage) macro attached to the 3 agent nodes; recomputed NODE_LAYOUT + canvas dims; extended <ol> a11y"
      contains: "stage_controls"
    - path: "tests/test_dag_canvas_render.py"
      provides: "control-render tests + updated overlap min_chip_height + updated exact-hx-post test + extended _DAG_KEYS"
      contains: "stages/"
  key_links:
    - from: "src/phaze/templates/pipeline/partials/dag_canvas.html"
      to: "/pipeline/stages/{stage}/pause|resume|priority (Phase 37)"
      via: "static hx-post on x-show-gated buttons + hx-vals delta on steppers"
      pattern: "/pipeline/stages/"
    - from: "stage_controls @htmx:after-request"
      to: "$store.pipeline"
      via: "JSON.parse($event.detail.xhr.response) → <stage>Priority/<stage>Paused"
      pattern: "htmx:after-request"
---

<objective>
Add the per-stage operator controls to the three agent nodes of the DAG canvas (REQ-38-1 pause/resume, REQ-38-2 priority stepper) and recompute the layout the taller chips force. A reusable `stage_controls(stage)` Jinja macro — modeled on the existing `enqueue_button` macro but using `hx-swap="none"` + an `@htmx:after-request` JSON-parse handler — attaches after the enqueue button inside `node-metadata`, `node-analyze`, `node-fingerprint`. The controls are store-driven read-only consumers of `$store.pipeline` (authoritative-only updates, no optimistic mutation) and are NOT gated by `agentBusy` (pausing a busy stage is the entire point — drain semantics).

Purpose: This is the operator's surface to drive the Phase 37 control plane directly from the graph.
Output: the `stage_controls` macro (two `x-show`-gated static-`hx-post` Pause/Resume buttons + ▲/▼ steppers + hint), attached to the 3 agent chips; the `<ol>` text mirror extended with paused/priority; a recomputed `NODE_LAYOUT` + grown canvas/SVG dims; updated guard tests (overlap `min_chip_height`, exact-hx-post count) + new control-render tests + extended `_DAG_KEYS`.

This plan obeys 38-UI-SPEC.md verbatim. The Pause/Resume MUST be TWO `x-show`-gated static-`hx-post` buttons (NOT a bound `:hx-post`); stepper buttons use `px-1` + `min-h-[28px]` (NOT `px-1.5`); update timing is AUTHORITATIVE-ONLY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v4.0-phases/38-pipeline-dag-pause-priority-ui-and-rescan-button-removal/38-RESEARCH.md
@.planning/milestones/v4.0-phases/38-pipeline-dag-pause-priority-ui-and-rescan-button-removal/38-PATTERNS.md
@.planning/milestones/v4.0-phases/38-pipeline-dag-pause-priority-ui-and-rescan-button-removal/38-UI-SPEC.md
@.planning/milestones/v4.0-phases/38-pipeline-dag-pause-priority-ui-and-rescan-button-removal/38-VALIDATION.md

<interfaces>
<!-- The macro shape, the attach points, the layout map, the test harness. Extracted from the repo + UI-SPEC. -->

Macro analog — `dag_canvas.html:98-112` `enqueue_button(node, endpoint, slot_id, action_phrase, btn_class)`: an `x-data` wrapper `<div>` with `@htmx:after-request`, `type="button"` buttons, `hx-post`, inline rose error reveal ("Couldn't ... Retry."), `:aria-label`. The new `stage_controls(stage)` copies this skeleton but: `hx-swap="none"` (NOT hx-target/innerHTML), `hx-disabled-elt="this"`, and a JSON-parse after-request handler.

stage_controls(stage) contract (38-UI-SPEC §Component Inventory — LOCKED):
- Container `<div class="mt-2 pt-2 border-t border-gray-100 dark:border-phaze-border" x-data @htmx:after-request="...">`. Handler: `if ($event.detail.successful) { const r = JSON.parse($event.detail.xhr.response); $store.pipeline.{{stage}}Priority = r.priority; $store.pipeline.{{stage}}Paused = r.paused ? 1 : 0; } else { error = true; }` (add `x-data="{ error: false }"` for the rose error reveal `text-sm text-rose-600 dark:text-rose-400`, copy "Couldn't update. Retry.").
- Inner button row: `flex items-center justify-between gap-2`.
- Pause/Resume = TWO buttons, each STATIC hx-post:
  - Pause: `x-show="!$store.pipeline.{{stage}}Paused"`, `hx-post="/pipeline/stages/{{stage}}/pause"`, amber (`bg-amber-600 dark:bg-amber-700`), label "Pause", `text-xs font-semibold`.
  - Resume: `x-show="$store.pipeline.{{stage}}Paused"`, `hx-post="/pipeline/stages/{{stage}}/resume"`, green (`bg-green-600 dark:bg-green-700`), label "Resume".
  - Both: `hx-swap="none"`, `hx-disabled-elt="this"`, `:aria-label="($store.pipeline.{{stage}}Paused ? 'Resume' : 'Pause') + ' {{stage}} stage'"`, focus ring `focus-visible:ring-2 focus-visible:ring-blue-500`.
- Stepper group `inline-flex items-center gap-1`:
  - ▲ Higher: `hx-post="/pipeline/stages/{{stage}}/priority"`, `hx-vals='{"delta": -10}'`, `:disabled="$store.pipeline.{{stage}}Priority <= 0"`, `aria-label="Higher priority for {{stage}} (runs sooner)"`, classes `px-1 py-1 min-h-[28px] text-xs rounded border border-gray-300 dark:border-phaze-border disabled:opacity-40`, label "▲ Higher".
  - value `<span class="text-sm font-semibold tabular-nums w-7 text-center" x-text="$store.pipeline.{{stage}}Priority" :aria-label="'priority ' + $store.pipeline.{{stage}}Priority">`.
  - ▼ Lower: `hx-vals='{"delta": 10}'`, `:disabled="$store.pipeline.{{stage}}Priority >= 100"`, `aria-label="Lower priority for {{stage}} (runs later)"`, same classes, label "▼ Lower".
  - Both steppers: `hx-swap="none"`, `hx-disabled-elt="this"`.
- Hint `<p class="mt-1 text-[11px] text-gray-500 dark:text-gray-400">lower number runs first</p>`.
- Glyphs are inline Unicode `▲` U+25B2 / `▼` U+25BC. Every element carries a `dark:` variant.
- The macro reads ONLY `$store.pipeline.{{stage}}Paused/Priority` — NEVER `nodes.{{stage}}.blocked` / `agentBusy`.

Attach points (after each `enqueue_button(...)`, inside the chip): `node-metadata` (~:218 → `{{ stage_controls('metadata') }}`), `node-analyze` (~:233 → `{{ stage_controls('analyze') }}`), `node-fingerprint` (~:248 → `{{ stage_controls('fingerprint') }}`).

Layout map — `dag_canvas.html:31-41` `NODE_LAYOUT` (col-1 uniform 182px gutter today: metadata y:24, analyze y:206, fingerprint y:388, scan_search y:570; agent `h:154`, scan_search `h:126`). Wrapper/SVG (`:177-178`): `style="width:1132px; height:720px;"` + `<svg width="1132" height="720" viewBox="0 0 1132 720">`. Edge anchors use `sy = src.y + src.h//2` (`:182-183`); chips are content-height (style sets only left/top/width). 38-UI-SPEC Layout Contract REQUIRED: gutter ≥ measured agent-chip-height + 16px (target ~260px) — metadata y:24, analyze ~300, fingerprint ~576, scan_search ~852; grow wrapper/SVG height to ≈1000; re-balance col-2/col-3 (proposals/scrape/execute/match) y so curved edges still land cleanly.

<ol> a11y — `dag_canvas.html:339-360` per-stage `<li>` (uses `<template x-if="nodes.{{row[0]}}.blocked">`). For the 3 agent rows append: `<template x-if="$store.pipeline.{{stage}}Paused"><span> — paused</span></template>` and `<span x-text="' — priority ' + $store.pipeline.{{stage}}Priority"></span>`. SVG stays `aria-hidden`.

Test harness — `tests/test_dag_canvas_render.py`: `_render_canvas` (~:91-117) + `_DAG_KEYS` (~:41-59) → `dag=dict.fromkeys(_DAG_KEYS, 0)`. Extend `_DAG_KEYS` with the 6 new keys. `test_topology_column_one_chips_do_not_overlap` (~:146-169, `min_chip_height` ~:157) — bump to the new measured agent-chip height. `test_gating_triggers_post_only_to_existing_endpoints` (~:240-251) asserts EXACTLY 4 `hx-post="/pipeline/..."` and a sorted enqueue-only list — must be updated. `test_render_every_node_has_dark_class` (~:208-215) is the dark-class assertion idiom to copy for the controls.

Phase 37 endpoint contract (what the controls POST to / the after-request parses): `POST /pipeline/stages/{stage}/priority` body `{delta:int}` (clamp [0,100]), `/pause`, `/resume` — all return JSON `{stage, priority, paused}`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: stage_controls macro + attach to the 3 agent nodes</name>
  <files>src/phaze/templates/pipeline/partials/dag_canvas.html, tests/test_dag_canvas_render.py</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/dag_canvas.html:98-112 (`enqueue_button` macro — skeleton to copy), :208-249 (the 3 agent chips + their enqueue_button calls / attach points)
    - 38-UI-SPEC.md §Component Inventory, §Copywriting Contract, §State Behavior Contract, §Accessibility
    - 38-PATTERNS.md §"dag_canvas.html" items 1 & 2
    - tests/test_dag_canvas_render.py:41-59 (`_DAG_KEYS`), :91-117 (`_render_canvas`), :208-215 (dark-class idiom)
  </read_first>
  <behavior>
    - For each of metadata/analyze/fingerprint, the rendered canvas contains a STATIC `hx-post="/pipeline/stages/{stage}/pause"` (Pause, `x-show="!$store.pipeline.{stage}Paused"`) AND a STATIC `hx-post="/pipeline/stages/{stage}/resume"` (Resume, `x-show="$store.pipeline.{stage}Paused"`).
    - Each renders `hx-post="/pipeline/stages/{stage}/priority"` twice with `hx-vals='{"delta": -10}'` (▲, `:disabled="$store.pipeline.{stage}Priority <= 0"`) and `hx-vals='{"delta": 10}'` (▼, `:disabled="$store.pipeline.{stage}Priority >= 100"`).
    - The priority value span binds `x-text="$store.pipeline.{stage}Priority"` with `tabular-nums`.
    - Every control carries `hx-swap="none"` and `hx-disabled-elt="this"`; the container has `@htmx:after-request` doing `JSON.parse(...)` → store write with `r.paused ? 1 : 0`.
    - The control markup contains NO `nodes.metadata.blocked` / `agentBusy` reference (assert `.blocked` absent within the control fragment).
    - Each control element carries a `dark:` class (mirror `test_render_every_node_has_dark_class`).
    - The static hint "lower number runs first" appears once per agent node.
  </behavior>
  <action>
    Add the `stage_controls(stage)` macro to `dag_canvas.html` exactly per the `<interfaces>` contract above — TWO `x-show`-gated static-`hx-post` Pause/Resume buttons (amber/green, label flip), ▲ Higher (`hx-vals {"delta":-10}`) / value span / ▼ Lower (`hx-vals {"delta":10}`) steppers with the disabled bounds, the `lower number runs first` hint, an `@htmx:after-request` JSON-parse → `$store.pipeline` writer (`r.paused ? 1 : 0`), the rose error reveal, and the `:aria-label` bindings. Stepper buttons use `px-1 py-1 min-h-[28px]` (NEVER `px-1.5`). Every control: `hx-swap="none"` + `hx-disabled-elt="this"`. The macro must read only `$store.pipeline.<stage>Paused/Priority` — do NOT reference `nodes.<stage>.blocked` or `agentBusy`. Call `{{ stage_controls('metadata') }}`, `{{ stage_controls('analyze') }}`, `{{ stage_controls('fingerprint') }}` immediately after each agent node's `enqueue_button(...)`. Add the render tests in `test_dag_canvas_render.py` first (extend `_DAG_KEYS` with the 6 new keys so the harness seeds them), asserting the behaviors above; write them red, then implement the macro to green.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dag_canvas_render.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Per agent stage: static `hx-post=".../pause"` and `.../resume` (x-show-gated), and `.../priority` with `hx-vals` deltas -10 and 10 all present
    - ▲ `:disabled` on `<= 0`, ▼ `:disabled` on `>= 100`; value span binds `<stage>Priority` with `tabular-nums`
    - every control has `hx-swap="none"` + `hx-disabled-elt="this"`; container `@htmx:after-request` writes the store with `r.paused ? 1 : 0`
    - control fragment contains no `.blocked` / `agentBusy`; each control carries a `dark:` class
    - stepper buttons use `min-h-[28px]` and `px-1` (assert `px-1.5` absent in the control fragment)
    - `uv run pytest tests/test_dag_canvas_render.py -x` exits 0
  </acceptance_criteria>
  <done>The 3 agent chips render the locked controls, store-driven, not agentBusy-gated; render tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Recompute NODE_LAYOUT + grow canvas/SVG + extend the <ol> a11y mirror</name>
  <files>src/phaze/templates/pipeline/partials/dag_canvas.html</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/dag_canvas.html:31-41 (`NODE_LAYOUT`), :177-178 (wrapper style + svg width/height/viewBox), :182-183 (edge anchor `sy = src.y + src.h//2`), :339-360 (the `<ol>` per-stage `<li>` mirror)
    - 38-UI-SPEC.md §"Layout Contract (CRITICAL — forced recompute)" and §Accessibility
    - 38-PATTERNS.md §"dag_canvas.html" items 4 & 5
  </read_first>
  <action>
    Recompute the col-1 `NODE_LAYOUT` y-coordinates so each gutter is ≥ the measured agent-chip height + 16px (UI-SPEC target ~260px): keep metadata y:24, set analyze ≈300, fingerprint ≈576, scan_search ≈852 (render-and-measure to the exact values that clear overlap). Grow the canvas wrapper `height` (`:177`) and the `<svg>` `height` + `viewBox` (`:178`) to fit (≈1000px). Re-balance the col-2/col-3 node y-positions (proposals/scrape/execute/match) so the curved edges still land cleanly on the new col-1 anchors. Extend the `<ol>` text mirror (`:339-360`) for the metadata/analyze/fingerprint `<li>` only: append a `<template x-if="$store.pipeline.<stage>Paused"><span> — paused</span></template>` and a `<span x-text="' — priority ' + $store.pipeline.<stage>Priority"></span>`. Keep the SVG `aria-hidden`. Chips stay content-height (do not set chip `height`); only `NODE_LAYOUT.h` (edge anchors) and the wrapper/SVG dims change.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dag_canvas_render.py -x</automated>
  </verify>
  <acceptance_criteria>
    - col-1 gutters are ≥ agent-chip-height + 16px; wrapper + `<svg>` height/viewBox grown to match
    - the 3 agent `<li>` mirrors render ` — priority N` and ` — paused` (when paused); non-agent `<li>` unchanged; SVG still `aria-hidden`
    - `uv run pytest tests/test_dag_canvas_render.py -x` exits 0 (no render regression)
  </acceptance_criteria>
  <done>Layout recomputed so taller chips clear; the text mirror surfaces paused/priority for the 3 agent stages.</done>
</task>

<task type="auto">
  <name>Task 3: Update the guard tests (overlap min_chip_height + exact-hx-post count)</name>
  <files>tests/test_dag_canvas_render.py</files>
  <read_first>
    - tests/test_dag_canvas_render.py:146-169 (`test_topology_column_one_chips_do_not_overlap`, `min_chip_height` ~:157), :240-251 (`test_gating_triggers_post_only_to_existing_endpoints` — the EXACTLY-4 assertion)
    - 38-PATTERNS.md §"tests/test_dag_canvas_render.py" items 1 & 2 (Pitfall 1 & 2)
    - 38-VALIDATION.md rows `38-overlap`, `38-hxpost4`
  </read_first>
  <action>
    Update `test_topology_column_one_chips_do_not_overlap`: bump `min_chip_height` from its current value to the new measured agent-chip height (the height the recomputed gutters were sized against); keep the loop logic. Update `test_gating_triggers_post_only_to_existing_endpoints` deliberately as an intentional contract change: scope the existing "exactly 4" enqueue-trigger assertion to the 4 trigger targets (`/pipeline/analyze`, `/pipeline/extract-metadata`, `/pipeline/fingerprint`, `/pipeline/proposals`) only, AND add a separate assertion for the new stage-control posts — `/pipeline/stages/{stage}/pause`, `/resume`, and `/priority` for each of metadata/analyze/fingerprint. Document in a comment that the count change is intentional (Phase 38 controls), not an accidental loosening.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dag_canvas_render.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `test_topology_column_one_chips_do_not_overlap` passes with the new `min_chip_height` (and would fail at the old gutters — i.e. it actually guards overlap)
    - the enqueue-trigger assertion is scoped to the 4 trigger targets; a new assertion covers the stage-control posts for all 3 agent stages
    - `uv run pytest tests/test_dag_canvas_render.py -x` exits 0
  </acceptance_criteria>
  <done>Both guard tests updated as intentional contract changes; full dag-canvas render suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API | Control clicks cross to Phase 37 endpoints (untrusted client input) |
| server values → `x-init`/`x-text` | Interpolated store values must be safe (ints / autoescaped text) |
| 5s OOB poll → controls | A racing poll must not clobber an in-flight click |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-DELTA | Denial of Service (pipeline stall) | the priority POST `delta` | mitigate (defense-in-depth) / transfer | The UI sends a FIXED `hx-vals` `±10` only; the authoritative clamp `[0,100]` + DB CHECK live in the Phase 37 endpoint (T-37-02) — the UI is never the sole guard. The ▲/▼ `:disabled` bounds are a UX mirror, not a security control. |
| T-38-XSS | Tampering (injection) | `x-text` / `:aria-label` interpolating store values | mitigate | `x-text` content is Alpine-bound to ints in `$store.pipeline` (no raw server string concatenation into markup); Jinja autoescape stays on. The macro interpolates only the literal `{{ stage }}` (a hardcoded allowlist value: metadata/analyze/fingerprint), never user input. |
| T-38-OOB | Tampering (state corruption / UX) | the 5s poll vs an in-flight control click | mitigate | Controls use `hx-swap="none"` + authoritative-only `@htmx:after-request` store write; buttons are never an OOB swap target (only hidden seed paragraphs are). No optimistic mutation, so a racing poll cannot revert a half-applied optimistic state. |
| T-38-SC | Tampering | uv/pip installs | accept | No package installed this phase (`uv.lock` unchanged). No legitimacy gate required. |
</threat_model>

<verification>
`uv run pytest tests/test_dag_canvas_render.py -x` green (control-render + both guard tests); `uv run pytest --cov --cov-report=term-missing` ≥85% on touched templates' render paths; pre-commit clean. Manual (homelab, post-Phase-37): clicking Pause flips to Resume; stepping priority updates the number flicker-free; the 3 agent chips do not visually overlap.
</verification>

<success_criteria>
Each agent chip renders the locked pause/resume + priority controls (store-driven, authoritative-only, not agentBusy-gated); the `<ol>` mirror surfaces paused/priority; the recomputed layout clears overlap; both guard tests pass as intentional contract updates.
</success_criteria>

<output>
Create `.planning/milestones/v4.0-phases/38-pipeline-dag-pause-priority-ui-and-rescan-button-removal/38-02-SUMMARY.md` when done.
</output>
