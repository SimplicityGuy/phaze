---
phase: 38-pipeline-dag-pause-priority-ui-and-rescan-button-removal
plan: 01
type: execute
wave: 1
depends_on: []
# Cross-phase: none. Pure deletion of a dead scroll anchor — no Phase 37 dependency.
files_modified:
  - src/phaze/templates/pipeline/partials/dag_canvas.html
  - tests/test_dag_canvas_render.py
autonomous: true
requirements: [REQ-38-3]
nyquist_compliant: true

must_haves:
  truths:
    - 'The "Rescan Files" anchor no longer appears anywhere in the DAG canvas'
    - 'No `href="#trigger-scan-heading"` link remains in the rendered canvas'
    - 'The Trigger Scan card remains the sole scan affordance; the empty-state hint still points to it'
  artifacts:
    - path: "src/phaze/templates/pipeline/partials/dag_canvas.html"
      provides: "Discovery node ending at node_bar with no Rescan anchor; stale comments corrected"
      contains: "node-discovery"
    - path: "tests/test_dag_canvas_render.py"
      provides: "Negative render assertion that the Rescan anchor is gone"
      contains: "Rescan"
  key_links:
    - from: "tests/test_dag_canvas_render.py"
      to: "src/phaze/templates/pipeline/partials/dag_canvas.html"
      via: "rendered-HTML string assertion (anchor absent)"
      pattern: "trigger-scan-heading"
---

<objective>
Remove the dead "Rescan Files" anchor from the Discovery node of the DAG canvas (REQ-38-3). The anchor (`dag_canvas.html:202-203`) is a plain `<a href="#trigger-scan-heading">` scroll link with no `hx-post` and no Alpine handler — a confusing duplicate of the Trigger Scan card's "Start Scan" (both ultimately drive `POST /pipeline/scans`). Scanning is initiated solely from the Trigger Scan card, so removal is behavior-safe.

Purpose: Eliminate a duplicate affordance that misleads the operator into thinking the Discovery node has its own scan trigger.
Output: A Discovery chip that ends at its `node_bar(...)` with no action element; corrected stale comments; a guard test asserting the anchor is gone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v4.0-phases/38-pipeline-dag-pause-priority-ui-and-rescan-button-removal/38-RESEARCH.md
@.planning/milestones/v4.0-phases/38-pipeline-dag-pause-priority-ui-and-rescan-button-removal/38-PATTERNS.md
@.planning/milestones/v4.0-phases/38-pipeline-dag-pause-priority-ui-and-rescan-button-removal/38-VALIDATION.md

<interfaces>
<!-- The exact markup to delete and the stale comments to correct. Extracted from the repo. -->
Deletion target — `dag_canvas.html:202-203`: an `<a href="#trigger-scan-heading" aria-label="Rescan files ...">Rescan Files</a>` inside `node-discovery` (`:191-204`). It carries NO `hx-post`, NO Alpine handler — only an in-page scroll. After deletion the Discovery chip ends at its `node_bar('discovery', ...)` call (`:201`).

Stale comments to correct (do NOT leave them referencing a now-removed anchor):
- `:152` — "Empty-state hint (LOCKED copy) — Discovery's Rescan stays enabled regardless." → drop the Rescan clause; the empty-state hint copy ("Trigger a scan to populate the pipeline.") stays and still points at the Trigger Scan card.
- `:191` — "1 · Discovery (cyan, full) — root; Rescan links to the existing Trigger Scan card." → drop the Rescan clause.
- `:133` — `discovery: mk(s.discovered, s.discovered, { cta: 'Rescan Files' })` — the `cta` value is now unused by any button; harmless to leave, but clean it to avoid implying a button exists.

Test analog — `tests/test_dag_canvas_render.py`: pure-Jinja render tests render the partial with a fake context and assert over the returned HTML string. RESEARCH A5 confirms no current test asserts the literal "Rescan" — add the negative assertion now.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the failing negative render assertion for the removed anchor</name>
  <files>tests/test_dag_canvas_render.py</files>
  <read_first>
    - tests/test_dag_canvas_render.py — the `_render_canvas` helper (~:91-117) and `_DAG_KEYS` (~:41-59); copy the existing string-assertion idiom (e.g. the gating/topology render tests)
    - 38-PATTERNS.md §"tests/test_dag_canvas_render.py" item 3 (Rescan-removed assertion)
    - 38-VALIDATION.md row `38-rescan`
  </read_first>
  <behavior>
    - Render the canvas via the existing `_render_canvas(...)` harness, then assert `"Rescan Files" not in html`.
    - Assert `'href="#trigger-scan-heading"' not in html`.
    - This test FAILS now (the anchor is still present) and PASSES after Task 2 deletes it.
  </behavior>
  <action>
    Add a test (mirror the existing render-test signature/style, e.g. `test_discovery_node_has_no_rescan_anchor`) that renders the DAG canvas partial with the standard fake context and asserts both `"Rescan Files" not in html` and `'href="#trigger-scan-heading"' not in html`. Use the in-file `_render_canvas` helper and `_DAG_KEYS` exactly as the neighboring render tests do — no new fixtures.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dag_canvas_render.py::test_discovery_node_has_no_rescan_anchor -x</automated>
  </verify>
  <acceptance_criteria>
    - The new test exists and currently FAILS (anchor still present), proving it exercises the deletion
    - It asserts both `"Rescan Files" not in html` and `'href="#trigger-scan-heading"' not in html`
  </acceptance_criteria>
  <done>The negative assertion exists and is red until the anchor is deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the Rescan anchor and correct the stale comments</name>
  <files>src/phaze/templates/pipeline/partials/dag_canvas.html</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/dag_canvas.html:130-205 — the `node-discovery` chip (`:191-204`), the anchor (`:202-203`), the `nodes` getter `cta` (`:133`), the empty-state hint comment (`:152`), the Discovery section comment (`:191`)
    - 38-PATTERNS.md §"3. The dead anchor to DELETE"
  </read_first>
  <action>
    Delete the two-line `<a href="#trigger-scan-heading" ...>Rescan Files</a>` anchor at `dag_canvas.html:202-203` so the Discovery chip ends at its `node_bar('discovery', ...)`. Correct the stale comments at `:152` and `:191` to remove the "Rescan" clauses (keep the empty-state hint copy "Trigger a scan to populate the pipeline." — it still points to the Trigger Scan card). Clean the now-unused `cta: 'Rescan Files'` in the Discovery `mk(...)` getter at `:133` (remove the `cta` key for discovery, or set it to an empty/neutral value) so no comment or data implies a Discovery-node button still exists. Do NOT touch the Trigger Scan card, `trigger_scan_card.html`, or `POST /pipeline/scans`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dag_canvas_render.py -x && ! grep -q 'trigger-scan-heading' src/phaze/templates/pipeline/partials/dag_canvas.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'trigger-scan-heading' src/phaze/templates/pipeline/partials/dag_canvas.html` is 0
    - `grep -c 'Rescan Files' src/phaze/templates/pipeline/partials/dag_canvas.html` is 0
    - `uv run pytest tests/test_dag_canvas_render.py -x` exits 0 (the Task-1 negative test now passes; no other render test regressed)
  </acceptance_criteria>
  <done>The anchor is gone, comments corrected, full dag-canvas render suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → DAG markup | Operator reads rendered HTML; this plan only removes a non-interactive scroll anchor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-RESCAN | Tampering (UX/affordance) | the duplicate scan anchor | mitigate | Delete the dead anchor; scan remains gated behind the single Trigger Scan card (`POST /pipeline/scans`). Removal reduces, never expands, attack/affordance surface. |
| T-38-SC | Tampering | npm/pip/uv installs | accept | No package is installed this phase (`uv.lock` unchanged). No legitimacy gate required. |
</threat_model>

<verification>
`uv run pytest tests/test_dag_canvas_render.py -x` green; the canvas contains no `trigger-scan-heading` or `Rescan Files` string; pre-commit (ruff/mypy) clean.
</verification>

<success_criteria>
The Discovery node renders header + count + bar only, with no Rescan action; the negative guard test passes; no scan behavior changed (Trigger Scan card untouched).
</success_criteria>

<output>
Create `.planning/milestones/v4.0-phases/38-pipeline-dag-pause-priority-ui-and-rescan-button-removal/38-01-SUMMARY.md` when done.
</output>
