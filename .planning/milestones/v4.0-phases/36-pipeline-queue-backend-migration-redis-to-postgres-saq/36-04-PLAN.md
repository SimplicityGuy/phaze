---
phase: 36-pipeline-queue-backend-migration-redis-to-postgres-saq
plan: 04
type: execute
wave: 3
depends_on: ["36-02"]
files_modified:
  - .planning/milestones/v4.0-phases/36-pipeline-queue-backend-migration-redis-to-postgres-saq/36-HOMELAB-CHANGE-PROMPT.md
  - README.md
  - docs/deployment.md
  - docs/configuration.md
  - .env.example
autonomous: true
requirements: [REQ-36-1]
nyquist_compliant: true

must_haves:
  truths:
    - "A ready-to-paste homelab change prompt exists covering PHAZE_QUEUE_URL, dep swap, saq_jobs first-boot/DB-perms, Redis-no-longer-broker, agent→Postgres firewall, pool-connection budget, and redeploy ordering"
    - "Project docs describe Redis as cache-only and Postgres as the queue broker, with PHAZE_QUEUE_URL documented and its _FILE secret sibling"
  artifacts:
    - path: ".planning/milestones/v4.0-phases/36-pipeline-queue-backend-migration-redis-to-postgres-saq/36-HOMELAB-CHANGE-PROMPT.md"
      provides: "Operator change prompt for the homelab repo agent"
      contains: "PHAZE_QUEUE_URL"
    - path: ".env.example"
      provides: "PHAZE_QUEUE_URL example + _FILE sibling"
      contains: "PHAZE_QUEUE_URL"
  key_links:
    - from: ".env.example"
      to: "config.py queue_url"
      via: "PHAZE_QUEUE_URL env var + _FILE convention"
      pattern: "PHAZE_QUEUE_URL"
    - from: "docs/deployment.md"
      to: "agent→Postgres:5432 firewall requirement"
      via: "deployment env + network topology note"
      pattern: "PHAZE_QUEUE_URL"
---

<objective>
Produce the Step D homelab change-prompt deliverable and bring the in-repo docs in line with the migration: Redis is now cache-only, Postgres is the queue broker, `PHAZE_QUEUE_URL` is the new broker DSN (secret), and agents now require Postgres network reachability.

Purpose: The migration is operationally incomplete without (a) a ready-to-paste prompt the operator hands to the homelab repo agent and (b) docs that stop calling Redis the broker. Both are project conventions (README-per-service, docs-up-to-date).
Output: `36-HOMELAB-CHANGE-PROMPT.md`; updated README.md, docs/deployment.md, docs/configuration.md, .env.example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v4.0-phases/36-pipeline-queue-backend-migration-redis-to-postgres-saq/36-RESEARCH.md
@.planning/milestones/v4.0-phases/36-pipeline-queue-backend-migration-redis-to-postgres-saq/36-PATTERNS.md

<interfaces>
<!-- Doc surfaces + operator facts. -->

README.md current statements to correct:
- line 44 Worker row lists `Redis` in its stack; line 46 `Redis | 6379 | Task queue broker and cache` → broker role moves to Postgres (Redis = cache + rate-limit + counters only).
- line 65 Mermaid `REDIS[("Redis 8 :6379")]`; the broker edge now points at Postgres.

docs/deployment.md current statements:
- line 28 redis row "Task-queue broker"; line 201 `PHAZE_REDIS_URL=redis://...` (stays, now cache-only); lines 313-323 env table + `<VAR>_FILE` secrets list (currently DATABASE_URL, REDIS_URL, OPENAI_API_KEY, ANTHROPIC_API_KEY, PHAZE_AGENT_TOKEN).

.env.example current shape: DATABASE_URL (29) + DATABASE_URL_FILE (30); REDIS_URL (36) + REDIS_URL_FILE (37). Add a parallel PHAZE_QUEUE_URL block.

Operator facts for the homelab prompt (RESEARCH §Runtime State Inventory, Pitfall 4/5, Security Domain; ROADMAP Step D):
- Add `PHAZE_QUEUE_URL` (libpq `postgresql://...`, NOT +asyncpg) to BOTH control and agent compose services; use the `<VAR>_FILE` secret convention.
- Image dep swap `saq[redis]`→`saq[postgres]` (rebuild required; psycopg3 wheels — confirm libpq present on the slim base, mirroring the Phase-30 essentia apt-layer lesson).
- `saq_jobs`/`saq_stats`/`saq_versions` are auto-created on first boot under a Postgres advisory lock — the DB role needs CREATE TABLE/INDEX in its schema and LISTEN/NOTIFY.
- New network edge: agents (`datum@nox`, `datum@lux`) must reach Postgres:5432 (firewall) — relaxes the prior D-25 agent-Postgres-free boundary.
- Connection budget: per-queue psycopg3 pools (controller 2/8, agent/per-agent 1/4) + SQLAlchemy engine pools must stay under Postgres `max_connections` — confirm the homelab `max_connections`.
- Redis stays running (cache/rate-limit/counters); it is no longer the broker.
- Cutover: live Redis jobs may be dropped — Phase-32 boot re-enqueue + cron self-heal `saq_jobs` from DB-truth (DISCOVERED files); no job-data migration needed. Sequence: redeploy control first (creates saq_jobs + re-enqueues), then agents.
- This is the migration-specific portion; final consolidation lands after Phase 38 if UI/control add env (ROADMAP Step D note).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the homelab change-prompt deliverable</name>
  <files>.planning/milestones/v4.0-phases/36-pipeline-queue-backend-migration-redis-to-postgres-saq/36-HOMELAB-CHANGE-PROMPT.md</files>
  <read_first>
    - .planning/milestones/v4.0-phases/36-pipeline-queue-backend-migration-redis-to-postgres-saq/36-RESEARCH.md (§Runtime State Inventory; Pitfall 4 connection budget; Pitfall 5 agent→Postgres edge; §Security Domain)
    - .planning/ROADMAP.md (Phase 36 Step D paragraph, line 251)
    - docs/deployment.md (lines 140-240 agent deploy steps + env vars; 280-330 secrets/validation — the existing homelab deploy shape to mirror)
  </read_first>
  <action>
    Create `36-HOMELAB-CHANGE-PROMPT.md` as a ready-to-paste prompt for the homelab repo agent, covering exactly the migration-specific changes: (1) add `PHAZE_QUEUE_URL` (libpq DSN, `<VAR>_FILE` secret) to BOTH the control and agent compose services; (2) image dependency swap `saq[redis]`→`saq[postgres]` and rebuild, with the libpq-on-slim-base verification note; (3) the `saq_jobs`/`saq_stats`/`saq_versions` first-boot auto-DDL and the required DB-role grants (CREATE in own schema + LISTEN/NOTIFY); (4) the new agent→Postgres:5432 firewall requirement (relaxing D-25) and an optional production credential-guard note (RESEARCH Open Q1); (5) the per-queue connection budget vs Postgres `max_connections` with the chosen pool sizes; (6) Redis stays as cache-only (no longer broker); (7) cutover/redeploy ordering — control first (creates saq_jobs + boot re-enqueue self-heals from DB-truth), then agents via `datum@nox` / `datum@lux`, no job-data migration. State clearly this is the migration-specific portion, with final consolidation after Phase 38. Never inline a real DSN with a password — use placeholders.
  </action>
  <verify>
    <automated>grep -q "PHAZE_QUEUE_URL" .planning/milestones/v4.0-phases/36-pipeline-queue-backend-migration-redis-to-postgres-saq/36-HOMELAB-CHANGE-PROMPT.md && grep -Eq "saq\[postgres\]" .planning/milestones/v4.0-phases/36-pipeline-queue-backend-migration-redis-to-postgres-saq/36-HOMELAB-CHANGE-PROMPT.md && grep -Eq "5432|firewall" .planning/milestones/v4.0-phases/36-pipeline-queue-backend-migration-redis-to-postgres-saq/36-HOMELAB-CHANGE-PROMPT.md && grep -q "saq_jobs" .planning/milestones/v4.0-phases/36-pipeline-queue-backend-migration-redis-to-postgres-saq/36-HOMELAB-CHANGE-PROMPT.md</automated>
  </verify>
  <acceptance_criteria>
    - The prompt mentions `PHAZE_QUEUE_URL` on both control and agent services with `_FILE` secret handling
    - It states the `saq[redis]`→`saq[postgres]` image dep swap + rebuild
    - It documents `saq_jobs` first-boot auto-DDL + required DB-role grants
    - It calls out the agent→Postgres:5432 firewall requirement and the connection budget vs `max_connections`
    - It specifies control-first → agents redeploy ordering and that Redis stays as cache-only
    - No real password/DSN is inlined (placeholders only)
  </acceptance_criteria>
  <done>The operator has a complete, paste-ready homelab change prompt for the migration-specific portion.</done>
</task>

<task type="auto">
  <name>Task 2: Update README, deployment/configuration docs, and .env.example</name>
  <files>README.md, docs/deployment.md, docs/configuration.md, .env.example</files>
  <read_first>
    - README.md (lines 40-70 services table + Mermaid; lines 96-128 enqueue/queue narrative)
    - docs/deployment.md (line 28 redis row; lines 195-210 agent env vars incl. PHAZE_REDIS_URL; lines 305-325 env table + `<VAR>_FILE` secrets list)
    - docs/configuration.md (the section documenting DATABASE_URL/REDIS_URL settings + the `_FILE` secrets-via-files section)
    - .env.example (lines 1-40: DATABASE_URL/_FILE at 29-30; REDIS_URL/_FILE at 36-37; the secrets header at 18-26)
  </read_first>
  <action>
    Update README.md so the Redis row/role reads "cache + rate-limit + counters" (no longer broker) and add Postgres as the SAQ queue broker (Worker stack + Mermaid broker edge now points at Postgres). In docs/deployment.md: change the redis row description from "Task-queue broker" to cache-only, add `PHAZE_QUEUE_URL` to the agent and control env-var tables (libpq DSN, with the agent→Postgres:5432 reachability note relaxing D-25), and add `queue_url` to the `<VAR>_FILE` secrets list (lines 313-323). In docs/configuration.md document the `PHAZE_QUEUE_URL` setting (libpq form, default derived from Postgres config, secret via `_FILE`) alongside DATABASE_URL/REDIS_URL. In .env.example add a `PHAZE_QUEUE_URL=postgresql://phaze:phaze@postgres:5432/phaze` block with a `# PHAZE_QUEUE_URL_FILE=/run/secrets/phaze_queue_url` sibling, mirroring the DATABASE_URL block, and note it must be the libpq form (NOT +asyncpg). Verify `scripts/update-project.sh` needs no manual edit (it auto-syncs dependency floors and is not service-list-bound here); only touch it if the new saq/redis floors require a cap, which they do not.
  </action>
  <verify>
    <automated>grep -q "PHAZE_QUEUE_URL" .env.example && grep -q "PHAZE_QUEUE_URL" docs/deployment.md && grep -q "PHAZE_QUEUE_URL" docs/configuration.md && ! grep -Eq "Task.queue broker and cache" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` contains a `PHAZE_QUEUE_URL` (libpq `postgresql://`) entry + `_FILE` sibling
    - `docs/deployment.md` documents `PHAZE_QUEUE_URL`, the agent→Postgres reachability note, and adds `queue_url` to the `<VAR>_FILE` list
    - `docs/configuration.md` documents the `PHAZE_QUEUE_URL` setting
    - README no longer describes Redis as the task-queue broker; Postgres is shown as the broker
    - `uv run pre-commit run --all-files` passes (yamllint/markdown hooks clean on touched docs)
  </acceptance_criteria>
  <done>In-repo docs and .env.example reflect Postgres-as-broker / Redis-as-cache and document the new secret-backed PHAZE_QUEUE_URL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file-server agents → control-host Postgres | new network edge (Postgres:5432) created by the broker move; documented for firewall scoping |
| operator secrets → compose env | `PHAZE_QUEUE_URL` carries DB credentials; docs must steer to `_FILE`/SOPS, never plaintext |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03 | Elevation of Privilege / Spoofing | agent→Postgres:5432 network edge | mitigate | Homelab prompt + deployment docs require firewalling agents→Postgres and a least-privilege DB role (CREATE only in its own schema); flags the optional production credential-guard analog (RESEARCH Open Q1) |
| T-36-02 | Information Disclosure | `PHAZE_QUEUE_URL` in compose/.env | mitigate | Docs + .env.example present the `<VAR>_FILE` secret convention (mirrors DATABASE_URL); never inline a password in the prompt or examples |
| T-36-10 | Tampering (first-boot DDL under shared schema) | `saq_jobs` auto-create | mitigate | Homelab prompt documents the advisory-lock-guarded idempotent `init_db()` and that the role should CREATE only in its own schema |
</threat_model>

<verification>
- Task 1 grep gate passes (homelab prompt covers all required topics)
- Task 2 grep gate passes; `uv run pre-commit run --all-files` clean on touched docs
- No real credentials appear in any touched file
</verification>

<success_criteria>
- A complete migration-specific homelab change prompt exists in the phase directory
- README/docs/.env.example describe Postgres-as-broker, Redis-as-cache, and the secret-backed PHAZE_QUEUE_URL
</success_criteria>

<output>
Create `.planning/milestones/v4.0-phases/36-pipeline-queue-backend-migration-redis-to-postgres-saq/36-04-SUMMARY.md` when done
</output>
