---
phase: 36-pipeline-queue-backend-migration-redis-to-postgres-saq
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/phaze/config.py
  - src/phaze/tasks/_shared/queue_factory.py
  - tests/test_queue_factory.py
autonomous: true
requirements: [REQ-36-1, REQ-36-5]
nyquist_compliant: true

must_haves:
  truths:
    - "saq[postgres] is the installed queue extra; psycopg3 + pool resolve; redis is an explicit dependency"
    - "PHAZE_QUEUE_URL is a settable libpq DSN that normalizes +asyncpg/+psycopg forms and is treated as a secret"
    - "build_pipeline_queue returns a PostgresQueue with both before-enqueue hooks and a dedicated cache_redis handle attached"
  artifacts:
    - path: "src/phaze/tasks/_shared/queue_factory.py"
      provides: "Single PostgresQueue construction seam (build_pipeline_queue)"
      contains: "PostgresQueue.from_url"
    - path: "src/phaze/config.py"
      provides: "queue_url Field + driver-strip validator + SECRET_FILE_FIELDS membership"
      contains: "queue_url"
    - path: "tests/test_queue_factory.py"
      provides: "Unit proof the factory returns PostgresQueue with both hooks and no connection at construction"
      contains: "build_pipeline_queue"
  key_links:
    - from: "src/phaze/tasks/_shared/queue_factory.py"
      to: "saq.queue.postgres.PostgresQueue"
      via: "PostgresQueue.from_url(url, name=, min_size=, max_size=)"
      pattern: "PostgresQueue\\.from_url"
    - from: "src/phaze/config.py queue_url"
      to: "BaseSettings.SECRET_FILE_FIELDS"
      via: "frozenset membership"
      pattern: "SECRET_FILE_FIELDS"
---

<objective>
Lay the foundation for the Redis→Postgres queue migration: swap the SAQ dependency extra, add the `PHAZE_QUEUE_URL` setting, and create the single `build_pipeline_queue` factory that all four construction sites will adopt in Plan 02.

Purpose: Establish the one seam (factory + config) that decouples the queue backend choice and pool sizing from every call site, and the secret-bearing DSN handling required before any `PostgresQueue` can be constructed.
Output: `saq[postgres]` + explicit `redis` deps installed; `queue_url` config field; `queue_factory.py` factory; `tests/test_queue_factory.py` unit proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v4.0-phases/36-pipeline-queue-backend-migration-redis-to-postgres-saq/36-RESEARCH.md
@.planning/milestones/v4.0-phases/36-pipeline-queue-backend-migration-redis-to-postgres-saq/36-PATTERNS.md
@.planning/milestones/v4.0-phases/36-pipeline-queue-backend-migration-redis-to-postgres-saq/36-VALIDATION.md

<interfaces>
<!-- Extracted from codebase. Executor: use these directly. -->

Existing DSN field shape (src/phaze/config.py:143-156):
- `database_url: str = Field(default="postgresql+asyncpg://phaze:phaze@postgres:5432/phaze", validation_alias=AliasChoices("PHAZE_DATABASE_URL","DATABASE_URL","database_url"))`
- `redis_url: str = Field(default="redis://redis:6379/0", validation_alias=AliasChoices("PHAZE_REDIS_URL","REDIS_URL","redis_url"))`
- `SECRET_FILE_FIELDS: ClassVar[frozenset[str]] = frozenset({"database_url","redis_url"})` (config.py:79, on BaseSettings; ControlSettings:302 and AgentSettings:331 union onto it)
- `field_validator` already imported (config.py:18); precedent `@field_validator("audfprint_url","panako_url")` at config.py:204

Hook callables to register (already exist):
- `phaze.tasks._shared.queue_defaults.apply_project_job_defaults`
- `phaze.tasks._shared.deterministic_key.apply_deterministic_key`

Module conventions to mirror from queue_defaults.py: `from __future__ import annotations`, `import structlog` + `logger = structlog.get_logger(__name__)`, `TYPE_CHECKING`-guarded saq import, explicit `__all__`.

Decided cache-Redis mechanism (RESEARCH Open Q3 — LOCKED for this phase):
- The factory attaches `q.cache_redis = redis.asyncio.Redis.from_url(cache_redis_url)` so the backend-agnostic before_enqueue/after_process counter hooks can read `getattr(job.queue, "cache_redis", None)` (the before_enqueue hook only receives `job`, so it must read off the queue object).
- Fileless tasks that have `ctx` (e.g. generate_proposals) read `ctx["redis"]` instead (set in Plan 02 startup). Both clients derive from `redis_url`. NOTHING reads `queue.redis`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap SAQ extra to postgres and pin redis as an explicit dependency</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml (the `dependencies` array — current `saq[redis]>=0.26.4` entry)
    - .planning/milestones/v4.0-phases/36-pipeline-queue-backend-migration-redis-to-postgres-saq/36-RESEARCH.md (§Standard Stack "Dependency change", §Package Legitimacy Audit, §Environment Availability)
  </read_first>
  <action>
    In pyproject.toml replace `saq[redis]>=0.26.4` with `saq[postgres]>=0.26.4`, and add an explicit `redis>=4.2,<8.0` dependency (dropping `saq[redis]` removes the transitive `redis`, but `redis.asyncio` is imported directly across the codebase — RESEARCH A4). Keep the dependency array alphabetically sorted per CLAUDE.md. Run `uv sync` to resolve `psycopg[pool]>=3.2.0` (pulled by the postgres extra) and regenerate uv.lock. All four packages (saq, psycopg, psycopg-pool, redis) are pre-approved in the RESEARCH Package Legitimacy Audit (authoritative-source-discovered, no [ASSUMED]/[SUS]/[SLOP]) — no legitimacy checkpoint required. Verify the install with `uv run python -c "import saq, psycopg, psycopg_pool; from saq.queue.postgres import PostgresQueue; print(saq.__version__)"`.
  </action>
  <verify>
    <automated>uv run python -c "import saq, psycopg, psycopg_pool, redis.asyncio; from saq.queue.postgres import PostgresQueue; print(saq.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `saq[postgres]>=0.26.4` and NOT `saq[redis]`
    - `pyproject.toml` contains an explicit `redis>=4.2,<8.0` dependency
    - `uv.lock` resolves `psycopg` >=3.2.0 and `psycopg-pool`
    - `from saq.queue.postgres import PostgresQueue` imports without error
    - `redis.asyncio` still imports (explicit dep, not transitive)
  </acceptance_criteria>
  <done>The postgres extra is installed, psycopg3 + pool resolve, and redis is a first-class dependency — PostgresQueue is importable.</done>
</task>

<task type="auto">
  <name>Task 2: Add PHAZE_QUEUE_URL setting with driver-strip validator and secret handling</name>
  <files>src/phaze/config.py</files>
  <read_first>
    - src/phaze/config.py (lines 69-160: BaseSettings, SECRET_FILE_FIELDS at 79, database_url/redis_url Fields at 143-156; lines 200-220 field_validator precedent; ControlSettings:298-302; AgentSettings:316-343, production guard 464-482)
    - .planning/milestones/v4.0-phases/36-pipeline-queue-backend-migration-redis-to-postgres-saq/36-RESEARCH.md (§Code Examples "Deriving the psycopg3 DSN"; §Security Domain V5/V6)
    - .planning/milestones/v4.0-phases/36-pipeline-queue-backend-migration-redis-to-postgres-saq/36-PATTERNS.md (§config.py — new queue_url field)
  </read_first>
  <action>
    On `BaseSettings`, add a `queue_url: str` Field defaulting to the libpq form `postgresql://phaze:phaze@postgres:5432/phaze` (NOT `postgresql+asyncpg://` — psycopg3's AsyncConnectionPool needs a raw libpq DSN, RESEARCH Anti-Patterns), with `validation_alias=AliasChoices("PHAZE_QUEUE_URL","queue_url")` and a description noting it is the psycopg3 broker DSN. Add a `@field_validator("queue_url", mode="before")` named `_strip_sqlalchemy_driver` that normalizes `postgresql+asyncpg://` and `postgresql+psycopg://` prefixes to `postgresql://`, mirroring the existing `audfprint_url/panako_url` validator precedent. Add `"queue_url"` to the `BaseSettings.SECRET_FILE_FIELDS` frozenset (it carries DB credentials — the ControlSettings:302 / AgentSettings:331 unions inherit it automatically). Do NOT add a production credential guard in this phase (RESEARCH Open Q1 — deferred to homelab discussion). Never log the full `queue_url`.
  </action>
  <verify>
    <automated>uv run python -c "from phaze.config import BaseSettings; s=BaseSettings(queue_url='postgresql+asyncpg://u:p@h:5432/d'); assert s.queue_url=='postgresql://u:p@h:5432/d', s.queue_url; assert 'queue_url' in BaseSettings.SECRET_FILE_FIELDS"</automated>
  </verify>
  <acceptance_criteria>
    - `BaseSettings().queue_url` defaults to a `postgresql://` (libpq) DSN, never `+asyncpg`
    - Passing `postgresql+asyncpg://...` or `postgresql+psycopg://...` normalizes to `postgresql://...`
    - `"queue_url"` is a member of `BaseSettings.SECRET_FILE_FIELDS`
    - `uv run mypy src/phaze/config.py` passes
  </acceptance_criteria>
  <done>`PHAZE_QUEUE_URL` is a settable, driver-normalized, secret-backed libpq DSN available on both roles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create build_pipeline_queue factory and its unit proof (Wave 0)</name>
  <files>src/phaze/tasks/_shared/queue_factory.py, tests/test_queue_factory.py</files>
  <read_first>
    - src/phaze/tasks/_shared/queue_defaults.py (module shape: __future__ import, structlog logger, TYPE_CHECKING saq import, __all__; the apply_project_job_defaults hook)
    - src/phaze/tasks/_shared/deterministic_key.py (apply_deterministic_key, and lines 108-135 where the counter hooks read getattr(job.queue, "redis", None) — confirms the .cache_redis seam this factory provides)
    - tests/test_web/test_saq_mount.py (no-lifespan/no-pool unit style; source-token inspection proving no connection opens)
    - tests/test_deterministic_key.py (direct-hook-invocation style; FakeRedis import from tests._queue_fakes)
    - .planning/milestones/v4.0-phases/36-pipeline-queue-backend-migration-redis-to-postgres-saq/36-PATTERNS.md (§queue_factory.py — recommended body + import note)
  </read_first>
  <behavior>
    - Test 1: `build_pipeline_queue("controller", "postgresql://...", cache_redis_url="redis://...")` returns an instance of `saq.queue.postgres.PostgresQueue`.
    - Test 2: the returned queue has BOTH `apply_project_job_defaults` and `apply_deterministic_key` registered on its before-enqueue callbacks (`queue._before_enqueues`).
    - Test 3: the returned queue exposes a `cache_redis` attribute (a redis.asyncio client) so the counter hooks can read `getattr(job.queue, "cache_redis", None)`.
    - Test 4: constructing the factory opens NO connection (PostgresQueue pool is created with `open=False`) — assert via the same source/AST inspection style test_saq_mount.py uses, no live DB required.
  </behavior>
  <action>
    Create `src/phaze/tasks/_shared/queue_factory.py` exporting `build_pipeline_queue(name: str, url: str, *, cache_redis_url: str, min_size: int = 1, max_size: int = 4) -> PostgresQueue`. Import `from saq.queue.postgres import PostgresQueue` (NOT `from saq import Queue`). Body: `q = PostgresQueue.from_url(url, name=name, min_size=min_size, max_size=max_size)`, then `q.register_before_enqueue(apply_project_job_defaults)` and `q.register_before_enqueue(apply_deterministic_key)` (Phase 27 / Phase 35 hooks carry over unchanged — they live on the backend-agnostic base Queue), then attach the dedicated cache handle `q.cache_redis = redis.asyncio.Redis.from_url(cache_redis_url)`. Mirror queue_defaults.py module conventions and add `__all__`. Write `tests/test_queue_factory.py` covering the four behaviors above with no live DB/Redis (construction only).
  </action>
  <verify>
    <automated>uv run pytest tests/test_queue_factory.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_queue_factory.py -x` exits 0
    - `build_pipeline_queue(...)` returns a `PostgresQueue`
    - Both hooks are present in the returned queue's `_before_enqueues`
    - Returned queue has a `cache_redis` attribute; no connection is opened at construction
    - `uv run mypy src/phaze/tasks/_shared/queue_factory.py` passes
  </acceptance_criteria>
  <done>One factory is the sole PostgresQueue construction seam, attaches both hooks plus a decoupled cache_redis handle, and is proven by a passing unit test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config → process env | `PHAZE_QUEUE_URL` is operator-supplied and carries DB credentials |
| package registry → build | swapping the SAQ extra resolves new transitive packages (psycopg, psycopg-pool) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02 | Information Disclosure | `queue_url` DSN (config.py) | mitigate | Add `queue_url` to `SECRET_FILE_FIELDS` (`<VAR>_FILE`/SOPS convention like `database_url`); never log the full DSN — mirror `redis_url` handling |
| T-36-05 | Tampering (input validation) | `PHAZE_QUEUE_URL` value | mitigate | `_strip_sqlalchemy_driver` validator normalizes to a libpq DSN at load — rejects the `+asyncpg` dialect form psycopg3 cannot parse (ASVS V5) |
| T-36-SC | Tampering (supply chain) | `uv add saq[postgres]` / psycopg install | accept | All four packages pre-approved in RESEARCH Package Legitimacy Audit (already-resolved or official `saq[postgres]` extra; no [ASSUMED]/[SUS]/[SLOP]); `uv sync` pins via lockfile. No blocking checkpoint required. |
</threat_model>

<verification>
- `uv run pytest tests/test_queue_factory.py -x` green
- `uv run python -c "from saq.queue.postgres import PostgresQueue"` succeeds
- `uv run ruff check . && uv run mypy src/phaze` clean for touched files
</verification>

<success_criteria>
- saq[postgres] + explicit redis installed; psycopg3 + pool resolved in uv.lock
- `PHAZE_QUEUE_URL` is a normalized, secret-backed libpq DSN on both roles
- `build_pipeline_queue` exists, returns PostgresQueue with both hooks + cache_redis, proven by a passing unit test
</success_criteria>

<output>
Create `.planning/milestones/v4.0-phases/36-pipeline-queue-backend-migration-redis-to-postgres-saq/36-01-SUMMARY.md` when done
</output>
