---
phase: 32-pipeline-reboot-resilience-re-enqueue
plan: 03
type: execute
wave: 3
depends_on: [32-02]
files_modified:
  - src/phaze/tasks/controller.py
  - tests/test_tasks/test_controller_reenqueue.py
autonomous: true
requirements: [RESIL-01, RESIL-02]
must_haves:
  truths:
    - "The controller startup hook stashes ctx['task_router'] = AgentTaskRouter(redis_url) and closes it in shutdown"
    - "The controller startup hook calls reenqueue_discovered(ctx) once after building its resources, so a reboot resumes work with no manual 'Run Analysis'"
    - "A periodic CronJob(reenqueue_discovered, cron='*/5 * * * *') is registered in the controller settings cron list, alongside reap_stalled_scans and refresh_tracklists"
    - "reenqueue_discovered is registered in the controller settings functions list"
  artifacts:
    - path: "src/phaze/tasks/controller.py"
      provides: "task_router lifecycle + startup re-enqueue call + CronJob + functions registration"
      contains: "reenqueue_discovered"
    - path: "tests/test_tasks/test_controller_reenqueue.py"
      provides: "Tests asserting startup stashes/closes task_router + calls reenqueue once, and the cron/functions registration"
      contains: "def test_"
  key_links:
    - from: "src/phaze/tasks/controller.py::startup"
      to: "phaze.tasks.reenqueue.reenqueue_discovered"
      via: "ctx['task_router'] stash + one boot-time call"
      pattern: "reenqueue_discovered"
    - from: "src/phaze/tasks/controller.py::settings"
      to: "CronJob(reenqueue_discovered, cron='*/5 * * * *')"
      via: "cron_jobs list entry"
      pattern: "CronJob\\(reenqueue_discovered"
---

<objective>
Wire `reenqueue_discovered` into the controller worker so reboot recovery is automatic: stash a single `AgentTaskRouter` in `ctx` at startup (closed at shutdown), call the re-enqueue once on boot (immediate post-reboot recovery — Redis is empty after a reboot, so all DISCOVERED files re-enqueue), and register a 5-minute `CronJob` for mid-run stall recovery (CONTEXT "Trigger" + "Location" decisions; RESEARCH §Q2).

Purpose: Without this wiring the Wave-2 task is dead code. The startup call is the "no manual Run Analysis after reboot" guarantee; the cron is the "catch mid-run stalls without a restart" guarantee. The task_router must be built ONCE and reused (RESEARCH Pitfall 4) — mirror the existing `ctx["discogs_client"]` create/close lifecycle.
Output: edits to `tasks/controller.py` (startup, shutdown, settings) + a registration/behavior test module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v4.0-phases/32-pipeline-reboot-resilience-re-enqueue/32-RESEARCH.md
@.planning/milestones/v4.0-phases/32-pipeline-reboot-resilience-re-enqueue/32-CONTEXT.md
@.planning/milestones/v4.0-phases/32-pipeline-reboot-resilience-re-enqueue/32-VALIDATION.md

<interfaces>
<!-- The controller to edit. From src/phaze/tasks/controller.py:44-133. -->
- `async def startup(ctx)` — builds `task_engine`, `ctx["async_session"]`, `ctx["discogs_client"] = DiscogsographyClient(...)`, `ctx["proposal_service"]`, `ctx["queue"] = queue`. The create/close lifecycle to mirror: `ctx["discogs_client"]` created in startup, `await ctx["discogs_client"].close()` in shutdown.
- `async def shutdown(ctx)` — disposes `task_engine`, closes `discogs_client`.
- module-level `queue = Queue.from_url(get_settings().redis_url, name="controller")` (the CONTROLLER queue — NOT for process_file).
- `settings = {..., "functions": [generate_proposals, match_tracklist_to_discogs, search_tracklist, scrape_and_store_tracklist, reap_stalled_scans], "cron_jobs": [CronJob(refresh_tracklists, cron="0 3 1 * *"), CronJob(reap_stalled_scans, cron="* * * * *")], "startup": startup, "shutdown": shutdown}`.
- `from saq import CronJob, Queue` already imported. `get_settings().redis_url` available (config.py:153).

<!-- The task to wire. From src/phaze/tasks/reenqueue.py (Wave 2). -->
- `async def reenqueue_discovered(ctx: dict[str, Any]) -> dict[str, int]` — reads `ctx["async_session"]` + `ctx["task_router"]`.

<!-- The router to stash. From src/phaze/services/agent_task_router.py:64-77,172-176. -->
- `AgentTaskRouter(redis_url: str)`; `.queue_for(agent_id)`; `async def close() -> None` (disconnects every cached queue; idempotent).

<!-- Test patterns. From tests/test_tasks/test_controller_startup_banner.py:1-40. -->
- Controller-startup tests monkeypatch the heavyweight constructors (`create_async_engine`, `async_sessionmaker`, `DiscogsographyClient`, `load_prompt_template`, `ProposalService`, `get_settings`) with `MagicMock`/lambdas, then call `startup(ctx)` and assert on ctx / logs. Import the module AFTER patching get_settings.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire task_router lifecycle + startup re-enqueue call + cron/functions registration</name>
  <files>src/phaze/tasks/controller.py</files>
  <read_first>
    - src/phaze/tasks/controller.py (the file being modified — startup, shutdown, settings, the discogs_client create/close lifecycle to mirror)
    - src/phaze/tasks/reenqueue.py (reenqueue_discovered — what to import + call)
    - src/phaze/services/agent_task_router.py:64-77,172-176 (AgentTaskRouter + close())
    - .planning/milestones/v4.0-phases/32-pipeline-reboot-resilience-re-enqueue/32-RESEARCH.md §Q2 + Pitfalls 3,4
  </read_first>
  <action>
    Edit `src/phaze/tasks/controller.py`. Add imports: `from phaze.services.agent_task_router import AgentTaskRouter` and `from phaze.tasks.reenqueue import reenqueue_discovered`. In `startup(ctx)`, AFTER the existing resource setup (and after `ctx["queue"] = queue`), construct `ctx["task_router"] = AgentTaskRouter(cfg.redis_url)` (reuse the `cfg` already bound at the top of startup), then call `await reenqueue_discovered(ctx)` once and log its returned counts at INFO (e.g. bind `reenqueued`/`skipped`). Guard the boot-time call so a re-enqueue failure NEVER aborts controller boot: the Wave-2 task already swallows `NoActiveAgentError`, but wrap the call in a broad `try/except Exception` that logs `logger.exception("reenqueue on startup failed")` and continues — boot resilience is non-negotiable (RESEARCH Pitfall 3). In `shutdown(ctx)`, mirror the discogs_client cleanup: `task_router = ctx.get("task_router")` then `if task_router is not None: await task_router.close()`. In the module-level `settings` dict, append `reenqueue_discovered` to `functions` and add `CronJob(reenqueue_discovered, cron="*/5 * * * *")` to `cron_jobs` (5-field standard form, alongside the existing `reap_stalled_scans` / `refresh_tracklists`; keep the existing `# type: ignore[type-var]` style on the new CronJob to match). Add a brief comment noting the cron cadence (every 5 min — re-enqueue scans more rows than the 1-min reaper, so 5 min balances recovery latency vs DB load; CONTEXT Claude's Discretion). Keep ruff clean (150-char, double quotes, import sort) and mypy strict.
  </action>
  <acceptance_criteria>
    - `grep -n "reenqueue_discovered" src/phaze/tasks/controller.py` matches the import, the startup call, the functions entry, and the CronJob.
    - `grep -n "task_router" src/phaze/tasks/controller.py` matches the startup stash and the shutdown close.
    - `grep -n 'cron="\*/5 \* \* \* \*"' src/phaze/tasks/controller.py` matches the new CronJob.
    - `uv run mypy src/phaze/tasks/controller.py` clean; `uv run ruff check src/phaze/tasks/controller.py` clean.
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy src/phaze/tasks/controller.py && uv run ruff check src/phaze/tasks/controller.py</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Test controller startup re-enqueue + cron/functions registration</name>
  <files>tests/test_tasks/test_controller_reenqueue.py</files>
  <read_first>
    - tests/test_tasks/test_controller_startup_banner.py:1-40 (monkeypatch-the-constructors startup-test pattern, import-after-patch)
    - src/phaze/tasks/controller.py (the wired startup/shutdown/settings under test)
    - src/phaze/services/agent_task_router.py (AgentTaskRouter — patch its constructor to a stub exposing close())
  </read_first>
  <action>
    Create `tests/test_tasks/test_controller_reenqueue.py`. Two groups of tests:
    (1) Registration (`-k cron` / `-k functions`): import `phaze.tasks.controller` and assert `reenqueue_discovered` is in `settings["functions"]`; assert `settings["cron_jobs"]` contains a `CronJob` whose `function` is `reenqueue_discovered` with `cron == "*/5 * * * *"` (inspect the CronJob's `function`/`cron` attributes). Also assert the existing `reap_stalled_scans` + `refresh_tracklists` crons are still present (no regression).
    (2) Startup behavior (`-k startup`): mirror `test_controller_startup_banner.py` — monkeypatch `create_async_engine`, `async_sessionmaker`, `DiscogsographyClient`, `load_prompt_template`, `ProposalService`, `get_settings` (fake cfg with `redis_url`, `database_url`, `models_path`, `log_level`, `log_json`, etc.), AND monkeypatch `phaze.tasks.controller.AgentTaskRouter` to return a stub object with an async `close()` and a `queue_for`, AND monkeypatch `phaze.tasks.controller.reenqueue_discovered` to an `AsyncMock` returning `{"reenqueued": 3, "skipped": 1}`. Call `startup(ctx)`; assert `ctx["task_router"]` is the stub AND `reenqueue_discovered` was awaited exactly once with `ctx`. Then call `shutdown(ctx)` and assert the stub router's `close()` was awaited. Add a test that when the patched `reenqueue_discovered` raises, `startup` still completes (does not propagate) — proves boot resilience.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_tasks/test_controller_reenqueue.py -q` passes.
    - A test asserts the `*/5 * * * *` CronJob for `reenqueue_discovered` is registered and that `reap_stalled_scans` + `refresh_tracklists` crons remain.
    - A test asserts startup stashes `ctx["task_router"]`, awaits `reenqueue_discovered(ctx)` once, and shutdown awaits `task_router.close()`.
    - A test asserts a raising `reenqueue_discovered` does NOT abort startup.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_tasks/test_controller_reenqueue.py -q</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| controller worker boot/cron → Redis | Boot-time + periodic internal enqueue; no new external input surface. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | Denial of Service | A re-enqueue exception aborting controller boot (restart loop) | mitigate | Boot-time call wrapped in `try/except Exception` + `logger.exception`; test asserts startup survives a raising re-enqueue |
| T-32-06 | Denial of Service | Per-tick AgentTaskRouter construction leaking Redis connection pools | mitigate | Single router built in startup, stashed in ctx, `close()`d in shutdown; test asserts the stash + close lifecycle |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `uv run pytest tests/test_tasks/test_controller_reenqueue.py tests/test_tasks/test_controller_startup_banner.py -q` green (no startup-banner regression).
- `uv run mypy src/phaze/tasks/controller.py` clean; `uv run ruff check src/phaze/tasks/controller.py` clean.
- Phase gate: `uv run pytest --cov --cov-report=term-missing` green with coverage ≥85%; full pre-commit (`pre-commit run --all-files`) passes (never `--no-verify`).
</verification>

<success_criteria>
- The controller boots → re-enqueues DISCOVERED files once → schedules a 5-min cron, with the task_router built once and closed at shutdown.
- A re-enqueue failure can never crash controller boot.
</success_criteria>

<output>
Create `.planning/milestones/v4.0-phases/32-pipeline-reboot-resilience-re-enqueue/32-03-SUMMARY.md` when done.
</output>
