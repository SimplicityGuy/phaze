---
phase: 32-pipeline-reboot-resilience-re-enqueue
plan: 01
type: execute
wave: 1
depends_on: [32-00]
files_modified:
  - src/phaze/services/analysis_enqueue.py
  - src/phaze/routers/pipeline.py
  - tests/test_services/test_analysis_enqueue.py
  - tests/test_routers/test_pipeline.py
autonomous: true
requirements: [RESIL-03, RESIL-05]
must_haves:
  truths:
    - "A single shared helper produces the deterministic key f'process_file:{file_id}' for both the dashboard path and the (Wave-2) re-enqueue path"
    - "Every process_file enqueue carries the COMPLETE 5-field ProcessFilePayload plus timeout=14400 and explicit retries=2"
    - "The dashboard 'Run Analysis' path now sets the deterministic key it previously lacked"
  artifacts:
    - path: "src/phaze/services/analysis_enqueue.py"
      provides: "process_file_job_key(file_id) + enqueue_process_file(queue, file, agent_id, models_path) in a FastAPI-free module"
      exports: ["process_file_job_key", "enqueue_process_file"]
    - path: "src/phaze/routers/pipeline.py"
      provides: "_enqueue_analysis_jobs delegating to the shared helper"
      contains: "enqueue_process_file"
  key_links:
    - from: "src/phaze/routers/pipeline.py::_enqueue_analysis_jobs"
      to: "src/phaze/services/analysis_enqueue.py::enqueue_process_file"
      via: "import + per-file call"
      pattern: "enqueue_process_file"
    - from: "src/phaze/services/analysis_enqueue.py::enqueue_process_file"
      to: "queue.enqueue"
      via: "key=process_file_job_key(file.id), timeout=14400, retries=2, **payload"
      pattern: "process_file_job_key"
---

<objective>
Create the single source of truth for the `process_file` deterministic key + complete payload + job policy, in a FastAPI-FREE module, and refactor the dashboard "Run Analysis" path to call it. This is the seam that guarantees BOTH producers (dashboard + the Wave-2 reboot re-enqueue) emit the IDENTICAL key `process_file:<file_id>` so SAQ's per-queue dedup works (CONTEXT "Dedup" decision; RESEARCH §Q4).

Purpose: Today `_enqueue_analysis_jobs` (routers/pipeline.py:64-83) enqueues with NO key → random uuid1 → zero dedup. Centralizing the key+payload+policy is the load-bearing refactor; the Wave-2 task imports the same helper so the two paths cannot drift.
Output: `services/analysis_enqueue.py` with `process_file_job_key` + `enqueue_process_file`; `routers/pipeline.py::_enqueue_analysis_jobs` delegating to it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v4.0-phases/32-pipeline-reboot-resilience-re-enqueue/32-RESEARCH.md
@.planning/milestones/v4.0-phases/32-pipeline-reboot-resilience-re-enqueue/32-CONTEXT.md
@.planning/milestones/v4.0-phases/32-pipeline-reboot-resilience-re-enqueue/32-VALIDATION.md

<interfaces>
<!-- The current dashboard enqueue path to refactor. From src/phaze/routers/pipeline.py:44-83. -->
- `async def _enqueue_analysis_jobs(queue: Any, files: list[FileRecord], agent_id: str, models_path: str) -> None` — loops files, builds `ProcessFilePayload(file_id=f.id, original_path=f.original_path, file_type=f.file_type, agent_id=agent_id, models_path=models_path)`, then `await queue.enqueue("process_file", timeout=14400, retries=2, **payload.model_dump(mode="json"))`. NO `key=` today.

<!-- Payload contract. From src/phaze/schemas/agent_tasks.py:28-37. -->
- `class ProcessFilePayload(BaseModel)` — `model_config = ConfigDict(extra="forbid")`; fields: `file_id: uuid.UUID`, `original_path: str`, `file_type: str`, `agent_id: str`, `models_path: str`. All five required.

<!-- File model. From src/phaze/models/file.py:47-66. -->
- `class FileRecord` — `.id: uuid.UUID`, `.original_path: str`, `.file_type: str`, `.agent_id: str`. (`enqueue_process_file` reads only these — already loaded by get_files_by_state.)

<!-- SAQ kwarg routing (verified). From 32-RESEARCH §Q1. -->
- `Queue.enqueue("process_file", key=..., timeout=..., retries=..., **payload_kwargs)`: `key`/`timeout`/`retries` are `Job` dataclass fields → set as Job properties; the 5 payload names are NOT Job fields → routed into `job.kwargs` (the worker payload). No collision.

<!-- Import-boundary rule. From 32-RESEARCH §Q4. -->
- The helper MUST live in a FastAPI-free module: it may import only `saq` (type-only / TYPE_CHECKING), `phaze.schemas.agent_tasks.ProcessFilePayload`, `phaze.models.file.FileRecord`, and stdlib `uuid`. `services/pipeline.py` is confirmed FastAPI-free; a NEW `services/analysis_enqueue.py` keeps concerns separate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create FastAPI-free shared helper services/analysis_enqueue.py</name>
  <files>src/phaze/services/analysis_enqueue.py, tests/test_services/test_analysis_enqueue.py</files>
  <read_first>
    - src/phaze/routers/pipeline.py:44-83 (the exact payload-build + timeout/retries to preserve)
    - src/phaze/schemas/agent_tasks.py:28-37 (ProcessFilePayload, extra="forbid")
    - src/phaze/models/file.py:47-66 (FileRecord fields read by the helper)
    - .planning/milestones/v4.0-phases/32-pipeline-reboot-resilience-re-enqueue/32-RESEARCH.md §Q4 (helper shape + import boundary) and §Q1 (kwarg routing)
    - tests/_queue_fakes.py (FakeQueue.captured_policy holds the `key`; use it to assert the key format)
  </read_first>
  <behavior>
    - `process_file_job_key(file_id)` returns exactly `f"process_file:{file_id}"` for a `uuid.UUID` (string-interpolated).
    - `enqueue_process_file(queue, file, agent_id, models_path)` builds a complete `ProcessFilePayload` (all 5 fields from the FileRecord + agent_id + models_path) and calls `queue.enqueue("process_file", key=process_file_job_key(file.id), timeout=14400, retries=2, **payload.model_dump(mode="json"))`, returning whatever `queue.enqueue` returns (a Job, or None on dedup).
    - The captured policy carries `key == "process_file:<file.id>"`, `timeout == 14400`, `retries == 2`; the captured payload carries the 5 ProcessFilePayload fields and nothing else.
  </behavior>
  <action>
    Create `src/phaze/services/analysis_enqueue.py` with `from __future__ import annotations`, importing only `uuid`, `ProcessFilePayload` from `phaze.schemas.agent_tasks`, and `FileRecord` from `phaze.models.file` (the latter two under `TYPE_CHECKING` where they are only annotations; `ProcessFilePayload` must be a real import because it is constructed). Define `def process_file_job_key(file_id: uuid.UUID) -> str` returning `f"process_file:{file_id}"`. Define `async def enqueue_process_file(queue: Any, file: FileRecord, agent_id: str, models_path: str) -> Any` that builds the payload and enqueues with `key=process_file_job_key(file.id)`, `timeout=14400`, `retries=2`, `**payload.model_dump(mode="json")`, and RETURNS the enqueue result (so the Wave-2 loop can count `None` as a dedup skip). Copy the inline rationale comments from `_enqueue_analysis_jobs` explaining WHY `timeout=14400`/`retries=2` are explicit (apply_project_job_defaults would clobber retries to worker_max_retries=4 — RESEARCH Pitfall 2). Do NOT import anything from `phaze.routers` or `fastapi`. mypy strict applies (this is under src/, not tests/) — fully annotate; `queue`/return as `Any` matches the existing `_enqueue_analysis_jobs` signature. Then write `tests/test_services/test_analysis_enqueue.py` covering the `behavior` block above using `FakeQueue` from `tests._queue_fakes`: a `-k key` test asserting `process_file_job_key` and the captured key on the queue, and a `-k payload` test asserting the 5-field payload + `timeout=14400` + `retries=2` in `captured`/`captured_policy`.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_services/test_analysis_enqueue.py -q` passes.
    - `process_file_job_key(uuid.UUID(int=1)) == "process_file:00000000-0000-0000-0000-000000000001"`.
    - A test asserts `queue.captured_policy[0]["key"] == f"process_file:{file.id}"`, `["timeout"] == 14400`, `["retries"] == 2`, and `queue.captured[0][1]` has exactly the 5 ProcessFilePayload keys.
    - `uv run mypy src/phaze/services/analysis_enqueue.py` clean; module imports neither `fastapi` nor `phaze.routers`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_services/test_analysis_enqueue.py -q && uv run mypy src/phaze/services/analysis_enqueue.py</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Refactor routers/pipeline.py::_enqueue_analysis_jobs to delegate to the helper</name>
  <files>src/phaze/routers/pipeline.py, tests/test_routers/test_pipeline.py</files>
  <read_first>
    - src/phaze/routers/pipeline.py:44-83 (the loop to refactor) and :225-252 (the HTMX `/pipeline/analyze` caller that also funnels through it)
    - src/phaze/services/analysis_enqueue.py (the helper created in Task 1)
    - tests/test_routers/test_pipeline.py (existing dashboard analyze tests + how they wire FakeQueue/FakeTaskRouter via tests/_queue_fakes.py)
  </read_first>
  <action>
    Edit `_enqueue_analysis_jobs` so its body loops `for f in files:` and calls `await enqueue_process_file(queue, f, agent_id, models_path)` (import `enqueue_process_file` from `phaze.services.analysis_enqueue`). Remove the now-duplicated inline `ProcessFilePayload` construction and the inline `queue.enqueue("process_file", timeout=14400, retries=2, ...)` — the policy now lives once in the helper. Keep the surrounding docstring but update it to note the key + policy now come from the shared helper so the dashboard and reboot paths cannot drift. Do NOT change the function signature (`queue, files, agent_id, models_path`) or any caller (`trigger_analysis`, `trigger_analysis_ui`). Then ADD a `-k key` assertion to the existing dashboard analyze test in `tests/test_routers/test_pipeline.py`: drive the analyze endpoint with seeded DISCOVERED files + a seeded active agent (use `seed_active_agent` + the fake-wiring helpers from `tests/_queue_fakes.py`), then assert the per-agent queue's `captured_policy[i]["key"] == f"process_file:{file_id}"` for each enqueued file — proving the dashboard producer now emits the deterministic key.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_routers/test_pipeline.py -q` passes including the new key assertion.
    - `grep -n "ProcessFilePayload(" src/phaze/routers/pipeline.py` no longer matches inside `_enqueue_analysis_jobs` (the payload build moved to the helper).
    - `grep -n "enqueue_process_file" src/phaze/routers/pipeline.py` matches (delegation in place).
    - `uv run mypy src/phaze/routers/pipeline.py` clean.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_routers/test_pipeline.py -q && uv run mypy src/phaze/routers/pipeline.py</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Refactor of an existing internal enqueue path; no new external input. The `process_file` payload is still validated by `ProcessFilePayload` `extra="forbid"`. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | Redis key built from `file_id` | mitigate | `file_id` is a server-generated UUID interpolated as a UUID string into `process_file:<id>` — no untrusted free-text in the key (RESEARCH §Security) |
| T-32-05 | Denial of Service | Dropping explicit timeout/retries lets apply_project_job_defaults clobber retries→4 (4x re-analysis churn) | mitigate | Helper always passes `timeout=14400, retries=2`; Task-1 test asserts both on `captured_policy` |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `uv run pytest tests/test_services/test_analysis_enqueue.py tests/test_routers/test_pipeline.py -q` green.
- `uv run mypy src/phaze/services/analysis_enqueue.py src/phaze/routers/pipeline.py` clean.
- Import boundary: `services/analysis_enqueue.py` imports neither `fastapi` nor `phaze.routers`.
</verification>

<success_criteria>
- One shared FastAPI-free helper owns the key + complete payload + timeout/retries.
- The dashboard path delegates to it and provably emits `process_file:<file_id>`.
</success_criteria>

<output>
Create `.planning/milestones/v4.0-phases/32-pipeline-reboot-resilience-re-enqueue/32-01-SUMMARY.md` when done.
</output>
