---
phase: 34-pipeline-queue-depth-status-double-enqueue-guard
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - src/phaze/routers/pipeline.py
  - src/phaze/templates/pipeline/partials/stats_bar.html
  - tests/test_routers/test_pipeline.py
autonomous: true
requirements: [Q34-3]
must_haves:
  truths:
    - "Both /pipeline/ (initial render) and /pipeline/stats (5s poll) carry agent_busy, controller_busy, the four sub-counts, and a precomputed queue_progress_percent"
    - "The dashboard poll never 500s when app.state queues are absent (degrade path returns zeros)"
    - "The 5s poll response carries hx-swap-oob store-write nodes that push agentBusy/controllerBusy into $store.pipeline"
  artifacts:
    - path: "src/phaze/routers/pipeline.py"
      provides: "get_queue_activity wired into dashboard() + pipeline_stats_partial() contexts with guarded percent"
      contains: "get_queue_activity"
    - path: "src/phaze/templates/pipeline/partials/stats_bar.html"
      provides: "OOB store-write nodes agent-busy-seed / controller-busy-seed gated by oob_counts"
      contains: "agent-busy-seed"
  key_links:
    - from: "src/phaze/routers/pipeline.py dashboard()/pipeline_stats_partial()"
      to: "services.pipeline.get_queue_activity"
      via: "await get_queue_activity(request.app.state, session)"
      pattern: "get_queue_activity"
    - from: "stats_bar.html OOB nodes"
      to: "$store.pipeline.agentBusy / controllerBusy"
      via: "x-init store-write on hx-swap-oob nodes, gated by oob_counts"
      pattern: "hx-swap-oob"
---

<objective>
Surface the queue-activity counts through the EXISTING 5s `/pipeline/stats` poll and the initial `/pipeline/` full-page render — no new loop. Wire `get_queue_activity` into BOTH `dashboard()` and `pipeline_stats_partial()` contexts, precompute the DB-derived progress percent (guarded against divide-by-zero), and add the `hx-swap-oob` store-write nodes to `stats_bar.html` that push `agentBusy`/`controllerBusy` into `$store.pipeline` on each tick — mirroring the existing `discovered`/`analyzed` OOB pattern exactly.

Purpose: Make the live queue depth available to the card (Plan 03) and button gating (Plan 04), and prove the poll cannot 500.
Output: Router context wiring + percent + the two new OOB store-write nodes + router tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v4.0-phases/34-pipeline-queue-depth-status-double-enqueue-guard/34-CONTEXT.md
@.planning/milestones/v4.0-phases/34-pipeline-queue-depth-status-double-enqueue-guard/34-RESEARCH.md
@.planning/milestones/v4.0-phases/34-pipeline-queue-depth-status-double-enqueue-guard/34-VALIDATION.md

<interfaces>
<!-- Contracts the executor wires together. -->
- Service (Plan 01): await get_queue_activity(app_state, session) -> dict[str,int] with keys agent_queued, agent_active, controller_queued, controller_active, agent_busy, controller_busy. Import via: from phaze.services.pipeline import get_files_by_state, get_pipeline_stats, get_queue_activity.
- dashboard() (routers/pipeline.py ~169) builds context = {request, stats, current_page, settings_batch_size, agents, recent_scans} and returns templates.TemplateResponse(name="pipeline/dashboard.html", ...). Pass request.app.state as app_state.
- pipeline_stats_partial() (~207) returns pipeline/partials/stats_bar.html with context={request, stats, settings_batch_size, oob_counts: True}. stats is the dict from get_pipeline_stats (key analyzed).
- Percent: denom = stats["analyzed"] + activity["agent_busy"]; queue_progress_percent = round(stats["analyzed"] / denom * 100) if denom else 0. Pass into BOTH contexts as an int. The card (Plan 03) consumes it; keep it server-computed for unit-testability (RESEARCH A3).
- stats_bar.html existing OOB pattern (lines 45-48): inside {% if oob_counts %} a paragraph id="analyze-files-ready" with hx-swap-oob="true" and x-init="$store.pipeline.discovered = {{ stats.discovered }}". Mirror EXACTLY for the two new busy store-writes.
- New OOB node ids (contract shared with Plan 04's in-place anchors): agent-busy-seed, controller-busy-seed. They render hidden (class hidden, no visible text) and only carry the x-init store-write $store.pipeline.agentBusy = {{ agent_busy }} / $store.pipeline.controllerBusy = {{ controller_busy }}. Counts are ints from count()/DB — safe to interpolate into the x-init numeric assignment (no quotes, no XSS; RESEARCH Security Domain).
- Test harness: wire_fakes(client), install_fake_queues(client), FakeTaskRouter.set_counts, FakeQueue.set_counts, seed_active_agent(session) (tests/_queue_fakes.py). _make_file(state=...) helper already in test_pipeline.py.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire get_queue_activity + guarded percent into both contexts</name>
  <files>src/phaze/routers/pipeline.py</files>
  <read_first>
    - src/phaze/routers/pipeline.py (dashboard() ~169-204 and pipeline_stats_partial() ~207-222; the import line ~26)
    - src/phaze/services/pipeline.py (get_queue_activity signature from Plan 01)
    - .planning/.../34-RESEARCH.md "Failure Isolation" + "HTMX OOB + Alpine pattern"
  </read_first>
  <action>
    First, in src/phaze/services/pipeline.py (alongside get_queue_activity), define a module-level PURE helper `def queue_progress_percent(analyzed: int, agent_busy: int) -> int:` that returns `round(analyzed / denom * 100) if (denom := analyzed + agent_busy) else 0` — the single source of truth for the DB-derived progress formula, extracted here so it is unit-testable from raw inputs (closes plan-check W1: the inline expression could harbor a reversed denominator that no test would catch). Then in src/phaze/routers/pipeline.py, extend the get_files_by_state, get_pipeline_stats import to ALSO import get_queue_activity AND queue_progress_percent. In dashboard(), after computing stats, call activity = await get_queue_activity(request.app.state, session); set queue_progress = queue_progress_percent(stats["analyzed"], activity["agent_busy"]); merge the six activity keys plus queue_progress_percent=queue_progress into the returned context dict (do not drop agents/recent_scans). In pipeline_stats_partial(), do the same and add them alongside the existing oob_counts: True. Use the existing request.app.state handle. Add no new try/except here — get_queue_activity already isolates failures and returns zeros. Keep the HTMLResponse return type intact.
  </action>
  <verify>
    <automated>uv run mypy src/phaze/routers/pipeline.py src/phaze/services/pipeline.py && uv run ruff check src/phaze/routers/pipeline.py src/phaze/services/pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - src/phaze/services/pipeline.py contains `def queue_progress_percent(analyzed: int, agent_busy: int) -> int`.
    - grep -c "get_queue_activity" src/phaze/routers/pipeline.py is at least 3 (import + both call sites).
    - grep -c "queue_progress_percent" src/phaze/routers/pipeline.py is at least 3 (import + both contexts).
    - uv run mypy and uv run ruff check on both files exit 0.
  </acceptance_criteria>
  <done>A pure queue_progress_percent helper lives in services/pipeline.py; both endpoints compute queue activity + the guarded percent and pass agent_busy/controller_busy/the four sub-counts/queue_progress_percent into their template contexts.</done>
</task>

<task type="auto">
  <name>Task 2: Add agentBusy/controllerBusy OOB store-write nodes to stats_bar.html</name>
  <files>src/phaze/templates/pipeline/partials/stats_bar.html</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/stats_bar.html (the existing {% if oob_counts %} block, lines 45-48 — the exact pattern to mirror)
    - .planning/.../34-CONTEXT.md "Store initialization" + "Four buttons" sections
  </read_first>
  <action>
    Inside the existing {% if oob_counts %} ... {% endif %} block in stats_bar.html, add two nodes mirroring the analyze-files-ready paragraph: a paragraph id="agent-busy-seed" with hx-swap-oob="true", class "hidden", and x-init="$store.pipeline.agentBusy = {{ agent_busy }}"; and a paragraph id="controller-busy-seed" with hx-swap-oob="true", class "hidden", and x-init="$store.pipeline.controllerBusy = {{ controller_busy }}". They carry no visible text — their sole job is to re-push live busy counts into $store.pipeline on each 5s poll so the Plan 04 button :disabled bindings react. Keep them inside the oob_counts gate so they are NOT emitted on the initial full-page include (htmx only honors hx-swap-oob during a swap; the initial seed lives in stage_cards.html via Plan 04). Add a brief Jinja comment documenting the same-id contract with stage_cards.html in-place anchors. No scripts or fenced code; plain template nodes only.
  </action>
  <verify>
    <automated>uv run python -c "from fastapi.templating import Jinja2Templates; Jinja2Templates(directory='src/phaze/templates').env.get_template('pipeline/partials/stats_bar.html')" && grep -c "agent-busy-seed" src/phaze/templates/pipeline/partials/stats_bar.html</automated>
  </verify>
  <acceptance_criteria>
    - Jinja parses stats_bar.html without error (the python -c above exits 0).
    - grep finds both agent-busy-seed and controller-busy-seed, each with hx-swap-oob and an x-init store-write.
    - The new nodes are inside the {% if oob_counts %} gate (not emitted at initial load).
  </acceptance_criteria>
  <done>The 5s poll response re-seeds $store.pipeline.agentBusy/controllerBusy via two hidden OOB nodes; the initial include omits them.</done>
</task>

<task type="auto">
  <name>Task 3: Router tests — degrade-200 + wired busy counts surfaced</name>
  <files>tests/test_routers/test_pipeline.py</files>
  <read_first>
    - tests/test_routers/test_pipeline.py (test_dashboard_page, test_pipeline_stats_partial, _make_file, wire_fakes/install_fake_queues usage)
    - tests/_queue_fakes.py (FakeTaskRouter.set_counts / FakeQueue.set_counts / seed_active_agent — Plan 00)
  </read_first>
  <action>
    Add tests to tests/test_routers/test_pipeline.py covering VALIDATION 34-02-01. (1) test_pipeline_stats_degrades_without_queues: GET /pipeline/stats with NO fakes wired → 200 (proves the AttributeError degrade keeps the poll alive); GET /pipeline/ likewise → 200. (2) test_pipeline_stats_surfaces_agent_busy: seed_active_agent(session, "nox"); install_fake_queues(client) then set the task_router/controller fake depths via FakeTaskRouter.set_counts("nox", queued=4, active=1) and the controller queue's set_counts(queued=2, active=0); GET /pipeline/stats → 200 and assert the response body contains the agentBusy store-write substring "$store.pipeline.agentBusy = 5" and "$store.pipeline.controllerBusy = 2" (sum queued+active). (3) test_dashboard_seeds_busy_on_first_load: same wiring, GET /pipeline/ → 200 (initial render does not 500 with queues wired). Name them so -k busy selects (2) and (3). (4) test_queue_progress_percent_formula: import queue_progress_percent from phaze.services.pipeline and assert queue_progress_percent(30, 10) == 75 (proves the denominator is analyzed+agent_busy and the numerator is analyzed — a reversed ratio would yield 25, not 75), queue_progress_percent(0, 0) == 0 (divide-by-zero guard), and queue_progress_percent(11428, 0) == 100 (fully analyzed). Keep using the existing async client/session fixtures; tests may print/ignore PLC/S105 per per-file rules.
  </action>
  <verify>
    <automated>uv run pytest tests/test_routers/test_pipeline.py -k "busy or degrade" -q</automated>
  </verify>
  <acceptance_criteria>
    - uv run pytest tests/test_routers/test_pipeline.py -k busy -q selects at least one test and passes (asserts the rendered agentBusy/controllerBusy values reflect the seeded fake depths).
    - test_queue_progress_percent_formula passes: queue_progress_percent(30,10)==75, (0,0)==0, (11428,0)==100 — the formula is verified from raw inputs, not an echoed value.
    - The un-wired /pipeline/stats and /pipeline/ both return 200 (degrade path), proving no 500 regression.
    - Full module green: uv run pytest tests/test_routers/test_pipeline.py -q.
  </acceptance_criteria>
  <done>Router tests prove the counts reach both contexts when queues are wired and that absent queues degrade to a live 200 poll.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /pipeline/stats (5s poll) | Unauthenticated single-user admin endpoint on a private network; emits server-derived integer counts only. |
| router → Redis (via get_queue_activity) | Read-only depth reads isolated by the Plan 01 service. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Denial of Service | pipeline_stats_partial on the 5s poll | mitigate | Counts come from the degrade-isolated service; the endpoint adds no new failure path; a Redis outage yields zeros, never a 500/stall. |
| T-34-03 | Injection (XSS) | x-init store-write interpolation in stats_bar.html | accept | Interpolated values are ints from count()/DB (not user-controlled); Jinja autoescape is on; numeric assignment carries no quote-break surface. |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit: zero install surface). |
</threat_model>

<verification>
uv run pytest tests/test_routers/test_pipeline.py -q green; uv run mypy src/phaze/routers/pipeline.py clean; uv run ruff check src/phaze/routers/pipeline.py clean; stats_bar.html parses via Jinja. The previously-passing test_dashboard_page / test_pipeline_stats_partial / test_dashboard_includes_settings_batch_size remain green (degrade proven).
</verification>

<success_criteria>
Both endpoints carry agent_busy/controller_busy/the four sub-counts/queue_progress_percent; stats_bar.html re-seeds the store via two gated OOB nodes; router tests prove wired-counts-surfaced and absent-queues-degrade-to-200; mypy + ruff clean.
</success_criteria>

<output>
Create `.planning/milestones/v4.0-phases/34-pipeline-queue-depth-status-double-enqueue-guard/34-02-SUMMARY.md` when done.
</output>
