---
phase: 34-pipeline-queue-depth-status-double-enqueue-guard
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/_queue_fakes.py
  - tests/_queue_fakes_test.py
autonomous: true
requirements: [Q34-1]
must_haves:
  truths:
    - "A test can seed a FakeQueue with per-kind depths and read them back via await count(kind)"
    - "A test can make a fake queue's count raise, to exercise the degrade path"
  artifacts:
    - path: "tests/_queue_fakes.py"
      provides: "FakeQueue.count + per-agent depth seeding for queue-activity tests"
      contains: "async def count"
    - path: "tests/_queue_fakes_test.py"
      provides: "Direct coverage of the new harness method"
      contains: "count"
  key_links:
    - from: "tests/_queue_fakes.py FakeTaskRouter.queue_for"
      to: "FakeQueue.count"
      via: "queue_for returns FakeQueue instances that now expose count"
      pattern: "queue_for"
---

<objective>
Wave-0 prerequisite. The shared test doubles in `tests/_queue_fakes.py` (`FakeQueue`/`FakeTaskRouter`) have NO `count` method, so no `get_queue_activity` test (Plan 01) or router test (Plans 02-04) can exercise live queue-depth reads. Add an `async def count(self, kind)` to `FakeQueue` with seedable per-kind depths, a helper to seed per-agent depths through `FakeTaskRouter`, and an opt-in "raise on count" switch so the degrade path is testable.

Purpose: Unblock every downstream test that asserts summed queue depth or the Redis-error / missing-attr degrade.
Output: Extended `FakeQueue`/`FakeTaskRouter` + a focused harness test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v4.0-phases/34-pipeline-queue-depth-status-double-enqueue-guard/34-CONTEXT.md
@.planning/milestones/v4.0-phases/34-pipeline-queue-depth-status-double-enqueue-guard/34-RESEARCH.md
@.planning/milestones/v4.0-phases/34-pipeline-queue-depth-status-double-enqueue-guard/34-VALIDATION.md

<interfaces>
<!-- Current FakeQueue / FakeTaskRouter shapes the executor extends. From tests/_queue_fakes.py -->
- `FakeQueue.__init__(self, name: str, capture: Capture | None = None)` — sets `self.name`, `self.captured`, `self.captured_policy`, `self.job` (AsyncMock), `self._counter`.
- `FakeQueue.enqueue(self, task_name, **kwargs) -> MagicMock` — existing; do not change its behavior.
- `FakeTaskRouter.__init__(self, capture=None)` — holds `self.queues: dict[str, FakeQueue]`, `self.queue_for_calls: list[str]`.
- `FakeTaskRouter.queue_for(self, agent_id: str) -> FakeQueue` — lazily creates+caches `FakeQueue(f"phaze-agent-{agent_id}", self.captures)`.
- `install_fake_queues(client) -> (FakeQueue, FakeTaskRouter)` and `wire_fakes(client) -> Capture` attach `app.state.controller_queue` + `app.state.task_router`.
- `seed_active_agent(session, agent_id="nox") -> Agent` — inserts one non-revoked agent.
- SAQ contract being mimicked: `Queue.count(kind)` is `async`, `kind ∈ {"queued","active","incomplete"}`, returns `int`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add seedable async count + raise switch to FakeQueue/FakeTaskRouter</name>
  <files>tests/_queue_fakes.py</files>
  <read_first>
    - tests/_queue_fakes.py (the file being modified — FakeQueue.__init__/enqueue, FakeTaskRouter.queue_for, install_fake_queues, wire_fakes)
    - .planning/.../34-RESEARCH.md "Testing Approach (Research Q4)" + "Wave 0 Gaps"
  </read_first>
  <action>
    In `FakeQueue.__init__`, add an instance attribute `self._counts: dict[str, int]` defaulting to `{"queued": 0, "active": 0, "incomplete": 0}` and a boolean `self._count_raises = False`. Add `async def count(self, kind: str) -> int` that, when `self._count_raises` is True, raises `RuntimeError("fake redis down")` (to exercise the Plan 01 degrade path); otherwise returns `self._counts.get(kind, 0)`. Add a synchronous helper `set_counts(self, *, queued: int = 0, active: int = 0, incomplete: int = 0) -> None` that updates `self._counts`. Add `def fail_count(self) -> None` that sets `self._count_raises = True`. Do NOT alter `enqueue`, `captured`, or the capture wiring.
    On `FakeTaskRouter`, add `def set_counts(self, agent_id: str, *, queued: int = 0, active: int = 0) -> None` that calls `self.queue_for(agent_id)` (forcing lazy creation/caching) and then seeds that queue's counts — so a test can pre-seed an agent's depth before the service enumerates it. Keep `queue_for` returning the same cached `FakeQueue`.
    Tests ignore `disallow_untyped_defs` (tests are mypy-excluded), but keep type hints for clarity. No `print`.
  </action>
  <verify>
    <automated>uv run ruff check tests/_queue_fakes.py && uv run python -c "import ast,sys; ast.parse(open('tests/_queue_fakes.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "async def count" tests/_queue_fakes.py` returns a match on `FakeQueue`.
    - `FakeQueue` exposes `set_counts` and `fail_count`; `FakeTaskRouter` exposes `set_counts(agent_id, ...)`.
    - `uv run ruff check tests/_queue_fakes.py` exits 0.
  </acceptance_criteria>
  <done>FakeQueue has an async count returning seeded per-kind ints (or raising when fail_count() was called), and FakeTaskRouter can seed per-agent depths.</done>
</task>

<task type="auto">
  <name>Task 2: Cover the new harness method</name>
  <files>tests/_queue_fakes_test.py</files>
  <read_first>
    - tests/_queue_fakes.py (the methods added in Task 1)
    - tests/test_services/test_pipeline.py (pytest.mark.asyncio + async test style to mirror)
  </read_first>
  <action>
    Create `tests/_queue_fakes_test.py` (named with the `_test.py` suffix so `uv run pytest tests/_queue_fakes_test.py` discovers it under the default pattern). Add `@pytest.mark.asyncio` async tests: (a) a `FakeQueue` after `set_counts(queued=3, active=2)` returns 3 for `await q.count("queued")` and 2 for `"active"`; (b) an un-seeded kind returns 0; (c) after `fail_count()`, `await q.count("queued")` raises `RuntimeError`; (d) `FakeTaskRouter.set_counts("nox", queued=5, active=1)` then `router.queue_for("nox")` is the same cached instance whose `count("queued")` is 5. Allow `T201`/`PLC`/`S105` per tests per-file ignores; no prints needed.
  </action>
  <verify>
    <automated>uv run pytest tests/_queue_fakes_test.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/_queue_fakes_test.py -q` passes (4 assertions/tests green).
    - Test file asserts both the seeded-value path and the raising path.
  </acceptance_criteria>
  <done>The harness extension is proven: seeded depths read back, missing kind is 0, fail_count makes count raise, and per-agent seeding routes through the cached fake.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness only | No production trust boundary — this plan edits test doubles exclusively. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: zero install surface). |
</threat_model>

<verification>
`uv run pytest tests/_queue_fakes_test.py -q` green; `uv run ruff check tests/_queue_fakes.py` clean. Existing tests that import `_queue_fakes` (`test_pipeline.py`, `test_enqueue_router.py`, etc.) still collect and pass: `uv run pytest tests/test_routers/test_pipeline.py -q`.
</verification>

<success_criteria>
FakeQueue exposes an awaitable, seedable `count` plus a raise switch; FakeTaskRouter can seed per-agent depths; a dedicated harness test proves all four behaviors; no regression in modules importing `_queue_fakes`.
</success_criteria>

<output>
Create `.planning/milestones/v4.0-phases/34-pipeline-queue-depth-status-double-enqueue-guard/34-00-SUMMARY.md` when done.
</output>
