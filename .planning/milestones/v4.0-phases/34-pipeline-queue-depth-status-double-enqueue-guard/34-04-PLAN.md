---
phase: 34-pipeline-queue-depth-status-double-enqueue-guard
plan: 04
type: execute
wave: 3
depends_on: ["34-02"]
files_modified:
  - src/phaze/templates/base.html
  - src/phaze/templates/pipeline/partials/stage_cards.html
  - tests/test_template_helpers/test_stage_cards_partial.py
autonomous: true
requirements: [Q34-5]
must_haves:
  truths:
    - "All four pipeline-action buttons render: Run Analysis, Fingerprint, Extract Metadata, Generate Proposals"
    - "Run Analysis / Fingerprint / Extract Metadata are disabled while $store.pipeline.agentBusy > 0 (cannot double-enqueue the corpus)"
    - "Generate Proposals is disabled while $store.pipeline.controllerBusy > 0"
    - "$store.pipeline defines agentBusy and controllerBusy defaulting to 0 so bindings never read undefined before the first poll"
  artifacts:
    - path: "src/phaze/templates/base.html"
      provides: "Alpine store defaults agentBusy:0, controllerBusy:0"
      contains: "agentBusy"
    - path: "src/phaze/templates/pipeline/partials/stage_cards.html"
      provides: "Four trigger buttons with agentBusy/controllerBusy disable bindings + initial busy store seed"
      contains: "agentBusy"
  key_links:
    - from: "stage_cards.html button :disabled bindings"
      to: "$store.pipeline.agentBusy / controllerBusy"
      via: "Alpine :disabled reactive expression"
      pattern: "agentBusy"
    - from: "stage_cards.html in-place seed anchors"
      to: "$store.pipeline.agentBusy / controllerBusy"
      via: "x-init store-write at initial page load (ids agent-busy-seed / controller-busy-seed)"
      pattern: "agent-busy-seed"
---

<objective>
Close the double-enqueue hole and surface all four pipeline actions. Add the two missing trigger buttons (Fingerprint → `/pipeline/fingerprint`, Extract Metadata → `/pipeline/extract-metadata`) mirroring the existing Run Analysis button exactly, extend the `:disabled` bindings so the three agent-task buttons are gated by `$store.pipeline.agentBusy > 0` and Generate Proposals by `$store.pipeline.controllerBusy > 0`, seed those store keys at initial load via in-place anchors, and add the `agentBusy: 0, controllerBusy: 0` defaults to the Alpine store in base.html.

Purpose: Make Run Analysis (and siblings) un-clickable while the corpus is already queued — the second headline must-have.
Output: 4 gated buttons + store defaults + initial seed + stage-cards render tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v4.0-phases/34-pipeline-queue-depth-status-double-enqueue-guard/34-CONTEXT.md
@.planning/milestones/v4.0-phases/34-pipeline-queue-depth-status-double-enqueue-guard/34-RESEARCH.md
@.planning/milestones/v4.0-phases/34-pipeline-queue-depth-status-double-enqueue-guard/34-VALIDATION.md

<interfaces>
<!-- Existing button pattern in stage_cards.html to mirror EXACTLY (Run Analysis card): -->
- Card wrapper: a bordered div with x-data="{ loading: false }".
- Button: hx-post to the endpoint, hx-target/hx-swap="innerHTML" to an own response div (e.g. #analyze-response), hx-indicator to a spinner span, @click="loading = true", :disabled binding, then a trigger_response.html fragment renders the result into the response div.
- Run Analysis today: :disabled="loading || $store.pipeline.discovered === 0" → MUST become :disabled="loading || $store.pipeline.discovered === 0 || $store.pipeline.agentBusy > 0".
- Generate Proposals today: :disabled="loading || $store.pipeline.analyzed === 0" → MUST become :disabled="loading || $store.pipeline.analyzed === 0 || $store.pipeline.controllerBusy > 0".
- In-place ready-count anchors today: id="analyze-files-ready" x-init="$store.pipeline.discovered = {{ stats.discovered }}"; id="proposals-files-ready" x-init="$store.pipeline.analyzed = {{ stats.analyzed }}".
- Endpoints already exist (verified): POST /pipeline/fingerprint (eligible = METADATA_EXTRACTED; ready-count source = stats.metadata_extracted) and POST /pipeline/extract-metadata (backfill over all music/video files; no single stage count). Both already return trigger_response.html and handle the no-active-agent case.
- trigger_response.html switches on action / count / no_active_agent (already supports the fingerprinting and metadata extraction action labels via the existing UI endpoints).
- New buttons disable rules (CONTEXT-locked):
  - Fingerprint: :disabled="loading || $store.pipeline.metadataExtracted === 0 || $store.pipeline.agentBusy > 0" (seed metadataExtracted from stats.metadata_extracted).
  - Extract Metadata: :disabled="loading || $store.pipeline.agentBusy > 0" (backfill — no per-state ready count; Claude's Discretion is a static label like "Backfill all music/video files").
- base.html store (line ~91): Alpine.store('pipeline', { discovered: 0, analyzed: 0 }); → add agentBusy: 0, controllerBusy: 0 (and metadataExtracted: 0 for the Fingerprint gate).
- Initial busy seed anchors (contract with Plan 02 stats_bar OOB ids): in-place (no hx-swap-oob) paragraphs id="agent-busy-seed" x-init="$store.pipeline.agentBusy = {{ agent_busy }}" class="hidden" and id="controller-busy-seed" x-init="$store.pipeline.controllerBusy = {{ controller_busy }}" class="hidden". stage_cards renders inside dashboard() context (Plan 02 supplies agent_busy/controller_busy + stats.metadata_extracted).
- Test harness: Jinja2Templates render of pipeline/partials/stage_cards.html with a context dict (mirror test_progress_partial.py). The disable bindings are static strings — assert on the rendered attribute text.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add agentBusy/controllerBusy/metadataExtracted store defaults</name>
  <files>src/phaze/templates/base.html</files>
  <read_first>
    - src/phaze/templates/base.html (line ~83-91 — the Alpine.store('pipeline', {...}) definition + comment)
  </read_first>
  <action>
    In base.html, extend the Alpine.store('pipeline', { discovered: 0, analyzed: 0 }); call to also define metadataExtracted: 0, agentBusy: 0, controllerBusy: 0. Update the adjacent comment to note these are the live queue-busy gates seeded from the server-rendered counts at load and refreshed by the 5s poll OOB nodes, so the four button :disabled bindings never read undefined before the first tick. Do not change the existing discovered/analyzed keys.
  </action>
  <verify>
    <automated>grep -c "agentBusy\|controllerBusy\|metadataExtracted" src/phaze/templates/base.html</automated>
  </verify>
  <acceptance_criteria>
    - The Alpine.store('pipeline', ...) object literal now contains discovered, analyzed, metadataExtracted, agentBusy, controllerBusy all defaulting to 0.
    - grep finds agentBusy, controllerBusy, and metadataExtracted in base.html.
  </acceptance_criteria>
  <done>$store.pipeline defines all five keys at 0 so every button :disabled binding has a defined default before the first poll.</done>
</task>

<task type="auto">
  <name>Task 2: Add Fingerprint + Extract Metadata buttons and extend disable bindings + initial seed</name>
  <files>src/phaze/templates/pipeline/partials/stage_cards.html</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/stage_cards.html (the two existing cards + their :disabled bindings + in-place ready-count anchors)
    - src/phaze/templates/pipeline/partials/trigger_response.html (the response fragment the new buttons target)
    - .planning/.../34-CONTEXT.md "Four buttons (two new) + coarse disable" decision
  </read_first>
  <action>
    In stage_cards.html: (1) Extend the existing Run Analysis :disabled to append " || $store.pipeline.agentBusy > 0", and the Generate Proposals :disabled to append " || $store.pipeline.controllerBusy > 0". (2) Add a Fingerprint card mirroring the Run Analysis card exactly: x-data="{ loading: false }", a button hx-post="/pipeline/fingerprint" with hx-target/hx-swap to an own #fingerprint-response div, hx-indicator to a #fingerprint-spinner span, @click="loading = true", :disabled="loading || $store.pipeline.metadataExtracted === 0 || $store.pipeline.agentBusy > 0", a heading + description, and an in-place ready-count anchor id="fingerprint-files-ready" x-init="$store.pipeline.metadataExtracted = {{ stats.metadata_extracted }}" showing "{{ stats.metadata_extracted }} files ready". (3) Add an Extract Metadata card mirroring the same structure: button hx-post="/pipeline/extract-metadata" to #extract-metadata-response with #extract-metadata-spinner, @click="loading = true", :disabled="loading || $store.pipeline.agentBusy > 0", a heading, and a static description like "Backfill all music/video files" (no per-state ready count — Claude's Discretion per CONTEXT). (4) Add two hidden in-place seed anchors mirroring the existing analyze-files-ready x-init trick: id="agent-busy-seed" x-init="$store.pipeline.agentBusy = {{ agent_busy }}" class="hidden" and id="controller-busy-seed" x-init="$store.pipeline.controllerBusy = {{ controller_busy }}" class="hidden" — these are the initial-load store seed; the Plan 02 stats_bar OOB nodes reuse the SAME ids to re-seed on each poll. Keep all four buttons inside the #pipeline-stages subtree so the 5s poll never re-renders them (loading state preserved). Match the existing dark-mode-aware Tailwind classes. Order: Run Analysis, Extract Metadata, Fingerprint, Generate Proposals (pipeline order) OR keep Run Analysis + Generate Proposals positions and insert the two new cards between — Claude's Discretion, but all four must render.
  </action>
  <verify>
    <automated>uv run python -c "from fastapi.templating import Jinja2Templates; Jinja2Templates(directory='src/phaze/templates').env.get_template('pipeline/partials/stage_cards.html')" && grep -c "agentBusy\|controllerBusy" src/phaze/templates/pipeline/partials/stage_cards.html && grep -c "/pipeline/fingerprint\|/pipeline/extract-metadata" src/phaze/templates/pipeline/partials/stage_cards.html</automated>
  </verify>
  <acceptance_criteria>
    - stage_cards.html parses via Jinja (python -c exits 0).
    - Four hx-post targets present: /pipeline/analyze, /pipeline/fingerprint, /pipeline/extract-metadata, /pipeline/proposals.
    - Run Analysis/Fingerprint/Extract-Metadata :disabled each contain "$store.pipeline.agentBusy > 0"; Generate Proposals contains "$store.pipeline.controllerBusy > 0".
    - Hidden in-place anchors id="agent-busy-seed" and id="controller-busy-seed" carry the x-init store-writes.
  </acceptance_criteria>
  <done>All four pipeline-action buttons render; the three agent buttons gate on agentBusy and Generate Proposals on controllerBusy; the busy store keys are seeded in-place at initial load.</done>
</task>

<task type="auto">
  <name>Task 3: Stage-cards render tests for the four buttons and gating</name>
  <files>tests/test_template_helpers/test_stage_cards_partial.py</files>
  <read_first>
    - tests/test_template_helpers/test_progress_partial.py (_fake_request, TEMPLATES_DIR, render-then-decode pattern)
    - src/phaze/templates/pipeline/partials/stage_cards.html (the template under test, from Task 2)
  </read_first>
  <action>
    Create tests/test_template_helpers/test_stage_cards_partial.py mirroring test_progress_partial.py. Add a _render(**context) that renders pipeline/partials/stage_cards.html with a context supplying stats (a dict with discovered, analyzed, metadata_extracted), agent_busy, controller_busy, settings_batch_size. Tests covering VALIDATION 34-04-01: (a) test_four_buttons_render: assert all four hx-post endpoints (/pipeline/analyze, /pipeline/fingerprint, /pipeline/extract-metadata, /pipeline/proposals) appear in the markup. (b) test_agent_buttons_gated_on_agent_busy: assert the rendered Run Analysis, Fingerprint, and Extract-Metadata :disabled attributes each contain "$store.pipeline.agentBusy > 0". (c) test_proposals_gated_on_controller_busy: assert the Generate Proposals :disabled contains "$store.pipeline.controllerBusy > 0". (d) test_initial_busy_seed_present: with context agent_busy=5, controller_busy=2, assert the in-place anchors render x-init store-writes "$store.pipeline.agentBusy = 5" and "$store.pipeline.controllerBusy = 2". Name tests so -k stage_cards selects them (per VALIDATION 34-04-01). Allow T201/PLC/S105 per per-file rules.
  </action>
  <verify>
    <automated>uv run pytest tests/test_template_helpers/test_stage_cards_partial.py -k "stage_cards or buttons or busy or gated" -q</automated>
  </verify>
  <acceptance_criteria>
    - uv run pytest tests/test_template_helpers/test_stage_cards_partial.py -q passes.
    - A test asserts all four hx-post endpoints render; a test asserts the three agent buttons gate on agentBusy and Proposals on controllerBusy; a test asserts the initial busy seed values render.
    - uv run ruff check tests/test_template_helpers/test_stage_cards_partial.py exits 0.
  </acceptance_criteria>
  <done>Render tests prove four buttons exist, the coarse agentBusy/controllerBusy gating is wired, and the initial busy seed anchors carry the correct store-writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser button → existing /pipeline/* trigger endpoints | Buttons post to already-existing enqueue endpoints; this plan adds no new enqueue path, only client-side disable gating. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04 | Elevation/Abuse (double-enqueue) | Run Analysis / agent-task buttons | mitigate | :disabled gates on $store.pipeline.agentBusy > 0 so the corpus cannot be re-enqueued while a run is in flight (client-side guard for a single-user admin UI; the server endpoints are unchanged). |
| T-34-03 | Injection (XSS) | x-init store-write interpolation in stage_cards.html | accept | Interpolated values are ints from DB/count(); Jinja autoescape on; numeric assignment carries no quote-break surface. |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit: zero install surface). |
</threat_model>

<verification>
uv run pytest tests/test_template_helpers/test_stage_cards_partial.py -q green; stage_cards.html + base.html parse/serve via Jinja; uv run pytest tests/test_routers/test_pipeline.py -q remains green (dashboard renders the extended stage cards). Phase gate: uv run pytest --cov --cov-report=term-missing (≥85%), uv run ruff check . , uv run ruff format --check . , uv run mypy . , pre-commit run --all-files all pass.
</verification>

<success_criteria>
Four pipeline-action buttons render; Run Analysis/Fingerprint/Extract-Metadata gate on agentBusy and Generate Proposals on controllerBusy; $store.pipeline defines all gate keys at 0 and is seeded in-place at load; render tests prove the buttons and gating; full suite green with ≥85% coverage and clean ruff/mypy/pre-commit.
</success_criteria>

<output>
Create `.planning/milestones/v4.0-phases/34-pipeline-queue-depth-status-double-enqueue-guard/34-04-SUMMARY.md` when done.
</output>
