---
phase: 34-pipeline-queue-depth-status-double-enqueue-guard
plan: 03
type: execute
wave: 3
depends_on: ["34-02"]
files_modified:
  - src/phaze/templates/pipeline/partials/processing_card.html
  - src/phaze/templates/pipeline/dashboard.html
  - src/phaze/templates/pipeline/partials/stats_bar.html
  - tests/test_template_helpers/test_processing_card_partial.py
autonomous: true
requirements: [Q34-4]
must_haves:
  truths:
    - "When agent_busy > 0 the card shows a progress bar at queue_progress_percent width plus the text '{agent_queued} queued · {agent_active} active'"
    - "When controller_busy > 0 a second compact line shows the controller/proposals queue"
    - "When both agent_busy and controller_busy are 0 the card renders empty (no bar markup)"
    - "The card persists across a page refresh because it renders from server context on first load AND is OOB-swapped on every 5s poll"
  artifacts:
    - path: "src/phaze/templates/pipeline/partials/processing_card.html"
      provides: "Persistent Processing card: progress bar + queued/active text + controller line, OOB-gated"
      contains: "queue_progress_percent"
    - path: "src/phaze/templates/pipeline/dashboard.html"
      provides: "Initial (non-OOB) include of the card above #pipeline-stats"
      contains: "processing_card.html"
  key_links:
    - from: "src/phaze/templates/pipeline/dashboard.html"
      to: "partials/processing_card.html"
      via: "{% include %} above #pipeline-stats (no oob flag)"
      pattern: "processing_card"
    - from: "src/phaze/templates/pipeline/partials/stats_bar.html"
      to: "partials/processing_card.html"
      via: "{% include %} with the oob gate so the 5s poll OOB-swaps the card"
      pattern: "processing_card"
---

<objective>
Build the persistent "Processing" card that fixes the core UX bug: after clicking Run Analysis and refreshing, the in-flight run must stay visible. The card renders from server context on initial `/pipeline/` load AND is OOB-swapped on each 5s `/pipeline/stats` tick. It shows a DB-derived progress bar (`queue_progress_percent` from Plan 02) plus `"{agent_queued} queued · {agent_active} active"` when `agent_busy > 0`, a second compact controller line when `controller_busy > 0`, and renders empty when both are idle (with the divide-by-zero already guarded upstream).

Purpose: Make an enqueued run observable across refreshes — the headline must-have.
Output: processing_card.html + dashboard include + stats_bar OOB include + direct partial-render tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v4.0-phases/34-pipeline-queue-depth-status-double-enqueue-guard/34-CONTEXT.md
@.planning/milestones/v4.0-phases/34-pipeline-queue-depth-status-double-enqueue-guard/34-RESEARCH.md
@.planning/milestones/v4.0-phases/34-pipeline-queue-depth-status-double-enqueue-guard/34-VALIDATION.md

<interfaces>
<!-- Context keys provided by Plan 02's router wiring (present in both dashboard() and stats partial contexts): -->
- agent_busy, agent_queued, agent_active (ints) — agent-task queue depth.
- controller_busy, controller_queued, controller_active (ints) — controller/proposals queue depth.
- queue_progress_percent (int 0-100, divide-by-zero already guarded to 0 by the router).
- stats (dict, key analyzed) — available in the card context.
- oob_counts (bool) — True only on the /pipeline/stats poll response; absent/false on the initial dashboard include.
- dashboard.html currently includes partials above #pipeline-stats (lines 16-19): the stats bar div polls /pipeline/stats every 5s. The card include goes ABOVE that div.
- stats_bar.html is the template returned by /pipeline/stats; it already gates its OOB block with {% if oob_counts %}. The card's OOB swap must ride this same response.
- Existing styling cues to match (dark-mode-aware): stats_bar.html uses Tailwind rounded-lg p-4, text-* color pairs with dark: variants. Progress-bar width should be an inline style width percentage (mirror any existing bar; e.g. the scan progress card uses a width style driven by a server percent).
- Test harness: Jinja2Templates(directory=TEMPLATES_DIR).TemplateResponse(request=_fake_request(), name="pipeline/partials/processing_card.html", context={...}); _fake_request() + TEMPLATES_DIR pattern from tests/test_template_helpers/test_progress_partial.py.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create processing_card.html and wire both includes</name>
  <files>src/phaze/templates/pipeline/partials/processing_card.html, src/phaze/templates/pipeline/dashboard.html, src/phaze/templates/pipeline/partials/stats_bar.html</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/stats_bar.html (OOB gating + dark-mode Tailwind classes)
    - src/phaze/templates/pipeline/dashboard.html (include order; #pipeline-stats div)
    - .planning/.../34-CONTEXT.md "Persistent Processing card" decision
  </read_first>
  <action>
    Create src/phaze/templates/pipeline/partials/processing_card.html with a stable outer element id="processing-card". On the /pipeline/stats poll (when oob_counts is truthy) the outer element must carry hx-swap-oob="true" so htmx swaps it in place; on the initial dashboard include (oob_counts falsy) it must NOT carry hx-swap-oob — gate the attribute with {% if oob_counts %}hx-swap-oob="true"{% endif %}. Inside: when agent_busy > 0, render a labeled progress bar whose filled width is an inline style of queue_progress_percent percent (dark-mode-aware Tailwind, matching stats_bar's rounded-lg / color-pair style), plus a text line exactly "{{ agent_queued }} queued · {{ agent_active }} active" (use the middle-dot separator). When controller_busy > 0, render a second compact line for the controller/proposals queue, e.g. "Proposals: {{ controller_queued }} queued · {{ controller_active }} active". When both agent_busy == 0 and controller_busy == 0, the card body must be empty (no bar markup, no text) — wrap the visual block in {% if agent_busy > 0 or controller_busy > 0 %}. The card is display-only and must NOT write $store.pipeline (the store-writes live in stats_bar OOB nodes + stage_cards in-place anchors). Then edit dashboard.html to {% include "pipeline/partials/processing_card.html" %} immediately ABOVE the #pipeline-stats div (initial render, no oob). Then edit stats_bar.html to {% include "pipeline/partials/processing_card.html" %} inside its {% if oob_counts %} gate so the 5s poll response carries the OOB card swap. Keep numbers as ints; thousands-separator formatting (if added) goes only in the visible text node, never in an x-init.
  </action>
  <verify>
    <automated>uv run python -c "from fastapi.templating import Jinja2Templates; e=Jinja2Templates(directory='src/phaze/templates').env; [e.get_template(n) for n in ('pipeline/partials/processing_card.html','pipeline/dashboard.html','pipeline/partials/stats_bar.html')]" && grep -c "processing_card.html" src/phaze/templates/pipeline/dashboard.html src/phaze/templates/pipeline/partials/stats_bar.html</automated>
  </verify>
  <acceptance_criteria>
    - All three templates parse via Jinja (python -c exits 0).
    - dashboard.html includes processing_card.html above #pipeline-stats; stats_bar.html includes it inside the oob_counts gate.
    - processing_card.html outer id is processing-card; the hx-swap-oob attribute is conditional on oob_counts.
    - The visual block is wrapped in a {% if agent_busy > 0 or controller_busy > 0 %} guard.
  </acceptance_criteria>
  <done>The Processing card exists, renders the bar + queued/active text + controller line when busy, is empty when idle, includes once initially and once as an OOB poll swap.</done>
</task>

<task type="auto">
  <name>Task 2: Direct partial-render tests for the card states</name>
  <files>tests/test_template_helpers/test_processing_card_partial.py</files>
  <read_first>
    - tests/test_template_helpers/test_progress_partial.py (_fake_request(), TEMPLATES_DIR, _templates, render-then-decode pattern)
    - src/phaze/templates/pipeline/partials/processing_card.html (the template under test, from Task 1)
  </read_first>
  <action>
    Create tests/test_template_helpers/test_processing_card_partial.py mirroring test_progress_partial.py: build TEMPLATES_DIR + a module _templates = Jinja2Templates(directory=...) and a _fake_request(), and a _render(**context) helper that renders pipeline/partials/processing_card.html and returns response.body.decode(). Tests covering VALIDATION 34-03-01 + 34-03-02: (a) test_card_busy_shows_bar_and_counts: context agent_busy=10, agent_queued=7, agent_active=3, controller_busy=0, queue_progress_percent=75, stats={"analyzed":30} → asserts "7 queued" and "3 active" and the middle-dot text are present AND a width style of 75% appears in the markup. (b) test_card_idle_renders_empty: agent_busy=0, controller_busy=0 (all zero), queue_progress_percent=0 → assert NO progress-bar markup (e.g. the bar's distinctive class/width style is absent) and the queued/active text is absent. (c) test_card_controller_line: agent_busy=0, controller_busy=4, controller_queued=4, controller_active=0 → assert the second controller/proposals line is present. (d) test_card_percent_math_seventyfive: confirm the 75% width string given analyzed=30 + agent_busy=10 (percent is passed in, so assert the rendered width matches the passed queue_progress_percent). (e) test_card_zero_denominator_guard: agent_busy=0, stats={"analyzed":0}, queue_progress_percent=0 → renders empty, no error (the upstream guard means percent is 0). Allow T201/PLC/S105 per per-file rules.
  </action>
  <verify>
    <automated>uv run pytest tests/test_template_helpers/test_processing_card_partial.py -k "processing_card or progress or card" -q</automated>
  </verify>
  <acceptance_criteria>
    - uv run pytest tests/test_template_helpers/test_processing_card_partial.py -q passes all card-state tests.
    - The busy test asserts both the queued/active text and the 75% bar width; the idle test asserts the bar markup is absent.
    - A controller-line test and a divide-by-zero-guard (empty render, no exception) test are present.
  </acceptance_criteria>
  <done>Five partial tests prove busy bar+counts, idle-empty, controller line, the 75% width math, and the zero-denominator empty render.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-rendered partial → browser DOM | The card emits server-derived integer counts into HTML; HTMX OOB-swaps it by id on the poll. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03 | Injection (XSS) | processing_card.html count/percent interpolation | accept | All interpolated values are ints from count()/DB; Jinja autoescape on; the inline width style takes a numeric percent only — no user-controlled data. |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit: zero install surface). |
</threat_model>

<verification>
uv run pytest tests/test_template_helpers/test_processing_card_partial.py -q green; all three templates parse via Jinja; uv run pytest tests/test_routers/test_pipeline.py -q remains green (the dashboard/stats endpoints still render with the new include). Run uv run ruff format --check on touched templates is not applicable (Jinja), but ensure no Python lint regressions in the new test file: uv run ruff check tests/test_template_helpers/test_processing_card_partial.py.
</verification>

<success_criteria>
The persistent Processing card renders bar + queued/active text + controller line when busy and empty when idle, is included once for initial render and once as an OOB poll swap, and is covered by five direct partial-render tests; no router-test regression.
</success_criteria>

<output>
Create `.planning/milestones/v4.0-phases/34-pipeline-queue-depth-status-double-enqueue-guard/34-03-SUMMARY.md` when done.
</output>
