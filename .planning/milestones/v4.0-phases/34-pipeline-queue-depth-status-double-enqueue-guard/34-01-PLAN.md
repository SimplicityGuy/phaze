---
phase: 34-pipeline-queue-depth-status-double-enqueue-guard
plan: 01
type: execute
wave: 1
depends_on: ["34-00"]
files_modified:
  - src/phaze/services/pipeline.py
  - tests/test_services/test_pipeline.py
autonomous: true
requirements: [Q34-1, Q34-2]
must_haves:
  truths:
    - "get_queue_activity sums queued+active across ALL non-revoked agents' queues plus the controller queue"
    - "A Redis error or a missing app.state attribute degrades the result to all-zero without raising"
    - "Scheduled/cron jobs are never counted (only the queued and active kinds are read)"
  artifacts:
    - path: "src/phaze/services/pipeline.py"
      provides: "get_queue_activity(app_state, session) read-only queue-depth service with hard failure isolation"
      contains: "async def get_queue_activity"
    - path: "tests/test_services/test_pipeline.py"
      provides: "Unit coverage: multi-agent sum, scheduled-exclusion, redis-degrade, missing-attr-degrade, controller-independence"
      contains: "get_queue_activity"
  key_links:
    - from: "src/phaze/services/pipeline.py get_queue_activity"
      to: "app_state.task_router.queue_for(agent.id).count"
      via: "enumerate revoked_at IS NULL agents, await count('queued')+count('active')"
      pattern: "queue_for"
    - from: "src/phaze/services/pipeline.py get_queue_activity"
      to: "Agent.revoked_at.is_(None)"
      via: "the exact dashboard() non-revoked predicate (NOT select_active_agent)"
      pattern: "revoked_at.is_"
---

<objective>
Add the read-only `get_queue_activity(app_state, session)` service that reads live SAQ queue depth and is the authoritative "is anything in flight" signal the DB cannot provide. It sums `count("queued") + count("active")` across every non-revoked agent's per-agent queue plus the controller queue, and degrades to all-zero on BOTH a missing `app.state` attribute (test lifespan-skip) AND any Redis error — a hiccup must never 500 the 5s dashboard poll.

Purpose: The single backend primitive that Plans 02-04 surface in the UI.
Output: `get_queue_activity` + unit tests covering sum, scheduled-exclusion, and both degrade modes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v4.0-phases/34-pipeline-queue-depth-status-double-enqueue-guard/34-CONTEXT.md
@.planning/milestones/v4.0-phases/34-pipeline-queue-depth-status-double-enqueue-guard/34-RESEARCH.md
@.planning/milestones/v4.0-phases/34-pipeline-queue-depth-status-double-enqueue-guard/34-VALIDATION.md

<interfaces>
<!-- Contracts the executor builds against. -->
- Module under edit: `src/phaze/services/pipeline.py` (already type-checked by mypy strict — NOT excluded; the exclude `^(tests/|prototype/|services/)` only matches the top-level deploy `services/`, not `src/phaze/services/`). Full type hints required.
- Existing in this module: `get_pipeline_stats(session: AsyncSession) -> dict[str, int]`, `get_files_by_state(...)`, `from sqlalchemy import func, select`, `from phaze.models.file import FileRecord, FileState`. `AsyncSession` imported under `TYPE_CHECKING`.
- Non-revoked agent predicate to reuse verbatim (from routers/pipeline.py:186): `select(Agent).where(Agent.revoked_at.is_(None))` — iterate `.scalars().all()`. `from phaze.models.agent import Agent`.
- DO NOT use `enqueue_router.select_active_agent` — it returns ONE recently-seen agent and raises `NoActiveAgentError` when none; wrong for a passive sum.
- Queue handle shapes on `app_state`: `app_state.controller_queue.count(kind)` and `app_state.task_router.queue_for(agent_id).count(kind)`; both `count` are async, return int, `kind ∈ {"queued","active"}`. NEVER pass `"incomplete"` (it includes scheduled crons `reap_stalled_scans`/`refresh_tracklists`).
- `app_state` param typed `Any` (matches enqueue_router precedent; keeps the missing-attr degrade type-clean under strict mypy).
- Project logging: `import structlog` then `logger = structlog.get_logger(__name__)` (as in services/fingerprint.py, services/metadata.py). No `print` (T201).
- Test harness (Plan 00): `FakeTaskRouter().set_counts(agent_id, queued=, active=)`, `FakeQueue().set_counts(...)`/`.fail_count()`; `seed_active_agent(session, agent_id=...)` from tests/_queue_fakes.py; `SimpleNamespace` for a missing-attr app_state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement get_queue_activity with split failure isolation</name>
  <files>src/phaze/services/pipeline.py</files>
  <behavior>
    - Two non-revoked agents seeded queued/active 3/2 and 4/1 → agent_queued=7, agent_active=3, agent_busy=10.
    - Controller queue queued/active 5/0 → controller_queued=5, controller_active=0, controller_busy=5.
    - Only "queued" and "active" are read; a queue reporting a large "incomplete" does not change any returned value.
    - app_state without task_router/controller_queue (SimpleNamespace) → every value 0, no raise.
    - A queue whose count raises → that source degrades to 0; the OTHER source's counts remain intact (split try/except).
  </behavior>
  <read_first>
    - src/phaze/services/pipeline.py (the file being modified)
    - src/phaze/routers/pipeline.py lines ~169-204 (dashboard(): the revoked_at IS NULL select to mirror)
    - src/phaze/services/enqueue_router.py (app_state: Any precedent; do NOT reuse select_active_agent)
    - .planning/.../34-RESEARCH.md "Failure Isolation (Research Q3)" + "Per-Agent Queue Enumeration"
  </read_first>
  <action>
    Add `async def get_queue_activity(app_state: Any, session: AsyncSession) -> dict[str, int]` to `src/phaze/services/pipeline.py`. Import `Any` (from `typing`), `Agent` (from `phaze.models.agent`), and add `import structlog` + a module-level `logger = structlog.get_logger(__name__)`. Initialise `agent_queued = agent_active = controller_queued = controller_active = 0`. In a FIRST try block: execute `select(Agent).where(Agent.revoked_at.is_(None))`, iterate `.scalars().all()`, and for each agent get `q = app_state.task_router.queue_for(agent.id)` then `agent_queued += await q.count("queued")` and `agent_active += await q.count("active")`. In a SEPARATE second try block: `controller_queued = await app_state.controller_queue.count("queued")` and `controller_active = await app_state.controller_queue.count("active")`. Each try catches a broad `Exception` (covers `AttributeError` from the test lifespan-skip AND redis/connection errors) — annotate each `except` with `# noqa: BLE001` and a one-line comment justifying that a Redis/app.state hiccup must degrade to zero, never 500 the poll; inside each `except` set that source's two counters back to 0 and emit `logger.warning("queue_activity_degraded", source=..., exc_info=True)` (no `print`). Compute `agent_busy = agent_queued + agent_active` and `controller_busy = controller_queued + controller_active`. Return a dict with keys `agent_queued`, `agent_active`, `controller_queued`, `controller_active`, `agent_busy`, `controller_busy`. Never read or pass the `"incomplete"` kind.
  </action>
  <verify>
    <automated>uv run mypy src/phaze/services/pipeline.py && uv run ruff check src/phaze/services/pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "async def get_queue_activity" src/phaze/services/pipeline.py` matches.
    - `grep -v '^#' src/phaze/services/pipeline.py | grep -c 'incomplete'` returns 0 (the kind is never referenced).
    - Two independent `try`/`except` blocks (agent vs controller) are present.
    - `uv run mypy src/phaze/services/pipeline.py` and `uv run ruff check src/phaze/services/pipeline.py` exit 0.
  </acceptance_criteria>
  <done>get_queue_activity returns the six-key int dict, sums across non-revoked agents, reads only queued+active, and degrades each source independently to 0 with a structlog warning.</done>
</task>

<task type="auto">
  <name>Task 2: Unit-test the sum, scheduled-exclusion, and both degrade modes</name>
  <files>tests/test_services/test_pipeline.py</files>
  <read_first>
    - tests/test_services/test_pipeline.py (existing get_pipeline_stats tests + session fixture usage)
    - tests/_queue_fakes.py (FakeTaskRouter.set_counts / FakeQueue.fail_count / seed_active_agent — from Plan 00)
  </read_first>
  <action>
    Extend `tests/test_services/test_pipeline.py` with `@pytest.mark.asyncio` tests importing `get_queue_activity` and the Plan-00 harness. (1) `test_get_queue_activity_sums_across_agents`: `seed_active_agent(session, "nox")` and a second non-revoked agent (insert directly or a second `seed_active_agent(session, "lux")`); build a `SimpleNamespace(task_router=FakeTaskRouter(), controller_queue=FakeQueue("controller"))`; `task_router.set_counts("nox", queued=3, active=2)`, `set_counts("lux", queued=4, active=1)`; controller `set_counts(queued=5, active=0)`; assert `agent_queued==7`, `agent_active==3`, `agent_busy==10`, `controller_queued==5`, `controller_busy==5`. (2) `test_get_queue_activity_excludes_scheduled`: also `set_counts(..., incomplete=999)` on a queue and assert `agent_busy` is unchanged (only queued+active counted). (3) `test_get_queue_activity_degrades_on_redis_error`: seed an agent, call `fail_count()` on its queue (and the controller queue) → all six values 0, no exception. (4) `test_get_queue_activity_degrades_on_missing_app_state`: pass `SimpleNamespace()` (no `task_router`/`controller_queue`) → all six values 0. (5) `test_get_queue_activity_controller_independent_of_agents`: controller queue `fail_count()` but agents healthy → agent counts intact, controller counts 0. Name tests so `-k queue_activity`, `-k scheduled`, and `-k degrade` each select the right subset (per VALIDATION 34-01-01/02/03).
  </action>
  <verify>
    <automated>uv run pytest tests/test_services/test_pipeline.py -k "queue_activity or scheduled or degrade" -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_services/test_pipeline.py -k queue_activity -q` selects ≥1 test and passes.
    - `uv run pytest tests/test_services/test_pipeline.py -k scheduled -q` passes (incomplete ignored).
    - `uv run pytest tests/test_services/test_pipeline.py -k degrade -q` passes (both redis-error and missing-attr → all-zero).
    - Full module green: `uv run pytest tests/test_services/test_pipeline.py -q`.
  </acceptance_criteria>
  <done>Five tests prove the multi-agent sum, scheduled-exclusion, redis-degrade, missing-attr-degrade, and controller/agent independence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → Redis (SAQ) | The service reads integer queue depths from Redis; Redis may be unreachable or slow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Denial of Service | get_queue_activity on the 5s poll path | mitigate | Read-only O(agents) O(1) LLEN per source; any failure caught per-source and degraded to 0 so a Redis outage cannot 500 or stall the dashboard. |
| T-34-02 | Information Disclosure | summed queue counts | accept | Single-user admin tool on a private network; counts are non-sensitive integers. |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit: zero install surface). |
</threat_model>

<verification>
`uv run pytest tests/test_services/test_pipeline.py -q` green; `uv run mypy src/phaze/services/pipeline.py` clean; `uv run ruff check src/phaze/services/pipeline.py` clean. Confirm no `"incomplete"` literal in the new function.
</verification>

<success_criteria>
get_queue_activity exists, sums across non-revoked agents using the dashboard() predicate, reads only queued+active, degrades each source independently to zero, and is covered by five unit tests; mypy + ruff clean.
</success_criteria>

<output>
Create `.planning/milestones/v4.0-phases/34-pipeline-queue-depth-status-double-enqueue-guard/34-01-SUMMARY.md` when done.
</output>
