---
phase: 30-fix-systemic-control-plane-saq-queue-misrouting-every-manual
plan: 05
type: execute
wave: 3
depends_on: ["30-02", "30-03", "30-04"]
files_modified:
  - tests/test_no_default_queue_producers.py
  - README.md
autonomous: true
requirements: [QR-01, QR-03]
must_haves:
  truths:
    - "A guard test fails if any control-plane source file (routers/services) reintroduces an enqueue to the unnamed default queue or constructs an unnamed SAQ Queue"
    - "The guard test fails if resolve_queue_for_task is bypassed by a raw app.state.queue reference reappearing in routers/services"
    - "Project docs describe the queue-routing model (controller queue + per-agent queues) and that the default queue has no producers"
  artifacts:
    - path: "tests/test_no_default_queue_producers.py"
      provides: "Static guard asserting no default-queue producers and an unknown-task routing rejection"
      contains: "def test"
  key_links:
    - from: "tests/test_no_default_queue_producers.py"
      to: "src/phaze (routers + services)"
      via: "source scan for app.state.queue / unnamed Queue.from_url"
      pattern: "app.state.queue"
---

<objective>
Lock the fix in so the bug class cannot silently recur, and update docs. After Plans 02-04 every misrouted site is fixed; this plan adds a static guard test that fails if anyone reintroduces a default-queue producer, plus a runtime assertion that the routing helper rejects unknown task names. It also documents the queue-routing model.

Purpose: the original incident was invisible (silent 200, jobs rotting). A committed guard makes regressions loud at CI time.
Output: `tests/test_no_default_queue_producers.py` and README routing documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/milestones/v4.0-phases/30-fix-systemic-control-plane-saq-queue-misrouting-every-manual/CONTEXT.md
@.planning/milestones/v4.0-phases/30-fix-systemic-control-plane-saq-queue-misrouting-every-manual/30-01-PLAN.md

<interfaces>
<!-- Established by Plans 01-04. -->
- src/phaze/services/enqueue_router.py: CONTROLLER_TASKS, AGENT_TASKS, resolve_queue_for_task (raises ValueError on unknown task), select_active_agent, NoActiveAgentError.
- src/phaze/main.py: app.state.controller_queue = Queue.from_url(settings.redis_url, name="controller"); unnamed app.state.queue removed.
- Fixed routers (no app.state.queue): pipeline.py, tracklists.py, scan.py; ingestion.run_scan receives a per-agent queue.
- The only legitimate "queue" remaining in app.state: controller_queue (named) + task_router (per-agent) + redis (shared client).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Static + runtime guard against default-queue producers</name>
  <files>tests/test_no_default_queue_producers.py</files>
  <read_first>
    - src/phaze/main.py (confirm the only Queue construction is `name="controller"`)
    - src/phaze/services/enqueue_router.py (resolve_queue_for_task ValueError-on-unknown contract)
    - src/phaze/routers/ and src/phaze/services/ (the directories the static guard scans)
  </read_first>
  <behavior>
    - Static: scanning every .py under src/phaze/routers and src/phaze/services finds NO non-comment line matching `app.state.queue` (the removed default attr) and NO `Queue.from_url(` call that lacks a `name=` argument.
    - Runtime: `await resolve_queue_for_task("definitely_not_a_task", app_state, session)` raises ValueError.
    - Runtime: every name in CONTROLLER_TASKS routes to the controller queue (agent_id None) and every name in AGENT_TASKS routes to a `phaze-agent-<id>` queue when an active agent exists.
  </behavior>
  <action>
Create `tests/test_no_default_queue_producers.py`:
- A static-scan test: walk `src/phaze/routers` and `src/phaze/services` `.py` files; for each line, strip comments by ignoring lines whose stripped form starts with `#` (use `line.split("#", 1)[0]` for inline comments before matching to avoid false positives from doc comments); assert no remaining code substring `app.state.queue` (the removed default attribute — `controller_queue` / `task_router` / `redis` are fine; the assertion targets the exact token `app.state.queue` NOT followed by an identifier char, e.g. via regex `app\.state\.queue(?![_a-zA-Z])`). Also assert no `Queue.from_url(` occurrence in those dirs lacks a `name=` (these dirs should not construct queues at all post-fix; if any do, they must be named). Emit the offending file:line in the assertion message so a future regression is self-explaining.
- A runtime test: build a fake `app_state` (SimpleNamespace with `.controller_queue` sentinel and `.task_router.queue_for` returning a named sentinel), seed one active Agent in the DB `session`, then assert `resolve_queue_for_task` for each CONTROLLER_TASKS name returns the controller sentinel with agent_id None, each AGENT_TASKS name returns the per-agent sentinel, and an unknown name raises ValueError.
Keep the static scan resilient to `cat -n`-style false positives by operating on file contents read via pathlib, not shell.
  </action>
  <acceptance_criteria>
    - Behavior: the static-scan test passes on the post-fix tree (zero offending lines) and would fail if `app.state.queue` or an unnamed `Queue.from_url(` were reintroduced into routers/services (verify by a temporary local edit during development, then revert).
    - Behavior: `resolve_queue_for_task` runtime test covers all CONTROLLER_TASKS, all AGENT_TASKS, and the unknown-task ValueError.
    - Test: `uv run pytest tests/test_no_default_queue_producers.py -q` exits 0.
    - Lint/types: `uv run ruff check tests/test_no_default_queue_producers.py` and `uv run mypy tests/test_no_default_queue_producers.py` exit 0 (tests relax untyped-decorator only).
  </acceptance_criteria>
  <verify><automated>uv run pytest tests/test_no_default_queue_producers.py -q && uv run ruff check tests/test_no_default_queue_producers.py</automated></verify>
  <done>A committed guard test fails loudly if a default-queue producer or unnamed Queue is reintroduced, and asserts the routing helper rejects unknown tasks.</done>
</task>

<task type="auto">
  <name>Task 2: Document the queue-routing model + run the full suite</name>
  <files>README.md</files>
  <read_first>
    - README.md (find the architecture / task-queue section; keep badge line on line 1 untouched per project convention)
    - .planning/milestones/v4.0-phases/30-fix-systemic-control-plane-saq-queue-misrouting-every-manual/CONTEXT.md (the routing model + operational note)
  </read_first>
  <action>
Add a concise "Task queue routing" subsection to README.md (under the existing architecture/task-processing area) that states: control-plane (API/UI) enqueues never target an unnamed default queue; controller-bound tasks (generate_proposals, search_tracklist, scrape_and_store_tracklist, match_tracklist_to_discogs, refresh_tracklists) route to the `controller` queue consumed by the lux phaze-worker; per-agent tasks (process_file, extract_file_metadata, fingerprint_file, scan_live_set, scan_directory, execute_approved_batch) route via `AgentTaskRouter` to `phaze-agent-<id>` consumed by the nox phaze-agent-worker, selected by active-agent selection (non-revoked, recently-seen); zero active agents surfaces a clear error. Mention the guard test that enforces "no default-queue producers". Do not modify the line-1 badge block or re-add removed badges (project convention). Note the operational follow-up (clearing the stranded `saq:job:default:*` jobs after redeploy) is a deploy step, not code.
Run the full test suite + lint/type gates to confirm the whole phase integrates.
  </action>
  <acceptance_criteria>
    - Source: `grep -n "controller" README.md` and `grep -n "phaze-agent" README.md` show the routing subsection.
    - Source: README line 1 is unchanged (badge/marker convention preserved).
    - Test: `uv run pytest -q` exits 0 (full suite green across all phase-30 changes).
    - Gates: `uv run ruff check .`, `uv run ruff format --check .`, and `uv run mypy .` exit 0.
    - Coverage: `uv run pytest --cov --cov-report=term-missing -q` reports >= 85% total.
  </acceptance_criteria>
  <verify><automated>uv run pytest -q && uv run ruff check . && uv run mypy .</automated></verify>
  <done>README documents the queue-routing model and the guard; the full suite, lint, types, and coverage gates pass for the complete phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future code change -> CI | A regression that reintroduces a default-queue producer must be caught before merge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Denial of Service / Data-integrity | silent reintroduction of a default-queue producer | mitigate | Static guard test scans routers/services for `app.state.queue` and unnamed `Queue.from_url`; runtime test asserts unknown task names raise. CI fails on regression. |
| T-30-SC | Tampering | package installs | accept | No new packages in this plan. |
</threat_model>

<verification>
- `uv run pytest -q` exits 0 (full suite).
- `uv run pytest tests/test_no_default_queue_producers.py -q` exits 0.
- `uv run ruff check .` and `uv run mypy .` exit 0.
- `uv run pytest --cov --cov-report=term-missing -q` reports >= 85%.
</verification>

<success_criteria>
The bug class is guarded by a committed test, the routing model is documented, and the full phase integrates green across tests, lint, types, and coverage.
</success_criteria>

<output>
Create `.planning/milestones/v4.0-phases/30-fix-systemic-control-plane-saq-queue-misrouting-every-manual/30-05-SUMMARY.md` when done.
</output>
