---
phase: 55-routing-state-ledger-integration-the-live-seam
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - docker-compose.yml
  - docs/configuration.md
  - docs/cloud-burst.md
  - docs/deployment.md
  - tests/test_config_role_split.py
autonomous: true
requirements: [KROUTE-01]
must_haves:
  truths:
    - "Per D-02: every operator-facing config surface is migrated from PHAZE_CLOUD_BURST_ENABLED to PHAZE_CLOUD_TARGET (local/a1/k8s) — or cloud silently goes local on redeploy. The rename is made LOUD so an operator cannot miss it."
    - "PHAZE_CLOUD_TARGET is a control-plane-only setting (ControlSettings): it is ADDED to .env.example and the control-plane compose service env, NOT to any agent compose file (.env.example.agent does not exist — RESEARCH A1)."
    - "docs/cloud-burst.md (the Phase 51 homelab runbook) and docs/configuration.md describe cloud_target with the three values and the k8s setup knobs (kube_api_url/namespace/local_queue, s3_bucket/endpoint); the legacy single-switch prose is gone."
    - "Zero PHAZE_CLOUD_BURST_ENABLED / cloud_burst_enabled references remain in docs/ or operator config surfaces."
  artifacts:
    - path: ".env.example"
      provides: "PHAZE_CLOUD_TARGET (+ k8s/S3 _FILE var pointers) documented loudly"
      contains: "PHAZE_CLOUD_TARGET"
    - path: "docs/cloud-burst.md"
      provides: "cloud_target runbook prose (local/a1/k8s) replacing the single-switch toggle"
      contains: "cloud_target"
  key_links:
    - from: ".env.example"
      to: "ControlSettings.cloud_target"
      via: "PHAZE_CLOUD_TARGET env var documentation"
      pattern: "PHAZE_CLOUD_TARGET"
    - from: "docker-compose.yml"
      to: "control-plane service env"
      via: "PHAZE_CLOUD_TARGET added to api/worker environment"
      pattern: "PHAZE_CLOUD_TARGET"
---

<objective>
Migrate every operator-facing config surface from the removed `PHAZE_CLOUD_BURST_ENABLED` toggle to the
new `PHAZE_CLOUD_TARGET` selector (D-02). This is the "or cloud silently goes local on redeploy" half of
the breaking rename — the code half is Plan 01. Wave 1, parallel to Plans 01/02 (touches only docs +
compose + .env.example + one contract test — disjoint from all code plans).

RESEARCH resolved two CONTEXT/repo discrepancies the executor MUST honor:
- `.env.example` currently has NO cloud vars → this is an ADD of `PHAZE_CLOUD_TARGET` (+ the kube/S3
  `_FILE` var pointers), not a rename.
- `.env.example.agent` named in CONTEXT D-02 does NOT exist; `cloud_target` is control-plane-only
  (`ControlSettings`), so it does NOT belong on any agent compose file. Do not create a phantom file.
- `docker-compose*.yml` carries no cloud toggle today → ADD `PHAZE_CLOUD_TARGET` to the control-plane
  service env (`api` / `worker`), not a rename.

The rename must be LOUD: `.env.example` and `docs/cloud-burst.md` (the Phase 51 homelab runbook) call out
that `PHAZE_CLOUD_BURST_ENABLED` is removed and `PHAZE_CLOUD_TARGET` replaces it, defaulting to `local`.

Purpose: an operator redeploying after Phase 55 sees the rename and sets cloud_target correctly; cloud
does not silently turn off.
Output: migrated `.env.example`, control-plane compose env, `docs/configuration.md`, `docs/cloud-burst.md`,
`docs/deployment.md`, and an `.env.example` contract assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-routing-state-ledger-integration-the-live-seam/55-CONTEXT.md
@.planning/phases/55-routing-state-ledger-integration-the-live-seam/55-RESEARCH.md
@.planning/phases/55-routing-state-ledger-integration-the-live-seam/55-PATTERNS.md

<interfaces>
<!-- Verified operator surfaces (file:line from RESEARCH "Docs / operator surfaces"). -->

docs/configuration.md:89,107,112,128 — master-switch table row + kube-config section + "Master toggle" subsection.
docs/cloud-burst.md:285-300+ — "Toggle & runtime-state semantics" (PHAZE_CLOUD_BURST_ENABLED single-switch prose); THIS file is the Phase 51 homelab runbook.
docs/deployment.md:50 — "ships off by default (PHAZE_CLOUD_BURST_ENABLED=false)".
.env.example — no cloud vars today (ADD PHAZE_CLOUD_TARGET + kube/S3 _FILE pointers).
docker-compose.yml — control-plane services `api` (:13) and `worker` (:36); add PHAZE_CLOUD_TARGET to their environment. Agent compose files (docker-compose.agent.yml / .cloud-agent.yml) get NOTHING (control-plane-only setting).
tests/test_config_role_split.py:224-251 — the existing `.env.example` contract test (`_read_env_example` + per-key asserts) to extend for PHAZE_CLOUD_TARGET.

kube/S3 settings (already exist, config.py:533-579): kube_api_url, kube_namespace, kube_local_queue, s3_bucket, s3_endpoint_url, plus _FILE secret variants.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate .env.example + control-plane compose env to PHAZE_CLOUD_TARGET</name>
  <files>.env.example, docker-compose.yml, tests/test_config_role_split.py</files>
  <read_first>
    - .env.example — the file being modified (currently no cloud vars).
    - docker-compose.yml:13 (api) and :36 (worker) control-plane service env — file being modified.
    - tests/test_config_role_split.py:224-251 (_read_env_example + key-presence asserts) — the contract test to extend.
    - 55-PATTERNS.md "CONTEXT vs codebase discrepancies" table (the ADD-not-rename rows + the phantom .env.example.agent note).
  </read_first>
  <action>
    Add to .env.example, loudly, a cloud section documenting `PHAZE_CLOUD_TARGET` with the three values (`local` default = cloud off, `a1` = rsync→compute, `k8s` = S3→Kueue), an explicit note that it REPLACES the removed `PHAZE_CLOUD_BURST_ENABLED`, and example commented pointers to the per-target knobs (a1: PHAZE_COMPUTE_SCRATCH_DIR; k8s: PHAZE_KUBE_API_URL / PHAZE_KUBE_NAMESPACE / PHAZE_KUBE_LOCAL_QUEUE / PHAZE_S3_BUCKET / PHAZE_S3_ENDPOINT_URL and their _FILE secret variants). Add `PHAZE_CLOUD_TARGET` to the control-plane service environment in docker-compose.yml (`api` and `worker`) defaulting to `local`; do NOT add it to docker-compose.agent.yml or docker-compose.cloud-agent.yml (control-plane-only). Extend tests/test_config_role_split.py to assert `.env.example` mentions `PHAZE_CLOUD_TARGET` (mirroring the existing required-key asserts at :237-247).
  </action>
  <acceptance_criteria>
    - `grep -c PHAZE_CLOUD_TARGET .env.example` ≥ 1; the note that it replaces PHAZE_CLOUD_BURST_ENABLED is present.
    - `grep -c PHAZE_CLOUD_TARGET docker-compose.yml` ≥ 1 (in the control-plane service env); `grep -c PHAZE_CLOUD_TARGET docker-compose.agent.yml docker-compose.cloud-agent.yml` == 0.
    - `grep -rc 'PHAZE_CLOUD_BURST_ENABLED\|cloud_burst' .env.example docker-compose*.yml` == 0.
    - `uv run pytest tests/test_config_role_split.py -x` passes with the new PHAZE_CLOUD_TARGET assertion.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_config_role_split.py -x</automated>
  </verify>
  <done>.env.example + control-plane compose env document PHAZE_CLOUD_TARGET loudly; agent compose files untouched; contract test green; no cloud_burst strings remain in these surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite docs/configuration.md, docs/cloud-burst.md, docs/deployment.md to cloud_target</name>
  <files>docs/configuration.md, docs/cloud-burst.md, docs/deployment.md</files>
  <read_first>
    - docs/configuration.md:89,107,112,128 (master-switch row + kube section + Master-toggle subsection) — file being modified.
    - docs/cloud-burst.md:285-300+ (Toggle & runtime-state semantics; the Phase 51 homelab runbook) — file being modified.
    - docs/deployment.md:50 (off-by-default line) — file being modified.
    - 55-RESEARCH.md "Docs / operator surfaces (MUST migrate)" + "Runtime State Inventory" (the loud-rename mandate).
  </read_first>
  <action>
    Rewrite the cloud sections to describe `cloud_target` (`local` / `a1` / `k8s`) as the single source of truth: in configuration.md replace the master-switch table row + "Master toggle" subsection with a `PHAZE_CLOUD_TARGET` row documenting the three values and the per-target required knobs (a1→compute_scratch_dir; k8s→kube_api_url/namespace/local_queue + s3_bucket/endpoint). In cloud-burst.md replace the single-switch "Toggle & runtime-state semantics" prose with cloud_target semantics AND add the k8s setup steps (select cloud_target=k8s, set the kube/S3 knobs). In deployment.md update the off-by-default line to `PHAZE_CLOUD_TARGET=local` (cloud off by default). Preserve the GSD doc marker on line 1 and the README/doc badge conventions. Make the removal of PHAZE_CLOUD_BURST_ENABLED explicit (a "renamed in v6.0" callout) so the operator updates their live env.
  </action>
  <acceptance_criteria>
    - `grep -rc 'PHAZE_CLOUD_BURST_ENABLED\|cloud_burst_enabled' docs/` == 0.
    - configuration.md, cloud-burst.md, deployment.md each reference `cloud_target` / `PHAZE_CLOUD_TARGET` with the local/a1/k8s values.
    - cloud-burst.md documents the k8s setup knobs (kube_api_url/namespace/local_queue, s3_bucket/endpoint).
    - An explicit "PHAZE_CLOUD_BURST_ENABLED removed/renamed" callout is present in at least cloud-burst.md and .env.example (loud rename).
    - The GSD doc marker on line 1 of each doc is intact.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(grep -rn 'PHAZE_CLOUD_BURST_ENABLED\|cloud_burst_enabled' docs/)" && pre-commit run --files docs/configuration.md docs/cloud-burst.md docs/deployment.md</automated>
  </verify>
  <done>All three operator docs describe cloud_target (local/a1/k8s) with the per-target k8s knobs and a loud rename callout; zero cloud_burst references remain in docs/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/.env.example → operator redeploy | the documented env var name is what the operator sets in the live (out-of-git) homelab env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-DOC-01 | Denial of Service | stale docs → operator keeps PHAZE_CLOUD_BURST_ENABLED → cloud silently off on redeploy | mitigate | loud rename callout in .env.example + cloud-burst.md; every operator surface migrated; grep gate asserts zero legacy references |
| T-55-DOC-02 | Information disclosure | documenting kube/S3 secrets inline | mitigate | docs reference the `_FILE` secret-pointer convention only (SecretStr, control-plane), never literal secret values |
| T-55-DOC-03 | Tampering | adding control-plane-only cloud_target to an agent compose file | mitigate | acceptance criterion asserts PHAZE_CLOUD_TARGET count == 0 in the agent compose files (RESEARCH A1) |
| T-55-DOC-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed (docs-only plan) |
</threat_model>

<verification>
- `grep -rn 'PHAZE_CLOUD_BURST_ENABLED\|cloud_burst_enabled\|cloud_burst' .env.example docker-compose*.yml docs/` → empty.
- `grep -c PHAZE_CLOUD_TARGET .env.example docker-compose.yml` ≥ 1 each; agent compose files == 0.
- `uv run pytest tests/test_config_role_split.py -x` green (the .env.example contract assertion).
- `pre-commit run --files .env.example docker-compose.yml docs/configuration.md docs/cloud-burst.md docs/deployment.md` clean (yamllint/markdown hooks).
</verification>

<success_criteria>
- Every operator-facing surface (.env.example, control-plane compose, configuration.md, cloud-burst.md, deployment.md) documents PHAZE_CLOUD_TARGET (local/a1/k8s) and the per-target k8s knobs.
- The PHAZE_CLOUD_BURST_ENABLED removal is loud; zero legacy references remain in docs or operator config.
- cloud_target stays control-plane-only (no agent compose file references it).
</success_criteria>

<output>
Create `.planning/phases/55-routing-state-ledger-integration-the-live-seam/55-06-SUMMARY.md` when done.
</output>
