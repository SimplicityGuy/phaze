---
phase: 54-kube-submit-watch-reconcile-cron
plan: 05
type: execute
wave: 3
depends_on: ["54-02", "54-03"]
files_modified:
  - src/phaze/tasks/submit_cloud_job.py
  - src/phaze/services/enqueue_router.py
  - src/phaze/tasks/controller.py
  - tests/test_tasks/test_submit_cloud_job.py
  - tests/test_enqueue_router.py
  - tests/test_task_split.py
autonomous: true
requirements: [KSUBMIT-01, KSUBMIT-02, KSUBMIT-06]
user_setup: []

must_haves:
  truths:
    - "KSUBMIT-02: submit_cloud_job does ONE kube POST (via kube_staging.submit_job) and returns in seconds — it never awaits analysis; it is a controller-queue task routed through enqueue_router"
    - "KSUBMIT-01: a re-submit for the same file_id is idempotent — the deterministic Job name + the kube 409→refresh path mean no duplicate Job and no duplicate cloud_job row (upsert on the unique file_id FK)"
    - "KSUBMIT-06: the submit path writes ONLY the cloud_job row (status=SUBMITTED, kueue_workload=<job-name>) — it NEVER seeds a process_file:<id> SchedulingLedger row (the CLOUDROUTE-02 hazard)"
    - "submit_cloud_job is registered in enqueue_router.CONTROLLER_TASKS and controller.settings['functions']; it is NOT auto-wired into stage_cloud_window here (Phase 55 owns the trigger)"
  artifacts:
    - path: "src/phaze/tasks/submit_cloud_job.py"
      provides: "the fast controller-queue submit producer (upsert cloud_job + kube submit, no ledger seed)"
      contains: "async def submit_cloud_job"
      min_lines: 40
    - path: "src/phaze/services/enqueue_router.py"
      provides: "submit_cloud_job added to CONTROLLER_TASKS"
      contains: "submit_cloud_job"
  key_links:
    - from: "src/phaze/tasks/submit_cloud_job.py"
      to: "src/phaze/services/kube_staging.py submit_job"
      via: "single kube POST returning the Job name/uid recorded as kueue_workload"
      pattern: "kube_staging"
    - from: "src/phaze/services/enqueue_router.py CONTROLLER_TASKS"
      to: "controller.settings['functions']"
      via: "submit_cloud_job present in both (the routability invariant)"
      pattern: "submit_cloud_job"
---

<objective>
Build the fast `submit_cloud_job` controller-queue task: one kube POST that returns in seconds,
upserts the `cloud_job` row (SUBMITTED + the Kueue Job name), and — critically — seeds NO
`process_file` ledger row. Register it as a routable controller task.

Purpose: KSUBMIT-02 (fast submit, never blocks a worker), KSUBMIT-01 (idempotent via the
deterministic name + 409 + the unique-file_id upsert), KSUBMIT-06 (no ledger seed → recover_orphaned_work
can never re-enqueue a K8s file onto a local agent queue). The task is built + unit-tested here but is
NOT wired into the live `stage_cloud_window` routing seam — Phase 55 owns that trigger.
Output: submit_cloud_job.py + enqueue_router/controller registration + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-kube-submit-watch-reconcile-cron/54-CONTEXT.md
@.planning/phases/54-kube-submit-watch-reconcile-cron/54-RESEARCH.md
@.planning/phases/54-kube-submit-watch-reconcile-cron/54-PATTERNS.md
@src/phaze/services/cloud_staging.py
@src/phaze/services/enqueue_router.py
@src/phaze/tasks/controller.py
@src/phaze/services/kube_staging.py
@CLAUDE.md

<interfaces>
<!-- cloud_staging.stage_file_to_s3 is the upsert-then-act analog (the producer body shape). -->
stmt = pg_insert(CloudJob).values(id=uuid.uuid4(), file_id=file.id, s3_key=..., status=CloudJobStatus.UPLOADING.value, upload_id=upload_id)
stmt = stmt.on_conflict_do_update(index_elements=["file_id"], set_={...})   # id OUT of set_ (PK immutable on re-stage)
# For Phase 54: status=CloudJobStatus.SUBMITTED.value, kueue_workload=<job-name>; increment attempts only on a re-drive.
# kube_staging seam (Plan 03):
name, uid = await kube_staging.submit_job(file_id)   # one POST; 409→refresh inside the seam (idempotent)
# enqueue_router CONTROLLER_TASKS (frozenset) MUST stay in sync with controller.settings["functions"]; resolve_queue_for_task fails loud on an unroutable task.
# NO-LEDGER-SEED: do NOT import/write SchedulingLedger here (reenqueue.recover_orphaned_work replays process_file:<id> ledger rows onto a LOCAL agent queue — KSUBMIT-06 / CLOUDROUTE-02).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: submit_cloud_job fast producer</name>
  <files>src/phaze/tasks/submit_cloud_job.py, tests/test_tasks/test_submit_cloud_job.py</files>
  <behavior>
    - submit_cloud_job(ctx, file_id) resolves ControlSettings, calls kube_staging.submit_job(file_id) exactly once, and upserts the cloud_job row with status=SUBMITTED and kueue_workload=<job-name> on conflict(file_id).
    - It returns promptly after the single POST (no analysis await) — assert it does not call any analysis/result writer.
    - A second submit for the same file_id is idempotent: one cloud_job row (upsert), and the seam's 409→refresh means no duplicate Job (test via monkeypatched seam returning the same name).
    - It writes NO SchedulingLedger row (no process_file:<id> seed).
  </behavior>
  <read_first>
    - src/phaze/services/cloud_staging.py (stage_file_to_s3 upsert-then-enqueue body 52-126; redrive_upload 129-143)
    - src/phaze/services/kube_staging.py (submit_job from Plan 03)
    - src/phaze/tasks/release_awaiting_cloud.py (CONTROL-ONLY cron/task discipline: needs ctx["async_session"]; FastAPI-free imports)
    - src/phaze/tasks/reenqueue.py (recover_orphaned_work + how SchedulingLedger process_file rows are replayed — the hazard to avoid)
    - tests/kube_fakes.py (the Layer-1 fakes), tests/test_services/test_cloud_staging.py (monkeypatched-seam test idiom)
    - .planning/phases/54-kube-submit-watch-reconcile-cron/54-PATTERNS.md (submit_cloud_job.py section)
  </read_first>
  <action>
    Create `src/phaze/tasks/submit_cloud_job.py` mirroring the cloud_staging producer discipline (`__future__` annotations, TYPE_CHECKING guard, structlog, CONTROL-ONLY — reads ctx["async_session"] and ControlSettings via get_settings()). Implement `async def submit_cloud_job(ctx, file_id)`: open a session, call `kube_staging.submit_job(file_id)` once to get the Job name/uid, then `pg_insert(CloudJob)...on_conflict_do_update(index_elements=["file_id"], set_=...)` setting status=`CloudJobStatus.SUBMITTED.value` and `kueue_workload=<job-name>` (stamp `id=uuid.uuid4()` in values, keep it OUT of set_ per the CR-01 precedent), commit, log. Do NOT touch SchedulingLedger and do NOT import it. Keep the task thin — the 409 idempotency lives in the seam. Expose a deterministic enqueue key convention `submit_cloud_job:<file_id>` for callers (a module helper mirroring push_file_job_key), but do NOT call stage_cloud_window or wire any trigger.
    Write tests/test_tasks/test_submit_cloud_job.py using a monkeypatched kube_staging.submit_job: assert one cloud_job row at status SUBMITTED with kueue_workload set; a re-submit yields exactly one row (upsert) and calls submit_job again returning the same name (idempotent, no duplicate); assert NO SchedulingLedger row exists for the file (`select(SchedulingLedger).where(key == f"process_file:{file_id}")` is empty) and that the module source imports no SchedulingLedger symbol; assert submit_cloud_job never calls put_analysis/report_analysis_failed (fast-return, no result write).
  </action>
  <verify>
    <automated>uv run pytest tests/test_tasks/test_submit_cloud_job.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "async def submit_cloud_job" src/phaze/tasks/submit_cloud_job.py` returns 1.
    - `grep -c "SchedulingLedger" src/phaze/tasks/submit_cloud_job.py` returns 0 (no-ledger-seed invariant).
    - The test proves: one SUBMITTED row with kueue_workload after submit; one row (not two) after re-submit; zero process_file ledger rows.
    - `grep -n "submit_cloud_job:" src/phaze/tasks/submit_cloud_job.py` shows the deterministic key helper.
    - `uv run mypy src/phaze/tasks/submit_cloud_job.py` passes.
  </acceptance_criteria>
  <done>A fast, idempotent submit producer writes only the cloud_job row (SUBMITTED + kueue_workload) with no ledger seed and no analysis await.</done>
</task>

<task type="auto">
  <name>Task 2: Register submit_cloud_job as a routable controller task</name>
  <files>src/phaze/services/enqueue_router.py, src/phaze/tasks/controller.py, tests/test_enqueue_router.py, tests/test_task_split.py</files>
  <read_first>
    - src/phaze/services/enqueue_router.py (CONTROLLER_TASKS frozenset 44-58 + the "must mirror controller functions" docstring)
    - src/phaze/tasks/controller.py (the import block 40-43, settings["functions"] 205-213)
    - tests/test_enqueue_router.py (the routability test for controller tasks)
    - tests/test_task_split.py (the control/agent import-boundary assertions)
    - .planning/phases/54-kube-submit-watch-reconcile-cron/54-PATTERNS.md (enqueue_router.py + controller.py sections)
  </read_first>
  <action>
    Add `"submit_cloud_job"` to `CONTROLLER_TASKS` in enqueue_router.py (control-plane work — kube creds live there). In controller.py import `submit_cloud_job` and add it to `settings["functions"]` so it is dispatchable on the controller queue. Do NOT add any CronJob for submit_cloud_job and do NOT call it from stage_cloud_window (Phase 55 wires the trigger). Update tests/test_enqueue_router.py to assert `resolve_queue_for_task("submit_cloud_job", ...)` routes to the controller queue, and that CONTROLLER_TASKS stays in sync with controller.settings["functions"]. Update tests/test_task_split.py if it enumerates control-only functions, to include submit_cloud_job (and confirm it is NOT in the agent worker).
  </action>
  <verify>
    <automated>uv run pytest tests/test_enqueue_router.py tests/test_task_split.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "submit_cloud_job" src/phaze/services/enqueue_router.py` shows it inside CONTROLLER_TASKS.
    - `grep -n "submit_cloud_job" src/phaze/tasks/controller.py` shows both the import and its presence in settings["functions"].
    - `grep -c "CronJob(submit_cloud_job" src/phaze/tasks/controller.py` returns 0 (no cron for the submit task).
    - The enqueue-router test proves submit_cloud_job resolves to the controller queue and the sync invariant holds.
  </acceptance_criteria>
  <done>submit_cloud_job is a routable controller task (CONTROLLER_TASKS + functions in sync), with no trigger wiring (deferred to Phase 55).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| enqueue seam → SAQ queue | every K8s submit must route through enqueue_router to a consumer (never the default queue) |
| submit path → recovery ledger | the K8s submit must NOT enter the local-agent re-enqueue ledger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-12 | Elevation of privilege | process_file ledger seed for a K8s file | mitigate | KSUBMIT-06: submit_cloud_job writes ONLY the cloud_job row; a grep test asserts no SchedulingLedger import/write so recover_orphaned_work can never re-enqueue the file onto a local agent queue (CLOUDROUTE-02) |
| T-54-13 | Tampering | enqueue onto a consumer-less default queue | mitigate | submit_cloud_job is in CONTROLLER_TASKS; resolve_queue_for_task fails loud on an unroutable task (Phase 30 invariant) |
| T-54-14 | Denial of service | duplicate Job submission | mitigate | deterministic name + seam 409→refresh + unique-file_id upsert make re-submit idempotent (no duplicate Job/row) |
</threat_model>

<verification>
- `uv run pytest tests/test_tasks/test_submit_cloud_job.py tests/test_enqueue_router.py tests/test_task_split.py -x` green.
- `uv run ruff check . && uv run mypy src/phaze/tasks/submit_cloud_job.py` clean.
</verification>

<success_criteria>
A fast, idempotent `submit_cloud_job` controller task writes only the `cloud_job` row (SUBMITTED +
Kueue Job name) with NO ledger seed (KSUBMIT-01/02/06), routed through enqueue_router and registered in
controller functions, with the live trigger deferred to Phase 55.
</success_criteria>

<output>
Create `.planning/phases/54-kube-submit-watch-reconcile-cron/54-05-SUMMARY.md` when done.
</output>
