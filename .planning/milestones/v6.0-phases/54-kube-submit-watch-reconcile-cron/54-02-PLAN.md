---
phase: 54-kube-submit-watch-reconcile-cron
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/models/cloud_job.py
  - alembic/versions/026_add_cloud_job_kube_columns.py
  - tests/test_models/test_cloud_job.py
  - tests/test_migrations/test_migration_026_kube_columns.py
autonomous: true
requirements: [KSUBMIT-04, KSUBMIT-05, KSUBMIT-06]
user_setup: []

must_haves:
  truths:
    - "D-09: CloudJobStatus gains the submit/reconcile lifecycle members SUBMITTED, RUNNING, SUCCEEDED (existing UPLOADING/UPLOADED/FAILED retained); only the CHECK membership list changes (string-backed, no Postgres enum-type migration)"
    - "D-09: cloud_job gains `kueue_workload` (the Kueue/Job name), `attempts` (int, default 0), and `inadmissible` (bool, default false) columns in Phase 54's OWN migration"
    - "D-09: `cloud_phase` is left untouched (reserved for Phase 55)"
    - "Migration 026 chains off 025, is additive + reversible, and touches ONLY cloud_job (never saq_jobs)"
  artifacts:
    - path: "src/phaze/models/cloud_job.py"
      provides: "extended CloudJobStatus + kueue_workload/attempts/inadmissible columns + updated CHECK"
      contains: "kueue_workload"
    - path: "alembic/versions/026_add_cloud_job_kube_columns.py"
      provides: "additive reversible migration for the 3 columns + CHECK swap"
      contains: "026"
  key_links:
    - from: "src/phaze/models/cloud_job.py CheckConstraint"
      to: "migration 026 create_check_constraint"
      via: "identical membership list ('uploading','uploaded','submitted','running','succeeded','failed')"
      pattern: "submitted.*running.*succeeded"
---

<objective>
Extend the Phase 53 `cloud_job` sidecar with the Kube submit/reconcile lifecycle: new
CloudJobStatus members and the `kueue_workload` / `attempts` / `inadmissible` columns, delivered in
Phase 54's own additive Alembic migration (D-09).

Purpose: the reconcile cron uses `cloud_job` rows as the durable in-flight registry (D-02);
`kueue_workload` records the Job name at submit, `attempts` tracks the bounded re-drive cap (D-08),
`inadmissible` drives the operator alert (D-06). `cloud_phase` is deliberately deferred to Phase 55.
Output: extended model + migration 026 + model/migration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-kube-submit-watch-reconcile-cron/54-CONTEXT.md
@.planning/phases/54-kube-submit-watch-reconcile-cron/54-RESEARCH.md
@.planning/phases/54-kube-submit-watch-reconcile-cron/54-PATTERNS.md
@src/phaze/models/cloud_job.py
@alembic/versions/025_add_cloud_job.py
@CLAUDE.md

<interfaces>
<!-- Current model (cloud_job.py) — extend in place. -->
class CloudJobStatus(enum.StrEnum):  # string-backed; CHECK is the membership gate
    UPLOADING = "uploading"; UPLOADED = "uploaded"; FAILED = "failed"
status: Mapped[str] = mapped_column(String(16), nullable=False)   # "succeeded" (9 chars) fits String(16)
__table_args__ = (CheckConstraint("status IN ('uploading', 'uploaded', 'failed')", name="status_enum"),)
<!-- Migration 025 idioms to mirror: bare constraint name "status_enum" (the convention re-prefixes ck_cloud_job_);
     CRITICAL banner "this migration touches ONLY cloud_job; never reference saq_jobs". -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend CloudJobStatus + add kube columns to the model</name>
  <files>src/phaze/models/cloud_job.py, tests/test_models/test_cloud_job.py</files>
  <behavior>
    - CloudJobStatus has members SUBMITTED="submitted", RUNNING="running", SUCCEEDED="succeeded" plus the existing UPLOADING/UPLOADED/FAILED.
    - CloudJob has nullable `kueue_workload: str | None` (String(255)), non-null `attempts: int` (server_default "0", default 0), non-null `inadmissible: bool` (server_default false, default False).
    - The CheckConstraint membership list is exactly ('uploading','uploaded','submitted','running','succeeded','failed').
    - `cloud_phase` is NOT added (Phase 55).
  </behavior>
  <read_first>
    - src/phaze/models/cloud_job.py (the StrEnum at 28-39, the columns + CheckConstraint at 46-62)
    - tests/test_models/test_cloud_job.py (existing model tests to extend)
    - .planning/phases/54-kube-submit-watch-reconcile-cron/54-PATTERNS.md (src/phaze/models/cloud_job.py section — exact column shapes)
  </read_first>
  <action>
    Add SUBMITTED/RUNNING/SUCCEEDED to CloudJobStatus (keep UPLOADING/UPLOADED/FAILED). Add `kueue_workload` (Mapped[str | None], String(255), nullable=True), `attempts` (Mapped[int], Integer, nullable=False, server_default="0", default=0), and `inadmissible` (Mapped[bool], Boolean, nullable=False, server_default="false", default=False). Update the CheckConstraint(name="status_enum") membership to the 6-member list above. Do NOT add `cloud_phase`. Extend tests/test_models/test_cloud_job.py to assert the new enum members' string values and that the new column attributes exist with the documented defaults.
  </action>
  <verify>
    <automated>uv run pytest tests/test_models/test_cloud_job.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "SUBMITTED|RUNNING|SUCCEEDED" src/phaze/models/cloud_job.py` shows the 3 new members.
    - `grep -nE "kueue_workload|attempts|inadmissible" src/phaze/models/cloud_job.py` shows 3 columns.
    - The CheckConstraint string contains `submitted`, `running`, and `succeeded`.
    - `grep -c "cloud_phase" src/phaze/models/cloud_job.py` returns 0.
    - `uv run mypy src/phaze/models/cloud_job.py` passes.
  </acceptance_criteria>
  <done>Model carries the full submit/reconcile lifecycle enum + the 3 new columns; cloud_phase untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 026 + migration test</name>
  <files>alembic/versions/026_add_cloud_job_kube_columns.py, tests/test_migrations/test_migration_026_kube_columns.py</files>
  <read_first>
    - alembic/versions/025_add_cloud_job.py (revision header, the bare-name CHECK idiom at 53/63, the CRITICAL "touches ONLY cloud_job" banner)
    - tests/test_migrations/test_migration_025_cloud_job.py (the static-revision + integration upgrade/downgrade + never-references-saq_jobs test structure to mirror)
    - tests/test_migrations/conftest.py (MIGRATIONS_TEST_DATABASE_URL, upgrade_to/downgrade_to helpers)
    - .planning/phases/54-kube-submit-watch-reconcile-cron/54-PATTERNS.md (alembic/versions/026_*.py section — exact upgrade/downgrade body)
  </read_first>
  <action>
    Create migration `026` with `down_revision = "025"`. upgrade(): `op.add_column` for `kueue_workload` (sa.String(255), nullable=True), `attempts` (sa.Integer(), server_default="0", nullable=False), `inadmissible` (sa.Boolean(), server_default=sa.false(), nullable=False); then `op.drop_constraint("status_enum", "cloud_job", type_="check")` and `op.create_check_constraint("status_enum", "cloud_job", "status IN ('uploading', 'uploaded', 'submitted', 'running', 'succeeded', 'failed')")`. Pass the BARE name `status_enum` (the naming convention re-prefixes `ck_cloud_job_`). downgrade(): drop + recreate the CHECK with the original 3-member list, then drop the 3 columns (reverse order). Copy the CRITICAL banner: this migration touches ONLY `cloud_job`; never reference `saq_jobs`.
    Write tests/test_migrations/test_migration_026_kube_columns.py mirroring test_migration_025: static assertions (revision=="026", down_revision=="025", branch_labels is None) that run without a DB; a never-references-saq_jobs grep assertion; and an integration body that upgrades to 025 then 026, asserts the 3 columns exist and the CHECK accepts 'submitted'/'running'/'succeeded' and rejects an unknown value, then downgrades to 025 and asserts the columns are gone and the CHECK rejects 'submitted' again.
  </action>
  <verify>
    <automated>uv run pytest tests/test_migrations/test_migration_026_kube_columns.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'down_revision' alembic/versions/026_add_cloud_job_kube_columns.py` shows `"025"`.
    - The migration adds exactly `kueue_workload`, `attempts`, `inadmissible` and swaps the CHECK to the 6-member list.
    - `grep -i 'saq_jobs' alembic/versions/026_add_cloud_job_kube_columns.py` appears ONLY inside the banner comment (never in op.* calls).
    - The integration test proves upgrade adds columns + new CHECK membership and downgrade reverses cleanly.
    - `uv run pytest tests/test_migrations/ -k cloud_job` green.
  </acceptance_criteria>
  <done>Additive reversible migration 026 lands the 3 columns + CHECK swap, scoped to cloud_job, with upgrade/downgrade proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application code → database schema | the migration alters a table the control plane writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-03 | Tampering | cloud_job.status values | mitigate | DB CHECK constraint is the authoritative membership gate; only the 6 known members are accepted |
| T-54-04 | Denial of service | irreversible migration | mitigate | downgrade() reverses both columns and CHECK; migration test proves round-trip |
| T-54-05 | Tampering | accidental saq_jobs reference | mitigate | banner + grep-style test asserts the migration never touches the SAQ-owned table |
</threat_model>

<verification>
- `uv run pytest tests/test_models/test_cloud_job.py tests/test_migrations/test_migration_026_kube_columns.py -x` green.
- `uv run ruff check . && uv run mypy src/phaze/models/cloud_job.py` clean.
</verification>

<success_criteria>
`cloud_job` carries the submit/reconcile lifecycle enum + `kueue_workload`/`attempts`/`inadmissible`
columns via additive reversible migration 026 (D-09); `cloud_phase` deferred to Phase 55; model and
migration tests prove the schema and the CHECK membership.
</success_criteria>

<output>
Create `.planning/phases/54-kube-submit-watch-reconcile-cron/54-02-SUMMARY.md` when done.
</output>
