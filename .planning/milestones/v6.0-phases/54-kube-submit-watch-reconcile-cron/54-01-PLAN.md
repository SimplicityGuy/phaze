---
phase: 54-kube-submit-watch-reconcile-cron
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/phaze/config.py
  - tests/test_config/test_kube_settings.py
  - docs/configuration.md
autonomous: false
requirements: [KSUBMIT-01, KSUBMIT-05]
user_setup: []

must_haves:
  truths:
    - "D-08: a new `cloud_submit_max_attempts` ControlSettings knob (default 3, bounded gt=0/lt=20) exists, mirroring push_max_attempts"
    - "The kube client surface (api url, namespace, local-queue, job image, resource requests, workload apiVersion) is configurable on ControlSettings, all optional (default None) in Phase 54"
    - "Kube credentials (`kube_kubeconfig`, `kube_sa_token`) resolve via the `<VAR>_FILE` secret convention and are SecretStr (never logged)"
    - "kr8s is a control-plane dependency, verified legitimate before install"
  artifacts:
    - path: "src/phaze/config.py"
      provides: "cloud_submit_max_attempts + kube_* config fields + kube creds in SECRET_FILE_FIELDS"
      contains: "cloud_submit_max_attempts"
    - path: "pyproject.toml"
      provides: "kr8s control-plane dependency (alphabetically sorted)"
      contains: "kr8s"
  key_links:
    - from: "src/phaze/config.py"
      to: "_resolve_secret_files before-validator"
      via: "SECRET_FILE_FIELDS membership for kube_kubeconfig / kube_sa_token"
      pattern: "kube_kubeconfig"
---

<objective>
Lay the dependency + configuration foundation for the Kube submit/reconcile leg: install the kr8s
control-plane client (behind a package-legitimacy gate) and add every K8s/kube config field the
seam, submit task, and reconcile cron will read — all optional in Phase 54 so existing Phase 53
cloud-on-but-no-kube deploys keep working.

Purpose: D-08 introduces a distinct `cloud_submit_max_attempts` retry budget; the seam needs a
kube client surface (`kube_api_url`/`kube_namespace`/`kube_local_queue`/`kube_job_image`/resource
requests/`kube_workload_api_version`) and `_FILE`-resolved kube credentials.
Output: kr8s in `pyproject.toml`/`uv.lock`; new ControlSettings fields; config tests; docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-kube-submit-watch-reconcile-cron/54-CONTEXT.md
@.planning/phases/54-kube-submit-watch-reconcile-cron/54-RESEARCH.md
@.planning/phases/54-kube-submit-watch-reconcile-cron/54-PATTERNS.md
@src/phaze/config.py
@CLAUDE.md

<interfaces>
<!-- Existing precedents the new fields must mirror exactly (config.py). -->
push_max_attempts: int = Field(default=3, gt=0, lt=20, validation_alias=AliasChoices("PHAZE_PUSH_MAX_ATTEMPTS", "push_max_attempts"), ...)   # config.py:421
cloud_max_in_flight: int = Field(default=2, gt=0, lt=100, ...)                                                                                # config.py:411
SECRET_FILE_FIELDS: ClassVar[frozenset[str]] = BaseSettings.SECRET_FILE_FIELDS | {"openai_api_key","anthropic_api_key","s3_access_key_id","s3_secret_access_key"}  # config.py:345
# Optional-S3-field shape to mirror for the kube_* fields:
s3_endpoint_url: str | None = Field(default=None, validation_alias=AliasChoices("PHAZE_S3_ENDPOINT_URL","s3_endpoint_url"), ...)               # config.py:447
# The _resolve_secret_files before-validator (config.py:90-148) auto-resolves <VAR>_FILE siblings — no new resolver code needed.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: kr8s package-legitimacy gate (blocking)</name>
  <read_first>
    - .planning/phases/54-kube-submit-watch-reconcile-cron/54-RESEARCH.md (## Package Legitimacy Audit — kr8s tagged [ASSUMED]; slopcheck unavailable at research time)
  </read_first>
  <files>(none — verification-only gate; the install happens in Task 2)</files>
  <what-built>
    Nothing yet — this gate precedes the `uv add kr8s` install in Task 2. RESEARCH tagged kr8s
    `[ASSUMED]` because slopcheck was unavailable; the install must not proceed unverified
    (threat T-54-SC, supply-chain tampering). This checkpoint is NOT auto-approvable.
  </what-built>
  <action>
    Pause for the operator to verify kr8s legitimacy BEFORE Task 2 runs `uv add kr8s`. Present the
    pypi.org/project/kr8s and github.com/kr8s-org/kr8s evidence below and wait for an explicit
    "approved". Do NOT auto-advance this checkpoint even if workflow.auto_advance is set
    (legitimacy gates are never auto-approvable). On approval, proceed to Task 2; on any concern, halt.
  </action>
  <verify>
    <human-check>Operator has confirmed kr8s 0.20.15 is the genuine kr8s-org package and typed "approved".</human-check>
  </verify>
  <done>Operator approved the kr8s install (or halted the plan with documented concerns).</done>
  <how-to-verify>
    1. Open https://pypi.org/project/kr8s/ — confirm version 0.20.15 exists, `requires_python >=3.9`,
       a real release history (0.1.0 → 0.20.15), and project URLs (Repository/Docs/Changelog) resolve.
    2. Open https://github.com/kr8s-org/kr8s — confirm an active, documented source repo (docs.kr8s.org).
    3. Confirm the wheel is pure-Python with no postinstall scripts.
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms kr8s 0.20.15 on PyPI is the genuine kr8s-org package with a resolving source repo.
    - This checkpoint is recorded as blocking-human (never auto-advanced).
  </acceptance_criteria>
  <resume-signal>Type "approved" to allow the kr8s install, or describe concerns to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install kr8s + add kube config surface to ControlSettings</name>
  <files>pyproject.toml, uv.lock, src/phaze/config.py, tests/test_config/test_kube_settings.py, docs/configuration.md</files>
  <read_first>
    - src/phaze/config.py (push_max_attempts at line 421, cloud_max_in_flight at 411, SECRET_FILE_FIELDS at 345, the optional s3_* fields at 447-466, the _resolve_secret_files validator at 90-148)
    - tests/test_config/test_push_config.py (the analog test module for a bounded int knob)
    - tests/test_config/test_s3_settings.py (the analog for optional fields + _FILE resolution)
    - .planning/phases/54-kube-submit-watch-reconcile-cron/54-RESEARCH.md (## kr8s Client & Config — the full field table + the "optional in Phase 54, do NOT couple to cloud_burst_enabled" note)
    - .planning/phases/54-kube-submit-watch-reconcile-cron/54-PATTERNS.md (src/phaze/config.py section)
  </read_first>
  <action>
    Run `uv add kr8s` (resolves >=0.20.15; the 7-day exclude-newer cooldown is satisfied — kr8s 0.20.15 is well-aged). Keep `[project.dependencies]` alphabetically sorted per CLAUDE.md. Place kr8s as a control-plane dependency (do NOT add it to any agent/pod dependency surface — kube creds live only on the control plane, mirroring aioboto3).
    In ControlSettings add `cloud_submit_max_attempts: int` mirroring `push_max_attempts` EXACTLY (default=3, gt=0, lt=20, AliasChoices("PHAZE_CLOUD_SUBMIT_MAX_ATTEMPTS","cloud_submit_max_attempts")) per D-08 — a distinct budget from push_max_attempts.
    Add these OPTIONAL kube fields (all `default=None` except the apiVersion default), mirroring the s3_endpoint_url shape with AliasChoices("PHAZE_<UPPER>","<lower>"): `kube_api_url: str | None`, `kube_namespace: str | None`, `kube_local_queue: str | None`, `kube_job_image: str | None`, `kube_job_cpu_request: str | None`, `kube_job_memory_request: str | None`, and `kube_workload_api_version: str` with default `"kueue.x-k8s.io/v1beta1"`. Do NOT add a fail-fast model validator coupling them to `cloud_burst_enabled` — that validator is Phase 55 (KDEPLOY-02). Adding it here would break existing Phase 53 cloud-on/no-kube deploys.
    Add `kube_kubeconfig: SecretStr | None` and `kube_sa_token: SecretStr | None`, and extend `ControlSettings.SECRET_FILE_FIELDS` with `"kube_kubeconfig"` and `"kube_sa_token"` so the existing `_resolve_secret_files` before-validator auto-resolves their `<VAR>_FILE` siblings (no new resolver code).
    Write `tests/test_config/test_kube_settings.py` asserting: default `cloud_submit_max_attempts == 3`; an out-of-range value (e.g. 0 or 20) raises a validation error; the kube_* fields default to None / the apiVersion default; `kube_kubeconfig`/`kube_sa_token` are SecretStr and resolve from a `PHAZE_KUBE_KUBECONFIG_FILE` sibling written to a tmp path.
    Document the new fields in docs/configuration.md under the cloud/kube section (table row per field; note they are optional in Phase 54, fail-fast validation arrives in Phase 56).
  </action>
  <verify>
    <automated>uv run pytest tests/test_config/test_kube_settings.py -x && uv run python -c "import kr8s"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "cloud_submit_max_attempts" src/phaze/config.py` shows a Field with `gt=0, lt=20, default=3`.
    - `grep -nE "kube_api_url|kube_namespace|kube_local_queue|kube_job_image|kube_workload_api_version" src/phaze/config.py` returns 5 field definitions.
    - `grep -n "kube_kubeconfig" src/phaze/config.py` appears both as a SecretStr field AND inside SECRET_FILE_FIELDS.
    - `grep -n "kr8s" pyproject.toml` shows the dependency; `uv run python -c "import kr8s"` exits 0.
    - No model validator references `cloud_target` or couples kube_* to `cloud_burst_enabled` (Phase 55 owns that).
    - `uv run ruff check src/phaze/config.py` and `uv run mypy src/phaze/config.py` pass.
  </acceptance_criteria>
  <done>kr8s installed as a control-plane dep; all kube_* + cloud_submit_max_attempts fields present, optional, with kube creds as `_FILE`-resolved SecretStr; config tests green; docs updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator secret store → control plane | kube credentials (kubeconfig / SA token) enter via `<VAR>_FILE` mounts |
| package registry → build | `uv add kr8s` pulls a third-party wheel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-SC | Tampering | `uv add kr8s` install | mitigate | Task 1 blocking-human legitimacy gate (kr8s is `[ASSUMED]`); verify on pypi.org/github before install; never auto-approve |
| T-54-01 | Information disclosure | kube creds on ControlSettings | mitigate | `kube_kubeconfig`/`kube_sa_token` are SecretStr in SECRET_FILE_FIELDS; resolved from `_FILE` mounts, never request bodies, never logged |
| T-54-02 | Tampering | config field bounds | mitigate | `cloud_submit_max_attempts` bounded gt=0/lt=20 so a misconfig cannot create an unbounded retry storm |
</threat_model>

<verification>
- `uv run pytest tests/test_config/test_kube_settings.py -x` green.
- `uv run python -c "import kr8s"` exits 0.
- `uv run ruff check . && uv run mypy .` clean for touched files.
</verification>

<success_criteria>
kr8s is a legitimacy-gated control-plane dependency; ControlSettings exposes `cloud_submit_max_attempts`
(D-08) plus the optional kube client surface with `_FILE`-resolved SecretStr credentials; no Phase-55
fail-fast coupling introduced; docs/configuration.md reflects the new fields.
</success_criteria>

<output>
Create `.planning/phases/54-kube-submit-watch-reconcile-cron/54-01-SUMMARY.md` when done.
</output>
