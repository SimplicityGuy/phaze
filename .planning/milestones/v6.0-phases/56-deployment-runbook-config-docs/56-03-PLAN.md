---
phase: 56-deployment-runbook-config-docs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/templates/admin/agents.html
  - tests/test_services/test_agent_liveness.py
  - tests/test_task_split.py
autonomous: true
requirements: [KDEPLOY-04]
must_haves:
  truths:
    - "D-07: the Agents page carries a static informational note explaining the k8s burst lane is ephemeral / Job-based and does not register as a heartbeating agent"
    - "D-07: the k8s lane can never render a perpetually-DEAD pill — classify() returns 'never' (not 'dead') whenever last_seen_at is None, proven as an executable invariant"
    - "The ephemeral pod (job_runner) never imports or calls the heartbeat loop, so no Agent row for the k8s lane is ever heartbeated"
    - "The note copy is static through Jinja autoescape and uses a neutral (not amber/red) info treatment"
  artifacts:
    - path: "src/phaze/templates/admin/agents.html"
      provides: "static ephemeral k8s-lane note after the intro paragraph"
      contains: "ephemeral"
    - path: "tests/test_services/test_agent_liveness.py"
      provides: "never-not-dead invariant assertion"
      contains: "dead"
    - path: "tests/test_task_split.py"
      provides: "job_runner does not import the heartbeat loop"
      contains: "heartbeat"
  key_links:
    - from: "src/phaze/templates/admin/agents.html"
      to: "static info note"
      via: "rendered after intro <p>, before agents_table include"
      pattern: "ephemeral"
    - from: "tests/test_services/test_agent_liveness.py"
      to: "phaze.services.agent_liveness.classify"
      via: "assert classify(agent, now) != 'dead' when last_seen_at is None"
      pattern: "classify"
---

<objective>
Deliver the minimal v6.0 ephemeral-identity treatment (D-07): a static informational note on the Agents page, plus executable proof of the structural DEAD-suppression invariant (the k8s one-shot pod never heartbeats, so its bearer-token Agent row classifies 'never', never 'dead'). The full synthetic "k8s burst" card is explicitly deferred to v7.0 RECORD-03.

Purpose: Tell the operator why a non-heartbeating k8s lane exists and where to watch its live activity (the pipeline dashboard Kueue workloads), and guarantee — by test — that the lane can never show as a false DEAD agent.
Output: a static note in admin/agents.html; an extended never-not-dead invariant test; an extended import-boundary test asserting job_runner has no heartbeat loop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/56-deployment-runbook-config-docs/56-CONTEXT.md
@.planning/phases/56-deployment-runbook-config-docs/56-PATTERNS.md
@.planning/phases/56-deployment-runbook-config-docs/56-UI-SPEC.md

<interfaces>
<!-- Existing structure to extend; the DEAD-suppression is a verification target, not a build. -->

src/phaze/services/agent_liveness.py:
  classify(agent, now) -> AgentStatus  (:68)
  if agent.last_seen_at is None: return "never"  (:79-80)   <-- precedence ahead of the dead branch
  dead requires last_seen_at set AND delta >= 300s  (:81-86)

src/phaze/templates/admin/agents.html:
  <h1> "Agents" (:8); intro <p class="text-sm text-gray-500 dark:text-gray-400">Live status ...</p> (:9-11);
  {% include "admin/partials/agents_table.html" %} (:12)

job_runner: src/phaze/job_runner.py — one-shot (presign -> download -> analyze -> callback -> sys.exit); has NO heartbeat loop.
The heartbeat loop lives only in the long-lived SAQ agent worker: src/phaze/tasks/agent_worker.py:67 imports _heartbeat_loop.

Existing tests to extend:
  tests/test_services/test_agent_liveness.py::test_classify_never_when_last_seen_at_is_none (:72)
  tests/test_task_split.py — import-boundary style (e.g. test_job_runner_does_not_import_phaze_database, test_reconcile_cloud_jobs_is_control_only_not_in_agent_worker)

Locked copy (56-UI-SPEC §Copywriting — do NOT alter):
  (optional inline heading) K8s burst lane
  body: The Kubernetes burst lane runs as ephemeral, per-file Jobs — it does not register as a heartbeating agent here. Its live activity is visible as in-flight Kueue workloads on the pipeline dashboard.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: static ephemeral k8s-lane note on the Agents page</name>
  <read_first>
    - src/phaze/templates/admin/agents.html:8-12 (intro paragraph + agents_table include — placement + tone)
    - src/phaze/templates/admin/partials/agents_table.html (the neutral-panel idiom: border … rounded-lg p-4, text-sm text-gray-*)
    - .planning/phases/56-deployment-runbook-config-docs/56-UI-SPEC.md (§Copywriting, §Color neutral note, §Interaction Contract fully static)
    - .planning/phases/56-deployment-runbook-config-docs/56-PATTERNS.md (agents.html note section)
  </read_first>
  <action>In src/phaze/templates/admin/agents.html, after the intro <p> (:11) and before the agents_table include (:12), add a static neutral info panel: a <section> (or <div>) with classes "border border-gray-200 dark:border-phaze-border rounded-lg p-4", body text "text-sm text-gray-600 dark:text-gray-400". Optionally lead with an ℹ glyph in "text-blue-600 dark:text-blue-400" marked aria-hidden="true", and an optional inline heading "K8s burst lane". Body is the exact locked copy. Keep it fully static — NO hx-trigger, NO Alpine, NO poll, NO data binding; it must not participate in the agents table 5s self-refresh. Use neutral (gray) treatment — deliberately NOT amber/red — so it reads as steady-state info. All strings static through autoescape.</action>
  <verify>
    <automated>uv run pytest tests/test_services/test_agent_liveness.py -k never -x && grep -q "runs as ephemeral, per-file Jobs" src/phaze/templates/admin/agents.html</automated>
  </verify>
  <acceptance_criteria>
    - src/phaze/templates/admin/agents.html contains the locked body string "runs as ephemeral, per-file Jobs"
    - The note uses neutral gray panel classes (not amber/red) and is placed after the intro <p>, before the agents_table include
    - No hx-* attributes, no x-data/Alpine, no poll on the note element
  </acceptance_criteria>
  <done>The static ephemeral-lane note renders once on the Agents page with the locked copy and neutral treatment.</done>
</task>

<task type="auto">
  <name>Task 2: prove the DEAD-suppression invariant (never != dead; job_runner has no heartbeat)</name>
  <read_first>
    - src/phaze/services/agent_liveness.py:68-86 (classify precedence: never before dead)
    - tests/test_services/test_agent_liveness.py:72 (test_classify_never_when_last_seen_at_is_none — extend it)
    - tests/test_task_split.py (existing import-boundary tests, e.g. the database/agent-worker boundary cases — copy the introspection style)
    - src/phaze/job_runner.py (confirm no _heartbeat_loop import/call) and src/phaze/tasks/agent_worker.py:67 (where _heartbeat_loop actually lives)
    - .planning/phases/56-deployment-runbook-config-docs/56-RESEARCH.md (§Net-New Code Design item 2 — the structural proof)
  </read_first>
  <action>Extend tests/test_services/test_agent_liveness.py with an invariant test (e.g. test_classify_never_not_dead_when_last_seen_at_none) that asserts classify(agent, now) != "dead" for an agent with last_seen_at is None across several now values, including a now far in the future (parametrize) — the structural DEAD-suppression proof. Extend tests/test_task_split.py with an import-boundary test (e.g. test_job_runner_does_not_run_heartbeat_loop) asserting the phaze.job_runner module never imports or calls _heartbeat_loop (mirror the existing import-boundary introspection/subprocess style — inspect the module source or its imported names; do NOT actually start a worker). Both tests assert already-true invariants and should pass immediately (regression guards for D-07).</action>
  <verify>
    <automated>uv run pytest tests/test_services/test_agent_liveness.py tests/test_task_split.py -k "never or heartbeat" -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_services/test_agent_liveness.py asserts classify(...) != "dead" when last_seen_at is None (including a far-future now)
    - tests/test_task_split.py asserts phaze.job_runner does not import/call _heartbeat_loop
    - `uv run pytest tests/test_services/test_agent_liveness.py tests/test_task_split.py -k "never or heartbeat" -x` exits 0
  </acceptance_criteria>
  <done>The never-not-dead invariant and the no-heartbeat-in-job_runner invariant are locked as passing regression guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| k8s pod → callback auth | The pod authenticates with a bearer token; its Agent row exists but must never go DEAD |
| static template strings → browser | Jinja autoescape; no operator free-text in the note |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-DEAD | Spoofing/Integrity | Agents UI liveness | mitigate | invariant test proves classify() returns 'never' (never 'dead') when last_seen_at is None; job_runner never heartbeats — no false DEAD/false-healthy agent |
| T-56-XSS | Tampering | agents.html note | mitigate | static copy through Jinja autoescape; no operator string interpolated (T-54-11) |
| T-56-FILTER | Availability | agents table | accept | do NOT add a kind=compute filter (would hide the v5.0 A1 agent which SHOULD show); accept the 'never' pill + note (56-RESEARCH Pitfall 4) |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | zero new external packages |
</threat_model>

<verification>
- `uv run pytest tests/test_services/test_agent_liveness.py tests/test_task_split.py -x` green.
- Manual: GET /admin/agents (or the agents route) shows the neutral note after the intro and before the table; no k8s row ever shows a DEAD pill.
- Do NOT touch _heartbeat_loop or add a compute-agent filter (56-RESEARCH Pitfall 4).
- `pre-commit run --all-files` passes.
</verification>

<success_criteria>
- Static ephemeral note present with locked copy + neutral treatment.
- never-not-dead and no-heartbeat invariants pass.
</success_criteria>

<output>
Create `.planning/phases/56-deployment-runbook-config-docs/56-03-SUMMARY.md` when done.
</output>
