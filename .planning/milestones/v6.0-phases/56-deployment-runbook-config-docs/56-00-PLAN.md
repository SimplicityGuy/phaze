---
phase: 56-deployment-runbook-config-docs
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/kube_fakes.py
  - tests/test_services/test_kube_staging.py
  - tests/test_tasks/test_controller_startup_localqueue.py
  - tests/test_routers/test_pipeline_localqueue.py
  - tests/test_deployment/test_k8s_runbook.py
autonomous: true
requirements: [KDEPLOY-01, KDEPLOY-04]
must_haves:
  truths:
    - "A fake_local_queue test helper + a 404/transient seam exist so get_local_queue is unit-testable without a live cluster"
    - "RED tests fully specify the get_local_queue contract (success / NotFoundError / transient) before any code is written"
    - "RED tests fully specify the controller startup probe contract (gated on cloud_target=='k8s', non-fatal boot-resilience) per D-05/D-06"
    - "RED tests fully specify the dashboard LocalQueue-unreachable alert (degrade-safe read -> False, empty when reachable, OOB carrier) per D-05"
    - "A runbook test parses docs/k8s-burst.md YAML manifests and asserts the RBAC verb set covers the kr8s call graph (jobs create/get/delete; workloads list/get/watch; localqueues get) per D-01"
  artifacts:
    - path: "tests/kube_fakes.py"
      provides: "fake_local_queue(...) SimpleNamespace factory mirroring fake_job"
      contains: "def fake_local_queue"
    - path: "tests/test_services/test_kube_staging.py"
      provides: "get_local_queue success/not-found/transient cases"
      contains: "test_get_local_queue"
    - path: "tests/test_tasks/test_controller_startup_localqueue.py"
      provides: "probe gating + boot-resilience tests"
      contains: "localqueue_probe"
    - path: "tests/test_routers/test_pipeline_localqueue.py"
      provides: "alert OOB + degrade-safe read tests"
      contains: "localqueue"
    - path: "tests/test_deployment/test_k8s_runbook.py"
      provides: "runbook YAML-validity + RBAC-covers-call-graph assertions"
      contains: "test_rbac_covers_call_graph"
  key_links:
    - from: "tests/test_services/test_kube_staging.py"
      to: "phaze.services.kube_staging.get_local_queue"
      via: "import + kube_respx mock"
      pattern: "get_local_queue"
    - from: "tests/test_deployment/test_k8s_runbook.py"
      to: "docs/k8s-burst.md"
      via: "YAML fenced-block parse"
      pattern: "k8s-burst.md"
---

<objective>
Create the Wave 0 test scaffolding (Nyquist RED-first) for the Phase 56 net-new code (KDEPLOY-04 LocalQueue probe + dashboard alert) and the runbook (KDEPLOY-01). These tests define the contracts that the implementation plans (56-01, 56-02) and the runbook plan (56-04) must satisfy.

Purpose: Lock every net-new-code contract as an executable test before implementation, so executors implement against a fixed target instead of exploring. This satisfies 56-VALIDATION.md's Wave 0 requirements.
Output: 5 new/extended test files. Code-dependent tests are RED until their implementation plan lands; the runbook test is RED until 56-04 writes docs/k8s-burst.md. This is the expected Wave 0 state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/56-deployment-runbook-config-docs/56-CONTEXT.md
@.planning/phases/56-deployment-runbook-config-docs/56-PATTERNS.md
@.planning/phases/56-deployment-runbook-config-docs/56-RESEARCH.md
@.planning/phases/56-deployment-runbook-config-docs/56-VALIDATION.md

<interfaces>
<!-- Contracts these RED tests pin. Executor uses these directly; no exploration needed. -->

The function under test (NOT YET WRITTEN — defined by 56-01):
  phaze.services.kube_staging.get_local_queue() -> Any
    - GETs the configured Kueue LocalQueue by name; refresh() raises kr8s.NotFoundError on 404,
      other exceptions on transient kube-API/mesh failure. Caller treats BOTH as "unreachable".

The dashboard read service (NOT YET WRITTEN — defined by 56-01):
  phaze.services.pipeline.get_localqueue_unreachable(redis: Any) -> bool
    - True iff the controller flagged the LocalQueue unreachable; any error / redis is None -> False.
    - Redis key: "phaze:k8s:localqueue_unreachable" (exists -> True).

The probe (NOT YET WRITTEN — defined by 56-01) lives in phaze.tasks.controller.startup, gated on
  cfg.cloud_target == "k8s", writes the Redis key via ctx["redis"] (.set on failure, .delete on success),
  wrapped in a broad try/except that NEVER re-raises.

Existing analogs to clone (read them):
  - tests/kube_fakes.py:21-50 (fake_workload / fake_job SimpleNamespace factories)
  - tests/test_services/test_kube_staging.py: test_get_job_returns_status + the kube_respx fixture
    (tests/conftest.py: KUBE_TEST_API_URL="https://kube.test", _StubCfg / stub_cfg fixture carrying
    kube_workload_api_version="kueue.x-k8s.io/v1beta1")
  - tests/test_tasks/test_controller_startup_banner.py (monkeypatch recipe for controller.startup)
  - tests/test_routers/test_pipeline_inadmissible.py (dashboard alert render + OOB recipe)
  - tests/test_deployment/test_job_image.py (deployment-test file shape)

RBAC verb floor the runbook must cover (from 56-RESEARCH §Runbook RBAC, derived from the kr8s call graph):
  batch/jobs: create, get, delete   (submit_job / get_job / delete_job)
  kueue.x-k8s.io/workloads: list    (get_workload_for) + get, watch (conservative D-01 spec)
  kueue.x-k8s.io/localqueues: get   (NEW — the Phase 56 startup probe)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: fake_local_queue helper + get_local_queue RED unit tests</name>
  <read_first>
    - tests/kube_fakes.py (clone fake_job/fake_workload at :21-50)
    - tests/test_services/test_kube_staging.py (clone test_get_job_returns_status + reuse kube_respx)
    - tests/conftest.py (the kube_respx fixture, KUBE_TEST_API_URL, _StubCfg/stub_cfg)
    - .planning/phases/56-deployment-runbook-config-docs/56-PATTERNS.md (kube_fakes + test_kube_staging sections)
    - src/phaze/services/kube_staging.py:197-229 (get_job idiom + new_class Kueue-group idiom — the contract being pinned)
  </read_first>
  <action>Add fake_local_queue(name="phaze-lq", namespace="phaze") to tests/kube_fakes.py returning a SimpleNamespace with metadata=SimpleNamespace(name=..., namespace=...), mirroring fake_job. In tests/test_services/test_kube_staging.py add three cases against the soon-to-exist phaze.services.kube_staging.get_local_queue: test_get_local_queue_success (mock GET on /apis/kueue.x-k8s.io/v1beta1/namespaces/{ns}/localqueues/{name} returns 200 -> returns the object), test_get_local_queue_not_found (404 -> raises kr8s.NotFoundError), test_get_local_queue_transient (500 -> raises). Drive the GET via the shared kube_respx fixture and stub_cfg (which already carries kube_workload_api_version="kueue.x-k8s.io/v1beta1" and kube_local_queue/kube_namespace). These tests are RED until 56-01 writes get_local_queue — that is the expected Wave 0 state.</action>
  <verify>
    <automated>uv run pytest tests/test_services/test_kube_staging.py -k local_queue --co -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/kube_fakes.py contains "def fake_local_queue"
    - tests/test_services/test_kube_staging.py contains "test_get_local_queue_success", "test_get_local_queue_not_found", "test_get_local_queue_transient"
    - The three tests reference phaze.services.kube_staging.get_local_queue and the localqueues GET path
    - `uv run pytest tests/test_services/test_kube_staging.py -k local_queue --co -q` lists the three test ids (collection succeeds; the tests themselves are RED/erroring until 56-01)
  </acceptance_criteria>
  <done>fake_local_queue helper exists; three get_local_queue RED cases collected and asserting the success/NotFoundError/transient contract.</done>
</task>

<task type="auto">
  <name>Task 2: controller-startup probe + dashboard-alert RED tests</name>
  <read_first>
    - tests/test_tasks/test_controller_startup_banner.py (whole file — the monkeypatch recipe for controller.startup)
    - tests/test_routers/test_pipeline_inadmissible.py (whole file — alert render empty-when-healthy / banner-when-flagged / OOB on /pipeline/stats)
    - src/phaze/tasks/controller.py:51-157 (startup hook, ctx["redis"] at :104, boot-resilience try/except at :145-157)
    - .planning/phases/56-deployment-runbook-config-docs/56-PATTERNS.md (controller-startup + pipeline-router sections)
    - .planning/phases/56-deployment-runbook-config-docs/56-CONTEXT.md (D-05, D-06)
  </read_first>
  <action>Create tests/test_tasks/test_controller_startup_localqueue.py with: test_localqueue_probe_skipped_when_not_k8s (fake_cfg.cloud_target != "k8s" => kube_staging.get_local_queue never called), test_localqueue_probe_sets_flag_on_failure (patch kube_staging.get_local_queue to raise => ctx["redis"].set("phaze:k8s:localqueue_unreachable", ...) called AND controller.startup returns without raising), test_localqueue_probe_clears_flag_on_success (success => ctx["redis"].delete("phaze:k8s:localqueue_unreachable") called). Reuse the banner test's monkeypatch recipe to stub create_async_engine/async_sessionmaker/get_settings and provide ctx["redis"] as an AsyncMock. Create tests/test_routers/test_pipeline_localqueue.py with: test_localqueue_alert_empty_when_reachable (Redis key absent OR app.state.redis missing => section renders with no amber body), test_localqueue_alert_renders_when_flagged (key set => locked amber copy "K8s LocalQueue unreachable" appears), test_localqueue_alert_oob_on_stats (stable section id on both first-load GET /pipeline and the 5s OOB GET /pipeline/stats), and test_get_localqueue_unreachable_degrades_to_false (call phaze.services.pipeline.get_localqueue_unreachable(None) -> False; a raising redis -> False). All RED until 56-01/56-02 land.</action>
  <verify>
    <automated>uv run pytest tests/test_tasks/test_controller_startup_localqueue.py tests/test_routers/test_pipeline_localqueue.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_tasks/test_controller_startup_localqueue.py contains "test_localqueue_probe_skipped_when_not_k8s", "test_localqueue_probe_sets_flag_on_failure", "test_localqueue_probe_clears_flag_on_success"
    - Failure-path test asserts startup returns without raising (boot-resilience) and that the Redis key "phaze:k8s:localqueue_unreachable" is written
    - tests/test_routers/test_pipeline_localqueue.py asserts the locked copy "K8s LocalQueue unreachable" and a degrade-safe get_localqueue_unreachable returning False on None/error
    - Both files collect without import errors (test bodies RED until implementation)
  </acceptance_criteria>
  <done>Probe-gating + boot-resilience tests and the degrade-safe dashboard-alert tests are collected and pin the D-05/D-06 contracts.</done>
</task>

<task type="auto">
  <name>Task 3: runbook YAML-validity + RBAC-covers-call-graph test</name>
  <read_first>
    - tests/test_deployment/test_job_image.py (deployment-test file shape)
    - .planning/phases/56-deployment-runbook-config-docs/56-RESEARCH.md (§Runbook Content — the exact v1beta1 manifests + the RBAC verb floor)
    - .planning/phases/56-deployment-runbook-config-docs/56-PATTERNS.md (test_k8s_runbook section)
    - .planning/phases/56-deployment-runbook-config-docs/56-CONTEXT.md (D-01)
  </read_first>
  <action>Create tests/test_deployment/test_k8s_runbook.py that reads docs/k8s-burst.md, extracts every fenced yaml block (triple-backtick yaml fences), and parses each with yaml.safe_load_all (skip non-manifest snippets gracefully). Assert: (1) test_runbook_manifests_are_valid_yaml — every yaml block parses without error; (2) test_runbook_has_required_kinds — the parsed docs include kinds ResourceFlavor, ClusterQueue, LocalQueue, ServiceAccount, Role, RoleBinding, Secret; (3) test_rbac_covers_call_graph — the Role's rules cover the documented kr8s call-graph floor: batch/jobs {create,get,delete} subset, kueue.x-k8s.io/workloads includes at least "list" (and may include get/watch), kueue.x-k8s.io/localqueues includes "get". Define the required floor as a module-level constant (REQUIRED_RBAC mapping resource->required-verb-set) and assert runbook_verbs ⊇ required. Use `from pathlib import Path` (PTH) and `yaml` (already a dev/test dep via pre-commit/pyyaml — confirm import; if pyyaml is not importable in tests, add it under the dev dependency group with `uv add --dev pyyaml` and note it). This test is RED until 56-04 writes docs/k8s-burst.md.</action>
  <verify>
    <automated>uv run pytest tests/test_deployment/test_k8s_runbook.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_deployment/test_k8s_runbook.py contains "test_runbook_manifests_are_valid_yaml", "test_runbook_has_required_kinds", "test_rbac_covers_call_graph"
    - A module-level REQUIRED_RBAC constant encodes jobs:{create,get,delete}, workloads superset-of {list}, localqueues:{get}
    - The test reads docs/k8s-burst.md via pathlib and parses yaml fenced blocks
    - File collects without import error (assertions RED until 56-04 creates the runbook)
  </acceptance_criteria>
  <done>Runbook test pins the D-01 manifest set + the exact RBAC verb floor; coupled to 56-04 which makes it green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| control-plane → kube API | The probe + submit/reconcile cross this; the RBAC floor pinned here bounds the grant |
| operator → runbook YAML | Operator-applied manifests; the test guards the least-privilege floor against drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-RBAC | Elevation of Privilege | runbook RBAC Role verbs | mitigate | test_rbac_covers_call_graph asserts the Role is the exact namespaced floor (no cluster-wide / extra verbs slip in undetected) |
| T-56-DOS | Denial of Service | controller startup probe | mitigate | RED tests assert the probe is gated + non-fatal (startup returns on failure) before code exists |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | Zero new external packages (56-RESEARCH Package Legitimacy Audit: N/A). pyyaml, if needed for the test, is a dev-group test dep only |
</threat_model>

<verification>
- `uv run pytest tests/test_services/test_kube_staging.py tests/test_tasks/test_controller_startup_localqueue.py tests/test_routers/test_pipeline_localqueue.py tests/test_deployment/test_k8s_runbook.py --co -q` collects all new tests without import errors.
- `pre-commit run --all-files` passes (ruff/mypy on test files; tests dir is excluded from strict mypy).
- Expected RED bodies: get_local_queue cases (until 56-01), controller probe (until 56-01), dashboard alert + render (until 56-01/56-02), runbook content (until 56-04). This is the intended Wave 0 state.
</verification>

<success_criteria>
- All five test files exist and collect.
- Each net-new-code contract from 56-VALIDATION.md is encoded as an executable assertion.
- The runbook RBAC floor is machine-checkable.
</success_criteria>

<output>
Create `.planning/phases/56-deployment-runbook-config-docs/56-00-SUMMARY.md` when done.
</output>
