---
milestone: v7.0
name: UI Redesign (DAG-Centric Hybrid Console)
audited: 2026-07-02T16:09:37Z
status: passed
scores:
  requirements: 28/28
  phases: 7/7
  integration: wired (0 broken flows, 0 orphaned HX targets)
  flows: 1/1 (full empty-state→scan→…→record chain traced)
gaps: {}   # no critical blockers
nyquist:
  compliant_phases: [57, 57.1, 58, 59, 60, 61, 62]
  partial_phases: []
  missing_phases: []
  overall: compliant
tech_debt:
  - phase: 62-polish-cutover
    items:
      - "Vestigial dead code: shell.py:71 `_STAGE_PLACEHOLDER` variable + templates/shell/partials/_stage_placeholder.html are unreferenced at runtime (every stage resolves to a real workspace). The dead-template guard's literal-capture regex masks them as 'reachable' via the unused assignment. Delete both as a CUT-02 follow-up cleanup. NOT a blocker."
      - "Doc-currency (reconciled during this audit): 62-02-SUMMARY.md frontmatter omitted `requirements-completed: [CUT-04]`; REQUIREMENTS.md traceability table Status column still read 'Planned' for phases 59-62."
  - phase: 62-polish-cutover / cross-cutting
    items:
      - "/saq SAQ-monitor link: the /saq app stays mounted (main.py:169, direct-URL reachable) but has no in-UI link in the v7.0 shell (was in the deleted dashboard.html). Documented deferral (62-04-SUMMARY). Never a v7.0 REQ-ID. Backlog for whichever phase next touches the Agents/RECORD-03 surface."
  - phase: cross-milestone
    items:
      - "WR-02 fingerprint.done-always-0: services/pipeline.py:371 counts fingerprint.done on status=='completed' but writers persist 'success' (per its own _trackid_engine_badge doc) — always 0. Being fixed as its own PR off main (out of v7.0 no-backend scope)."
---

# v7.0 Milestone Audit — UI Redesign (DAG-Centric Hybrid Console)

**Verdict: PASSED.** All 28 requirements satisfied, all 7 phases verified, all Nyquist-compliant, cross-phase integration clean (0 broken flows, 0 orphaned HTMX targets), full E2E flow traced. Three non-blocking tech-debt items tracked below.

## Scope

7 phases, dependency-strict 57→62: **57** Shell & DAG rail · **57.1** Incremental window persistence (the one scoped backend exception) · **58** Enrich + Analyze workspaces · **59** Identify workspaces · **60** Review & Apply · **61** Full record + ⌘K + Agents · **62** Polish & cutover.

Milestone definition of done (from ROADMAP/REQUIREMENTS): rewrite the tabbed UI into a DAG-centric three-column console (`/` = home + nav spine, no `/pipeline` URL), reusing existing routers/services with **no backend behavior change** except Phase 57.1. 28 requirements across SHELL / PROG / WORK / IDENT / REVIEW / RECORD / CUT groups.

## Requirements Coverage — 28/28 (3-source cross-reference)

Every REQ-ID cross-checked across phase VERIFICATION.md (status), SUMMARY.md `requirements-completed` frontmatter, and the REQUIREMENTS.md traceability checklist. Integration checker independently traced each to a live cross-phase connection.

| Group | REQ-IDs | Phase | VERIFICATION | Result |
|-------|---------|-------|--------------|--------|
| SHELL | 01–05 | 57 | passed | satisfied |
| PROG  | 01–03 | 57.1 | passed | satisfied |
| WORK  | 01–05 | 58 | passed | satisfied |
| IDENT | 01–02 | 59 | passed | satisfied |
| REVIEW| 01–05 | 60 | passed | satisfied |
| RECORD| 01–04 | 61 | passed | satisfied |
| CUT   | 01–04 | 62 | passed | satisfied |

**Orphans:** none. **Unsatisfied:** none. One reconciled bookkeeping note: CUT-04's SUMMARY frontmatter tag was missing (fixed during audit); CUT-04 itself is verified (live UAT rail-collapse PASS + `test_rail_narrow_width.py` green + 62-VERIFICATION passed).

**Deliberately deferred (out of v7.0 scope, not counted):** RECORD-05 (light-theme C3), SHELL-06 (touch/tablet), REVIEW-06 (configurable thresholds), WORK-06 (cloud_phase admission cards), IDENT-03 (AcoustID/MusicBrainz backend).

## Phase Verification — 7/7 passed

All phases carry `VERIFICATION.md status: passed` with empty `gaps_remaining`. 57.1 and 58 flipped `human_needed→passed` after live Playwright UAT; 62 flipped `gaps_found→passed`. No unverified phase.

## Nyquist Compliance — compliant

All 7 `*-VALIDATION.md` carry `nyquist_compliant: true` + `wave_0_complete: true` (status `validated`/`verified`). No phase needs `/gsd:validate-phase`.

## Cross-Phase Integration — wired, 0 blockers

- **Stage routing (SHELL-01/02):** all 12 `STAGE_PARTIALS` keys (discover/metadata/fingerprint/analyze/trackid/tracklist/propose/rename/tagwrite/move/dedupe/cue) resolve to workspace partials that exist on disk; `rail.html` node ids match 1:1. (+2 plain links: Audit log, Compute/agents.) `test_shell_routes.py` 6/6.
- **Cutover (CUT-02):** dead-template guard green, `_ALLOWLIST=frozenset()`; no surviving include/extends/TemplateResponse references a deleted template; 20 legacy templates confirmed absent.
- **Redirects (SHELL-05):** 8/8 legacy routes 302 into the shell (`/pipeline`→`/`, `/proposals`→`/s/propose`, `/tracklists`→`/s/tracklist`, `/tags`→`/s/tagwrite`, `/cue`→`/s/cue`, `/duplicates`→`/s/dedupe`, `/search`→`/?palette=1`, `/preview`→`/s/move`). Dual-purpose HX branches still render live fragments.
- **Single-poll OOB (WORK-05):** exactly one `/pipeline/stats` poll in shell chrome; all 33 `dag-seed-*` + 5 legacy + 6 cloud-card OOB targets have pre-mounted hidden sinks via `_workspace_poll_seeds.html` (included by every workspace + empty_state). No orphan-OOB target.
- **PROG-03 discriminator:** writer stamps `analysis_completed_at` only on the completion branch; the counter-only progress endpoint leaves it NULL; reader `get_proposal_pending_batches` gates on `IS NOT NULL` — in-flight rows cannot leak into Propose.
- **E2E flow:** empty-state → scan → discover → enrich → analyze → identify → propose → review&apply → record — every hop resolves to a live route with a real DB-backed context builder. 0 broken.

## Tech Debt (non-blocking)

1. **Vestigial `_STAGE_PLACEHOLDER`** (`shell.py:71` + `_stage_placeholder.html`) — dead variable/template, unreferenced at runtime, masked from the CUT-02 guard by its literal-capture regex. Recommend deleting both as a small follow-up (mildly ironic for a "remove dead code" phase).
2. **`/saq` in-UI link** — mounted + direct-URL reachable but unlinked in the shell. Documented deferral; backlog for the next Agents-page touch (RECORD-03 is the natural home).
3. **WR-02 fingerprint.done-always-0** — `services/pipeline.py:371` counts on `status=='completed'` (never written; writers use `'success'`). Being fixed as its own PR off main.

## Next Step

Milestone is clean. Proceed to `/gsd:complete-milestone v7.0` (archive + tag). Tech-debt items are tracked here for backlog/follow-up — none block completion.
