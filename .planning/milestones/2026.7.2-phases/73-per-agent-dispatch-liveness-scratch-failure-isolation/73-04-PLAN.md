---
phase: 73-per-agent-dispatch-liveness-scratch-failure-isolation
plan: 04
type: execute
wave: 3
depends_on: ["73-01", "73-02", "73-03"]
files_modified:
  - src/phaze/config.py
  - tests/analyze/services/test_backends.py
  - tests/analyze/tasks/test_release_awaiting_cloud.py
  - tests/analyze/services/test_backend_selection.py
  - tests/analyze/services/test_compute_binding_golden.py
  - tests/shared/config/test_bucket_registry.py
autonomous: true
requirements: [MCOMP-02, MCOMP-03, MCOMP-04, MCOMP-05]

must_haves:
  truths:
    - "N compute backends: an offline bound agent makes only THAT backend unavailable; the drain snapshot leaves healthy compute lanes eligible (MCOMP-02)"
    - "The tiered drain spreads long files across N compute backends by rank then per-agent cap, spilling to the next-eligible backend when one is full/offline (MCOMP-04)"
    - "One flaky compute backend (is_available raises/times out) degrades to 0 slots and the drain tick COMPLETES — healthy lanes still dispatch (MCOMP-05)"
    - "D-05: attribution stays one-row-per-file keyed by backend_id (unique(file_id) preserved) — the regressions run against the existing schema with no migration/schema change"
    - "The ≤1-compute deploy pushes byte-identically once the payload carries the single destination: remote_dest and /pushed scratch_path strings are unchanged (behavior-preservation)"
    - "active_compute_scratch_dir is deleted (its last runtime reader was retired in Plan 03) with no remaining runtime reader"
  artifacts:
    - path: "tests/analyze/tasks/test_release_awaiting_cloud.py"
      provides: "N-compute one-flaky isolation drain regression (MCOMP-05)"
      contains: "flaky"
    - path: "tests/analyze/services/test_backend_selection.py"
      provides: "N-compute rank/cap load-spread regression (MCOMP-04)"
      contains: "compute"
    - path: "src/phaze/config.py"
      provides: "active_compute_scratch_dir property removed"
  key_links:
    - from: "tests/analyze/services/test_compute_binding_golden.py"
      to: "single-compute remote_dest + /pushed scratch_path"
      via: "byte-identical golden assertion"
      pattern: "byte.identical|scratch"
---

<objective>
Prove the machinery Phase 73 REUSES (D-08: no new scheduler policy) with regression tests, retire the now-dead `active_compute_scratch_dir` accessor, and lock the ≤1-compute behavior-preservation golden. MCOMP-04 (rank/cap spread) and MCOMP-05 (one-flaky isolation) machinery already lives in the Phase-69 `select_backend` + the Phase-70 per-backend snapshot try/except — this plan adds regressions ONLY. MCOMP-02 per-agent liveness is delivered by Phase 72's bound-agent `is_available` — this plan adds an N-compute regression. All regressions run against the existing one-row-per-file cloud_job schema (D-05: no migration, no schema change). Then delete the transitional global and prove the single-compute path is byte-identical.

Purpose: Close the nyquist validation map (every MCOMP-02..06 behavior → an automated regression) and finish the transitional-global retirement.

Output: N-compute liveness/spread/isolation regressions, the deleted accessor, the behavior-preservation golden, and a documented `reenqueue.py:374` known-limitation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-per-agent-dispatch-liveness-scratch-failure-isolation/73-CONTEXT.md
@.planning/phases/73-per-agent-dispatch-liveness-scratch-failure-isolation/73-RESEARCH.md
@.planning/phases/73-per-agent-dispatch-liveness-scratch-failure-isolation/73-VALIDATION.md
@.planning/phases/73-per-agent-dispatch-liveness-scratch-failure-isolation/73-01-SUMMARY.md
@.planning/phases/73-per-agent-dispatch-liveness-scratch-failure-isolation/73-02-SUMMARY.md
@.planning/phases/73-per-agent-dispatch-liveness-scratch-failure-isolation/73-03-SUMMARY.md

<interfaces>
<!-- The UNCHANGED machinery under test (D-08) + the artifact to delete. -->

Per-backend snapshot isolation (tasks/release_awaiting_cloud.py:151-157) — the MCOMP-05 machinery:
- `try: available = await backend.is_available(session); remaining = max(0, backend.cap - await backend.in_flight_count(session)) except Exception: ... snapshot[backend.id] = {"backend": backend, "available": False, "remaining": 0, "cap": backend.cap}; continue`

Rank/cap selection (services/backends.py + test_backend_selection.py): `_compute(id=..., rank=..., cap=...)` helper (test_backend_selection.py:38) builds pure selection backends; existing analogs `test_rank_first_picks_lowest_rank_available` (L86), `test_spill_when_lowest_rank_full_picks_next_rank` (L95).

Per-agent liveness (services/backends.py ComputeAgentBackend.is_available:265-278): resolves self._agent_ref() via select_agent_by_id; NoActiveAgentError → False. Existing analogs in test_backends.py: test_compute_is_available_true_when_bound_agent_online, test_compute_is_available_reads_bound_ref_not_single_active_pick.

D-05 (one-row-per-file, no schema change): cloud_job.file_id stays `unique=True` (models/cloud_job.py:72); backend_id records the current dispatch target; spill re-upserts the SAME row. The regressions in this plan add NO migration and NO column — they exercise the existing schema.

The accessor to delete (config.py:483-501): `active_compute_scratch_dir` @property. After Plan 03, its only prior runtime reader (agent_push.py:133) reads backend.scratch_dir instead — so the property is dead code. Reader audit (RESEARCH): remaining refs are docstrings/comments/tests only.

Behavior-preservation golden (test_compute_binding_golden.py:64-149): today asserts active_compute_scratch_dir == "/srv/scratch" and scratch_path == f"{active_compute_scratch_dir}/{file_id}.{file_type}". Re-anchor to the new per-backend resolution while keeping the resulting STRING byte-identical for the single-compute registry.

KNOWN LIMITATION (document, do NOT change): reenqueue.py:374 `recover_orphaned_work` still re-drives held files to `select_active_agent(kind="compute")` (single-active). Out of scope (not a dispatch/push/reconcile seam); silently widening it risks the 44.5k over-enqueue incident class (STATE.md). Record as a PROV-01 backlog follow-up.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: N-compute liveness + rank/cap spread + one-flaky isolation regressions (D-08, test-only)</name>
  <files>tests/analyze/services/test_backends.py, tests/analyze/services/test_backend_selection.py, tests/analyze/tasks/test_release_awaiting_cloud.py</files>
  <read_first>
    - tests/analyze/services/test_backends.py (test_compute_is_available_* + backends_toml_env fixture ~L917 — extend to a local + 2-compute registry with distinct scratch_dir/push_host/agent_ref)
    - tests/analyze/services/test_backend_selection.py (_compute helper L38 + test_spill_when_lowest_rank_full_picks_next_rank L95)
    - tests/analyze/tasks/test_release_awaiting_cloud.py (stage_cloud_window snapshot tests — the try/except isolation surface)
    - src/phaze/tasks/release_awaiting_cloud.py (the L151-157 per-backend try/except being proven — do NOT modify it)
  </read_first>
  <action>Add regressions against the UNCHANGED machinery (no source edits; D-05: exercise the existing one-row-per-file schema, add no migration): (MCOMP-02) in test_backends.py, a 2-compute registry where bound agent A is online and B is offline → A's ComputeAgentBackend.is_available True, B's False (per-entry gating, not a single-active pick). (MCOMP-04) in test_backend_selection.py, using the `_compute(id, rank, cap)` helper, assert N compute backends are filled lowest-rank-first up to cap then spill to the next-rank compute lane (free-arm64-lower-rank preferred over paid-x86-higher-rank) — extend the existing spill test to 2+ compute entries. (MCOMP-05) in test_release_awaiting_cloud.py, drive stage_cloud_window with one compute backend whose is_available raises (or times out) → its snapshot entry is {available: False, remaining: 0}, the tick COMPLETES without raising, and a healthy sibling compute lane still receives a dispatch. Cross-reference each new test to its MCOMP id in a comment.</action>
  <verify>
    <automated>uv run pytest tests/analyze/services/test_backends.py tests/analyze/services/test_backend_selection.py tests/analyze/tasks/test_release_awaiting_cloud.py -x -q -k "compute or rank or spread or flaky or isolat or available"</automated>
  </verify>
  <acceptance_criteria>
    - A test asserting bound-agent-A-online / B-offline → only A eligible passes (MCOMP-02).
    - A test asserting N compute backends spread by rank then cap, spilling to the next-rank lane passes (MCOMP-04).
    - A test asserting a raising is_available degrades that lane to 0 slots while stage_cloud_window completes and a healthy lane still dispatches passes (MCOMP-05).
    - No file under src/ is modified by this task (`git diff --name-only src/` shows nothing new); the three test commands exit 0.
  </acceptance_criteria>
  <done>MCOMP-02/04/05 each have an automated regression against the live (unchanged) machinery, all on the existing one-row-per-file schema (D-05).</done>
</task>

<task type="auto">
  <name>Task 2: Delete the active_compute_scratch_dir accessor + migrate its unit tests</name>
  <files>src/phaze/config.py, tests/shared/config/test_bucket_registry.py, tests/analyze/services/test_backends.py, tests/analyze/services/test_compute_binding_golden.py</files>
  <read_first>
    - src/phaze/config.py (active_compute_scratch_dir @property L483-501 + the retired-global comment block L502-510 for the deletion-note style)
    - RESEARCH reader audit (73-RESEARCH.md §"active_compute_scratch_dir reader audit"): confirm the ONLY runtime reader was agent_push.py:133 (retired in Plan 03); all remaining refs are docstrings/comments/tests
    - tests/analyze/services/test_backends.py (test_active_compute_scratch_dir_resolves_under_local_2kueue_1compute ~L917) and tests/shared/config/test_bucket_registry.py (the property's ≤1/N reduction tests ~L287-348)
  </read_first>
  <action>First re-confirm no runtime reader remains: `grep -rn "active_compute_scratch_dir" src/` should show ONLY docstrings/comments (no `settings.active_compute_scratch_dir`/`self.active_compute_scratch_dir` expression). Then delete the `active_compute_scratch_dir` @property from config.py and replace it with a one-line "RETIRED (Phase 73, MCOMP-03): scratch is resolved per-file from the recorded cloud_job.backend_id via resolve_compute_backend" comment, mirroring the adjacent Phase-70 active_kube/active_bucket retirement notes. Remove or re-purpose the property's now-obsolete unit tests: the reduction/≤1/N tests in test_bucket_registry.py and test_active_compute_scratch_dir_resolves_under_local_2kueue_1compute in test_backends.py — where they asserted the accessor value, either delete them or convert to assert the equivalent resolve_compute_backend(cfg, backend_id).scratch_dir. Update any docstring references in test_compute_binding_golden.py that name the accessor. Do NOT touch AgentSettings.cloud_scratch_dir (the compute janitor field, Landmine 2).</action>
  <verify>
    <automated>uv run pytest tests/shared/config/test_bucket_registry.py tests/analyze/services/test_backends.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "def active_compute_scratch_dir" src/phaze/config.py` returns nothing.
    - `grep -rn "active_compute_scratch_dir" src/` returns only comment lines (no attribute-access expression).
    - `grep -rn "cloud_scratch_dir" src/phaze/config.py` still shows the AgentSettings field (Landmine 2 intact).
    - `uv run pytest tests/shared/config/test_bucket_registry.py tests/analyze/services/test_backends.py -x -q` exits 0; `uv run mypy src/phaze/config.py` exits 0.
  </acceptance_criteria>
  <done>The transitional global accessor is gone with no runtime reader; scratch resolution is fully per-file.</done>
</task>

<task type="auto">
  <name>Task 3: ≤1-compute behavior-preservation golden (D-05) + reenqueue.py:374 known-limitation note</name>
  <files>tests/analyze/services/test_compute_binding_golden.py</files>
  <read_first>
    - tests/analyze/services/test_compute_binding_golden.py (test_single_compute_registry_resolution_is_byte_identical L64-149 — the exact golden strings /srv/scratch/...)
    - src/phaze/tasks/push.py (the Plan-02 _build_rsync_argv — the remote_dest to characterize)
    - src/phaze/routers/agent_push.py (the Plan-03 /pushed scratch_path — the second golden string)
  </read_first>
  <action>Extend the compute-binding golden so a SINGLE-compute registry (one compute entry, dest_ssh_user unset → cfg.push_ssh_user fallback) produces byte-identical outputs after the payload carries the single destination (D-05: same one-row-per-file cloud_job, no schema change — only the payload/resolution path changed): (a) `_build_rsync_argv` remote_dest equals the pre-Phase-73 string `<push_ssh_user>@<push_host>:<scratch_dir>/<file_id>.<file_type>` (assert the exact interpolated value, proving A3 — the fallback preserves the user); (b) the /pushed scratch_path equals `<scratch_dir>/<file_id>.<file_type>` resolved from the recorded backend_id (same string the retired active_compute_scratch_dir produced). Add a module-level docstring note (and record in the SUMMARY) the KNOWN LIMITATION: `reenqueue.py:374 recover_orphaned_work` remains a `select_active_agent(kind="compute")` single-active reader — OUT OF SCOPE for Phase 73 (not a dispatch/push/reconcile seam), tracked as a PROV-01 backlog follow-up; do NOT widen it (44.5k over-enqueue incident risk). Make NO change to reenqueue.py.</action>
  <verify>
    <automated>uv run pytest tests/analyze/services/test_compute_binding_golden.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - A golden test asserting the single-compute remote_dest string is byte-identical (with dest_ssh_user=None → cfg.push_ssh_user) passes.
    - A golden test asserting the single-compute /pushed scratch_path string is byte-identical passes.
    - `grep -rn "reenqueue" tests/analyze/services/test_compute_binding_golden.py` shows the known-limitation note; `git diff --name-only src/phaze/tasks/reenqueue.py` shows NO change.
    - `uv run pytest tests/analyze/services/test_compute_binding_golden.py -x -q` exits 0.
  </acceptance_criteria>
  <done>The ≤1-compute push path is proven byte-identical on the unchanged schema (D-05); the single remaining single-active reader is documented, not widened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drain tick → N backends | one flaky compute backend must not abort the whole tick (DoS on healthy lanes) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-11 | Denial of service | A flaky compute backend's probe failure cascading to abort the drain tick | mitigate | Per-backend snapshot try/except (release_awaiting_cloud.py:151-157) — PROVEN by the MCOMP-05 regression (Task 1) |
| T-73-12 | Tampering | recover_orphaned_work single-active re-drive mis-routing a recovered held file on N-compute deploys | accept | Documented known limitation (PROV-01 backlog); NOT widened this phase — widening risks the 44.5k over-enqueue incident class |
| T-73-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies; pyproject untouched |
</threat_model>

<verification>
- Full targeted suite `uv run pytest tests/analyze/services/test_backends.py tests/analyze/services/test_backend_selection.py tests/analyze/tasks/test_release_awaiting_cloud.py tests/analyze/services/test_compute_binding_golden.py tests/shared/config/test_bucket_registry.py -x -q` green.
- Phase gate: `uv run pytest --cov --cov-report=term-missing` (≥90% floor) + `uv run ruff check . && uv run ruff format --check . && uv run mypy .` clean.
</verification>

<success_criteria>
- MCOMP-02/04/05 each have an automated regression against the unchanged machinery (D-08).
- D-05: the regressions + golden run on the existing one-row-per-file cloud_job schema (no migration/schema change).
- active_compute_scratch_dir is deleted with no runtime reader remaining.
- The ≤1-compute remote_dest + /pushed scratch_path are proven byte-identical (behavior-preservation).
- reenqueue.py:374 is documented as a PROV-01 known limitation, unchanged.
</success_criteria>

<output>
Create `.planning/phases/73-per-agent-dispatch-liveness-scratch-failure-isolation/73-04-SUMMARY.md` when done.
</output>
</content>
