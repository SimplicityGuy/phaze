---
phase: 74-docs-runbook-n-lane-compute-ui-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/shared/services/test_lane_snapshot.py
autonomous: true
requirements: [MCOMP-07]
must_haves:
  truths:
    - "A deterministic test proves N≥2 compute backends each render their own lane card with no kind dedup (MCOMP-07, D-04, R-2)"
    - "A real-fan-out test with two ONLINE compute agents is the arbiter of whether _probe_availability's shared-session probe races (MCOMP-07, D-04, Pitfall 1)"
    - "The 74-03-SUMMARY records the Variant B result (PASS = verification-only; FAIL = Plan 04 must serialize compute probes)"
  artifacts:
    - path: "tests/shared/services/test_lane_snapshot.py"
      provides: "Compute-parity lane regression tests (Variant A deterministic + Variant B real fan-out)"
      contains: "one_lane_per_compute"
  key_links:
    - from: "tests/shared/services/test_lane_snapshot.py (Variant B)"
      to: "seed_active_agent(session, id, kind='compute')"
      via: "two online compute agents whose ids match the two backends' agent_ref"
      pattern: "seed_active_agent"
    - from: "Variant B result"
      to: "Plan 04 conditional _probe_availability fix"
      via: "arbiter — recorded in SUMMARY"
      pattern: "available=True"
---

<objective>
Add the compute-parity lane regression test(s) to `tests/shared/services/test_lane_snapshot.py` (the REAL home —
CONTEXT's `tests/analyze/services/test_backends.py` candidate does NOT own get_backend_lane_snapshot coverage).
Two variants: (A) a deterministic monkeypatched-probe test asserting each of N compute backends renders its own
lane card (no kind dedup), and (B) a REAL `_probe_availability` fan-out with two ONLINE compute agents — the
ARBITER of whether the N≥2-compute shared-session probe race (R-2 / Pitfall 1) actually manifests.

Purpose: MCOMP-07 (D-04) — verify the Phase-71 BEUI N-lane UI renders each compute agent as its own lane and lock
it with a regression test. This test must exist and be green independent of the conditional fix (Plan 04); its
Variant B outcome gates whether Plan 04 needs to serialize the compute probes.

Output: new tests in `tests/shared/services/test_lane_snapshot.py`; the Variant B result recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-RESEARCH.md
@.planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-PATTERNS.md

<interfaces>
<!-- Verified constructors/fixtures — use directly, no codebase scavenger hunt. -->

Existing analog: tests/shared/services/test_lane_snapshot.py:268 test_snapshot_shape_and_rank_order —
  builds LocalBackend/ComputeAgentBackend/KueueBackend, monkeypatch.setattr(backends_mod,"resolve_backends",lambda _s:[...]),
  monkeypatch.setattr(backends_mod,"_probe_availability", _fake_probe), asserts composed lane dicts.
  Backend ctors are keyword: ComputeAgentBackend(id=..., rank=..., cap=...). _kind_of test at :255.

Compute-with-bound-config factory to mirror (tests/analyze/services/test_backends.py:67-95 `_compute`):
  builds phaze.config_backends.ComputeBackend(kind="compute", id=bid, rank, cap, agent_ref=<id>,
  scratch_dir="/srv/scratch", push_host=f"{bid}.push.example", ssh_user=None) and passes it as
  ComputeAgentBackend(id=bid, rank, cap, config=config). is_available resolves self.config.agent_ref via
  select_agent_by_id(session, agent_ref, kind="compute"); an unbound (config=None) backend fails loud in _agent_ref().

Online-agent seeding fixture (tests/_queue_fakes.py:331):
  async def seed_active_agent(session, agent_id="nox", *, kind="fileserver") -> Agent
  — commits a non-revoked, fresh last_seen_at Agent; pass kind="compute" so select_agent_by_id(...,kind="compute")
  resolves online. Already imported by tests/shared/services/test_enqueue_router.py:39;
  usage: await seed_active_agent(session, "compute-01", kind="compute").

get_backend_lane_snapshot lane dict keys: {id, kind, rank, cap, in_flight, available, quota_wait, inadmissible}.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Variant A — deterministic "one lane per compute backend" (monkeypatched probe)</name>
  <files>tests/shared/services/test_lane_snapshot.py</files>
  <behavior>
    - Given a registry of two ComputeAgentBackend (a1-arm64 rank 10, x86-spill rank 20) + one LocalBackend (rank 99),
      with resolve_backends monkeypatched to return them and _probe_availability monkeypatched to a deterministic shim,
      get_backend_lane_snapshot yields exactly two lanes with kind=="compute", ids ["a1-arm64","x86-spill"] in rank order.
    - No kind collapse: len([ln for ln in lanes if ln["kind"]=="compute"]) == 2.
    - Local lane still present (kind=="local", rank 99), lanes sorted (rank, id).
  </behavior>
  <read_first>
    - tests/shared/services/test_lane_snapshot.py (lines 250-321 — _kind_of test at :255, test_snapshot_shape_and_rank_order at :268-321; the monkeypatch pattern for resolve_backends + _fake_probe; existing imports)
    - .planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-PATTERNS.md (Variant A sketch lines 137-140; core snapshot-test pattern lines 124-135)
    - src/phaze/services/backends.py (lines 675-720 get_backend_lane_snapshot loop body + lane dict keys)
  </read_first>
  <action>
    Add a test (name e.g. `test_snapshot_renders_one_lane_per_compute_backend`, include the token
    `one_lane_per_compute` in the name so `-k one_lane_per_compute` selects it) to
    tests/shared/services/test_lane_snapshot.py mirroring test_snapshot_shape_and_rank_order:268. Build two
    ComputeAgentBackend (id `a1-arm64` rank 10 cap 2; id `x86-spill` rank 20 cap 1) plus one LocalBackend
    (id `local` rank 99 cap 1); monkeypatch backends_mod.resolve_backends to return [arm64, x86, local];
    monkeypatch backends_mod._probe_availability with a deterministic shim returning all-available. Call
    `await get_backend_lane_snapshot(session)`. Assert `[ln["id"] for ln in lanes if ln["kind"]=="compute"]`
    == `["a1-arm64","x86-spill"]` (rank order) and that exactly 2 compute lanes exist (no dedup/collapse on
    kind). Use the existing `session` fixture and `@pytest.mark.asyncio` as the sibling tests do.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/services/test_lane_snapshot.py -k one_lane_per_compute -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/shared/services/test_lane_snapshot.py -k one_lane_per_compute -x` exits 0
    - The test asserts exactly 2 compute lanes with ids `["a1-arm64","x86-spill"]` in rank order
    - The test monkeypatches `_probe_availability` (deterministic variant) per the existing sibling pattern
    - `uv run ruff check tests/shared/services/test_lane_snapshot.py` clean
  </acceptance_criteria>
  <done>A deterministic test proves N≥2 compute backends each render their own lane card with no kind dedup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Variant B — REAL probe fan-out with two ONLINE compute agents (the race arbiter)</name>
  <files>tests/shared/services/test_lane_snapshot.py</files>
  <behavior>
    - Given two ComputeAgentBackend each bound to a real ComputeBackend config whose agent_ref matches a seeded
      ONLINE compute Agent, and NO monkeypatch of _probe_availability, get_backend_lane_snapshot exercises the
      real asyncio.gather fan-out over the shared AsyncSession.
    - Both compute lanes come back available=True (a healthy N≥2-compute deployment shows both online).
    - If instead one lane returns available=False, the shared-session race (Pitfall 1) has manifested — record
      this as the arbiter result for Plan 04.
  </behavior>
  <read_first>
    - tests/_queue_fakes.py (lines 331-360 seed_active_agent — commits an online agent; kind="compute" for compute)
    - tests/analyze/services/test_backends.py (lines 67-95 the `_compute` factory that binds a real ComputeBackend config with agent_ref/scratch_dir/push_host — mirror this so is_available resolves the seeded agent)
    - src/phaze/services/backends.py (lines 285-308 ComputeAgentBackend.is_available → select_agent_by_id; lines 651-661 _probe_availability asyncio.gather over shared session; lines 690-697 the post-fan-out session.rollback guard)
    - src/phaze/services/enqueue_router.py (select_agent_by_id ~:131-155 → session.execute — the shared-session touch point)
    - .planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-PATTERNS.md (Variant B lines 141-162; _DbPoisoningLane/_PoisonRecoverSession existing degrade-path helpers to study)
    - .planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-RESEARCH.md (Pitfall 1 lines 175-180; Open Question 2 fixture note)
  </read_first>
  <action>
    Add a test (name e.g. `test_compute_probe_real_fanout_keeps_both_lanes_online`, include the token
    `compute_probe_real` so `-k compute_probe_real` selects it) to tests/shared/services/test_lane_snapshot.py.
    Build two ComputeAgentBackend each carrying a REAL bound ComputeBackend config (mirror the `_compute` factory
    at tests/analyze/services/test_backends.py:67-95): ids `a1-arm64` (rank 10 cap 2, agent_ref `a1-arm64`) and
    `x86-spill` (rank 20 cap 1, agent_ref `x86-spill`), each with scratch_dir + push_host set. Seed TWO online
    compute agents via `await seed_active_agent(session, "a1-arm64", kind="compute")` and
    `await seed_active_agent(session, "x86-spill", kind="compute")` — agent ids MUST equal the backends' agent_ref
    values. Monkeypatch backends_mod.resolve_backends to return the two compute backends (+ optionally a
    LocalBackend). Do NOT monkeypatch _probe_availability (Pitfall 1 — exercise the real fan-out). Call
    `await get_backend_lane_snapshot(session)` and assert BOTH compute lanes come back `available == True`.
    This variant is the arbiter: if it FAILS (a lane is available=False due to the shared-session concurrency
    race), the SUMMARY MUST record "Variant B FAILED — Plan 04 must serialize compute probes"; if it PASSES,
    record "Variant B PASSED — verification-only, Plan 04 does docstring-only". Do not attempt the _probe_availability
    fix here — that is Plan 04's scope, gated on this result.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/services/test_lane_snapshot.py -k compute_probe_real -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The test seeds two ONLINE `kind="compute"` agents via `seed_active_agent` whose ids equal the two backends' `agent_ref`
    - The test does NOT monkeypatch `_probe_availability` (real fan-out exercised)
    - The test asserts both compute lanes' `available` is `True`
    - The 74-03-SUMMARY explicitly records the Variant B outcome (PASSED → verification-only / FAILED → Plan 04 serializes compute probes)
    - `uv run ruff check tests/shared/services/test_lane_snapshot.py` clean
  </acceptance_criteria>
  <done>A real-fan-out, two-online-compute-agents test exists and its PASS/FAIL outcome is recorded as the arbiter for Plan 04's conditional fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → DB | Tests seed and read agent/cloud_job rows on the test Postgres; no production or network boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-05 | (none — test-only) | new regression tests | accept | This plan adds test code only; it introduces no runtime attack surface. The tests certify an existing invariant (N compute lanes render + probe safety) and disclose no secrets (lane dicts carry only {id,kind,rank,cap,in_flight,available,quota_wait,inadmissible}). |
</threat_model>

<verification>
- `uv run pytest tests/shared/services/test_lane_snapshot.py -x` green (both new variants).
- Variant B outcome recorded in 74-03-SUMMARY as the arbiter for Plan 04.
- `just test-bucket shared` green.
</verification>

<success_criteria>
- MCOMP-07 (D-04) N-lane compute rendering is locked by a deterministic regression test, and the real-fan-out
  race question (R-2 / Pitfall 1) is settled by an executed test whose result gates Plan 04.
</success_criteria>

<output>
Create `.planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-03-SUMMARY.md` when done — and it MUST
state the Variant B result (PASSED = verification-only; FAILED = Plan 04 must serialize compute probes).
</output>
