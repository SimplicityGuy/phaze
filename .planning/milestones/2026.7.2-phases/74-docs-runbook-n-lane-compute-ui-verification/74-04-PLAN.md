---
phase: 74-docs-runbook-n-lane-compute-ui-verification
plan: 04
type: execute
wave: 2
depends_on: ["74-03"]
files_modified:
  - src/phaze/services/backends.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [MCOMP-07]
must_haves:
  truths:
    - "The stale '≤1 compute' _probe_availability docstring is corrected to reflect the Phase-72 N-compute reality, REGARDLESS of the arbiter result (MCOMP-07, Pitfall 1)"
    - "IF Plan 03 Variant B failed, the compute (select_agent_by_id) probes are serialized so N≥2 online compute lanes stay available; local/kueue probes stay concurrent (MCOMP-07, D-04)"
    - "At closeout, MCOMP-07 checkbox + Traceability + ROADMAP Phase 74 flip to Complete and just docs-drift is green (MCOMP-07, Pitfall 4)"
  artifacts:
    - path: "src/phaze/services/backends.py"
      provides: "Corrected _probe_availability docstring (+ conditional compute-probe serialization)"
      contains: "_probe_availability"
    - path: ".planning/REQUIREMENTS.md"
      provides: "MCOMP-07 traceability flipped at closeout"
      contains: "MCOMP-07"
  key_links:
    - from: "src/phaze/services/backends.py _probe_availability"
      to: "conditional compute-probe serialization"
      via: "partition compute vs local/kueue via _kind_of, gated on 74-03 Variant B result"
      pattern: "_probe_availability"
    - from: ".planning/REQUIREMENTS.md MCOMP-07"
      to: "just docs-drift green"
      via: "checkbox [x] + Traceability Complete + ROADMAP [x] all agree"
      pattern: "MCOMP-07"
---

<objective>
Close out MCOMP-07: (1) correct the stale "≤1 compute" `_probe_availability` docstring in
`src/phaze/services/backends.py` REGARDLESS of the arbiter result, and — CONDITIONALLY, only if Plan 03's
Variant B (real fan-out) failed — serialize the compute (`select_agent_by_id`) probes so N≥2 online compute
lanes never spuriously go offline; (2) at phase closeout, flip the MCOMP-07 traceability bookkeeping and run
`just docs-drift` green.

Purpose: This is the verify-then-fix seam (D-04 "fix only if a gap surfaces") plus the milestone-close
traceability hygiene (Pitfall 4). Whether the code fix lands is determined by 74-03's recorded Variant B result;
the docstring correction and the closeout flip happen either way.

Output: updated `src/phaze/services/backends.py` (docstring always; serialization conditionally) + the closeout
flips in `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-RESEARCH.md
@.planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-PATTERNS.md
@.planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-03-SUMMARY.md

<interfaces>
<!-- Verified current source — edit precisely, no exploration. -->

src/phaze/services/backends.py:
  - _probe_one(session, backend) ~:632-648: LocalBackend short-circuits to (id, True) with NO I/O; every other
    backend awaits is_available under asyncio.wait_for(_PROBE_TIMEOUT_SEC); any exception/timeout → (id, False).
  - _probe_availability(session, backends) ~:651-661: `results = await asyncio.gather(*(_probe_one(session, b)
    for b in backends)); return dict(results)`. STALE docstring (~:655-658) still claims "the D-05 invariant caps
    compute at ≤1, so at most ONE probe ever uses the session concurrently" — retired by Phase 72 (MCOMP-01).
  - _kind_of(backend) ~:664-672: isinstance dispatch → "local"/"compute"/"kueue" — the seam to partition compute
    vs non-compute backends.
  - get_backend_lane_snapshot :690-697 post-fan-out `await session.rollback()` guard (handles single-probe poison,
    NOT two probes racing before it).
  Only compute probes touch the shared AsyncSession (via select_agent_by_id → session.execute); local is
  short-circuited (no I/O), Kueue uses kr8s (no session).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the ≤1-compute docstring (always) + conditionally serialize compute probes</name>
  <files>src/phaze/services/backends.py</files>
  <read_first>
    - .planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-03-SUMMARY.md (the recorded Variant B arbiter result — PASSED = docstring-only; FAILED = serialize compute probes)
    - src/phaze/services/backends.py (lines 632-672 _probe_one / _probe_availability / _kind_of; lines 675-720 get_backend_lane_snapshot with its :690-697 rollback guard)
    - .planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-PATTERNS.md (conditional-fix section lines 166-185; fix pattern = keep local/kueue concurrent, serialize compute)
    - .planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-RESEARCH.md (Pitfall 1 lines 175-180; Assumption A1)
  </read_first>
  <action>
    First read 74-03-SUMMARY.md for the Variant B result.
    ALWAYS: rewrite the `_probe_availability` docstring (~:655-658) to remove the retired "the D-05 invariant caps
    compute at ≤1, so at most ONE probe ever uses the session concurrently" claim. Replace it with the correct
    Phase-72 (MCOMP-01) reality: N compute backends are legal; each compute probe touches the shared session via
    select_agent_by_id, local is short-circuited (no I/O), Kueue uses kr8s (no session).
    CONDITIONAL on Variant B: IF 74-03-SUMMARY records Variant B FAILED (a compute lane went available=False under
    the real concurrent fan-out), change `_probe_availability` to keep the local + Kueue probes concurrent
    (asyncio.gather) but run the compute (session-touching) probes SEQUENTIALLY — partition backends via `_kind_of`
    (compute vs non-compute), gather the non-compute probes, await the compute probes one at a time, merge into
    the `{backend_id: available}` dict. Preserve the existing bounded `_probe_one`/`asyncio.wait_for` degrade-safe
    behavior and secret-free logging. Re-run the Plan 03 Variant B test to confirm both compute lanes now come back
    available=True.
    IF 74-03-SUMMARY records Variant B PASSED: make NO behavioral change to _probe_availability — the docstring
    correction is the only edit (note in the SUMMARY that the race did not manifest, verification-only per D-04).
    Either way, keep `src/phaze/services/backends.py` at ≥90% per-module coverage (CLAUDE.md floor); add/extend a
    unit assertion if the serialization branch is added.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/services/test_lane_snapshot.py -x -q && uv run mypy src/phaze/services/backends.py</automated>
  </verify>
  <acceptance_criteria>
    - The `_probe_availability` docstring no longer contains the "caps compute at ≤1" / "at most ONE probe" claim and states N compute backends are legal (Phase-72 reality)
    - If 74-03 Variant B FAILED: compute probes run sequentially, local/kueue stay concurrent, and `uv run pytest tests/shared/services/test_lane_snapshot.py -k compute_probe_real -x` exits 0 (both lanes available=True)
    - If 74-03 Variant B PASSED: `_probe_availability` behavior is unchanged (docstring-only edit) and the SUMMARY records verification-only
    - `uv run mypy src/phaze/services/backends.py` clean; `uv run ruff check src/phaze/services/backends.py` clean
    - `services/backends.py` per-module coverage stays ≥90% (`uv run pytest --cov=phaze.services.backends --cov-report=term-missing` on the relevant tests)
  </acceptance_criteria>
  <done>The stale ≤1-compute docstring is corrected; the compute-probe race is either fixed (if it manifested) or confirmed absent (verification-only), with the full lane-snapshot suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Closeout — flip MCOMP-07 traceability + ROADMAP Phase 74 and run docs-drift green</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 18 MCOMP-07 checkbox `- [ ]`; line 53 Traceability row `MCOMP-07 | Phase 74 | Pending`)
    - .planning/ROADMAP.md (line 24 Phase 74 `- [ ]`; the Phase 74 § at lines 1071-1081)
    - tests/shared/core/test_requirements_traceability.py (the guard mechanics — for a passed phase every mapped req must be checkbox [x] AND table Complete, and the two encodings must agree)
    - .planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-RESEARCH.md (Pitfall 4 lines 193-196 — flip BOTH encodings together, in-flight [ ]+Pending tolerated until the phase passes)
  </read_first>
  <action>
    CLOSEOUT ONLY — perform this after 74-VERIFICATION.md reports `status: passed` (the guard tolerates the
    in-flight `[ ]`+Pending state during execution; flipping prematurely while the phase is not yet passed is the
    Pitfall 4 hazard). Flip `.planning/REQUIREMENTS.md` MCOMP-07 checkbox `- [ ]` → `- [x]` (line 18) AND its
    Traceability Status row `Pending` → `Complete` (line 53) — BOTH must agree. Flip `.planning/ROADMAP.md`
    Phase 74 `- [ ]` → `- [x]` (line 24). Do NOT touch any other requirement's state. Then run `just docs-drift`
    and confirm it exits 0. Do NOT perform the milestone-close (`/gsd:complete-milestone 2026.7.2`) or the release
    tag — those are explicitly out of scope (a separate step after this phase's PR merges).
  </action>
  <verify>
    <automated>just docs-drift</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` MCOMP-07 shows `- [x]` AND its Traceability row reads `Complete` (the two agree)
    - `.planning/ROADMAP.md` Phase 74 line shows `- [x]`
    - `just docs-drift` exits 0
    - No other requirement's checkbox/table state was modified; no milestone-close or tag performed
  </acceptance_criteria>
  <done>MCOMP-07 checkbox, Traceability table, and ROADMAP Phase 74 all read Complete/[x] in agreement and the docs-drift guard is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lane probe → DB session | The compute probe crosses into the shared AsyncSession; the conditional fix concerns correctness/availability, not a security boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-06 | Denial of Service (availability) | _probe_availability shared-session fan-out with N≥2 online compute backends | mitigate | If the arbiter test proves the race, serialize the compute probes so a healthy compute lane never spuriously degrades to offline; preserve the bounded `_probe_one` timeout so no lane can stall the 5s poll. |
| T-74-07 | Information Disclosure | probe logging / lane dicts | accept | Unchanged invariant: `_probe_one` logs `backend_id` only (never SecretStr/tokens); lane dicts carry no config/secret (T-71-01). This plan does not alter that. |
</threat_model>

<verification>
- `_probe_availability` docstring corrected; `uv run pytest tests/shared/services/test_lane_snapshot.py -x` green.
- If the fix landed: `-k compute_probe_real` passes with both lanes available=True; `services/backends.py` ≥90% coverage.
- `uv run mypy src/phaze/services/backends.py` clean; `just test-bucket shared` green.
- At closeout: MCOMP-07 `[x]`+Complete, ROADMAP Phase 74 `[x]`, `just docs-drift` exits 0.
</verification>

<success_criteria>
- MCOMP-07 is fully closed: the stale docstring is corrected, the N≥2-compute probe safety is certified (fixed if
  it manifested, else verification-only per D-04), and the traceability/ROADMAP bookkeeping is consistent with the
  docs-drift guard green — WITHOUT performing the out-of-scope milestone-close/tag.
</success_criteria>

<output>
Create `.planning/phases/74-docs-runbook-n-lane-compute-ui-verification/74-04-SUMMARY.md` when done — state whether
the conditional _probe_availability fix landed (Variant B FAILED) or was skipped (Variant B PASSED, docstring-only).
</output>
