---
phase: 72-per-entry-compute-binding-fail-fast-retirement
plan: 02
type: execute
wave: 2
depends_on: ["72-01"]
files_modified:
  - src/phaze/config.py
  - src/phaze/services/backends.py
  - tests/analyze/services/test_backends.py
  - tests/shared/config/test_bucket_registry.py
autonomous: true
requirements: [MCOMP-01]

must_haves:
  truths:
    - "resolved_non_local_kind no longer raises on a compute-only registry of 2+ compute backends — it returns 'compute' (D-03; the discretion confirmation that the compute-only branch still returns 'compute' for N compute)."
    - "active_compute_scratch_dir no longer raises on 2+ compute backends — it stops raising and the ≤1-compute return value is byte-identical (D-03, D-07)."
    - "A backends.toml declaring 2+ compute backends is accepted through both accessors where before the ≤1-compute fail-fast raised (D-03)."
    - "The Plan-01 golden ≤1-compute + zero-compute characterization stays green (D-06)."
  artifacts:
    - path: "src/phaze/services/backends.py"
      provides: "resolved_non_local_kind generalized for N compute (no >1 raise)"
      contains: "def resolved_non_local_kind"
    - path: "src/phaze/config.py"
      provides: "active_compute_scratch_dir generalized for N compute (no >1 raise), ≤1 return byte-identical"
      contains: "def active_compute_scratch_dir"
  key_links:
    - from: "src/phaze/services/backends.py::resolved_non_local_kind"
      to: "return non_local[0].kind"
      via: "compute-only branch drops the >1 raise, still yields 'compute'"
      pattern: "non_local\\[0\\]\\.kind"
    - from: "src/phaze/config.py::active_compute_scratch_dir"
      to: "compute[0].scratch_dir"
      via: "delete the len(compute) > 1 raise block; ≤1 return unchanged"
      pattern: "compute\\[0\\]"
---

<objective>
Retire the two `≤1-compute` fail-fasts (D-03): the compute-only `>1` raise in
`resolved_non_local_kind` (`services/backends.py` ~L494) and the `>1` raise in
`active_compute_scratch_dir` (`config.py` ~L483). Generalize both for a `local + N-Kueue + N-compute`
registry while keeping the ≤1-compute return values byte-identical (D-07). Flip the three existing
tests that assert the retired raise to assert the generalized (no-raise) behavior.

Purpose: A `backends.toml` with 2+ compute backends is accepted through these accessors where before
it 500'd — the last `≤1`-reduction in the compute lane (the STATE.md-tracked PROV-01 descope), closing
success criterion 3.
Output: two one-block deletions in production accessors + three flipped assertions; Plan-01 golden stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-per-entry-compute-binding-fail-fast-retirement/72-CONTEXT.md
@.planning/phases/72-per-entry-compute-binding-fail-fast-retirement/72-PATTERNS.md

<interfaces>
<!-- The exact accessors to edit and the sibling generalization already shipped for Kueue (WR-01). -->

From src/phaze/services/backends.py::resolved_non_local_kind (L487-499 — the block to change):
```
if not settings.cloud_enabled:
    return "local"
non_local = [backend for backend in settings.backends if backend.kind != "local"]
if any(backend.kind == "kueue" for backend in non_local):
    return "kueue"
# REMOVE the following len(non_local) > 1 raise (D-03); keep the trailing return:
if len(non_local) > 1:
    raise ValueError("multiple compute backends ... (multi-compute lands in PROV-01)")
return non_local[0].kind        # yields "compute" for N compute — the discretion confirmation
```

From src/phaze/config.py::active_compute_scratch_dir (L480-489 — the block to change):
```
compute = [backend for backend in self.backends if backend.kind == "compute"]
if not compute:
    return None
# REMOVE the following len(compute) > 1 raise (D-03); keep ≤1 return byte-identical:
if len(compute) > 1:
    raise ValueError("multiple compute backends ... (multi-compute lands in PROV-01)")
backend = compute[0]
return backend.scratch_dir if isinstance(backend, ComputeBackend) else None
```

Precedent (WR-01, Phase 70 — already generalized the any-kueue branch, no raise): the kueue-any
branch at backends.py:490-491 tolerates N Kueue. This plan applies the same generalize-not-descope to
the compute-only branch.

Tests to FLIP (currently assert the retired raise):
- tests/analyze/services/test_backends.py::test_resolved_non_local_kind_raises_on_multiple_compute_only (L577)
- tests/shared/config/test_bucket_registry.py::test_multiple_compute_backends_scratch_dir_raises (L317)
Tests already asserting the target no-raise N behavior (keep green): test_resolve_backends_returns_all_non_local (L689), test_active_compute_scratch_dir_resolves_under_local_2kueue_1compute (L737).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire the compute-only >1 raise in resolved_non_local_kind (D-03)</name>
  <files>src/phaze/services/backends.py, tests/analyze/services/test_backends.py</files>
  <read_first>
    - src/phaze/services/backends.py §`resolved_non_local_kind` (L469-499 — full docstring + the any-kueue branch + the compute-only >1 raise)
    - tests/analyze/services/test_backends.py §`test_resolved_non_local_kind_raises_on_multiple_compute_only` (L577-608) and §`test_resolved_non_local_kind_returns_kueue_for_n_kueue` (L658-683, the sibling no-raise assertion style)
    - .planning/phases/72-per-entry-compute-binding-fail-fast-retirement/72-PATTERNS.md §"Generalize-not-descope for fail-fasts (WR-01, Phase 70)"
  </read_first>
  <action>
    Delete the `if len(non_local) > 1: raise ValueError(...)` block (backends.py ~L494-498) so the
    function falls through to `return non_local[0].kind`, which yields `"compute"` for a compute-only
    registry of any N — the D-03 generalization and the discretion confirmation that the compute-only
    branch still returns `"compute"` for N compute. Leave the `not cloud_enabled -> "local"` guard and
    the `any(kind == "kueue") -> "kueue"` branch UNCHANGED (they already tolerate N Kueue, WR-01).
    Update the docstring: replace the "fail-fast is retained ONLY for the compute-only >1 case" wording
    with a note that the compute-only branch now returns `"compute"` for N compute (per-agent dispatch
    attribution lands in Phase 73). Then FLIP `test_resolved_non_local_kind_raises_on_multiple_compute_only`:
    rename it to `test_resolved_non_local_kind_returns_compute_for_multiple_compute_only`, drop the
    `pytest.raises(...)`, and assert `backends.resolved_non_local_kind(settings) == "compute"` for the
    two-compute-backend registry (cite D-03). Keep the `cloud_enabled is True` assertion.
  </action>
  <verify>
    <automated>uv run pytest tests/analyze/services/test_backends.py -k "resolved_non_local_kind" -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "len(non_local) > 1" src/phaze/services/backends.py` returns nothing (raise block deleted).
    - Behavior assertion: `resolved_non_local_kind` returns `"compute"` for a 2-compute-only registry and still returns `"kueue"` for any-kueue and `"local"` for all-local (the two sibling tests stay green).
    - The flipped test asserts equality with `"compute"`, not a raise.
  </acceptance_criteria>
  <done>resolved_non_local_kind accepts N compute (returns "compute"); the raise is gone; any-kueue and all-local branches unchanged; flipped test green.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the >1 raise in active_compute_scratch_dir, keep ≤1 byte-identical (D-03, D-07)</name>
  <files>src/phaze/config.py, tests/shared/config/test_bucket_registry.py</files>
  <read_first>
    - src/phaze/config.py §`active_compute_scratch_dir` (L468-489 — full docstring + the >1 raise + the compute[0] return)
    - tests/shared/config/test_bucket_registry.py §`test_multiple_compute_backends_scratch_dir_raises` (L317-348)
    - tests/analyze/services/test_backends.py §`test_active_compute_scratch_dir_resolves_under_local_2kueue_1compute` (L737-758, the ≤1 byte-identical case that must stay green)
    - .planning/phases/72-per-entry-compute-binding-fail-fast-retirement/72-CONTEXT.md §D-07 (keep ≤1 resolution byte-identical; per-agent scratch widening is Phase 73)
  </read_first>
  <action>
    Delete the `if len(compute) > 1: raise ValueError(...)` block (config.py ~L483-487). After deletion
    the accessor falls through to `backend = compute[0]; return backend.scratch_dir if isinstance(...) else None`
    — the ≤1 return value is byte-identical (for exactly one compute backend, `compute[0]` is that one),
    satisfying D-07. For N compute it now returns the first compute entry's scratch_dir as a documented
    TRANSITIONAL reduction; add a one-line docstring note that per-agent scratch resolution replaces this
    global accessor in Phase 73 (MCOMP-03) and that agent_push.py must stay byte-identical in 72 (D-07).
    Do NOT re-derive per-agent scratch here (Phase 73 territory). Then FLIP
    `test_multiple_compute_backends_scratch_dir_raises` in test_bucket_registry.py: rename to
    `test_multiple_compute_backends_scratch_dir_no_longer_raises`, drop `pytest.raises`, and assert the
    accessor returns the first compute backend's scratch_dir (e.g. `"/scratch/a"`) without raising, with
    a docstring citing D-03 (retire the fail-fast) + D-07 (per-agent widening deferred to Phase 73).
  </action>
  <verify>
    <automated>uv run pytest tests/shared/config/test_bucket_registry.py -k "scratch_dir" tests/analyze/services/test_backends.py -k "scratch_dir" -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "len(compute) > 1" src/phaze/config.py` returns nothing (raise block deleted).
    - Behavior assertion: a 2-compute registry yields `active_compute_scratch_dir == "/scratch/a"` (first entry) with no raise; the local+2kueue+1compute case still returns `"/srv/scratch"` (≤1 byte-identical).
    - The flipped test asserts a returned scratch_dir string, not a raise.
  </acceptance_criteria>
  <done>active_compute_scratch_dir accepts N compute (no raise), ≤1 return byte-identical; the flipped test green; the local+2kueue+1compute ≤1 test still green.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm the Plan-01 golden + full backend suites stay green (D-06)</name>
  <files>tests/analyze/services/test_backends.py</files>
  <read_first>
    - tests/analyze/services/test_compute_binding_golden.py (the Plan-01 golden — must remain untouched and green)
    - tests/analyze/services/test_backends.py (the resolved_non_local_kind + resolve_backends + scratch_dir cells)
    - tests/shared/config/test_bucket_registry.py and tests/shared/config/test_backend_registry.py (the registry-validation suites)
  </read_first>
  <action>
    Run the Plan-01 golden and the backend/registry suites to confirm the two raise deletions are
    behavior-preserving on the ≤1-compute and all-local paths (D-06). Do NOT edit the golden. If any
    cell other than the two intentionally-flipped raise tests changed behavior, STOP — that indicates a
    non-preserving edit, not a test-update opportunity. Only the two flipped tests (Tasks 1-2) may
    differ from the pre-change suite. Fix any incidental ruff/mypy drift introduced by the docstring edits.
  </action>
  <verify>
    <automated>uv run pytest tests/analyze/services/test_compute_binding_golden.py tests/analyze/services/test_backends.py tests/shared/config/test_bucket_registry.py tests/shared/config/test_backend_registry.py -q && uv run mypy src/phaze/config.py src/phaze/services/backends.py</automated>
  </verify>
  <acceptance_criteria>
    - The Plan-01 golden module passes unchanged (byte-identical ≤1-compute + zero-compute proof holds).
    - The full backend + registry suites pass; only `test_resolved_non_local_kind_returns_compute_for_multiple_compute_only` and `test_multiple_compute_backends_scratch_dir_no_longer_raises` differ from the pre-change assertions.
    - `uv run mypy` clean on both edited production modules.
  </acceptance_criteria>
  <done>Golden + backend + registry suites green; only the two raise tests flipped; mypy clean — the retirement is proven behavior-preserving on ≤1-compute and all-local.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → backends.toml → ControlSettings accessors | operator registry config drives resolved_non_local_kind / active_compute_scratch_dir reductions consumed by the /pushed callback + dashboard/backfill |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-02-01 | Elevation of privilege / wrong-target dispatch | active_compute_scratch_dir returning a wrong scratch_dir for N compute | accept | Documented transitional reduction: the ≤1 return is byte-identical (D-07); the N-compute per-agent scratch resolution is Phase 73 (MCOMP-03) and agent_push.py stays byte-identical in 72. The real target deploy is local+N-Kueue+1-compute (≤1 compute), so N-compute is groundwork not yet dispatched. |
| T-72-02-02 | Denial of service | dropping the fail-fast masks a genuine mis-config | mitigate | The dropped raise is replaced by (a) the Plan-04 boot-time duplicate-agent_ref guard and (b) D-05 runtime degrade-to-hold — a mis-referenced agent holds rather than silently dispatching to the wrong node. |
| T-72-02-03 | Tampering | non-preserving edit slips past review | mitigate | The Plan-01 golden (D-06) is re-run in Task 3 and MUST stay green; only the two intentionally-flipped raise tests may differ. |
| T-72-02-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new dependencies; no install task exists, so the supply-chain gate is not triggered. |
</threat_model>

<verification>
- `grep -n "len(non_local) > 1" src/phaze/services/backends.py` and `grep -n "len(compute) > 1" src/phaze/config.py` both return nothing.
- `uv run pytest tests/analyze/services/test_compute_binding_golden.py tests/analyze/services/test_backends.py tests/shared/config/test_bucket_registry.py tests/shared/config/test_backend_registry.py -q` passes.
- `uv run ruff check src/phaze/config.py src/phaze/services/backends.py` and `uv run mypy src/phaze/config.py src/phaze/services/backends.py` clean.
</verification>

<success_criteria>
- Both compute-only `>1` raises are retired; resolved_non_local_kind returns "compute" for N compute; active_compute_scratch_dir returns without raising for N compute with the ≤1 return byte-identical (success criterion 3).
- The two raise-asserting tests are flipped to assert the generalized behavior; the Plan-01 golden stays green (behavior-preserving on ≤1-compute and all-local).
</success_criteria>

<output>
Create `.planning/phases/72-per-entry-compute-binding-fail-fast-retirement/72-02-SUMMARY.md` when done.
</output>
