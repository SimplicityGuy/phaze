---
phase: 72-per-entry-compute-binding-fail-fast-retirement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/analyze/services/test_compute_binding_golden.py
autonomous: true
requirements: [MCOMP-01]

must_haves:
  truths:
    - "The ≤1-compute dispatch/resolution path (is_available, resolved_non_local_kind, active_compute_scratch_dir, the /pushed scratch-path format) is pinned by a committed characterization test that is green against current behavior (D-06)."
    - "The zero-compute implicit-all-local path (cloud_enabled False, resolved_non_local_kind 'local', active_compute_scratch_dir None, no ComputeAgentBackend, no cloud_job activity) is pinned by an explicit regression that is green against current behavior (D-06)."
    - "The golden characterizes ONLY the byte-identical single-compute deploy where the online compute agent's Agent.id equals the backend's agent_ref (the real single-compute deploy that D-01 binds to)."
  artifacts:
    - path: "tests/analyze/services/test_compute_binding_golden.py"
      provides: "D-06 golden byte-identical characterization of the ≤1-compute path + explicit zero-compute all-local regression"
      contains: "def test_"
      min_lines: 60
  key_links:
    - from: "tests/analyze/services/test_compute_binding_golden.py"
      to: "phaze.services.backends.resolve_backends / resolved_non_local_kind"
      via: "ControlSettings from a 1-compute backends.toml resolved through resolve_backends"
      pattern: "resolve_backends|resolved_non_local_kind"
    - from: "tests/analyze/services/test_compute_binding_golden.py"
      to: "phaze.config.ControlSettings.active_compute_scratch_dir / cloud_enabled"
      via: "settings property reads under 1-compute and all-local registries"
      pattern: "active_compute_scratch_dir|cloud_enabled"
---

<objective>
Establish the D-06 acceptance safety net BEFORE any production change: a golden byte-identical
characterization of the ≤1-compute dispatch/resolution path, PLUS an explicit zero-compute
(implicit all-local) regression proving no cloud activity. This test is committed green against
CURRENT behavior in Wave 1; Waves 2 and 3 change production code and MUST keep this test green —
that is the byte-identical proof (mirrors the Phase-68 D-01 golden-characterization precedent).

Purpose: Lock the observable ≤1-compute and all-local behavior so the fail-fast retirement (Plan 02)
and per-entry binding rewire (Plan 03) are provably behavior-preserving on the real deploys.
Output: `tests/analyze/services/test_compute_binding_golden.py` (test-only; no production edits).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-per-entry-compute-binding-fail-fast-retirement/72-CONTEXT.md
@.planning/phases/72-per-entry-compute-binding-fail-fast-retirement/72-PATTERNS.md

<interfaces>
<!-- Contracts the golden exercises. Use these directly — no codebase exploration needed. -->

From src/phaze/services/backends.py:
```
def resolve_backends(settings: ControlSettings) -> list[Backend]        # one impl per registry entry; ComputeAgentBackend(id, rank, cap, config=entry)
def resolved_non_local_kind(settings: ControlSettings) -> str           # "local" all-local, "kueue" any-kueue, "compute" compute-only
class ComputeAgentBackend(_BaseBackend):
    async def is_available(self, session: AsyncSession) -> bool          # CURRENT: True iff select_active_agent(kind="compute") resolves; False (never raises) when absent
class LocalBackend(_BaseBackend): ...                                    # is_available always True; in_flight_count always 0
```

From src/phaze/config.py (ControlSettings):
```
@property def cloud_enabled(self) -> bool                               # True iff any non-local backend
@property def active_compute_scratch_dir(self) -> str | None            # the single compute backend's scratch_dir, else None
```

From src/phaze/routers/agent_push.py (~L133 — the /pushed scratch-path format, BOUNDARY, must NOT change in 72):
```
scratch_path = f"{settings.active_compute_scratch_dir}/{file_id}.{file.file_type}"
```

Test helpers (already in the suite):
```
from tests._queue_fakes import seed_active_agent   # seed_active_agent(session, agent_id="oci-a1", *, kind="compute") -> Agent (online, non-revoked)
# backends_toml_env fixture: writes a backends.toml, points PHAZE env at it, clears get_settings cache
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Golden ≤1-compute characterization (byte-identical, matching-ref deploy)</name>
  <files>tests/analyze/services/test_compute_binding_golden.py</files>
  <read_first>
    - tests/analyze/services/test_backends.py (module top: the `backends = pytest.importorskip(...)` idiom, the `backends_toml_env` usage, `_LOCAL_2KUEUE_HEAD` toml shapes, and `_make_file`)
    - tests/_queue_fakes.py §`seed_active_agent` (signature: `agent_id` + `kind="compute"`)
    - src/phaze/services/backends.py §`ComputeAgentBackend.is_available` (L245-255) and §`resolved_non_local_kind` (L469-499)
    - src/phaze/config.py §`active_compute_scratch_dir` (L468-489) and §`cloud_enabled` (L458-466)
    - src/phaze/routers/agent_push.py (L133 scratch_path format — the format string this golden pins)
  </read_first>
  <action>
    Create the golden test module. Frame every cell as pinning CURRENT ≤1-compute behavior that must
    survive Waves 2-3 unchanged (cite D-06). Build a single-compute registry via `backends_toml_env`:
    one `kind="local"` (id="local", rank=99, cap=1) plus one `kind="compute"` entry with
    id="oci-a1", agent_ref="oci-a1", scratch_dir="/srv/scratch", rank=30, cap=2 — the operator's
    agent_ref EQUALS the agent's Agent.id, which is the byte-identical single-compute deploy D-01 binds to.
    Construct `ControlSettings()` and assert:
      - `settings.cloud_enabled is True`
      - `resolved_non_local_kind(settings) == "compute"`
      - `settings.active_compute_scratch_dir == "/srv/scratch"`
      - the /pushed scratch-path format is exactly `f"{settings.active_compute_scratch_dir}/{file_id}.{file_type}"`
        (assert the composed string for a fixed file_id/file_type equals `/srv/scratch/<uuid>.mp3`) — pins
        the D-07 boundary that agent_push.py must keep byte-identical.
    Then seed an ONLINE compute agent via `seed_active_agent(session, agent_id="oci-a1", kind="compute")`,
    resolve the ComputeAgentBackend via `resolve_backends(settings)` (select the id=="oci-a1" impl), and assert
    `await backend.is_available(session) is True`. Add a companion cell: with NO compute agent seeded,
    assert `await backend.is_available(session) is False` (the degrade-safe hold, never raises).
    Do NOT characterize the "compute agent online but id != agent_ref" case here — that is the intended
    behavior CHANGE (belongs in Plan 03), not a byte-identical invariant.
  </action>
  <verify>
    <automated>uv run pytest tests/analyze/services/test_compute_binding_golden.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The new test file collects and PASSES against current (unchanged) production code — proving it pins real behavior, not a future contract.
    - Source assertion: the file contains an assertion on `resolved_non_local_kind(settings) == "compute"`, on `settings.active_compute_scratch_dir == "/srv/scratch"`, and on the exact `f"{...}/{file_id}.{file_type}"` scratch-path string.
    - Behavior assertion: `is_available` returns True when agent id "oci-a1" is online and False (no raise) when absent.
  </acceptance_criteria>
  <done>Committed golden ≤1-compute characterization is green against current behavior; pins is_available (matching-ref online + absent), resolved_non_local_kind, active_compute_scratch_dir, and the /pushed scratch-path format.</done>
</task>

<task type="auto">
  <name>Task 2: Explicit zero-compute (implicit all-local) regression</name>
  <files>tests/analyze/services/test_compute_binding_golden.py</files>
  <read_first>
    - src/phaze/config_backends.py §`_default_local_registry` (L223-229: id="local", rank=99, cap=1) — the implicit all-local baseline
    - src/phaze/config.py §`cloud_enabled` (L458-466) and §`active_compute_scratch_dir` (L468-489)
    - src/phaze/services/backends.py §`resolve_backends` (L443-466) and §`resolved_non_local_kind` (L469-499)
    - tests/shared/config/test_backend_registry.py §`test_default_local_registry_returns_single_rank99_local` (the default-registry assertion idiom)
  </read_first>
  <action>
    Add a zero-compute regression cell to the same module. Construct `ControlSettings()` with NO
    `backends.toml` present (the implicit `_default_local_registry` all-local baseline) and assert the
    "no cloud activity" surface (cite D-06):
      - `settings.cloud_enabled is False`
      - `resolved_non_local_kind(settings) == "local"`
      - `settings.active_compute_scratch_dir is None`
      - `resolve_backends(settings)` returns exactly one impl, a `LocalBackend`, and contains NO
        `ComputeAgentBackend` (assert `not any(isinstance(b, backends.ComputeAgentBackend) for b in resolved)`).
    Use the same env-clearing discipline the existing default-registry test uses so no stray
    `PHAZE_BACKENDS_TOML` leaks in. This cell is the explicit all-local regression D-06 requires alongside
    the ≤1-compute golden.
  </action>
  <verify>
    <automated>uv run pytest tests/analyze/services/test_compute_binding_golden.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: under the implicit all-local registry, `cloud_enabled` is False, `resolved_non_local_kind` is "local", `active_compute_scratch_dir` is None, and `resolve_backends` yields a single LocalBackend with zero ComputeAgentBackend.
    - The whole module passes: `uv run pytest tests/analyze/services/test_compute_binding_golden.py -q` exits 0 against unchanged production code.
  </acceptance_criteria>
  <done>Explicit zero-compute all-local regression is committed and green, proving the implicit-local registry produces no cloud lane and no compute backend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → backends.toml → ControlSettings | operator-authored registry config crosses into the process; `agent_ref` becomes a DB lookup key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01-01 | Tampering | golden characterization asserting the wrong baseline | mitigate | Test authored against CURRENT code and run green in Wave 1 (Task verify) — a mis-pinned baseline fails immediately, before it can green-light a non-preserving refactor. |
| T-72-01-02 | Information disclosure | test fixtures echoing secrets | accept | Registry uses no SecretStr fields (compute entries carry only agent_ref/scratch_dir); no credential material is constructed or logged. |
| T-72-01-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new dependencies in this phase; no package-manager install task exists, so the supply-chain gate is not triggered. |
</threat_model>

<verification>
- `uv run pytest tests/analyze/services/test_compute_binding_golden.py -q` passes against unchanged production code.
- `uv run ruff check tests/analyze/services/test_compute_binding_golden.py` and `uv run ruff format --check` clean.
- No file under `src/` is modified by this plan (test-only).
</verification>

<success_criteria>
- A committed, green golden module pins the ≤1-compute path (is_available matching-ref-online + absent, resolved_non_local_kind, active_compute_scratch_dir, /pushed scratch-path format) and the zero-compute all-local path (cloud_enabled/resolved_non_local_kind/scratch_dir/no-compute-backend).
- The module characterizes only byte-identical invariants (matching agent_ref); the intended behavior change is deliberately excluded.
</success_criteria>

<output>
Create `.planning/phases/72-per-entry-compute-binding-fail-fast-retirement/72-01-SUMMARY.md` when done.
</output>
