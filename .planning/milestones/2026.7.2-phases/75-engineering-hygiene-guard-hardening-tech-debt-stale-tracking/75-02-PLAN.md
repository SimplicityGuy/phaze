---
phase: 75-engineering-hygiene-guard-hardening-tech-debt-stale-tracking
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/shared/routers/test_pipeline.py
autonomous: true
requirements: [HYG-04]
must_haves:
  truths:
    - "Force-local True on POST /api/v1/analyze routes a _LONG DISCOVERED file LOCAL — zero AWAITING_CLOUD rows held (gate pipeline.py:396, D-09/D-10)"
    - "Force-local True on POST /pipeline/analyze routes a _LONG DISCOVERED file LOCAL — zero AWAITING_CLOUD rows held (gate pipeline.py:718)"
    - "Force-local True on POST /pipeline/backfill-cloud is a ZERO-mutation no-op — ANALYSIS_FAILED row unchanged, no SchedulingLedger seed, nothing enqueued (gate pipeline.py:793)"
    - "Force-local False control: the same _LONG file IS held AWAITING_CLOUD (registry honored) — proving the toggle is the only variable (D-10)"
    - "All 3 gate sites are exercised at real-route altitude via the client AsyncClient fixture, driven by a persisted RouteControl(id='global', force_local=True) row (D-09)"
    - "This region CONSOLIDATES + EXTENDS existing coverage: tests/shared/routers/test_routing.py::test_route_forced_local_no_hold already covered gate L396 (POST /api/v1/analyze, cloud-ON registry + force_local=True → awaiting_cloud == 0); per D-09 the consolidated test lives in test_pipeline.py and adds the genuinely-uncovered L718 + L793 sites — no gate-site case is dropped"
  artifacts:
    - path: "tests/shared/routers/test_pipeline.py"
      provides: "force-local duration-router gate regression region (4 cases)"
      contains: "force_local"
  key_links:
    - from: "tests/shared/routers/test_pipeline.py"
      to: "RouteControl(id='global', force_local=True) on the shared session"
      via: "client fixture overrides get_session to the same session"
      pattern: "RouteControl\\(id=.global."
    - from: "get_route_control(session) in pipeline.py"
      to: "effective_cloud_enabled fold"
      via: "seeded row read at the gate line"
      pattern: "effective_cloud_enabled"
---

<objective>
Add the one genuine deliverable of Phase 75: a committed regression test covering the force-local
duration-router gate at all three live gate sites (`pipeline.py:396`, `:718`, `:793`). The test drives the
REAL FastAPI endpoints through the `client` AsyncClient fixture with a persisted
`RouteControl(force_local=True)` row, proving that when force-local is engaged every file routes local with
ZERO `AWAITING_CLOUD` holds and the backfill is a zero-mutation no-op — and that with force-local OFF the
same long file IS held for the cloud drain (the toggle is the only variable).

Coverage baseline (research correction): gate **L396** already had PARTIAL coverage via
`tests/shared/routers/test_routing.py::test_route_forced_local_no_hold` (cloud-ON `[_COMPUTE_BACKEND]`
registry + `force_local=True` → `awaiting_cloud == 0`). Per D-09 this plan CONSOLIDATES that coverage into
`test_pipeline.py` and EXTENDS it to the genuinely-uncovered `L718` and `L793` gate sites. All three
gate-site cases (L396, L718, L793) plus the False control are kept — none is dropped.

Purpose: Guards the subtle T-71-08 regression (a long file silently held in `AWAITING_CLOUD`, or a backfill
mutating state, under force-local) that a routing-count-only assertion would miss.
Output: A new force-local test region in `tests/shared/routers/test_pipeline.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/75-engineering-hygiene-guard-hardening-tech-debt-stale-tracking/75-CONTEXT.md
@.planning/phases/75-engineering-hygiene-guard-hardening-tech-debt-stale-tracking/75-RESEARCH.md
@.planning/phases/75-engineering-hygiene-guard-hardening-tech-debt-stale-tracking/75-VALIDATION.md
@tests/shared/routers/test_pipeline.py
@tests/shared/routers/test_routing.py
@src/phaze/routers/pipeline.py

<interfaces>
<!-- Extracted from the live worktree this session. Use these directly — no exploration needed. -->

Gate sites under test (src/phaze/routers/pipeline.py — re-grep before editing, lines may drift):
- L396  trigger_analysis        (POST /api/v1/analyze):  effective_cloud_enabled = settings.cloud_enabled and not await get_route_control(session)
- L718  trigger_analysis_ui     (POST /pipeline/analyze): same fold → _route_discovered_by_duration
- L793  trigger_backfill_cloud  (POST /pipeline/backfill-cloud): `if not settings.cloud_enabled or await get_route_control(session): <zero-mutation no-op>`

Existing partial coverage to CONSOLIDATE + EXTEND (research correction — do NOT re-grep only test_pipeline.py):
- tests/shared/routers/test_routing.py::test_route_forced_local_no_hold (~L112) ALREADY drives POST /api/v1/analyze
  with `monkeypatch.setattr(settings, "backends", [_COMPUTE_BACKEND])` (cloud ON) + a seeded force_local=True row,
  asserting `data["awaiting_cloud"] == 0` and the long file's state != AWAITING_CLOUD. That is prior PARTIAL
  coverage of gate L396. Gates L718 and L793 are the genuinely-uncovered sites. Per D-09 the consolidated
  regression region lives in test_pipeline.py; keep all three gate-site cases + the False control.

Force-local toggle:
- get_route_control(session) is in src/phaze/services/route_control.py — reads the single RouteControl 'global' row, returns force_local (False when absent). NEVER raises.
- RouteControl model: src/phaze/models/route_control.py — RouteControl(id: str PK (use "global"), force_local: bool).
- IMPORTANT (research correction): there is NO `set_route_control(...)` service function — it is fictional. Seed the toggle by directly inserting `RouteControl(id="global", force_local=True)` onto the shared `session` and `await session.commit()` BEFORE the POST. The `client` fixture overrides get_session to that same session (conftest ~L216); the RouteControl table is created by Base.metadata.create_all (conftest ~L193). No migration wiring or monkeypatch of the reader.

Existing helpers/fixtures/constants in tests/shared/routers/test_pipeline.py (REUSE — build no new harness):
- _LONG = 6000.0 (>= cloud_route_threshold_sec 5400); _SHORT = 100.0
- autouse fixture `_cloud_compute_registry` pins settings.backends = [_COMPUTE_BACKEND] (cloud ON) — KEEP it so the toggle is the ONLY thing forcing local. (_LOCAL_BACKEND is the all-local driver used by the existing tests — do NOT swap to it.)
- async _persist_files_with_duration(session, [_LONG]) -> list[FileRecord]   (DISCOVERED files w/ duration)
- async _persist_failed_with_duration(session, [_LONG], with_ledger=...) -> list[FileRecord]  (ANALYSIS_FAILED files)
- async seed_active_agent(session, "nox", kind="fileserver") / (session, "cloud", kind="compute")
- wire_fakes(client) -> capture list;  install_fake_queues(client);  async _drain_background()
- FileState.AWAITING_CLOUD / FileState.ANALYSIS_FAILED / FileState.DISCOVERED
- SchedulingLedger model (key == f"process_file:{id}")

Model this test region byte-for-byte on the EXISTING pair:
- test_backfill_disabled_when_cloud_local (~L882): the zero-mutation no-op assertion idiom (capture == [], refresh + state stays ANALYSIS_FAILED, SchedulingLedger select == []). Swap its `monkeypatch.setattr(settings,"backends",[_LOCAL_BACKEND])` driver for the persisted RouteControl(force_local=True) row while KEEPING the autouse cloud-ON registry.
- test_analyze_long_file_held_awaiting_cloud_even_with_compute_online (~L438): the AWAITING_CLOUD-hold assertion idiom — reuse for the force-local False control (which must STILL hold).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Force-local analyze gates (L396 + L718) + False control — analyze cases</name>
  <files>tests/shared/routers/test_pipeline.py</files>
  <read_first>
    - tests/shared/routers/test_pipeline.py (fixtures/helpers L40-120; _LONG/_SHORT + duration helpers L388-520; the hold test ~L438)
    - tests/shared/routers/test_routing.py (test_route_forced_local_no_hold ~L112 — the existing partial L396 coverage this region consolidates)
    - src/phaze/routers/pipeline.py (trigger_analysis L372-408; trigger_analysis_ui L693-724 — the two gate folds L396/L718)
    - src/phaze/services/route_control.py (get_route_control reader) + src/phaze/models/route_control.py (RouteControl model)
    - .planning/phases/75-.../75-VALIDATION.md (Observable signals; Sampling adequacy)
  </read_first>
  <behavior>
    - force_local_analyze_api (gate L396): autouse cloud-ON registry + a persisted RouteControl(id="global", force_local=True) row + an online fileserver agent (seed_active_agent "nox", kind="fileserver") + a _LONG DISCOVERED file → POST /api/v1/analyze → after _drain_background, `SELECT FileRecord WHERE state == AWAITING_CLOUD` returns ZERO rows; the long file is NOT held (it routes local / is only ever skipped for no-agent reasons, never held) — byte-identical outcome to an all-local registry.
    - force_local_analyze_ui (gate L718): same setup and same assertions driven through POST /pipeline/analyze (distinct endpoint/handler, same fold — must be exercised separately because it is a physically separate gate line).
    - force_local_analyze_api_false_control (control): SAME cloud-ON registry, NO RouteControl row (or force_local=False), compute agent online, same _LONG file → POST /api/v1/analyze → the long file IS held in AWAITING_CLOUD (registry honored). Proves the persisted toggle — not some other condition — drives the local routing.
    - Anti-cheat: each True case must fail if the `and not await get_route_control(session)` clause were removed from the gate (i.e., assert AWAITING_CLOUD ABSENCE, never a bare enqueue count).
  </behavior>
  <action>
    Add a new force-local test region to `tests/shared/routers/test_pipeline.py`. Name the cases so
    `-k force_local_analyze_api`, `-k force_local_analyze_ui`, and `-k force_local` select them. Seed the
    toggle with a DIRECT `RouteControl(id="global", force_local=True)` row on the shared `session` +
    `await session.commit()` BEFORE the POST — do NOT monkeypatch get_route_control and do NOT call the
    fictional `set_route_control`. KEEP the autouse `_cloud_compute_registry` (cloud ON) so the toggle is the
    sole force-local driver. Reuse `_persist_files_with_duration([_LONG])`, `seed_active_agent`, `wire_fakes` /
    `install_fake_queues`, `_drain_background`. Assert AWAITING_CLOUD ABSENCE via a
    `select(FileRecord).where(FileRecord.state == FileState.AWAITING_CLOUD)` scalars → `== []`, NOT a routing
    count (RESEARCH Pitfall 2). Use `@pytest.mark.asyncio` and the `client`/`session` fixtures exactly as the
    neighbouring tests do. Type-hint every helper/function (mypy runs on non-test excludes only, but keep the
    file's existing typed style). No `src/` change.

    Coverage note (research correction): gate **L396** already had PARTIAL coverage via
    `tests/shared/routers/test_routing.py::test_route_forced_local_no_hold` (cloud-ON registry + force_local=True
    → `awaiting_cloud == 0`). Per D-09 this region consolidates that coverage into `test_pipeline.py` and
    extends it to the genuinely-uncovered `L718` (and, in Task 2, `L793`) sites. Keep the L396 analyze case
    here anyway — do NOT drop it just because `test_routing.py` overlaps; the consolidated regression region is
    the committed home per D-09. A brief docstring/comment on the analyze-api case should cite the
    `test_routing.py::test_route_forced_local_no_hold` cross-reference.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/routers/test_pipeline.py -k "force_local_analyze" -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/shared/routers/test_pipeline.py -k force_local_analyze_api -x` passes (gate L396 covered).
    - `uv run pytest tests/shared/routers/test_pipeline.py -k force_local_analyze_ui -x` passes (gate L718 covered).
    - The False-control case passes and asserts the SAME _LONG file IS held AWAITING_CLOUD with the toggle off.
    - Each True case seeds `RouteControl(id="global", force_local=True)` via a direct row insert + commit (grep: `RouteControl(id="global"` present) — no `set_route_control`, no monkeypatch of `get_route_control`.
    - The autouse `_cloud_compute_registry` (cloud-ON `[_COMPUTE_BACKEND]`) is NOT overridden to `[_LOCAL_BACKEND]` in the True cases.
    - The analyze-api case carries a cross-reference to `test_routing.py::test_route_forced_local_no_hold` (docstring/comment) noting the consolidation.
    - `git diff --stat -- src/` is EMPTY.
  </acceptance_criteria>
  <done>Both analyze gate sites (L396, L718) covered force-local-True with zero-AWAITING_CLOUD assertions, plus a force-local-False control proving the toggle is the only variable; L396 consolidation cross-referenced to test_routing.py; src unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Force-local backfill gate (L793) zero-mutation no-op case</name>
  <files>tests/shared/routers/test_pipeline.py</files>
  <read_first>
    - tests/shared/routers/test_pipeline.py (backfill region L636-916; model on test_backfill_disabled_when_cloud_local ~L882)
    - src/phaze/routers/pipeline.py (trigger_backfill_cloud L763-813 — the L793 early-return + T-71-08 comment L789-793)
    - .planning/phases/75-.../75-CONTEXT.md (D-10; Specific Ideas — AWAITING_CLOUD absence, T-71-08)
  </read_first>
  <behavior>
    - force_local_backfill (gate L793): autouse cloud-ON registry + persisted RouteControl(id="global", force_local=True) + a compute agent online + an ANALYSIS_FAILED ∧ _LONG candidate (`_persist_failed_with_duration([_LONG])`) → POST /pipeline/backfill-cloud → response 200; after _drain_background: the capture list is empty (nothing enqueued); the candidate row STAYS FileState.ANALYSIS_FAILED (never reset to DISCOVERED / never held AWAITING_CLOUD); `select(SchedulingLedger).where(key == f"process_file:{id}")` returns `== []` (no ledger seed). Byte-identical to test_backfill_disabled_when_cloud_local, but the force-local ROW (not an all-local registry) drives the no-op.
    - Anti-cheat: the case must fail if the `or await get_route_control(session)` clause were removed from the L793 early-return.
  </behavior>
  <action>
    Add the backfill force-local case to the same region. Name it so `-k force_local_backfill` selects it.
    Reuse the `test_backfill_disabled_when_cloud_local` assertion idiom EXACTLY (`capture == []`,
    `await session.refresh(row)` → state stays `ANALYSIS_FAILED`, `SchedulingLedger` select → `== []`) but
    keep the autouse cloud-ON registry and drive the no-op with a persisted
    `RouteControl(id="global", force_local=True)` row (+ commit) instead of
    `monkeypatch.setattr(settings, "backends", [_LOCAL_BACKEND])`. Seed a compute + fileserver agent as the
    neighbouring backfill tests do. This L793 site was genuinely uncovered before this phase (test_routing.py
    covered only L396). No `src/` change.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/routers/test_pipeline.py -k "force_local" -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/shared/routers/test_pipeline.py -k force_local_backfill -x` passes (gate L793 covered).
    - The case asserts ALL THREE zero-mutation signals: `capture == []`, row stays `ANALYSIS_FAILED`, and `SchedulingLedger` select `== []`.
    - `uv run pytest tests/shared/routers/test_pipeline.py -k force_local -x` passes (all 4 force-local cases green together).
    - `uv run pytest tests/shared/routers/test_pipeline.py` passes (no regression in the file).
    - `git diff --stat -- src/` is EMPTY.
  </acceptance_criteria>
  <done>Backfill gate site (L793) covered force-local-True with the three zero-mutation assertions; full test file green; src unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. This plan adds test code only (`tests/shared/routers/test_pipeline.py`); there is NO
`src/` behavior change, no new endpoint, no new input/parser, no auth/session/crypto change. The force-local
toggle it EXERCISES (not adds) is an internal-realm operator control shipped in Phase 71 behind the
reverse-proxy internal boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-02-NA | — | new regression test | accept | No new attack surface — test-only, drives already-validated routes; no new inputs/auth/crypto (RESEARCH Security Domain: N/A; ASVS V5/V6 do not apply). |
| T-75-SC | Tampering | package installs | accept | This plan installs ZERO packages (`uv sync` already provides pytest/pytest-asyncio/httpx). No legitimacy gate applies. |
</threat_model>

<verification>
- `uv run pytest tests/shared/routers/test_pipeline.py -k force_local -x` — all 4 force-local cases pass.
- `uv run pytest tests/shared/routers/test_pipeline.py` — full file green (no regression).
- All 3 gate sites (L396, L718, L793) each covered by a force-local-True case + the analyze False control.
- L396 consolidation cross-referenced to the pre-existing `test_routing.py::test_route_forced_local_no_hold`.
- `git diff --stat -- src/` is EMPTY.
</verification>

<success_criteria>
- Force-local True → zero AWAITING_CLOUD on both analyze endpoints + zero-mutation backfill no-op.
- Force-local False control → the same _LONG file IS held AWAITING_CLOUD (toggle is the only variable).
- Real-route altitude via the `client` AsyncClient fixture + a persisted RouteControl row (no monkeypatch, no unit-call).
- Coverage consolidated per D-09: L396 (was partially covered in test_routing.py) + the newly-covered L718/L793 all live in test_pipeline.py; no gate-site case dropped.
- Zero `src/` diff.
</success_criteria>

<output>
Create `.planning/phases/75-engineering-hygiene-guard-hardening-tech-debt-stale-tracking/75-02-SUMMARY.md` when done
</output>
</content>
</invoke>
