---
phase: 82-counts-pending-set-cutover
plan: 04
type: execute
wave: 4
depends_on: [82-02, 82-03]
files_modified:
  - scripts/seed_perf_corpus.py
  - justfile
  - .planning/phases/82-counts-pending-set-cutover/82-VERIFICATION.md
autonomous: true
requirements: [PERF-02]
must_haves:
  truths:
    - "The /pipeline/stats endpoint latency at a ~200K synthetic corpus (migration HEAD ≥036) is measured and recorded in VERIFICATION regardless of pass/fail"
    - "EXPLAIN ANALYZE confirms the 032 partial indexes are scanned (Index/Index-Only Scan, not Seq Scan) on the three pending queries + the four-bucket query"
    - "The DENORM-01 go/no-go decision is recorded, licensed by the measured number against the < ~1s budget"
  artifacts:
    - path: "scripts/seed_perf_corpus.py"
      provides: "idempotent, parameterized ~200K synthetic corpus seeder (uv-run standalone)"
      contains: "async def"
    - path: "justfile"
      provides: "perf-seed / perf-explain (or perf-stats-bench) recipes in the db group"
      contains: "perf-"
    - path: ".planning/phases/82-counts-pending-set-cutover/82-VERIFICATION.md"
      provides: "recorded PERF-02 numbers + index-scan evidence + D-02 ≥036 gate + DENORM-01 decision"
      contains: "PERF-02"
  key_links:
    - from: "seed_perf_corpus.py"
      to: "migration HEAD (≥036)"
      via: "seeds at alembic head so the 032 partial indexes exist"
      pattern: "alembic|db-upgrade|head"
    - from: "EXPLAIN ANALYZE"
      to: "032 partial indexes"
      via: "Index Scan on ix_fprint_success / ix_analysis_completed / ix_analysis_failed / ix_metadata_failed / ix_cloud_job_awaiting"
      pattern: "ix_(fprint_success|analysis_completed|metadata_failed|cloud_job_awaiting)"
---

<objective>
Measure the `/pipeline/stats` poll latency at 200K-file scale on a local synthetic corpus at migration HEAD (≥036), record the EXPLAIN ANALYZE plans + endpoint timing in the phase VERIFICATION, and make the DENORM-01 go/no-go call (PERF-02, D-06, D-07). The recorded numbers are the deliverable — they license the YAGNI decision to NOT build the denormalized stage-bitmap column.

Purpose: prove the derived read queries stay fast at scale (or surface that they do not) using the real cut-over query shapes from Plans 02+03, on a corpus that has the 032 partial indexes the anti-joins ride. A live prod probe is invalid for this measurement (prod ~031 lacks the 032 indexes → pessimistic plans, D-06).

Output: `scripts/seed_perf_corpus.py`, `just perf-*` recipes, and the recorded PERF-02 section in VERIFICATION.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-counts-pending-set-cutover/82-CONTEXT.md
@.planning/phases/82-counts-pending-set-cutover/82-RESEARCH.md
@.planning/phases/82-counts-pending-set-cutover/82-PATTERNS.md
@.planning/phases/82-counts-pending-set-cutover/82-VALIDATION.md
@.planning/phases/82-counts-pending-set-cutover/82-02-SUMMARY.md
@.planning/phases/82-counts-pending-set-cutover/82-03-SUMMARY.md

<interfaces>
<!-- Contracts extracted from main. Use directly. -->

Seed shape (D-06, realistic mid-pipeline selectivity over ~200K music/video FileRecords at HEAD):
  ~70% metadata row (some failed_at) · ~55% fingerprint_results success · ~40% analysis with analysis_completed_at ·
  ~5% analyze failed_at (terminal) · ~1% cloud_job awaiting/pushing/pushed · ~2% dedup_resolution markers ·
  few-thousand scheduling_ledger rows (in-flight). Seed FileRecord.state too (dual-write realism → shadow gate stays green).

Hot queries to EXPLAIN ANALYZE (the real cut-over shapes from Plans 02/03):
  get_metadata_pending_files · get_fingerprint_pending_files · get_discovered_files_with_duration (with LEFT JOIN) ·
  the three GROUP BY stage_status_case(stage) four-bucket queries · the full GET /pipeline/stats endpoint (fans out many reads).

032 partial index names to confirm in the plans (alembic/versions/032_add_derived_status_schema.py:150-156):
  ix_fprint_success, ix_analysis_completed, ix_analysis_failed, ix_metadata_failed, ix_cloud_job_awaiting.

Analog shapes: scripts/coverage_floor.py (uv-run standalone script) · justfile [group('db')] recipes (db-upgrade/db-revision/db-current ~:458) + test-db (~:136) + integration-test (~:209).

Env footguns: test-DB port 5433, test-Redis 6380; export BOTH TEST_DATABASE_URL and MIGRATIONS_TEST_DATABASE_URL. Run the seed/bench in ISOLATION (full-suite colima flake). Do NOT probe live lux for the plan measurement (Alembic ~031, no 032 indexes → invalid plan).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: seed_perf_corpus.py + just perf recipes</name>
  <files>scripts/seed_perf_corpus.py, justfile</files>
  <read_first>
    - scripts/coverage_floor.py — the uv-run standalone-script shape (arg parsing, async entry, session construction).
    - justfile — the [group('db')] recipes (db-upgrade/db-revision/db-current) + test-db + integration-test as the recipe-authoring pattern; confirm the ports (5433/6380).
    - src/phaze/models/ — FileRecord, FileMetadata, FingerprintResult, AnalysisResult, CloudJob, DedupResolution, SchedulingLedger column contracts for the bulk inserts.
    - alembic/versions/032_add_derived_status_schema.py:150-156 — the partial index names + their exact predicates (so the seed distribution actually exercises them).
    - .planning/phases/82-counts-pending-set-cutover/82-PATTERNS.md — the seed_perf_corpus.py section (constraints + EXPLAIN targets).
  </read_first>
  <action>
    Create `scripts/seed_perf_corpus.py` — an idempotent, uv-run standalone that takes `--n` (default 200000) and bulk-inserts ~N music/video `FileRecord` rows plus output-table rows distributed to the D-06 selectivity profile (metadata ~70% incl. some failed_at, fingerprint success ~55%, analysis-with-completed_at ~40%, analyze failed ~5%, cloud_job awaiting/pushing/pushed ~1%, dedup markers ~2%, a few thousand scheduling_ledger in-flight rows), and stamps `FileRecord.state` consistently (dual-write realism so `just shadow-compare` stays green). Use asyncpg `executemany` / batched inserts with PARAMETERIZED values only — never string-interpolated SQL — to keep seeding in minutes, not hours. Make it safe to re-run (idempotent by deterministic sha256/id, or a truncate-and-reseed flag). Add `just perf-seed` (migrate to HEAD via db-upgrade, then run the seeder) and `just perf-explain` (or `perf-stats-bench`) recipes in the `[group('db')]` group that run EXPLAIN (ANALYZE, BUFFERS) on the five hot query shapes and time the full `/pipeline/stats` endpoint (httpx or `time curl`, N iterations, p50/p95). Recipes delegate to `uv run` / the script — no inline business logic (project rule: workflows use just).
  </action>
  <verify>
    <automated>uv run python scripts/seed_perf_corpus.py --help 2>&1 | grep -qi "n" && uv run ruff check scripts/seed_perf_corpus.py</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/seed_perf_corpus.py --n 1000` runs against the test-DB and produces the target row distribution (counts at small N match the selectivity profile within tolerance).
    - The script uses only parameterized inserts (no f-string SQL) — a grep for SQL-in-f-string shows none (T-42-03).
    - Re-running the seeder does not duplicate/corrupt rows (idempotent or explicit reseed flag).
    - `just perf-seed` and `just perf-explain` (or `perf-stats-bench`) exist in the db group and delegate to `uv run`.
    - `uv run ruff check scripts/seed_perf_corpus.py` clean; the justfile recipes pass formatting/shellcheck as applicable.
  </acceptance_criteria>
  <done>The seeder + recipes exist, produce the realistic distribution at small N, and use only parameterized inserts.</done>
</task>

<task type="auto">
  <name>Task 2: Run the 200K bench, record numbers + index evidence + DENORM-01 call in VERIFICATION</name>
  <files>.planning/phases/82-counts-pending-set-cutover/82-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/82-counts-pending-set-cutover/82-RESEARCH.md — OQ5 (methodology, lines 340-358), D-07 (< ~1s budget), Pitfall 5 (live-vs-synthetic).
    - .planning/phases/82-counts-pending-set-cutover/82-VALIDATION.md — the Manual-Only Verifications table (200K bench + D-02 lux probe).
    - .planning/phases/82-counts-pending-set-cutover/82-CONTEXT.md — D-02 (verify ≥036 + zero analyzed-NULL) and the "measurement is a deliverable" note.
  </read_first>
  <action>
    Bring up the local test-DB (`just test-db`), migrate to HEAD (`just db-upgrade`, confirm ≥036), seed ~200K (`just perf-seed` / `uv run python scripts/seed_perf_corpus.py --n 200000`), then run `just perf-explain`. Capture, for each of the five hot query shapes, the EXPLAIN (ANALYZE, BUFFERS) actual time + rows AND confirm an Index Scan / Index-Only Scan on the relevant 032 partial index (ix_fprint_success / ix_analysis_completed / ix_analysis_failed / ix_metadata_failed / ix_cloud_job_awaiting) — NOT a Seq Scan. Time the full `GET /pipeline/stats` endpoint (p50/p95). Write a PERF-02 section into `82-VERIFICATION.md` recording: the seed parameters, every measured number (regardless of pass/fail), the index-scan evidence per query, the pass/fail against the `< ~1s` budget (D-07), and the DENORM-01 go/no-go decision (defer if within budget — YAGNI; pull forward only if over budget). Also record the D-02 deploy-target gate as DEPLOYMENT-GATED: the analyze pending-set flip is trusted only once the target is at Alembic ≥036 AND `COUNT(files WHERE state='analyzed' AND analysis_completed_at IS NULL AND failed_at IS NULL) = 0` (prod currently ~031 — note this must be re-run read-only against lux at homelab rollout, per the VALIDATION Manual-Only table).
  </action>
  <verify>
    <automated>grep -qi "PERF-02" .planning/phases/82-counts-pending-set-cutover/82-VERIFICATION.md && grep -Eqi "ix_(fprint_success|analysis_completed|metadata_failed|cloud_job_awaiting)" .planning/phases/82-counts-pending-set-cutover/82-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `82-VERIFICATION.md` contains a PERF-02 section with: seed parameters, EXPLAIN ANALYZE actual-time numbers for all five hot shapes, per-query index-scan evidence (named 032 indexes, not Seq Scan), the `/pipeline/stats` p50/p95, and the explicit pass/fail vs the `< ~1s` budget.
    - The DENORM-01 go/no-go decision is stated with the measured number as its justification (YAGNI-defer on pass; pull-forward only on fail).
    - The D-02 deploy-target gate is recorded as deployment-gated (≥036 + zero analyzed-NULL, re-run read-only at homelab rollout — prod ~031 today).
    - The measurement was taken on a local corpus at migration HEAD (NOT live lux) — stated explicitly (Pitfall 5).
  </acceptance_criteria>
  <done>PERF-02 numbers + index evidence + DENORM-01 decision + D-02 deployment-gated note are recorded in VERIFICATION.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script → test DB | The seeder writes bulk synthetic rows to a local ephemeral DB; must never touch prod and must use parameterized inserts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-07 | Tampering | seed_perf_corpus.py bulk inserts | mitigate | Parameterized `executemany` / bound params only; no f-string SQL (T-42-03). Runs against the local test-DB (5433), never prod. |
| T-82-08 | Denial of Service (accidental prod probe) | PERF-02 measurement | mitigate | D-06: measure ONLY on the local synthetic corpus at HEAD; live lux is read-only COUNT sanity-check only, never the EXPLAIN target. |
| T-82-DDL | Tampering | schema | accept | No new migration; the seeder runs against existing HEAD schema (≥036). No DDL authored here. |
</threat_model>

<verification>
- `scripts/seed_perf_corpus.py` runs at small N producing the target distribution; `just perf-seed` / `just perf-explain` exist and delegate to `uv run`.
- `82-VERIFICATION.md` records the PERF-02 numbers, index-scan evidence, pass/fail vs `< ~1s`, DENORM-01 decision, and the deployment-gated D-02 gate.
- `uv run ruff check scripts/seed_perf_corpus.py` clean; justfile formatting/shellcheck clean.
- The recorded numbers + DENORM-01 call are surfaced for operator review at `/gsd:verify-work`.
</verification>

<success_criteria>
- The `/pipeline/stats` poll latency at 200K is measured and recorded in VERIFICATION regardless of pass/fail (PERF-02).
- EXPLAIN ANALYZE confirms the 032 partial indexes are used on the derived hot queries; DENORM-01 stays deferred unless the measurement exceeds the `< ~1s` budget (YAGNI).
</success_criteria>

<output>
Create `.planning/phases/82-counts-pending-set-cutover/82-04-SUMMARY.md` when done. The recorded EXPLAIN ANALYZE numbers are the phase deliverable that licenses the DENORM-01 YAGNI decision — include them (or a pointer to the VERIFICATION section) in the SUMMARY.
</output>
</content>
