---
phase: 84-dedup-fingerprint-progress-cutover
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/services/stage_status.py
  - src/phaze/models/dedup_resolution.py
autonomous: true
requirements: [READ-04, SIDECAR-02]
must_haves:
  truths:
    - "D-13: the dedup-resolved predicate is a file-level ColumnElement[bool] living in services/stage_status.py, consumed by services/dedup.py (module-level) and services/fingerprint.py (function-local)"
    - "D-13: the predicate stays OUT of the Stage dispatch ladders (done_clause/failed_clause/inflight_clause/domain_completed_clause/stage_status_case) — it takes NO stage argument and does not touch the Phase-78 equivalence test"
    - "D-08: models/dedup_resolution.py docstring documents that scan_deletion.py's dual-FK delete un-resolves a keeper's duplicates when the keeper's scan batch is deleted (so it is not rediscovered as a bug)"
  artifacts:
    - path: "src/phaze/services/stage_status.py"
      provides: "dedup_resolved_clause() correlated exists(...) marker predicate + DedupResolution import"
      contains: "def dedup_resolved_clause("
    - path: "src/phaze/models/dedup_resolution.py"
      provides: "D-08 scan_deletion behavior note in the class/module docstring"
      contains: "scan_deletion"
  key_links:
    - from: "src/phaze/services/stage_status.py::dedup_resolved_clause"
      to: "dedup_resolution table"
      via: "exists(select(DedupResolution.id).where(DedupResolution.file_id == FileRecord.id))"
      pattern: "exists\\(select\\(DedupResolution"
---

<objective>
Add the single-source **file-level** dedup-resolved predicate `dedup_resolved_clause()` to
`services/stage_status.py` (D-13) — the module Phase 78 established as the one home for predicates
derived from output tables — and document the `scan_deletion` dual-FK behavior in
`models/dedup_resolution.py` (D-08). This predicate is the shared clause both the Wave-2 dedup readers
(`~dedup_resolved_clause()`) and `get_fingerprint_progress`'s denominator consume; landing it in Wave 1
lets the two Wave-2 plans run in parallel without an add/add conflict on `stage_status.py`.

Purpose: dedup is not a `Stage`, so it must NOT enter the drift-locked `Stage` dispatch ladders; it is a
correlated marker-existence predicate over `FileRecord`.
Output: `dedup_resolved_clause()` in `stage_status.py`; the D-08 docstring note in the model.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-dedup-fingerprint-progress-cutover/84-CONTEXT.md
@.planning/phases/84-dedup-fingerprint-progress-cutover/84-RESEARCH.md
@.planning/phases/84-dedup-fingerprint-progress-cutover/84-PATTERNS.md

@src/phaze/services/stage_status.py
@src/phaze/models/dedup_resolution.py
@src/phaze/services/shadow_compare.py
@src/phaze/services/scan_deletion.py

<interfaces>
<!-- The house-style correlated-exists idiom this predicate reproduces (already present twice) -->
From services/stage_status.py (done_clause, FINGERPRINT branch):
  exists(select(FingerprintResult.id).where(FingerprintResult.file_id == FileRecord.id, FingerprintResult.status.in_(_DONE_FP)))
From services/shadow_compare.py:85-87 (identical body, private copy — reproduce it as the new public clause):
  def _dedup_exists() -> ColumnElement[bool]:
      return exists(select(DedupResolution.id).where(DedupResolution.file_id == FileRecord.id))

<!-- CONTRACT this plan publishes for Wave-2 consumers (84-03 dedup readers, 84-04 fingerprint) -->
  def dedup_resolved_clause() -> ColumnElement[bool]   # NO stage arg; correlates to FileRecord
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dedup_resolved_clause() to stage_status.py (D-13)</name>
  <files>src/phaze/services/stage_status.py</files>
  <read_first>
    - src/phaze/services/stage_status.py (model imports ~:66-73; done_clause/failed_clause house style ~:89-147; the Stage dispatch ladders inflight_clause/domain_completed_clause/stage_status_case — the predicate must NOT go into these)
    - src/phaze/services/shadow_compare.py (~:85-87 _dedup_exists — the exact body to reproduce)
    - .planning/phases/84-dedup-fingerprint-progress-cutover/84-CONTEXT.md (D-13)
  </read_first>
  <action>
    In `services/stage_status.py` add `from phaze.models.dedup_resolution import DedupResolution` to the
    model-import block (~:66-73). Add a module-level function `def dedup_resolved_clause() ->
    ColumnElement[bool]:` that takes NO `stage` argument and returns
    `exists(select(DedupResolution.id).where(DedupResolution.file_id == FileRecord.id))` — the correlated
    marker-existence predicate (existence = resolved), identical to `shadow_compare._dedup_exists`. Give
    it a docstring stating it is a FILE-LEVEL predicate (not a `Stage`), that `~dedup_resolved_clause()`
    means "not resolved", and that it is deliberately kept out of the `Stage` dispatch ladders. Do NOT
    add it to `done_clause`/`failed_clause`/`inflight_clause`/`domain_completed_clause`/
    `stage_status_case` — all raise `ValueError` on unknown stages and are drift-locked by
    `tests/integration/test_stage_status_equivalence.py`; a non-`Stage` clause must not touch that test.
    Do not modify `shadow_compare.py` (its private copy is harmless; refactoring it is optional and out of
    scope here).
  </action>
  <verify>
    <automated>uv run python -c "from phaze.services.stage_status import dedup_resolved_clause; from sqlalchemy import ColumnElement; c=dedup_resolved_clause(); assert 'dedup_resolution' in str(c.compile(compile_kwargs={'literal_binds': False}))"</automated>
  </verify>
  <acceptance_criteria>
    - `services/stage_status.py` contains `def dedup_resolved_clause(` with no `stage` parameter.
    - The compiled clause SQL references `dedup_resolution` and correlates on `file_id = files.id` (a correlated `EXISTS`, not an outer-join / negated-membership).
    - The Phase-78 equivalence test still passes unchanged: `uv run pytest tests/integration/test_stage_status_equivalence.py -x` exits 0 (run with `MIGRATIONS_TEST_DATABASE_URL`/test DB as that suite requires).
    - `dedup_resolved_clause` does NOT appear inside any `if stage is` branch of `done_clause`/`failed_clause`/`inflight_clause`/`domain_completed_clause`/`stage_status_case`.
    - `uv run ruff check .` and `uv run mypy .` exit 0.
  </acceptance_criteria>
  <done>dedup_resolved_clause() exists as a stage-less file-level correlated exists predicate; equivalence test unaffected; ruff+mypy clean.</done>
</task>

<task type="auto">
  <name>Task 2: Document scan_deletion dual-FK behavior in the model (D-08)</name>
  <files>src/phaze/models/dedup_resolution.py</files>
  <read_first>
    - src/phaze/models/dedup_resolution.py (the existing module + class docstring — the note lands here)
    - src/phaze/services/scan_deletion.py (~:102-109 — the dual-FK delete: DedupResolution.file_id.in_(batch) | DedupResolution.canonical_file_id.in_(batch))
    - .planning/phases/84-dedup-fingerprint-progress-cutover/84-CONTEXT.md (D-08)
  </read_first>
  <action>
    In `models/dedup_resolution.py`, add a docstring note (no code change) recording that
    `services/scan_deletion.py`'s marker delete matches EITHER FK
    (`file_id IN batch | canonical_file_id IN batch`), so deleting the scan batch that holds a *keeper*
    (canonical) file un-resolves its duplicates — they reappear for re-review. Note that this is
    accepted deliberately (D-08): the keeper is gone, so "keep this one, drop those" no longer holds; the
    failure mode is safe; and D-03 (go-forward writes populate `canonical_file_id` with the operator's
    actual pick) exposes every go-forward resolution to this. This is documentation only — do not modify
    `scan_deletion.py`.
  </action>
  <verify>
    <automated>uv run python -c "import phaze.models.dedup_resolution as m; d=(m.__doc__ or '')+(m.DedupResolution.__doc__ or ''); assert 'scan_deletion' in d and 'canonical' in d"</automated>
  </verify>
  <acceptance_criteria>
    - `models/dedup_resolution.py` docstring mentions `scan_deletion` and the dual-FK un-resolve behavior.
    - `services/scan_deletion.py` is unchanged (git diff shows no change to that file).
    - `uv run ruff check src/phaze/models/dedup_resolution.py` exits 0.
  </acceptance_criteria>
  <done>Model docstring records the D-08 scan_deletion behavior; no code change to scan_deletion.py.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure predicate-builder + docstring change; no runtime input, no new I/O boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-02-01 | Tampering | `dedup_resolved_clause` SQL shape | accept | Correlated `exists(...)` over a static column set; no user input reaches the clause construction. The equivalence-test drift lock guards against a mis-placed predicate leaking into a `Stage` ladder. |
| T-84-02-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies. |
</threat_model>

<verification>
- `dedup_resolved_clause()` importable, compiles to a correlated `EXISTS` over `dedup_resolution`.
- Phase-78 equivalence test unaffected.
- Model docstring carries the D-08 note; `scan_deletion.py` untouched.
</verification>

<success_criteria>
The shared file-level dedup predicate exists in the single-source predicate module, stays out of the
drift-locked `Stage` ladders, and the `scan_deletion` behavior is documented — unblocking the two Wave-2
cutover plans to consume `dedup_resolved_clause()` in parallel.
</success_criteria>

<output>
Create `.planning/phases/84-dedup-fingerprint-progress-cutover/84-02-SUMMARY.md` when done.
</output>
