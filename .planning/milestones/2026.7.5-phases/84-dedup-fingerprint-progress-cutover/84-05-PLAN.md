---
phase: 84-dedup-fingerprint-progress-cutover
plan: 05
type: execute
wave: 3
depends_on: [84-03, 84-04]
files_modified:
  - tests/shared/test_dedup_fingerprint_source_scan.py
autonomous: true
requirements: [READ-04, SIDECAR-02]
must_haves:
  truths:
    - "D-14: the source-scan AST guard asserts FileState.DUPLICATE_RESOLVED appears in NO read position in services/dedup.py (inside a Compare, or as any argument — positional included — to where/filter/filter_by/having) and allows EXACTLY ONE occurrence: the RHS of an Assign to a *.state attribute (the surviving dual-writer, D-00a)"
    - "D-14: the guard asserts ZERO FileState.FINGERPRINTED occurrences in services/fingerprint.py (no writer there — clean absence)"
    - "D-14: an ast.walk scan (never a line-oriented grep — a grep false-positives on the surviving writer and misses positional .where(a,b,c) reads); the scan walks Call positional AND keyword args, matching ast.Attribute nodes whose value resolves to FileState"
    - "D-14: the guard is mutation-tested BOTH directions — reintroducing a read in a positional .where(a,b,c) arg goes RED; the surviving writer at dedup.py:268 stays GREEN (no false positive)"
    - "READ-04: the guard is the standing insurance that a FileRecord.state read is not reintroduced at a new site the behavioral tests do not exercise"
  artifacts:
    - path: "tests/shared/test_dedup_fingerprint_source_scan.py"
      provides: "AST source-scan guard over dedup.py (1 write allowed, 0 reads) + fingerprint.py (0 FINGERPRINTED)"
      contains: "ast.walk"
  key_links:
    - from: "tests/shared/test_dedup_fingerprint_source_scan.py"
      to: "src/phaze/services/dedup.py + src/phaze/services/fingerprint.py"
      via: "ast.parse(source) + ast.walk"
      pattern: "ast\\.(walk|parse)"
---

<objective>
Ship the DB-free AST source-scan guard (D-14 insurance) that asserts the `FileState` cutover in
`services/dedup.py` and `services/fingerprint.py` cannot silently regress: `dedup.py` may contain
`FileState.DUPLICATE_RESOLVED` in EXACTLY ONE place — the surviving dual-writer assignment (D-00a) — and
NOWHERE in a read/compare/`.where()` position; `fingerprint.py` may contain ZERO
`FileState.FINGERPRINTED`. Both guards are mutation-tested in both directions before the phase closes.

Purpose: the divergence + real-DB tests catch a wrong predicate at the sites they exercise; this guard
catches a `state` read reintroduced at a NEW site. It must survive the Phase-83 trap (a line-oriented
grep false-positives on the surviving writer and is blind to positional `.where(a, b, c)` reads), so it
is an `ast.walk` scan, not a grep. Runs after both source cutovers (Wave 2), so both assertions are
satisfiable.
Output: `tests/shared/test_dedup_fingerprint_source_scan.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-dedup-fingerprint-progress-cutover/84-CONTEXT.md
@.planning/phases/84-dedup-fingerprint-progress-cutover/84-RESEARCH.md
@.planning/phases/84-dedup-fingerprint-progress-cutover/84-PATTERNS.md

# The CORRECTED writer-guard model (walk **splat-aware); READ-scan is net-new — invert the direction
@tests/analyze/services/test_single_awaiting_writer.py
@src/phaze/services/dedup.py
@src/phaze/services/fingerprint.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AST source-scan guard over dedup.py + fingerprint.py (D-14)</name>
  <files>tests/shared/test_dedup_fingerprint_source_scan.py</files>
  <read_first>
    - tests/analyze/services/test_single_awaiting_writer.py (FULL — the corrected Phase-83 AST walker; note _values_call_writes_awaiting handling of keyword.arg is None (**splat); this guards a WRITE — Phase 84 inverts it to guard a READ)
    - src/phaze/services/dedup.py (post-84-03: the surviving writer f.state = FileState.DUPLICATE_RESOLVED at ~:268; no read occurrences)
    - src/phaze/services/fingerprint.py (post-84-04: zero FileState.FINGERPRINTED)
    - .planning/phases/84-dedup-fingerprint-progress-cutover/84-PATTERNS.md (§ CAUTIONARY ANALOG — the correct AST shape + both mutation directions)
    - .planning/phases/84-dedup-fingerprint-progress-cutover/84-RESEARCH.md (Pitfall 1; Contradictions Found)
  </read_first>
  <behavior>
    - dedup.py scan: match ast.Attribute nodes with .attr == "DUPLICATE_RESOLVED" whose value resolves to FileState. Exactly ONE is allowed — the RHS of an ast.Assign whose target ends in `.state` (the surviving writer). Every other occurrence is forbidden: inside a Compare node, or as any argument (positional OR keyword) to a Call whose func attr is in {where, filter, filter_by, having}. Assert count-of-reads == 0 and count-of-writer-assign == 1.
    - fingerprint.py scan: assert ZERO ast.Attribute nodes with .attr == "FINGERPRINTED" whose value resolves to FileState.
    - The scan uses ast.parse over the file source read from disk; it walks Call args (positional list AND keyword list) — never a line-oriented string match.
  </behavior>
  <action>
    Create `tests/shared/test_dedup_fingerprint_source_scan.py` (DB-free — `shared` bucket; scans source
    text only, no PG, no settings, no saq_jobs). Read `src/phaze/services/dedup.py` and
    `src/phaze/services/fingerprint.py` from disk, `ast.parse` each. Implement helpers that classify each
    `FileState.<X>` `ast.Attribute` occurrence as WRITE (RHS of an `Assign` whose target is an
    `ast.Attribute` ending in `.state`), READ-IN-COMPARE (inside an `ast.Compare`), or READ-IN-WHERE
    (a positional or keyword arg of a `Call` whose `func` is an attribute in
    {`where`,`filter`,`filter_by`,`having`}). Follow the corrected `test_single_awaiting_writer.py`
    walking discipline, INCLUDING keyword args (`keyword.arg is None` **splat handling) so the read-scan
    is not blind to positional `.where(a, b, c)` args. Assertions: dedup.py has exactly one
    `DUPLICATE_RESOLVED` WRITE and zero reads; fingerprint.py has zero `FINGERPRINTED` occurrences of any
    kind. Do NOT ship a `grep`/line-oriented match anywhere (Pitfall 1 — the token is a single unbroken
    chain and a grep false-positives on the surviving writer). Include comments naming the mutation for
    each direction.
  </action>
  <verify>
    <automated>just test-bucket shared 2>/dev/null || uv run pytest tests/shared/test_dedup_fingerprint_source_scan.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/shared/test_dedup_fingerprint_source_scan.py` uses `ast.parse` + `ast.walk` (no `grep`, no line-oriented `in`-string match on source lines).
    - `just test-bucket shared` runs the file and it exits 0 in isolation.
    - The guard walks Call positional AND keyword args for `where`/`filter`/`filter_by`/`having` (verified by reading the walker).
    - The test imports no `phaze.database`/`phaze.models`/`saq` and constructs no `Settings` (no lru_cache leak) — DB-free.
    - MUTATION #1 (false negative, SUMMARY): reintroduce a `FileState.DUPLICATE_RESOLVED` read as a positional 2nd arg in a `.where(a, b, c)` in `dedup.py` → guard RED; remove → GREEN.
    - MUTATION #2 (false positive, SUMMARY): the surviving dual-writer `f.state = FileState.DUPLICATE_RESOLVED` at `dedup.py:268` stays untouched → guard GREEN (writer allowed).
    - MUTATION #3 (SUMMARY): add a `FileState.FINGERPRINTED` reference to `fingerprint.py` → guard RED; remove → GREEN.
  </acceptance_criteria>
  <done>The AST guard is green, walks positional+keyword args, allows exactly the one dedup writer, forbids all reads, and all three mutations were observed (RED where expected, GREEN for the writer) and restored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | DB-free static-source test; no runtime surface, no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-05-01 | Tampering | a reintroduced `FileRecord.state` read slipping past the guard | mitigate | The guard is the mitigation: an `ast.walk` scan (not a grep) that walks positional + keyword `Call` args, closing the two Phase-83 blind spots (line-split false-positive; positional-arg blindness). Mutation-tested both directions to prove it has teeth. |
| T-84-05-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies (`ast` is stdlib). |
</threat_model>

<verification>
- `just test-bucket shared` green in isolation.
- Guard is AST-based, positional+keyword aware, DB-free.
- All three mutations observed and restored.
</verification>

<success_criteria>
A mutation-tested AST guard permanently forbids reintroducing a `FileState.DUPLICATE_RESOLVED` read in
`dedup.py` (while tolerating the one surviving writer) and any `FileState.FINGERPRINTED` in
`fingerprint.py` — the standing insurance behind READ-04 for these two files.
</success_criteria>

<output>
Create `.planning/phases/84-dedup-fingerprint-progress-cutover/84-05-SUMMARY.md` when done. Record all
three mutation-check results.
</output>
