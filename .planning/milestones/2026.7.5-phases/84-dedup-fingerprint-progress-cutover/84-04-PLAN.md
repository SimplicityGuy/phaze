---
phase: 84-dedup-fingerprint-progress-cutover
plan: 04
type: execute
wave: 2
depends_on: [84-01, 84-02]
files_modified:
  - src/phaze/services/fingerprint.py
  - tests/fingerprint/services/test_fingerprint.py
  - tests/integration/test_fingerprint_progress.py
autonomous: true
requirements: [READ-04]
must_haves:
  truths:
    - "D-09: get_fingerprint_progress keeps its 3-key contract (total/completed/failed); each body is redefined over the derived predicates — zero API break for docs/api.md:35 and justfile:500"
    - "D-10: total = count(files) where file_type IN MUSIC_VIDEO_TYPES AND ~dedup_resolved_clause() — no FileRecord.state read"
    - "D-11: completed and failed both become FILE counts — completed via done_clause(Stage.FINGERPRINT) (rides ix_fprint_success), failed via failed_clause(Stage.FINGERPRINT); failed stops being a fingerprint_results ROW count"
    - "D-12: no per-engine breakdown — done_clause(FINGERPRINT) is the per-engine coverage predicate (EXISTS any engine success); GROUP BY engine is Phase 87"
    - "D-17: all three keys share total's denominator (file_type IN MUSIC_VIDEO_TYPES AND ~dedup_resolved_clause()), so completed ⊆ total and failed ⊆ total — the progress bar can never exceed 100%"
    - "D-00c: done(fingerprint) = any engine status IN ('success','completed'); failed = no engine succeeded AND ≥1 failed — reused via done_clause/failed_clause, DERIV-05 aggregation unchanged"
    - "D-00e: every new dependency (MUSIC_VIDEO_TYPES, done_clause/failed_clause/dedup_resolved_clause, Stage) is imported FUNCTION-LOCALLY — fingerprint.py is agent-worker-imported and must not import phaze.database/phaze.models at module level"
    - "D-15: the mock stub at tests/fingerprint/services/test_fingerprint.py:291-309 is REPLACED with a real-DB integration test that goes RED if completed reverts to state==FINGERPRINTED and RED if failed reverts to a row count"
    - "READ-04: get_fingerprint_progress derives from output tables + the dedup marker, never FileRecord.state"
  artifacts:
    - path: "src/phaze/services/fingerprint.py"
      provides: "get_fingerprint_progress rewritten over MUSIC_VIDEO_TYPES + dedup marker + done/failed_clause, all imports function-local"
      contains: "done_clause"
    - path: "tests/integration/test_fingerprint_progress.py"
      provides: "Real-DB test pinning D-10 denominator, D-11 units, DERIV-05 aggregation, D-17 subset"
      contains: "get_fingerprint_progress"
  key_links:
    - from: "src/phaze/services/fingerprint.py::get_fingerprint_progress"
      to: "services/stage_status.py::done_clause/failed_clause/dedup_resolved_clause"
      via: "function-local import + correlated count"
      pattern: "done_clause\\(Stage.FINGERPRINT\\)"
    - from: "src/phaze/services/fingerprint.py::get_fingerprint_progress"
      to: "services/pipeline.py::MUSIC_VIDEO_TYPES"
      via: "function-local import"
      pattern: "MUSIC_VIDEO_TYPES"
---

<objective>
Rewrite `get_fingerprint_progress` (`services/fingerprint.py:256-295`) off `FileRecord.state` and onto
the derived predicates while preserving its 3-key `{total, completed, failed}` contract (D-09). `total`
becomes `file_type IN MUSIC_VIDEO_TYPES AND ~dedup_resolved_clause()` (D-10); `completed`/`failed` become
FILE counts over `done_clause`/`failed_clause(Stage.FINGERPRINT)` (D-11) sharing `total`'s denominator
(D-17). Replace the toothless mock test at `tests/fingerprint/services/test_fingerprint.py:291-309` with
a real-DB integration test (D-15).

Purpose: `completed` currently reads `state == FINGERPRINTED` (sole writer `retry_analysis_failed`, so it
counts ≈nothing) and `failed` currently counts `fingerprint_results` ROWS (double-counts a two-engine
failure, misclassifies one-success/one-failure as failed). Post-cutover `completed` visibly JUMPS and
`failed` visibly DROPS — the fix, not a regression (record in the SUMMARY). All DB imports stay
function-local (agent-worker boundary, D-00e).
Output: rewritten `get_fingerprint_progress`; real-DB replacement test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-dedup-fingerprint-progress-cutover/84-CONTEXT.md
@.planning/phases/84-dedup-fingerprint-progress-cutover/84-RESEARCH.md
@.planning/phases/84-dedup-fingerprint-progress-cutover/84-PATTERNS.md

@src/phaze/services/fingerprint.py
@src/phaze/services/stage_status.py
@src/phaze/services/pipeline.py
@tests/fingerprint/services/test_fingerprint.py
@tests/integration/test_shadow_compare.py

<interfaces>
<!-- Published by 84-02 (Wave 1) + Phase 78 (existing) — import ALL of these FUNCTION-LOCALLY -->
From services/stage_status.py:
  def dedup_resolved_clause() -> ColumnElement[bool]          # correlated exists(marker)
  def done_clause(stage: Stage) -> ColumnElement[bool]        # FINGERPRINT: exists any engine status IN ('success','completed') — rides ix_fprint_success
  def failed_clause(stage: Stage) -> ColumnElement[bool]      # FINGERPRINT: ~done AND exists status=='failed'
From services/pipeline.py:
  MUSIC_VIDEO_TYPES: list[str]                                # ext (no dot) for MUSIC/VIDEO categories
From enums/stage.py:
  class Stage(...)  # Stage.FINGERPRINT

<!-- current signature (unchanged) -->
  async def get_fingerprint_progress(session: AsyncSession) -> dict[str, int]   # keys: total, completed, failed
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite get_fingerprint_progress over the derived predicates (D-09..D-12, D-17)</name>
  <files>src/phaze/services/fingerprint.py</files>
  <read_first>
    - src/phaze/services/fingerprint.py (:256-295 — the current body + the function-local-import block at :268-271; the docstring at :257-266)
    - src/phaze/services/stage_status.py (done_clause/failed_clause FINGERPRINT branches; dedup_resolved_clause from 84-02)
    - src/phaze/services/pipeline.py (~:46 MUSIC_VIDEO_TYPES)
    - .planning/phases/84-dedup-fingerprint-progress-cutover/84-RESEARCH.md (Pitfalls 5,6,8; D-17)
  </read_first>
  <behavior>
    - denom = (FileRecord.file_type.in_(MUSIC_VIDEO_TYPES), ~dedup_resolved_clause()).
    - total = count(FileRecord.id) where *denom.
    - completed = count(FileRecord.id) where (*denom, done_clause(Stage.FINGERPRINT)).
    - failed = count(FileRecord.id) where (*denom, failed_clause(Stage.FINGERPRINT)).
    - Returns dict {"total": int, "completed": int, "failed": int} (3-key contract preserved).
    - completed ⊆ total and failed ⊆ total (shared denominator).
  </behavior>
  <action>
    Rewrite `get_fingerprint_progress`. Inside the function (NOT at module level — D-00e/Pitfall 5), add
    the local imports alongside the existing `# noqa: PLC0415` locals:
    `from phaze.services.pipeline import MUSIC_VIDEO_TYPES`,
    `from phaze.services.stage_status import done_clause, failed_clause, dedup_resolved_clause`,
    `from phaze.enums.stage import Stage`, and keep `from phaze.models.file import FileRecord` +
    `from sqlalchemy import func, select`. DROP the now-unused `FileState` and `FingerprintResult`
    function-local imports (no writer here; the source scan asserts zero `FileState.FINGERPRINTED`). Build
    `denom = (FileRecord.file_type.in_(MUSIC_VIDEO_TYPES), ~dedup_resolved_clause())` and compute the three
    counts per the behavior block — `completed` uses `done_clause(Stage.FINGERPRINT)` (which renders
    `status IN ('success','completed')` = the `ix_fprint_success` partial-index predicate byte-for-byte,
    Pitfall 6 — do NOT re-spell the status set); `failed` uses `failed_clause(Stage.FINGERPRINT)`. Rewrite
    the docstring (:257-266) to describe the derived contract and note the number changes (completed jumps,
    failed drops). Sole caller is `routers/pipeline.py:1339`; do not change the endpoint or its callers.
  </action>
  <verify>
    <automated>uv run ruff check src/phaze/services/fingerprint.py && uv run mypy src/phaze/services/fingerprint.py</automated>
  </verify>
  <acceptance_criteria>
    - `get_fingerprint_progress` returns exactly the keys `total`, `completed`, `failed`.
    - Every new dependency (`MUSIC_VIDEO_TYPES`, `done_clause`, `failed_clause`, `dedup_resolved_clause`, `Stage`) is imported INSIDE the function body; `grep -n "^from phaze" src/phaze/services/fingerprint.py` shows NO module-level `phaze.models`/`phaze.database`/`phaze.services.pipeline`/`phaze.services.stage_status` import.
    - No `FileState` reference remains in `get_fingerprint_progress` (the AST guard in 84-05 asserts zero `FileState.FINGERPRINTED` in the file).
    - `completed` uses `done_clause(Stage.FINGERPRINT)` (not a re-spelled `status IN (...)`); `failed` uses `failed_clause(Stage.FINGERPRINT)` (not a `count(FingerprintResult.id)` row count).
    - All three counts carry `file_type.in_(MUSIC_VIDEO_TYPES)` and `~dedup_resolved_clause()` (D-17).
    - Agent-worker import boundary intact: `uv run python -c "import phaze.services.fingerprint"` succeeds without importing `phaze.database` (spot-check the module has no top-level model imports).
    - `uv run ruff check .` and `uv run mypy .` exit 0.
  </acceptance_criteria>
  <done>get_fingerprint_progress derives all three keys from output tables + the dedup marker with function-local imports; 3-key contract + shared denominator preserved; ruff+mypy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the mock test with a real-DB integration test (D-15)</name>
  <files>tests/fingerprint/services/test_fingerprint.py, tests/integration/test_fingerprint_progress.py</files>
  <read_first>
    - tests/fingerprint/services/test_fingerprint.py (:285-314 — TestGetFingerprintProgress.test_get_progress_returns_counts, the side_effect stub to REMOVE)
    - tests/integration/test_shadow_compare.py (:84-113 db_session fixture; :116-132 _new_file; :157 DedupResolution(file_id=...))
    - .planning/phases/84-dedup-fingerprint-progress-cutover/84-RESEARCH.md (§ Validation SC#2 — the corpus + the two RED mutations)
  </read_first>
  <behavior>
    - Corpus (real PG): a MUSIC file with a fingerprint_results status='success' → counts total AND completed; a VIDEO file with no fingerprint rows → total only; a non-audio file (e.g. file_type 'txt') → excluded from total entirely; a dedup-resolved duplicate (marker present, music type) → excluded from total; a music file with state='duplicate_resolved' but NO marker → INCLUDED in total (marker is authority, proves derivation is not state-based); a file with one engine success + one engine failure → adds 1 to completed, 0 to failed (DERIV-05); a file with all engines failed → adds 1 to failed, 0 to completed.
    - Asserts real integer counts; completed ⊆ total; failed ⊆ total.
    - Two RED mutations: reverting completed to state==FINGERPRINTED makes the success-row files vanish from completed (RED); reverting failed to a fingerprint_results ROW count makes the two-engine-failure file add 2 not 1 (RED).
  </behavior>
  <action>
    Remove `TestGetFingerprintProgress.test_get_progress_returns_counts` (the `session.execute`
    side_effect stub, :291-309) from `tests/fingerprint/services/test_fingerprint.py`. If that leaves the
    class/file empty of value, keep the surrounding real behavioral tests intact — only delete the stub.
    Create `tests/integration/test_fingerprint_progress.py` on the `test_shadow_compare.py:84-113` real-PG
    `db_session` fixture (no saq_jobs; do not use the SAQ fixture). Seed the corpus per the behavior block
    (music/video/non-audio; marker-present resolved dup; `state='duplicate_resolved'`+no-marker music
    file; one-success/one-failure file; all-failed file — construct `DedupResolution(file_id=...)` via the
    `:157` idiom, `FingerprintResult` rows via the model). Call `get_fingerprint_progress(session)` and
    assert exact counts pinning D-10's denominator, D-11's file-vs-row units, DERIV-05's aggregation, and
    D-17's subset. `integration` bucket. If bucket membership of anything changes, update
    `tests/buckets.json` accordingly (the new file is already under `tests/integration/`).
  </action>
  <verify>
    <automated>MIGRATIONS_TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_migrations_test" TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_test" uv run pytest tests/integration/test_fingerprint_progress.py -x</automated>
  </verify>
  <acceptance_criteria>
    - The `side_effect`-stub `test_get_progress_returns_counts` no longer exists in `tests/fingerprint/services/test_fingerprint.py` (`grep -c "side_effect=\[mock_result_total" tests/fingerprint/services/test_fingerprint.py` returns 0).
    - `just test-bucket integration` runs `tests/integration/test_fingerprint_progress.py` and it exits 0 in isolation on port 5433.
    - The corpus includes both a marker-present resolved dup (excluded from total) and a `state='duplicate_resolved'`+no-marker music file (INCLUDED in total) — proving marker-not-state derivation.
    - MUTATION CHECK #1 (SUMMARY): revert `completed` to `state == FileState.FINGERPRINTED` → RED; restore → GREEN.
    - MUTATION CHECK #2 (SUMMARY): revert `failed` to `count(FingerprintResult.id) where status=='failed'` → RED (two-engine-failure file adds 2 not 1); restore → GREEN.
    - `just test-bucket fingerprint` still exits 0 (remaining mock tests unaffected).
  </acceptance_criteria>
  <done>The mock stub is gone; the real-DB test pins the denominator, units, aggregation, and subset; both RED mutations observed then restored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP → `GET /api/v1/fingerprint/progress` | `routers/pipeline.py:1339`; parameterless read-only endpoint; no untrusted input reaches the query. |
| `services/fingerprint.py` module load → agent worker | The agent worker imports this module; it MUST NOT drag `phaze.database`/`phaze.models` into its import graph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-04-01 | Denial of Service | agent-worker import crash from a module-level DB import | mitigate | D-00e: every new dependency is imported FUNCTION-LOCALLY inside `get_fingerprint_progress`; asserted by the acceptance criteria (no top-level `phaze.models`/`phaze.services.pipeline`/`phaze.services.stage_status` import). A module-level import would crash the worker at import time. |
| T-84-04-02 | Information Disclosure | count endpoint | accept | Returns three integers over the whole corpus; no per-file or PII data; existing internal-realm reverse-proxy posture unchanged. |
| T-84-04-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies. |
</threat_model>

<verification>
- `just test-bucket fingerprint` (remaining mocks) + `just test-bucket integration` (new real-DB test) green in isolation on 5433.
- Function-local import boundary preserved; 3-key contract preserved; shared denominator (D-17) enforced.
- Both RED mutations observed then restored.
</verification>

<success_criteria>
`get_fingerprint_progress` derives `total`/`completed`/`failed` from `MUSIC_VIDEO_TYPES` + the dedup
marker + `done_clause`/`failed_clause(Stage.FINGERPRINT)` with function-local imports and a shared
denominator, and the toothless mock is replaced by a real-DB test that goes RED on either regression
(`completed`→state, `failed`→row-count).
</success_criteria>

<output>
Create `.planning/phases/84-dedup-fingerprint-progress-cutover/84-04-SUMMARY.md` when done. Record the
before/after `completed` jump and `failed` drop framing and both mutation-check results.
</output>
