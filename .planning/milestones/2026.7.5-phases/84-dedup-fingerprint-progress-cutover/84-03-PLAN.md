---
phase: 84-dedup-fingerprint-progress-cutover
plan: 03
type: execute
wave: 2
depends_on: [84-01, 84-02]
files_modified:
  - src/phaze/services/dedup.py
  - tests/integration/test_dedup_divergence.py
  - tests/integration/test_dedup_resolve_undo_shadow.py
autonomous: true
requirements: [READ-04, SIDECAR-02]
must_haves:
  truths:
    - "D-01: resolve_group gains the go-forward dedup_resolution writer that did not exist since 032's one-shot backfill"
    - "D-02: resolve_group bulk-inserts markers via one postgresql.insert(DedupResolution).values(rows).on_conflict_do_nothing(index_elements=['file_id']) inside the caller-owned transaction, never committing"
    - "D-03: each inserted row carries canonical_file_id = the operator's actual canonical_id (strictly better than 032's ORDER BY c.id LIMIT 1 guess); backfilled NULLs left alone"
    - "D-05: undo_resolve DELETEs the marker and restores previous_state; the client [{id, previous_state}] payload shape is unchanged"
    - "D-06: the CAS anchor is the marker — one DELETE ... RETURNING file_id, and previous_state is restored ONLY to the returned ids, so a stale-tab replay finds no marker, returns zero rows, and no-ops"
    - "D-07: the writer stays on_conflict_do_nothing (not DO UPDATE) — post-cutover the selection filters ~dedup_resolved_clause() so the conflict only fires on a genuine concurrent double-submit, first-writer-wins"
    - "D-00a: the dual-writer f.state = FileState.DUPLICATE_RESOLVED at dedup.py:268 STAYS (dies Phase 90); only reliance on state is replaced"
    - "D-00d: previous_state to restore on undo comes from the browser payload, not the marker (the marker carries no previous_state column)"
    - "D-14: the divergence test seeds an inconsistent corpus (marker+state='analyzed' → EXCLUDED; state='duplicate_resolved'+no-marker → INCLUDED) and covers the five dedup readers; reverting any reader's predicate inverts both assertions (mutation-tested)"
    - "D-16: SC#3 proven way 1 — a committed integration test asserts run_shadow_compare(session).hard_fail_total == 0 through resolve → undo → re-resolve"
    - "READ-04: all nine dedup read sites key on ~dedup_resolved_clause() (marker), never FileRecord.state"
  artifacts:
    - path: "src/phaze/services/dedup.py"
      provides: "pg_insert marker writer in resolve_group; DELETE...RETURNING CAS undo; nine reader flips to ~dedup_resolved_clause()"
      contains: "on_conflict_do_nothing"
    - path: "tests/integration/test_dedup_divergence.py"
      provides: "Inconsistent-corpus divergence test across the five dedup readers"
      contains: "def test_"
    - path: "tests/integration/test_dedup_resolve_undo_shadow.py"
      provides: "resolve→undo→re-resolve shadow-compare integration test (D-16.1)"
      contains: "run_shadow_compare"
  key_links:
    - from: "src/phaze/services/dedup.py::resolve_group"
      to: "dedup_resolution table"
      via: "postgresql.insert(DedupResolution).on_conflict_do_nothing"
      pattern: "pg_insert\\(DedupResolution\\)|insert\\(DedupResolution\\)"
    - from: "src/phaze/services/dedup.py::undo_resolve"
      to: "dedup_resolution marker (CAS anchor)"
      via: "delete(DedupResolution).where(file_id.in_(ids)).returning(file_id)"
      pattern: "delete\\(DedupResolution\\).*returning"
    - from: "src/phaze/services/dedup.py (nine readers)"
      to: "services/stage_status.py::dedup_resolved_clause"
      via: "~dedup_resolved_clause()"
      pattern: "dedup_resolved_clause\\(\\)"
---

<objective>
Cut `services/dedup.py` over to the durable marker: add the go-forward `pg_insert(DedupResolution)`
writer in `resolve_group` (D-01/D-02/D-03/D-07), convert `undo_resolve` into a
`DELETE ... RETURNING file_id` CAS whose `previous_state` restore is gated on the returned ids
(D-05/D-06), and flip the nine `FileRecord.state != FileState.DUPLICATE_RESOLVED` read sites to
`~dedup_resolved_clause()` (READ-04). Prove it with a mutation-tested inconsistent-corpus divergence
test (D-14, five dedup readers) and a `resolve → undo → re-resolve` shadow-compare integration test
(D-16.1).

Purpose: the marker becomes the single authority for "is this file a resolved duplicate"; a stale-tab
undo replay against a since-re-resolved file matches zero rows and no-ops. The dual-writer
`f.state = FileState.DUPLICATE_RESOLVED` STAYS (D-00a) until Phase 90.
Output: cut-over `dedup.py`; two integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-dedup-fingerprint-progress-cutover/84-CONTEXT.md
@.planning/phases/84-dedup-fingerprint-progress-cutover/84-RESEARCH.md
@.planning/phases/84-dedup-fingerprint-progress-cutover/84-PATTERNS.md

@src/phaze/services/dedup.py
@src/phaze/routers/duplicates.py
@src/phaze/routers/agent_analysis.py
@src/phaze/services/scan_deletion.py
@src/phaze/routers/agent_push.py
@tests/integration/test_shadow_compare.py

<interfaces>
<!-- Published by 84-02 (Wave 1) — use directly, no exploration -->
From services/stage_status.py:
  def dedup_resolved_clause() -> ColumnElement[bool]   # correlated exists(marker); ~ = "not resolved"

<!-- The pg_insert + explicit-id idiom (precedent, verbatim) — agent_analysis.py:202-205,222 -->
  # id stamped explicitly because the model's default=uuid.uuid4 is Python-side; pg_insert bypasses it
  stmt = pg_insert(AnalysisResult).values([{**dumped, "file_id": file_id, "id": uuid.uuid4()}])
  stmt = stmt.on_conflict_do_nothing(index_elements=["file_id"])

<!-- The CAS-delete hygiene (precedent) — scan_deletion.py:119 / agent_push.py:139-151 -->
  result = await session.execute(stmt.execution_options(synchronize_session=False))  # RETURNING/rowcount
  # then gate the FileRecord dual-write behind the returned set / rowcount != 0

<!-- resolve_group / undo_resolve current signatures (unchanged) -->
  async def resolve_group(session, group_hash: str, canonical_id: UUID) -> tuple[int, list[dict[str, Any]]]
  async def undo_resolve(session, file_states: list[dict[str, Any]]) -> int   # entries {id, previous_state}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Writer + undo CAS + nine reader flips in dedup.py</name>
  <files>src/phaze/services/dedup.py</files>
  <read_first>
    - src/phaze/services/dedup.py (FULL — the nine read sites at 78,90,128,141,188,209,221,235,260; resolve_group 251-271; undo_resolve 274-286; module imports 6-10)
    - src/phaze/models/dedup_resolution.py (id=Python-only default=uuid.uuid4; unique file_id; canonical_file_id nullable; resolved_at server_default)
    - src/phaze/routers/agent_analysis.py (~:200-223 — pg_insert + explicit id stamp + on_conflict_do_nothing)
    - src/phaze/services/scan_deletion.py (~:117-120 — delete().execution_options(synchronize_session=False) + rowcount cast)
    - src/phaze/routers/agent_push.py (~:131-151 — CAS-then-gated dual-write on rowcount)
    - src/phaze/routers/duplicates.py (:151,162,176,214,242 — resolve/undo/bulk callers; the browser-held file_states payload)
    - .planning/phases/84-dedup-fingerprint-progress-cutover/84-RESEARCH.md (Pitfalls 1,2,3,4,7,9)
  </read_first>
  <behavior>
    - resolve_group inserts one dedup_resolution marker per non-canonical file (canonical_file_id = canonical_id), idempotent under a double-submit (ON CONFLICT (file_id) DO NOTHING), inside the caller's txn (flush, no commit). f.state still stamped DUPLICATE_RESOLVED (dual-write). Return signature tuple[int, list[dict]] unchanged; previous_state captured before the stamp.
    - undo_resolve deletes markers for the payload ids via one DELETE ... RETURNING file_id; restores previous_state ONLY for returned ids; a stale replay against a file with no marker restores nothing (returns a smaller count, which callers do not use for control flow).
    - The nine readers exclude resolved files via ~dedup_resolved_clause() (marker), never FileRecord.state.
  </behavior>
  <action>
    In `services/dedup.py`:
    (1) Module imports: add `from sqlalchemy.dialects.postgresql import insert as pg_insert`,
    `from sqlalchemy import delete`, `from phaze.models.dedup_resolution import DedupResolution`, and
    `from phaze.services.stage_status import dedup_resolved_clause` (all module-level — dedup.py already
    imports models at module level). Keep `FileState` (still needed for the surviving writer at :268).
    (2) resolve_group: after the `for f in files` loop that captures `previous_state` (:267) and stamps
    `f.state = FileState.DUPLICATE_RESOLVED` (:268 — LEAVE THIS), add the writer: build
    `rows = [{"id": uuid_mod.uuid4(), "file_id": f.id, "canonical_file_id": canonical_id} for f in files]`
    and `await session.execute(pg_insert(DedupResolution).values(rows).on_conflict_do_nothing(index_elements=["file_id"]))`
    only when `files` is non-empty. Stamp `id` explicitly (Pitfall 2 — pg_insert bypasses the Python-side
    default → NULL PK failure); omit `resolved_at` (server_default). Keep the `.where(...)` selection at
    :257-261 but replace its 3rd positional arg `FileRecord.state != FileState.DUPLICATE_RESOLVED` with
    `~dedup_resolved_clause()`. Do NOT commit.
    (3) undo_resolve: replace the unconditional per-file `update()` loop (:280-284) with — parse ids
    (`UUID(e["id"]) if isinstance(e["id"], str) else e["id"]`); one
    `delete(DedupResolution).where(DedupResolution.file_id.in_(ids)).returning(DedupResolution.file_id)
    .execution_options(synchronize_session=False)`; collect the returned file_ids into a set; restore
    `previous_state` ONLY for entries whose id is in that set (Claude's Discretion: N per-file UPDATEs or
    one UPDATE ... FROM (VALUES ...); only returned ids may be written). Return the count actually
    restored. flush, no commit.
    (4) Flip the remaining eight reader sites (78,90,128,141,188,209 chained `.where()`; 221,235
    positional 2nd arg) from `FileRecord.state != FileState.DUPLICATE_RESOLVED` to
    `~dedup_resolved_clause()`. Do NOT touch the `LIMIT/OFFSET`-without-`ORDER BY` at :81,:131,:207
    (Pitfall 7 — deferred, out of scope) or any other clause.
    (5) Ruff hygiene (Pitfall 9): if `update` is no longer used after the undo rewrite, drop it from the
    imports; keep `FileState` (writer at :268). Make `uv run ruff check .` pass.
  </action>
  <verify>
    <automated>uv run ruff check src/phaze/services/dedup.py && uv run mypy src/phaze/services/dedup.py</automated>
  </verify>
  <acceptance_criteria>
    - `services/dedup.py` imports `DedupResolution` and `dedup_resolved_clause`; `resolve_group` contains `on_conflict_do_nothing(index_elements=["file_id"])` with an explicit `uuid_mod.uuid4()` id per row and `canonical_file_id` set to `canonical_id`.
    - `undo_resolve` contains exactly one `delete(DedupResolution)...returning(DedupResolution.file_id)` and the state restore is guarded by membership in the returned set (no unconditional per-file update remains).
    - `f.state = FileState.DUPLICATE_RESOLVED` still present exactly once (the writer at ~:268).
    - `grep -c "FileRecord.state != FileState.DUPLICATE_RESOLVED" src/phaze/services/dedup.py` returns 0; `grep -c "dedup_resolved_clause()" src/phaze/services/dedup.py` returns 9 (the nine former read sites).
    - `LIMIT/OFFSET` at :81/:131/:207 and all ordering/grouping are unchanged (git diff shows only the `.where` clauses + writer + undo changed).
    - `uv run ruff check .` and `uv run mypy .` exit 0.
    - Existing dedup unit tests still pass: `just test-bucket discovery` exits 0.
  </acceptance_criteria>
  <done>dedup.py has the marker writer, the CAS undo, and nine marker-keyed readers; dual-writer preserved; ruff+mypy clean; existing units green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Divergence test — inconsistent corpus across the five dedup readers (D-14)</name>
  <files>tests/integration/test_dedup_divergence.py</files>
  <read_first>
    - tests/integration/test_shadow_compare.py (:84-113 db_session fixture; :116-132 _new_file; :157 DedupResolution(file_id=...) construction idiom — COPY this fixture, NOT the SAQ one)
    - src/phaze/services/dedup.py (the five reader entry points: find_duplicate_groups, find_duplicate_groups_with_metadata, count_duplicate_groups, get_duplicate_stats, resolve_group's selection)
    - .planning/phases/84-dedup-fingerprint-progress-cutover/84-RESEARCH.md (§ Validation Architecture SC#1; the mutation that inverts each reader)
  </read_first>
  <behavior>
    - Seed an INCONSISTENT corpus (marker ≢ state): File A = marker present + state='analyzed'; File B = state='duplicate_resolved' + NO marker; plus a normal duplicate pair sharing a sha256_hash so groups are non-empty.
    - Assert File A is EXCLUDED from every dedup reader (marker wins) and File B is INCLUDED (marker is authority): find_duplicate_groups / find_duplicate_groups_with_metadata do not surface A and do surface B in a group; count_duplicate_groups / get_duplicate_stats reflect B not A; resolve_group's selection would pick B (not A) as a non-canonical member.
    - The mutation: reverting any one reader's ~dedup_resolved_clause() back to state != DUPLICATE_RESOLVED inverts both assertions (A wrongly included, B wrongly excluded).
  </behavior>
  <action>
    Create `tests/integration/test_dedup_divergence.py` reusing the `test_shadow_compare.py:84-113`
    real-PG `db_session` fixture (probe broker → pytest.skip if down; `Base.metadata.create_all`; seed the
    `legacy-application-server` agent; rolled-back txn per test). Do NOT pull in `saq.PostgresQueue` /
    `test_pg_dedup.py`'s SAQ fixture (saq_jobs stub-poison hazard; this phase reads no saq_jobs). Build the
    inconsistent corpus per the behavior block (File A marker+`analyzed`; File B `duplicate_resolved`+no
    marker; a normal duplicate pair on a shared hash). Write one test per reader asserting A EXCLUDED / B
    INCLUDED. Include a comment naming the exact one-line mutation per reader that must invert the
    assertions. This test lives in the `integration` bucket (needs real PG for marker+state seeding).
  </action>
  <verify>
    <automated>MIGRATIONS_TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_migrations_test" TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_test" uv run pytest tests/integration/test_dedup_divergence.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `just test-bucket integration` (with the test DB provisioned, port 5433) runs the file and it exits 0 in isolation.
    - The corpus is inconsistent (File A: marker + `state='analyzed'`; File B: `state='duplicate_resolved'` + no marker) — verified by reading the seed code.
    - All five dedup readers are covered by at least one assertion.
    - MUTATION CHECK (recorded in the SUMMARY): reverting one reader's `~dedup_resolved_clause()` to `FileRecord.state != FileState.DUPLICATE_RESOLVED` makes the file exit non-zero (RED); restoring returns it to 0 (GREEN).
    - Test imports no `saq` / `saq_jobs`; passes in `just test-bucket integration` in isolation.
  </acceptance_criteria>
  <done>Divergence test green; five readers covered; the revert-one-reader mutation observed RED then restored GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: resolve → undo → re-resolve shadow-compare integration test (D-16.1)</name>
  <files>tests/integration/test_dedup_resolve_undo_shadow.py</files>
  <read_first>
    - tests/integration/test_shadow_compare.py (db_session fixture; run_shadow_compare usage; DedupResolution(file_id=...) at :157)
    - src/phaze/services/shadow_compare.py (run_shadow_compare / Report.hard_fail_total; the hard duplicate_resolved invariant at :135)
    - src/phaze/services/dedup.py (resolve_group / undo_resolve after Task 1)
  </read_first>
  <behavior>
    - On a synthetic real-PG corpus: resolve_group inserts a marker for each non-canonical file; run_shadow_compare(session).hard_fail_total == 0.
    - undo_resolve DELETEs the marker(s) and restores previous_state only for returned ids; hard_fail_total stays 0.
    - A stale replay of undo against a file re-resolved since (marker present again) matches 0 rows for the stale id and no-ops (does not clobber the re-resolved state).
    - re-resolve re-inserts the marker; hard_fail_total == 0 throughout.
  </behavior>
  <action>
    Create `tests/integration/test_dedup_resolve_undo_shadow.py` on the same `test_shadow_compare.py`
    `db_session` fixture. Seed a duplicate group (two+ files sharing a sha256_hash). Drive the real
    `resolve_group` → `undo_resolve` → `resolve_group` cycle through the service functions (flush; the
    fixture owns the txn). After each step assert `run_shadow_compare(session).hard_fail_total == 0`.
    Add a stale-replay case: capture a `[{id, previous_state}]` payload, re-resolve the same file, then
    replay `undo_resolve` with the stale payload and assert the marker/state were NOT clobbered (the CAS
    returned 0 rows for the stale id). `integration` bucket; no saq_jobs.
  </action>
  <verify>
    <automated>MIGRATIONS_TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_migrations_test" TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_test" uv run pytest tests/integration/test_dedup_resolve_undo_shadow.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `just test-bucket integration` runs the file and it exits 0 in isolation.
    - `run_shadow_compare(session).hard_fail_total == 0` is asserted after resolve, after undo, and after re-resolve.
    - The stale-replay case proves the CAS no-ops (marker/state of the re-resolved file unchanged by the stale undo).
    - MUTATION CHECK (recorded in the SUMMARY): deleting the `pg_insert` writer from `resolve_group` makes this test RED (`hard_fail_total > 0`); restoring makes it GREEN.
    - Test imports no `saq` / `saq_jobs`.
  </acceptance_criteria>
  <done>Shadow-compare integration test green through the resolve/undo/re-resolve cycle incl. stale-replay no-op; writer-deletion mutation observed RED then restored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `undo_resolve` | `routers/duplicates.py:162` echoes a `[{id, previous_state}]` payload into a hidden template field; the client posts it back at `:176`/`:242`. Every value is attacker-controllable (a stale or tampered tab). |
| `services/` → Postgres | `resolve_group`/`undo_resolve` build + flush in the router's `get_session` txn (never commit). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-03-01 | Elevation of Privilege | `undo_resolve` mass file_id assignment across files the operator never resolved | mitigate | D-06 CAS: the `DELETE ... RETURNING file_id` scopes the `state` restore to ONLY the file_ids that actually held a marker; ids without a marker return zero rows and are skipped. A crafted payload of arbitrary ids restores nothing. Proven by the stale-replay case in Task 3. |
| T-84-03-02 | Tampering | attacker-supplied `previous_state` string written into `FileRecord.state` | mitigate | The restore is bounded to marker-bearing (operator-resolved) ids (T-84-03-01), so the blast radius is limited to files the operator did resolve. `FileRecord.state` is a dying column (drops Phase 90). Executor SHOULD additionally coerce `previous_state` to a `FileState` member before the UPDATE and skip unknown values — a defensive add that does NOT change the payload shape (constraint 4). Record the choice in the SUMMARY. |
| T-84-03-03 | Tampering | concurrent HTMX double-submit of resolve | mitigate | D-02/D-07 `on_conflict_do_nothing(index_elements=["file_id"])` on the unique `file_id` — idempotent, first-writer-wins; a double-submit cannot raise `IntegrityError` or poison the txn. |
| T-84-03-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies. |
</threat_model>

<verification>
- `just test-bucket discovery` (existing dedup units) + `just test-bucket integration` (new divergence + shadow tests) green in isolation on port 5433.
- Nine readers marker-keyed; writer + CAS undo present; dual-writer preserved.
- Both mutation checks (revert-one-reader; delete-writer) observed RED then restored.
</verification>

<success_criteria>
`services/dedup.py` reads and writes the durable dedup marker at all nine sites plus resolve/undo, with
no `FileRecord.state` read; undo is a CAS `DELETE` that no-ops on a stale replay; and the divergence +
shadow-compare integration tests (both mutation-verified) prove the cutover keeps the Phase-79 hard
invariant green.
</success_criteria>

<output>
Create `.planning/phases/84-dedup-fingerprint-progress-cutover/84-03-SUMMARY.md` when done. Record both
mutation-check results and the `previous_state`-validation decision (T-84-03-02).
</output>
