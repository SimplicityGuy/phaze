---
phase: 84-dedup-fingerprint-progress-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/035_reconcile_dedup_resolution.py
  - tests/integration/test_migrations/test_migration_035_reconcile_dedup_resolution.py
autonomous: true
requirements: [SIDECAR-02]
must_haves:
  truths:
    - "D-04: migration 035 is a sync, data-only, bidirectional reconcile of dedup_resolution against files.state (insert missing markers + delete orphaned markers), no DDL, empty compare_metadata autogenerate diff"
    - "D-00b: after 035 runs on a corpus with post-032 resolved-without-marker rows, the hard shadow-compare invariant state=DUPLICATE_RESOLVED ⇒ dedup marker exists holds (035 is what makes it green)"
    - "035 lands before any dedup reader flips to NOT EXISTS(marker) — encoded as the wave-1 dependency of the reader-flip plans (load-bearing ordering)"
    - "files.state is byte-unchanged by 035 (READ-ONLY on files.state); 035 never references saq_jobs"
  artifacts:
    - path: "alembic/versions/035_reconcile_dedup_resolution.py"
      provides: "Data-only reconcile: _BACKFILL_DEDUP re-run verbatim + orphaned-marker DELETE"
      contains: "ON CONFLICT (file_id) DO NOTHING"
    - path: "tests/integration/test_migrations/test_migration_035_reconcile_dedup_resolution.py"
      provides: "Bare-number revision assert, static-SQL scan, both-direction corpus, idempotency, empty autogenerate diff, downgrade"
      contains: "def test_"
  key_links:
    - from: "alembic/versions/035_reconcile_dedup_resolution.py"
      to: "dedup_resolution table"
      via: "op.execute(sa.text(STATIC_SQL))"
      pattern: "op\\.execute\\(sa\\.text"
    - from: "035 upgrade"
      to: "files.state (read-only authority pre-cutover)"
      via: "WHERE f.state = 'duplicate_resolved' / WHERE f.state <> 'duplicate_resolved'"
      pattern: "duplicate_resolved"
---

<objective>
Ship migration `035_reconcile_dedup_resolution` — a sync, data-only, bidirectional reconcile of the
`dedup_resolution` marker table against `files.state`, and its per-migration integration test. This is
seam (a) of the phase and MUST land before any dedup reader flips to a marker-existence predicate
(D-04, load-bearing): pre-cutover `files.state` is still the authority, so `035` reconciles the derived
representation *to* it, making `marker ≡ state` exactly at the cutover instant.

Purpose: every group resolved since `032` landed carries `state = duplicate_resolved` with **no marker**
(D-01), violating the *hard* shadow-compare invariant at `services/shadow_compare.py:135`. `035` inserts
the missing markers and deletes orphaned ones so the Phase-79 gate is green on the live corpus for the
first time.
Output: `alembic/versions/035_reconcile_dedup_resolution.py` + its migration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-dedup-fingerprint-progress-cutover/84-CONTEXT.md
@.planning/phases/84-dedup-fingerprint-progress-cutover/84-RESEARCH.md
@.planning/phases/84-dedup-fingerprint-progress-cutover/84-PATTERNS.md

# Templates to mirror one-for-one
@alembic/versions/034_backfill_cloud_awaiting.py
@tests/integration/test_migrations/test_migration_034_backfill_cloud_awaiting.py
@tests/integration/test_migrations/conftest.py

# Source of the verbatim insert half + the marker table DDL
@alembic/versions/032_add_derived_status_schema.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author migration 035 (data-only bidirectional reconcile)</name>
  <files>alembic/versions/035_reconcile_dedup_resolution.py</files>
  <read_first>
    - alembic/versions/034_backfill_cloud_awaiting.py (the exact shape: sync upgrade, op.execute(sa.text(STATIC)), no DDL, no model import, CRITICAL saq_jobs banner, downgrade)
    - alembic/versions/032_add_derived_status_schema.py (lines ~70-165: _BACKFILL_DEDUP verbatim at :84-94, dedup_resolution DDL at :129-141)
    - .planning/phases/84-dedup-fingerprint-progress-cutover/84-RESEARCH.md (§ "Migration 035 Template")
  </read_first>
  <action>
    Create `alembic/versions/035_reconcile_dedup_resolution.py`. Header identifiers are bare-number
    strings exactly as 034: `revision = "035"`, `down_revision = "034"`, `branch_labels = None`,
    `depends_on = None`. `upgrade()` is a plain sync `def upgrade() -> None:` executing TWO static,
    parameter-free `op.execute(sa.text(...))` statements:
    (1) `_BACKFILL_DEDUP` copied VERBATIM from `032:84-94` — the `INSERT INTO dedup_resolution
    (id, file_id, canonical_file_id, resolved_at) SELECT gen_random_uuid(), f.id, (SELECT c.id FROM
    files c WHERE c.sha256_hash = f.sha256_hash AND c.state <> 'duplicate_resolved' ORDER BY c.id LIMIT
    1), COALESCE(f.updated_at, now()) FROM files f WHERE f.state = 'duplicate_resolved' ON CONFLICT
    (file_id) DO NOTHING` string, unmodified;
    (2) a NEW orphaned-marker delete: `DELETE FROM dedup_resolution dr USING files f WHERE dr.file_id =
    f.id AND f.state <> 'duplicate_resolved'`.
    No DDL, no model import, no f-string/`.format`/interpolation. Include the CRITICAL banner comment
    stating the migration must never reference `saq_jobs`. Document in the module docstring: (a) that
    this is a bidirectional reconcile making `marker ≡ state` at the cutover instant and why the failure
    mode is safe (a wrongly-deleted marker makes its file reappear for re-review; a wrongly-kept one
    would hide it forever); (b) the `downgrade()` choice per D-04 (Claude's Discretion) — choose EITHER a
    documented-lossy `DELETE FROM dedup_resolution` (034 precedent, cannot distinguish repaired from
    go-forward rows) OR a no-op, and state which and why in the docstring.
  </action>
  <verify>
    <automated>uv run python -c "import importlib.util,pathlib; s=importlib.util.spec_from_file_location('m','alembic/versions/035_reconcile_dedup_resolution.py'); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); assert m.revision=='035' and m.down_revision=='034'"</automated>
  </verify>
  <acceptance_criteria>
    - `alembic/versions/035_reconcile_dedup_resolution.py` exists; `revision == "035"`, `down_revision == "034"`.
    - File contains the literal `ON CONFLICT (file_id) DO NOTHING` and both `WHERE f.state = 'duplicate_resolved'` (insert) and `f.state <> 'duplicate_resolved'` (delete) clauses.
    - `grep -v '^#' alembic/versions/035_reconcile_dedup_resolution.py | grep -c saq_jobs` returns 0 (no saq_jobs reference outside comments).
    - No `op.add_column` / `op.create_table` / `op.create_index` (no DDL) and no `.format(` / `f"""` / `f'''` in the file.
    - Module docstring states the downgrade choice (lossy-DELETE or no-op) explicitly.
    - `uv run ruff check alembic/versions/035_reconcile_dedup_resolution.py` exits 0.
  </acceptance_criteria>
  <done>035 exists, header correct, two static statements, no DDL, no saq_jobs, downgrade choice documented, ruff clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration test — both directions, idempotency, empty autogenerate diff</name>
  <files>tests/integration/test_migrations/test_migration_035_reconcile_dedup_resolution.py</files>
  <read_first>
    - tests/integration/test_migrations/test_migration_034_backfill_cloud_awaiting.py (mirror 1:1: DB-free asserts at ~:74-106, integration body ~:156-231, _SEED_FILE_SQL, _diffs_touching_* empty-diff helper, downgrade + cleanup)
    - tests/integration/test_migrations/conftest.py (MIGRATIONS_TEST_DATABASE_URL, _build_alembic_config, downgrade_to, upgrade_to)
    - .planning/phases/84-dedup-fingerprint-progress-cutover/84-PATTERNS.md (§ test_migration_035 — the exact scaffold + FOOTGUN)
  </read_first>
  <behavior>
    - test_revision_identifiers_are_bare_numbers: revision=="035", down_revision=="034", branch_labels is None (module loaded by path via importlib.util.spec_from_file_location — name starts with a digit).
    - test_migration_never_references_saq_jobs: line scan excluding the banner comment finds no saq_jobs.
    - test_backfill_sql_is_static_and_parameter_free: "ON CONFLICT (file_id) DO NOTHING" present; ".format(" absent; f'''/f""" absent.
    - Integration (real PG, @pytest.mark.asyncio): downgrade_to(base) → upgrade_to("034"); seed a corpus covering BOTH directions — (a) state='duplicate_resolved' + no marker → gains one (canonical derived by the subquery); (b) orphaned marker (state<>'duplicate_resolved', marker present) → deleted; (c) control non-resolved file, no marker → stays row-less; (d) resolved file with a pre-existing marker → DO NOTHING, unchanged, no dup. Snapshot files.state before; assert byte-unchanged after (035 is READ-ONLY on files.state). upgrade_to("035"); assert per-file marker presence/absence. Idempotency: re-exec module._BACKFILL_DEDUP in a fresh engine.begin(); assert GROUP BY file_id HAVING count(*)>1 == [] and stable counts. Empty diff: _diffs_touching_035 with empty table/index/column sets → offenders == []. Downgrade: run it, assert its documented effect, cleanup DELETE FROM dedup_resolution, finally downgrade_to(base).
  </behavior>
  <action>
    Create `tests/integration/test_migrations/test_migration_035_reconcile_dedup_resolution.py` mirroring
    `test_migration_034_backfill_cloud_awaiting.py` 1:1. Copy the three DB-free assertions, the
    `_SEED_FILE_SQL` helper (agent_id `'legacy-application-server'` — the 012-seeded RESTRICT FK), the
    `_build_alembic_config`/`downgrade_to`/`upgrade_to` scaffold from conftest, and the empty-autogenerate
    -diff helper (rename `_diffs_touching_035` with empty `_O35_TABLES`/`_INDEXES`/`_COLUMNS` sets since
    `035` touches no ORM-mapped schema). Design the seed corpus for BOTH reconcile directions per the
    behavior block. Copy the docstring FOOTGUN warning verbatim (migration DB is on port 5433 but
    `MIGRATIONS_TEST_DATABASE_URL` defaults to 5432 and `just test-bucket` does not export it).
    This test needs no saq_jobs — do not import the SAQ/queue fixtures.
  </action>
  <verify>
    <automated>MIGRATIONS_TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_migrations_test" uv run pytest tests/integration/test_migrations/test_migration_035_reconcile_dedup_resolution.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `MIGRATIONS_TEST_DATABASE_URL=...5433...phaze_migrations_test just test-bucket integration` runs the file and it exits 0 (test-db provisioned via `just test-db` first).
    - The empty-autogenerate-diff assertion passes (`035` introduces no schema drift).
    - The files.state snapshot assertion proves `035` is read-only on `files.state`.
    - The idempotency assertion proves re-running `_BACKFILL_DEDUP` inserts zero duplicate rows.
    - The both-direction corpus proves: resolved-no-marker gains a marker (with derived canonical); orphaned-marker (state<>'duplicate_resolved') is deleted; pre-existing marker is untouched (DO NOTHING).
    - `uv run ruff check` and `uv run mypy .` exit 0.
  </acceptance_criteria>
  <done>Migration test green in isolation on port 5433, covering both directions + idempotency + empty diff + byte-unchanged files.state + downgrade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration → Postgres | `035` executes DDL-free SQL at app startup (`auto_migrate=True`, `main.py:87`) and in the migration harness. No user input crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01-01 | Tampering | `035` SQL statements | accept | Both statements are static, parameter-free string constants with no interpolation/f-string/`.format` — zero SQL-injection surface (asserted by `test_backfill_sql_is_static_and_parameter_free`). |
| T-84-01-02 | Denial of Service | orphaned-marker `DELETE` on the live corpus | accept | Set-based single-statement DELETE over an indexed FK (`uq_dedup_resolution_file_id`); the safe failure mode (reappear-for-review) is documented. Bounded by corpus size; runs once at deploy. |
| T-84-01-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies (Package Legitimacy Audit N/A per RESEARCH § Environment Availability). |
</threat_model>

<verification>
- Migration test green in isolation on port 5433.
- `035` header/statements pass the DB-free structural asserts.
- No `saq_jobs` reference; no DDL; empty autogenerate diff; `files.state` byte-unchanged.
</verification>

<success_criteria>
`035_reconcile_dedup_resolution.py` reconciles `dedup_resolution` to `files.state` in both directions,
is data-only with an empty autogenerate diff, and its migration test proves both directions +
idempotency + read-only-on-files.state in isolation. This unblocks the Wave-2 reader flips and makes the
Phase-79 hard `duplicate_resolved` invariant satisfiable on the live corpus.
</success_criteria>

<output>
Create `.planning/phases/84-dedup-fingerprint-progress-cutover/84-01-SUMMARY.md` when done.
</output>
