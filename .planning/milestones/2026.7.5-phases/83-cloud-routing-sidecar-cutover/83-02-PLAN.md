---
phase: 83-cloud-routing-sidecar-cutover
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/034_backfill_cloud_awaiting.py
  - tests/integration/test_migrations/test_migration_034_backfill_cloud_awaiting.py
autonomous: true
requirements: [SIDECAR-01]
must_haves:
  truths:
    - "D-04: a repair migration 034 re-runs 032's _BACKFILL_CLOUD_AWAITING INSERT…SELECT with ON CONFLICT (file_id) DO NOTHING, backfilling a cloud_job(status='awaiting') row for every file left at state='awaiting_cloud' with no sidecar row since 032"
    - "D-04: 034 chains down_revision='033' (verified alembic head), is SYNC (def upgrade / plain op.execute), uses static parameter-free SQL (no interpolation), and touches NO ORM-mapped schema — the 'awaiting' CHECK value and ix_cloud_job_awaiting already shipped in 032, so alembic revision --autogenerate stays EMPTY"
    - "Migration 034 lands BEFORE any reader cuts over to the sidecar (83-06), so held files repaired here are visible to the drain query instead of stranding"
  artifacts:
    - path: "alembic/versions/034_backfill_cloud_awaiting.py"
      provides: "one-shot corpus repair backfill of missing awaiting cloud_job rows"
      contains: "down_revision = \"033\""
    - path: "tests/integration/test_migrations/test_migration_034_backfill_cloud_awaiting.py"
      provides: "idempotent-backfill test + empty-autogenerate-diff assertion"
  key_links:
    - from: "alembic/versions/034_backfill_cloud_awaiting.py upgrade()"
      to: "cloud_job (INSERT … SELECT … 'awaiting' FROM files WHERE state='awaiting_cloud' ON CONFLICT DO NOTHING)"
      via: "op.execute static SQL"
      pattern: "ON CONFLICT \\(file_id\\) DO NOTHING"
---

<objective>
Add migration `034` — a one-shot data-only repair that backfills the missing `cloud_job(status='awaiting')`
sidecar rows for every file parked in `AWAITING_CLOUD` since `032` (the D-04 corpus gap). Without this,
the drain-candidate cutover (83-06) reads the sidecar and strands every already-held file.

Purpose: The go-forward writer (83-01/83-05) fixes NEW holds; `034` repairs the EXISTING corpus so the
hard shadow invariant `AWAITING_CLOUD ⇒ cloud_job(status='awaiting')` holds for all held files.

Output: `034` migration (sync, static SQL, empty autogenerate diff); an idempotent-backfill migration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-cloud-routing-sidecar-cutover/83-CONTEXT.md
@.planning/phases/83-cloud-routing-sidecar-cutover/83-RESEARCH.md
@.planning/phases/83-cloud-routing-sidecar-cutover/83-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 034 — repair backfill of missing awaiting cloud_job rows</name>
  <files>alembic/versions/034_backfill_cloud_awaiting.py</files>
  <read_first>
    - alembic/versions/032_add_derived_status_schema.py — the `_BACKFILL_CLOUD_AWAITING` statement at :96-102
      (the exact INSERT … SELECT gen_random_uuid(), f.id, 'awaiting' FROM files f WHERE f.state='awaiting_cloud'
      ON CONFLICT (file_id) DO NOTHING to re-run verbatim) and the CHECK/index at :144-156 (already shipped —
      do NOT re-add).
    - alembic/versions/033_add_analysis_completed_xor_failed.py — confirm `revision="033" down_revision="032"`
      so 034 chains `down_revision="033"`; mirror its sync `def upgrade()/def downgrade()` structure.
    - .planning/phases/83-cloud-routing-sidecar-cutover/83-RESEARCH.md — §"Migration 034" (lines 263-287):
      the sync structure, the empty-autogenerate note, the downgrade lossiness note.
    - .planning/phases/83-cloud-routing-sidecar-cutover/83-PATTERNS.md — the "alembic/versions/034_*.py"
      section.
  </read_first>
  <action>
    Create `alembic/versions/034_backfill_cloud_awaiting.py` with `revision = "034"`, `down_revision = "033"`,
    `branch_labels = None`, `depends_on = None`. `upgrade()` runs a single static `op.execute(...)` of 032's
    `_BACKFILL_CLOUD_AWAITING` verbatim: `INSERT INTO cloud_job (id, file_id, status) SELECT gen_random_uuid(),
    f.id, 'awaiting' FROM files f WHERE f.state = 'awaiting_cloud' ON CONFLICT (file_id) DO NOTHING`. No
    parameter interpolation, no f-strings, no schema DDL (the 'awaiting' CHECK value and ix_cloud_job_awaiting
    already exist from 032). `downgrade()` is a documented-lossy no-op OR `op.execute("DELETE FROM cloud_job
    WHERE status = 'awaiting'")` with a docstring stating the lossiness (it cannot distinguish repaired rows
    from live holds). Migration is SYNC — plain `op.execute`, no async. Do NOT add any ORM __table_args__
    change (nothing new to mirror; autogenerate must stay empty per 77 D-01).
  </action>
  <acceptance_criteria>
    - `alembic/versions/034_backfill_cloud_awaiting.py` exists with `revision = "034"` and
      `down_revision = "033"`.
    - `upgrade()` contains the literal `ON CONFLICT (file_id) DO NOTHING` and `WHERE f.state = 'awaiting_cloud'`
      and inserts `status` `'awaiting'`; the SQL is a single static string with no `%`/`.format`/f-string
      interpolation.
    - `MIGRATIONS_TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_migrations_test"
      DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze" uv run alembic upgrade head`
      applies 034 cleanly on a fresh test DB (run under `just test-db` provisioning).
    - `uv run ruff check alembic/versions/034_backfill_cloud_awaiting.py` passes.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check alembic/versions/034_backfill_cloud_awaiting.py</automated>
  </verify>
  <done>034 exists, chains 033, static parameter-free SQL, sync, no ORM schema change.</done>
</task>

<task type="auto">
  <name>Task 2: Migration test — idempotent backfill + empty autogenerate diff</name>
  <files>tests/integration/test_migrations/test_migration_034_backfill_cloud_awaiting.py</files>
  <read_first>
    - tests/integration/test_migrations/test_migration_032_additive_schema.py — the per-migration harness to
      copy: `_build_alembic_config(...)`, `upgrade_to(cfg, "033")`, seed corpus, `upgrade_to(cfg, "034")`,
      assert, re-run for idempotency, `compare_metadata` empty-diff assertion, downgrade.
    - tests/integration/test_migrations/conftest.py — the migrations-test fixtures (engine on the migrations
      test DB, config builder).
    - .planning/phases/83-cloud-routing-sidecar-cutover/83-RESEARCH.md — §"Environment Availability"
      (the MIGRATIONS_TEST_DATABASE_URL port-5433 footgun) and §"Validation Architecture" the D-04 row.
    - .planning/phases/83-cloud-routing-sidecar-cutover/83-VALIDATION.md — the D-04 rows (repair + autogenerate
      parity) and the port-footgun warning.
  </read_first>
  <action>
    Create the migration test mirroring `test_migration_032_additive_schema.py`. Upgrade to `033`, seed the
    corpus with FileRecord rows at `state='awaiting_cloud'` that have NO cloud_job row (plus a control file
    NOT in AWAITING_CLOUD, and a file already carrying a cloud_job row to prove ON CONFLICT DO NOTHING skips
    it). Upgrade to `034`. Assert: (a) every seeded awaiting_cloud file now has exactly one
    cloud_job(status='awaiting') row; (b) the control non-awaiting file has none; (c) the pre-existing
    cloud_job row is UNCHANGED (idempotent skip). Re-run upgrade path / re-execute the backfill statement to
    prove idempotency (no duplicate rows — uq_cloud_job_file_id + ON CONFLICT DO NOTHING). Add the empty
    autogenerate-diff assertion via the existing `compare_metadata` pattern (034 touches no ORM schema).
    Then downgrade and assert it runs. MUST export both DB URLs — the test/harness has to target port 5433,
    not the 5432 default (`MIGRATIONS_TEST_DATABASE_URL` and `DATABASE_URL`).
  </action>
  <acceptance_criteria>
    - Command (both URLs exported, port 5433):
      `MIGRATIONS_TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_migrations_test"
      just test-bucket integration` runs the migration test green IN ISOLATION.
    - The test asserts one awaiting cloud_job row per seeded awaiting_cloud file, zero for the control file,
      an unchanged pre-existing row, and idempotency on re-run (no duplicate rows).
    - The test asserts an EMPTY autogenerate diff after 034 (no ORM-mapped schema drift).
    - `uv run ruff check tests/integration/test_migrations/test_migration_034_backfill_cloud_awaiting.py`
      passes.
  </acceptance_criteria>
  <verify>
    <automated>MIGRATIONS_TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_migrations_test" just test-bucket integration</automated>
  </verify>
  <done>Migration test green in isolation with both DB URLs exported; idempotency + empty-diff proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/CI ↔ Postgres (migration) | `034` executes DDL/DML at deploy time; the only input is the static backfill SQL — no user-controlled value reaches it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-03 | Tampering | migration `034` backfill SQL | mitigate | `034` is static, parameter-free SQL (no interpolation) re-run verbatim from 032 — Task 1 acceptance criterion asserts no `%`/`.format`/f-string; Task 2 exercises it against the real test DB |
| T-83-04-IDEMP | Tampering | re-run of the backfill | mitigate | `ON CONFLICT (file_id) DO NOTHING` + `uq_cloud_job_file_id` make a double-run inert; Task 2 asserts no duplicate rows and an unchanged pre-existing row |
| T-83-SC | Tampering | package installs | accept | No new packages installed (zero-new-deps milestone constraint) |
</threat_model>

<verification>
- `just test-bucket integration` (with `MIGRATIONS_TEST_DATABASE_URL` exported on port 5433) green in isolation.
- `uv run alembic upgrade head` then `uv run alembic revision --autogenerate` produces an empty diff.
- Grep: `034` chains `down_revision = "033"`; no ORM `__table_args__` change in this plan.
</verification>

<success_criteria>
- The already-held `AWAITING_CLOUD` corpus is repaired: every such file gains its `awaiting` sidecar row.
- `034` is sync, static, idempotent, and produces no autogenerate drift.
- Sequenced in Wave 1 so it precedes the reader cutover (83-06).
</success_criteria>

<output>
Create `.planning/phases/83-cloud-routing-sidecar-cutover/83-02-SUMMARY.md` when done.
</output>
