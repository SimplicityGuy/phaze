---
phase: 77-additive-schema-rescan-wipe-fix-migration-032
plan: 03
type: execute
wave: 2
depends_on: [77-02]
files_modified:
  - alembic/versions/032_add_derived_status_schema.py
  - tests/integration/test_migrations/test_migration_032_additive_schema.py
autonomous: true
requirements: [MIG-01, PERF-01]

must_haves:
  truths:
    - "`alembic upgrade` applies `032` on a seeded corpus creating the analyze/metadata failure-marker columns, the `dedup_resolution` table, the widened cloud_job status CHECK ('awaiting'), and every partial index — with `files.state` byte-unchanged and `saq_jobs` never referenced (ROADMAP SC#1, MIG-01)."
    - "D-03: analyze backfills, metadata does NOT — `analysis.failed_at` is set for every `state='analysis_failed'` file via an UPSERT (INSERT..SELECT..ON CONFLICT (file_id) DO UPDATE, because report_analysis_failed writes no analysis row — RESEARCH Pitfall 2); `metadata.failed_at` stays all-NULL (no historical source)."
    - "D-04/D-06: `state='awaiting_cloud'` → cloud_job status='awaiting'; `state='pushing'/'pushed'` gap-fill → cloud_job status='uploading'/'uploaded' for rows missing one; D-05: LOCAL_ANALYZING gets NO sidecar row (derived later)."
    - "D-07: dedup backfill inserts a `dedup_resolution` row per `state='duplicate_resolved'` file, deriving `canonical_file_id` deterministically (ORDER BY c.id LIMIT 1 among non-resolved same-sha256 members; NULL if none — RESEARCH Pitfall 4)."
    - "PERF-01 / ROADMAP SC#2: `alembic revision --autogenerate` against the `032` head produces an EMPTY diff (no op.add_column/op.create_index/op.create_table/op.*), proving ORM↔DB parity."
    - "D-09: `032.downgrade()` is minimal best-effort DDL reversal only (drops indexes/table/columns, restores the 6-member CHECK); data backfills use a no-op downgrade. Per-migration integration test focuses on the upgrade+backfill path."
    - "D-02: the migration docstring flags that the reader phase's `done(metadata)` will tighten to `failed_at IS NULL`, and that LOCAL_ANALYZING is derived, not stored (D-05)."
  artifacts:
    - path: "alembic/versions/032_add_derived_status_schema.py"
      provides: "additive migration 032: columns, dedup_resolution table, CHECK widen, partial indexes, backfills, minimal downgrade"
      contains: "revision"
      min_lines: 90
    - path: "tests/integration/test_migrations/test_migration_032_additive_schema.py"
      provides: "per-migration integration test + saq_jobs grep guard + empty-autogenerate-diff assertion"
      contains: "test_migration_never_references_saq_jobs"
      min_lines: 80
  key_links:
    - from: "alembic/versions/032 op.create_index names + postgresql_where"
      to: "ORM __table_args__ (Plan 02)"
      via: "byte-identical names + normalized predicate text → empty autogenerate diff"
      pattern: "ix_analysis_failed|ix_metadata_failed|ix_cloud_job_awaiting|ix_fprint_success|ix_analysis_completed"
    - from: "032 analyze-failed backfill"
      to: "analysis.failed_at"
      via: "INSERT..SELECT FROM files WHERE state='analysis_failed' ON CONFLICT (file_id) DO UPDATE"
      pattern: "ON CONFLICT \\(file_id\\) DO UPDATE"
    - from: "032 backfill sources"
      to: "files.state"
      via: "READ-ONLY SELECT of state literals; files.state never written"
      pattern: "WHERE.*state = 'analysis_failed'|duplicate_resolved|awaiting_cloud|pushing|pushed"
---

<objective>
Write additive migration `032` and its per-migration integration test. `032` mirrors the ORM
declarations from Plan 02 into DDL, backfills the new markers/sidecar rows from `files.state`
(read-only), and never touches `files.state` or `saq_jobs`. The integration test proves the
upgrade + backfill on a seeded corpus, the `files.state`-unchanged invariant, the `saq_jobs`
banner guard, and — the sharpest PERF-01 risk — an EMPTY `--autogenerate` diff.

Purpose: this is the substrate the entire milestone's derivation layer reads. It must apply
cleanly on a live-corpus copy while leaving the legacy authority (`files.state`) byte-for-byte
intact, and it must round-trip against the ORM so future `autogenerate` runs stay clean.
Output: 1 new migration + 1 new integration test (both in the `integration` bucket).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-additive-schema-rescan-wipe-fix-migration-032/77-CONTEXT.md
@.planning/phases/77-additive-schema-rescan-wipe-fix-migration-032/77-RESEARCH.md
@.planning/phases/77-additive-schema-rescan-wipe-fix-migration-032/77-PATTERNS.md
@.planning/phases/77-additive-schema-rescan-wipe-fix-migration-032/77-VALIDATION.md
@src/phaze/models/dedup_resolution.py
@src/phaze/models/analysis.py
@src/phaze/models/cloud_job.py

<interfaces>
<!-- FileState string values (models/file.py) that drive the READ-ONLY backfill WHERE clauses: -->
  analysis_failed · duplicate_resolved · awaiting_cloud · pushing · pushed · local_analyzing
<!-- cloud_job status mapping for the backfill: awaiting_cloud→'awaiting', pushing→'uploading', pushed→'uploaded'. -->
<!-- Migration head is 031 → this is revision "032", down_revision "031". -->
<!-- Partial-index set (must match Plan 02 __table_args__ names + normalized predicate text EXACTLY):
     ix_analysis_completed  (analysis,  analysis_completed_at IS NOT NULL)
     ix_analysis_failed     (analysis,  failed_at IS NOT NULL)
     ix_metadata_failed     (metadata,  failed_at IS NOT NULL)
     ix_cloud_job_awaiting  (cloud_job, status = 'awaiting')
     ix_fprint_success      (fingerprint_results, status = ANY (ARRAY['success','completed']))  -- HAZARD, see Task 1 -->
</interfaces>

<interfaces>
<!-- Migration test harness (tests/integration/test_migrations/conftest.py) — reuse, don't reinvent: -->
  from tests.integration.test_migrations.conftest import (
      MIGRATIONS_TEST_DATABASE_URL, _build_alembic_config, downgrade_to, upgrade_to)
  # pattern: downgrade_to(cfg,"base") → upgrade_to(cfg,"031") → seed → upgrade_to(cfg,"032")
  #          → asserts → downgrade_to(cfg,"031") → finally engine.dispose() + downgrade_to(cfg,"base")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 032 — additive DDL + set-based backfill + minimal downgrade</name>
  <files>alembic/versions/032_add_derived_status_schema.py</files>
  <read_first>
    - alembic/versions/028_add_analysis_completed_at.py:1-45 (revision header + docstring banner + additive nullable column idiom)
    - alembic/versions/026_add_cloud_job_kube_columns.py:45-66 (CHECK-widen via bare name "status_enum" + downgrade restore)
    - alembic/versions/018_add_analysis_window_table.py:44-85 (op.create_table + op.f(...) PK/UQ/FK naming + drop order)
    - alembic/versions/019_add_proposals_pending_unique_index.py:47-78 (partial-index create + static backfill idiom)
    - alembic/versions/016_backfill_scan_batches_completed_at.py:39-58 (set-based op.execute(text(...)) static-literal backfill + no-op data downgrade)
    - 77-RESEARCH.md Pitfall 1 (IN→= ANY), Pitfall 2 (analyze upsert), Pitfall 4 (dedup canonical) + 77-CONTEXT.md D-02..D-09 + 77-PATTERNS.md §032 (sub-patterns A–E)
  </read_first>
  <action>
    Create `alembic/versions/032_add_derived_status_schema.py` with `revision = "032"`,
    `down_revision = "031"`, `branch_labels = None`, `depends_on = None`. Docstring carries: the one-line
    summary + "(Phase 77, MIG-01/PERF-01)"; the CRITICAL "NEVER reference saq_jobs" banner (028/031
    precedent); a D-02 callout (reader phase will tighten `done(metadata)` to `failed_at IS NULL`); a D-03
    callout (analyze backfills, metadata does NOT); a D-05 callout (LOCAL_ANALYZING is derived, no sidecar).
    `upgrade()` (sync, plain `op.*`), in order:
    (A) op.add_column x4: `failed_at` (DateTime(timezone=True), nullable) + `error_message` (Text, nullable)
        on both `analysis` and `metadata`.
    (B) op.create_table `dedup_resolution` mirroring the ORM: `id` UUID PK, `file_id` UUID NOT NULL,
        `canonical_file_id` UUID NULLABLE, `resolved_at` timestamptz server_default now() NOT NULL,
        `created_at`/`updated_at` timestamptz server_default now() NOT NULL; PK/UQ/FK named with `op.f(...)`
        (`pk_dedup_resolution`, `uq_dedup_resolution_file_id`, `fk_dedup_resolution_file_id_files`,
        `fk_dedup_resolution_canonical_file_id_files`). Add `from sqlalchemy.dialects import postgresql` for UUID.
    (C) Widen CHECK: `op.drop_constraint("status_enum","cloud_job",type_="check")` then
        `op.create_check_constraint("status_enum","cloud_job", <7-member list incl 'awaiting'>)` — pass the
        BARE name `"status_enum"` (the ck_%(table_name)s_%(constraint_name)s convention re-prefixes it; the
        already-prefixed name would double-prefix — RESEARCH/PATTERNS Pattern 1).
    (D) op.create_index for the 5 partial indexes with the EXACT names + predicate text from the interfaces
        block (must equal Plan 02's __table_args__). Spell fingerprint success as
        `status = ANY (ARRAY['success','completed'])`, NEVER bare `status IN (...)`. Plain transactional
        builds (house style — no CONCURRENTLY anywhere in-tree).
    (E) Backfills via `op.execute(sa.text(...))`, static literals only (no interpolation, no model import,
        no saq_jobs — S608 clean):
          - analyze-failed: INSERT INTO analysis (id,file_id,failed_at,error_message,created_at,updated_at)
            SELECT gen_random_uuid(), f.id, COALESCE(f.updated_at, now()), 'backfilled from ANALYSIS_FAILED',
            now(), now() FROM files f WHERE f.state='analysis_failed'
            ON CONFLICT (file_id) DO UPDATE SET failed_at=COALESCE(analysis.failed_at, EXCLUDED.failed_at),
            error_message=COALESCE(analysis.error_message, EXCLUDED.error_message)  -- UPSERT, Pitfall 2.
            Leave analysis_completed_at NULL for these rows.
          - dedup: INSERT INTO dedup_resolution (id,file_id,canonical_file_id,resolved_at) SELECT
            gen_random_uuid(), f.id, (SELECT c.id FROM files c WHERE c.sha256_hash=f.sha256_hash AND
            c.state<>'duplicate_resolved' ORDER BY c.id LIMIT 1), COALESCE(f.updated_at, now()) FROM files f
            WHERE f.state='duplicate_resolved' ON CONFLICT (file_id) DO NOTHING  -- canonical NULL if none, Pitfall 4.
          - awaiting: INSERT cloud_job status='awaiting' (s3_key/upload_id NULL) for files.state='awaiting_cloud'
            missing a cloud_job row (ON CONFLICT (file_id) DO NOTHING — reuse uq_cloud_job_file_id). D-04.
          - pushing/pushed: gap-fill cloud_job status='uploading'/'uploaded' for files.state='pushing'/'pushed'
            missing a row (ON CONFLICT (file_id) DO NOTHING). D-06.
          - metadata: NO backfill (D-03).
        NEVER write files.state in any statement.
    `downgrade()` (D-09, minimal best-effort): drop the 5 indexes, drop dedup_resolution table, restore the
    6-member `status_enum` CHECK, drop the 4 columns — reverse order. Data backfills are NOT reversed (no-op,
    016 precedent). Do NOT gold-plate.
  </action>
  <verify>
    <automated>uv run ruff check alembic/versions/032_add_derived_status_schema.py && uv run python -c "import importlib.util,pathlib; p=pathlib.Path('alembic/versions/032_add_derived_status_schema.py'); s=importlib.util.spec_from_file_location('m032',p); m=importlib.util.module_from_spec(s); s.loader.exec_module(m); assert m.revision=='032' and m.down_revision=='031' and m.branch_labels is None; body=p.read_text(); assert 'saq_jobs' not in body.replace('never reference','') or True; import re; assert not [l for l in body.splitlines() if 'saq_jobs' in l and not l.lstrip().startswith('#') and 'never reference' not in l.lower()]; assert 'status IN (' not in body.split('def downgrade')[0].replace(\"'awaiting'\",'') or 'ANY (ARRAY' in body; print('OK 032 static checks')"</automated>
  </verify>
  <acceptance_criteria>
    - `revision=='032'`, `down_revision=='031'`, `branch_labels is None`.
    - Docstring contains the `saq_jobs` NEVER-reference banner and the D-02/D-03/D-05 callouts.
    - No non-comment line references `saq_jobs` (banner-grep style).
    - The analyze-failed backfill is an `ON CONFLICT (file_id) DO UPDATE` upsert (NOT a plain UPDATE); the dedup backfill derives `canonical_file_id` via `ORDER BY c.id LIMIT 1` and is nullable-safe.
    - The fingerprint-success index (if present) uses `= ANY (ARRAY['success','completed'])`; no bare `status IN (...)` partial index is created.
    - No statement writes `files.state`; ruff clean.
  </acceptance_criteria>
  <done>Migration 032 exists: additive columns/table/CHECK/indexes + read-only set-based backfills + minimal downgrade; static/lint checks green.</done>
</task>

<task type="auto">
  <name>Task 2: Write the 032 integration test — upgrade/backfill/invariants + saq guard + empty-autogenerate-diff</name>
  <files>tests/integration/test_migrations/test_migration_032_additive_schema.py</files>
  <read_first>
    - tests/integration/test_migrations/test_migration_031_route_control.py (COPY the 3-part structure: by-path _load_migration loader; DB-free `test_revision_identifiers_are_bare_numbers` + `test_migration_never_references_saq_jobs` grep guard; the async upgrade/downgrade integration body with try/finally)
    - tests/integration/test_migrations/conftest.py (MIGRATIONS_TEST_DATABASE_URL, _build_alembic_config, upgrade_to, downgrade_to — reuse; asyncio.to_thread wrapping to avoid nested event loop)
    - alembic/env.py (target_metadata = Base.metadata, compare_type=True — why the empty-diff comparison is meaningful)
    - 77-VALIDATION.md §"Observable Behaviors" (the 6 asserts) + §"Manual-Only" (empty-diff fallback) + 77-CONTEXT.md D-03 (metadata.failed_at all NULL)
  </read_first>
  <action>
    Create the test mirroring `test_migration_031_route_control.py` exactly, pointed at
    `alembic/versions/032_add_derived_status_schema.py`.
    (1) DB-free statics: `test_revision_identifiers_are_bare_numbers` (032/031/None) and
        `test_migration_never_references_saq_jobs` (copy the exact list-comprehension grep guard).
    (2) Integration body (`@pytest.mark.asyncio`): `downgrade_to(cfg,"base")` → `upgrade_to(cfg,"031")` →
        seed a small corpus at 031 — files in each of `analysis_failed`, `duplicate_resolved`, `awaiting_cloud`,
        `pushing`, `pushed`, plus a matching `sha256_hash` group (≥2 rows sharing a hash, one resolved one not)
        so canonical derivation has a target, and at least one `analysis_failed` file WITHOUT any pre-existing
        analysis row (to exercise the upsert branch — Pitfall 2). Snapshot `files.state` for all seeded rows.
        `upgrade_to(cfg,"032")`. Assert: (a) `analysis`/`metadata` have `failed_at`+`error_message` columns;
        (b) `dedup_resolution` table exists; (c) `status_enum` CHECK admits `'awaiting'`; (d) COUNT(analysis
        WHERE failed_at IS NOT NULL) == COUNT(files WHERE state='analysis_failed'); (e) all `metadata.failed_at`
        IS NULL (D-03 no backfill); (f) cloud_job rows with status in ('awaiting','uploading','uploaded') exist
        for the awaiting/pushing/pushed files; (g) `dedup_resolution` row count == COUNT(files
        state='duplicate_resolved'); (h) `files.state` byte-unchanged vs the snapshot; (i) each of the 5 partial
        indexes present in `pg_indexes`. Then `downgrade_to(cfg,"031")` and assert the additive objects are gone.
        Wrap in try/finally with `engine.dispose()` + `downgrade_to(cfg,"base")`.
    (3) Empty-autogenerate-diff assertion (PERF-01 SC#2 — new capability, no in-tree precedent): with the DB
        at `032` head, run Alembic autogenerate in-process (via `alembic.autogenerate.compare_metadata` against
        `Base.metadata` on a MigrationContext with `compare_type=True`, or `command.revision(autogenerate=True)`
        into a temp dir then parse the body) and assert the produced upgrade op list is EMPTY — no add_column,
        create_index, create_table, or create/drop constraint for the 032 objects. CONTINGENCY: if the ONLY
        residual diff is a churn on `ix_fprint_success` (the IN→=ANY normalization hazard, RESEARCH Pitfall 1),
        the fix is to drop `ix_fprint_success` from BOTH the migration (Plan 03 Task 1) and the ORM
        (models/fingerprint.py, Plan 02) and defer it to Phase 82 (nothing in Phase 77 reads it); record that
        decision in the phase SUMMARY/VERIFICATION. Any OTHER non-empty diff is a hard failure to fix, not defer.
    Bucket: `integration`. NO fenced implementation in reasoning — follow the 031 analog.
  </action>
  <verify>
    <automated>just integration-test 2>&1 | tail -30 || uv run pytest tests/integration/test_migrations/test_migration_032_additive_schema.py -x 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `test_revision_identifiers_are_bare_numbers` and `test_migration_never_references_saq_jobs` pass DB-free.
    - The integration body passes on the seeded corpus: backfill counts match legacy `files.state` counts; `metadata.failed_at` all NULL; awaiting/uploading/uploaded cloud_job rows present; `files.state` byte-unchanged; all 5 partial indexes in `pg_indexes`; downgrade removes the additive objects.
    - The empty-autogenerate-diff assertion passes (or, if only `ix_fprint_success` churns, it is removed from both migration and ORM and the diff is then empty — recorded in SUMMARY).
    - `just test-bucket integration` passes in isolation (per-bucket hermeticity).
  </acceptance_criteria>
  <done>Per-migration integration test green: upgrade+backfill+invariants proven, saq guard enforced, empty-autogenerate-diff verified (or ix_fprint_success deferred per the recorded contingency).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration author → live Postgres corpus | Migration DDL/DML runs with schema-owner privilege against the ~200K-file corpus; the only "input" is static SQL literals authored in-file (no runtime/user input). |
| Alembic migration ↔ SAQ-owned `saq_jobs` | `saq_jobs` is owned by SAQ (init_db + saq_versions); an Alembic migration touching it would collide with SAQ's own schema management. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01 | Tampering | `032` backfill SQL (`op.execute(sa.text(...))`) | mitigate | Static string literals only (state values `'analysis_failed'`/`'duplicate_resolved'`/etc.), no interpolation, no model import — no injection surface (bandit S608, 016 precedent). |
| T-77-02 | Tampering / Repudiation of queue state | migration body possibly referencing `saq_jobs` | mitigate | CRITICAL banner in the docstring + `test_migration_never_references_saq_jobs` grep guard in the test; the migration touches only `analysis`/`metadata`/`dedup_resolution`/`cloud_job`. |
| T-77-07 | Tampering | backfill accidentally writing `files.state` | mitigate | `files.state` is READ-only (SELECT source) in every backfill; the integration test snapshots and asserts `files.state` byte-unchanged before/after `032` (ROADMAP SC#1 hard invariant). |

_No package installs in this plan (zero new dependencies — milestone hard constraint); no `T-77-SC` supply-chain checkpoint applies._
</threat_model>

<verification>
- `just integration-test` green (ephemeral PG :5433 + `phaze_migrations_test`); `just test-bucket integration` green in isolation.
- DB-free guards (`test_revision_identifiers_are_bare_numbers`, `test_migration_never_references_saq_jobs`) pass without Postgres.
- Empty-`--autogenerate`-diff verified and recorded in the phase VERIFICATION (primary: automated in the test; fallback: the documented `alembic revision --autogenerate` manual step from 77-VALIDATION.md).
- `uv run ruff check .` + `uv run mypy .` + `pre-commit run --all-files` green.
- VERIFICATION note (RESEARCH §Runtime State): `032` is additive/non-destructive — NO cloud-lane drain is required (drain is the destructive `033`/Phase-90 gate); mid-transfer PUSHING rows may simply re-derive on the next reconcile.
</verification>

<success_criteria>
ROADMAP SC#1, SC#2, SC#4 satisfied: `032` applies on a corpus copy creating the failure markers,
dedup-marker table, widened cloud CHECK, and sidecar backfill rows with `files.state` byte-unchanged
and `saq_jobs` never referenced (MIG-01); every partial index exists and is `IS NOT NULL`/`= ANY`-shaped
and mirrored into the ORM so `--autogenerate` yields an empty diff (PERF-01); and `032.downgrade()`
reverses the additive DDL (minimal per D-09), all proven by the per-migration integration test.
</success_criteria>

<output>
Create `.planning/phases/77-additive-schema-rescan-wipe-fix-migration-032/77-03-SUMMARY.md` when done.
</output>
