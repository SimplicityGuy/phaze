---
phase: 80-recovery-re-enqueue-cutover
plan: 05
type: execute
wave: 3
depends_on: [80-03, 80-04]
files_modified:
  - tests/shared/test_reenqueue_reconcile_source_scan.py
  - tests/integration/test_stage_status_equivalence.py
autonomous: true
requirements: [READ-03]

must_haves:
  truths:
    - "SC-1: a mutation-proven AST source guard asserts ZERO `FileRecord.state` reads and ZERO `FileState.<member>` references in BOTH reenqueue.py and reconcile_cloud_jobs.py (both files end the phase at clean absence, so no allow-list is needed — strictly stronger than the dedup guard)"
    - "SC-1: the guard flags every syntactic form #1–#6 a drifter could reach for (attribute `.in_()`, `ast.Compare`, `.values(state=...)`, bare `file.state` read, `getattr(_, 'state')`, positional `.where(a,b,state_read)` — walking BOTH Call.args and Call.keywords, the Phase-83 blind spot the Phase-84 scanner already closes) and each was observed RED under its mutation then restored"
    - "D-11: the equivalence-test SCOPE comment (:415-427) is amended with the rejected-option rationale — `~inflight_clause` must never enter domain_completed_clause; the DERIV-04 equivalence test stays GREEN under that trap (silent no-op for drain/card), which is exactly why the recovery-layer regression in Plan 80-04, not this test, is the real lock"
  artifacts:
    - path: "tests/shared/test_reenqueue_reconcile_source_scan.py"
      provides: "clean-absence AST scanner over reenqueue.py + reconcile_cloud_jobs.py; crafted-string mutation self-tests for forms #1–#6 + a GREEN cloud_job.status false-positive check"
      contains: "FileRecord"
    - path: "tests/integration/test_stage_status_equivalence.py"
      provides: "D-11 rejected-option rationale appended to the SCOPE comment; *_inflight seed exclusion kept"
  key_links:
    - from: "tests/shared/test_reenqueue_reconcile_source_scan.py"
      to: "src/phaze/tasks/reenqueue.py"
      via: "ast.walk clean-absence scan of the named target files"
      pattern: "reenqueue|reconcile_cloud_jobs"
---

<objective>
Lock the phase's core guarantee with a mutation-proven AST source guard asserting **zero
`FileRecord.state` reads** (and zero `FileState.<member>` references) in both `reenqueue.py` and
`reconcile_cloud_jobs.py` — both at clean absence after Plans 80-04 and 80-03. Model it on the Phase-84
mutation-proven scanner (already closes both Phase-83 blind spots) and extend it to catch bare
`FileRecord.state` / `file.state` reads and `getattr(_, "state")`. Also amend the DERIV-04 equivalence
test's SCOPE comment with the D-11 rejected-option rationale. This plan runs in Wave 3 because the guard
scans both cutover targets for clean absence — it can only be green once both cutovers have landed.

Purpose: turn the "no state read" invariant into a self-testing, drift-proof gate; document the D-11 trap
at the equivalence-test boundary.
Output: the new AST guard test file + the equivalence SCOPE amendment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/80-recovery-re-enqueue-cutover/80-CONTEXT.md
@.planning/phases/80-recovery-re-enqueue-cutover/80-RESEARCH.md
@.planning/phases/80-recovery-re-enqueue-cutover/80-PATTERNS.md
@.planning/phases/80-recovery-re-enqueue-cutover/80-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mutation-proven AST "zero FileRecord.state reads" guard</name>
  <files>tests/shared/test_reenqueue_reconcile_source_scan.py</files>
  <read_first>
    - tests/shared/test_dedup_fingerprint_source_scan.py — the PRIMARY analog (Phase 84, mutation-proven). Copy verbatim: `_WHERE_FUNCS`, `_in_compare:79`, `_in_where_arg:84` (walks BOTH Call.args AND Call.keywords — the two Phase-83 blind spots), `_classify:103`, `_SRC_ROOT:41`, and the crafted-string mutation self-tests :168-234 (incl. the false-positive GREEN check). It targets NAMED files and has a clean-absence path — exactly Phase 80's shape.
    - tests/analyze/services/test_single_awaiting_writer.py — the SECONDARY exemplar (Phase 83): borrow its `.values(**splat)` handling (`keyword.arg is None`, :113) and subscript-mutation binds if the guard must also catch a re-added `update(FileRecord).values(state=...)` write.
    - .planning/phases/80-recovery-re-enqueue-cutover/80-RESEARCH.md — §(b) the forms #1–#6 table + the mutation-test discipline; the three corrected paths (guard targets reenqueue.py + reconcile_cloud_jobs.py).
    - .planning/phases/80-recovery-re-enqueue-cutover/80-PATTERNS.md — the test_reenqueue_reconcile_source_scan.py section (copy targets, deviation: ADD the `.attr=="state"` + `getattr` scans).
    - project memory `feedback_mutation_test_guard_tests` — a guard never seen RED is worthless.
  </read_first>
  <action>
    Create `tests/shared/test_reenqueue_reconcile_source_scan.py`. Copy the Phase-84 scanner core, then
    EXTEND it (the dedup scanner keys only on `FileState.<member>`): add an `ast.Attribute` scan flagging
    any node with `.attr == "state"` whose base is `Name("FileRecord")` or a FileRecord-bound local in a
    non-assignment-target position (forms #1, #2, #4, #6), and a `getattr(_, "state")` Call scan (form #5);
    keep the `FileState.<member>` occurrence scan (form #3, the removed write). Assert CLEAN ABSENCE
    (zero occurrences) in BOTH `src/phaze/tasks/reenqueue.py` and `src/phaze/tasks/reconcile_cloud_jobs.py`
    — no allow-list needed (both files are clean after the cutover). Encode ALL of forms #1–#6 as
    crafted-STRING mutation self-tests (NEVER edit the real files — DB-free, hermetic), each asserting the
    mutated source is flagged; walk BOTH `Call.args` and `Call.keywords` for the `.where`-family funcs so
    the positional-read blind spot is covered. Add ONE GREEN false-positive check confirming a legitimate
    `cloud_job.status` read (`.attr == "status"`, not `"state"`) is NOT flagged. Place in `tests/shared/`
    (the Phase-84 guard's home; hermetic, no DB).
  </action>
  <acceptance_criteria>
    - The guard asserts zero `FileRecord.state`/`file.state`/`FileState.<member>`/`getattr(_,"state")` occurrences in BOTH target files.
    - Crafted-string self-tests cover forms #1–#6 (RESEARCH §(b) table); each mutated string is flagged. Walks Call.args AND Call.keywords.
    - A GREEN false-positive check confirms `cloud_job.status` (`.attr=="status"`) is NOT flagged.
    - Mutation discipline recorded: for each of #1–#6, the executor reintroduces the form in the real file, observes the guard RED, then restores (VALIDATION.md SC-1 mutation a/b). Note this in the SUMMARY.
    - Passes in isolation via `just test-bucket shared`.
  </acceptance_criteria>
  <verify>
    <automated>just test-bucket shared</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Amend the DERIV-04 equivalence SCOPE comment with the D-11 rationale</name>
  <files>tests/integration/test_stage_status_equivalence.py</files>
  <read_first>
    - tests/integration/test_stage_status_equivalence.py:415-427 — the existing SCOPE comment (documents the ledger-agnostic design + the `*_inflight` seed exclusion; `DOMAIN_COMPLETED_CASES` at :427).
    - .planning/phases/80-recovery-re-enqueue-cutover/80-CONTEXT.md — D-11.
    - .planning/phases/80-recovery-re-enqueue-cutover/80-VALIDATION.md — D-11 layer 2 (the equivalence test stays GREEN under the trap — the recovery test is the real lock).
  </read_first>
  <action>
    Amend ONLY the prose at `:415-427`: append the D-11 rejected-option rationale — `~inflight_clause(stage)`
    MUST NEVER be added to `domain_completed_clause`; because every recovery candidate is a ledger row by
    construction, the disjunct would make `domain_completed` return False for all of them (disabling the
    secondary over-enqueue net), yet stay a silent no-op for the drain and the count card — so the DERIV-04
    equivalence test stays GREEN under the trap and the recovery-layer regression in Plan 80-04, not this
    test, is the real lock. Keep the `*_inflight` seed exclusion. Do NOT change `DOMAIN_COMPLETED_CASES` or
    any assertion structure — comment/prose only.
  </action>
  <acceptance_criteria>
    - The SCOPE comment contains the D-11 rationale (why `~inflight_clause` must never enter domain_completed_clause; why the equivalence test is not the lock).
    - `*_inflight` seed exclusion and `DOMAIN_COMPLETED_CASES` structure are unchanged.
    - `just test-bucket integration` (with 5433 exports) stays green.
  </acceptance_criteria>
  <verify>
    <automated>just test-db && export TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_test" && export MIGRATIONS_TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_migrations_test" && export PHAZE_REDIS_URL="redis://localhost:6380/0" && just test-bucket integration</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI source guard → the two cutover modules | The AST guard is a static analysis gate; a future edit reintroducing a `FileRecord.state` read is the threat it defends against. No runtime input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-14 | Tampering (silent drift) | a future `FileRecord.state` read creeping back into recovery/reconcile | mitigate | Clean-absence AST guard over both named files, walking Call.args+Call.keywords; mutation-proven against forms #1–#6 (a toothless guard is the Phase-83 failure this explicitly avoids). |
| T-80-15 | Tampering (over-enqueue trap slipping past CI) | `~inflight_clause` added to domain_completed_clause staying green in the equivalence test | accept (documented) + mitigate | The equivalence test is a silent no-op for the trap by design — documented at :415-427; the real lock is the Plan 80-04 recovery regression (Cell B goes RED). |
| T-80-SC | Tampering (supply chain) | package installs | N/A | Zero dependency changes; no install occurs. |

Web/injection threats are **not applicable**: this plan is hermetic test code with no runtime input surface.
</threat_model>

<verification>
- The AST guard is green (both target files clean) and every mutation form #1–#6 observed RED then restored (Task 1).
- Equivalence SCOPE comment carries the D-11 rationale; equivalence test still green (Task 2).
- `just test-bucket shared` + `just test-bucket integration` green.
</verification>

<success_criteria>
- SC-1: mutation-proven zero-`FileRecord.state`-reads guard over both files.
- D-11: the trap documented at the equivalence-test boundary; recovery regression remains the real lock.
</success_criteria>

<output>
Create `.planning/phases/80-recovery-re-enqueue-cutover/80-05-SUMMARY.md` when done.
</output>
