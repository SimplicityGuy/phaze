---
phase: 80-recovery-re-enqueue-cutover
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/tasks/reconcile_cloud_jobs.py
  - tests/analyze/tasks/test_reconcile_cloud_jobs.py
autonomous: true
requirements: [READ-03]

must_haves:
  truths:
    - "D-04: reconcile_cloud_jobs.py ends the phase with ZERO FileRecord.state coupling — the single residual at-cap spill-back write at :212 is retired outright (diverging from 83 D-00c, which keeps the dual-write on the two sibling spill paths until Phase 90)"
    - "D-04: retiring the write fixes a HARD shadow violation live on main today — reconcile currently writes state=AWAITING_CLOUD while stamping cloud_job.status=FAILED, violating `state==AWAITING_CLOUD ⇒ cloud_job(status='awaiting')` (shadow_compare.py:131); after the fix the spilled kueue file stays at state=PUSHED and satisfies the loosened pushed/pushing invariants → strictly more green"
    - "D-12: the write swap reuses hold_awaiting_cloud in spill mode (reconcile becomes its fourth caller) with `attempts=cap` (budget-spent marker, NOT an increment), `expect_status=(SUBMITTED,RUNNING)`, `clear_cloud_phase=True`; `inadmissible=False` / `staging_bucket=None` stay inline"
    - "D-12: the `cloud_job.status = FAILED` pre-mutation at :208 is REMOVED so autoflush cannot race the CAS's `WHERE status IN (...)`; the CAS owns the status write (RESEARCH Landmine 3)"
    - "D-12: MKUE-04 clean-before-flip ordering is preserved byte-for-byte — delete_staged_object under the still-held pg_advisory_xact_lock(5_000_504) BEFORE the commit, cloud_job.attempts NOT incremented on the at-cap path, delete_job POST-commit"
  artifacts:
    - path: "src/phaze/tasks/reconcile_cloud_jobs.py"
      provides: "at-cap spill via hold_awaiting_cloud spill-mode CAS; zero FileRecord.state write; FileRecord/FileState import dropped"
      contains: "hold_awaiting_cloud"
    - path: "tests/analyze/tasks/test_reconcile_cloud_jobs.py"
      provides: "regression asserting cloud_job.status='awaiting' (not FAILED) after spill, MKUE-04 ordering, attempts-not-incremented"
  key_links:
    - from: "src/phaze/tasks/reconcile_cloud_jobs.py"
      to: "src/phaze/services/backends.py"
      via: "hold_awaiting_cloud spill-mode CAS (fourth caller)"
      pattern: "hold_awaiting_cloud"
---

<objective>
Retire the single residual `FileRecord.state` write in `reconcile_cloud_jobs.py` (the at-cap spill-back
at `:212`, inside `_handle_no_callback_terminal`) so the file is fully state-free. Swap it for
`hold_awaiting_cloud()` in spill mode — reconcile becomes the helper's fourth caller — removing the
`cloud_job.status = FAILED` pre-mutation at `:208` (autoflush race) and the `update(FileRecord)…
values(state=AWAITING_CLOUD)` write at `:212`, while preserving the MKUE-04 clean-before-flip ordering
byte-for-byte. This fixes a HARD shadow-gate violation that exists on `main` today.

Purpose: complete D-04's end-to-end ownership of this file; make the spill re-drivable (`awaiting`) and
shadow-consistent instead of falsely terminal (`FAILED` + `AWAITING_CLOUD` state).
Output: the rewritten at-cap block + dropped imports + the spill regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/80-recovery-re-enqueue-cutover/80-CONTEXT.md
@.planning/phases/80-recovery-re-enqueue-cutover/80-RESEARCH.md
@.planning/phases/80-recovery-re-enqueue-cutover/80-PATTERNS.md

<interfaces>
<!-- The single go-forward awaiting writer (Phase 83). Spill mode = rowcount-guarded CAS. -->
From src/phaze/services/backends.py:86:
- async def hold_awaiting_cloud(session, file, *, attempts=0, expect_status=None, clear_cloud_phase=False) -> bool
  # expect_status non-empty => spill-mode CAS: UPDATE cloud_job … WHERE file_id=… AND status IN expect_status.
  # Does NOT write file.state; does NOT touch FileRecord. Does NOT stamp inadmissible/staging_bucket.
From src/phaze/models/cloud_job.py:
- CloudJobStatus.SUBMITTED / .RUNNING / .AWAITING (no 'pushed' member)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite the at-cap spill block via hold_awaiting_cloud (retire the state write)</name>
  <files>src/phaze/tasks/reconcile_cloud_jobs.py</files>
  <read_first>
    - src/phaze/tasks/reconcile_cloud_jobs.py — the at-cap block :200-214 inside `_handle_no_callback_terminal` (delete_staged_object :207, `cloud_job.status = FAILED` :208, `update(FileRecord)…values(state=AWAITING_CLOUD)` :212, `session.commit()` :213, `delete_job` :214); the MKUE-04 ordering docstring :158-214; the `FileRecord, FileState` import :45; the function-local `resolve_backends` import :349 (the backends↔reconcile module-top cycle).
    - src/phaze/routers/agent_s3.py:212 + src/phaze/routers/agent_push.py:285 — the two existing spill-mode `hold_awaiting_cloud` callers (the call-shape analog). NOTE these two KEEP the gated FileRecord dual-write (83 D-00c) — reconcile deliberately does NOT (D-04).
    - src/phaze/services/backends.py:86 — the exact `hold_awaiting_cloud` signature.
    - .planning/phases/80-recovery-re-enqueue-cutover/80-CONTEXT.md — D-04, D-12.
    - .planning/phases/80-recovery-re-enqueue-cutover/80-RESEARCH.md — Landmines 3, 4, 8; § Shadow-Compare Impact.
    - .planning/phases/80-recovery-re-enqueue-cutover/80-PATTERNS.md — reconcile_cloud_jobs.py section (BEFORE/AFTER verbatim).
  </read_first>
  <behavior>
    - After an at-cap terminal spill, `cloud_job.status == 'awaiting'` (NOT `FAILED`); `cloud_job.inadmissible == False`; `cloud_job.staging_bucket is None`; `cloud_job.cloud_phase is None`.
    - `FileRecord.state` is NOT written by reconcile (the spilled kueue file stays at its prior `PUSHED` state).
    - `cloud_job.attempts` is NOT incremented on the at-cap path (`attempts=cap` is a set, not `+= 1`).
    - MKUE-04 ordering: `delete_staged_object` runs BEFORE `session.commit()` (under the still-held advisory lock); `delete_job` runs POST-commit.
  </behavior>
  <action>
    Remove `cloud_job.status = CloudJobStatus.FAILED.value` (:208) — do NOT pre-mutate the loaded ORM
    object's status before the CAS, or autoflush writes it and the CAS `WHERE status IN (SUBMITTED,RUNNING)`
    misses its own row (Landmine 3). Remove the `update(FileRecord).where(...).values(state=FileState.
    AWAITING_CLOUD)` write (:212) entirely (D-04). Replace with a spill-mode CAS: dereference the file
    (`cloud_job.file` or a `SELECT FileRecord WHERE id == file_id`), then `await hold_awaiting_cloud(session,
    file, attempts=cap, expect_status=(CloudJobStatus.SUBMITTED.value, CloudJobStatus.RUNNING.value),
    clear_cloud_phase=True)`. Keep `cloud_job.inadmissible = False` and `cloud_job.staging_bucket = None`
    INLINE (the helper does not stamp them; D-12 keeps its parameter set minimal) — apply them after the
    CAS or without touching `status`. PRESERVE byte-for-byte: `delete_staged_object` stays under the held
    `pg_advisory_xact_lock(5_000_504)` BEFORE the commit; `cloud_job.attempts` is NOT incremented;
    `session.commit()` (releases the per-row lock) then `delete_job` POST-commit. Drop the now-unused
    `FileRecord, FileState` import at :45 (ruff F401 fails the commit otherwise; verified no other use in
    the file). Import `hold_awaiting_cloud` at module top; if it re-triggers the backends↔reconcile cycle,
    import it function-locally like `resolve_backends:349`.
  </action>
  <acceptance_criteria>
    - `reconcile_cloud_jobs.py` contains no `FileRecord.state` write and no `FileState.<member>` reference (source-scanned by Plan 80-05's guard).
    - The at-cap path calls `hold_awaiting_cloud(..., attempts=cap, expect_status=(SUBMITTED,RUNNING), clear_cloud_phase=True)`; no `cloud_job.status = FAILED` pre-mutation remains.
    - `FileRecord, FileState` import removed; `uv run ruff check` passes (no F401), `uv run mypy` passes.
    - MKUE-04 ordering unchanged (delete_staged_object pre-commit; delete_job post-commit; attempts not incremented). Traces to VALIDATION.md SC-1 mutation (c).
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check src/phaze/tasks/reconcile_cloud_jobs.py && uv run mypy src/phaze/tasks/reconcile_cloud_jobs.py</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Spill regression test (cloud_job.status='awaiting', ordering, attempts)</name>
  <files>tests/analyze/tasks/test_reconcile_cloud_jobs.py</files>
  <read_first>
    - tests/analyze/tasks/test_reconcile_cloud_jobs.py — existing reconcile fixtures/seeds and the at-cap / no-callback-terminal test patterns.
    - .planning/phases/80-recovery-re-enqueue-cutover/80-VALIDATION.md — SC-1 row (mutations a/b/c) and § specifics #1 (assert cloud_job.status='awaiting', not FAILED, after spill).
    - .planning/phases/80-recovery-re-enqueue-cutover/80-CONTEXT.md — §specifics #1.
  </read_first>
  <action>
    Append a regression exercising the at-cap terminal spill through `_handle_no_callback_terminal`. Seed a
    kueue `cloud_job` at status `SUBMITTED`/`RUNNING` for a file at `state=PUSHED`, drive it at-cap, and
    assert from an INDEPENDENT session read (per the get_session-never-commits memory): (1)
    `cloud_job.status == 'awaiting'` — NOT `FAILED` (VALIDATION SC-1 mutation b: re-adding the
    `state=AWAITING_CLOUD` write / `status=FAILED` makes this RED); (2) `FileRecord.state` is UNCHANGED
    (still `PUSHED`) — reconcile wrote no state; (3) `cloud_job.attempts` did NOT increment; (4)
    `inadmissible is False` and `staging_bucket is None`. Add an ordering assertion that
    `delete_staged_object` was invoked BEFORE `session.commit()` (mock/spy the S3 delete and the commit;
    VALIDATION SC-1 mutation c: moving delete after commit → RED). Also assert the shadow invariant holds:
    a spilled row satisfies `state==AWAITING_CLOUD ⇒ cloud_job(status='awaiting')` is no longer violated
    (the file is at PUSHED, not AWAITING_CLOUD).
  </action>
  <acceptance_criteria>
    - Test asserts post-spill `cloud_job.status == 'awaiting'` from an independent session; RED if the FAILED-status / AWAITING_CLOUD-state write is reintroduced.
    - Test asserts `FileRecord.state` unchanged, `attempts` not incremented, `inadmissible=False`, `staging_bucket=None`.
    - Test asserts `delete_staged_object` precedes `session.commit()` (MKUE-04); RED if reordered.
    - Passes in isolation via `just test-bucket analyze`.
  </acceptance_criteria>
  <verify>
    <automated>just test-bucket analyze</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reconcile cron → cloud_job sidecar + files | The reconcile unit runs under a per-row `pg_advisory_xact_lock`; the at-cap path mutates the sidecar and (previously) the file state. No untrusted input — control-plane task code reconciling kueue Job outcomes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-07 | Tampering (lost cloud-routing update) | autoflush racing the spill CAS | mitigate | Remove the `cloud_job.status = FAILED` pre-mutation (:208); the rowcount-guarded CAS owns the status write with `WHERE status IN (SUBMITTED,RUNNING)` (Landmine 3). Test asserts final status is `awaiting`. |
| T-80-08 | Repudiation/Integrity (shadow-gate divergence) | `state=AWAITING_CLOUD` + `cloud_job.status=FAILED` HARD violation on main | mitigate | Retire the FileRecord.state write (D-04); spilled kueue file stays at `PUSHED` satisfying the loosened pushed/pushing invariants → strictly more green. Regression asserts the fix. |
| T-80-09 | Tampering (double-count / lost cleanup) | attempts double-increment or S3 object leak | mitigate | Preserve MKUE-04: attempts NOT incremented on at-cap path; `delete_staged_object` under held lock before commit; `delete_job` post-commit. Ordering assertion in the test. |
| T-80-SC | Tampering (supply chain) | package installs | N/A | Zero dependency changes; no install occurs. |

Web/injection threats are **not applicable**: reconcile is a control-plane cron task with no
client-facing input surface.
</threat_model>

<verification>
- reconcile has zero `FileRecord.state` coupling; imports clean (Task 1).
- Spill produces `cloud_job.status='awaiting'`, no state write, attempts unchanged, MKUE-04 ordering preserved (Task 2).
- `just test-bucket analyze` green.
</verification>

<success_criteria>
- D-04: `reconcile_cloud_jobs.py` fully state-free; the HARD shadow violation on main is fixed.
- D-12: spill-mode CAS via `hold_awaiting_cloud` (fourth caller) with the exact kwargs; autoflush race avoided; MKUE-04 preserved.
</success_criteria>

<output>
Create `.planning/phases/80-recovery-re-enqueue-cutover/80-03-SUMMARY.md` when done.
</output>
