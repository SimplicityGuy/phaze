---
phase: 80-recovery-re-enqueue-cutover
plan: 04
type: execute
wave: 2
depends_on: [80-01, 80-02]
files_modified:
  - src/phaze/tasks/reenqueue.py
  - tests/analyze/tasks/test_recovery.py
autonomous: true
requirements: [READ-03]

must_haves:
  truths:
    - "D-01: recovery's domain_completed exclusion is derived from the Phase-78/81 predicate layer (done_clause/failed_clause/domain_completed_clause), never hand-rolled — the analyze/fingerprint terminal asymmetry (analyze failed = terminal; fingerprint failed = re-drivable) is encoded, not smoothed"
    - "D-02: [RESOLVED — Phase 81 shipped] the analyze/metadata failure markers domain_completed reads now have a live writer (report_analysis_failed dual-writes analysis.failed_at; report_metadata_failed persists a metadata failed_at row); this is the resolved upstream-dependency rationale that made 81 a Phase-80 prerequisite"
    - "D-03: [RESOLVED — Phase 83 shipped] cloud_job.status='awaiting' now has a live go-forward writer (hold_awaiting_cloud); the resolved upstream-dependency rationale that made 83 a Phase-80 prerequisite"
    - "D-05: reenqueue.py drops its imports of get_metadata_pending_files/get_fingerprint_pending_files (:79,:81) and derives enrich done DIRECTLY via done_clause; is_domain_completed's metadata/fingerprint branches flip from `fid not in pending_set` → `fid in done_set`, closing the double-negation before Phase 82 redefines pending as `NOT done ∧ NOT in_flight`"
    - "D-06: the derived done-sets are ledger-scoped (O(|ledger|), never O(200K)) via a `= ANY(array)` bind (never a bare `.in_(fids)` — asyncpg 32767 param cap; the ledger hit ~44.5K in the 2026-06-18 incident)"
    - "D-07: _select_done_push_ids derives as `cloud_job.status='succeeded' OR domain_completed_clause(ANALYZE)` — no backend-kind resolution needed because a push_file ledger row implies compute (_enqueue_push_file is its only producer)"
    - "D-10: is_domain_completed resolves the metadata in_flight∧failed cell at the call site via SchedulingLedger.enqueued_at — metadata is domain-complete when `done(metadata) OR (failed(metadata) AND enqueued_at <= failed_at)`; orphaned operator retry (enqueued_at > failed_at) re-drives, callback-partial-failure (enqueued_at < failed_at) stays terminal; both twins stay ledger-agnostic"
    - "D-11: a reenqueue regression proves both metadata cells resolve correctly — the real lock on the `~inflight_clause` trap (adding it to domain_completed_clause would make Cell B go RED because every recovery candidate is a ledger row)"
    - "SC-2: a never-scheduled `discovered` file with no ledger row is NOT recovered (guards the 44.5K over-enqueue incident class)"
    - "SC-3: a failed analyze with a surviving process_file ledger row is treated domain-complete and never auto-re-enqueued — FAILURE_IS_TERMINAL[analyze] encoded at the recovery layer"
  artifacts:
    - path: "src/phaze/tasks/reenqueue.py"
      provides: "state-free done-set derivation via LOCKED predicate builders, ledger-scoped array bind, D-10 call-site metadata gate; three module-docstring reframes updated in place"
      contains: "domain_completed_clause"
    - path: "tests/analyze/tasks/test_recovery.py"
      provides: "SC-2 / SC-3 / D-10 both-cells / D-11 regression cases, each mutation-named"
  key_links:
    - from: "src/phaze/tasks/reenqueue.py"
      to: "src/phaze/services/stage_status.py"
      via: "done_clause / domain_completed_clause / awaiting_candidate_clause consumption (no FileRecord.state read)"
      pattern: "domain_completed_clause|done_clause|awaiting_candidate_clause"
    - from: "src/phaze/tasks/reenqueue.py"
      to: "src/phaze/models/scheduling_ledger.py"
      via: "get_ledger_rows → fids array bind; enqueued_at D-10 disambiguator"
      pattern: "enqueued_at"
---

<objective>
Cut `reenqueue.py`'s four done-set helpers and `is_domain_completed` over to derive done/in-flight from
the Phase-78/81 predicate layer + the `cloud_job` sidecar, with **zero `FileRecord.state` reads**, scoped
to the ledger's files (`= ANY(array)` bind, D-06). Drop the two pending-fn imports (D-05, the
double-negation cut), flip the enrich branches from `not in pending` → `in done`, derive push-done via
`succeeded OR domain_completed(ANALYZE)` (D-07), and resolve the metadata `in_flight ∧ failed` cell at the
call site using `SchedulingLedger.enqueued_at` (D-10). This is the phase's primary cutover — it lands
AFTER migration `036` (Plan 80-01) so `done(analyze)` reads a backfilled corpus, and consumes the shared
`awaiting_candidate_clause()` (Plan 80-02).

Purpose: preserve the ledger recovery contract and the analyze-terminal guarantee while removing the last
`FileRecord.state` reads from recovery, before Phase 82 redefines "pending".
Output: the rewritten helpers (docstrings updated in place) + the SC-2/SC-3/D-10/D-11 regression cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/80-recovery-re-enqueue-cutover/80-CONTEXT.md
@.planning/phases/80-recovery-re-enqueue-cutover/80-RESEARCH.md
@.planning/phases/80-recovery-re-enqueue-cutover/80-PATTERNS.md
@.planning/phases/80-recovery-re-enqueue-cutover/80-VALIDATION.md

<interfaces>
<!-- LOCKED predicate builders to REUSE verbatim (DERIV-04 lock; re-spelling breaks the equivalence test). -->
From src/phaze/services/stage_status.py:
- done_clause(stage) -> ColumnElement[bool]                     # :114 — done(fingerprint) = success/completed only
- domain_completed_clause(stage) -> ColumnElement[bool]        # :195 — done OR terminal-failed
- awaiting_candidate_clause() -> ColumnElement[bool]           # NEW in Plan 80-02 (status='awaiting' AND NOT inflight AND NOT domain_completed)
From src/phaze/tasks/reenqueue.py (the in-file TEMPLATE — already sidecar-derived, NO change):
- async def _in_flight_cloud_job_ids(session) -> set[str]      # :212 — the correct shape to copy
From src/phaze/services/scheduling_ledger.py:
- async def get_ledger_rows(session) -> Sequence[SchedulingLedger]
From src/phaze/models/scheduling_ledger.py:
- SchedulingLedger.enqueued_at                                  # :63 — the D-10 disambiguator
From src/phaze/enums/stage.py:
- Stage.ANALYZE / Stage.METADATA / Stage.FINGERPRINT ; FAILURE_IS_TERMINAL  # :87
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cut the four done-set helpers + is_domain_completed to the predicate layer</name>
  <files>src/phaze/tasks/reenqueue.py</files>
  <read_first>
    - src/phaze/tasks/reenqueue.py — READ IN FULL. The three module-docstring reframes (:11, :20, :41 — Phase-42 durability, Phase-45 ledger, domain-completed contract — UPDATE IN PLACE, never delete: Landmine 7). `_build_done_sets:136` (:162-163 pending-fn calls), `_select_done_analyze_ids:177` (state read :187), `_select_done_push_ids:190` (state read :197), `_get_awaiting_cloud_ids:200` (state read :209), `_in_flight_cloud_job_ids:212` (TEMPLATE, no change), `is_domain_completed:242`. Pending-fn imports :79,:81; `FileState` import :74. Do NOT touch `select_active_agent(kind="compute")` at :382 (PROV-01, deferred — report only).
    - src/phaze/services/stage_status.py — the LOCKED builders + the new `awaiting_candidate_clause` (Plan 80-02).
    - src/phaze/models/scheduling_ledger.py:63 — `enqueued_at`.
    - src/phaze/routers/pipeline.py:956 (retry_analysis_failed clears failed_at → no analyze D-10 cell) and :974 (retry_metadata_failed leaves failed_at → why metadata has one).
    - .planning/phases/80-recovery-re-enqueue-cutover/80-CONTEXT.md — D-01, D-05, D-06, D-07, D-10, D-11; the "carried forward" locks.
    - .planning/phases/80-recovery-re-enqueue-cutover/80-RESEARCH.md — §(d) per-stage index-friendly queries, §(e) `= ANY(array)` bind, Landmines 5,7; Open Question 1 (import boundary).
    - .planning/phases/80-recovery-re-enqueue-cutover/80-PATTERNS.md — reenqueue.py section (BEFORE/TEMPLATE/AFTER, import deltas) + § No Analog Found (the array bind).
  </read_first>
  <behavior>
    - `_select_done_analyze_ids` / `_select_done_push_ids` / `_get_awaiting_cloud_ids` read NO `FileRecord.state` and NO `FileState`.
    - done[analyze] = ledger-scoped `domain_completed_clause(Stage.ANALYZE)`; done[fingerprint] = `done_clause(Stage.FINGERPRINT)` (failed fingerprint auto-retries → done ONLY); done[metadata] = ledger-scoped `domain_completed_clause(Stage.METADATA)` with the D-10 gate applied at the call site.
    - push_done = `cloud_job.status == 'succeeded' OR domain_completed_clause(Stage.ANALYZE)` (D-07); a file at SUBMITTED/AWAITING/no-row is NOT push-done and re-drives.
    - `_get_awaiting_cloud_ids` consumes `awaiting_candidate_clause()` (not a bare `status=='awaiting'`).
    - is_domain_completed metadata is complete when `done(metadata) OR (failed(metadata) AND ledger.enqueued_at <= metadata.failed_at)`; enrich branches use `fid in done_set` (not `not in pending_set`).
    - All done-set queries are ledger-scoped via a single Postgres array bind (`sa.func.any(bindparam(..., type_=ARRAY(PGUUID)))`), never a bare `.in_(fids)`.
  </behavior>
  <action>
    Rewrite `_select_done_analyze_ids`/`_select_done_push_ids`/`_get_awaiting_cloud_ids` on the
    `_in_flight_cloud_job_ids:212` template (sidecar/predicate reads, no state). Scope every done-set query
    to the ledger's `fids` (read once from `get_ledger_rows` in `recover_orphaned_work`, mirroring the
    read-once-per-run shape) using a single Postgres array bind — `FileRecord.id ==
    sa.func.any(sa.bindparam("ids", value=list(fids), type_=ARRAY(PGUUID(as_uuid=True))))` — NEVER a bare
    `.in_(fids)` (asyncpg 32767 param cap; ledger reached ~44.5K in the incident). Keep the per-stage query
    shape (one targeted query per stage) so each Phase-77 partial index drives its own probe — do NOT
    collapse to a single `stage_status_case`. Derive: done[analyze] via `domain_completed_clause(ANALYZE)`;
    done[fingerprint] via `done_clause(FINGERPRINT)`; done[metadata] via `domain_completed_clause(METADATA)`;
    push_done via `cloud_job.status == 'succeeded' OR domain_completed_clause(ANALYZE)` (D-07 — one join/or_
    query or a succeeded-set unioned in Python, implementer's choice). `_get_awaiting_cloud_ids` consumes
    `awaiting_candidate_clause()`. In `is_domain_completed`, flip the metadata/fingerprint branches from
    `fid not in done_sets[_METADATA_PENDING]` → `fid in done_sets[_METADATA_DONE]` (D-05), and apply the
    D-10 metadata gate at the call site: metadata is domain-complete only when `done(metadata) OR
    (failed(metadata) AND row.enqueued_at <= metadata.failed_at)` — read `SchedulingLedger.enqueued_at`
    off the ledger row (both stage_status twins stay ledger-agnostic; the gate lives in reenqueue only).
    Import delta: DROP `get_metadata_pending_files`/`get_fingerprint_pending_files` (:79,:81) and `FileState`
    (:74, plus `FileRecord` if unused); ADD `from phaze.services.stage_status import domain_completed_clause,
    done_clause, awaiting_candidate_clause`. Rename the `_METADATA_PENDING`/`_FINGERPRINT_PENDING`/
    `_ANALYZE_DONE`/`_PUSH_DONE` key constants to done-semantics names so they stop lying after the
    inversion (discretionary but recommended). UPDATE the three module-docstring reframes IN PLACE to
    describe the derived-done contract (never delete them — institutional memory for two incidents). Confirm
    `reenqueue.py` importing `services.stage_status` does not trip `tests/test_task_split.py` (the
    control-only import boundary). Do NOT touch `select_active_agent(kind="compute")` at :382 (PROV-01).
  </action>
  <acceptance_criteria>
    - `reenqueue.py` contains zero `FileRecord.state` reads and zero `FileState.<member>` references (Plan 80-05's AST guard enforces this).
    - Pending-fn imports (:79,:81) removed; `done` derived directly via `done_clause`/`domain_completed_clause` (D-05); enrich branches use `fid in done_set`.
    - Done-set queries are ledger-scoped via a `= ANY(array)` bind — no bare `.in_(fids)` (Landmine 5).
    - push_done = `succeeded OR domain_completed(ANALYZE)` (D-07); metadata D-10 gate applied at the call site via `enqueued_at`.
    - The three module-docstring reframes are present and updated (not deleted).
    - `uv run ruff check` + `uv run mypy src/phaze/tasks/reenqueue.py` pass; `tests/test_task_split.py` (import boundary) stays green.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check src/phaze/tasks/reenqueue.py && uv run mypy src/phaze/tasks/reenqueue.py && just test-bucket shared</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: SC-2 / SC-3 / D-10 both-cells / D-11 regression cases</name>
  <files>tests/analyze/tasks/test_recovery.py</files>
  <read_first>
    - tests/analyze/tasks/test_recovery.py — the totality tests (`test_every_keyed_function_is_predicate_covered_xor_live_keys_only:705`, `test_domain_completed_stages_are_exactly_the_four_agent_stages:717`), the reusable seed helpers (`_make_file:94`, `_agent_payload:109`, `_seed_ledger:119`, `_make_ctx:88`, `_patch_inflight:70`, `_patch_live_keys:79`), and exemplars `test_never_scheduled_files_are_left_alone:141`, `test_analyze_done_row_is_excluded:343`, `test_awaiting_cloud_file_stays_pending_in_recovery:569`, `test_single_owner_in_flight_cloud_job_skips_ledger_recovery:1056`. SchedulingLedger already imported :43; AnalysisResult/CloudJob/CloudJobStatus :40-41.
    - .planning/phases/80-recovery-re-enqueue-cutover/80-VALIDATION.md — SC-2/SC-3 rows + D-10 both-cells + D-11 (each names the RED mutation).
    - .planning/phases/80-recovery-re-enqueue-cutover/80-CONTEXT.md — D-10, D-11, §specifics non-negotiables.
  </read_first>
  <action>
    Append, beside the totality tests, reusing the existing seed helpers (do NOT reinvent seeds): (SC-2) a
    `discovered` file with NO ledger row is NOT enqueued by `recover_orphaned_work` — mutation: iterate the
    corpus instead of `get_ledger_rows` → RED (re-creates the 44.5K over-enqueue class). (SC-3) an
    `analysis_failed` file WITH a surviving `process_file` ledger row is domain-complete and NOT
    re-enqueued — mutation: drop the `failed_clause` disjunct from `domain_completed_clause(ANALYZE)` (or
    bypass it in `is_domain_completed`) → RED. (D-10 Cell A) `enqueued_at > failed_at` (orphaned operator
    retry) → metadata NOT domain-complete → re-drives; mutation: flip the comparison to `>=`/`<` → RED.
    (D-10 Cell B) `enqueued_at < failed_at` (callback-partial-failure) → metadata IS domain-complete →
    stays terminal; mutation: drop the `enqueued_at <= failed_at` gate (revert to bare `done ∨ failed`) →
    Cell A goes RED, proving non-vacuity. Assert the analyze-vs-metadata asymmetry (analyze clears
    failed_at, metadata does not) so a future symmetric-retry change trips the test. (D-11) the both-cells
    test is the real lock: adding `~inflight_clause` to `domain_completed_clause` makes Cell B (terminal)
    go RED because every recovery candidate is a ledger row. Read assertions from an INDEPENDENT session
    (the get_session-never-commits memory).
  </action>
  <acceptance_criteria>
    - SC-2 case present; RED when recovery iterates the corpus instead of the ledger.
    - SC-3 case present; RED when the `failed_clause` disjunct is dropped from `domain_completed_clause(ANALYZE)`.
    - D-10 Cell A and Cell B both present; Cell A RED when the `enqueued_at <= failed_at` gate is reverted (non-vacuity proof); analyze/metadata retry asymmetry asserted.
    - D-11 lock present via the both-cells test; RED when `~inflight_clause` is added to `domain_completed_clause`.
    - Passes in isolation via `just test-bucket analyze`.
  </acceptance_criteria>
  <verify>
    <automated>just test-bucket analyze</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recovery task → scheduling ledger + predicate layer + saq_jobs | `recover_orphaned_work` decides which ledger rows to replay onto agent queues; called by BOTH the controller startup hook and the manual "Recover" button (`force=True`). No untrusted input — control-plane orchestration over internal state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-10 | Denial of Service (mass re-enqueue) | done-set derivation classifying a domain-complete file as recoverable | mitigate | Derive done directly via LOCKED `done_clause`/`domain_completed_clause` (D-01/D-05); SC-2 (never-scheduled not recovered) + SC-3 (failed analyze terminal) regressions guard the 44.5K incident class; `036` (Plan 80-01) ensures `done(analyze)` reads a backfilled corpus. |
| T-80-11 | Denial of Service (asyncpg crash at incident scale) | bare `.in_(fids)` >32767 binds | mitigate | Single Postgres array bind (`= ANY`) — one param regardless of ledger size (Landmine 5). |
| T-80-12 | Tampering (false-terminal / false-redrive) | the metadata in_flight∧failed cell | mitigate | D-10 `enqueued_at <= failed_at` gate at the call site; both cells regression-tested with a non-vacuity revert; D-11 trap locked. |
| T-80-13 | Elevation (import-boundary breach) | reenqueue importing a non-control-safe module | mitigate | `stage_status.py` imports only models+enums+`tasks._shared.stage_control`; `tests/test_task_split.py` asserts reenqueue is never importable from the agent worker. |
| T-80-SC | Tampering (supply chain) | package installs | N/A | Zero dependency changes; no install occurs. |

Web/injection threats are **not applicable**: recovery is control-plane task code with no client-facing
input surface. PROV-01 (single-active-compute selection at :382) is explicitly out of scope — report only.
</threat_model>

<verification>
- reenqueue has zero `FileRecord.state` reads; pending-fn imports dropped; done derived directly; array bind used (Task 1).
- SC-2/SC-3/D-10 both-cells/D-11 regressions green and each observed RED under its named mutation (Task 2).
- `just test-bucket analyze` + `tests/test_task_split.py` green.
</verification>

<success_criteria>
- D-01/D-05/D-06/D-07/D-10/D-11: recovery derives done/in-flight from the predicate layer, ledger-scoped, state-free, with the double-negation closed and the metadata cell resolved.
- SC-2 + SC-3 hold at the recovery layer.
- Historical D-02/D-03 rationale preserved (the upstream writer-cutover dependency record).
</success_criteria>

<output>
Create `.planning/phases/80-recovery-re-enqueue-cutover/80-04-SUMMARY.md` when done.
</output>
