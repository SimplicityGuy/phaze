---
phase: 80-recovery-re-enqueue-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/036_backfill_analysis_completed_at.py
  - tests/integration/test_migrations/test_migration_036_backfill_analysis_completed_at.py
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/milestones/PARALLEL-ENRICH-DAG-DESIGN.md
autonomous: true
requirements: [READ-03]

must_haves:
  truths:
    - "D-13: migration 036 backfills analysis.analysis_completed_at = analysis.updated_at for every state='analyzed' file whose analysis_completed_at IS NULL, so done_clause(ANALYZE) is TRUE for the ~1001 prod rows the cutover would otherwise re-enqueue for 4h re-analysis"
    - "D-13: the backfill carries the mandatory `failed_at IS NULL` guard because 033's constraint is a NAND (models/analysis.py:56), not an XOR — without it the migration aborts on every analysis_failed row"
    - "D-14: the planning docs stop hardcoding the destructive migration's number (de-numbered to 'the destructive migration (number assigned at plan time)') while the historical Phase-83 034 literal at ROADMAP.md:416 is left untouched"
  artifacts:
    - path: "alembic/versions/036_backfill_analysis_completed_at.py"
      provides: "data-only backfill of analysis_completed_at, revision='036', down_revision='035', documented no-op downgrade"
      contains: "analysis_completed_at"
    - path: "tests/integration/test_migrations/test_migration_036_backfill_analysis_completed_at.py"
      provides: "DB-free static asserts + integration backfill body mirroring test_migration_034"
  key_links:
    - from: "alembic/versions/036_backfill_analysis_completed_at.py"
      to: "alembic/versions/035_reconcile_dedup_resolution.py"
      via: "down_revision='035' chain link"
      pattern: "down_revision.*035"
---

<objective>
Land migration `036` — the **blocking prerequisite** of the reenqueue cutover (Plan 80-04). It
backfills `analysis.analysis_completed_at` from `analysis.updated_at` for every `state='analyzed'`
file, so that once `reenqueue.py` derives `done(analyze)` via `done_clause(ANALYZE)` (which requires
`analysis_completed_at IS NOT NULL`, `stage_status.py:123`) the ~1001 production `analyzed` rows that
currently hold NULL are correctly domain-complete and are NOT re-enqueued for 4-hour re-analysis (the
44.5K-job over-enqueue incident class). Also perform the D-14 documentation de-numbering that ends the
migration-number churn.

Purpose: close the over-enqueue safety hole opened by the cutover, atomically, in Phase 80's PR.
Output: `036` migration + its per-migration test + prose-only doc de-numbering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-recovery-re-enqueue-cutover/80-CONTEXT.md
@.planning/phases/80-recovery-re-enqueue-cutover/80-RESEARCH.md
@.planning/phases/80-recovery-re-enqueue-cutover/80-PATTERNS.md
@.planning/phases/80-recovery-re-enqueue-cutover/80-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 036 (data-only analysis_completed_at backfill)</name>
  <files>alembic/versions/036_backfill_analysis_completed_at.py</files>
  <read_first>
    - alembic/versions/034_backfill_cloud_awaiting.py — the structure/contract to copy verbatim: module-docstring contract block, sync `def upgrade()`, static parameter-free SQL, `saq_jobs`-never-referenced CRITICAL banner, documented downgrade, the `revision`/`down_revision`/`branch_labels`/`depends_on` header shape.
    - alembic/versions/032_add_derived_status_schema.py — `_BACKFILL_ANALYZE_FAILED` (~:73), the exact `files.state`-sourced backfill shape D-13 mirrors; and its no-op downgrade precedent (:167-185).
    - alembic/versions/035_reconcile_dedup_resolution.py — chain tip (`revision="035"`), confirming `036` sets `down_revision="035"`.
    - src/phaze/models/analysis.py:56 — the NAND CheckConstraint `NOT (analysis_completed_at IS NOT NULL AND failed_at IS NOT NULL)` (D-13, RESEARCH Landmine 6).
    - .planning/phases/80-recovery-re-enqueue-cutover/80-CONTEXT.md — D-13.
    - .planning/phases/80-recovery-re-enqueue-cutover/80-RESEARCH.md — § Migration 036 (verified valid), Landmine 6.
  </read_first>
  <action>
    Create `alembic/versions/036_backfill_analysis_completed_at.py` with `revision = "036"`,
    `down_revision = "035"`, `branch_labels = None`, `depends_on = None`. `upgrade()` is a sync function
    that runs ONE static `op.execute(sa.text(...))` backfill:
    `UPDATE analysis a SET analysis_completed_at = a.updated_at FROM files f WHERE a.file_id = f.id AND
    f.state = 'analyzed' AND a.analysis_completed_at IS NULL AND a.failed_at IS NULL;`.
    The `AND a.failed_at IS NULL` clause is MANDATORY (033's constraint is a NAND, not an XOR — omitting
    it aborts on every analysis_failed row). Source column is `a.updated_at` (AnalysisResult carries it
    via TimestampMixin; value is immaterial — `done_clause(ANALYZE)` tests only `IS NOT NULL`). The SQL
    must be a static string literal — no f-string / `.format(` / ORM-model import — so `alembic revision
    --autogenerate` produces an EMPTY diff (the column has existed since `028`; this migration touches no
    schema). Include the copied CRITICAL banner asserting the migration NEVER references `saq_jobs`.
    `downgrade()` is a documented no-op with a rationale comment: "data-only backfill of a column that
    predates this migration; pre-existing NULLs are indistinguishable from backfilled values; nothing to
    reverse" (mirrors 032's set-based-backfill no-op downgrade precedent).
  </action>
  <acceptance_criteria>
    - `036_backfill_analysis_completed_at.py` exists with `revision == "036"` and `down_revision == "035"`.
    - The backfill SQL contains `f.state = 'analyzed'`, `a.analysis_completed_at IS NULL`, AND `a.failed_at IS NULL` (the NAND guard). VALIDATION.md SC-4: dropping the `failed_at IS NULL` guard would abort the migration on analysis_failed rows.
    - The SQL is static/parameter-free (no f-string/`.format(`); autogenerate diff against the `036` head is empty.
    - The migration source contains no `saq_jobs` reference.
    - `downgrade()` is a documented no-op.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import ast,pathlib; s=pathlib.Path('alembic/versions/036_backfill_analysis_completed_at.py').read_text(); assert 'revision = \"036\"' in s or \"revision = '036'\" in s; assert '035' in s; assert 'failed_at IS NULL' in s; assert \"f.state = 'analyzed'\" in s; assert 'saq_jobs' not in s; print('036 shape OK')"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Per-migration test for 036 (mirror test_migration_034)</name>
  <files>tests/integration/test_migrations/test_migration_036_backfill_analysis_completed_at.py</files>
  <read_first>
    - tests/integration/test_migrations/test_migration_034_backfill_cloud_awaiting.py — copy structure verbatim: the DB-free static asserts (`test_revision_identifiers_are_bare_numbers`, `test_migration_never_references_saq_jobs`, `test_backfill_sql_is_static_and_parameter_free`, :84-107), the conftest imports + `_MIGRATION_PATH` (:44-52), the digit-name `importlib` loader (:74-81), the empty-diff scope tables, and the 5433 FOOTGUN docstring banner (:21-29).
    - tests/integration/test_migrations/conftest.py — `MIGRATIONS_TEST_DATABASE_URL` (defaults to 5432 — the footgun), `_build_alembic_config`, `downgrade_to`, `upgrade_to`.
    - .planning/phases/80-recovery-re-enqueue-cutover/80-PATTERNS.md — the `test_migration_036_*` section (integration body 1-6).
    - .planning/phases/80-recovery-re-enqueue-cutover/80-RESEARCH.md — § Migration 036 test harness + §(c) 5433 exports.
  </read_first>
  <action>
    Create `tests/integration/test_migrations/test_migration_036_backfill_analysis_completed_at.py`
    mirroring the `034` test. DB-free asserts: `revision=="036"` / `down_revision=="035"`; banner-aware
    `saq_jobs`-never-referenced scan; SQL-is-static-and-parameter-free; empty autogenerate diff scope
    (`_036` table/index/column sets all empty — `036` touches no ORM schema). Integration body: seed a
    small corpus at `035`, `upgrade_to "036"`, then assert (1) every `state='analyzed'` + `failed_at IS
    NULL` + `analysis_completed_at IS NULL` file now has `analysis_completed_at` set; (2) a control
    `analysis_failed` file (has `failed_at`) is UNCHANGED and does NOT trip the NAND CheckConstraint;
    (3) a control non-`analyzed` file gets nothing; (4) idempotency — re-running the backfill statement
    is inert; (5) `files.state` is byte-unchanged (read-only on `files`); (6) the documented no-op
    `downgrade()` runs cleanly. Copy the mandatory 5433 export banner into the module docstring.
  </action>
  <acceptance_criteria>
    - Test file exists at the exact bucketed path (never root-level — the partition guard fails CI on root tests).
    - Asserts (1)-(6) above are present; each `analyzed`-file assertion traces to VALIDATION.md SC-4.
    - Passes in isolation via `just test-bucket integration` WITH the mandatory 5433 env exports (`MIGRATIONS_TEST_DATABASE_URL` + `TEST_DATABASE_URL` on port 5433). Without the exports the harness silently talks to 5432 and fails like an infra flake.
  </acceptance_criteria>
  <verify>
    <automated>just test-db && export TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_test" && export MIGRATIONS_TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_migrations_test" && export PHAZE_REDIS_URL="redis://localhost:6380/0" && just test-bucket integration</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: D-14 documentation de-numbering (prose-only)</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/milestones/PARALLEL-ENRICH-DAG-DESIGN.md</files>
  <read_first>
    - .planning/phases/80-recovery-re-enqueue-cutover/80-CONTEXT.md — D-14.
    - .planning/phases/80-recovery-re-enqueue-cutover/80-RESEARCH.md — § Doc De-numbering (D-14): grep by content, leave ROADMAP.md:416 literal, `just docs-drift` does NOT check migration numbers (safe) but MUST NOT alter any requirement checkbox/ID/Traceability-Status.
    - .planning/REQUIREMENTS.md — MIG-04 (~line 98, carries the stale `034`).
  </read_first>
  <action>
    Grep the three docs BY CONTENT for the Phase-90 destructive-migration `034` mentions (the milestone
    header "destructive `034`", the Phase 90 title "### Phase 90: Destructive Migration `034`…", Phase 90
    SC#1/#2, MIG-04's text in REQUIREMENTS.md, and the design doc). Replace each with "the destructive
    migration (number assigned at plan time)". Do NOT touch `ROADMAP.md:416` (the Phase-83 SHIPPED
    historical `034_backfill_cloud_awaiting.py` record — dated/historical records keep their literal
    number per 81 D-08). Edits are PROSE ONLY: do not alter any `- [ ]`/`- [x]` checkbox, any requirement
    ID (`MIG-04`), or any Traceability Status column, so `just docs-drift` stays green.
  </action>
  <acceptance_criteria>
    - No Phase-90 destructive-migration reference hardcodes `034` in the three docs (verified by content grep).
    - `ROADMAP.md:416` still contains the literal `034_backfill_cloud_awaiting.py` historical record.
    - `just docs-drift` passes (no checkbox/ID/Status altered).
  </acceptance_criteria>
  <verify>
    <automated>just docs-drift</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration → live 200K corpus | A single UPDATE runs over the entire `analysis` table on first deploy (prod at `031`, sweeps `032→036`). No untrusted input crosses here — this is control-plane DDL/DML, not a request surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | Tampering (data-integrity) | `036` UPDATE aborting mid-migration | mitigate | Mandatory `failed_at IS NULL` guard keeps the UPDATE inside the NAND CheckConstraint (models/analysis.py:56); migration test asserts an `analysis_failed` control row is untouched and does not trip the constraint. |
| T-80-02 | Denial of Service (over-enqueue) | 1001 prod rows staying NULL after cutover | mitigate | `036` is a BLOCKING prerequisite of Plan 80-04; the two land atomically in one PR so there is no window where `done_clause(ANALYZE)` reads an un-backfilled corpus and re-enqueues 4h jobs. |
| T-80-03 | Tampering | destructive-migration renumber corrupting historical record | accept/mitigate | Prose-only edits; ROADMAP.md:416 historical `034` left literal; `just docs-drift` guards checkbox/ID/Status integrity. |
| T-80-SC | Tampering (supply chain) | package installs | N/A | This phase adds ZERO dependencies (milestone-wide constraint); no npm/pip/cargo install occurs, so no legitimacy checkpoint applies. |

Web/injection threats (S/T/R/I/D/E on request surfaces) are **not applicable**: this plan is Alembic
migration code + planning-doc prose, with no client-facing input surface.
</threat_model>

<verification>
- `036` applies on a `035` corpus, backfills `analyzed` rows, leaves `analysis_failed` + non-`analyzed` untouched, is idempotent, downgrade is a clean no-op (Task 2 integration body).
- Autogenerate diff against the `036` head is empty.
- `just docs-drift` green after the D-14 prose edits.
</verification>

<success_criteria>
- Migration `036` exists, is data-only, NAND-safe, and ships in this PR as the cutover's blocking prerequisite (D-13).
- Per-migration test passes in isolation via `just test-bucket integration` with the 5433 exports.
- Planning docs de-numbered (D-14); `just docs-drift` green.
</success_criteria>

<output>
Create `.planning/phases/80-recovery-re-enqueue-cutover/80-01-SUMMARY.md` when done.
</output>
