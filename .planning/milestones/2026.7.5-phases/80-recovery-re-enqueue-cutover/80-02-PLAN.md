---
phase: 80-recovery-re-enqueue-cutover
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/services/stage_status.py
  - src/phaze/services/pipeline.py
autonomous: true
requirements: [READ-03]

must_haves:
  truths:
    - "D-08: the awaiting-cloud candidate predicate is `status='awaiting' AND NOT inflight_clause(ANALYZE) AND NOT domain_completed_clause(ANALYZE)` — never a bare `status=='awaiting'` read, so a file mid-local-analysis (which still carries an inert awaiting row until the D-14 reap seams) is correctly excluded and not routed to a compute agent"
    - "D-09: the extracted `awaiting_candidate_clause()` builder lives in services/stage_status.py beside the LOCKED builders it composes (inflight_clause:175, domain_completed_clause:195); it needs only a status literal (no backends.toml config) so it does not violate 83 D-12's pushing/pushed rejection"
    - "D-08: all three consumers (get_awaiting_cloud_count, get_cloud_staging_candidates, and reenqueue's new _get_awaiting_cloud_ids in Plan 80-04) derive from the ONE builder so the card, the drain, and recovery can never disagree"
    - "D-11: domain_completed_clause's docstring records that `~inflight_clause(stage)` MUST NEVER be added to it (every recovery candidate is a ledger row by construction, so the disjunct would silently disable the secondary over-enqueue net)"
  artifacts:
    - path: "src/phaze/services/stage_status.py"
      provides: "awaiting_candidate_clause() composed from LOCKED inflight_clause + domain_completed_clause; CloudJob import added; D-11 docstring note"
      contains: "awaiting_candidate_clause"
  key_links:
    - from: "src/phaze/services/pipeline.py"
      to: "src/phaze/services/stage_status.py"
      via: "get_awaiting_cloud_count + get_cloud_staging_candidates consume awaiting_candidate_clause()"
      pattern: "awaiting_candidate_clause"
---

<objective>
Extract the byte-identical awaiting-cloud candidate `.where(...)` conjunct — currently spelled inline in
two places (`pipeline.py:1136-1140` and `:1310-1314`) — into ONE named builder
`awaiting_candidate_clause()` in `services/stage_status.py`, and repoint both existing call sites at it.
This is a pure behavior-identical refactor whose output the DERIV-04 equivalence test already exercises;
its own equivalence is the acceptance. It also lands the D-11 docstring note on `domain_completed_clause`.
Plan 80-04's new `_get_awaiting_cloud_ids` becomes the third consumer.

Purpose: stop the "three inline copies" rot before recovery starts *routing* on the predicate (83 D-15's
"the card and the drain can NEVER disagree" now extends to recovery).
Output: one shared clause builder + two repointed call sites + the D-11 trap docstring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/80-recovery-re-enqueue-cutover/80-CONTEXT.md
@.planning/phases/80-recovery-re-enqueue-cutover/80-RESEARCH.md
@.planning/phases/80-recovery-re-enqueue-cutover/80-PATTERNS.md

<interfaces>
<!-- LOCKED builders the new clause composes verbatim (do NOT re-spell — DERIV-04 lock). -->
From src/phaze/services/stage_status.py:
- inflight_clause(stage: Stage) -> ColumnElement[bool]          # :175
- domain_completed_clause(stage: Stage) -> ColumnElement[bool]  # :195
- done_clause(stage: Stage) -> ColumnElement[bool]              # :114
- dedup_resolved_clause() -> ColumnElement[bool]                # :90 — the "out-of-ladder, no stage arg" placement template
From src/phaze/models/cloud_job.py:
- CloudJob, CloudJobStatus  (AWAITING member present; NO 'pushed' member) — import to ADD (no cycle: cloud_job imports only SQLAlchemy + models.base)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add awaiting_candidate_clause() to stage_status.py + D-11 docstring note</name>
  <files>src/phaze/services/stage_status.py</files>
  <read_first>
    - src/phaze/services/stage_status.py — the LOCKED builders (`inflight_clause:175`, `domain_completed_clause:195`, `done_clause:114`), the nine model imports (:66-74, no `cloud_job` yet), and `dedup_resolved_clause:90` (the out-of-ladder placement template — takes NO stage arg, stays OUT of the equivalence-test Stage ladder). READ the D-01 DECISION RECORD in the module docstring.
    - src/phaze/models/cloud_job.py — confirm it imports only SQLAlchemy + models.base (no service imports → adding the import creates no cycle).
    - .planning/phases/80-recovery-re-enqueue-cutover/80-CONTEXT.md — D-08, D-09, D-11.
    - .planning/phases/80-recovery-re-enqueue-cutover/80-RESEARCH.md — § Clause Extraction (proposed signature, no-cycle proof); D-11 trap.
    - .planning/phases/80-recovery-re-enqueue-cutover/80-PATTERNS.md — stage_status.py section (out-of-ladder deviation from analog).
  </read_first>
  <behavior>
    - awaiting_candidate_clause() returns `and_(CloudJob.status == CloudJobStatus.AWAITING.value, ~inflight_clause(Stage.ANALYZE), ~domain_completed_clause(Stage.ANALYZE))` — same three conjuncts, same order, as the two inline spellings.
    - Composed entirely from the LOCKED builders (no re-spelled predicate) so the DERIV-04 equivalence guarantee holds.
    - Takes no `stage` argument and is NOT registered in the Stage-dispatch ladder (like dedup_resolved_clause), so the equivalence test that raises on unknown stages does not pick it up.
  </behavior>
  <action>
    Add `from phaze.models.cloud_job import CloudJob, CloudJobStatus` to the model import block. Define
    `def awaiting_candidate_clause() -> ColumnElement[bool]:` beside `domain_completed_clause`, returning
    `and_(CloudJob.status == CloudJobStatus.AWAITING.value, ~inflight_clause(Stage.ANALYZE),
    ~domain_completed_clause(Stage.ANALYZE))`. Its docstring must state it composes the LOCKED builders
    verbatim and that callers provide the `CloudJob`⋈`FileRecord` join so the correlated
    `~exists(... == FileRecord.id)` resolves. Place it out-of-ladder (model on `dedup_resolved_clause`,
    NOT on `domain_completed_clause` which is a Stage-dispatch builder). Additionally, amend
    `domain_completed_clause`'s docstring with the D-11 rejected-option note: `~inflight_clause(stage)`
    MUST NEVER be added to this clause — every recovery candidate is a scheduling-ledger row by
    construction, so the disjunct would make `domain_completed` return False for every candidate,
    silently disabling the secondary over-enqueue net (the 44.5K incident class) while staying a green
    no-op for the drain/card.
  </action>
  <acceptance_criteria>
    - `awaiting_candidate_clause` exists and is composed only from `inflight_clause` + `domain_completed_clause` + the AWAITING status literal (no re-spelled predicate).
    - `CloudJob`/`CloudJobStatus` imported with no import cycle (module imports clean).
    - `domain_completed_clause`'s docstring contains the D-11 `~inflight_clause` prohibition rationale.
    - `uv run mypy src/phaze/services/stage_status.py` passes; `uv run ruff check src/phaze/services/stage_status.py` passes (no F401).
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check src/phaze/services/stage_status.py && uv run mypy src/phaze/services/stage_status.py && uv run python -c "from phaze.services.stage_status import awaiting_candidate_clause; print(awaiting_candidate_clause() is not None)"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Repoint pipeline.py's two inline call sites at the builder</name>
  <files>src/phaze/services/pipeline.py</files>
  <read_first>
    - src/phaze/services/pipeline.py — `get_awaiting_cloud_count:1116` (`.where` at :1136-1140, wrapped in `_safe_count`) and `get_cloud_staging_candidates:1269` (`.where` at :1310-1314, chained `.with_for_update(of=CloudJob, skip_locked=True)` + `.order_by(FileRecord.created_at.asc())`). Both keep an INNER `join(FileRecord, ...)`.
    - .planning/phases/80-recovery-re-enqueue-cutover/80-PATTERNS.md — pipeline.py section (BEFORE/AFTER; preserve with_for_update/order_by/_safe_count/joins).
  </read_first>
  <action>
    Add `awaiting_candidate_clause` to the existing `services.stage_status` import in `pipeline.py`.
    Replace the three inline `.where(...)` conjuncts at BOTH `get_awaiting_cloud_count` (:1136-1140) and
    `get_cloud_staging_candidates` (:1310-1314) with a single `.where(awaiting_candidate_clause())`. Leave
    EVERYTHING else intact: the inner `join(FileRecord, ...)` on both (so the correlated `~exists`
    resolves), `get_cloud_staging_candidates`'s trailing `.with_for_update(...)` and
    `.order_by(FileRecord.created_at.asc())`, and `get_awaiting_cloud_count`'s `_safe_count(...)` wrapper.
    Output is behavior-identical — the two inline spellings were byte-identical to the builder body.
  </action>
  <acceptance_criteria>
    - Both call sites now call `awaiting_candidate_clause()`; neither retains the inline three-conjunct spelling.
    - `.with_for_update`, `.order_by`, `_safe_count`, and both inner joins are preserved unchanged.
    - The DERIV-04 equivalence test and the existing awaiting-count / drain-candidate tests stay green (behavior-identical refactor). Mutation: a divergent conjunct order or a dropped `~domain_completed_clause` in the builder would flip the drain-candidate / count tests RED.
    - `just test-bucket analyze` and `just test-bucket integration` pass (with 5433 exports for integration).
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check src/phaze/services/pipeline.py && uv run mypy src/phaze/services/pipeline.py && just test-bucket analyze</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service predicate layer → recovery/drain/count consumers | The builder's output governs which files are treated as drain/route candidates. No untrusted input crosses here — it is internal SQL-clause composition consumed by control-plane code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-04 | Tampering (routing regression) | a bare `status=='awaiting'` read mis-routing a locally-analyzing file to compute | mitigate | The builder encodes the full D-08 conjunct (`NOT inflight ∧ NOT domain_completed`); a single named source means the card/drain/recovery cannot diverge (D-08/D-09). |
| T-80-05 | Tampering (over-enqueue latent trap) | a future edit adding `~inflight_clause` to domain_completed_clause | mitigate | D-11 prohibition recorded in the docstring here; locked by the recovery regression in Plan 80-04 and the equivalence SCOPE comment in Plan 80-05. |
| T-80-06 | Elevation/Denial (import cycle) | adding CloudJob import to stage_status.py | accept | `models/cloud_job.py` imports only SQLAlchemy + models.base — verified no cycle; the module already imports nine models. |

Web/injection threats are **not applicable**: no client-facing input surface — this is internal
SQLAlchemy clause composition. No dependency changes (T-80-SC N/A).
</threat_model>

<verification>
- `awaiting_candidate_clause()` importable and composes the LOCKED builders (Task 1).
- Both pipeline call sites repointed with all chaining preserved; drain/count tests green (Task 2).
- `domain_completed_clause` docstring carries the D-11 trap note.
</verification>

<success_criteria>
- One shared `awaiting_candidate_clause()` in stage_status.py; two inline copies eliminated (D-08/D-09).
- Behavior-identical (DERIV-04 equivalence + drain/count tests green).
- D-11 docstring prohibition landed.
</success_criteria>

<output>
Create `.planning/phases/80-recovery-re-enqueue-cutover/80-02-SUMMARY.md` when done.
</output>
