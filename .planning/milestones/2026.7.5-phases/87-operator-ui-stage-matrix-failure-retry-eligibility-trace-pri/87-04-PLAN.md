---
phase: 87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri
plan: 04
type: execute
wave: 3
depends_on: [87-02]
files_modified:
  - src/phaze/templates/pipeline/partials/_stage_pill.html
  - src/phaze/templates/pipeline/partials/_stage_matrix.html
  - src/phaze/templates/pipeline/partials/files_table_view.html
  - src/phaze/services/pipeline.py
  - src/phaze/routers/pipeline.py
  - tests/integration/test_files_page.py
  - tests/shared/test_stage_pill_render.py
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "The stage-matrix pill renders all 5 buckets (done ✓ / in_flight ● / not_started — / failed ✗ / skipped ⊘) with glyph + word + aria-label, dark: pair (UI-01, UI-SPEC 5-bucket token, WCAG 1.4.1)"
    - "The matrix shows 6 pills in order Meta·FP·Analyze·Prop·Appr·Exec with the correct 7-stage→6-pill remap (Appr→REVIEW, Exec→APPLY; tracklist omitted) (D-01, RESEARCH landmine)"
    - "GET /pipeline/files is paginated (keyset/offset) and never scans the whole corpus per poll — the scannable files-table half of the D-02 dual home (UI-01, PERF-01, D-00c, D-02)"
    - "Per-page stage status is derived via correlated stage_status_case for the N page rows only (D-00a, D-00c)"
    - "The files-table query is SAVEPOINT degrade-safe — never 500s the poll (INFLIGHT-02, D-00c)"
  artifacts:
    - path: "src/phaze/templates/pipeline/partials/_stage_pill.html"
      provides: "5-bucket stage-status pill token"
    - path: "src/phaze/templates/pipeline/partials/_stage_matrix.html"
      provides: "6-pill matrix row"
    - path: "src/phaze/services/pipeline.py"
      provides: "get_files_page paginated, correlated-derivation, degrade-safe query"
      contains: "def get_files_page"
  key_links:
    - from: "src/phaze/services/pipeline.py:get_files_page"
      to: "stage_status_case"
      via: "correlated per-page CASE columns (never whole-corpus scan)"
      pattern: "stage_status_case"
---

<objective>
Build the operator's scannable "where's this file at?" overview: the 5-bucket stage-status pill, the 6-pill
matrix row, and a paginated files-table endpoint whose per-row status is derived — never a whole-corpus scan
per poll. This is the primary visual anchor of UI-01.

Purpose: Replace the raw-enum "State" string (retired in Plan 05) with a derived per-file stage matrix. The
anti-feature table forbids both "rendering raw internal status strings" and "a stats poll that scans the whole
corpus" — this plan honors both (correlated per-page derivation + keyset/offset pagination + SAVEPOINT
degrade).
Output: `_stage_pill.html`, `_stage_matrix.html`, `files_table_view.html`, `get_files_page`, `GET /pipeline/files`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-UI-SPEC.md
@.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-PATTERNS.md
@.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-VALIDATION.md
@src/phaze/templates/pipeline/partials/scan_status_pill.html
@src/phaze/templates/pipeline/partials/_file_table.html

<interfaces>
- `services/stage_status.py:stage_status_case(stage: Stage) -> Case` — returns the derived bucket string
  ('not_started'|'in_flight'|'done'|'skipped'|'failed') as a correlated CASE (5-way for enrich, 4-way downstream).
- `Stage` members: metadata, fingerprint, analyze, tracklist, propose, review, apply. Matrix remap:
  Meta=metadata · FP=fingerprint · Analyze=analyze · Prop=propose · Appr=Stage.REVIEW · Exec=Stage.APPLY
  (tracklist NOT shown). Getting Appr/Exec backwards silently mislabels pills.
- `services/pipeline.py:_safe_count` (~303) + `stage_status.py:saq_detail` SAVEPOINT idiom (~392-412) — degrade pattern.
- Pagination analog: `routers/tracklists.py:80-153 list_tracklists` (page/page_size Query, offset, Pagination ctx).
- Correlated-per-page analog: `services/pipeline.py:1367-1375 get_cloud_staging_candidates` (select(FileRecord, <clause>) .limit()).
</interfaces>

UI-SPEC 5-bucket token (mandatory geometry): `text-xs font-semibold px-2 py-0.5 rounded-full
bg-{hue}-100 dark:bg-{hue}-950 text-{hue}-700 dark:text-{hue}-400`. skipped = violet + `⊘` glyph +
`ring-1 ring-dashed ring-violet-400/60`. Every branch carries an `aria-label` (color never the sole channel).
</context>

<tasks>

<task type="auto">
  <name>Task 1: _stage_pill (5-bucket token) + _stage_matrix (6-pill row)</name>
  <files>src/phaze/templates/pipeline/partials/_stage_pill.html, src/phaze/templates/pipeline/partials/_stage_matrix.html, tests/shared/test_stage_pill_render.py</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/scan_status_pill.html (whole file — the pill geometry token, line ~6)
    - src/phaze/templates/pipeline/partials/_file_table.html (the cell/row idiom, ~9-29)
    - .planning/phases/87-.../87-UI-SPEC.md (§ Five-bucket stage-status token table + Component Inventory)
    - .planning/phases/87-.../87-PATTERNS.md (§ _stage_pill.html + _stage_matrix.html, incl. 7→6 remap LANDMINE)
  </read_first>
  <action>
    `_stage_pill.html`: params `stage_label`, `bucket`. Render one of 5 branches using the UI-SPEC token
    string with a `dark:` pair on EVERY hue: done = green + `✓`; in_flight = blue + `●` + `animate-pulse`;
    not_started = gray + `—` (muted); failed = red + `✗`; skipped = violet + `⊘` + `ring-1 ring-dashed
    ring-violet-400/60`. Each branch emits `aria-label="{stage_label}: {human bucket}"` (skipped →
    "skipped (force-completed)"). Pure template, no HTMX. `_stage_matrix.html`: render 6 `_stage_pill` in order
    Meta=metadata · FP=fingerprint · Analyze=analyze · Prop=propose · Appr=Stage.REVIEW · Exec=Stage.APPLY
    (tracklist omitted), container `flex flex-wrap gap-2` (wraps narrow, D-01), with ONE legend per surface
    (`✓ done · ● in-flight · — not-started · ✗ failed · ⊘ skipped`). Add
    `tests/shared/test_stage_pill_render.py` rendering each bucket + the matrix and asserting glyph + word +
    aria-label present for all 5 buckets and the 6-pill order/remap.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/test_stage_pill_render.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All 5 buckets render with glyph + word + aria-label + a `dark:` class (color never sole channel).
    - The matrix renders exactly 6 pills in the specified order; Appr maps to REVIEW, Exec maps to APPLY; tracklist absent.
    - skipped pill carries the dashed violet ring + `⊘` (visually unlike done's solid `✓`).
  </acceptance_criteria>
  <done>Pill + matrix components render all 5 buckets and the correct 6-stage remap.</done>
</task>

<task type="auto">
  <name>Task 2: get_files_page (paginated, correlated-derivation, degrade-safe) + GET /pipeline/files</name>
  <files>src/phaze/services/pipeline.py, src/phaze/routers/pipeline.py, src/phaze/templates/pipeline/partials/files_table_view.html, tests/integration/test_files_page.py</files>
  <read_first>
    - routers/tracklists.py:80-153 (list_tracklists pagination: page/page_size Query, offset, Pagination ctx)
    - src/phaze/services/pipeline.py:1367-1375 (get_cloud_staging_candidates — select(FileRecord, <clause>).limit) + :303 (_safe_count)
    - src/phaze/services/stage_status.py:392-412 (saq_detail begin_nested SAVEPOINT degrade)
    - src/phaze/templates/pipeline/partials/_file_table.html (columns/rows/row_file_ids contract, ~9-29) + tracklists/partials/pagination.html
    - .planning/phases/87-.../87-RESEARCH.md (§ Pattern: paginated per-page derivation) + 87-VALIDATION behavior 7
  </read_first>
  <action>
    Add `get_files_page(session, *, page, page_size, stage=None, bucket=None)` in `services/pipeline.py`:
    build `select(FileRecord, stage_status_case(Stage.METADATA), stage_status_case(Stage.FINGERPRINT),
    stage_status_case(Stage.ANALYZE), stage_status_case(Stage.PROPOSE), stage_status_case(Stage.REVIEW),
    stage_status_case(Stage.APPLY)).order_by(FileRecord.id).limit(page_size)` with keyset on `FileRecord.id`
    (preferred, no COUNT) OR `.offset((page-1)*page_size)`; if offset paging is used, wrap the total COUNT in
    `_safe_count` (never an unbounded whole-corpus COUNT per poll). Accept optional `stage`+`bucket` params NOW
    (plumb them through — filtering by `stage_status_case(stage) == bucket`) so Plan 05 is templates-only. Wrap
    the whole read in a `begin_nested()` SAVEPOINT try/except that logs + returns a safe empty page (degrade,
    never 500). Add `GET /pipeline/files` in `routers/pipeline.py` rendering `files_table_view.html`, which reuses
    `_file_table.html` with columns File · Type · Meta · FP · Analyze · Prop · Appr · Exec — the path cell is
    `font-mono text-xs truncate title=full_path`, each stage cell hosts one `_stage_pill`, `row_file_ids` binds
    rows to the record slide-in, and `tracklists/partials/pagination.html` renders the controls. Add
    `tests/integration/test_files_page.py`: seed a corpus with markers, assert the query is bounded
    (LIMIT/keyset), assert NO unbounded whole-corpus COUNT per poll, assert EXPLAIN shows the partial indexes
    (`ix_metadata_failed`/`ix_analysis_completed`/`ix_analysis_failed`/`ix_fprint_success`) are used, and assert
    the read degrades to a safe empty page on a forced error.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_files_page.py -x -q && uv run ruff check src/phaze/services/pipeline.py src/phaze/routers/pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - Behavior 7: the files-table query is paginated (LIMIT/keyset) with no unbounded whole-corpus COUNT per poll; EXPLAIN evidence shows index scans on a seeded corpus.
    - Per-page derivation uses correlated `stage_status_case` (evaluates for the N page rows only).
    - The read is SAVEPOINT degrade-safe (returns a safe empty page on error, never 500).
    - `GET /pipeline/files` renders the matrix per row and accepts (unused yet) `stage`+`bucket` params.
  </acceptance_criteria>
  <done>A paginated, derived, degrade-safe files table renders the pill matrix per row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser query params → paginated files query | page/page_size/stage/bucket cross into a corpus-scale query |
| file path / status → Jinja render | untrusted file paths rendered in the table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-11 | Denial of Service | whole-corpus scan/COUNT per 5s poll at 200K | mitigate | Keyset/offset pagination + LIMIT + correlated per-page derivation; no unbounded COUNT (behavior-7 test) |
| T-87-12 | Denial of Service | poll-time 500 on DB hiccup | mitigate | `begin_nested()` SAVEPOINT degrade → safe empty page (D-00c/INFLIGHT-02) |
| T-87-13 | Tampering | XSS via file path in template | mitigate | Jinja2 autoescape; path rendered as escaped text (never `\| safe`) |
| T-87-14 | Tampering | injection via stage/bucket param | mitigate | Params validated against `Stage`/`Status` allowlists; pure ORM bound params, never f-string SQL |

No package installs this phase (zero new deps) — no supply-chain checkpoint required.
</threat_model>

<verification>
- `just test-bucket integration` + `just test-bucket shared` green in isolation.
- `uv run ruff check .` clean; EXPLAIN evidence recorded in the SUMMARY (index scan, bounded rows).
</verification>

<success_criteria>
- 5-bucket pill + 6-pill matrix render correctly (glyph+word+aria-label, correct remap).
- `GET /pipeline/files` is paginated, derived, degrade-safe, and never scans the whole corpus per poll.
</success_criteria>

<output>
Create `.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-04-SUMMARY.md` when done.
</output>
