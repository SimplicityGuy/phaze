---
phase: 87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri
plan: 08
type: execute
wave: 6
depends_on: [87-02, 87-07]
files_modified:
  - src/phaze/services/pipeline.py
  - src/phaze/routers/pipeline.py
  - src/phaze/templates/shell/partials/rail.html
  - tests/integration/test_orphan_count.py
  - tests/shared/test_rail_priority_controls.py
autonomous: true
requirements: [UI-05, PRIO-01]
must_haves:
  truths:
    - "A per-stage orphaned/stuck-work count is derived as recovery-candidate count (ledger − live saq_jobs keys − domain_completed) and matches what recover_orphaned_work would re-enqueue (D-05, OQ-2, UI-05)"
    - "The orphan count is degrade-safe (_safe_count/SAVEPOINT → 0 on error) and rides the single #pipeline-stats OOB fanout with no self-poll (D-00c, D-05)"
    - "The orphan badge is amber, role=status, hidden at 0, placed near the affected enrich stage on the DAG rail (D-05, UI-SPEC)"
    - "The per-stage priority stepper AND pause/resume are re-wired to the live POST /pipeline/stages/{stage}/{priority,pause,resume} endpoints, response {stage,priority,paused} (D-11, PRIO-01)"
    - "Priority steppers carry explicit aria-labels + the D-11 clarifying tooltip (▲ raises priority = lowers the number)"
  artifacts:
    - path: "src/phaze/services/pipeline.py"
      provides: "Degrade-safe per-stage orphan-count helper"
      contains: "orphan"
    - path: "src/phaze/templates/shell/partials/rail.html"
      provides: "Amber orphan badge + priority stepper + pause/resume on the DAG rail"
  key_links:
    - from: "src/phaze/templates/shell/partials/rail.html"
      to: "POST /pipeline/stages/{stage}/{priority,pause,resume}"
      via: "hx-post + hx-vals (delta) to the live durable-control-row endpoints"
      pattern: "/pipeline/stages/"
---

<objective>
Surface the two ambient DAG-rail additions: the per-stage orphaned/stuck-work count badge (UI-05, derived for
free from the chosen in_flight source) and the restored per-stage priority stepper + pause/resume (PRIO-01,
re-wiring the still-live-but-orphaned endpoints removed in the v7.0 redesign).

Purpose: UI-05 makes stuck work visible where recovery acts (the rail), with the badge count matching exactly
what `recover_orphaned_work` would re-enqueue (no drift). PRIO-01 re-connects the orphaned priority/pause/resume
setters — the backend is fully live and threat-modeled; this is a pure UI re-wire.
Output: orphan-count helper + rail badge + priority/pause/resume rail controls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-UI-SPEC.md
@.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-PATTERNS.md
@src/phaze/routers/pipeline_stages.py
@src/phaze/templates/shell/partials/rail.html

<interfaces>
- Orphan derivation: `services/pipeline.py:566 get_live_job_keys` (ledger − live) + `stage_status.py:330-361
  awaiting_candidate_clause` composition + `stage_status.py:392-412 saq_detail` SAVEPOINT degrade + `_safe_count`
  (~303). Define orphan = per-stage recovery-candidate count = ledger rows − live saq_jobs keys − domain_completed.
- Footgun (Pitfall 4): `models/scheduling_ledger.py:63 enqueued_at` is a NAIVE TIMESTAMP — if any staleness
  threshold is used, compare naive-to-naive (or cast), else a TypeError aborts the txn. Wrap in `_safe_count`.
- Seed loop: `routers/pipeline.py:219-236` seeds `dag["{stage}Paused"]`/`dag["{stage}Priority"]` into the store
  each poll (degrade-safe) — add per-stage orphan counts to the same `#pipeline-stats` OOB fanout (no self-poll).
- Live control endpoints (do NOT rebuild): `routers/pipeline_stages.py:82-128` set_priority/pause/resume, all
  return `{stage, priority, paused}` from the durable control row. Do NOT resurrect Phase-38 templates.
- Rail markup: `shell/partials/rail.html:79,95,107,119` live-count `x-text` spans; enrich nodes ~86-120.
</interfaces>

UI-SPEC copy: priority label "Priority: {High|Normal|Low} ({n})"; ▲ `aria-label="Raise {stage} priority"`, ▼
`aria-label="Lower {stage} priority"`; tooltip "▲ Higher priority runs sooner (lowers the queue number). ▼ lowers
priority."; pause/resume toggle, "Paused" amber caption. Orphan badge = amber `bg-amber-100 text-amber-700`
`dark:bg-amber-950 dark:text-amber-400`, `role="status"`, hidden at 0.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-stage orphan-count helper (recovery-candidate parity, degrade-safe) + #pipeline-stats seed</name>
  <files>src/phaze/services/pipeline.py, src/phaze/routers/pipeline.py, tests/integration/test_orphan_count.py</files>
  <read_first>
    - src/phaze/services/pipeline.py:566 (get_live_job_keys) + :303 (_safe_count) + :219-236 in routers/pipeline.py (the DAG overlay seed loop)
    - src/phaze/services/stage_status.py:330-361 (awaiting_candidate_clause) + :392-412 (saq_detail SAVEPOINT)
    - src/phaze/models/scheduling_ledger.py:63 (enqueued_at naive TIMESTAMP footgun) + src/phaze/tasks/reenqueue.py (recover_orphaned_work classification)
    - .planning/phases/87-.../87-RESEARCH.md (OQ-2 orphan definition) + 87-VALIDATION (UI-05 test map)
  </read_first>
  <action>
    Add a per-stage orphan-count helper in `services/pipeline.py`: orphan = ledger rows for the stage − live
    `saq_jobs` keys (`get_live_job_keys`) − `domain_completed`. Make it exactly the set `recover_orphaned_work`
    would re-enqueue (no drift vs recovery). Wrap the read in a `begin_nested()` SAVEPOINT / `_safe_count`
    degrade → return 0 on error (never 500 the poll). If any staleness threshold is used, compare the NAIVE
    `enqueued_at` naive-to-naive (or cast) to avoid the TypeError abort. Seed the per-stage orphan counts into
    the `#pipeline-stats` context alongside the existing `dag[...]` overlay loop (`routers/pipeline.py:219-236`)
    so the badge rides the single OOB fanout — no new poll. Add `tests/integration/test_orphan_count.py`: seed
    an in-flight-ledger-no-progress file and assert the count equals the recovery-candidate set for that stage;
    assert the helper degrades to 0 on a forced error.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_orphan_count.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The orphan count equals what `recover_orphaned_work` would re-enqueue for that stage (no drift, UI-05/OQ-2).
    - The helper is degrade-safe (returns 0 on error, SAVEPOINT); no naive/aware timestamp TypeError.
    - The count is seeded into `#pipeline-stats` (no self-poll).
  </acceptance_criteria>
  <done>Orphan/stuck count is derived degrade-safe and agrees with recovery.</done>
</task>

<task type="auto">
  <name>Task 2: Rail — amber orphan badge + priority stepper + pause/resume re-wire</name>
  <files>src/phaze/templates/shell/partials/rail.html, tests/shared/test_rail_priority_controls.py</files>
  <read_first>
    - src/phaze/templates/shell/partials/rail.html:79,95,107,119 (live-count x-text spans) + enrich nodes ~86-120
    - src/phaze/routers/pipeline_stages.py:82-128 (live set_priority/pause/resume, {stage,priority,paused})
    - .planning/phases/87-.../87-UI-SPEC.md (§ Orphan-count badge + Priority stepper + Copywriting + Accessibility)
    - .planning/phases/87-.../87-PATTERNS.md (§ orphan badge + priority stepper rewire — do NOT resurrect Phase-38 templates)
  </read_first>
  <action>
    On the DAG rail, near each affected enrich node, add an amber orphan numeral badge (`role="status"`,
    `bg-amber-100 text-amber-700 dark:bg-amber-950 dark:text-amber-400`, `empty:hidden`/`x-show` at 0) bound to
    the store key seeded in Task 1 (rides the existing OOB fanout, no self-poll). Add per-enrich-stage `▲`/`▼`
    priority steppers (`hx-post="/pipeline/stages/{stage}/priority"` with `hx-vals` delta -10/+10) and a
    pause/resume toggle (`hx-post .../pause`, `.../resume`); the `{stage, priority, paused}` response re-renders
    the control from the durable control row. Label "Priority: {High|Normal|Low} ({n})"; explicit aria-labels
    (▲ "Raise {stage} priority", ▼ "Lower {stage} priority") PLUS the D-11 clarifying tooltip; "Paused" amber
    caption when paused. Build FRESH markup against the live endpoints — do NOT resurrect the removed Phase-38
    templates. The `{stage}Paused`/`{stage}Priority` store seeds already exist. Add
    `tests/shared/test_rail_priority_controls.py`: assert the steppers post to the live endpoints, the aria-labels
    + tooltip are present, and the orphan badge markup binds the store key + is hidden at 0.
  </action>
  <verify>
    <automated>uv run pytest tests/shared/test_rail_priority_controls.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - PRIO-01: both the priority stepper AND pause/resume post to the live `/pipeline/stages/{stage}/…` endpoints; aria-labels + clarifying tooltip present.
    - The amber orphan badge binds the seeded store key, `role="status"`, hidden at 0, near the affected enrich stage.
    - No Phase-38 templates resurrected (fresh markup against live endpoints).
  </acceptance_criteria>
  <done>The rail carries the orphan badge and the re-wired priority/pause/resume controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → priority/pause/resume endpoints | untrusted stage + delta cross into the durable control row |
| ledger/saq_jobs reads → poll | in_flight source read on the 5s poll |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-28 | Denial of Service | poll-time 500 on orphan derivation / naive-timestamp TypeError | mitigate | `_safe_count`/SAVEPOINT degrade → 0; naive-to-naive `enqueued_at` comparison (Pitfall 4) |
| T-87-29 | Tampering | priority delta out of range | mitigate | The live endpoint already clamps delta [0,100] (T-37-04) — preserve; UI only posts ±10 |
| T-87-30 | Tampering | injection via stage param on control post | mitigate | Live endpoint validates stage via allowlist (T-37-01); UI posts fixed stage paths |
| T-87-31 | Integrity | orphan badge drifts from recovery | mitigate | Orphan count defined AS the recovery-candidate set; parity asserted (Task 1 test) |

No package installs this phase (zero new deps) — no supply-chain checkpoint required.
</threat_model>

<verification>
- `just test-bucket integration` + `just test-bucket shared` green in isolation.
- `uv run ruff check .` clean; full suite `uv run pytest -q` green + 90% coverage + `uv run mypy .` at phase gate.
</verification>

<success_criteria>
- Orphan/stuck count surfaced per-stage (recovery-parity, degrade-safe, no self-poll).
- Priority stepper + pause/resume re-wired to live endpoints with clear labeling.
</success_criteria>

<output>
Create `.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-08-SUMMARY.md` when done.
</output>
