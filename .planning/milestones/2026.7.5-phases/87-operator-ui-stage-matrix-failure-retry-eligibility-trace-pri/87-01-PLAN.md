---
phase: 87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/models/stage_skip.py
  - src/phaze/models/__init__.py
  - alembic/versions/037_add_stage_skip.py
  - tests/integration/test_migrations/test_037_stage_skip.py
autonomous: true
requirements: [UI-04]
must_haves:
  truths:
    - "After `alembic upgrade head`, table `stage_skip` exists (D-13a, UI-04)"
    - "UNIQUE(file_id, stage) rejects a duplicate (file_id, stage) skip row (≤1-row invariant, D-13a)"
    - "CHECK restricts stage to the 3 enrich values metadata/analyze/fingerprint (D-10, OQ-3)"
    - "`alembic revision --autogenerate` produces an empty diff for stage_skip (ORM mirror, PERF-01 discipline)"
    - "Migration 037 references no `saq_jobs` token anywhere (CLAUDE migration rule)"
  artifacts:
    - path: "src/phaze/models/stage_skip.py"
      provides: "StageSkip ORM sidecar model"
      contains: "class StageSkip"
    - path: "alembic/versions/037_add_stage_skip.py"
      provides: "Additive migration creating stage_skip"
      contains: "create_table"
    - path: "tests/integration/test_migrations/test_037_stage_skip.py"
      provides: "Per-migration integration test (up/down + empty-diff + no saq_jobs)"
  key_links:
    - from: "src/phaze/models/__init__.py"
      to: "phaze.models.stage_skip.StageSkip"
      via: "import + __all__ registration (Base.metadata for autogenerate)"
      pattern: "StageSkip"
---

<objective>
Land the D-13 non-UI slice foundation: the `stage_skip` `(file_id, stage)` sidecar table — the durable,
distinct **force-skip marker** the rest of Phase 87 derives from. Schema only: ORM model, registry
registration, additive migration 037, and its integration test.

Purpose: Fingerprint has no 1:1 output table (`fingerprint_results` is 1:N), so the Phase-81 "add a
`skipped_at` column" shape cannot cover all three enrich stages uniformly. A `(file_id, stage)` sidecar is
the only uniform enrich-wide shape (RESEARCH §1). This marker is the sole *stored* fact; status stays
*derived* (derive-don't-store).
Output: `stage_skip` table + `StageSkip` model + migration 037 + migration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-CONTEXT.md
@.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-RESEARCH.md
@.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-PATTERNS.md
@src/phaze/models/dedup_resolution.py

Migration/test infra: DB tests need `MIGRATIONS_TEST_DATABASE_URL` at the `:5433` ephemeral DB (`just test-db`
provisions 5433 — port footgun). Bucket: `integration`. All commands `uv run …` (never bare pytest/mypy).
</context>

<tasks>

<task type="auto">
  <name>Task 1: StageSkip ORM model + registry registration</name>
  <files>src/phaze/models/stage_skip.py, src/phaze/models/__init__.py</files>
  <read_first>
    - src/phaze/models/dedup_resolution.py (the sidecar analog — imports, TimestampMixin+Base, UUID PK, FK-to-files.id, server_default func.now())
    - src/phaze/models/analysis.py:53-56 (the `__table_args__` UniqueConstraint/CheckConstraint mirror discipline)
    - src/phaze/models/__init__.py (DedupResolution import + __all__ registration lines to mirror)
    - .planning/phases/87-.../87-PATTERNS.md (§ "models/stage_skip.py" deltas + `__table_args__` mirror)
  </read_first>
  <action>
    Create `StageSkip(TimestampMixin, Base)` with `__tablename__ = "stage_skip"`: `id` UUID PK
    `default=uuid.uuid4`; `file_id` UUID `ForeignKey("files.id")` `nullable=False` (NOT `unique=True` on its
    own — uniqueness is composite); `stage` `Mapped[str]` (String, the enrich value); `reason` `Mapped[str]`
    (Text, `nullable=False` — the D-09 required reason); `skipped_at` `Mapped[datetime]`
    `DateTime(timezone=True)` `server_default=func.now()` `nullable=False`. `created_at`/`updated_at` come from
    `TimestampMixin` — do NOT redeclare. Add `__table_args__` = `UniqueConstraint("file_id", "stage",
    name="uq_stage_skip_file_stage")` plus `CheckConstraint("stage IN ('metadata','analyze','fingerprint')",
    name="ck_stage_skip_enrich_only")` (OQ-3 belt-and-suspenders on D-10; spell the predicate exactly as
    Postgres renders it — a plain b-tree UNIQUE avoids the `= ANY(ARRAY[...])` reserialization trap).
    Register in `models/__init__.py`: `from phaze.models.stage_skip import StageSkip` and append `"StageSkip"`
    to `__all__` (mirror the `DedupResolution` lines). This is load-bearing — the model must be on
    `Base.metadata` so Alembic autogenerate sees it (empty-diff contract).
  </action>
  <verify>
    <automated>uv run mypy src/phaze/models/stage_skip.py && uv run python -c "import phaze.models as m; assert 'StageSkip' in m.__all__; from phaze.models.base import Base; assert 'stage_skip' in Base.metadata.tables"</automated>
  </verify>
  <acceptance_criteria>
    - `StageSkip` is on `Base.metadata.tables` and exported in `models.__all__`.
    - `__table_args__` carries `uq_stage_skip_file_stage` UNIQUE and `ck_stage_skip_enrich_only` CHECK.
    - `uv run mypy src/phaze/models/stage_skip.py` passes; `uv run ruff check src/phaze/models/stage_skip.py` clean.
    - `reason` is `nullable=False` (D-09); `file_id` is NOT unique alone (composite uniqueness only).
  </acceptance_criteria>
  <done>Model exists, fully typed, registered on Base.metadata + __all__.</done>
</task>

<task type="auto">
  <name>Task 2: Additive migration 037 (create stage_skip; mirrored downgrade; no backfill; no saq_jobs)</name>
  <files>alembic/versions/037_add_stage_skip.py</files>
  <read_first>
    - alembic/versions/032_add_derived_status_schema.py (create-table §B ~lines 130-142; mirrored downgrade ~167-186; bare-name double-prefix warning ~66-67; NO-saq_jobs banner ~39-41)
    - alembic/versions/036_backfill_analysis_completed_at.py (immediate down_revision reference for the header)
    - .planning/phases/87-.../87-PATTERNS.md (§ "alembic/versions/037_add_stage_skip.py" — the exact create_table block + carry-overs)
  </read_first>
  <action>
    New revision file: `revision = "037"`, `down_revision = "036"`, `branch_labels = None`, `depends_on = None`
    (bare-number strings, mirror 032:59-63). `upgrade()` calls `op.create_table("stage_skip", …)` with columns
    id (UUID PK), file_id (UUID, nullable=False), stage (String, nullable=False), reason (Text, nullable=False),
    skipped_at (DateTime(timezone=True), server_default sa.func.now(), nullable=False), created_at/updated_at
    (DateTime, server_default now). Constraints via `op.f(...)` bare names: `pk_stage_skip`,
    `uq_stage_skip_file_stage` (UniqueConstraint file_id,stage), `fk_stage_skip_file_id_files` (FK → files.id),
    and a `CheckConstraint("stage IN ('metadata','analyze','fingerprint')", name=op.f("ck_stage_skip_enrich_only"))`.
    `downgrade()` = `op.drop_table("stage_skip")` (mirrored). CRITICAL carry-overs: NO `saq_jobs` reference
    anywhere; NO backfill statement (greenfield marker — there is no historical "skipped" source, unlike 032's
    backfill blocks); use `op.f()` so the naming convention applies the prefix exactly once (passing an
    already-prefixed name double-prefixes it).
  </action>
  <verify>
    <automated>uv run ruff check alembic/versions/037_add_stage_skip.py && grep -vi '^\s*#' alembic/versions/037_add_stage_skip.py | grep -ci saq_jobs | grep -qx 0 && echo NO_SAQ_JOBS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `revision == "037"`, `down_revision == "036"` (bare numbers).
    - `upgrade()` creates `stage_skip` with UNIQUE(file_id,stage), FK→files.id, and the enrich-only CHECK.
    - `downgrade()` drops the table; NO backfill; zero `saq_jobs` occurrences (grep gate, comments filtered).
  </acceptance_criteria>
  <done>Migration 037 applies additively and reverses cleanly; verified fully by Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Per-migration integration test (up/down + empty-diff + constraints + no saq_jobs)</name>
  <files>tests/integration/test_migrations/test_037_stage_skip.py</files>
  <read_first>
    - tests/integration/test_migrations/test_migration_032_additive_schema.py (whole file: `_load_migration_0XX` path loader ~81-88; `test_revision_identifiers_are_bare_numbers` ~91-96; `test_migration_never_references_saq_jobs` ~99-103; upgrade/seed/assert/empty-diff/downgrade body ~165-283; `_diffs_touching_032` ~139-162)
    - tests/integration/test_migrations/conftest.py (`_build_alembic_config`, `upgrade_to`, `downgrade_to`, `MIGRATIONS_TEST_DATABASE_URL`)
  </read_first>
  <behavior>
    - After upgrade to 037, `to_regclass('public.stage_skip')` is non-null.
    - A second insert of the same (file_id, stage) raises IntegrityError (UNIQUE).
    - Inserting stage='propose' (non-enrich) raises IntegrityError (CHECK ck_stage_skip_enrich_only).
    - Autogenerate diff touching stage_skip is empty (ORM mirror ≡ DB).
    - After downgrade to 036, `to_regclass('public.stage_skip')` is null.
    - Source grep: migration 037 contains no `saq_jobs`.
  </behavior>
  <action>
    Copy `test_migration_032_additive_schema.py` and retarget the revision chain 036→037. Reuse the loader,
    the bare-number assertion (assert "037" / down_revision "036"), and the no-`saq_jobs` grep guard verbatim.
    Seed a couple of `files` rows, insert a `stage_skip` row, assert the table exists; assert a duplicate
    (file_id, stage) insert is rejected; assert a non-enrich `stage` insert is rejected by the CHECK; add a
    `_diffs_touching_037` helper (copy `_diffs_touching_032` with `_037_TABLES = {"stage_skip"}`) and assert an
    empty autogenerate diff; downgrade to 036 and assert the table is dropped. DB tests require
    `MIGRATIONS_TEST_DATABASE_URL` at `:5433`.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_migrations/test_037_stage_skip.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All six behaviors above assert green under `just test-bucket integration` (isolation).
    - The empty-autogenerate-diff assertion passes (proves the ORM `__table_args__` mirror matches the DDL).
    - The no-`saq_jobs` grep guard passes.
  </acceptance_criteria>
  <done>Migration 037 verified up/down, constraints enforced, empty diff, no saq_jobs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ORM/migration → PostgreSQL | New DDL + constraints crossing into the live-corpus schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01 | Tampering | migration DDL vs saq_jobs | mitigate | Grep gate asserts zero `saq_jobs` references (CLAUDE rule); migration is app-schema-only |
| T-87-02 | Elevation of Privilege | non-enrich stage in stage_skip | mitigate | DB `CHECK stage IN (metadata,analyze,fingerprint)` (D-10) — approval/execute can never carry a skip marker at the schema layer |
| T-87-03 | Integrity | duplicate skip rows | mitigate | `UNIQUE(file_id, stage)` ≤1-row invariant, asserted by the migration test |
| T-87-04 | Denial of Service | destructive downgrade | accept | `downgrade()` is a plain `drop_table` on an additive greenfield table; no data loss for existing corpus (no backfill) |

No package installs this phase (zero new deps) — no supply-chain (T-*-SC) checkpoint required.
</threat_model>

<verification>
- `just test-bucket integration` green in isolation (non-hermetic-test hazard).
- `uv run mypy src/phaze/models/stage_skip.py` + `uv run ruff check .` clean.
- Empty autogenerate diff confirmed by the migration test.
</verification>

<success_criteria>
- `stage_skip` sidecar table exists with UNIQUE(file_id,stage) + enrich-only CHECK; StageSkip on Base.metadata.
- Migration 037 additive, mirrored downgrade, no backfill, no saq_jobs; empty autogenerate diff.
- Migration integration test green.
</success_criteria>

<output>
Create `.planning/phases/87-operator-ui-stage-matrix-failure-retry-eligibility-trace-pri/87-01-SUMMARY.md` when done.
</output>
