---
phase: 88-lane-agent-drill-in
plan: 03
type: execute
wave: 2
depends_on: [88-01]
files_modified:
  - src/phaze/routers/admin_agents.py
  - src/phaze/services/pipeline.py
  - src/phaze/templates/admin/partials/_agent_activity.html
  - tests/agents/routers/test_agent_activity.py
  - tests/integration/test_agent_stage_buckets.py
autonomous: true
requirements: [DRILL-02, DRILL-03]
must_haves:
  truths:
    - "DRILL-02: GET /admin/agents/{agent_id}/_activity returns an HTML fragment showing owned files grouped by derived stage_status, recent scan batches, per-lane queue depths, and liveness."
    - "D-00a: the agent stage grouping reads the single stage_status_case derivation — no second status-derivation path; the aggregate composes it verbatim, never re-spells the CASE ladder."
    - "D-04: owned files are grouped as a per-agent 6-stage matrix of COUNTS via a SQL GROUP BY over stage_status_case filtered to agent_id — NOT row-by-row materialization of the agent's files."
    - "D-05: the agent pane is stacked vertical sections in order: liveness header → stage-count matrix → per-lane queue depths → recent scan batches; everything visible on open, scroll for depth."
    - "D-00b: every read is bounded (one indexed GROUP BY per stage, LIMIT-N scans/queues) and _safe_count/SAVEPOINT-degrade-safe — degrades to 0/[], never 500s the poll or the own-tick."
    - "D-03: the loaded agent body carries its OWN bounded hx-trigger=\"every 5s\" tick scoped to agent_id, removed with the pane on dismiss (no orphan loop)."
  artifacts:
    - path: "src/phaze/services/pipeline.py"
      provides: "_agent_stage_buckets — GroupingError-safe per-agent GROUP BY stage_status_case aggregate (clone of _safe_bucket_counts + FileRecord.agent_id == agent_id conjunct)"
      contains: "def _agent_stage_buckets"
    - path: "src/phaze/routers/admin_agents.py"
      provides: "GET /admin/agents/{agent_id}/_activity read-only HTML-fragment endpoint (liveness + 6-stage buckets + queue depths + recent scans)"
      contains: "_activity"
    - path: "src/phaze/templates/admin/partials/_agent_activity.html"
      provides: "Stacked agent-activity body (liveness header, 6-stage count grid, queue depths, recent scans, own-tick)"
      min_lines: 50
  key_links:
    - from: "src/phaze/services/pipeline.py _agent_stage_buckets"
      to: "stage_status_case"
      via: "inner subquery materializes stage_status_case(stage).label('status') filtered by agent_id, then GROUP BY the scalar label"
      pattern: "stage_status_case"
    - from: "src/phaze/routers/admin_agents.py"
      to: "_agent_stage_buckets"
      via: "buckets = {stage.value: await _agent_stage_buckets(session, agent_id, stage) for stage in (METADATA,FINGERPRINT,ANALYZE,PROPOSE,REVIEW,APPLY)}"
      pattern: "_agent_stage_buckets"
    - from: "_agent_activity.html"
      to: "#detail-pane"
      via: "own hx-trigger=every 5s re-fetching /admin/agents/{id}/_activity"
      pattern: "every 5s"
---

<objective>
Deliver DRILL-02: the agent-activity endpoint `GET /admin/agents/{agent_id}/_activity` and its
`_agent_activity.html` body, rendered into the shared `#detail-pane` shell from plan 88-01.

The endpoint groups the agent's owned files as a per-agent 6-stage matrix of COUNTS via a single indexed
`GROUP BY stage_status_case` per stage filtered to `agent_id` (D-04/D-00a — the aggregate is a one-conjunct
clone of `_safe_bucket_counts`, materializing the derived label in an inner subquery to dodge the Postgres
`GroupingError`), then stacks liveness header → stage matrix → per-lane queue depths → recent scan batches
(D-05). Every read is bounded + degrade-safe (D-00b); the body self-refreshes on its own 5s tick (D-03).
An unknown agent_id or an agent owning 0 files renders a friendly empty fragment, never a 500.

Purpose: The agent half of the lane/agent drill-in twin. The single genuinely-new query — the per-agent
stage aggregate — is a one-line diff on `_safe_bucket_counts`, reusing the DERIV-04-locked `stage_status_case`.
Output: `GET /admin/agents/{agent_id}/_activity`, `_agent_stage_buckets` service helper,
`_agent_activity.html`, and two test modules (agents bucket endpoint + integration bucket real-PG aggregate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-lane-agent-drill-in/88-CONTEXT.md
@.planning/phases/88-lane-agent-drill-in/88-RESEARCH.md
@.planning/phases/88-lane-agent-drill-in/88-PATTERNS.md
@.planning/phases/88-lane-agent-drill-in/88-UI-SPEC.md
@.planning/phases/88-lane-agent-drill-in/88-01-PLAN.md

<interfaces>
<!-- From plan 88-01 (already merged in this wave's base). Render INTO this contract; do not re-derive. -->
Shell:  src/phaze/templates/pipeline/partials/_detail_pane.html
  - Swap target: #detail-pane (innerHTML); pane header <h2 tabindex="-1"> id; ✕ Close; aria-live=polite
  - This body renders in the shell's body slot (pane_body = "admin/partials/_agent_activity.html")
  - Own-tick contract: body element carries hx-get="/admin/agents/{id}/_activity" hx-trigger="every 5s" hx-target="#detail-pane" hx-swap="innerHTML"
  - Trigger already emits hx-get="/admin/agents/{{ agent.id }}/_activity" (from agents_table.html, plan 88-01)

Existing (read-only):
  services/stage_status.py: stage_status_case(stage)  # stage_status.py:392 — the single derivation (D-00a)
  services/pipeline.py:324 _safe_bucket_counts(session, stage) -> dict[str,int]  # the D-04 template (GroupingError-safe inner-subquery GROUP BY)
  services/pipeline.py:47 MUSIC_VIDEO_TYPES ; :256 all_lane_queues(agent_id)+.count()
  routers/admin_agents.py:38 classify(agent, now) ; :48 humanize_relative_time global ; :62 _load_agents ; router prefix="/admin/agents" (:50)
  enums/stage.py: Stage.METADATA, FINGERPRINT, ANALYZE, PROPOSE, REVIEW, APPLY (+ TRACKLIST — OMITTED from the 6-pill matrix)
  models/scan_batch.py: ScanBatch(agent_id, created_at) with ix_scan_batches_agent_id
  templates: admin/partials/_kind_badge.html, _status_pill.html ; pipeline/partials/_stage_matrix.html (stage order + Appr=review/Exec=apply remap), _stage_pill.html (bucket color tokens)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _agent_stage_buckets aggregate + real-PG GROUP BY test (integration bucket)</name>
  <files>src/phaze/services/pipeline.py, tests/integration/test_agent_stage_buckets.py</files>
  <read_first>
    - 88-PATTERNS.md §"services/pipeline.py — _agent_stage_buckets" — the VERBATIM _safe_bucket_counts shape (inner subquery materializing stage_status_case(stage).label('status') FIRST, then GROUP BY the scalar label) + the ONLY change: add `.where(FileRecord.agent_id == agent_id)` to the inner subquery; the GROUP-BY-agent precedent at pipeline.py:204.
    - 88-RESEARCH.md §"Pattern 1" + Pitfall 2 + Pitfall 3 — the GroupingError gotcha (cannot GROUP BY stage_status_case directly; correlated exists(...==FileRecord.id) subqueries re-project ungrouped files.id) and the 7-stage→6-pill Appr=REVIEW/Exec=APPLY remap (TRACKLIST omitted); the three downstream stages (propose/review/apply) yield only done/failed/not_started — correct.
    - src/phaze/services/pipeline.py:304-361 — _safe_count + _safe_bucket_counts (the exact try/except → guarded rollback → all-zero degrade to copy).
    - src/phaze/services/stage_status.py:392 stage_status_case — the single derivation (D-00a); do NOT re-spell it.
    - tests/integration/test_stage_progress_buckets.py — the real-PG db_session fixture, _file seed helper, *_test DB guard, and the "assert against DERIVED truth (stage_status_case/resolve_status), not a hand-count" drift-lock discipline to copy.
    - MEMORY: mutation-test the guard — a GREEN GROUP BY test proves nothing; after writing it, break the agent_id conjunct → watch it go RED (wrong counts) → restore. Real PG (5433), not SQLite (Pitfall 2). Export BOTH TEST_DATABASE_URL + MIGRATIONS_TEST_DATABASE_URL.
  </read_first>
  <behavior>
    - `_agent_stage_buckets(session, agent_id, stage)` returns `{bucket_value: count}` for the five Status buckets, counting ONLY files WHERE agent_id == agent_id AND file_type in MUSIC_VIDEO_TYPES, grouped by stage_status_case(stage) materialized in an inner subquery.
    - For a seeded agent owning M music/video files, the five enrich buckets SUM to M on a healthy query (the same invariant _safe_bucket_counts documents — healthy-path property only, never a runtime assertion in the poll path).
    - Counts match the DERIVED truth (stage_status_case), asserted against the derivation not a hand-count (drift-lock).
    - Runs on real Postgres without GroupingError (the inner-subquery-first shape).
    - On any query error → all-zero dict via guarded rollback (degrade), never raises.
    - Filtering by a different agent_id returns that agent's counts only (mutation-check: removing the agent_id conjunct changes the result).
  </behavior>
  <action>
    Add `_agent_stage_buckets(session, agent_id, stage)` to `services/pipeline.py` as a verbatim clone of
    `_safe_bucket_counts` (pipeline.py:324-361) with the single addition of
    `.where(FileRecord.agent_id == agent_id)` on the inner `status_subq`. Keep the inner-subquery-then-
    GROUP-BY-scalar-label shape EXACTLY (Pitfall 2 — grouping directly by the CASE expression throws
    GroupingError on Postgres). Keep the try/except → guarded rollback → all-zero degrade (D-00b). Do NOT
    re-spell the CASE ladder — compose `stage_status_case(stage)` (D-00a/DERIV-04).

    Create `tests/integration/test_agent_stage_buckets.py` (real-PG, analog test_stage_progress_buckets.py):
    seed an agent owning a known corpus with per-stage output rows placing files in known buckets (NEVER a
    bare FileRecord.state mutation — derive from output tables). Assert: (1) the per-agent GROUP BY runs on
    real PG without GroupingError; (2) per-stage bucket counts equal the DERIVED truth (resolve via
    stage_status_case / the same predicates), not a hand-count; (3) the five enrich buckets sum to the
    agent's music/video count on the healthy corpus; (4) a second agent's files are excluded (mutation-check
    that the agent_id conjunct is load-bearing). Guard the destructive seed with the `*_test` DB check.
  </action>
  <acceptance_criteria>
    - Source: `services/pipeline.py` contains `def _agent_stage_buckets(` whose inner subquery contains BOTH `stage_status_case(stage).label(` AND `FileRecord.agent_id == agent_id`, and GROUPs BY the subquery scalar label (not the CASE expression directly).
    - Behavior (real PG): the aggregate executes without `GroupingError`; counts match the derived truth for a seeded corpus; the five enrich buckets sum to the agent's music/video total on a healthy query; a second agent's rows are excluded.
    - Mutation-check documented: removing `FileRecord.agent_id == agent_id` makes the test RED (recorded in the SUMMARY).
    - Test: `uv run pytest tests/integration/test_agent_stage_buckets.py -x` exits 0 (real PG 5433, both DB URLs exported).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/integration/test_agent_stage_buckets.py -x -q</automated>
  </verify>
  <done>_agent_stage_buckets is a GroupingError-safe, degrade-safe per-agent GROUP BY over stage_status_case (one-conjunct clone of _safe_bucket_counts); the real-PG test asserts counts against the derived truth, the sum-to-total invariant, and the agent_id filter (mutation-checked).</done>
</task>

<task type="auto">
  <name>Task 2: GET /admin/agents/{agent_id}/_activity endpoint + _agent_activity.html body</name>
  <files>src/phaze/routers/admin_agents.py, src/phaze/services/pipeline.py, src/phaze/templates/admin/partials/_agent_activity.html, tests/agents/routers/test_agent_activity.py</files>
  <read_first>
    - 88-PATTERNS.md §"routers/admin_agents.py — GET /admin/agents/{agent_id}/_activity" — session.get(Agent, agent_id) → None → friendly empty fragment; the liveness idiom (agent._status = classify(agent, now); _status_pill.html / _kind_badge.html / humanize_relative_time); the buckets dict keyed by Stage VALUE for the 6-stage set; the ScanBatch recent-batches query (ix_scan_batches_agent_id).
    - 88-PATTERNS.md §"_agent_activity.html (NEW, agent body)" — stacked D-05 order; the _stage_matrix.html stage-order EXACTLY incl. Appr=review/Exec=apply remap; the per-COUNT-cell bucket color tokens from _stage_pill.html (done/in_flight/not_started/failed/skipped) rendered as a stage×bucket NUMERAL grid (NOT the pill-include as-is); WORD+GLYPH+aria-label per cell; legend reuse; overflow-x-auto narrow scroll.
    - 88-PATTERNS.md §"per-lane queue depth for the agent pane" — the get_queue_activity all_lane_queues(agent_id)+.count() loop with per-source try/except → 0.
    - 88-UI-SPEC.md §"Component Inventory" row 5 + §"Copywriting Contract" — stacked sections, empty-state copy ("This agent owns no files yet.", "No recent scan batches.", "Live data unavailable — retrying on the next tick."), degrade fallback.
    - src/phaze/routers/admin_agents.py:50 (router prefix="/admin/agents"), :62-86 (_load_agents transient-attr pattern), :38/:48 (classify + humanize_relative_time global).
    - src/phaze/templates/pipeline/partials/_stage_matrix.html:29-52 and _stage_pill.html — stage order + remap + bucket tokens + legend.
    - src/phaze/models/scan_batch.py — agent_id + created_at + ix_scan_batches_agent_id.
    - RESEARCH Pitfall 5 + Pitfall 6 — NEVER render f.state (guard test_no_raw_state_render.py); get_session never commits (read-only), seed via conftest factories, assert on returned HTML / independent session.
  </read_first>
  <action>
    Add `GET /{agent_id}/_activity` to `routers/admin_agents.py` (router prefix `/admin/agents`, so the
    full path is `/admin/agents/{agent_id}/_activity`), `response_class=HTMLResponse`, `agent_id: str`.
    `agent = await session.get(Agent, agent_id)`; None → render `_detail_pane.html` with the friendly
    "agent not found" empty body (HTML fragment, optionally 404 — never HTTPException/JSON). For a found
    agent: `now = datetime.now(UTC)`; `agent._status = classify(agent, now)` (transient attr); build
    `buckets = {stage.value: await _agent_stage_buckets(session, agent_id, stage) for stage in
    (Stage.METADATA, Stage.FINGERPRINT, Stage.ANALYZE, Stage.PROPOSE, Stage.REVIEW, Stage.APPLY)}` (D-04);
    recent scans = `ScanBatch WHERE agent_id ORDER BY created_at DESC LIMIT N` (_safe wrapped); per-lane
    queue depths via the `all_lane_queues(agent_id)`+`.count()` loop (per-source degrade to 0). Render
    `_detail_pane.html` with `pane_body="admin/partials/_agent_activity.html"` and context
    `{agent, now, buckets, recent_scans, queue_depths, refreshed_at}`. Read-only — no commit.

    Create `_agent_activity.html` with the D-05 stacked sections in order: (1) liveness header —
    `{% include "admin/partials/_kind_badge.html" %}` + `{% include "admin/partials/_status_pill.html" %}`
    + `{{ humanize_relative_time(agent.last_seen_at, now=now) }}`; (2) the 6-stage COUNT grid — reuse the
    `_stage_matrix.html` stage order + Appr=review/Exec=apply remap, rendering each `stage × bucket` cell as
    a NUMERAL with the `_stage_pill.html` bucket color token + WORD + GLYPH + `aria-label` (do NOT pass
    count-dicts into `_stage_matrix.html` unchanged — it pill-renders a single bucket); reuse the legend;
    `overflow-x-auto` for narrow; if the agent owns 0 files render "This agent owns no files yet."; (3)
    per-lane queue depths (`analyze N · io N`); (4) recent scan batches (last N, newest-first) or "No recent
    scan batches." Add the D-03 own-tick element
    `hx-get="/admin/agents/{{ agent.id }}/_activity" hx-trigger="every 5s" hx-target="#detail-pane" hx-swap="innerHTML"`.
    NEVER render `f.state` (Pitfall 5). Keep agent name/id/kind autoescaped.

    Create `tests/agents/routers/test_agent_activity.py` (client fixture, smoke-app idiom): known agent →
    200 fragment with 6 stage rows + liveness header + queue depths + recent scans; the Appr column reflects
    Stage.REVIEW and Exec reflects Stage.APPLY (Pitfall 3 remap); unknown agent_id → friendly empty fragment,
    NOT 500; agent owning 0 files → "This agent owns no files yet." empty state; a forced source error →
    degraded fragment (0/—), never 500. Assert per-agent counts equal the derived-truth fixture totals.
  </action>
  <acceptance_criteria>
    - Source: `routers/admin_agents.py` contains `@router.get("/{agent_id}/_activity"` returning HTMLResponse, builds `buckets` over the six Stage members (METADATA,FINGERPRINT,ANALYZE,PROPOSE,REVIEW,APPLY), and does `session.get(Agent, agent_id)` with a None → fragment branch (no HTTPException/JSON).
    - Source: `_agent_activity.html` renders sections in D-05 order, keys the matrix `metadata/fingerprint/analyze/propose/review/apply` with Appr=review & Exec=apply, contains an own-tick `hx-trigger="every 5s"`, and does NOT render `agent`/`f` `.state`.
    - Behavior: `GET /admin/agents/{id}/_activity` returns 6 stage rows; per-agent counts equal the hand-seeded fixture totals; unknown agent_id → friendly empty fragment (not 500); agent with 0 files → "This agent owns no files yet."
    - Test: `uv run pytest tests/agents/routers/test_agent_activity.py -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/agents/routers/test_agent_activity.py -x -q</automated>
  </verify>
  <done>GET /admin/agents/{agent_id}/_activity returns a stacked, degrade-safe fragment (liveness header → 6-stage COUNT matrix with correct Appr/Exec remap → per-lane queue depths → recent scan batches, own 5s tick); unknown/zero-file agent → friendly empty state, never 500; no raw state render.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → FastAPI (private LAN, no auth) | `{agent_id}` path param crosses here into `session.get(Agent, …)` + template. |
| service → Postgres / SAQ broker | Per-agent GROUP BY aggregate, ScanBatch scan-history, per-lane queue-depth reads on the 5s tick. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-07 | Elevation/Info-disclosure (IDOR/path) | `{agent_id}` path param | mitigate | `session.get(Agent, agent_id)`; None → friendly empty fragment (record.py precedent), never a 500 or a raw-param-driven query. |
| T-88-08 | DoS (unbounded read on the 5s tick) | per-agent aggregate / scans / queues | mitigate | One indexed `GROUP BY` per stage (D-04, NOT row materialization) + LIMIT-N scans + per-source `_safe_count`/SAVEPOINT degrade to 0/[] (D-00b/PERF-01) — no whole-corpus scan per poll. |
| T-88-09 | Tampering (reflected XSS) | `agent.name`/`agent_id`/`agent.kind` in the fragment | mitigate | Jinja autoescape (default on); never `\|safe`/`\|tojson` on operator-declared values (T-71-05). |
| T-88-10 | Info-disclosure (stale/raw-state leak) | agent grouping source | mitigate | Read ONLY the derived `stage_status_case`; NEVER render `FileRecord.state` (Pitfall 5; guard `test_no_raw_state_render.py`). |
| T-88-SC | Tampering | package installs | accept (none) | Zero packages installed (RESEARCH §Package Legitimacy Audit). |

Residual risk: LOW — read-only endpoint over a bounded, degrade-safe, derivation-locked aggregate.
</threat_model>

<verification>
- `uv run pytest tests/agents/routers/test_agent_activity.py tests/integration/test_agent_stage_buckets.py -x` green.
- `just test-bucket agents` and `just test-bucket integration` pass in isolation (export BOTH `TEST_DATABASE_URL` + `MIGRATIONS_TEST_DATABASE_URL`, port 5433 — the per-agent GROUP BY MUST run on real PG to catch GroupingError).
- Mutation-check performed: removing the `agent_id` conjunct turns the aggregate test RED, then restored (record in SUMMARY).
- `uv run ruff check . && uv run ruff format --check . && uv run mypy .` clean (services/ mypy-excluded; keep hints anyway).
- No raw `FileRecord.state` render (guard `test_no_raw_state_render.py` / `just docs-drift` green).
- 90% coverage floor maintained.
</verification>

<success_criteria>
- Clicking an agent row opens `GET /admin/agents/{agent_id}/_activity` showing owned files grouped by derived stage_status, recent scan batches, per-lane queue depths, and liveness (DRILL-02).
- The grouping is a per-agent 6-stage COUNT matrix via a GROUP BY over the single stage_status_case derivation — no second derivation, no row materialization (D-00a/D-04), Appr=REVIEW/Exec=APPLY correct.
- Stacked sections in D-05 order; everything visible on open.
- Every read bounded + degrade-safe; unknown/zero-file agent → friendly empty fragment, never 500 (D-00b).
- The open body self-refreshes on its own bounded 5s tick and stops on dismiss (D-03).
</success_criteria>

<output>
Create `.planning/phases/88-lane-agent-drill-in/88-03-SUMMARY.md` when done.
</output>
