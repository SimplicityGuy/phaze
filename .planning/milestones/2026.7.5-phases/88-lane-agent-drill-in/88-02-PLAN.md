---
phase: 88-lane-agent-drill-in
plan: 02
type: execute
wave: 2
depends_on: [88-01]
files_modified:
  - src/phaze/routers/pipeline.py
  - src/phaze/services/backends.py
  - src/phaze/templates/pipeline/partials/_lane_detail.html
  - tests/analyze/routers/test_lane_detail.py
autonomous: true
requirements: [DRILL-01, DRILL-03]
must_haves:
  truths:
    - "DRILL-01: GET /pipeline/lanes/{backend_id} returns an HTML fragment showing that lane's queues / in-flight / waiting / quota / recent completions."
    - "D-06: the lane detail is kind-adaptive — common fields (rank, in-flight/cap, availability, recent completions) for every lane; kueue-only quota-waiting + Inadmissible; NO fabricated zero/n-a quota rows on non-kueue lanes."
    - "D-07: recent completions are bounded by a fixed last-N (N=20), newest-first — predictable render cost under any throughput."
    - "D-00b: every read (lane snapshot, recent completions, queue depths) is bounded and _safe_count/SAVEPOINT-degrade-safe — degrades to 0/None/[] and never 500s the poll or the own-tick."
    - "D-03: the loaded lane body carries its OWN bounded hx-trigger=\"every 5s\" tick scoped to backend_id, removed with the pane on dismiss (no orphan loop)."
  artifacts:
    - path: "src/phaze/templates/pipeline/partials/_lane_detail.html"
      provides: "Lane-detail body slot (kind-adaptive fields, capacity bar, kueue admission alert, recent completions, offline empty state, own-tick)"
      min_lines: 40
    - path: "src/phaze/routers/pipeline.py"
      provides: "GET /pipeline/lanes/{backend_id} read-only HTML-fragment endpoint"
      contains: "/pipeline/lanes/"
    - path: "src/phaze/services/backends.py"
      provides: "Degrade-safe lane recent-completions (CloudJob, compute/kueue only) + per-lane queue-depth reads for the lane detail"
      contains: "def "
  key_links:
    - from: "src/phaze/routers/pipeline.py"
      to: "get_backend_lane_snapshot"
      via: "lane_detail selects the snapshot entry whose id == backend_id; None -> Lane offline empty state"
      pattern: "get_backend_lane_snapshot"
    - from: "src/phaze/services/backends.py recent-completions"
      to: "CloudJob"
      via: "WHERE backend_id == id AND status == succeeded ORDER BY updated_at DESC LIMIT 20"
      pattern: "CloudJob"
    - from: "_lane_detail.html"
      to: "#detail-pane"
      via: "own hx-trigger=every 5s re-fetching /pipeline/lanes/{id}"
      pattern: "every 5s"
---

<objective>
Deliver DRILL-01: the lane-detail endpoint `GET /pipeline/lanes/{backend_id}` and its `_lane_detail.html`
body, rendered into the shared `#detail-pane` shell from plan 88-01.

The endpoint reads a backend-id-scoped slice of the existing `get_backend_lane_snapshot()` (degrade-safe),
renders kind-adaptive fields (D-06 — kueue-only quota/Inadmissible, no n/a fillers elsewhere), the last
N=20 newest-first recent completions (D-07), and per-lane queue depths — every read `_safe_count`/SAVEPOINT
degrade-safe (D-00b). The body carries its own bounded 5s tick (D-03). An unknown/offline backend_id
renders the friendly "Lane offline" empty fragment, never a 500.

Purpose: The lane half of the lane/agent drill-in twin — a thin, read-only, additive endpoint composing
existing service reads into the plan-01 shell contract.
Output: `GET /pipeline/lanes/{backend_id}`, `_lane_detail.html`, degrade-safe lane data helpers, and a
DRILL-01 test module (analyze bucket).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-lane-agent-drill-in/88-CONTEXT.md
@.planning/phases/88-lane-agent-drill-in/88-RESEARCH.md
@.planning/phases/88-lane-agent-drill-in/88-PATTERNS.md
@.planning/phases/88-lane-agent-drill-in/88-UI-SPEC.md
@.planning/phases/88-lane-agent-drill-in/88-01-PLAN.md

<interfaces>
<!-- From plan 88-01 (already merged in this wave's base). Render INTO this contract; do not re-derive. -->
Shell:  src/phaze/templates/pipeline/partials/_detail_pane.html
  - Swap target: #detail-pane (innerHTML); pane header <h2 tabindex="-1"> id; ✕ Close; aria-live=polite
  - This body renders in the shell's body slot (pane_body = "pipeline/partials/_lane_detail.html")
  - Own-tick contract: body element carries hx-get="/pipeline/lanes/{id}" hx-trigger="every 5s" hx-target="#detail-pane" hx-swap="innerHTML"
  - Trigger already emits hx-get="/pipeline/lanes/{{ lane.id }}" (from _lane_card.html, plan 88-01)

From services/backends.py (existing, read-only):
  async def get_backend_lane_snapshot(session) -> list[dict]   # backends.py:756 — [{id,kind,rank,cap,in_flight,available,quota_wait,inadmissible}], degrades to []
From models/cloud_job.py: CloudJob(backend_id: str|None, status: str, updated_at); CloudJobStatus.SUCCEEDED = "succeeded"
From services/pipeline.py:256: app_state.task_router.all_lane_queues(agent_id) + .count("queued"/"active")  # per-lane depth idiom
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Degrade-safe lane data helpers (recent completions + queue depths) in services/backends.py</name>
  <files>src/phaze/services/backends.py, tests/analyze/routers/test_lane_detail.py</files>
  <read_first>
    - 88-PATTERNS.md §"routers/pipeline.py — GET /pipeline/lanes/{backend_id}" — the CloudJob recent-completions query (backend_id + status=succeeded + updated_at DESC LIMIT N) and the note that a LocalBackend writes NO cloud_job rows (backends.py:711 short-circuit).
    - 88-PATTERNS.md §"Shared Patterns → Degrade-safe read" — the _safe_count / begin_nested() SAVEPOINT try/except → guarded rollback → 0/None/[] shape to copy verbatim.
    - 88-RESEARCH.md §"Lane detail data sources" table + Open Question 1 — local lane has no CloudJob completions; RESOLUTION: omit recent-completions on local (render "No completions in the last 20"); source compute/kueue from CloudJob.
    - src/phaze/services/backends.py:652-800 — get_backend_lane_snapshot + the secret-free lane-dict contract (only {id,kind,rank,cap,in_flight,available,quota_wait,inadmissible} ever exposed — do NOT add raw backend.config).
    - src/phaze/models/cloud_job.py — CloudJob.backend_id (String, nullable), status (String), SUCCEEDED="succeeded", updated_at (TimestampMixin).
    - src/phaze/services/pipeline.py:222-283 get_queue_activity — the all_lane_queues(agent_id) + .count() per-source try/except → 0 loop for queue depth.
    - tests/integration/test_stage_progress_buckets.py — real-PG db_session fixture + _file seed helper + *_test DB guard (analog for the CloudJob-seeded recent-completions test); tests/agents/routers/test_admin_agents.py — client fixture idiom.
    - MEMORY: verify a CloudJob-with-backend_id factory exists in conftest; if not, add one for the recent-completions test.
  </read_first>
  <behavior>
    - `get_lane_recent_completions(session, backend_id, kind, limit=20)`: for kind in {compute, kueue}, returns up to 20 CloudJob rows WHERE backend_id==id AND status=="succeeded" ORDER BY updated_at DESC; for kind=="local" returns [] (D-06/OQ1 — local writes no cloud_job). On any query error → [] (degrade), never raise.
    - `get_lane_queue_depths(session, app_state, backend_id)` (or reuse the get_queue_activity idiom): returns per-lane {queued, active} for the lane's backing agent; a missing app.state attr or broker hiccup degrades each count to 0, never 500.
    - Both helpers are bounded (LIMIT 20 / per-source count) — no whole-corpus scan (D-00b).
  </behavior>
  <action>
    Add two read-only, degrade-safe helpers to `services/backends.py`, co-located with
    `get_backend_lane_snapshot`. `get_lane_recent_completions(session, backend_id, kind, limit=20)`:
    kueue/compute → the CloudJob succeeded LIMIT-20 newest-first query (88-PATTERNS.md excerpt); local →
    return `[]` (D-06/Open-Question-1 resolution: omit — the template shows the "No completions in the
    last 20" empty state). Wrap the query in the `begin_nested()`/try-except → guarded rollback → `[]`
    degrade shape. `get_lane_queue_depths(...)`: reuse the `all_lane_queues` + `.count()` idiom with the
    per-source `try/except → 0` isolation. Do NOT expose any secret/config field — only the snapshot-level
    scalars + completion timestamps/status. Define `LANE_RECENT_N = 20` as a module constant (D-07).

    Write the RED-first test seed cases in `tests/analyze/routers/test_lane_detail.py` for these helpers:
    seed N>20 succeeded CloudJob rows for one backend_id and assert exactly 20 returned newest-first;
    seed a local lane and assert `[]`; force a query error and assert `[]` (degrade). Use the real-PG
    db_session fixture (the CloudJob backend_id filter is a real column read).
  </action>
  <acceptance_criteria>
    - Source: `services/backends.py` contains `def get_lane_recent_completions(` and `LANE_RECENT_N = 20` and a `begin_nested()`-or-try/except degrade that returns `[]`.
    - Behavior: with 25 seeded succeeded CloudJob rows for a compute backend, the helper returns exactly 20, ordered by `updated_at` DESC; for a local backend it returns `[]`.
    - Behavior: a forced DB error inside the helper returns `[]` (never raises).
    - Test: `uv run pytest tests/analyze/routers/test_lane_detail.py -k "completions or degrade" -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/analyze/routers/test_lane_detail.py -k "completions or degrade" -x -q</automated>
  </verify>
  <done>Bounded, degrade-safe lane recent-completions (N=20, compute/kueue only) and per-lane queue-depth helpers exist in services/backends.py; local lane yields [] completions; a forced error degrades to [], never 500.</done>
</task>

<task type="auto">
  <name>Task 2: GET /pipeline/lanes/{backend_id} endpoint + _lane_detail.html body</name>
  <files>src/phaze/routers/pipeline.py, src/phaze/templates/pipeline/partials/_lane_detail.html, tests/analyze/routers/test_lane_detail.py</files>
  <read_first>
    - 88-PATTERNS.md §"routers/pipeline.py — GET /pipeline/lanes/{backend_id}" (Analog A/B/C) — record.py:41-61 typed-param + friendly-404 fragment shape; the `lane = next((l for l in lanes if l["id"] == backend_id), None)` lookup; pipeline.py:740 call-site mirror.
    - 88-PATTERNS.md §"_lane_detail.html (NEW, lane body)" — reuse _lane_card.html kind→token map (local 🖥️/emerald, compute ☁️/blue, kueue ⎈/amber), RANK micro-label, the h-1.5 capacity bar, and the D-06 kueue-only admission branch (word-labelled, amber + role="alert" when inadmissible > 0); offline empty state (opacity-60 + word "offline").
    - 88-UI-SPEC.md §"Component Inventory" row 4 + §"Copywriting Contract" — kind-adaptive field list, recent-completions N=20, empty-state copy ("Lane offline — no live queue data on this poll." / "No completions in the last 20."), degrade fallback (— / 0).
    - src/phaze/routers/record.py:41-61 — the typed-path-param HTML-fragment + friendly 404 (never JSON/HTTPException) precedent.
    - src/phaze/routers/pipeline.py:311 (router = APIRouter(tags=["pipeline"])), :685-745 (pipeline_stats_partial + get_backend_lane_snapshot call site).
    - src/phaze/templates/pipeline/partials/_lane_card.html:54-85 — kind tokens + capacity bar + kueue admission branch to reuse verbatim.
    - 88-RESEARCH.md §"Security Domain" — backend_id is operator-declared, stays autoescaped; prefer lookup-in-known-set over trusting the raw param; unknown → empty fragment not 500.
  </read_first>
  <action>
    Add `GET /pipeline/lanes/{backend_id}` to `routers/pipeline.py` (`router = APIRouter(tags=["pipeline"])`,
    :311), `response_class=HTMLResponse`, `backend_id: str` typed path param, `session: Depends(get_session)`.
    Call `get_backend_lane_snapshot(session)` (degrade-safe → []), select the entry whose `id == backend_id`;
    if absent render the shared `_detail_pane.html` with the "Lane offline" empty body (optionally
    `status_code=404`, HTML fragment — never HTTPException/JSON, per record.py). For a found lane call the
    Task-1 helpers for recent completions (kind-adaptive) + queue depths, all `_safe_count`-wrapped. Render
    `_detail_pane.html` with `pane_body="pipeline/partials/_lane_detail.html"` and context
    `{lane, backend_id, recent_completions, queue_depths, refreshed_at}`. Read-only — no commit.

    Create `_lane_detail.html`: header reuses `_lane_card.html` kind glyph/color + `RANK {{ lane.rank }}`;
    common fields = in-flight/cap (reuse the `h-1.5` capacity bar), availability; **D-06 kueue-only branch**
    `{% if lane.kind == 'kueue' %}` for quota-waiting + Inadmissible (amber + `role="alert"` when
    `inadmissible > 0`) — NO n/a rows on local/compute; recent completions = the last-20 list
    (`{% for c in recent_completions %}`), else the "No completions in the last 20." empty state; per-lane
    queue depths (`analyze N · io N`). Add the D-03 own-tick element:
    `hx-get="/pipeline/lanes/{{ lane.id }}" hx-trigger="every 5s" hx-target="#detail-pane" hx-swap="innerHTML"`.
    Every value degrade-safe (`—`/`0`, never blank). Keep all ids/kinds autoescaped.

    Extend `tests/analyze/routers/test_lane_detail.py`: known backend → 200 fragment with rank/in-flight/cap;
    kueue lane → contains "inadmissible"/quota; non-kueue (local) fragment does NOT contain "inadmissible";
    unknown backend_id → friendly empty fragment ("Lane offline"), NOT 500; recent completions bounded to
    20 newest-first; a forced snapshot error → degraded fragment (0/—), never 500.
  </action>
  <acceptance_criteria>
    - Source: `routers/pipeline.py` contains `@router.get("/pipeline/lanes/{backend_id}"` returning HTMLResponse; the body does a lookup-in-known-set (`next((l ... if l["id"] == backend_id`)) and renders a fragment (no HTTPException/JSONResponse).
    - Source: `_lane_detail.html` gates quota/Inadmissible under `{% if lane.kind == 'kueue' %}` and contains an own-tick `hx-trigger="every 5s"` element.
    - Behavior: `GET /pipeline/lanes/{kueue_id}` returns 200 and contains "inadmissible"; `GET /pipeline/lanes/{local_id}` returns 200 and does NOT contain "inadmissible".
    - Behavior: `GET /pipeline/lanes/__nope__` returns a friendly "Lane offline" fragment (not a 500 / not JSON).
    - Test: `uv run pytest tests/analyze/routers/test_lane_detail.py -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/analyze/routers/test_lane_detail.py -x -q</automated>
  </verify>
  <done>GET /pipeline/lanes/{backend_id} returns a kind-adaptive, degrade-safe HTML fragment (kueue-only quota/Inadmissible, N=20 newest-first completions, per-lane queue depths, own 5s tick); unknown id → friendly empty state, never 500.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → FastAPI (private LAN, no auth) | `{backend_id}` path param crosses here into a snapshot lookup + template. |
| service → Postgres / SAQ broker | Lane snapshot, CloudJob completions, per-lane queue-depth reads on the 5s tick. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-03 | Elevation/Info-disclosure (IDOR/path) | `{backend_id}` path param | mitigate | Look `backend_id` up in the `get_backend_lane_snapshot()` known-id set; unknown → friendly "Lane offline" fragment (record.py:55-61 precedent), never a 500 or a raw-param-driven read. |
| T-88-04 | Info-disclosure (secret leak) | `_lane_detail.html` body | mitigate | Render only the secret-free snapshot scalars + completion status/timestamps; never add raw `backend.config`/SecretStr/kube token (backends.py:765 T-71-01). |
| T-88-05 | Tampering (reflected XSS) | `backend_id`/`lane.kind` in the fragment | mitigate | Jinja autoescape (default on); never `\|safe`/`\|tojson` on operator-declared ids/kinds (T-71-05). |
| T-88-06 | DoS (unbounded read on the 5s tick) | recent completions / queue depths | mitigate | LIMIT 20 (D-07) + per-source `_safe_count`/SAVEPOINT degrade to 0/[] (D-00b/PERF-01) — no whole-corpus scan per poll. |
| T-88-SC | Tampering | package installs | accept (none) | Zero packages installed (RESEARCH §Package Legitimacy Audit). |

Residual risk: LOW — read-only endpoint over degrade-safe, secret-free, bounded service reads.
</threat_model>

<verification>
- `uv run pytest tests/analyze/routers/test_lane_detail.py -x` green.
- `just test-bucket analyze` passes in isolation (export BOTH `TEST_DATABASE_URL` + `MIGRATIONS_TEST_DATABASE_URL`, port 5433 — the CloudJob-seeded completions test needs real PG).
- `uv run ruff check . && uv run ruff format --check . && uv run mypy .` clean (note: `services/` is mypy-excluded; keep type hints per project style anyway).
- No raw `FileRecord.state` render (guard `test_no_raw_state_render.py` / `just docs-drift` green).
- 90% coverage floor maintained.
</verification>

<success_criteria>
- Clicking a lane card opens `GET /pipeline/lanes/{backend_id}` showing queues / in-flight / waiting / quota / recent completions (DRILL-01).
- Kind-adaptive fields: kueue shows quota-waiting + Inadmissible; local/compute show NO n/a quota rows (D-06).
- Recent completions bounded to N=20 newest-first (D-07); local lane shows the empty state (Open Question 1 resolved: omit).
- Every read degrade-safe; unknown backend_id → friendly empty fragment, never 500 (D-00b).
- The open body self-refreshes on its own bounded 5s tick and stops on dismiss (D-03).
</success_criteria>

<output>
Create `.planning/phases/88-lane-agent-drill-in/88-02-SUMMARY.md` when done.
</output>
