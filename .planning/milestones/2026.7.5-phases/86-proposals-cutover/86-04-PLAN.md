---
phase: 86-proposals-cutover
plan: 04
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - tests/review/services/test_proposal.py
autonomous: true
requirements: [SIDECAR-03]

must_haves:
  truths:
    - "Full `tests/review` bucket is green — no test asserts the removed FileRecord.state cascade"
    - "test_creates_rename_proposal_records no longer asserts file_record.state == 'proposal_generated'"
    - "The inline comment/docstring claiming the file state advances is gone (matches SIDECAR-03 cutover)"
  artifacts:
    - path: "tests/review/services/test_proposal.py"
      provides: "store_proposals unit tests with no stale file.state cascade assertion"
      contains: "def test_creates_rename_proposal_records"
  key_links:
    - from: "tests/review/services/test_proposal.py"
      to: "src/phaze/services/proposal.py::store_proposals"
      via: "unit test of the upsert-only writer (no file.state side effect)"
      pattern: "store_proposals"
---

<objective>
Close VERIFICATION Gap 1: a pre-existing test outside the phase's declared scope is now RED because
it asserts the exact `FileRecord.state` cascade write Plan 01 deleted from `store_proposals`.

`tests/review/services/test_proposal.py::TestStoreProposals::test_creates_rename_proposal_records`
asserts `file_record.state == "proposal_generated"` — a behavior SIDECAR-03 intentionally removed
(no writer sets `file.state` from `store_proposals` anymore). This file was never in any of the three
executed plans' `files_modified` lists, so no plan updated it. The full `tests/review` bucket is
`1 failed, 427 passed`; the three SUMMARY "review bucket green" claims came from running only the
individually-touched files, never the full bucket.

Purpose: restore a green `tests/review` bucket so SIDECAR-03 re-verification can pass.
Output: the stale assertion and its misleading comment/docstring removed; full `tests/review` bucket green.

TESTS ONLY — no production source changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-proposals-cutover/86-VERIFICATION.md
@.planning/phases/86-proposals-cutover/86-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the stale file.state cascade assertion and its misleading comment</name>
  <files>tests/review/services/test_proposal.py</files>
  <read_first>
    - tests/review/services/test_proposal.py (the whole `TestStoreProposals` class; the target method
      `test_creates_rename_proposal_records` is at lines 637-675)
    - .planning/phases/86-proposals-cutover/86-VERIFICATION.md (Gap 1 `missing:` list — the exact fixes)
    - .planning/phases/86-proposals-cutover/86-CONTEXT.md (D-01: `store_proposals` no longer loads or
      writes `FileRecord`; the file-load-and-guard block was deleted, not just the assignment)
  </read_first>
  <action>
    In `tests/review/services/test_proposal.py`, method
    `TestStoreProposals::test_creates_rename_proposal_records` (lines 637-675):
    (1) Delete the assertion `assert file_record.state == "proposal_generated"` (currently at line 675).
    (2) Fix the inline comment above it (currently line 672:
    "# The upsert is issued (one pg_insert per proposal) and the file state advances.") so it no longer
    claims the file state advances — reword to reflect that `store_proposals` issues the upsert only and
    does NOT touch `FileRecord.state` (SIDECAR-03 cutover). Keep the two surviving positive assertions
    `mock_pg_insert.assert_called_once()` and `session.execute.assert_awaited()` — they still hold.
    Leave the `file_record` MagicMock scaffolding (lines 646-652) in place; it is harmless and trimming
    it is out of scope. Do NOT modify any other test method and do NOT touch any file under `src/`.
    If a class- or method-level docstring elsewhere in this file also asserts that `store_proposals`
    advances `file.state`, correct that prose too; do not invent new docstrings.
  </action>
  <verify>
    <automated>uv run pytest tests/review/services/test_proposal.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'file_record.state == "proposal_generated"' tests/review/services/test_proposal.py` returns
      nothing (assertion removed).
    - No remaining comment or docstring in the file claims `store_proposals` advances / sets
      `file.state` (`grep -niE "file[_ ]state advances" tests/review/services/test_proposal.py` returns nothing).
    - `uv run pytest tests/review/services/test_proposal.py::TestStoreProposals::test_creates_rename_proposal_records -q` exits 0.
    - No file under `src/` is modified (`git diff --name-only -- src/` is empty).
  </acceptance_criteria>
  <done>The stale cascade assertion and its misleading comment are gone; the touched test file passes.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the full tests/review bucket is green</name>
  <files>tests/review/services/test_proposal.py</files>
  <read_first>
    - .planning/phases/86-proposals-cutover/86-VERIFICATION.md (Gap 1 `missing:` item 2 — "Re-run the
      full `tests/review` bucket, not just the individually touched files")
  </read_first>
  <action>
    Run the ENTIRE `tests/review` bucket (not just the touched file) to confirm zero regressions from
    the SIDECAR-03 deletions, per the verification note that the SUMMARY "bucket green" claims were only
    ever backed by individually-touched files. If anything else in the bucket still asserts the removed
    `FileRecord.state` proposal cascade, it is in scope for this gap plan — fix the stale assertion the
    same way (remove the cascade assertion, correct the prose). Do NOT modify production source to make a
    test pass; only stale test expectations of the deleted cascade are in scope.
  </action>
  <verify>
    <automated>uv run pytest tests/review -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/review -q` exits 0 (0 failed).
    - The pytest summary line shows 0 failed and at least 428 passed (the previously-failing test now
      passes alongside the 427 that already passed).
    - No file under `src/` is modified (`git diff --name-only -- src/` is empty).
  </acceptance_criteria>
  <done>The full `tests/review` bucket exits 0; the SIDECAR-03 review-bucket regression is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-suite → CI review-bucket gate | A red `tests/review` bucket blocks the phase PR |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-04-01 | Tampering | test edit weakens coverage | mitigate | Only the assertion of the DELETED cascade is removed; the two positive assertions (`pg_insert` called, `session.execute` awaited) are preserved, so `store_proposals`' real behavior stays covered. |
| T-86-04-02 | Repudiation | "bucket green" claimed but not run | mitigate | Acceptance requires the FULL `uv run pytest tests/review -q` exit 0, not a single-file run — closing the exact false-claim Gap 1 documents. |
</threat_model>

<verification>
- `uv run pytest tests/review -q` exits 0.
- `git diff --name-only -- src/` is empty (tests-only change).
</verification>

<success_criteria>
The stale `file_record.state == "proposal_generated"` assertion and its misleading comment are removed,
and the full `tests/review` bucket is green — closing VERIFICATION Gap 1 (truth 9).
</success_criteria>

<output>
Create `.planning/phases/86-proposals-cutover/86-04-SUMMARY.md` when done. Record the full-bucket
`uv run pytest tests/review -q` result line as evidence (not a single-file run).
</output>
