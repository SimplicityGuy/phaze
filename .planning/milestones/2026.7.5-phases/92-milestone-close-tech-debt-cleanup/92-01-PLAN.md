---
phase: 92-milestone-close-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/services/backends.py
  - src/phaze/routers/agent_files.py
autonomous: true
requirements: [CLEAN-03]
must_haves:
  truths:
    - "The duplicated KubeConfig comment block in backends.reconcile appears exactly once (D-09)"
    - "The agent_files ON-CONFLICT comment no longer references the Phase-90-dropped files.state / DISCOVERED-stamp semantics (D-10)"
    - "No runtime behavior changes; existing anti-drift guards still pass"
  artifacts:
    - path: "src/phaze/services/backends.py"
      provides: "De-duplicated MKUE-01/D-04 KubeConfig comment (one copy)"
      contains: "MKUE-01/D-04: thread THIS backend's KubeConfig"
    - path: "src/phaze/routers/agent_files.py"
      provides: "Corrected ON-CONFLICT comment reflecting current (post-Phase-90) schema"
  key_links: []
---

<objective>
Two comment-only doc-hygiene fixes surfaced by the 2026.7.5 milestone audit (CLEAN-03 / D-09 + D-10). Zero runtime change.

Purpose: Remove stale/duplicated comments before milestone close so the source reads accurately.
Output: Corrected comments in `backends.py` and `agent_files.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-milestone-close-tech-debt-cleanup/92-CONTEXT.md
@.planning/phases/92-milestone-close-tech-debt-cleanup/92-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicated comment (D-09) and correct stale ON-CONFLICT comment (D-10)</name>
  <files>src/phaze/services/backends.py, src/phaze/routers/agent_files.py</files>
  <read_first>
    - src/phaze/services/backends.py:560-570 (the byte-identical duplicated block at 563-566)
    - src/phaze/routers/agent_files.py:128-138 (the ON-CONFLICT `set_` block + its comment at 131-135)
    - .planning/phases/92-milestone-close-tech-debt-cleanup/92-PATTERNS.md (CLEAN-03 sections — exact locations + anti-drift note)
  </read_first>
  <action>
    D-09: In `backends.py`, lines 563-564 and 565-566 are byte-identical ("MKUE-01/D-04: thread THIS backend's KubeConfig so every get_job/get_workload_for/ delete_job inside reconcile targets the file's own cluster."). Delete ONE copy (drop the second occurrence at 565-566), keeping a single copy immediately above the `await _reconcile_one(...)` call inside `KueueBackend.reconcile`. No code line changes.

    D-10: In `agent_files.py`, the comment block inside the `on_conflict_do_update(... set_={...})` is stale — it describes the Phase-90-dropped `files.state` column ("NEVER overwrite `state` on conflict", "regress its pipeline progress to DISCOVERED", "New-file INSERT still stamps state=DISCOVERED via ... data['state']"). Read the CURRENT `set_` dict (it updates sha256_hash/file_size/batch_id/file_type and does NOT contain a `state` key). Reword the comment to accurately describe what the ON-CONFLICT block does today: an agent rescan of an existing file refreshes hash/size/batch/file_type and never touches identity; remove all references to the removed `files.state` / DISCOVERED-stamp behavior. Keep the AUTH-01 note if still accurate. Comment-only; do not change the `set_` dict.

    Do NOT touch any other lines. This is behavior-preserving.
  </action>
  <verify>
    <automated>test $(grep -c "thread THIS backend's KubeConfig" src/phaze/services/backends.py) -eq 1 && ! grep -q "regress its pipeline progress to DISCOVERED" src/phaze/routers/agent_files.py && uv run ruff check src/phaze/services/backends.py src/phaze/routers/agent_files.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "thread THIS backend's KubeConfig" src/phaze/services/backends.py` returns exactly `1`.
    - `grep "regress its pipeline progress to DISCOVERED" src/phaze/routers/agent_files.py` returns no match; no remaining reference to a `files.state` column in that comment block.
    - `git diff --stat` shows only comment-line changes (no changes to `set_=` keys, no logic lines).
    - `uv run ruff check src/phaze/services/backends.py src/phaze/routers/agent_files.py` exits 0.
    - Any existing anti-drift / partition guard (`uv run pytest tests/shared/test_partition_guard.py`) still passes green.
  </acceptance_criteria>
  <done>Duplicated backends.py comment reduced to one copy; agent_files.py comment describes current schema; ruff clean; zero logic diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Comment-only change; no code path, input, or data flow crosses any boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01-CMT | Tampering | source comments | accept | Comment-only edit, byte-verifiable via git diff; anti-drift guards re-run |
| T-92-01-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in Phase 92 (RESEARCH §Standard Stack) — no supply-chain surface |
</threat_model>

<verification>
- `git diff` confirms only comment lines changed in both files.
- `uv run ruff check .` and `uv run ruff format --check .` on the two files pass.
- No test regressions attributable to this plan (behavior-preserving).
</verification>

<success_criteria>
- backends.py contains exactly one copy of the KubeConfig comment.
- agent_files.py comment no longer references the dropped `files.state` / DISCOVERED-stamp behavior.
- Zero runtime/logic change.
</success_criteria>

<output>
Create `.planning/phases/92-milestone-close-tech-debt-cleanup/92-01-SUMMARY.md` when done.
</output>
