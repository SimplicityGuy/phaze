---
phase: 92-milestone-close-tech-debt-cleanup
plan: 05
type: execute
wave: 4
depends_on: [92-01, 92-02, 92-04]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/92-milestone-close-tech-debt-cleanup/92-VERIFICATION.md
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03]
must_haves:
  truths:
    - "CLEAN-01/CLEAN-02/CLEAN-03 appear in .planning/REQUIREMENTS.md (both a per-requirement checkbox and a Traceability row mapped to Phase 92), so the DOCS-01 traceability guard has real rows to gate instead of a blind spot"
    - "The full ~1750-test suite passes green under per-bucket CI isolation for EVERY bucket in tests/buckets.json (D-08)"
    - "No committed seed row survives between tests (no pk_agents collision) under isolation"
    - "The D-08 acceptance gate result is recorded in 92-VERIFICATION.md"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "CLEAN-01/02/03 checkboxes + Traceability rows (Phase 92) — closes the milestone-close bookkeeping gap"
      contains: "CLEAN-01"
    - path: ".planning/phases/92-milestone-close-tech-debt-cleanup/92-VERIFICATION.md"
      provides: "Recorded per-bucket green results for all 9 buckets (D-08 gate)"
      contains: "test-bucket"
  key_links: []
---

<objective>
CLEAN-02 acceptance gate (D-08) + milestone-close bookkeeping: (1) register CLEAN-01/02/03 in `.planning/REQUIREMENTS.md` so the traceability guard is no longer blind to them; (2) the phase does NOT land until the full ~1750-test suite passes green under per-bucket CI isolation for EVERY bucket in `tests/buckets.json`. This is the blast-radius verification that the create_savepoint rewrite + call-site migration + production-fan-out routing + CLEAN-01 parallelization + CLEAN-03 comment edits all coexist hermetically.

Purpose: Prove the whole suite is hermetic under the project isolation standard before milestone close, and close the requirements-traceability gap so the milestone audit is complete.
Output: CLEAN rows in `.planning/REQUIREMENTS.md` + recorded per-bucket results in `92-VERIFICATION.md`.

DEPENDS ON 92-01 (CLEAN-03), 92-02 (CLEAN-01), 92-04 (CLEAN-02 migration). This runs LAST so CLEAN-02's blast-radius verification does not mask CLEAN-01/03 regressions (they were verified independently in wave 1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/92-milestone-close-tech-debt-cleanup/92-VALIDATION.md
@.planning/phases/92-milestone-close-tech-debt-cleanup/92-03-SUMMARY.md
@.planning/phases/92-milestone-close-tech-debt-cleanup/92-04-SUMMARY.md

<interfaces>
Buckets (tests/buckets.json): discovery, metadata, fingerprint, analyze, identify, review, agents, integration, shared.
Traceability guard: `tests/shared/core/test_requirements_traceability.py` (DOCS-01, Phase 66). It cross-checks, for the ACTIVE `.planning/REQUIREMENTS.md`, the intersection of per-requirement checkboxes (`- [ ] **ID**` above `## Traceability`) and Traceability table rows (`| ID | Phase N | Status |`). In-flight tolerance (D-05): a not-yet-passed phase whose mapped requirements are `[ ]` + `Pending` PASSES — so at plan time CLEAN-01/02/03 rows must be `- [ ]` + `Pending`. NOTE: the archived Phase-66 milestone reuses IDs `CLEAN-01/02`; those live only in `.planning/milestones/*-REQUIREMENTS.md` (consistency-checked, not cross-linked to the active file) — adding CLEAN-01/02/03 to the ACTIVE REQUIREMENTS.md does not collide.
Port footgun (RESEARCH Pitfall 5 / MEMORY reference_migrations_test_db_port): TEST_DATABASE_URL defaults 5432/phaze_test; `just test-db` provisions the ephemeral integration Postgres on 5433; MIGRATIONS_TEST_DATABASE_URL defaults 5432. Export the correct DB URLs/ports before running buckets in isolation so a port error is not misread as a hermeticity failure.
Colima note (RESEARCH Pitfall 6): per-bucket isolation is the project standard and sidesteps whole-suite VM pressure; do not conflate a VM-pressure connection flake with a hermeticity failure.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register CLEAN-01/02/03 in REQUIREMENTS.md traceability (close the DOCS-01 blind spot)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the v1 Requirements checkbox list + the `## Traceability` table + the Coverage note)
    - tests/shared/core/test_requirements_traceability.py:1-120 (the guard's checkbox↔table cross-check + in-flight tolerance D-05 + status-vocabulary normalization)
    - .planning/ROADMAP.md § Phase 92 (the CLEAN-01/02/03 descriptions to mirror)
  </read_first>
  <action>
    The milestone audit surfaced CLEAN-01/02/03, but they live ONLY in ROADMAP § Phase 92 — absent from `.planning/REQUIREMENTS.md`. The DOCS-01 guard only iterates rows that EXIST, so wholly-absent IDs are a blind spot. Close it (this is the milestone-close phase):

    1. Add a new `### Milestone-Close Tech Debt (CLEAN)` subsection to the v1 Requirements checkbox list (BEFORE the `## Traceability` heading), with three `- [ ]` (unchecked — the phase is in-flight, not yet passed) requirement checkboxes:
       - `- [ ] **CLEAN-01**: PERF-02 follow-up — parallelize the independent reads in get_stage_progress via asyncio.gather and re-measure /pipeline/stats poll latency at 200K scale (drives the DENORM-01 revisit).`
       - `- [ ] **CLEAN-02**: The test suite is hermetic under per-bucket CI isolation — the 83-01/83-03 flake class fixed at its shared conftest root (session-scoped engine + create_savepoint rollback).`
       - `- [ ] **CLEAN-03**: Doc hygiene — remove the duplicated backends.py KubeConfig comment and the stale agent_files.py DISCOVERED-stamp comment (zero runtime change).`
    2. Add three rows to the `## Traceability` table, mapped to Phase 92, Status `Pending` (the guard's in-flight tolerance requires `[ ]` + `Pending` while the phase is unpassed):
       - `| CLEAN-01 | Phase 92 | Pending |`
       - `| CLEAN-02 | Phase 92 | Pending |`
       - `| CLEAN-03 | Phase 92 | Pending |`
    3. Update the Coverage note prose to reflect the three added milestone-close requirements (do NOT silently leave the "42 total" line contradicting the new rows — either add a one-line "+3 milestone-close tech-debt (CLEAN-01..03)" annotation or recompute; keep the guard's checkbox↔table consistency intact).

    Use `- [ ]` (unchecked) and `Pending` deliberately — marking them Complete while Phase 92 is not yet `status: passed` would TRIP the guard's bidirectional check. They flip to Complete when the phase verification passes (downstream, not here).
  </action>
  <verify>
    <automated>grep -q "CLEAN-01" .planning/REQUIREMENTS.md && grep -q "| CLEAN-02 | Phase 92 |" .planning/REQUIREMENTS.md && uv run pytest tests/shared/core/test_requirements_traceability.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` has `- [ ] **CLEAN-01**` / `**CLEAN-02**` / `**CLEAN-03**` checkboxes in the requirements list (above `## Traceability`) AND three `| CLEAN-0X | Phase 92 | Pending |` Traceability rows.
    - The rows are `Pending` + unchecked (in-flight tolerance) — NOT Complete.
    - `uv run pytest tests/shared/core/test_requirements_traceability.py -q` exits 0 (checkbox↔table consistent; no drift).
    - The Coverage note no longer contradicts the added rows.
  </acceptance_criteria>
  <done>CLEAN-01/02/03 registered in REQUIREMENTS.md (checkboxes + Phase-92 Traceability rows, Pending); DOCS-01 guard green.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Full-suite green under per-bucket isolation for all 9 buckets (D-08)</name>
  <files>.planning/phases/92-milestone-close-tech-debt-cleanup/92-VERIFICATION.md</files>
  <read_first>
    - tests/buckets.json (the 9 bucket names)
    - .planning/phases/92-milestone-close-tech-debt-cleanup/92-RESEARCH.md (Pitfall 5 port footgun, Pitfall 6 colima)
    - MEMORY reference_migrations_test_db_port, reference_ci_bucket_isolation
  </read_first>
  <action>
    Run EVERY bucket in `tests/buckets.json` in isolation and prove the whole suite is hermetic (D-08 acceptance gate for CLEAN-02). This is the blocking gate — nothing in the phase lands until it is green.

    1. Export the correct DB URLs/ports to avoid the 5432/5433 footgun: set TEST_DATABASE_URL / MIGRATIONS_TEST_DATABASE_URL to match the running Postgres port used by `just test-db`. Record the exact env used.
    2. Run each bucket cold, in isolation: `just test-bucket discovery`, `... metadata`, `... fingerprint`, `... analyze`, `... identify`, `... review`, `... agents`, `... integration`, `... shared`. Each must exit 0.
    3. If a bucket is red, distinguish the failure class:
       - **Surviving seed row / `pk_agents` collision** → a real hermeticity leak (missed verify-session site or fixture-wiring gap); BLOCKS. Fix in the owning plan's file.
       - **A seed-then-`get_stage_progress` / `GET /pipeline/stats` read returning ZERO/STALE counts** (e.g. an awaiting-cloud card showing 0 where the test seeded 3) → the PRODUCTION fan-out routing from **92-03 Task 2** is not being applied on that test path (NOT a verify-session migration miss). Fix the conftest `phaze.database.async_session` monkeypatch / `_STATS_FANOUT` serialization scoping, not a `verify`-fixture rebind.
       - **A colima VM-pressure connection flake** (RESEARCH Pitfall 6) → re-run the failed subset in isolation; only a real leak/zero-read blocks. Do NOT use `pytest --lf`/watch-mode flags; each bucket runs clean from cold.
    4. Record every bucket's result + the exact DB URL/port env in `92-VERIFICATION.md` under a "CLEAN-02 D-08 per-bucket gate" section, and note which failure-class distinctions (if any) were exercised.
  </action>
  <verify>
    <automated>for b in discovery metadata fingerprint analyze identify review agents integration shared; do echo "== $b =="; just test-bucket "$b" || exit 1; done && grep -q "CLEAN-02 D-08 per-bucket gate" .planning/phases/92-milestone-close-tech-debt-cleanup/92-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - All 9 buckets in `tests/buckets.json` exit 0 via `just test-bucket <name>` run in isolation.
    - `92-VERIFICATION.md` contains a "CLEAN-02 D-08 per-bucket gate" section listing every bucket's result + the DB URL/port env used.
    - No hermeticity failure (no `pk_agents` collision / surviving-seed-row) and no zero/stale fan-out read in any bucket; any red was confirmed an unrelated VM-pressure flake and re-run green.
    - No `--lf`/watch-mode flags used.
  </acceptance_criteria>
  <done>All 9 buckets green under per-bucket isolation from cold; results + env recorded in 92-VERIFICATION.md; D-08 gate satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| full test suite ↔ shared test DB | Verification-only; the gate confirms no cross-test state leak across the whole suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-05-GATE | Tampering (undetected leak) | per-bucket isolation gate | mitigate | All 9 buckets run cold in isolation; port footgun + colima flake + zero/stale-fan-out-read explicitly distinguished so a real hermeticity leak cannot hide behind an env error |
| T-92-05-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in Phase 92 (RESEARCH §Standard Stack) — no supply-chain surface |
</threat_model>

<verification>
- CLEAN-01/02/03 registered in REQUIREMENTS.md; DOCS-01 traceability guard green.
- Every bucket green via `just test-bucket <name>` in isolation; results recorded in 92-VERIFICATION.md.
- No `pk_agents` collision / surviving-seed-row and no zero/stale fan-out read in any bucket.
</verification>

<success_criteria>
- CLEAN-01/02/03 have real REQUIREMENTS.md traceability rows (no DOCS-01 blind spot).
- The full ~1750-test suite is hermetic under per-bucket CI isolation (D-08 acceptance gate for CLEAN-02).
</success_criteria>

<output>
Create `.planning/phases/92-milestone-close-tech-debt-cleanup/92-05-SUMMARY.md` when done.
</output>
