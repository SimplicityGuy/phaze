---
phase: 85-executed-gate-revival
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/services/stage_status.py
  - tests/shared/test_applied_clause.py
  - src/phaze/templates/proposals/partials/proposal_row.html
  - tests/review/routers/test_proposals.py
autonomous: true
requirements: [READ-05]

must_haves:
  truths:
    - "READ-05/D-01: `applied_clause()` returns EXISTS(proposals WHERE file_id=files.id AND status='executed') — true iff an executed proposal exists, never reads FileRecord.state, never execution_log"
    - "D-01: `is_applied(session, file_id)` returns True for a file with an executed proposal and False for failed/approved/pending/no-proposal, even when file.state != 'executed'"
    - "D-01: a file with BOTH a failed and an executed proposal is applied (True) — multi-proposal semantics"
    - "D-04: the proposal_row badge derives 'Executed' from `proposal.status == 'executed'`, not `proposal.file.state` — no `file.state` reader survives in the template"
  artifacts:
    - path: "src/phaze/services/stage_status.py"
      provides: "applied_clause() ColumnElement + async is_applied() per-record helper"
      contains: "def applied_clause"
    - path: "tests/shared/test_applied_clause.py"
      provides: "SC#1 unit contract for the predicate pair"
      contains: "is_applied"
    - path: "src/phaze/templates/proposals/partials/proposal_row.html"
      provides: "badge derived from proposal.status"
      contains: "proposal.status == \"executed\""
  key_links:
    - from: "src/phaze/services/stage_status.py::applied_clause"
      to: "models.proposal.RenameProposal.status == 'executed'"
      via: "correlated exists over proposals.file_id == FileRecord.id"
      pattern: "RenameProposal.status == \"executed\""
---

<objective>
Establish the single-source `applied()` predicate pair (D-01) that every dead `state == EXECUTED`
gate will be swapped onto, and close the last stray `file.state` reader in the UI badge (D-04). This
is the interface-defining foundation plan: Plans 02–04 consume `applied_clause()` / `is_applied()`
from this plan.

Purpose: Revive READ-05's dead gates against the real apply-outcome source (`proposals.status ==
'executed'`) instead of a `FileState.EXECUTED` value that no writer in `src/` ever produces.
Output: `applied_clause()` + `is_applied()` in `services/stage_status.py`, a shared-bucket unit
contract, and the corrected proposal-row badge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-executed-gate-revival/85-CONTEXT.md
@.planning/phases/85-executed-gate-revival/85-RESEARCH.md
@.planning/phases/85-executed-gate-revival/85-PATTERNS.md

<interfaces>
<!-- Extracted from src/phaze/services/stage_status.py — executor uses these directly, no exploration. -->
Existing template (services/stage_status.py:91-112) — the EXACT sibling to mirror:
  def dedup_resolved_clause() -> ColumnElement[bool]:
      return exists(select(DedupResolution.id).where(DedupResolution.file_id == FileRecord.id))

Already-imported building blocks in stage_status.py (nothing new to import except `uuid`):
  from sqlalchemy import ColumnElement, ..., exists, ..., select, ...   (line 62)
  from phaze.models.file import FileRecord                              (line 70)
  from phaze.models.proposal import RenameProposal                      (line 73 — already imported)
  if TYPE_CHECKING: from sqlalchemy.ext.asyncio import AsyncSession     (line 80)
  # NOTE: `import uuid` is NOT present — add it (module-level) for the file_id annotation.

ProposalStatus.EXECUTED.value == "executed"  (models/proposal.py:20) — use the string literal
"executed" to match how done_clause/failed_clause/shadow_compare spell status comparisons.

proposals.file_id is a plain FK; uq_proposals_file_id_pending enforces one PENDING proposal per file
ONLY (models/proposal.py:53-59). A file CAN have multiple non-pending proposals — exists(status==
'executed') is the correct authoritative test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add applied_clause() + is_applied() to services/stage_status.py</name>
  <files>src/phaze/services/stage_status.py</files>
  <read_first>
    - src/phaze/services/stage_status.py (lines 58-112 imports + dedup_resolved_clause template; lines 231-264 awaiting_candidate_clause — the other no-Stage sibling that is deliberately kept OUT of the Stage ladders)
    - src/phaze/models/proposal.py (ProposalStatus enum, uq_proposals_file_id_pending partial-unique index)
    - .planning/phases/85-executed-gate-revival/85-PATTERNS.md (section "services/stage_status.py — add applied_clause() + is_applied()")
  </read_first>
  <behavior>
    - applied_clause() returns a correlated exists over proposals where file_id == FileRecord.id AND status == "executed"; takes NO Stage arg; never references FileRecord.state; never references ExecutionLog.
    - is_applied(session, file_id) → True when an executed proposal exists for file_id, else False; issues a single scalar EXISTS query; never lazy-loads proposal.file (lazy="raise").
  </behavior>
  <action>
    Add module-level `import uuid` (D-01 helper needs `uuid.UUID` for the `file_id` annotation; verify
    it is not already imported before adding). Drop `applied_clause() -> ColumnElement[bool]`
    immediately beneath `dedup_resolved_clause()` (line 112), templated on it: `exists(select(
    RenameProposal.id).where(RenameProposal.file_id == FileRecord.id, RenameProposal.status ==
    "executed"))`. Keep it a standalone non-`Stage` function — do NOT add it to `done_clause`/
    `failed_clause`/`stage_status_case` or any Stage dispatch ladder (RESEARCH anti-pattern: it must
    not perturb the DERIV-04 equivalence test `tests/integration/test_stage_status_equivalence.py`).
    Add the per-record twin `async def is_applied(session: AsyncSession, file_id: uuid.UUID) -> bool`
    returning `bool(await session.scalar(select(exists(select(RenameProposal.id).where(
    RenameProposal.file_id == file_id, RenameProposal.status == "executed")))))`. Docstrings must state
    "READ-05 / D-01", "NEVER reads FileRecord.state", "NEVER execution_log". Do NOT reuse
    `done_clause(Stage.APPLY)` (it joins execution_log — rejected by D-01).
  </action>
  <verify>
    <automated>uv run ruff check src/phaze/services/stage_status.py && uv run mypy src/phaze/services/stage_status.py && grep -q "def applied_clause" src/phaze/services/stage_status.py && grep -q "async def is_applied" src/phaze/services/stage_status.py</automated>
  </verify>
  <acceptance_criteria>
    - `services/stage_status.py` contains `def applied_clause` and `async def is_applied`.
    - `grep -n "RenameProposal.status == \"executed\"" src/phaze/services/stage_status.py` matches inside both new functions.
    - `grep -c "FileRecord.state" src/phaze/services/stage_status.py` shows applied_clause/is_applied add ZERO `FileRecord.state` references (no new occurrences in the two functions).
    - `applied_clause` is NOT referenced inside `done_clause`, `failed_clause`, or `stage_status_case`.
    - `uv run mypy src/phaze/services/stage_status.py` exits 0; `uv run ruff check` exits 0 (no F401).
  </acceptance_criteria>
  <done>Both helpers exist, mypy/ruff clean, predicate expressed purely over proposals, kept out of the Stage ladders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SC#1 unit contract for the predicate pair (tests/shared)</name>
  <files>tests/shared/test_applied_clause.py</files>
  <read_first>
    - tests/shared/test_awaiting_candidate_clause.py (exact sibling: no-Stage clause unit test in the shared bucket)
    - tests/shared/test_stage_resolver.py (clause-shape / DB-backed contract idiom)
    - src/phaze/services/stage_status.py (the just-added applied_clause/is_applied)
    - .planning/phases/85-executed-gate-revival/85-VALIDATION.md (SC#1 row + Wave 0 requirements)
  </read_first>
  <behavior>
    - executed proposal → applied_clause() selects the file; is_applied → True.
    - failed / approved / pending / no proposal → not selected; is_applied → False.
    - file with a failed AND an executed proposal for the same file → applied (True).
    - CRITICAL: seed the file with `state='moved'` (NOT 'executed') plus an executed proposal → still applied. This asserts the predicate never reads FileRecord.state (SC#1 / the whole point of the phase).
  </behavior>
  <action>
    Create `tests/shared/test_applied_clause.py` in the `shared` bucket (stage_status is shared). Seed a
    real `FileRecord` (with `state` deliberately set to a non-'executed' value such as `moved`) and
    `RenameProposal` rows covering: executed→True, failed→False, approved→False, pending→False,
    no-proposal→False, and multi-proposal (failed + executed for the same file)→True. Exercise BOTH forms:
    a `select(FileRecord.id).where(applied_clause())` query and `await is_applied(session, file_id)`.
    Include the load-bearing "never reads file.state" case (file.state != 'executed' but an executed
    proposal exists ⇒ applied). Match the DB-fixture/session idiom of `test_awaiting_candidate_clause.py`.
    Respect one-bucket-per-file (partition guard) — file lives entirely in `shared`.
  </action>
  <verify>
    <automated>just test-bucket shared</automated>
  </verify>
  <acceptance_criteria>
    - `tests/shared/test_applied_clause.py` exists and asserts executed→True, failed/approved/pending/none→False, multi-proposal→True.
    - A named case seeds `state='moved'` + executed proposal and asserts applied ⇒ True (proves file.state is never read).
    - `just test-bucket shared` exits 0 in isolation.
    - `uv run pytest tests/shared/test_partition_guard.py -x` passes (one-bucket-per-file honored).
  </acceptance_criteria>
  <done>Predicate contract green in the shared bucket in isolation; the file.state-independence case passes.</done>
</task>

<task type="auto">
  <name>Task 3: Derive the proposal-row badge from proposal.status (D-04)</name>
  <files>src/phaze/templates/proposals/partials/proposal_row.html, tests/review/routers/test_proposals.py</files>
  <read_first>
    - src/phaze/templates/proposals/partials/proposal_row.html (lines 44-53: the dead `proposal.file.state == "executed"` branch at :46 vs the sibling `proposal.status`-reading branches at :48+)
    - tests/review/routers/test_proposals.py (existing proposal-row render assertions — analog for the badge test)
    - .planning/phases/85-executed-gate-revival/85-PATTERNS.md (section "templates/.../proposal_row.html — badge")
  </read_first>
  <action>
    Change line 46 from `{% if proposal.file.state == "executed" %}` to
    `{% if proposal.status == "executed" %}` — mirroring the sibling `{% elif proposal.status == ... %}`
    branches already in the same chain (lines 48+). No template context change is needed
    (`proposal.status` is already in scope). This removes the last stray `proposal.file.state` reader so
    Phase 90 (drop `files.state`) does not trip over it (Pitfall 4). Badge label wording
    ("Executed"/"Applied") is cosmetic — leave as-is or use "Applied". Add/extend a case in
    `tests/review/routers/test_proposals.py` asserting a proposal with `status == 'executed'` renders the
    badge and that the rendered row does NOT reference `proposal.file.state`.
  </action>
  <verify>
    <automated>grep -q 'proposal.status == "executed"' src/phaze/templates/proposals/partials/proposal_row.html && ! grep -q 'proposal.file.state' src/phaze/templates/proposals/partials/proposal_row.html && just test-bucket review</automated>
  </verify>
  <acceptance_criteria>
    - `proposal_row.html` contains `proposal.status == "executed"` at the badge branch.
    - `grep -c "proposal.file.state" src/phaze/templates/proposals/partials/proposal_row.html` == 0.
    - A test in `tests/review/routers/test_proposals.py` asserts the executed badge renders from `proposal.status`.
    - `just test-bucket review` exits 0 in isolation.
  </acceptance_criteria>
  <done>Badge derives from proposal.status; zero file.state readers remain in the template; review bucket green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → control-plane PATCH /proposals/{id}/state | authenticated agent sets proposals.status; the write source for applied() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-01 | Spoofing/Tampering | applied() predicate reachable by a proposals.status='executed' write | accept | Reaching 'executed' requires the apply path's copy→verify→delete (step 2) to succeed BEFORE the proposal PATCH (step 4); an IO failure forces status='failed' (RESEARCH §Central Tension). The PATCH route is `Depends(get_authenticated_agent)` (V4 access control, unchanged). Spoofing requires a compromised authenticated agent — out of scope for a single-operator private-network tool. No new surface added by reading the column. |
| T-85-02 | Tampering/Integrity | choice of predicate source | mitigate | Use `proposals.status` (transactionally coupled to `current_path`) NOT `execution_log` (best-effort, swallowed-exception audit log that can be a false-positive) — closes the "tag write to a stale/deleted path" hazard at the predicate level. |
| T-85-SC | Tampering | package installs | N/A | Zero new packages in this phase (milestone constraint). No install task. |
</threat_model>

<verification>
- `uv run ruff check .` and `uv run mypy .` clean for touched files.
- `just test-bucket shared` and `just test-bucket review` green in isolation.
- `grep -rn "proposal.file.state" src/phaze/templates/` returns nothing.
</verification>

<success_criteria>
- `applied_clause()` + `is_applied()` exist in `services/stage_status.py`, expressed purely over `proposals`, never reading `FileRecord.state` or `execution_log`, and kept out of the Stage ladders.
- SC#1 unit contract green in the shared bucket, including the file.state-independence case.
- D-04 badge derives from `proposal.status`; no `file.state` reader survives the template.
</success_criteria>

<output>
Create `.planning/phases/85-executed-gate-revival/85-01-SUMMARY.md` when done.
</output>
