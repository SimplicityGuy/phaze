---
phase: 90-destructive-migration-writer-removal
plan: 02
type: execute
wave: 2
depends_on: [90-01]
files_modified:
  - src/phaze/routers/agent_files.py
  - src/phaze/routers/agent_metadata.py
  - src/phaze/routers/agent_analysis.py
  - src/phaze/routers/agent_push.py
  - src/phaze/routers/agent_s3.py
  - src/phaze/routers/pipeline.py
  - src/phaze/services/backends.py
  - src/phaze/services/dedup.py
  - tests/metadata/routers/test_agent_metadata.py
  - tests/agents/routers/test_agent_s3.py
autonomous: true
requirements: [MIG-04]

must_haves:
  truths:
    - "D-09: all ~17 FileRecord.state writers are removed AFTER PR-A cut over every reader, so they are now truly unconsumed — behavior is preserved because the derived sources are already the sole live authority."
    - "The two CAS-guard writers (agent_metadata:106, agent_s3:128) have their embedded .where(state==...) READ removed together with the .values(state=...) WRITE — the read+write deletion stays ATOMIC in PR-B (D-09 CAS-guard clarification), NOT split into PR-A."
    - "D-09 CAS-guard clarification: removing the two CAS .where(state==...) guards preserves idempotency, PROVEN by named runnable regression tests (not a threat-model claim) — each test calls the affected callback/endpoint TWICE and asserts no duplicate or incorrect effect, because the marker / ON CONFLICT path already supplies idempotency."
    - "No src/ code writes files.state after this PR (attribute-assign, .values(state=...), and INSERT-stamp forms all gone); FileState class still exists (deleted in PR-C) so mypy stays green."
    - "SQL⇔Python derived equivalence (test_stage_status_equivalence.py) stays green; the dedup marker DELETE remains the undo authority now that the previous_state restore writer is gone."
  artifacts:
    - path: "src/phaze/services/backends.py"
      provides: "AWAITING_CLOUD/LOCAL_ANALYZING/PUSHING dual-writes removed"
    - path: "src/phaze/services/dedup.py"
      provides: "DUPLICATE_RESOLVED writer + undo-restore writer removed; marker path intact"
    - path: "tests/metadata/routers/test_agent_metadata.py"
      provides: "test_metadata_callback_idempotent_after_cas_removal — double-call idempotency proof for the DISCOVERED→METADATA_EXTRACTED CAS removal"
    - path: "tests/agents/routers/test_agent_s3.py"
      provides: "test_s3_push_status_transition_idempotent_after_cas_removal — double-call idempotency proof for the PUSHING→PUSHED CAS removal"
  key_links:
    - from: "PR-A derived readers"
      to: "output/marker tables + cloud_job"
      via: "already the sole authority — writers now safe to delete"
      pattern: "failed_clause|awaiting_candidate_clause|dedup_resolved_clause"
---

<objective>
PR-B of the readers-first D-09 sequence: remove ALL ~17 `FileRecord.state` writers now that PR-A made every
reader derived. Pure deletion of dual-writes (each annotated "dies in Phase 90"), plus removal of the CAS-guard
`state` READs embedded in two of them and any now-unused `FileState` imports / dead branches. The `FileState`
class and the `files.state` column stay intact (deleted in PR-C), so mypy/ruff stay green throughout.

Per the D-09 "CAS-guard reads clarification" (CONTEXT.md, 2026-07-12): the two CAS `.where(state==...)` guards are
NOT standalone reader surfaces — each is the guard of a single `update(...).values(state=...)` write, so read+write
are deleted ATOMICALLY here in PR-B (never split to PR-A). Because that removal drops an idempotency guard, PR-B
MUST prove idempotency is preserved with a concrete, named, runnable regression test (double-call → no duplicate /
incorrect effect), not merely a threat-model assertion.

Purpose: with readers derived (PR-A), these writers are unconsumed; deleting them makes the column truly dead
so PR-C can drop it. Independently shippable and green.
Output: 7 src files with every `state=` writer removed; two named idempotency regression tests; equivalence + full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-destructive-migration-writer-removal/90-CONTEXT.md
@.planning/phases/90-destructive-migration-writer-removal/90-RESEARCH.md
@.planning/phases/90-destructive-migration-writer-removal/90-PATTERNS.md
@.planning/phases/90-destructive-migration-writer-removal/90-01-SUMMARY.md

<writer_inventory>
<!-- Verified 2026-07-12 tree (RESEARCH Pitfall 1 / PATTERNS "~17 writer sites"). Each is a pure dual-write. -->
1  routers/agent_files.py:111     data["state"]=DISCOVERED            (bulk-upsert INSERT stamp)
2  routers/agent_metadata.py:106  update(...).where(state==DISCOVERED).values(state=METADATA_EXTRACTED)  ← CAS embeds a READ
3  routers/agent_analysis.py:247  .values(state=ANALYZED)
4  routers/agent_analysis.py:382  .values(state=ANALYSIS_FAILED)      (comment at :380 "dies in Phase 90")
5  routers/pipeline.py:999        file.state=DISCOVERED
6  routers/pipeline.py:1129       f.state=FINGERPRINTED
7  routers/pipeline.py:1273       file.state=FINGERPRINTED
8  routers/agent_push.py:151      .values(state=PUSHED)
9  routers/agent_push.py:306      .values(state=AWAITING_CLOUD)       (spill)
10 routers/agent_s3.py:128        update(...).where(state==PUSHING).values(state=PUSHED)  ← CAS embeds a READ
11 routers/agent_s3.py:232        .values(state=AWAITING_CLOUD)       (spill)
12 services/backends.py:124       AWAITING_CLOUD
13 services/backends.py:304       LOCAL_ANALYZING
14 services/backends.py:395       PUSHING (compute)
15 services/backends.py:508       PUSHING (kueue)
16 services/dedup.py:274          f.state=DUPLICATE_RESOLVED
17 services/dedup.py:346          .values(state=restore_by_id[...])   (undo restore)
</writer_inventory>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the router-side writers (8 sites incl. both CAS guards) + prove CAS-removal idempotency</name>
  <files>src/phaze/routers/agent_files.py, src/phaze/routers/agent_metadata.py, src/phaze/routers/agent_analysis.py, src/phaze/routers/agent_push.py, src/phaze/routers/agent_s3.py, src/phaze/routers/pipeline.py, tests/metadata/routers/test_agent_metadata.py, tests/agents/routers/test_agent_s3.py</files>
  <read_first>
    - src/phaze/routers/agent_files.py (:111 INSERT stamp)
    - src/phaze/routers/agent_metadata.py (:106 CAS — .where(state==DISCOVERED).values(state=METADATA_EXTRACTED))
    - src/phaze/routers/agent_analysis.py (:247 ANALYZED, :380-:382 ANALYSIS_FAILED)
    - src/phaze/routers/agent_push.py (:151 PUSHED, :306 AWAITING_CLOUD)
    - src/phaze/routers/agent_s3.py (:128 CAS — .where(state==PUSHING).values(state=PUSHED), :232 AWAITING_CLOUD)
    - src/phaze/routers/pipeline.py (:999 DISCOVERED, :1129 FINGERPRINTED, :1273 FINGERPRINTED)
    - tests/metadata/routers/test_agent_metadata.py (existing metadata-callback test fixtures/shape — extend, don't replace)
    - tests/agents/routers/test_agent_s3.py (existing s3 push-status test fixtures/shape — extend, don't replace)
    - CONTEXT.md D-09 "CAS-guard reads clarification" (2026-07-12) — atomic read+write removal in PR-B + mandatory idempotency test
    - PATTERNS "~17 writer sites — PURE DELETION, PR-B" (Note #2/#10 CAS caveat)
  </read_first>
  <action>
    Delete writer sites 1-11 above. For the two attribute-stamp / plain-assign sites (agent_files.py:111 INSERT data["state"], pipeline.py:999/:1129/:1273) remove the state key/assignment; the surrounding upsert/route logic and any AUTH-01 / rescan-preserve behavior (memory 77-01) stays. For the `.values(state=...)` UPDATE sites (agent_analysis.py:247/:382, agent_push.py:151/:306, agent_s3.py:232) drop the `state=` key from the `.values(...)`; if that leaves an empty `.update(...).values()` with no other column, remove the whole statement (confirm no other column is written). For the two CAS writers (agent_metadata.py:106, agent_s3.py:128) the `.where(FileRecord.state == ...)` guard embeds a READ — the derived path is already authority (PR-A), so remove the ENTIRE CAS statement (both the `.where(state==...)` READ and the `.values(state=...)` WRITE) ATOMICALLY, keeping the deletion in PR-B (do NOT split the where-clause into PR-A). Remove any `FileState` import that becomes unused in each touched router (ruff F401 will flag leftovers).

    Then PROVE the CAS removal preserved idempotency with two NAMED regression tests (D-09 CAS-guard clarification — a threat-model claim is insufficient):
    - In tests/metadata/routers/test_agent_metadata.py add `test_metadata_callback_idempotent_after_cas_removal`: seed a file, invoke the metadata-extraction callback/endpoint TWICE with the same payload, and assert the SECOND call produces no duplicate or incorrect effect — exactly one metadata row (the marker / `ON CONFLICT` path is the idempotency authority now that the DISCOVERED→METADATA_EXTRACTED `.where` guard is gone), no error, and an unchanged final observable result. Assert from an INDEPENDENT session (memory: get_session never commits; conftest overrides get_session).
    - In tests/agents/routers/test_agent_s3.py add `test_s3_push_status_transition_idempotent_after_cas_removal`: drive the PUSHING→PUSHED push-status transition endpoint TWICE and assert the second call is a no-op / idempotent (final cloud_job status is the single expected terminal value, no duplicate side effect, no error), proving the removed PUSHING-guard CAS is not load-bearing for correctness.
    Both tests live in already-registered buckets (metadata, agents) so tests/buckets.json needs no change.
  </action>
  <verify>
    <automated>uv run pytest tests/metadata/routers/test_agent_metadata.py::test_metadata_callback_idempotent_after_cas_removal tests/agents/routers/test_agent_s3.py::test_s3_push_status_transition_idempotent_after_cas_removal -x && uv run pytest tests/metadata/routers/test_agent_metadata.py tests/agents/routers/test_agent_s3.py tests/agents/routers/test_agent_push.py -x && uv run ruff check src/phaze/routers</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "\.state\s*=|values\([^)]*state=|data\[.state.\]\s*=" src/phaze/routers/agent_files.py src/phaze/routers/agent_metadata.py src/phaze/routers/agent_analysis.py src/phaze/routers/agent_push.py src/phaze/routers/agent_s3.py` returns nothing.
    - `grep -c "FileRecord.state" src/phaze/routers/pipeline.py` (comment-stripped: `grep -v '^\s*#' src/phaze/routers/pipeline.py | grep -c "FileRecord.state"`) == 0 (writer sites 5/6/7 gone; PR-A already removed the readers).
    - `grep -rc "FileState" src/phaze/routers/agent_*.py` shows no unused imports (ruff clean).
    - CONCRETE idempotency proof (both must be RUNNABLE and GREEN): `uv run pytest tests/metadata/routers/test_agent_metadata.py::test_metadata_callback_idempotent_after_cas_removal -x` passes AND `uv run pytest tests/agents/routers/test_agent_s3.py::test_s3_push_status_transition_idempotent_after_cas_removal -x` passes — each calls the affected callback/endpoint TWICE and asserts no duplicate/incorrect effect. Bucket form: `just test-bucket metadata` and `just test-bucket agents` stay green.
    - Router bucket tests pass in isolation.
  </acceptance_criteria>
  <done>All 8 router writers (incl. both CAS reads, removed atomically in PR-B) removed; two named double-call idempotency regression tests GREEN; unused FileState imports gone; router tests + ruff green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the service-side writers (backends x4, dedup x2) + dead branches</name>
  <files>src/phaze/services/backends.py, src/phaze/services/dedup.py</files>
  <read_first>
    - src/phaze/services/backends.py (:124 AWAITING_CLOUD, :304 LOCAL_ANALYZING, :395 PUSHING compute, :508 PUSHING kueue)
    - src/phaze/services/dedup.py (:274 DUPLICATE_RESOLVED writer, :346 undo-restore writer; note :270 previous_state capture is STILL PRESENT entering PR-B — PR-A only decoupled undo_resolve's DELETE gate, it did NOT remove :270 — so remove :270 + :274 + :346 together here as a matched set; :76/:187/:296 comments "marker is authority")
    - PATTERNS "services/dedup.py previous_state capture/restore — PR-A/PR-B" + dedup_resolved_clause (stage_status.py:94)
  </read_first>
  <action>
    services/backends.py: delete the four `state = ...` / `.values(state=...)` dual-writes (:124 AWAITING_CLOUD, :304 LOCAL_ANALYZING, :395 + :508 PUSHING). These are pure mirrors of the cloud_job/marker authority the routing already writes; removing them leaves cloud_job as the sole source (PR-A readers already consume it). If a write becomes an empty `.values()`, drop the statement; keep all surrounding dispatch/routing logic. services/dedup.py: delete the DUPLICATE_RESOLVED writer (:274) and the undo-restore `.values(state=restore_by_id[...])` writer (:346); the DedupResolution marker create/DELETE (undo authority, :296 comment) stays untouched. Remove any now-unused `FileState` import and any dead branch that only existed to compute a state value (e.g. the `restore_by_id` mapping if it now feeds nothing). ruff/mypy must stay clean.
  </action>
  <verify>
    <automated>uv run pytest tests/shared -x -k "backends or dedup" && uv run pytest tests/integration/test_stage_status_equivalence.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "\.state\s*=|values\([^)]*state=" src/phaze/services/backends.py src/phaze/services/dedup.py` returns nothing.
    - `grep -c "FileState" src/phaze/services/backends.py` and `src/phaze/services/dedup.py` show no unused imports (ruff clean); dedup_resolved_clause / DedupResolution marker path unchanged.
    - test_stage_status_equivalence.py green; dedup + backends bucket tests pass; a dedup-undo test still round-trips via marker DELETE (no state restore).
  </acceptance_criteria>
  <done>All 6 service writers removed; dedup marker undo intact; equivalence + affected buckets green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent→/api/internal/agent/* & control-plane routing | writers formerly stamped files.state on these paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90B-01 | Tampering | removing a CAS writer (agent_metadata:106 / agent_s3:128) drops an idempotency guard | mitigate | The derived/marker + `ON CONFLICT` path already provides idempotency (PR-A). PROVEN, not asserted: named regression tests `test_metadata_callback_idempotent_after_cas_removal` and `test_s3_push_status_transition_idempotent_after_cas_removal` each invoke the endpoint TWICE and assert no duplicate/incorrect effect (`uv run pytest ...::<name> -x`). CAS read+write are deleted atomically in PR-B. |
| T-90B-02 | Tampering | removing a backends writer could strand a routing signal | mitigate | Only pure state mirrors are removed; cloud_job/marker (the real authority PR-A reads) is left as the sole source; equivalence test green. |
| T-90B-03 | Repudiation | dedup-undo loses its restore path | accept | The DedupResolution marker DELETE is the true undo authority (dedup.py:296); the state restore was redundant bookkeeping (D-05). |
| T-90B-SC | Tampering | npm/pip/cargo installs | N/A | Zero new dependencies; no package installs. |
</threat_model>

<verification>
- `uv run pytest` full suite green at the 90% floor (re-run failed subset in isolation on colima flake).
- `uv run pytest tests/metadata/routers/test_agent_metadata.py::test_metadata_callback_idempotent_after_cas_removal tests/agents/routers/test_agent_s3.py::test_s3_push_status_transition_idempotent_after_cas_removal -x` green (CAS-removal idempotency proof).
- `uv run pytest tests/integration/test_stage_status_equivalence.py` green.
- `uv run ruff check . && uv run mypy .` clean — no unused FileState imports remain in touched files.
- `FileState` class + `files.state` column still present (PR-C deletes them).
- `grep -rnE "\.values\([^)]*state=|FileRecord\.state\s*=|\.state\s*=\s*FileState" src/phaze` returns nothing (all writers gone).
</verification>

<success_criteria>
Every `FileState` writer is removed and the codebase writes `files.state` nowhere; the CAS reads are gone
(deleted atomically with their writes in PR-B); two named double-call regression tests PROVE the CAS removal
preserved idempotency; the derived sources remain the sole authority; equivalence + full suite green. PR-B is
independently shippable with the column and enum still present.
</success_criteria>

<output>
Create `.planning/phases/90-destructive-migration-writer-removal/90-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
