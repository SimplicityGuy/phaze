---
phase: 90-destructive-migration-writer-removal
plan: 04
type: execute
wave: 4
depends_on: [90-01, 90-02, 90-03]
files_modified:
  - src/phaze/services/shadow_compare.py
  - src/phaze/cli/shadow_compare.py
  - src/phaze/models/file.py
  - src/phaze/models/__init__.py
  - src/phaze/services/pipeline.py
  - src/phaze/config.py
  - tests/conftest.py
  - tests/shared/test_no_filestate_guard.py
  - tests/integration/test_migrations/test_migration_039_drop_files_state_column.py
  # PLUS the ~91 FileState-referencing test files (a FLOOR — enumerate with
  # `git grep -l FileState tests`) migrated to derived seeds, and the shadow-compare
  # subsystem tests retired/migrated (`git grep -l shadow_compare tests`).
autonomous: true
requirements: [MIG-04]

must_haves:
  truths:
    - "The FileState enum class (models/file.py:20-71), the `state` mapped column (:86), and Index('ix_files_state','state') (:97) are DELETED; models/__init__ no longer imports or re-exports FileState (:9,:36); the pipeline.py:21 FileState import + PIPELINE_STAGES list (:81-90) are removed. mypy + ruff + `import phaze` are the PRIMARY anti-drift guard (D-08) and are GREEN — no EXECUTABLE FileState reference survives in src/phaze (residual docstring prose is harmless and is what the guard's string-stripping is FOR)."
    - "The now-dead shadow_compare subsystem is FULLY removed with NO relocation (services/shadow_compare.py + cli/shadow_compare.py), verified safe because its couplings in pipeline.py/backends.py/stage_status.py are comment/docstring prose only and the dependency runs shadow_compare -> stage_status (not the reverse); its HARD invariants already live FROZEN inline in migration 039 (D-07). Its dedicated tests are retired (incl. the readonly source-guard that path-reads the deleted module); test_drain_double_dispatch is MIGRATED not deleted (ROADMAP double-dispatch hard gate, state-independent core kept); test_dedup_resolve_undo_shadow is diff-then-migrated-or-retired (never a silent coverage drop)."
    - "The ~91 dependent test files are migrated off FileState/`state=` seeds to the derived authority PR-A/PR-B established, via the CENTRAL lever (conftest.py make_file :416 drops its `state=` param + the 8 sibling factories seed derived markers) — incidental seeds collapse to kwarg-drops, derived-status seeds get explicit markers (AnalysisResult / cloud_job / DedupResolution / FileMetadata). NEVER by deleting coverage. `git grep -l FileState tests` == 0."
    - "D-08: ONE mutation-tested source guard (tests/shared/test_no_filestate_guard.py) forbids EXECUTABLE `FileState`, `FileRecord.state`, `files.state`, and multi-line `.values(state=...)` from reappearing in src/phaze — using tokenize to strip BOTH comments AND string/docstring literals (NOT `#`-only), scoped attribute regexes (NOT bare `.state`, to dodge FastAPI app.state), a `.values(**splat)` soft-warn with an honest limitation note, a planted-match self-test with negative lookalikes, and a DOCUMENTED manual RED->GREEN mutation run recorded in the SUMMARY (a GREEN guard proves nothing — memory feedback_mutation_test_guard_tests)."
    - "test_039_autogenerate_diff_is_empty_for_dropped_objects flips GREEN once the model column/index are removed (models <-> migration 039 consistent). BOTH coverage gates stay green: per-module 90% (scripts/coverage_floor.py) AND combined 95% (`just coverage-combine`)."
  artifacts:
    - path: "src/phaze/models/file.py"
      provides: "FileState class + state column + ix_files_state index removed"
    - path: "tests/shared/test_no_filestate_guard.py"
      provides: "Mutation-tested tokenize-based source guard (D-08)"
      contains: "FileState"
  key_links:
    - from: "tests/shared/test_no_filestate_guard.py"
      to: "src/phaze/**"
      via: "tokenize-stripped source scan forbidding executable FileState/FileRecord.state/files.state/.values(state="
      pattern: "FileState"
    - from: "src/phaze/models/file.py"
      to: "alembic/versions/039_drop_files_state_column.py"
      via: "model column/index removal makes alembic autogenerate diff empty"
      pattern: "ix_files_state"
---

<objective>
90-04 completes PR-C (MIG-04): the model/enum retirement + dead-subsystem cleanup that was SPLIT out of
90-03 at a decision checkpoint when its real blast radius (91 test files referencing `FileState`, plus a
shadow_compare service/CLI/divergence-test subsystem) proved far larger than 90-03's budget.

**Migration 039 (the irreversible drop of `ix_files_state` + `files.state`) ALREADY LANDED in 90-03 — do
NOT touch `alembic/versions/039_drop_files_state_column.py`.** 90-04 is a code/test-only structural
deletion on top of that shipped migration: it removes the now-dead `FileState` enum, `state` column, and
`ix_files_state` index from the ORM; retires the now-void `shadow_compare` subsystem; migrates the ~91
dependent test files to the derived sources PR-A/PR-B established; adds the D-08 mutation-tested anti-drift
guard; and flips the one expected-RED autogenerate test (`test_039_autogenerate_diff_is_empty_for_dropped_objects`)
GREEN.

Depends on 90-01 (readers derived), 90-02 (writers removed), 90-03 (migration 039 present). The single
judgement call — full removal of shadow_compare — resolves to **FULL REMOVAL, no relocation**: every
app-layer coupling is prose, the dependency runs shadow_compare -> stage_status (not the reverse), and the
hard invariants are frozen inline in 039's guard. All claims here are grounded in 90-04-RESEARCH.md
(grep/read-verified at phase HEAD, 2026-07-12) — re-grep before editing, line numbers drift on any change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-destructive-migration-writer-removal/90-CONTEXT.md
@.planning/phases/90-destructive-migration-writer-removal/90-04-RESEARCH.md
@.planning/phases/90-destructive-migration-writer-removal/90-PATTERNS.md
@.planning/phases/90-destructive-migration-writer-removal/90-01-SUMMARY.md
@.planning/phases/90-destructive-migration-writer-removal/90-02-SUMMARY.md
@.planning/phases/90-destructive-migration-writer-removal/90-03-SUMMARY.md

<facts>
<!-- Verified at phase HEAD, 2026-07-12 (90-04-RESEARCH.md). Re-grep before editing. -->
- Migration 039 is LANDED (`alembic/versions/039_drop_files_state_column.py`) — do NOT touch it. Its guard
  transcribed the shadow_compare HARD invariants INLINE (D-07; 039:52 "NO phaze.* imports", 039:96-113 the
  13 hard invariants). This is WHY full shadow_compare removal loses no safety.
- Blast radius (measured): `git grep -l FileState tests | wc -l` = 91; `git grep -l FileState src/phaze` = 22
  (mostly dead docstring prose after PR-A/PR-B).
- shadow_compare dependency direction is INVERTED from the original fear: `shadow_compare.py:54` imports
  `done_clause`/`failed_clause` FROM `stage_status.py`; NOTHING at runtime in the live app imports
  shadow_compare (pipeline.py:1353/1378, backends.py:97, stage_status.py:12/101 are prose only). Only its own
  `cli/shadow_compare.py:32` imports the service. The CLI is a standalone `python -m` module (:138), NOT
  registered in `cli/__init__.py` and has NO pyproject script entry — deletion needs no de-registration.
- Model removal surface (`src/phaze/models/file.py`): `class FileState(enum.StrEnum)` lines 20-71;
  `state: Mapped[str] = mapped_column(String(30), ..., default=FileState.DISCOVERED)` line 86;
  `Index("ix_files_state","state")` line 97 (first entry of `__table_args__`); `import enum` line 5 (drop
  only if now unused — ruff F401 flags it). `models/__init__.py`: line 9 import + line 36 `__all__`.
- The ONLY other EXECUTABLE FileState residuals in src after PR-A/PR-B: `services/pipeline.py:21`
  (`from phaze.models.file import FileRecord, FileState`) and `PIPELINE_STAGES = [...]` (:81-90). The list has
  NO runtime consumer — its only reader is `tests/shared/services/test_pipeline.py:155-157`
  (`assert FileState.ANALYSIS_FAILED not in PIPELINE_STAGES`). Delete the list + import; migrate/remove that
  one test. Everything else (`config.py:619`, `models/analysis.py`, `models/cloud_job.py`, `backends.py`,
  many routers/tasks/templates) is dead docstring/comment prose.
- The central test lever: `tests/conftest.py:416 make_file` passes `state=` into `FileRecord(state=state)`
  (:436). Its 8 sibling factories hardcode `state=FileState.X` internally (verified: :461,497,532,552,614,
  679,712,722). Fixing this one file collapses the incidental majority. NOTE: `files.state` has
  `default=FileState.DISCOVERED`, so dropping make_file's `state=` param is GREEN while the column still
  exists (the default fills it) — this is why the test migration (Task 2) can go green BEFORE the model
  deletion (Task 3).
- The one expected-RED test: `tests/integration/test_migrations/test_migration_039_drop_files_state_column.py
  ::test_039_autogenerate_diff_is_empty_for_dropped_objects` — RED until the model column/index are removed
  (Task 3), then GREEN automatically.
- Coverage: TWO gates. Per-module 90% (`scripts/coverage_floor.py:33 FLOOR=90.0`) AND combined 95%
  (`just coverage-combine` -> `coverage report --fail-under=95` + `coverage_floor.py`). Retiring the
  shadow/divergence tests removes SOURCE + tests together (net-neutral) BUT retiring divergence tests without
  removing the dedup/pending code they exercise can drop those modules below the per-module floor — migrated
  PR-A tests must preserve it; run `just coverage-combine` before declaring done.
- Test-DB footgun (memory reference_migrations_test_db_port): `just test-bucket` does NOT export the DB URLs.
  Export both at :5433 with the +asyncpg scheme before any DB test:
    export TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_test"
    export MIGRATIONS_TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_migrations_test"
  Bare `postgresql://` fails with `ModuleNotFoundError: psycopg2`. `just test-db` provisions the containers.
- Anti-drift analog: `tests/shared/test_no_raw_state_render.py` (parents[2]/src/phaze root, vacuous-glob
  assert :81, planted-match self-test :87-101, comment-strip :62-64). The FileState guard MUST additionally
  strip STRING/docstring tokens (use `tokenize`, NOT `#`-only) and cover the multi-line `.values(\n state=)`
  and `.values(**splat)` forms (memory feedback_mutation_test_guard_tests — Phase 83 shipped two toothless
  guards; mutate EVERY syntactic form).
- uv-only: prefix everything `uv run`; never `--no-verify`; 150-char lines; pre-commit hooks (ruff+mypy)
  must pass on every commit. Commit frequently (per-task atomic commits; bucket-by-bucket intermediate
  commits within Task 2 are fine).
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire the now-dead shadow_compare subsystem (full removal, no relocation)</name>
  <files>src/phaze/services/shadow_compare.py, src/phaze/cli/shadow_compare.py</files>
  <read_first>
    - src/phaze/services/shadow_compare.py + src/phaze/cli/shadow_compare.py (the subsystem being deleted)
    - Enumerate every shadow-compare test: `git grep -l "shadow_compare" tests`
    - Runtime importers (the retire/migrate set): `git grep -ln "from phaze\.\(services\|cli\)\.shadow_compare import\|import phaze.cli.shadow_compare" tests`
    - tests/integration/test_drain_double_dispatch.py (module docstring :1-10 — the ROADMAP double-dispatch hard gate)
    - tests/integration/test_dedup_resolve_undo_shadow.py + 90-01-SUMMARY Task 4 (the id-only /resolve->/undo regressions — the possible twin)
  </read_first>
  <action>
    Delete `src/phaze/services/shadow_compare.py` and `src/phaze/cli/shadow_compare.py` WHOLESALE — no
    symbol relocation. This is safe per 90-04-RESEARCH.md Q1: the couplings in `routers/pipeline.py:1353,1378`,
    `services/backends.py:97`, and `services/stage_status.py:12,101` are 100% comment/docstring prose (leave
    them — they are harmless and the Task-4 guard strips them); the dependency runs shadow_compare ->
    stage_status (shadow_compare.py:54 imports FROM stage_status), so removing the downstream consumer cannot
    break PR-A's derived readers; and the 13 HARD invariants are already frozen inline in migration 039
    (039:96-113, D-07). The CLI is a standalone `python -m` module with NO cli/__init__ registration and NO
    pyproject script entry — no de-registration needed.

    Then handle the subsystem's test fallout by CLASSIFICATION (do NOT blanket-delete — migrate what carries
    state-independent coverage):
    - RETIRE (delete) the dedicated-subsystem tests: `test_shadow_compare.py`, `test_shadow_compare_skipped.py`,
      `tests/shared/test_shadow_compare_cli.py`, and `tests/shared/test_shadow_compare_readonly.py` (this last
      one path-reads the deleted module source, so it FileNotFoundErrors — it dies with the module). Confirm
      the retire set by re-running the runtime-importer + path-reader greps above.
    - RETIRE `test_dedup_divergence.py` and `test_pending_set_divergence.py`: their premise is "seed an
      inconsistent `state` vs markers and prove readers use markers" — once `state` is gone the inconsistency
      is UNSEEDABLE and marker-authority is structurally guaranteed. Import-safe: their "copied from
      test_shadow_compare" strings are docstring prose; each file owns its `db_session` fixture (no Python
      import couples them — verified). See coverage note below.
    - MIGRATE (do NOT delete) `test_drain_double_dispatch.py`: it is a ROADMAP-designated HARD GATE for the
      2026-06-18 ~44.5K over-enqueue class. Its core assertion — a file dispatched EXACTLY ONCE across two
      `stage_cloud_window` ticks, blocked on tick 2 by `~inflight_clause` over the committed `scheduling_ledger`
      row — is STATE-INDEPENDENT. Strip only the removed `LOCAL_ANALYZING` state seeds/assertions (a PR-B
      writer); KEEP the ledger exactly-once coverage. Delete outright ONLY if inspection proves every assertion
      is state-bound (it is not).
    - MIGRATE-OR-RETIRE `test_dedup_resolve_undo_shadow.py`: most tests assert BOTH `run_shadow_compare(...)
      .hard_fail_total == 0` (subsystem gone) AND `dup.state == FileState.DISCOVERED` (column gone) — both
      invalid. FIRST diff against PR-A's id-only /resolve->/undo round-trip regressions (90-01 Task 4,
      commit c6f0a040): IF those already cover this file's marker resolve/undo/noop/malformed-uuid behavior,
      RETIRE it without coverage loss; ELSE MIGRATE the marker-based coverage (strip the shadow + state
      assertions, assert the DedupResolution marker). Record the diff verdict in the SUMMARY.

    Coverage note: retiring the divergence tests removes tests but NOT the dedup/pending source they exercise —
    ensure the surviving PR-A tests keep `services/dedup.py` / `services/pipeline.py` above the 90% per-module
    floor (verified by `just coverage-combine` in Task 3). Commit atomically (ruff+mypy hooks on).
  </action>
  <verify>
    <automated>test ! -f src/phaze/services/shadow_compare.py && test ! -f src/phaze/cli/shadow_compare.py && test -z "$(git grep -l 'from phaze\.\(services\|cli\)\.shadow_compare import\|import phaze.cli.shadow_compare' tests)" && uv run python -c "import phaze; import phaze.cli" && uv run ruff check . && echo "shadow_compare retired, cli imports clean, ruff green"</automated>
  </verify>
  <acceptance_criteria>
    - `services/shadow_compare.py` and `cli/shadow_compare.py` are deleted; `git grep -ln "from phaze.*shadow_compare import\|import phaze.cli.shadow_compare" src/phaze tests` returns NOTHING (no runtime importer survives).
    - `uv run python -c "import phaze; import phaze.cli"` succeeds (no dangling import); `uv run ruff check .` clean.
    - Retired ONLY the dedicated-subsystem + unseedable-divergence tests; `test_drain_double_dispatch.py` still EXISTS and its ledger exactly-once test passes; `test_dedup_resolve_undo_shadow` diff-verdict (migrate vs retire) is recorded in the SUMMARY with no net coverage loss.
    - The prose refs in pipeline.py/backends.py/stage_status.py are left intact (harmless; Task-4 guard strips them).
  </acceptance_criteria>
  <done>The dead shadow_compare service + CLI + their dedicated tests are removed with no relocation and no dangling import/registration; test_drain_double_dispatch migrated (state-independent core kept); test_dedup_resolve_undo_shadow migrate-or-retire decided by diff; ruff green.</done>
</task>

<task type="auto">
  <name>Task 2: Central make_file lever + migrate the ~91 FileState test files to derived seeds</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py:416-443 (make_file) + the 8 sibling factories at :461,497,532,552,614,679,712,722
    - 90-01-SUMMARY (derived authority) + 90-02-SUMMARY `patterns` block (the exact state->derived mapping:
      cloud_job status for AWAITING_CLOUD/PUSHING; analysis_completed_at/failed_at for ANALYZED/ANALYSIS_FAILED;
      metadata/DedupResolution marker for METADATA_EXTRACTED/DUPLICATE_RESOLVED)
    - the ~91 files: `git grep -l FileState tests`
  </read_first>
  <action>
    Fix the CENTRAL lever FIRST, then sweep the fallout — this collapses the incidental majority to mechanical
    kwarg-drops (90-04-RESEARCH.md Q2).

    In `tests/conftest.py`: drop the `state: str = FileState.DISCOVERED` param from `make_file._make` and the
    `state=state` line (:436) — `FileRecord`'s `default=FileState.DISCOVERED` still fills the column (which is
    NOT dropped until Task 3), so this is GREEN immediately. Convert the 8 sibling factories that hardcode
    `state=FileState.X` (:461 PROPOSAL_GENERATED, :497/:552 MOVED, :532/:679 EXECUTED, :614 ANALYZED,
    :712/:722 AWAITING_CLOUD) to seed the DERIVED marker instead (RenameProposal / AnalysisResult.analysis_completed_at
    / cloud_job / etc.) in that one file. RECOMMENDED: add convenience flags to make_file (`analyzed=True`,
    `analysis_failed=True`, `awaiting_cloud=True`) that seed the marker in one place, so derived-status call
    sites read `make_file(analyzed=True)` instead of hand-rolling rows — centralizes the derived-seed logic and
    keeps the sweep diff small.

    Then sweep the ~91 files BUCKET-BY-BUCKET (export the :5433 DB URLs — see facts). Two classes:
    - INCIDENTAL seeds (majority): `state=` passed only to satisfy the old NOT-NULL column, never asserted on
      -> pure kwarg deletion + drop the now-unused `FileState` import. Mechanical.
    - DERIVED-STATUS seeds (minority): seeded `state=FileState.X` and expect the file in an X-derived reader
      -> seed the marker (AnalysisResult / cloud_job / DedupResolution / FileMetadata) and assert the DERIVED
      result, EXACTLY the pattern PR-A/PR-B used. Do NOT delete assertions.
    Include `tests/shared/services/test_pipeline.py:155-157` (the `FileState.ANALYSIS_FAILED not in
    PIPELINE_STAGES` assertion) — remove/migrate its FileState ref now; the `PIPELINE_STAGES` list it names is
    deleted in Task 3. Run each bucket until green (intermediate commits fine). Transient red mid-sweep is
    expected and acceptable within this one plan (research Pitfall 6).

    Do NOT delete coverage to make a test pass — migrate to the derived source. `git grep -l FileState tests`
    must reach 0 by the end of this task.
  </action>
  <verify>
    <automated>export TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_test"; export MIGRATIONS_TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_migrations_test"; test -z "$(git grep -l FileState tests)" && uv run pytest tests/shared -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -l FileState tests | wc -l` == 0 (every test reference migrated or removed — NONE deleted for coverage).
    - `make_file` no longer accepts/sets `state=`; the 8 sibling factories seed derived markers; the recommended convenience flags (if added) seed the marker centrally.
    - With the :5433 DB URLs exported, each bucket runs green (`uv run pytest tests/<bucket> -q`); derived-status tests assert the DERIVED result (marker / cloud_job), not a `state` value.
  </acceptance_criteria>
  <done>make_file + its 8 sibling factories drive derived seeds; all ~91 test files migrated off FileState/`state=` to the derived authority; no FileState reference remains in tests; buckets green on the :5433 DB.</done>
</task>

<task type="auto">
  <name>Task 3: Delete FileState enum + state column + ix_files_state + PIPELINE_STAGES; flip the 039 autogen test GREEN</name>
  <files>src/phaze/models/file.py, src/phaze/models/__init__.py, src/phaze/services/pipeline.py, src/phaze/config.py</files>
  <read_first>
    - src/phaze/models/file.py (FileState class :20-71; state column :86; Index("ix_files_state") :97; import enum :5)
    - src/phaze/models/__init__.py (:9 import, :36 __all__)
    - src/phaze/services/pipeline.py:21 (FileState import) + :81-90 (PIPELINE_STAGES list)
    - src/phaze/config.py:619 (comment-only ref — optional tidy)
  </read_first>
  <action>
    Delete from `models/file.py`: the entire `class FileState(enum.StrEnum)` (:20-71), the `state` mapped_column
    (:86, including its `default=FileState.DISCOVERED`), and the `Index("ix_files_state","state")` entry in
    `__table_args__` (:97 — pairs 1:1 with 039's `op.drop_index('ix_files_state')`). Remove `import enum` (:5)
    IF ruff F401 now flags it as unused. In `models/__init__.py` drop `FileState` from the `from
    phaze.models.file import ...` line (:9) and from `__all__` (:36).

    In `services/pipeline.py`: remove the `FileState` import (:21 -> `from phaze.models.file import FileRecord`)
    and DELETE the `PIPELINE_STAGES = [...]` list (:81-90). It has NO runtime consumer (the src refs at :100,
    :1299, :1324 are comments; the only reader was the test assertion already migrated in Task 2). Optionally
    tidy the comment-only `config.py:619` ref (NOT required — the Task-4 guard strips comments).

    Then remove any FURTHER executable FileState reference the compiler flags: run `uv run mypy . && uv run
    ruff check .` and clean iteratively until BOTH are green (the `files_modified` list is a FLOOR — touch
    whatever mypy/ruff flag; after PR-A/PR-B the only executable residuals are the four sites above, everything
    else is dead docstring prose that mypy/ruff correctly ignore). Do NOT chase docstring/comment mentions —
    that is what the guard's string-stripping is for; a raw `grep -v '#'` is the WRONG acceptance check here
    (it does not filter triple-quoted docstrings and never reaches 0).

    Removing the model column/index makes `alembic` autogenerate report an empty diff for `files.state` /
    `ix_files_state`, so `test_039_autogenerate_diff_is_empty_for_dropped_objects` flips GREEN. Confirm it, run
    the full suite bucket-by-bucket (export the :5433 URLs), and run `just coverage-combine` to enforce BOTH
    coverage gates. Commit atomically.
  </action>
  <verify>
    <automated>export TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_test"; export MIGRATIONS_TEST_DATABASE_URL="postgresql+asyncpg://phaze:phaze@localhost:5433/phaze_migrations_test"; uv run python -c "import phaze" && uv run mypy . && uv run ruff check . && uv run pytest tests/integration/test_migrations/test_migration_039_drop_files_state_column.py::test_039_autogenerate_diff_is_empty_for_dropped_objects -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class FileState" src/phaze/models/file.py` == 0; the `state` mapped_column and the `ix_files_state` Index are gone; `PIPELINE_STAGES` is gone from `services/pipeline.py`.
    - `uv run python -c "import phaze"` succeeds; `uv run mypy .` and `uv run ruff check .` are BOTH clean (the REAL "no executable FileState" gate — NOT a `grep -v '#'` no-op).
    - `test_039_autogenerate_diff_is_empty_for_dropped_objects` passes GREEN (models <-> migration 039 consistent).
    - `just coverage-combine` passes BOTH gates: per-module 90% (`scripts/coverage_floor.py`) AND combined 95% (`coverage report --fail-under=95`).
  </acceptance_criteria>
  <done>FileState class + state column + ix_files_state index + re-export + pipeline.py import/PIPELINE_STAGES deleted; mypy+ruff+import green; the 039 autogen test GREEN; both coverage gates green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Mutation-tested D-08 anti-drift guard (tokenize-based, no buckets.json edit)</name>
  <files>tests/shared/test_no_filestate_guard.py</files>
  <read_first>
    - tests/shared/test_no_raw_state_render.py (parents[2]/src/phaze root; vacuous-glob assert :81; planted-match self-test :87-101; comment-strip :62-64)
    - memory feedback_mutation_test_guard_tests (mutate EVERY syntactic form; line-grep is blind to multi-line `.values`)
  </read_first>
  <behavior>
    - Scans `src/phaze/**/*.py` (root via `Path(__file__).resolve().parents[2] / "src" / "phaze"`); vacuous-glob assert: `assert py_files, "guard scanned no src/phaze files"`.
    - Strips BOTH COMMENT and STRING tokens via `tokenize` (NOT `#`-only) before scanning — ~20 src files carry FileState in docstrings; a `#`-only strip would self-fail the guard.
    - FLAGS (executable, post-strip): bare token `FileState`; `FileRecord.state`; `\bfiles\.state\b`; the multi-line `.values(\n  state=...)` form (whole-file DOTALL / token-stream scan, NOT line-by-line).
    - SOFT-WARN only on `.values(**` (splat) with a documented limitation note — a dict `{"state": ...}` splatted in cannot be statically resolved; the real backstop is structural (the `files.state` column is gone, so such a write fails at SQL compile/execute).
    - Planted-match self-test asserts the regex set MATCHES each forbidden form (incl. a multi-line `.values(\n    state="x")` literal and a `.values(**payload)` string) AND does NOT match lookalikes: `stage_status`, `cloud_job.status`, `app.state.redis`, `request.app.state`.
  </behavior>
  <action>
    Create `tests/shared/test_no_filestate_guard.py` modeled on `test_no_raw_state_render.py` but with the
    tokenize-based strip (90-04-RESEARCH.md Q4). Resolve the `src/phaze` root, assert the scanned glob is
    NON-EMPTY, tokenize each file and drop COMMENT + STRING tokens, then `test_no_filestate_in_src` asserts
    ZERO matches for the forbidden forms across the reconstructed executable-token stream. Scope the attribute
    regexes to `FileRecord.state` / `\bfiles\.state\b` / the bare `FileState` token — do NOT use a bare
    `\w+\.state` regex (it false-positives on FastAPI `app.state` / `request.app.state`). Add
    `test_guard_flags_planted_matches` with the positive forms AND the negative lookalikes from `<behavior>`.
    Emit a soft WARNING (not a failure) on `.values(**` with an inline comment documenting the static
    limitation and the structural backstop.

    Place the file at `tests/shared/` — it lands in the existing "shared" bucket automatically. Do NOT edit
    `tests/buckets.json` (a "bucket" is a top-level `tests/` subdir NAME; `just test-bucket shared` runs
    `pytest tests/shared` — no per-file registration exists; the analog guards live in `tests/shared/` with no
    registration). The buckets.json edit in the superseded draft was a no-op.

    MANUAL mutation check (record verbatim in the SUMMARY per feedback_mutation_test_guard_tests — a GREEN
    guard proves nothing): temporarily reintroduce a REAL `FileState` import OR a `.values(state=...)` into a
    src file, run `uv run pytest tests/shared/test_no_filestate_guard.py -q`, CONFIRM RED, RESTORE, confirm
    GREEN. Back the mutation up to a scratch path before restoring (90-01 lost uncommitted work to a
    `git restore` during its mutation check — do not `git restore` a file carrying uncommitted work).
  </action>
  <verify>
    <automated>uv run pytest tests/shared/test_no_filestate_guard.py -q && uv run ruff check tests/shared/test_no_filestate_guard.py && uv run mypy tests/shared/test_no_filestate_guard.py</automated>
  </verify>
  <acceptance_criteria>
    - `test_no_filestate_guard.py` passes; its planted-match self-test proves the regex catches `FileState` / `FileRecord.state` / `files.state` / multi-line `.values(state=)` AND does NOT match `app.state` / `cloud_job.status` / `stage_status` / `request.app.state`.
    - The guard uses `tokenize` to strip comments AND string/docstring tokens (verified by the ~20 docstring FileState mentions NOT tripping it); the `.values(**splat)` limitation is documented inline.
    - A DOCUMENTED manual mutation run went RED then GREEN-on-restore, recorded in the SUMMARY.
    - `just test-bucket shared` includes the guard (no buckets.json edit); ruff + mypy clean on the new file.
  </acceptance_criteria>
  <done>A tokenize-based, mutation-verified anti-drift guard forbids any executable FileState/FileRecord.state/files.state/.values(state= reappearance in src, with an honest .values(**splat) note; auto-registered in the shared bucket; documented RED->GREEN mutation run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model schema <-> shipped migration 039 | The ORM (`models/file.py`) must converge to the already-dropped DB shape; drift is caught by the autogenerate-diff sentinel test. |
| Retired tests <-> covered source | Deleting a test that is the sole exerciser of live source silently drops coverage; the two coverage gates are the boundary control. |
| Future edits <-> deleted `FileState` | A later change could reintroduce a `files.state` read/write; the D-08 guard + mypy/ruff are the drift boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01 | Tampering (data-loss-via-dropped-coverage) | retired shadow/divergence tests + the ~91 migrated tests | mitigate | MIGRATE (never delete) any test carrying state-independent or derived coverage: `test_drain_double_dispatch` (ledger exactly-once) kept; `test_dedup_resolve_undo_shadow` diff-then-decide; enforce BOTH `just coverage-combine` gates (per-module 90% + combined 95%) in Task 3 before done. |
| T-90-02 | Tampering / Repudiation (anti-drift guard toothlessness) | tests/shared/test_no_filestate_guard.py | mitigate | Tokenize-strip comments AND strings so the guard is not self-satisfied by docstring prose; planted-match self-test with negative lookalikes proves specificity; DOCUMENTED manual RED->GREEN mutation run (a GREEN guard proves nothing — memory feedback_mutation_test_guard_tests); cover the multi-line `.values(state=)` form; honest `.values(**splat)` limitation note. |
| T-90-03 | Information disclosure / Denial of service | migration 039 (LANDED) | accept | Out of scope — 039 already shipped in 90-03; this plan is code/test-only and MUST NOT touch it. |
| T-90-04 | Tampering (model<->DB drift) | models/file.py vs 039 | mitigate | `test_039_autogenerate_diff_is_empty_for_dropped_objects` flips GREEN only when the model column/index removal exactly matches 039's drops — the standing drift sentinel. |
| T-90-SC | Tampering (supply chain) | package installs | N/A | No new dependencies; zero package-manager installs (RESEARCH: "No new packages"). No legitimacy audit required. |
</threat_model>

<verification>
- `uv run python -c "import phaze; import phaze.cli"` succeeds; `uv run mypy . && uv run ruff check .` clean — no EXECUTABLE FileState reference in src/phaze (docstring prose is harmless).
- `git grep -l FileState tests | wc -l` == 0; `git grep -ln "from phaze.*shadow_compare import\|import phaze.cli.shadow_compare" src/phaze tests` returns nothing (subsystem fully removed, no dangling import/CLI registration).
- `services/shadow_compare.py` + `cli/shadow_compare.py` deleted; `test_drain_double_dispatch.py` still present and its ledger exactly-once test passes; `test_dedup_resolve_undo_shadow` migrate/retire verdict recorded in SUMMARY.
- With the :5433 DB URLs exported, the full suite is green bucket-by-bucket, including the now-GREEN `test_039_autogenerate_diff_is_empty_for_dropped_objects`.
- `just coverage-combine` passes BOTH gates: per-module 90% (`scripts/coverage_floor.py`) AND combined 95% (`coverage report --fail-under=95`).
- `tests/shared/test_no_filestate_guard.py` green incl. planted-match self-test + negative lookalikes; tokenize strips comments AND strings; documented manual mutation run RED->GREEN in the SUMMARY; no `tests/buckets.json` edit.
</verification>

<success_criteria>
FileState enum + `state` column + `ix_files_state` index + `models/__init__` re-export + `pipeline.py` import/`PIPELINE_STAGES` deleted; the dead shadow_compare subsystem fully removed (no relocation) without losing frozen-invariant safety or coverage; the ~91 dependent test files migrated to the derived authority via the central make_file lever (no coverage deleted); a tokenize-based mutation-tested D-08 guard forbids any reappearance; `test_039_autogenerate_diff_is_empty_for_dropped_objects` is GREEN; both coverage gates green. Phase 90 (MIG-04) is then complete and ready for verification.
</success_criteria>

<output>
Create `.planning/phases/90-destructive-migration-writer-removal/90-04-SUMMARY.md` when done. Record: the
shadow_compare full-removal verdict, the `test_dedup_resolve_undo_shadow` migrate-vs-retire diff decision,
and the VERBATIM manual RED->GREEN mutation run for the D-08 guard.
</output>
