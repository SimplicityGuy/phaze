---
phase: 48-compute-agent-type
plan: 03
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - src/phaze/templates/admin/partials/_kind_badge.html
  - src/phaze/templates/admin/partials/agents_table.html
  - tests/test_routers/test_admin_agents.py
  - tests/test_task_split.py
autonomous: true
requirements: [CLOUDAGENT-02, CLOUDAGENT-03]
must_haves:
  truths:
    - "The Agents admin page renders a COMPUTE kind badge for kind='compute' rows and a FILE SERVER badge for kind='fileserver' rows"
    - "The kind badge appears on BOTH the full-page load (GET /admin/agents) and the 5s HTMX poll partial (GET /admin/agents/_table)"
    - "Each kind badge carries an aria-label ('Kind: compute' / 'Kind: file server') so the attribute is not color-only"
    - "The compute agent's worker module (phaze.tasks.agent_worker) imports no ORM/app-DB modules; the Postgres broker is present"
  artifacts:
    - path: "src/phaze/templates/admin/partials/_kind_badge.html"
      provides: "kind badge partial (if/elif compute/else neutral fallback), LOCKED pill geometry"
      contains: "COMPUTE"
    - path: "src/phaze/templates/admin/partials/agents_table.html"
      provides: "Kind column (th + td include) between Agent and Status"
      contains: "_kind_badge.html"
    - path: "tests/test_routers/test_admin_agents.py"
      provides: "badge render assertions for both kinds, both render paths"
    - path: "tests/test_task_split.py"
      provides: "reaffirmed compute-agent ORM import boundary"
  key_links:
    - from: "src/phaze/templates/admin/partials/agents_table.html"
      to: "_kind_badge.html"
      via: "{% include %} between Agent cell and Status cell"
      pattern: "include \"admin/partials/_kind_badge.html\""
    - from: "src/phaze/templates/admin/partials/_kind_badge.html"
      to: "agent.kind"
      via: "if/elif on the loop var agent.kind"
      pattern: "agent.kind"
---

<objective>
Make available cloud capacity visible at a glance (CLOUDAGENT-03) and reaffirm the compute agent's ORM/app-DB isolation (CLOUDAGENT-02): add a `_kind_badge.html` partial + a "Kind" column to the Agents admin table, and extend the existing subprocess import-boundary test to explicitly cover the compute-agent invariant.

Purpose: The operator needs to count cloud capacity by running their eye down one column ("how many COMPUTE agents are up?"). The badge rides on the `Agent.kind` column from Plan 01 — `_load_agents` already does a bare `SELECT Agent`, so NO router/query change is needed. CLOUDAGENT-02's job-pull/result-PUT path needs ZERO new code (the compute agent runs the same `agent_worker`, same `phaze-agent-<id>` queue, same `PUT /api/internal/agent/analysis/{file_id}`) — its security guarantee (no ORM/app-DB access) is already enforced by `tests/test_task_split.py`, which this plan reaffirms rather than reinvents.
Output: kind badge UI honoring the approved 48-UI-SPEC, plus a reaffirmed import-boundary test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-compute-agent-type/48-RESEARCH.md
@.planning/phases/48-compute-agent-type/48-PATTERNS.md
@.planning/phases/48-compute-agent-type/48-VALIDATION.md
@.planning/phases/48-compute-agent-type/48-UI-SPEC.md
@.planning/phases/48-compute-agent-type/48-01-SUMMARY.md

<interfaces>
<!-- Extracted from UI-SPEC / PATTERNS.md / codebase. The UI-SPEC is APPROVED and LOCKED. -->

Badge geometry (LOCKED, copy verbatim from _status_pill.html): `text-xs font-semibold px-2 py-0.5 rounded-full`
(do NOT change py-0.5 to py-1 — UI-SPEC §Spacing locks it).

Variants (UI-SPEC §Component Contract — palette + labels LOCKED):
  agent.kind == "compute"   -> label "COMPUTE",     classes "bg-indigo-100 dark:bg-indigo-950 text-indigo-700 dark:text-indigo-400", aria-label="Kind: compute"
  agent.kind == "fileserver" / else fallback -> label "FILE SERVER", classes "bg-slate-100 dark:bg-slate-800 text-slate-700 dark:text-slate-300", aria-label="Kind: file server"
  Structure: if / elif compute / else neutral fallback (cell never blank), mirroring _status_pill.html's defensive if/elif.
  Loop var is `agent` (set by {% for agent in agents %} in agents_table.html:45).

agents_table.html placement (UI-SPEC §Placement — Kind AFTER Agent, BEFORE Status):
  Header row (~:35-42): insert <th scope="col" class="px-4 py-3">Kind</th> between the Agent <th> (~:36) and Status <th> (~:37).
  Body (~:47-51): insert <td class="px-4 py-3">{% include "admin/partials/_kind_badge.html" %}</td> after the Agent <td> (~:50), before the Status <td> include (~:51).
  Both admin_agents.page and the /_table partial render this same agents_table.html (one edit covers both paths — RESEARCH Pitfall 5).

src/phaze/routers/admin_agents.py — NO CHANGE (_load_agents :67 does select(Agent); kind rides free).

Test analogs:
  tests/test_routers/test_admin_agents.py: smoke fixture session.add_all([...Agent(id=..., name=..., scan_roots=...)...]) ~:50-74 (add kind= when seeding); test_status_pills_render_all_5_states ~:150-171 (mirror for badge render across full page + partial).
  tests/test_task_split.py: test_agent_worker_does_not_import_phaze_database ~:33-91 — subprocess imports phaze.tasks.agent_worker, asserts forbidden=("phaze.database","phaze.tasks.session","sqlalchemy.ext.asyncio") absent from sys.modules AND saq.queue.postgres present.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Kind badge partial + Kind column in the agents table</name>
  <files>src/phaze/templates/admin/partials/_kind_badge.html, src/phaze/templates/admin/partials/agents_table.html, tests/test_routers/test_admin_agents.py</files>
  <read_first>
    - .planning/phases/48-compute-agent-type/48-UI-SPEC.md (APPROVED contract — §Component Contract, §Placement, §Spacing, §Color, §Accessibility)
    - src/phaze/templates/admin/partials/_status_pill.html (full — geometry + defensive if/elif + aria-label to mirror)
    - src/phaze/templates/admin/partials/agents_table.html (full — header row ~:35-42, Agent cell + status-pill include ~:47-51, {% for agent in agents %} ~:45)
    - tests/test_routers/test_admin_agents.py (full — smoke fixture seed ~:50-74, test_status_pills_render_all_5_states ~:150-171 to mirror)
    - src/phaze/routers/admin_agents.py (confirm _load_agents :67 select(Agent) — NO change; kind rides free)
  </read_first>
  <behavior>
    - test_kind_badge_compute_renders: GET /admin/agents (full page) for a seeded kind='compute' agent contains "COMPUTE", "bg-indigo-100 dark:bg-indigo-950", and aria-label="Kind: compute"
    - test_kind_badge_fileserver_renders: same page for a kind='fileserver' agent contains "FILE SERVER", "bg-slate-100", and aria-label="Kind: file server"
    - test_kind_badge_in_poll_partial: GET /admin/agents/_table (HTMX poll partial) contains the same COMPUTE + FILE SERVER badges (no first-load-vs-poll flicker)
    - test_kind_column_header_present: the rendered table contains a "Kind" <th> positioned after "Agent" and before "Status"
  </behavior>
  <action>
    Write the router-render tests above FIRST (RED — seed the smoke fixture with one kind='compute' and one kind='fileserver' Agent, add kind= to the existing Agent(...) seeds). Then create src/phaze/templates/admin/partials/_kind_badge.html as an if/elif on agent.kind: the compute branch and the fileserver branch per the LOCKED variants above, with `{% else %}` falling through to the fileserver (neutral) branch so an out-of-enum value never blanks the cell. Copy the geometry literal `text-xs font-semibold px-2 py-0.5 rounded-full` verbatim from _status_pill.html. Text is UPPERCASE ("COMPUTE" / "FILE SERVER"). No icon (design system is text-only). Then edit agents_table.html: add `<th scope="col" class="px-4 py-3">Kind</th>` between the Agent and Status headers, and `<td class="px-4 py-3">{% include "admin/partials/_kind_badge.html" %}</td>` after the Agent cell and before the Status cell — one edit site covers both the full-page and poll render paths. Do NOT touch admin_agents.py, the status pill, the queue-depth cell, the empty state, the error banner, or the poll cadence (all LOCKED per UI-SPEC §Scope).
  </action>
  <verify>
    <automated>uv run pytest tests/test_routers/test_admin_agents.py -k "kind" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/phaze/templates/admin/partials/_kind_badge.html exists, branches on agent.kind, contains the strings "COMPUTE" and "FILE SERVER", the classes "bg-indigo-100 dark:bg-indigo-950" and "bg-slate-100", and aria-label values "Kind: compute" / "Kind: file server", with the LOCKED geometry `text-xs font-semibold px-2 py-0.5 rounded-full`
    - agents_table.html includes "admin/partials/_kind_badge.html" and a "Kind" <th> between the Agent and Status headers
    - src/phaze/routers/admin_agents.py is unchanged
    - `uv run pytest tests/test_routers/test_admin_agents.py -k kind -x` exits 0 covering both kinds in both the full-page and /_table render paths
  </acceptance_criteria>
  <done>The Agents admin page shows a COMPUTE/FILE SERVER kind badge in a dedicated Kind column on both render paths, honoring the approved UI-SPEC geometry, palette, labels, and aria-labels.</done>
</task>

<task type="auto">
  <name>Task 2: Reaffirm the compute-agent ORM import boundary</name>
  <files>tests/test_task_split.py</files>
  <read_first>
    - tests/test_task_split.py (full — test_agent_worker_does_not_import_phaze_database ~:33-91: subprocess import of phaze.tasks.agent_worker, forbidden=("phaze.database","phaze.tasks.session","sqlalchemy.ext.asyncio"), asserts broker saq.queue.postgres present)
    - .planning/phases/48-compute-agent-type/48-RESEARCH.md (§Pitfall 4 — the "no media" half is runtime-enforced via empty scan roots + no mount, NOT import-enforced; do NOT attempt to ban essentia/file reads)
  </read_first>
  <action>
    The existing subprocess test ALREADY runs the exact module a compute agent runs (phaze.tasks.agent_worker) and asserts the ORM/async-engine modules stay out of sys.modules while the Postgres broker is present — this is CLOUDAGENT-02's import boundary. Reaffirm it: update the test's docstring/comment to explicitly name the compute-agent invariant (a compute agent reaches ONLY the SAQ Postgres broker + cache Redis + HTTP API — no app ORM tables, no media filesystem). Do NOT duplicate the test into a second file and do NOT add an assertion banning essentia or file reads (the worker legitimately reads the scratch audio path it is handed; the "no media" guarantee is runtime — empty scan roots + no mount, a Phase 51 compose concern). If a single concise extra assertion buys clarity (e.g. asserting media-walking scan modules are not eagerly imported), add it only if it does not over-constrain; otherwise the reaffirmation is comment-level. The test must still pass unchanged in behavior.
  </action>
  <verify>
    <automated>uv run pytest tests/test_task_split.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_task_split.py docstring/comment explicitly names the compute-agent ORM/media isolation invariant (CLOUDAGENT-02)
    - the forbidden set still includes "phaze.database", "phaze.tasks.session", "sqlalchemy.ext.asyncio" and the test still asserts saq.queue.postgres present
    - no second/duplicate import-boundary test file was created; no assertion bans essentia or file reads
    - `uv run pytest tests/test_task_split.py -x` exits 0
  </acceptance_criteria>
  <done>The import-boundary test explicitly covers the compute-agent invariant and passes; no duplication, no over-constraint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compute agent worker → control plane | The compute agent reaches ONLY the SAQ Postgres broker + cache Redis + HTTP API; it must NOT reach app ORM tables or the media filesystem (CLOUDAGENT-02/03). |
| `agents` row → admin template render | The page renders attacker-influenceable column data (`kind`) into HTML; must fail safe on unexpected values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Elevation of Privilege | `phaze.tasks.agent_worker` (the module the compute agent runs) | mitigate | `tests/test_task_split.py` subprocess-asserts `phaze.database`, `phaze.tasks.session`, `sqlalchemy.ext.asyncio` never enter `sys.modules` while `saq.queue.postgres` (broker) is present — proves the compute agent cannot reach the app ORM/DB. Reaffirmed this plan. (Media isolation is runtime: empty scan roots + no mount, Phase 51.) |
| T-48-02 | Tampering | `_kind_badge.html` render | mitigate | Defensive `if/elif compute/else` falls back to the neutral fileserver branch for any out-of-enum value, so a tampered `kind` cannot blank the cell or inject an unhandled state; Jinja2 autoescaping handles the static label/class literals (no user text is interpolated into the badge). DB CHECK (Plan 01) already prevents bad values reaching the row. |
| T-48-SC | Tampering | dependency install | accept | No packages installed — Jinja2/HTMX/Tailwind already shipped, self-hosted audited Tailwind build (quick task 260606-qgu). No supply-chain surface. |
</threat_model>

<verification>
- `uv run pytest tests/test_routers/test_admin_agents.py -k kind tests/test_task_split.py -x -q` green
- `uv run pytest --cov --cov-report=term-missing` ≥ 85% after this wave
- UI-SPEC compliance: badge geometry/palette/labels/aria-labels match the LOCKED contract; status pill, queue depth, empty/error states, and poll cadence untouched
- Manual (deferred, per 48-VALIDATION §Manual-Only): a live compute agent appears on the Agents admin page with kind badge + green liveness + queue depth
</verification>

<success_criteria>
- The Agents admin page distinguishes compute vs file-server agents via a kind badge alongside the existing liveness pill + queue depth, on both render paths
- The kind attribute is screen-reader announced and not color-only
- The compute-agent ORM import boundary is reaffirmed and passing
- No regression to the LOCKED admin-page elements; router unchanged
</success_criteria>

<output>
Create `.planning/phases/48-compute-agent-type/48-03-SUMMARY.md` when done
</output>
