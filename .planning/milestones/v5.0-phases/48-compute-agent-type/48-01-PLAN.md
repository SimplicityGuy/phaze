---
phase: 48-compute-agent-type
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/phaze/models/agent.py
  - alembic/versions/024_add_agents_kind.py
  - tests/test_models/test_agent.py
  - tests/test_migrations/test_024.py
autonomous: true
requirements: [CLOUDAGENT-01]
must_haves:
  truths:
    - "Agent.kind defaults to 'fileserver' for every existing/new row"
    - "The DB rejects any kind value outside {'fileserver','compute'}"
    - "Migration 024 chains cleanly off 023 (single alembic head) and round-trips up/down"
  artifacts:
    - path: "src/phaze/models/agent.py"
      provides: "kind String(16) NOT NULL DEFAULT 'fileserver' column + ck_agents_kind_enum CHECK"
      contains: "kind"
    - path: "alembic/versions/024_add_agents_kind.py"
      provides: "additive kind column DDL + CHECK, backfill via server_default"
      contains: "revision"
    - path: "tests/test_models/test_agent.py"
      provides: "kind column default + ck_agents_kind_enum constraint assertions"
    - path: "tests/test_migrations/test_024.py"
      provides: "024 up/down round-trip + backfill + CHECK-reject assertions"
  key_links:
    - from: "src/phaze/models/agent.py"
      to: "agents.kind column + ck_agents_kind_enum"
      via: "mapped_column + CheckConstraint in __table_args__"
      pattern: "kind IN \\('fileserver', 'compute'\\)"
    - from: "alembic/versions/024_add_agents_kind.py"
      to: "023 (down_revision)"
      via: "revision chain"
      pattern: "down_revision.*023"
---

<objective>
Add the `kind` capability marker to the `Agent` model and ship Alembic migration 024 that adds the column to the live `agents` table.

Purpose: `kind` is durable agent identity — the marker that distinguishes a media-less compute (cloud) agent from a file-server agent. It MUST live on the `agents` row (not config alone) because the Agents admin page reads from the table (CLOUDAGENT-03) and the CLI insert sets it (CLOUDAGENT-01). This plan is the schema foundation every downstream plan builds on.
Output: `Agent.kind` Mapped column with a CHECK constraint, migration 024, and tests proving default/backfill/CHECK behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-compute-agent-type/48-RESEARCH.md
@.planning/phases/48-compute-agent-type/48-PATTERNS.md
@.planning/phases/48-compute-agent-type/48-VALIDATION.md

<interfaces>
<!-- Extracted from PATTERNS.md / codebase. Use directly — no exploration needed. -->

Existing column + constraint pattern in src/phaze/models/agent.py (~:25-38):
  scan_roots: Mapped[list[str]] = mapped_column(JSONB, nullable=False, server_default=text("'[]'::jsonb"))
  __table_args__ = ( CheckConstraint("id ~ '^[a-z0-9]+(-[a-z0-9]+)*$'", name="id_charset"), )
  String and text are ALREADY imported at agent.py:7 — no new imports.

SQLAlchemy auto-prefixes constraint names: declared name="id_charset" surfaces in the DB and in
Agent.__table__.constraints as "ck_agents_id_charset" (verified tests/test_models/test_agent.py:42).
So name="kind_enum" -> "ck_agents_kind_enum".

Current alembic head is 023 (alembic/versions/023_add_scheduling_ledger_job_policy.py:
revision="023", down_revision="022"). Migration 023 is the structural analog (additive op.add_column).

Migration test analog: tests/test_migrations/test_023.py — loads the digit-prefixed module via
importlib.util.spec_from_file_location; asserts bare-number revision identifiers; runs an async
downgrade/upgrade round-trip using conftest helpers MIGRATIONS_TEST_DATABASE_URL, _build_alembic_config,
downgrade_to, upgrade_to; queries information_schema.columns for the target table.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add kind column + CHECK constraint to the Agent model</name>
  <files>src/phaze/models/agent.py, tests/test_models/test_agent.py</files>
  <read_first>
    - src/phaze/models/agent.py (full — see existing scan_roots server_default column and the id_charset CheckConstraint in __table_args__; String + text already imported at :7)
    - tests/test_models/test_agent.py (full — see the required-columns set assertion ~:16-28 and test_id_charset_constraint_declared ~:40-42 to mirror)
  </read_first>
  <behavior>
    - test_kind_defaults_fileserver: Agent.__table__.c.kind.server_default renders 'fileserver' (column declared with server_default=text("'fileserver'"))
    - test_kind_column_not_null_string16: Agent.__table__.c.kind.nullable is False and the type is String(16)
    - test_kind_charset_constraint_declared: "ck_agents_kind_enum" is in {c.name for c in Agent.__table__.constraints}
    - extend the existing required-columns set assertion to include "kind"
  </behavior>
  <action>
    Write the test assertions above FIRST (RED), then add to src/phaze/models/agent.py a column `kind: Mapped[str] = mapped_column(String(16), nullable=False, server_default=text("'fileserver'"))` placed alongside the other scalar columns, and append `CheckConstraint("kind IN ('fileserver', 'compute')", name="kind_enum")` to the existing `__table_args__` tuple (do NOT remove the id_charset constraint). Reuse the already-imported String and text — add no new imports. Default 'fileserver' so every existing row (incl. LEGACY_AGENT_ID) reads correctly with no data backfill step. Note the auto-prefix: declared name="kind_enum" becomes "ck_agents_kind_enum" in __table__.constraints — assert the prefixed name.
  </action>
  <verify>
    <automated>uv run pytest tests/test_models/test_agent.py -k kind -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/phaze/models/agent.py contains a `kind` mapped_column with String(16), nullable=False, server_default=text("'fileserver'")
    - src/phaze/models/agent.py __table_args__ contains CheckConstraint with name="kind_enum" and expression "kind IN ('fileserver', 'compute')"
    - `uv run pytest tests/test_models/test_agent.py -k kind -x` exits 0
    - test asserts "ck_agents_kind_enum" in Agent.__table__.constraints names
  </acceptance_criteria>
  <done>Agent model has the kind column + CHECK; model tests for default, type/nullable, and the ck_agents_kind_enum constraint pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Alembic migration 024 — additive kind column + CHECK + backfill</name>
  <files>alembic/versions/024_add_agents_kind.py, tests/test_migrations/test_024.py</files>
  <read_first>
    - alembic/versions/023_add_scheduling_ledger_job_policy.py (full — copy the module structure: imports, revision identifiers block, upgrade/downgrade)
    - tests/test_migrations/test_023.py (full — copy the importlib module loader, the bare-number revision-identifier test, and the async up/down round-trip + information_schema query shape)
    - tests/test_migrations/conftest.py (helpers MIGRATIONS_TEST_DATABASE_URL, _build_alembic_config, downgrade_to, upgrade_to, _reset_schema)
  </read_first>
  <behavior>
    - test_revision_identifiers_are_bare_numbers: revision == "024", down_revision == "023", branch_labels is None
    - round-trip (async): downgrade_to(base) -> upgrade_to("023") -> assert no `kind` column on agents -> upgrade_to("024") -> assert kind present, is_nullable == "NO", data_type == "character varying", column_default carries 'fileserver'
    - backfill: a row inserted before/at 023 reads kind == 'fileserver' after upgrade to 024 (server_default backfill)
    - CHECK reject: inserting an agents row with kind='bogus' raises (constraint ck_agents_kind_enum)
    - downgrade_to("023") drops the kind column (and its constraint) cleanly
  </behavior>
  <action>
    Write tests/test_migrations/test_024.py FIRST (RED — migration module does not exist yet), mirroring test_023.py exactly (digit-prefixed module loaded via importlib.util.spec_from_file_location, conftest round-trip helpers, information_schema.columns query against table_name='agents'). Then create alembic/versions/024_add_agents_kind.py with revision="024", down_revision="023", branch_labels=None, depends_on=None. upgrade(): op.add_column("agents", sa.Column("kind", sa.String(16), nullable=False, server_default="fileserver")) then op.create_check_constraint("kind_enum", "agents", "kind IN ('fileserver', 'compute')"). downgrade(): op.drop_constraint("ck_agents_kind_enum", "agents", type_="check") then op.drop_column("agents", "kind") (drop dependents first). The server_default backfills existing rows automatically — no separate UPDATE. Confirm a single head: `uv run alembic heads` must show only 024 after the file lands.
  </action>
  <verify>
    <automated>uv run pytest tests/test_migrations/test_024.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - alembic/versions/024_add_agents_kind.py exists with revision="024" and down_revision="023"
    - `uv run alembic heads` reports a single head (024) with no "Multiple head revisions" error
    - `uv run pytest tests/test_migrations/test_024.py -x` exits 0
    - test proves upgrade adds NOT NULL kind defaulting 'fileserver', backfills a pre-existing row, CHECK rejects 'bogus', and downgrade drops the column
  </acceptance_criteria>
  <done>Migration 024 adds the kind column with CHECK + backfill, chains off 023 as a single head, and the up/down round-trip + backfill + CHECK-reject test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application code → Postgres (`agents` table) | The kind value is written by the CLI (Plan 02) and read by the admin page (Plan 03); the DB is the authoritative store of agent identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-02 | Tampering | `agents.kind` column | mitigate | DB-layer enforcement: `CheckConstraint("kind IN ('fileserver','compute')", name="kind_enum")` (→ `ck_agents_kind_enum`) rejects any out-of-enum value at the database — the innermost of the 3-layer defense (CLI choices + config Literal land in Plan 02). Migration test asserts `kind='bogus'` is rejected. |
| T-48-SC | Tampering | dependency install | accept | This plan installs NO packages — only SQLAlchemy/Alembic already pinned in pyproject.toml/uv.lock. No supply-chain surface; slopcheck not invoked. |
</threat_model>

<verification>
- `uv run pytest tests/test_models/test_agent.py tests/test_migrations/test_024.py -x -q` green
- `uv run alembic heads` shows a single head (024)
- `uv run ruff check src/phaze/models/agent.py alembic/versions/024_add_agents_kind.py` and `uv run mypy src/phaze/models/agent.py` clean
</verification>

<success_criteria>
- `Agent.kind` exists as `String(16)` NOT NULL with server_default 'fileserver' and a `ck_agents_kind_enum` CHECK
- Migration 024 chains off 023 (single head), backfills existing rows to 'fileserver', and rejects out-of-enum values
- All new/extended model + migration tests pass
</success_criteria>

<output>
Create `.planning/phases/48-compute-agent-type/48-01-SUMMARY.md` when done
</output>
