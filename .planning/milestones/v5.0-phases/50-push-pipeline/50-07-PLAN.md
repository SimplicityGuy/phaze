---
phase: 50-push-pipeline
plan: 07
type: execute
wave: 4
depends_on: ["50-06"]
files_modified:
  - src/phaze/services/pipeline.py
  - src/phaze/routers/pipeline.py
  - src/phaze/templates/pipeline/partials/staged_pushing_card.html
  - src/phaze/templates/pipeline/partials/analyzing_cloud_card.html
  - src/phaze/templates/pipeline/dashboard.html
  - src/phaze/templates/pipeline/partials/stats_bar.html
  - tests/test_pipeline_dag_context.py
autonomous: true
requirements: [CLOUDPIPE-01]

must_haves:
  truths:
    - "The dashboard shows a 'Staged (pushing)' card (PUSHING count) and an 'Analyzing (cloud)' card (PUSHED count) next to the existing 'Awaiting cloud' card (D-09)"
    - "Both counts come from degrade-safe _safe_count helpers (never 500 the 5s poll)"
    - "Both cards re-push via the existing OOB stats-poll path (same id on initial render and OOB swap)"
  artifacts:
    - path: "src/phaze/services/pipeline.py"
      provides: "get_pushing_count + get_pushed_count (_safe_count)"
      contains: "get_pushing_count"
    - path: "src/phaze/templates/pipeline/partials/staged_pushing_card.html"
      provides: "Staged (pushing) count card"
      contains: "pushing_count"
    - path: "src/phaze/templates/pipeline/partials/analyzing_cloud_card.html"
      provides: "Analyzing (cloud) count card"
      contains: "analyzing_cloud_count"
  key_links:
    - from: "src/phaze/routers/pipeline.py"
      to: "get_pushing_count / get_pushed_count"
      via: "dashboard context + stats-poll context"
      pattern: "pushing_count"
---

<objective>
Surface the bounded cloud window to the operator: add the two D-09 dashboard count cards — "Staged (pushing)" (PUSHING) and "Analyzing (cloud)" (PUSHED) — reusing the Phase-49 `_safe_count` + count-card + OOB-poll pattern verbatim.

Purpose: CLOUDPIPE-01 observability — make the "one ahead" window visible at a glance, alongside the existing "Awaiting cloud" card. Backend+template reuse, not a design task; click-through to per-file lists is deferred (D-09).
Output: two count helpers, two card partials, dashboard + stats-bar wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-push-pipeline/50-PATTERNS.md
@src/phaze/services/pipeline.py
@src/phaze/routers/pipeline.py
@src/phaze/templates/pipeline/partials/awaiting_cloud_card.html

<interfaces>
<!-- From 50-01: FileState.PUSHING / FileState.PUSHED. From 50-06: services/pipeline.py already has the window-count helper; this plan adds the two per-card counts. -->
get_pushing_count(session) -> int: _safe_count over select(func.count(FileRecord.id)).where(state==PUSHING), node="pushing"  (clone get_awaiting_cloud_count pipeline.py:805-816)
get_pushed_count(session) -> int: same with state==PUSHED, node="analyzing_cloud"
router surfacing (pipeline.py): initial-load read L477-502 + stats-poll re-push L533-553 — add pushing_count + analyzing_cloud_count to BOTH context dicts.
templates: clone awaiting_cloud_card.html (id pattern + {% if oob %}hx-swap-oob="true"{% endif %}); dashboard.html include at :25; stats_bar.html OOB include at :78 ({% with oob = True %}...{% endwith %}). Same id on both renders = the OOB contract.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PUSHING/PUSHED count helpers + router surfacing</name>
  <files>src/phaze/services/pipeline.py, src/phaze/routers/pipeline.py, tests/test_pipeline_dag_context.py</files>
  <read_first>
    - src/phaze/services/pipeline.py (get_awaiting_cloud_count L805-816 — exact clone target; _safe_count degrade-to-0 + rollback L272-289)
    - src/phaze/routers/pipeline.py (awaiting_cloud_count initial-load surfacing L477-502; stats-poll re-push L533-553)
    - .planning/phases/50-push-pipeline/50-PATTERNS.md (services/pipeline.py + routers/pipeline.py card sections)
    - tests/test_pipeline_dag_context.py (existing context-dict assertion style)
  </read_first>
  <behavior>
    - get_pushing_count returns the count of FileRecord rows in state PUSHING; get_pushed_count returns the PUSHED count.
    - On a DB error each returns 0 (degrade-safe, rolls back) — the 5s poll never 500s.
    - Both the dashboard-load context and the stats-poll context expose pushing_count and analyzing_cloud_count.
  </behavior>
  <action>
    In `services/pipeline.py` add `get_pushing_count` (clone get_awaiting_cloud_count, `state == FileState.PUSHING`, `node="pushing"`) and `get_pushed_count` (`state == FileState.PUSHED`, `node="analyzing_cloud"`), both through `_safe_count`. In `routers/pipeline.py` add `pushing_count` and `analyzing_cloud_count` to BOTH the dashboard initial-load context dict and the stats-poll context dict (mirror the awaiting_cloud_count surfacing). Extend tests/test_pipeline_dag_context.py to assert both keys are present in both contexts and degrade to 0 on error.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pipeline_dag_context.py -q; uv run mypy src/phaze/services/pipeline.py src/phaze/routers/pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove pushing_count + analyzing_cloud_count appear in both the dashboard and stats-poll contexts and degrade to 0 on a DB error.
    - `grep -q 'get_pushing_count' src/phaze/services/pipeline.py` and `grep -q 'get_pushed_count' src/phaze/services/pipeline.py`.
    - `uv run mypy` passes on both files.
  </acceptance_criteria>
  <done>Both window counts are computed degrade-safely and exposed to the dashboard and the 5s poll.</done>
</task>

<task type="auto">
  <name>Task 2: Add the two count-card partials + dashboard/stats-bar wiring</name>
  <files>src/phaze/templates/pipeline/partials/staged_pushing_card.html, src/phaze/templates/pipeline/partials/analyzing_cloud_card.html, src/phaze/templates/pipeline/dashboard.html, src/phaze/templates/pipeline/partials/stats_bar.html</files>
  <read_first>
    - src/phaze/templates/pipeline/partials/awaiting_cloud_card.html (whole file — the section id + {% if oob %}hx-swap-oob="true"{% endif %} pattern at L17-27)
    - src/phaze/templates/pipeline/dashboard.html (the awaiting_cloud include ~L25)
    - src/phaze/templates/pipeline/partials/stats_bar.html (the OOB awaiting_cloud include ~L78, `{% with oob = True %}...{% endwith %}`)
    - .planning/phases/50-push-pipeline/50-PATTERNS.md (templates section — clone + id contract)
  </read_first>
  <action>
    Clone `awaiting_cloud_card.html` into `staged_pushing_card.html` (id `#staged-pushing-card`, label "Staged (pushing)", renders `{{ pushing_count }}`) and `analyzing_cloud_card.html` (id `#analyzing-cloud-card`, label "Analyzing (cloud)", renders `{{ analyzing_cloud_count }}`), each keeping the `{% if oob %}hx-swap-oob="true"{% endif %}` structure. Include both in `dashboard.html` beside the awaiting_cloud include, and add both OOB includes in `stats_bar.html` (`{% with oob = True %}{% include ... %}{% endwith %}`). The id must be identical on the initial render and the OOB swap (the OOB contract).
  </action>
  <verify>
    <automated>uv run pytest tests/test_dag_canvas_render.py tests/test_pipeline_dag_context.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Both partials exist and render `{{ pushing_count }}` / `{{ analyzing_cloud_count }}` inside a section whose id matches between dashboard.html and stats_bar.html.
    - `grep -q 'staged_pushing_card' src/phaze/templates/pipeline/dashboard.html` and `grep -q 'analyzing_cloud_card' src/phaze/templates/pipeline/dashboard.html`; both also referenced in stats_bar.html.
    - Render/context tests stay green.
  </acceptance_criteria>
  <done>The dashboard shows the Staged and Analyzing-cloud cards on load and refreshes them via the existing OOB poll.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| hot 5s /pipeline/stats poll → DB | a count query failure must not 500 the dashboard. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-poll-500 | Denial of Service | count query on the 5s poll | mitigate | counts go through `_safe_count` (rolls back the aborted transaction, returns 0) — the poll never 500s. |
| T-50-stale-count | Information Disclosure (minor) | a transiently stale window count card | accept | counts are observational only; the load-bearing window is enforced by the cron from committed FileState, not by the card. |
| T-50-SC | Tampering | npm/pip/cargo installs | accept | No new packages; supply-chain gate N/A. |
</threat_model>

<verification>
`uv run pytest tests/test_pipeline_dag_context.py tests/test_dag_canvas_render.py -q` green; `uv run ruff check .`, `uv run mypy .` pass. Visual confirmation of the two cards is the Phase-49-style manual check noted in 50-VALIDATION.md (Manual-Only).
</verification>

<success_criteria>
The operator can see the "one ahead" window — PUSHING (staged) and PUSHED (analyzing) counts — on the pipeline dashboard, refreshed by the existing poll, with degrade-safe counts.
</success_criteria>

<output>
Create `.planning/phases/50-push-pipeline/50-07-SUMMARY.md` when done.
</output>
