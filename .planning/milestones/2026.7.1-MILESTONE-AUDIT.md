---
milestone: 2026.7.1
name: Multi-Cloud Backends
audited: 2026-07-05T05:00:00Z
status: passed
scores:
  requirements: 21/21
  phases: 5/5
  integration: 5/5
  flows: 5/5
  nyquist: 5/5
  security: 5/5
gaps: {}   # no critical blockers
tech_debt:
  - phase: 67-backend-registry-config-model
    items:
      - "docker-compose.yml lines 25/52 still pass PHAZE_CLOUD_TARGET as an env var with stale 'live control-plane knob' comments — silently dropped by extra=ignore (functionally inert), but misleading to an operator. Recommend deleting the two lines (quick fix)."
  - phase: 68-backend-protocol-3-implementations
    items:
      - "BACK-01 protocol reduction accessors (resolved_non_local_kind / active_compute_scratch_dir) fail-fast on >1 compute backend LAZILY (at first invocation), not at boot-time _validate_registry. Deliberate documented descope (multi-compute-agent routing = PROV-01 backlog); fails loud with id-tagged message, never misroutes. (Integration W1)"
  - phase: 71-deployment-config-docs-n-lane-ui
    items:
      - "No permanent regression test for the pipeline.py force-local gate at the two duration-router callers + backfill trigger (pipeline.py:396/718/793). Live-verified during this audit (real app+DB) but assertions not committed. Recommend adding tests/shared/routers/test_pipeline.py coverage. (Integration W2, affects BEUI-02)"
  - phase: 70-multi-kueue-n-clusters
    items:
      - "70-UAT test 7 (live 2nd real Kueue cluster + dual live S3 bucket endpoints) deployment-gated — needs real infra unavailable in-session. Carried as a rollout-verification item (v6.0 JOB-ENV-CONTRACT precedent). Tests 1-6 pass 6/6, 0 issues."
deferred_verification:
  - "Live E2E against a real 2nd Kueue cluster + dual S3 buckets (70-UAT #7) — verify at rollout."
---

# Milestone 2026.7.1 — Multi-Cloud Backends — Audit

**Status: ✅ PASSED** — 21/21 requirements satisfied, 5/5 phases verified, 5/5 cross-phase flows wired, 0 critical blockers.

## Scope

Phases 67–71. Converts the single-cloud-target burst pipeline into a pluggable, cost-tiered
registry of backends (local + N Kueue clusters + N cloud-compute agents) running
simultaneously, statically configured (no provisioning), with a bucket registry
(public/shared vs cluster-specific) that reverses the design's one-shared-bucket §6/§7.

| Phase | Name | Verification | Nyquist | Security |
|-------|------|--------------|---------|----------|
| 67 | Backend Registry & Config Model | ✅ passed | ✅ compliant | ✅ verified |
| 68 | Backend Protocol + 3 Implementations | ✅ passed | ✅ compliant | ✅ verified |
| 69 | Tiered Drain Scheduler | ✅ passed (repaired from gaps_found) | ✅ compliant | ✅ verified |
| 70 | Multi-Kueue (N Clusters) | ✅ passed | ✅ compliant | ✅ verified |
| 71 | Deployment, Config, Docs & N-Lane UI | ✅ passed (HUMAN-UAT complete) | ✅ compliant | ✅ verified |

## Requirements Coverage — 21/21 satisfied (3-source cross-reference)

Every REQ-ID is `[x]`/Complete in the REQUIREMENTS.md traceability table, marked ✓ SATISFIED
in its phase VERIFICATION.md requirements table, and integration-touched by the cross-phase
checker. SUMMARY frontmatter lists a per-plan subset; VERIFICATION tables are authoritative.

| Group | REQs | Phase | Status |
|-------|------|-------|--------|
| REG   | 01–05 | 67 | ✅ satisfied (REG-05 S3 bucket registry added post-design) |
| BACK  | 01–04 | 68 | ✅ satisfied (BACK-02 = documented no-backfill reinterpretation, D-06) |
| SCHED | 01–05 | 69 | ✅ satisfied (SCHED-05 WR-03 caveat tracked; addressed in Phase 70) |
| MKUE  | 01–04 | 70 | ✅ satisfied |
| BEUI  | 01–03 | 71 | ✅ satisfied |

No unsatisfied requirements. No orphaned requirements (all 21 map 1:1 to a phase and appear in
that phase's VERIFICATION table).

## Cross-Phase Integration — 5/5 flows WIRED

Integration checker (live-verified against a throwaway Postgres 16 + real FastAPI app; 162
mapped tests green):

1. **Config → dispatch (67→68→69→70)** — WIRED. `resolve_backends()` feeds the `Backend`
   protocol impls consumed by the tiered `select_backend`/drain, extended to N Kueue clusters +
   per-file buckets. Zero dangling references to removed 67 fields (`cloud_target`,
   `active_cloud_kind`, `active_bucket`, `active_kube`, `_staging_config`) in live code.
2. **Migration chain 029→030→031** — WIRED. Single linear chain, `alembic heads` → `031` (single head).
3. **Staging read-back** — WIRED. Dispatch stamps `backend_id`+`staging_bucket`; every later
   reader (presign/delete/reconcile/callbacks) reads the RECORDED value, never re-derives.
4. **Force-local gate (71)** — WIRED (live-verified). All 4 enqueue sites gate on
   `get_route_control` (drain + 2 duration-router callers + backfill); with force_local a long
   file routed local (not held AWAITING_CLOUD) and backfill returned the disabled fragment with
   zero mutation.
5. **UI lane snapshot (71)** — WIRED. `get_backend_lane_snapshot` reads the SAME
   `resolve_backends()` registry; both context builders seed `lanes` identically; OOB swap over `#analyze-lanes`.

## Tech Debt (non-critical, deferred — see frontmatter)

- **67**: stale `PHAZE_CLOUD_TARGET` comments in docker-compose.yml (inert, misleading) — quick cleanup.
- **68 / W1**: lazy (not boot-time) fail-fast on >1 compute backend — deliberate PROV-01-backlog descope, fails loud.
- **71 / W2**: no committed regression test for the pipeline.py force-local gate — live-verified only; add a test.
- **70**: 70-UAT test 7 (live 2nd cluster + dual buckets) deployment-gated — verify at rollout.

None block the milestone. All are documented and/or already backlogged.

## Verdict

**PASSED.** All 21 requirements satisfied and integration-wired; all phases verified, Nyquist-compliant,
and security-verified. Proceed to `/gsd:complete-milestone 2026.7.1`. Carry the tech-debt items above
into STATE.md Deferred Items at close.
