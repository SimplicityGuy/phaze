---
milestone: 2026.7.5
milestone_name: Parallel Enrich DAG (Retire Linear FileState)
audited: 2026-07-13
status: passed
scores:
  requirements: 45/45
  phases: 16/16
  integration: 5/5 seams verified
  flows: 1/1 E2E flow complete
gaps: {}  # No critical blockers, 0 unsatisfied, 0 orphaned
resolved_this_milestone:  # Debt the prior (pre-P92) audit listed as open, now closed by Phase 92
  - "PERF-02 over-budget / DENORM-01: P92 CLEAN-01 parallelized get_stage_progress via asyncio.gather (the prior audit's exact recommended follow-up) — 200K poll 1469->861ms direct / 1738->1072ms endpoint, under budget. DENORM-01 obviated (v2-deferred, no longer needed)."
  - "83-01 / 83-03 non-hermetic bucket flakes: P92 CLEAN-02 fixed the shared conftest root (session-scoped engine + create_savepoint + idempotent test-fileserver seeding). All 9 buckets green under per-bucket isolation."
  - "backends.py duplicated KueueBackend.reconcile comment + agent_files.py stale DISCOVERED-stamp comment: P92 CLEAN-03 (D-09/D-10)."
deferred_operational_gate:  # Deployment-gated; non-blocking for closure, blocking for prod deploy
  - id: "MIG-02 / SC-3 live-corpus shadow-compare"
    phases: [79, 84]
    note: "Committed re-runnable gate is built + verified in CI on synthetic corpus. The live 200K-corpus restore run is deferred by design (decision D-02) — no live dump exists in the worktree. Must run green on the DRAINED corpus before migration 039 deploys. Tracked in 79-HUMAN-UAT.md and 84-06-DEFERRED.md."
  - id: "MIG-04 / 039 real-corpus rehearsal"
    phase: 90
    note: "Restore prod snapshot -> apply 032->038 -> shadow-compare green (drained) -> run 039 -> assert files_state_archive populated + files.state/ix_files_state gone -> downgrade() restores verbatim -> record ACCESS EXCLUSIVE lock + DDL timing. Prod is at Alembic 031; 039 must never be prod's first migration. Tracked in 90-HUMAN-UAT.md."
  - id: "READ-05 live tag/CUE write UAT"
    phase: 85
    note: "On a real deploy, an actually-applied file surfaces in Tag-write/CUE review workspaces and write/generate produce real filesystem output (dead before this phase). Filesystem-mutating; verify against live corpus. Tracked in 85-HUMAN-UAT.md."
tech_debt: []  # ALL code-level tech debt paid down on the phase-92 branch 2026-07-14 (see debt_paydown_2026_07_14). Only the deployment_operational_gate remains (deploy-blocking, not closure-blocking).
debt_paydown_2026_07_14:  # Operator directed a full paydown on the phase-92 branch after the audit; 10 commits, full 9-bucket D-08 re-gate GREEN (3,409 tests, 0 failed)
  - "P92 review: WR-03 (verify fixture now depends on session), WR-04 (dead async_engine params removed), + perf seeder no longer writes the dropped files.state column (reaches HEAD in one alembic upgrade). RESOLVED (4057bbf0/7b2c95f7/5288edad)."
  - "P81: WR-01 fixed (metadata failure upsert guarded via `failed_at IS NOT NULL` conflict predicate so a DONE row is never clobbered) + regression test; WR-02 investigated and resolved as a TEST BLIND SPOT — the reenqueue call site already reconciles the in_flight∧failed cell via the D-10 gate; locked D-11 RESPECTED (NOT reopened); a twin-divergence test now covers the now-reachable cell and doubles as a D-11 mutation guard. RESOLVED (a14e8b50/9fe72f8e)."
  - "P85: WR-01..04 review-builder starvation fixed — keyset paging in the read path caps QUALIFYING rows, a migration-free NO_OP TagWriteLog terminal marker in the write path evicts zero-change files (tag_write_log.status is String(20), no migration needed), cue eligible half SQL-bounded, single-DISTINCT stat count. RESOLVED (995f80fc/1b3dc323)."
  - "P83-06: root cause was WORSE than documented (STRANDING, not mis-route) and the audit-note fix was insufficient. Operator approved OPTION A: D-09 REVERSED — backfill produces a clean drainable held file, stage_cloud_window is the single owner. RESOLVED (89f291b4/fa9df92d/032fc711). See STATE.md Decisions for the full D-09 reversal record."
nyquist:
  compliant_phases: [77, 78, 79, 80, 81, 83, 84, 85, 86, 87, 88, 89, 90, 92]
  partial_phases: [82]
  missing_phases: [91]
  overall: "14/16 compliant; P82 partial (deployment-gated D-02 live invariant + the PERF-02 budget it missed, since met by P92 — not a coverage gap); P91 is a hygiene phase with no formal REQ-IDs, verified via merged PRs #239/#241, no VALIDATION.md by design."
---

# Milestone 2026.7.5 — Parallel Enrich DAG — Audit Report (post-Phase-92)

**Verdict:** All 45 requirements satisfied, all 16 phases verified, cross-phase integration (5/5 seams) and E2E flow clean. No critical blockers, 0 unsatisfied, 0 orphaned. Status is **passed** — after the audit, the operator directed a full paydown of ALL code-level tech debt on the phase-92 branch (10 commits, full 9-bucket D-08 re-gate GREEN at 3,409 tests / 0 failed; see `debt_paydown_2026_07_14`), so no tech debt remains outstanding. The only remaining item is the deploy-time operational rehearsal (`deferred_operational_gate`), which blocks prod deploy, not milestone closure. **This milestone also paid down its own predecessors' debt** (PERF-02/DENORM-01, the 83-01/83-03 flakes, and — post-audit — P81/P83/P85 correctness warnings, the last consciously reversing locked D-09).

## Milestone goal

Make the enrich pipeline truly per-file parallel by deleting the linear `FileState` enum and deriving per-file, per-stage status (`not_started`/`in_flight`/`done`/`failed`) from the output tables that already exist. Executed as a live-corpus data-model migration, seam by seam: additive `032` → standing shadow-compare gate → readers-before-writers cutover → destructive `039` (drop `files.state`) → milestone-close hygiene (91) + tech-debt cleanup (92).

## Requirements coverage — 45/45 satisfied (3-source cross-reference)

Every REQ-ID cross-referenced across (1) REQUIREMENTS.md traceability table (all 45 `[x] Complete`), (2) each phase's VERIFICATION.md coverage table, and (3) SUMMARY.md `requirements_completed` frontmatter. 0 orphans.

| Group | REQs | Phase(s) | Status |
|-------|------|----------|--------|
| DERIV | 01–05 | 78 | ✓ satisfied |
| ELIG | 01–04 | 78 | ✓ satisfied |
| INFLIGHT | 01–03 | 78 | ✓ satisfied |
| FAIL | 01–04 | 81 | ✓ satisfied |
| READ | 01–05 | 82, 80, 84, 85 | ✓ satisfied |
| SIDECAR | 01–03 | 83, 84, 86 | ✓ satisfied |
| UI | 01–05 | 87 | ✓ satisfied |
| PERF | 01–02 | 77, 82 | ✓ satisfied (PERF-02 budget met by P92 CLEAN-01) |
| MIG | 01–04 | 77, 79, 90 | ✓ satisfied (MIG-02/04 live run = operational gate below) |
| LEGACY | 01–03 | 89 | ✓ satisfied |
| PRIO | 01 | 87 | ✓ satisfied |
| DRILL | 01–03 | 88 | ✓ satisfied |
| CLEAN | 01–03 | 92 | ✓ satisfied |

Explicitly deferred to v2: **DENORM-01** (denormalized stage-bitmap column) — **now obviated**: P92 CLEAN-01's `asyncio.gather` parallelization brought the poll under budget, so the denormalization it hedged is no longer needed. No dangling reference.

## Phase verifications — 16/16

77–90 and 92 each carry a VERIFICATION.md with `status: passed`. **P91** (milestone-close hygiene; HYG-01..04, no formal REQ-IDs) is SUMMARY-only in-tree by design — its work shipped and was reviewed via merged PRs #239/#241 with the verifier at 13/13; the missing in-tree VERIFICATION.md is a cherry-pick-delivery artifact, not an unverified phase.

## Cross-phase integration — 5/5 mandated seams WIRED, E2E complete

Milestone integration checker (2026-07-13) confirmed, running the guards live against a real Postgres:

1. **Single source of truth** — `enums/stage.py` + `services/stage_status.py` genuinely invoked (not bypassed) by reenqueue (80), pipeline pending-sets + `get_stage_progress` (82), dedup/fingerprint (84), cue/tags (85), proposals (86), UI eligibility-trace (87), drill-in (88). Zero executable `FileRecord.state` reads in `src/`.
2. **Post-90 FileState elimination** — model has no `state` column; migration `039` (single alembic head, chain 031→039 unbroken) drops it; migration test suite 14/14 live.
3. **Anti-drift guards wired + green live** — `test_no_filestate_guard`, `test_stage_status_equivalence`, `test_no_raw_state_render`, `test_conftest_hermeticity`.
4. **E2E per-file flow** — discover → 3 parallel enrich stages → dedup → propose (gated on metadata+analyze) → review → apply → tag/CUE; poll chain `/pipeline/stats → get_stage_progress (derived) → stats_bar/_analyze_lanes`; stage matrix, retry, eligibility trace, force-skip, drill-in, priority all traced to live routes.
5. **P92 fan-out transparent** — `get_stage_progress` preserves its exact output contract; all P82/87/88 consumers unchanged; the CLEAN-01↔CLEAN-02 landmine fixed and proven by `test_production_fanout_sees_in_test_seeded_row`.

**Integration WARNING (non-blocker, dev tooling):** `scripts/seed_perf_corpus.py` still writes a `files.state` column dropped by 039 — runnable only via the staged-migration workaround in `92-VERIFICATION.md`. No REQ-ID.

## Outstanding items

### Deployment-gated operational gate (non-blocking for closure, BLOCKING for prod deploy)
All pending HUMAN-UAT items converge on one deploy-time rehearsal: restore a prod snapshot → apply `032→038` → shadow-compare green on the **drained** corpus → run `039` → verify archive + column drop + `downgrade()` → then live tag/CUE write behavior surfaces. Prod is at Alembic **031**; the milestone's `032→039` are all unreleased. See `deferred_operational_gate` frontmatter (79/84/85/90-HUMAN-UAT).

### Tech debt — NONE outstanding (all paid down 2026-07-14)
The operator directed a full paydown on the phase-92 branch after this audit (10 commits, see `debt_paydown_2026_07_14`): P92 review WR-03/04 + perf seeder; P81 WR-01 (fixed) + WR-02 (resolved as test blind spot, D-11 respected); P85 WR-01..04 (keyset paging + NO_OP eviction marker); P83-06 (fixed via the operator-approved D-09 reversal — root cause was STRANDING, worse than the note documented). Full 9-bucket D-08 re-gate GREEN (3,409 tests, 0 failed). **Also resolved earlier by P92:** PERF-02/DENORM-01, 83-01/83-03 flakes, both cosmetic comments.

## Recommendation

The milestone met its definition of done in code, is integration-verified, and now carries ZERO outstanding tech debt (the post-audit paydown cleared it, full D-08 re-gate green). Proceed to **`/gsd:complete-milestone 2026.7.5`** (archive + tag) after opening the Phase-92 PR (off `main`). Schedule the **039 real-corpus rehearsal + drained-corpus shadow-compare + executed-gate live UAT** as the pre-deploy operator gate before rolling `032→039` to production — the only remaining item, and it blocks deploy, not closure.
